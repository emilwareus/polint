# Held-Out Delta Fixture

This fixture documents the adapted-report partition shape. Selection cases
are the examples used to create or edit model facts; held-out cases are evaluated
separately and reported with unknown, precision, recall, runtime, and cache deltas.

The executable guard lives in `eval::delta::held_out_delta_report_labels_selection_and_held_out_cases`.
