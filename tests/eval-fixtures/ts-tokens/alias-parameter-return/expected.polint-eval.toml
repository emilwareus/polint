schema_version = "polint-eval-fixture-1"
case_id = "ts-tokens-alias-parameter-return"
area = "facts"

# Phase 49 Plan 03 TS token fixture.
#
# The executable gate in `eval::ts_tokens` reads the private solver-derived rows.
# Semantic-graph producers emit TS token CopyEdges for direct aliases plus simple
# parameter callback, returned-function, and returned-closure source flows.
# Property/prototype/this cases remain outside the function-token driver boundary.

[repo]
path = "repo"

[budget]
max_runtime_ms = 120000
