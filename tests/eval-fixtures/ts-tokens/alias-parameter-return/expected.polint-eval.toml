schema_version = "polint-eval-fixture-1"
case_id = "ts-tokens-alias-parameter-return"
area = "facts"

# Phase 49 Plan 03 TS token fixture.
#
# The executable gate in `eval::ts_tokens` reads the private solver-derived rows.
# Current semantic-graph producers emit TS token CopyEdges for direct function aliases.
# Parameter, return, and closure cases are present as source coverage and unresolved
# handoff/boundary evidence; the Plan 02 fixpoint unit tests cover those flows once a
# frontend emits their CopyEdge inputs.

[repo]
path = "repo"

[budget]
max_runtime_ms = 120000
