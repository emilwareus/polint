# polint perf-10x overnight log — 2026-08-29

Machine: 8-core EPYC, 15GB RAM, 22GB free disk at start. Orchestrator: Hermes (z-ai/glm). Implementer: Claude Code (opus, effort max).

## Timeline (UTC)

- 01:00 Mission start. Verified worktrees, binary, gh auth (emilwareus). Created branch `perf/scan-10x` from 13e53f35 (v0.3.0, == origin/main).
- 01:01-01:13 Baseline quick-bench (v0.3.0 binary, today's machine; /tmp/bpol-* was empty so warm-1 runs paid rule-host rebuilds and are discarded):
  - plinty: warm 13.99s, cold 20.7s / 23.8s (baseline 2026-08-28: warm 15.7s, cold 25.1s)
  - devloupe: warm 10.83s, cold 12.4s / 12.3s (baseline: warm 11.2s, cold 13.1s)
  - All rc=0. Rule-host target dirs + cargo homes now prewarmed for future quick-benches.
- 01:13 Dispatched Claude Code research run (opus, effort max) -> thoughts/perf-10x/2026-08-29-RESEARCH.md
- 02:04 Research complete (761 lines, 12 hypotheses, measured A/B prototype: plinty warm 13.59->2.68s = 5.07x, byte-identical). Key cost centres: (1) per-fact lines().count() in CanonicalGoSyntaxOutput::from_db = 9.67s/69.8% plinty warm, (2) rule_execution_rows O(rules x files) glob clone = 1.6s both repos, (3) layer-hit double deserialize + re-serialize, (4) devloupe metrics manifest 5.73MB > 4MiB read limit = permanent miss, (5) 83-96% of warm wall single-threaded.
- 02:05-02:45 CRITICAL HARNESS WORK. Benchmark rule hosts link polint from crates.io (devloupe "0.3.0") / git tag v0.3.0 (plinty) — optimizations in the worktree would NEVER reach the benchmarks. Built a patch-injection harness:
  - `cargo vendor --locked` snapshots of both packs' dep trees -> /tmp/perf10x/vendor-{plinty,devloupe} (only locked versions => no resolution drift).
  - /workspace/bench-polint-results/bin/cargo-polint-local wrapper (enabled via POLINT_LOCAL_PATCH=1): replaces crates.io source with the vendor dir and [patch]-redirects `polint` to the perf worktree. POLINT_CARGO env makes the polint parent use it.
  - VALIDATED on both repos: patched host builds from worktree, deps from vendor dirs (no drift), output byte-identical to released v0.3.0 host, Cargo.lock rewritten by patched resolution -> restored via git checkout (tracked file; restore after every bench).
  - Mistake fixed: initially invoked plain `cargo` instead of the wrapper (no-op), then appended --config after `--` (cargo rejected); final form puts --config before user args. Also accidentally created a 555MB target dir inside benchmark-plinty/.polint/rules (removed; repos verified clean).
- Bench protocol from now on: export POLINT_CARGO=/workspace/bench-polint-results/bin/cargo-polint-local POLINT_LOCAL_PATCH=1 before quick-bench/run-bench; restore both packs' Cargo.lock after every bench invocation.
- 02:47 PLAN complete (683 lines): 3 waves (W1: H1,H2,H3,H11 — all key-changing work batched; W2: H4,H5,H10,H12; W3: H6,H9-lite,H7; H8 recommended cut), abort criteria, time budget to 09:30 freeze. Honest projection: plinty warm 7-9x (met), devloupe warm ~4x (near miss, 1.70s single-rule critical path in the pack), cold tiers ~2.5-3.8x (parse-bound; blockers named).
- 02:48-03:05 Baseline captures (released v0.3.0 binary, preserved at /tmp/perf10x/polint-v0.3.0-released):
  - inner JSON md5: plinty 8507f4fae91f6608c6dce4912be0926f, devloupe ca7193c06a6fbce1ec6447abcf197760 — IDENTICAL warm vs cold on the same binary. These md5s are the per-wave byte-identity gate (stronger + cheaper than per-cell captures).
  - outer walls today: plinty warm 14.30s / cold 24.78s; devloupe warm 11.53s / cold 12.63s (cold via throwaway POLINT_CACHE_DIR, repo caches untouched).
  - Ops note: outer runs must set CARGO_HOME + POLINT_RULES_TARGET_DIR like quick-bench does, or the child rebuilds into a default target dir (cost one 188s mistake; no repo pollution found afterwards).
- 03:05 Base-commit cargo test --workspace running in background -> /tmp/perf10x/tests-base.txt (pre-existing failure set for wave gates).
- 03:12 Base test set established (full cargo test --workspace, clean tree): 2370 passed / 3 failed — pre-existing failures = {capability_matrix_changed_files_review_only, capability_matrix_go_supported_views_are_non_empty, capability_matrix_typescript_supported_views_are_non_empty}. Wave gate: failing set must equal exactly this set. (An earlier truncated run showed 122 failures in one suite; full clean rerun shows the stable set of 3 — cold go-module cache was the cause.)
- 03:15 Wave 1 dispatched to Claude Code (H1, H2, H3, H11; 4 atomic commits; gates: fmt/clippy/test).
