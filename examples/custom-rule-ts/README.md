# Custom TypeScript Rule

```bash
polint new-rule ts no-product-hex-colors
```

Use `ctx.string_literals()` and `ctx.jsx_attributes()` to enforce project-specific frontend policies such as design-token usage.
