// This example repo has one local polint rule: local/no-denied-literals.
// The rule reads the deny list from .polint.toml and reports matching string
// literals. The value below is intentionally configured as denied.
export const selector = "legacy-testid";
