// This one is tracked as intentional debt.
// polint-ignore-next-line local/no-denied-literals -- generated selector kept for migration coverage
export const migratedSelector = "legacy-testid";

// This one stays visible in `polint check`.
export const newSelector = "legacy-testid";
