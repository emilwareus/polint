---
phase: 31-p0-abstract-domain-kernel
plan: 01
title: Private Domain Contracts And Law Tests
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/polint/src/analysis/mod.rs
  - crates/polint/src/analysis/domains/mod.rs
  - crates/polint/src/analysis/domains/lattice.rs
  - crates/polint/src/analysis/domains/core.rs
  - crates/polint/src/analysis/domains/state.rs
autonomous: true
requirements: [SAE-INT-01]
must_haves:
  truths:
    - "Abstract-domain internals are crate-private and not visible to rule authors."
    - "Every P0 domain slot has bottom/top, partial order, join, join_into, widening, and stable digest behavior."
    - "Domain-law tests prove partial order, join, widening, and digest invariants before solver wiring."
  artifacts:
    - path: "crates/polint/src/analysis/domains/lattice.rs"
      provides: "AbstractDomain trait, Changed, TopReason, WidenSite, WidenFuel, and stable digest contracts"
      contains: "pub(crate) trait AbstractDomain"
    - path: "crates/polint/src/analysis/domains/core.rs"
      provides: "Reachability, nilness/nullishness, truthiness, constants, simple strings, and initializedness domains"
      contains: "pub(crate) enum NilnessDomain"
    - path: "crates/polint/src/analysis/domains/state.rs"
      provides: "Deterministic ProductState and CoreDomains containers over BTreeMap-backed place slots"
      contains: "pub(crate) struct ProductState"
  key_links:
    - from: "crates/polint/src/analysis/mod.rs"
      to: "crates/polint/src/analysis/domains/mod.rs"
      via: "crate-private module registration"
      pattern: "pub\\(crate\\) mod domains"
    - from: "crates/polint/src/analysis/domains/core.rs"
      to: "crates/polint/src/analysis/domains/lattice.rs"
      via: "AbstractDomain implementations"
      pattern: "impl AbstractDomain for .*Domain"
---

<objective>
Create the private abstract-domain vocabulary and P0 domain product that later Phase 31 plans solve over MIR/CFG.

Purpose: Establish law-tested lattice/domain contracts before any provider or public-facing behavior exists.
Output: Crate-private `analysis::domains` modules with P0 domain slots and focused law tests.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/31-p0-abstract-domain-kernel/31-CONTEXT.md
@./AGENTS.md
@.agents/skills/rust-best-practices/SKILL.md
@.claude/skills/polint/SKILL.md
@research/abstract-interpretation/FINAL-REPORT.md
@research/abstract-interpretation/RECOMMENDED_IMPLEMENTATION.md
@research/abstract-interpretation/VALIDATION.md
@research/abstract-interpretation/implementation/MIR-CONTRACT.md
@research/abstract-interpretation/implementation/BOOTSTRAP-SEQUENCE.md
@crates/polint/src/analysis/mod.rs
@crates/polint/src/analysis/ids.rs
@crates/polint/src/analysis/stable_key.rs
@crates/polint/src/analysis/places.rs
@crates/polint/src/analysis_kernel/incremental/digest.rs

<interfaces>
Existing private module pattern:
```rust
pub(crate) mod calls;
pub(crate) mod cfg;
pub(crate) mod mir;
```

Existing MIR/place IDs the domain state may reference later:
```rust
pub(crate) struct MirBodyId(pub(crate) u64);
pub(crate) struct MirOpId(pub(crate) u64);
pub(crate) struct PlaceId(pub(crate) u64);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add private lattice contracts</name>
  <files>crates/polint/src/analysis/mod.rs, crates/polint/src/analysis/domains/mod.rs, crates/polint/src/analysis/domains/lattice.rs</files>
  <read_first>
    - .planning/phases/31-p0-abstract-domain-kernel/31-CONTEXT.md
    - research/abstract-interpretation/RECOMMENDED_IMPLEMENTATION.md
    - research/abstract-interpretation/VALIDATION.md
    - crates/polint/src/analysis/mod.rs
    - crates/polint/src/analysis_kernel/incremental/digest.rs
  </read_first>
  <behavior>
    - RED: tests fail until `AbstractDomain`, `Changed`, `TopReason`, `WidenSite`, and `WidenFuel` exist.
    - GREEN: `join_into` mutates only when `join` changes the canonical state.
    - GREEN: stable digests are deterministic for equal domain values and differ for distinct top reasons.
  </behavior>
  <action>Create crate-private `analysis::domains` per D-01. Add `pub(crate) mod domains;` to `analysis/mod.rs`; do not touch `lib.rs`, SDK, runner, CLI, README, or docs/facts. In `lattice.rs`, define `pub(crate) trait AbstractDomain: Clone + Send + Sync + Eq + 'static` with `const ID: &'static str`, `const VERSION: u32`, `bottom`, `top(TopReason)`, `is_bottom`, `is_top`, `leq`, `join`, `join_into`, `widen`, and `stable_digest_parts(&self) -> Vec<String>`. Add `Changed::{Yes,No}`, `TopReason::{UnknownValue, UnsupportedSemantic, DynamicWrite, UnresolvedCall, SetupMissing, BudgetExceeded, Widened, ConflictingFacts}`, `WidenSite { stable_key: String }`, and `WidenFuel { remaining: u32 }`. Use `BTreeSet`/sorted `Vec<String>` for digest parts. This implements D-04 and D-12; keep comments limited to why top reasons exist for truthfulness.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::domains::lattice --locked</automated>
    <automated>rg -n "pub mod domains|pub use crate::analysis::domains|Nilness&lt;'_|Constants&lt;'" crates/polint/src/lib.rs crates/polint/src/sdk crates/polint/src/runner README.md docs/facts || true</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "pub\\(crate\\) mod domains" crates/polint/src/analysis/mod.rs` returns one match.
    - `rg -n "pub\\(crate\\) trait AbstractDomain|enum TopReason|fn join_into|fn stable_digest_parts" crates/polint/src/analysis/domains/lattice.rs` returns matches.
    - `cargo test -p polint --lib analysis::domains::lattice --locked` passes.
    - No SDK, runner, CLI, README, or docs/facts file contains promoted domain view names.
  </acceptance_criteria>
  <done>Private lattice contracts compile, are law-testable, and remain outside public surfaces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement P0 core domain slots</name>
  <files>crates/polint/src/analysis/domains/core.rs, crates/polint/src/analysis/domains/mod.rs</files>
  <read_first>
    - crates/polint/src/analysis/domains/lattice.rs
    - crates/polint/src/analysis/places.rs
    - research/abstract-interpretation/FINAL-REPORT.md
    - research/abstract-interpretation/RECOMMENDED_IMPLEMENTATION.md
    - research/abstract-interpretation/implementation/EXTENSION-DOMAIN-CONTRACT.md
  </read_first>
  <behavior>
    - RED: law tests fail for each slot until order, join, top, and widening semantics exist.
    - GREEN: reachability orders `Unreachable <= Reachable <= Ambiguous`.
    - GREEN: capped constant and string literal sets widen to top with `TopReason::Widened` when the configured cap is exceeded.
    - GREEN: initializedness distinguishes initialized, maybe-uninitialized, uninitialized, and top/unknown.
  </behavior>
  <action>In `core.rs`, define all first slots required by SAE-INT-01 and D-05: `ReachabilityDomain`, `NilnessDomain`, `TruthinessDomain`, `ConstantDomain`, `StringDomain`, and `InitializednessDomain`. Use small enums for reachability/nilness/truthiness/initializedness and capped sorted literal sets for constants and strings. Constants must support at least bool, null, undefined, nil, string, number, and unknown literal labels as polint-owned strings; strings must track capped possible literal values plus top reason. Implement `AbstractDomain` for every slot with deterministic `stable_digest_parts`. Include widening samples for literal-set caps and no relational numeric/shape/typestate/path-predicate implementation, per D-06.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::domains::core --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "ReachabilityDomain|NilnessDomain|TruthinessDomain|ConstantDomain|StringDomain|InitializednessDomain" crates/polint/src/analysis/domains/core.rs` returns all six names.
    - `rg -n "impl AbstractDomain for" crates/polint/src/analysis/domains/core.rs | wc -l` reports at least `6`.
    - Tests named with `partial_order`, `join_is`, `widen`, and `stable_digest` exist in `core.rs`.
    - `cargo test -p polint --lib analysis::domains::core --locked` passes.
  </acceptance_criteria>
  <done>P0 domain slots exist, are deterministic, and have law-focused unit coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add deterministic product state</name>
  <files>crates/polint/src/analysis/domains/state.rs, crates/polint/src/analysis/domains/mod.rs</files>
  <read_first>
    - crates/polint/src/analysis/domains/lattice.rs
    - crates/polint/src/analysis/domains/core.rs
    - crates/polint/src/analysis/ids.rs
    - crates/polint/src/analysis/places.rs
    - research/abstract-interpretation/RECOMMENDED_IMPLEMENTATION.md
  </read_first>
  <behavior>
    - RED: product join tests fail until every slot is joined through `join_into`.
    - GREEN: place-indexed slots are stored in sorted `BTreeMap<PlaceId, _>` collections and serialize/digest independently of insertion order.
    - GREEN: product reductions propagate constant literals into nilness, truthiness, and string slots without claiming path-sensitive precision.
  </behavior>
  <action>In `state.rs`, add `CoreDomains` and `ProductState` per D-05. `CoreDomains` must contain function-level reachability plus place-indexed nilness, truthiness, constants, strings, and initializedness maps. Add `ProductState::bottom`, `ProductState::entry`, `join_into`, `widen`, `stable_digest_parts`, and a bounded `reduce_value_only` pass that derives nilness/truthiness/string information from literal constants. Do not add extension slots beyond a private zero-sized `ExtensionDomainSlots` marker carrying an empty sorted digest component, per D-06 design pressure. Use `join_into` APIs rather than open-coded `leq` polarity checks, per the Phase 31 specifics.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::domains::state --locked</automated>
    <automated>cargo test -p polint --lib analysis::domains --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "struct CoreDomains|struct ProductState|BTreeMap<PlaceId" crates/polint/src/analysis/domains/state.rs` returns matches.
    - `rg -n "reduce_value_only|stable_digest_parts|join_into" crates/polint/src/analysis/domains/state.rs` returns matches.
    - Insertion-order determinism and reduction tests exist and pass.
    - `cargo test -p polint --lib analysis::domains --locked` passes.
  </acceptance_criteria>
  <done>Product state provides a deterministic reduced product over all P0 slots without public exposure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| private domain contracts -> future provider | Incorrect lattice laws can make later analysis unsound or nondeterministic. |
| source-derived literals -> digest/state labels | Literal labels are untrusted code-derived data and must not become raw-source public output in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01-01 | T | `analysis::domains::lattice` | mitigate | Law tests cover partial order, join upper-bound/idempotence/commutativity/associativity, widening convergence, and stable digest ordering. |
| T-31-01-02 | I | `analysis::domains::core` literal slots | mitigate | Store only compact literal labels in private tests; no debug, eval, SDK, CLI, README, or docs/facts output is added in this plan. |
| T-31-01-03 | D | `ProductState::reduce_value_only` | mitigate | Bound reduction rounds and test convergence so reduction cannot loop indefinitely. |
| T-31-01-04 | E | public API boundary | mitigate | Keep modules `pub(crate)` and add grep acceptance checks against SDK/runner/CLI/docs promotion. |
</threat_model>

<verification>
Run:
- `cargo test -p polint --lib analysis::domains --locked`
- `cargo fmt --all -- --check`
- `rg -n "pub mod domains|pub use crate::analysis::domains|Nilness&lt;'_|Constants&lt;|Truthiness&lt;'" crates/polint/src/lib.rs crates/polint/src/sdk crates/polint/src/runner README.md docs/facts || true`
</verification>

<success_criteria>
Plan 31-01 succeeds when the private P0 domain product compiles, law tests pass, all domain slots are present, and no public SDK/CLI/docs surface is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/31-p0-abstract-domain-kernel/31-01-SUMMARY.md`.
</output>
