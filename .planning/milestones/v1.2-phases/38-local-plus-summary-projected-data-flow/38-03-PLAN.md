---
phase: 38-local-plus-summary-projected-data-flow
plan: 03
title: Local MIR And Place Value-Flow Graph
type: execute
wave: 3
depends_on: [38-01, 38-02]
files_modified:
  - crates/polint/src/analysis/data_flow/local.rs
  - crates/polint/src/analysis/data_flow/provider.rs
  - crates/polint/src/analysis/data_flow/facts.rs
  - crates/polint/src/analysis/data_flow/store.rs
autonomous: true
requirements: [SAE-PREC-03]
must_haves:
  truths:
    - "D-06/D-08 are covered: local value flow covers assignments, reads/writes, parameters, receivers, returns, fields/properties/indexes, call arguments/returns, and low-depth access paths."
    - "D-07 is covered: unsupported MIR operations, unknown writes, dynamic access, async/generator gaps, and setup-missing facts emit explicit unknown/havoc rows."
    - "D-09 is covered: the local graph remains generic value flow; taint/source-sink behavior is layered later through model/query facts."
  artifacts:
    - path: "crates/polint/src/analysis/data_flow/local.rs"
      provides: "Local MIR/place/CFG value-flow extraction"
---

<objective>
Populate the data-flow provider with local value-flow nodes and edges derived from existing MIR operations, places, CFG locations, access paths, and unsupported/havoc evidence.
</objective>

<execution_context>
@.planning/phases/38-local-plus-summary-projected-data-flow/38-CONTEXT.md
@.planning/phases/38-local-plus-summary-projected-data-flow/38-PATTERNS.md
@.agents/skills/rust-best-practices/SKILL.md
@AGENTS.md
</execution_context>

<context>
@crates/polint/src/analysis/mir/op.rs
@crates/polint/src/analysis/mir/body.rs
@crates/polint/src/analysis/places.rs
@crates/polint/src/analysis/cfg/facts.rs
@crates/polint/src/analysis/access_paths/facts.rs
@crates/polint/src/analysis/points_to/facts.rs
@crates/polint/src/analysis/data_flow/facts.rs
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build local node construction</name>
  <files>crates/polint/src/analysis/data_flow/local.rs</files>
  <read_first>
    - crates/polint/src/analysis/mir/op.rs
    - crates/polint/src/analysis/places.rs
    - crates/polint/src/analysis/cfg/facts.rs
    - crates/polint/src/analysis/data_flow/facts.rs
  </read_first>
  <action>Create a local builder that emits deterministic node drafts for parameters, receivers, locals, globals/module values, literals, temporaries, field/property/index places, call arguments, call receivers, call returns, and return values. Each node must carry existing file/function/MIR/CFG/place/call anchors when available and a stable key built from those anchors.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::data_flow::local --locked</automated>
  </verify>
  <acceptance_criteria>
    - Tests prove parameter, local assignment, and return nodes have stable keys independent of traversal order.
    - Tests prove nodes reference existing `PlaceId` and `MirOpId` where applicable.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit local value-flow edges</name>
  <files>crates/polint/src/analysis/data_flow/local.rs, crates/polint/src/analysis/data_flow/facts.rs</files>
  <read_first>
    - crates/polint/src/analysis/mir/op.rs
    - crates/polint/src/analysis/values/facts.rs
    - crates/polint/src/analysis/access_paths/facts.rs
    - crates/polint/src/analysis/aliases/facts.rs
  </read_first>
  <action>Emit local edges for assignment, read/write, field/property/index access, receiver flow, call argument binding nodes, call-return nodes, and return values. Use Phase 36 access-path and points-to facts only within the configured access-path depth/fanout. Add edge evidence and input stable keys for MIR ops, places, access paths, and points-to rows used.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::data_flow::local --locked</automated>
  </verify>
  <acceptance_criteria>
    - Unit tests prove `param -> local -> return` flow in a synthetic Go or TS/JS body.
    - Unit tests prove field/property edges include access-path evidence.
    - Unit tests prove dynamic property/index flow emits unknown or budgeted status instead of exact flow.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Emit unknown and havoc rows for unsupported local semantics</name>
  <files>crates/polint/src/analysis/data_flow/local.rs, crates/polint/src/analysis/data_flow/provider.rs</files>
  <read_first>
    - crates/polint/src/analysis/mir/op.rs
    - crates/polint/src/analysis/store.rs
    - crates/polint/src/analysis/data_flow/provider.rs
  </read_first>
  <action>Translate unsupported MIR semantics, unknown writes, conservative actions such as `HavocAffectedPlaces`, dynamic property/index access, missing setup, and configured access-path budget overflow into explicit data-flow unknown/havoc/budget facts. Wire local builder output into `derive_data_flow_with_cache_stats` before output digest calculation.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::data_flow::provider --locked</automated>
  </verify>
  <acceptance_criteria>
    - Tests prove an unsupported data-flow MIR row creates a data-flow unknown/havoc or unsupported row.
    - Provider output digest changes when local edges are emitted.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parser/MIR internals -> data-flow facts | Data-flow facts must contain polint-owned IDs and spans, not parser objects or raw source bodies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T38-05 | Tampering | local flow precision | Mitigated | Emit unknown/havoc/budget facts for unsupported local semantics. |
| T38-06 | Denial of Service | access-path expansion | Mitigated | Enforce depth/fanout budgets from provider parameters. |
</threat_model>

<verification>
- `cargo fmt --all --check`
- `cargo test -p polint --lib analysis::data_flow --locked`
</verification>

<success_criteria>
Local value-flow nodes and edges exist for supported MIR/place constructs, and unsupported local semantics are visible as unknown/havoc/budget facts.
</success_criteria>
