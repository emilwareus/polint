---
phase: 38-local-plus-summary-projected-data-flow
plan: 01
title: Private Data-Flow Fact Contracts And Store
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/polint/src/analysis/ids.rs
  - crates/polint/src/analysis/mod.rs
  - crates/polint/src/analysis/data_flow/mod.rs
  - crates/polint/src/analysis/data_flow/facts.rs
  - crates/polint/src/analysis/data_flow/store.rs
  - crates/polint/src/analysis_kernel/metadata.rs
  - crates/polint/src/core/mod.rs
autonomous: true
requirements: [SAE-PREC-03]
must_haves:
  truths:
    - "D-01/D-02 are covered: data flow is a crate-private `analysis::data_flow` family with stable-key identity and run-local dense IDs only as handles."
    - "D-03/D-04 are covered: nodes and edges anchor to existing file/function/MIR/CFG/place/call/summary/trust-boundary/model facts and distinguish local, interprocedural, summary, source, sink, sanitizer, barrier, model, unknown, havoc, and budget edge kinds."
    - "D-05 is covered: every row can carry producer, precision, status, confidence, validation, provenance, evidence, and stable key metadata."
    - "D-28 is covered: the store builds rebuildable indexes by node, kind, place, call site, function/body, model id, provenance, and status."
  artifacts:
    - path: "crates/polint/src/analysis/data_flow/facts.rs"
      provides: "DataFlowNodeFact, DataFlowEdgeFact, model fact, status, precision, provenance, validation, and budget vocabulary"
    - path: "crates/polint/src/analysis/data_flow/store.rs"
      provides: "Normalized DataFlowOutput and DataFlowStore indexes"
---

<objective>
Define the private Phase 38 data-flow contract: IDs, fact rows, model/source/sink/sanitizer/barrier vocabulary, normalized output, store indexes, AnalysisDb storage, and metadata fact families.
</objective>

<execution_context>
@.planning/phases/38-local-plus-summary-projected-data-flow/38-CONTEXT.md
@.planning/phases/38-local-plus-summary-projected-data-flow/38-PATTERNS.md
@.agents/skills/rust-best-practices/SKILL.md
@AGENTS.md
</execution_context>

<context>
@crates/polint/src/analysis/refined_calls/facts.rs
@crates/polint/src/analysis/refined_calls/store.rs
@crates/polint/src/analysis/summaries/facts.rs
@crates/polint/src/analysis/entrypoints/facts.rs
@crates/polint/src/analysis/places.rs
@crates/polint/src/analysis_kernel/metadata.rs
@crates/polint/src/core/mod.rs
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add data-flow IDs and private module registration</name>
  <files>crates/polint/src/analysis/ids.rs, crates/polint/src/analysis/mod.rs, crates/polint/src/analysis/data_flow/mod.rs</files>
  <read_first>
    - .agents/skills/rust-best-practices/SKILL.md
    - AGENTS.md
    - crates/polint/src/analysis/ids.rs
    - crates/polint/src/analysis/mod.rs
    - crates/polint/src/analysis/refined_calls/mod.rs
  </read_first>
  <action>Add crate-private dense ID newtypes `DataFlowNodeId`, `DataFlowEdgeId`, `DataFlowModelId`, `DataFlowBudgetId`, and `DataFlowPathId` using the existing `pub(crate) struct X(pub(crate) u64)` pattern. Register `pub(crate) mod data_flow;` in `analysis/mod.rs`. Create `analysis/data_flow/mod.rs` exposing crate-private `facts` and `store` modules. Do not modify `lib.rs`, `sdk`, `runner`, CLI, README, or `docs/facts`.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::ids --locked</automated>
    <automated>rg -n "pub mod data_flow|DataFlow&lt;'_|DataFlowNodeFact" crates/polint/src/lib.rs crates/polint/src/sdk crates/polint/src/runner README.md docs || true</automated>
  </verify>
  <acceptance_criteria>
    - `crates/polint/src/analysis/ids.rs` contains `DataFlowNodeId` and `DataFlowEdgeId`.
    - `crates/polint/src/analysis/mod.rs` contains `pub(crate) mod data_flow;`.
    - No public SDK, runner, README, or docs file exposes private Phase 38 data-flow internals.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define data-flow row vocabulary</name>
  <files>crates/polint/src/analysis/data_flow/facts.rs</files>
  <read_first>
    - crates/polint/src/analysis/mir/op.rs
    - crates/polint/src/analysis/places.rs
    - crates/polint/src/analysis/cfg/facts.rs
    - crates/polint/src/analysis/calls/facts.rs
    - crates/polint/src/analysis/refined_calls/facts.rs
    - crates/polint/src/analysis/summaries/facts.rs
    - crates/polint/src/analysis/entrypoints/facts.rs
    - crates/polint/src/analysis/extensions/sinks.rs
  </read_first>
  <action>Define `DataFlowNodeFact`, `DataFlowEdgeFact`, `DataFlowModelFact`, and `DataFlowBudgetFact` or equivalent rows. Node fields must include optional anchors for `FileId`, `FunctionId`, `MirBodyId`, `MirOpId`, `CfgNodeId`, `PlaceId`, `SymbolId`, `ReferenceId`, `CallSiteId`, summary stable key, trust-boundary stable key, and model id. Edge fields must include `from`, `to`, `kind`, `algorithm`, `status`, `precision`, `validation`, `confidence`, `provenance`, optional call/refined-call/summary/model links, evidence strings, input stable keys, and `stable_key`. Define enums for node kind, edge kind, model kind, algorithm, status, precision, provenance, validation, confidence, and budget reason covering all D-01 through D-05 vocabulary.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::data_flow::facts --locked</automated>
  </verify>
  <acceptance_criteria>
    - `DataFlowEdgeKind` includes assignment/local, field/property/access-path, call argument, call return, receiver, return value, summary-projected, source, sink, sanitizer, barrier, additional model step, unknown, havoc, and budget-truncated variants or exact equivalents.
    - `DataFlowStatus` includes present/resolved, unknown, unsupported, setup-missing, rejected, havoc, and budget-exceeded variants or exact equivalents.
    - Fact row tests prove stable keys and dense IDs are separate.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add normalized output, store indexes, and AnalysisDb accessors</name>
  <files>crates/polint/src/analysis/data_flow/store.rs, crates/polint/src/core/mod.rs</files>
  <read_first>
    - crates/polint/src/analysis/refined_calls/store.rs
    - crates/polint/src/analysis/types/store.rs
    - crates/polint/src/core/mod.rs
  </read_first>
  <action>Create `DataFlowOutput` with nodes, edges, model facts, and budget facts. `normalized()` must sort by stable key, edge kind, provenance, status, and old id, then reassign dense IDs from zero. Create `DataFlowStore::from_output` with deterministic duplicate-stable-key errors and indexes by node, edge kind, place, call site, function/body, model id, provenance, status, and precision. Add a private `AnalysisDb` store field plus `replace_data_flow_facts`, `data_flow_nodes`, `data_flow_edges`, and targeted index accessors.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::data_flow::store --locked</automated>
    <automated>cargo test -p polint --lib core:: --locked</automated>
  </verify>
  <acceptance_criteria>
    - Store tests prove output order and dense IDs are deterministic.
    - Store tests prove indexes return source, sanitizer, summary-projected, and budget/havoc edges by kind/status.
    - `AnalysisDb` exposes crate-private replacement and accessors for data-flow facts.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Register data-flow metadata families</name>
  <files>crates/polint/src/analysis_kernel/metadata.rs</files>
  <read_first>
    - crates/polint/src/analysis_kernel/metadata.rs
    - crates/polint/src/analysis/data_flow/facts.rs
  </read_first>
  <action>Add `FactFamily` variants and labels for data-flow nodes, data-flow edges, data-flow model facts, data-flow budget facts, and path/query rows if Plan 01 introduces stored query rows. Ensure metadata labels are internal and do not change public report JSON.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis_kernel::metadata --locked</automated>
  </verify>
  <acceptance_criteria>
    - `FactFamily` labels every Phase 38 fact row added in this plan.
    - Metadata tests pass and no `unreachable_pub` issue is introduced.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| private facts -> public API | Data-flow internals must remain crate-private until Phase 41 promotion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T38-01 | Information Disclosure | data-flow facts | Mitigated | Keep modules `pub(crate)` and add public no-leak proof in Plan 38-07. |
| T38-02 | Tampering | stable identity | Mitigated | Normalize by stable key and reject duplicate/conflicting stable keys. |
</threat_model>

<verification>
- `cargo fmt --all --check`
- `cargo test -p polint --lib analysis::data_flow --locked`
- `cargo test -p polint --lib analysis_kernel::metadata --locked`
</verification>

<success_criteria>
Private data-flow contracts compile, use existing identity anchors, carry provenance/precision/status, and expose no public SDK or CLI surface.
</success_criteria>
