---
phase: 38-local-plus-summary-projected-data-flow
plan: 02
title: Data-Flow Provider, Cache Identity, And Kernel Wiring
type: execute
wave: 2
depends_on: [38-01]
files_modified:
  - crates/polint/src/analysis/data_flow/cache_key.rs
  - crates/polint/src/analysis/data_flow/provider.rs
  - crates/polint/src/analysis/data_flow/mod.rs
  - crates/polint/src/analysis_kernel/provider.rs
  - crates/polint/src/analysis_kernel/mod.rs
  - tests/eval-fixtures/kernel/provider-order/expected.polint-eval.toml
autonomous: true
requirements: [SAE-PREC-03]
must_haves:
  truths:
    - "D-25/D-26 are covered: `polint.data_flow` runs after `polint.refined_calls` and before metrics, consuming all upstream facts needed for local, interprocedural, summary, model, and type/value-aware flow."
    - "D-27 is covered: cache identity includes provider/schema, upstream digests, config/lifecycle, model/extension/tool sentinels, and budget/precision parameters."
    - "D-13 is covered: provider order avoids refined-call/data-flow dependency cycles."
  artifacts:
    - path: "crates/polint/src/analysis/data_flow/provider.rs"
      provides: "derive_data_flow_with_cache_stats provider entrypoint"
    - path: "crates/polint/src/analysis_kernel/provider.rs"
      provides: "polint.data_flow manifest after refined calls and before metrics"
---

<objective>
Wire a deterministic private `polint.data_flow` provider into cache identity, provider manifests, kernel execution, and provider-order fixtures.
</objective>

<execution_context>
@.planning/phases/38-local-plus-summary-projected-data-flow/38-CONTEXT.md
@.planning/phases/38-local-plus-summary-projected-data-flow/38-PATTERNS.md
@.agents/skills/rust-best-practices/SKILL.md
@AGENTS.md
</execution_context>

<context>
@crates/polint/src/analysis/refined_calls/provider.rs
@crates/polint/src/analysis/refined_calls/cache_key.rs
@crates/polint/src/analysis/types/cache_key.rs
@crates/polint/src/analysis_kernel/provider.rs
@crates/polint/src/analysis_kernel/mod.rs
@tests/eval-fixtures/kernel/provider-order/expected.polint-eval.toml
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add data-flow cache parameters and digest helpers</name>
  <files>crates/polint/src/analysis/data_flow/cache_key.rs, crates/polint/src/analysis/data_flow/mod.rs</files>
  <read_first>
    - crates/polint/src/analysis/refined_calls/cache_key.rs
    - crates/polint/src/analysis/types/cache_key.rs
    - crates/polint/src/analysis_kernel/incremental/keys.rs
  </read_first>
  <action>Create `DATA_FLOW_SCHEMA_LABEL` and `DataFlowProviderParameters` with deterministic defaults for local edge limit, access-path depth, access-path fanout, interprocedural call depth, summary projection depth, path search node/edge/depth limits, and model expansion limit. Add parameter digest helpers for default settings and input snapshots. Include absent model, extension, and tool sentinels where no components exist.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::data_flow::cache_key --locked</automated>
  </verify>
  <acceptance_criteria>
    - A test proves changing access-path depth changes the provider parameter digest.
    - A test proves model, extension, and tool input components participate in snapshot parameter digest.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add deterministic provider skeleton</name>
  <files>crates/polint/src/analysis/data_flow/provider.rs, crates/polint/src/analysis/data_flow/mod.rs</files>
  <read_first>
    - crates/polint/src/analysis/refined_calls/provider.rs
    - crates/polint/src/analysis/data_flow/store.rs
    - crates/polint/src/core/mod.rs
  </read_first>
  <action>Implement `derive_data_flow_with_cache_stats` that accepts `AnalysisDb`, `InputSnapshot`, the `polint.data_flow` manifest, and upstream output digests for semantic MIR, CFG, calls, direct summaries, entrypoints, extensions, type_value_alias, and refined_calls. Initially it may emit an empty normalized output, but it must compute a deterministic output digest from provider/schema, parameters, upstream digests, config/lifecycle, model/extension/tool components, and stable payload rows, then write through `replace_data_flow_facts` and return cache stats plus diagnostics.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::data_flow::provider --locked</automated>
  </verify>
  <acceptance_criteria>
    - Empty output digest includes a `data_flow_output=empty` or equivalent marker.
    - Provider tests prove output digest changes when an upstream digest or budget parameter changes.
    - Provider writes through `AnalysisDb::replace_data_flow_facts`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Register provider manifest and kernel execution</name>
  <files>crates/polint/src/analysis_kernel/provider.rs, crates/polint/src/analysis_kernel/mod.rs, tests/eval-fixtures/kernel/provider-order/expected.polint-eval.toml</files>
  <read_first>
    - crates/polint/src/analysis_kernel/provider.rs
    - crates/polint/src/analysis_kernel/mod.rs
    - tests/eval-fixtures/kernel/provider-order/expected.polint-eval.toml
  </read_first>
  <action>Add a `polint.data_flow` provider manifest after `polint.refined_calls` and before `polint.metrics`. Inputs must include source/config/lifecycle-sensitive upstream labels for symbols/references, MIR, places, CFG, calls, refined call edges, summaries, entrypoints/trust boundaries/dispatch/unresolved framework, type/value/access-path/points-to/alias facts, extension facts/rejections, and model/tool sentinels. Outputs must include `data_flow_nodes`, `data_flow_edges`, `data_flow_models`, and `data_flow_budgets` or exact labels from Plan 38-01. Add kernel execution and provider output metadata to `KernelRunReport`. Update provider-order eval expectations.</action>
  <verify>
    <automated>cargo test -p polint --lib provider_order --locked</automated>
    <automated>cargo test -p polint --lib eval_observed_kernel_collects_provider_order_invariants_from_real_kernel --locked</automated>
  </verify>
  <acceptance_criteria>
    - Provider order includes `polint.data_flow` after `polint.refined_calls`.
    - Kernel report includes data-flow provider output metadata.
    - Provider-order eval fixture expects `polint.data_flow`.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| extension/model input -> cache identity | Extension and model facts can affect data-flow output and must invalidate/quarantine relevant cache entries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T38-03 | Tampering | cache identity | Mitigated | Include upstream digests, model/extension/tool components, and budget parameters. |
| T38-04 | Denial of Service | provider execution | Mitigated | Start with deterministic provider skeleton and explicit budget parameters before algorithms. |
</threat_model>

<verification>
- `cargo fmt --all --check`
- `cargo test -p polint --lib analysis::data_flow --locked`
- `cargo test -p polint --lib provider_order --locked`
</verification>

<success_criteria>
`polint.data_flow` exists as a deterministic private provider with complete cache inputs and safe provider ordering.
</success_criteria>
