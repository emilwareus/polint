---
phase: 38-local-plus-summary-projected-data-flow
plan: 10
title: Data-Flow Eval Fixtures Debug And Public Boundary Proof
type: execute
wave: 10
depends_on: [38-08, 38-09]
files_modified:
  - crates/polint/src/analysis/data_flow/debug.rs
  - crates/polint/src/analysis/data_flow/validate.rs
  - crates/polint/src/analysis/data_flow/mod.rs
  - crates/polint/src/analysis_kernel/debug.rs
  - crates/polint/src/analysis_kernel/validation.rs
  - crates/polint/src/eval/model.rs
  - crates/polint/src/eval/fixtures.rs
  - crates/polint/src/eval/observed.rs
  - crates/polint/src/sdk/facts.rs
  - docs/facts/data-flow.md
  - tests/eval-fixtures/data-flow/
autonomous: true
requirements: [SAE-PREC-03]
gap_closure:
  verification_file: .planning/phases/38-local-plus-summary-projected-data-flow/38-VERIFICATION.md
  gaps:
    - data-flow eval fixtures are absent
    - dedicated data-flow public no-leak proof is incomplete
    - debug/validation coverage does not prove all Phase 38 gap rows
must_haves:
  truths:
    - "Data-flow eval fixtures cover local flow, direct-call projection, summary projection, models, rejected extension facts, unknown/havoc facts, and deterministic budget handling."
    - "The plan-listed eval commands run non-zero tests."
    - "A dedicated data-flow public no-leak proof covers check JSON, CLI help, SDK exports, runner surface, README, and docs/facts."
    - "Public docs do not present private provider ids or internal row schemas as a supported SDK contract."
  artifacts:
    - path: "tests/eval-fixtures/data-flow/"
      provides: "Native data-flow fixture suite"
    - path: "crates/polint/src/eval/fixtures.rs"
      provides: "Data-flow fixture runner and taxonomy tests"
    - path: "crates/polint/src/analysis/data_flow/debug.rs"
      provides: "Deterministic test-facing data-flow debug rows"
---

<objective>
Close the Phase 38 proof gaps by adding data-flow debug/eval fixtures, validation coverage, non-zero focused tests, and a dedicated public-boundary proof.
</objective>

<execution_context>
@.planning/phases/38-local-plus-summary-projected-data-flow/38-CONTEXT.md
@.planning/phases/38-local-plus-summary-projected-data-flow/38-PATTERNS.md
@.planning/phases/38-local-plus-summary-projected-data-flow/38-VERIFICATION.md
@.agents/skills/rust-best-practices/SKILL.md
@AGENTS.md
</execution_context>

<context>
@crates/polint/src/analysis/refined_calls/debug.rs
@crates/polint/src/analysis/refined_calls/validate.rs
@crates/polint/src/analysis_kernel/debug.rs
@crates/polint/src/analysis_kernel/validation.rs
@crates/polint/src/eval/model.rs
@crates/polint/src/eval/fixtures.rs
@crates/polint/src/eval/observed.rs
@tests/eval-fixtures/refined-calls/direct-vs-refined/expected.polint-eval.toml
@docs/API-VISIBILITY-PLAN.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add validation and debug rows for completed data-flow facts</name>
  <files>crates/polint/src/analysis/data_flow/validate.rs, crates/polint/src/analysis/data_flow/debug.rs, crates/polint/src/analysis_kernel/validation.rs, crates/polint/src/analysis_kernel/debug.rs</files>
  <read_first>
    - crates/polint/src/analysis/data_flow/validate.rs
    - crates/polint/src/analysis/data_flow/store.rs
    - crates/polint/src/analysis/data_flow/facts.rs
    - crates/polint/src/analysis/refined_calls/debug.rs
    - crates/polint/src/analysis_kernel/debug.rs
    - crates/polint/src/analysis_kernel/validation.rs
  </read_first>
  <action>Extend data-flow validation and debug after Plans 38-08 and 38-09. Validation must reject dangling file/function/MIR/place/call/refined-call/summary/model/budget references, missing provenance, exact unvalidated model facts, budget-exceeded edges without a budget row, and summary-projected rows without summary stable-key evidence. Debug JSON must emit deterministic rows and counts for nodes, edges, models, budgets, local edges, direct-call edges, summary-projected edges, source/sink/sanitizer/barrier model rows, rejected rows, unknown/havoc rows, and path budget/truncation counts. Use relative paths and compact payload labels only.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::data_flow::validate --locked</automated>
    <automated>cargo test -p polint --lib analysis::data_flow::debug --locked</automated>
    <automated>cargo test -p polint --lib analysis_kernel::validation --locked</automated>
    <automated>cargo test -p polint --lib analysis_kernel::debug --locked</automated>
  </verify>
  <acceptance_criteria>
    - Debug JSON contains deterministic counts for `local_edges`, `summary_projected_edges`, `unknown_or_havoc_edges`, and `budget_rows` or exact equivalent field names.
    - Validation tests reject a `BudgetExceeded` edge that lacks a budget reference.
    - Debug tests assert no absolute workspace root, raw source body, parser object id, or timestamp appears.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add native data-flow eval fixture suite</name>
  <files>crates/polint/src/eval/model.rs, crates/polint/src/eval/fixtures.rs, crates/polint/src/eval/observed.rs, tests/eval-fixtures/data-flow/</files>
  <read_first>
    - crates/polint/src/eval/model.rs
    - crates/polint/src/eval/fixtures.rs
    - crates/polint/src/eval/observed.rs
    - tests/eval-fixtures/refined-calls/direct-vs-refined/expected.polint-eval.toml
    - tests/eval-fixtures/type-value-alias/extension-precision/expected.polint-eval.toml
  </read_first>
  <action>Add a `data-flow` native fixture area and observed-row support sourced from data-flow debug JSON. Create one or more fixture directories under `tests/eval-fixtures/data-flow/` whose expected TOML asserts local param-local-return flow, direct-call argument/return projection, summary-projected TITO flow, trust-boundary source model rows, sink/sanitizer/barrier model rows where available, extension-added source/sink/additional-step rows, rejected malformed extension model evidence, missing-summary unknown/havoc rows, false-positive trap rows for sanitized/barriered flow, and budget/path truncation invariants. Add exact tests named `eval_native_fixture_runner_data_flow_fixture_passes` and `eval_data_flow_manifests_cover_required_taxonomy` so the commands from `38-VERIFICATION.md` run non-zero tests.</action>
  <verify>
    <automated>cargo test -p polint --lib eval_native_fixture_runner_data_flow_fixture_passes --locked</automated>
    <automated>cargo test -p polint --lib eval_data_flow_manifests_cover_required_taxonomy --locked</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p polint --lib eval_native_fixture_runner_data_flow_fixture_passes --locked` reports at least 1 test and passes.
    - `cargo test -p polint --lib eval_data_flow_manifests_cover_required_taxonomy --locked` reports at least 1 test and passes.
    - Fixture expectations include local, direct-call, summary-projected, source/sink/sanitizer/barrier, extension, rejected, unknown/havoc, and budget taxonomy entries.
    - Fixture output hashes are deterministic across repeated runs.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add dedicated data-flow public no-leak proof</name>
  <files>crates/polint/src/eval/fixtures.rs, crates/polint/src/eval/observed.rs, crates/polint/src/sdk/facts.rs, docs/facts/data-flow.md, README.md</files>
  <read_first>
    - AGENTS.md
    - docs/API-VISIBILITY-PLAN.md
    - crates/polint/src/sdk/facts.rs
    - crates/polint/src/runner/mod.rs
    - crates/polint/src/cli/mod.rs
    - README.md
    - docs/facts/data-flow.md
  </read_first>
  <action>Add a focused public-boundary test named `data_flow_public_no_leak` or equivalent. It must render normal `polint check --format json`, inspect CLI help/public CLI help as available in existing no-leak helpers, SDK exports, runner surface, README, and `docs/facts`. The forbidden marker set must include `polint.data_flow`, `DataFlowNodeFact`, `DataFlowEdgeFact`, `DataFlowModelFact`, `DataFlowBudgetFact`, `summary_projected`, `query path search`, and internal model schema labels unless a file is intentionally internal/test-only. Replace `docs/facts/data-flow.md` with a short reserved-view note that says `DataFlow<'_>` is not a supported SDK fact view yet, or move the internal-limit content into the Phase 38 planning directory. Do not expose new public methods on `DataFlow<'_>` in this phase.</action>
  <verify>
    <automated>cargo test -p polint --lib data_flow_public_no_leak --locked</automated>
    <automated>cargo test -p polint --lib data_flow --locked</automated>
  </verify>
  <acceptance_criteria>
    - The no-leak test fails if `polint.data_flow` appears in normal CLI JSON, public help, SDK exports, runner docs, README, or `docs/facts`.
    - `docs/facts/data-flow.md` no longer presents internal provider ids or internal row schemas as public fact documentation.
    - `DataFlow<'_>` remains a reserved unsupported view with no public query methods.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Run final gap-closure regression set</name>
  <files>no source edits expected</files>
  <read_first>
    - .planning/phases/38-local-plus-summary-projected-data-flow/38-VERIFICATION.md
    - .planning/phases/38-local-plus-summary-projected-data-flow/38-08-PLAN.md
    - .planning/phases/38-local-plus-summary-projected-data-flow/38-09-PLAN.md
    - .planning/phases/38-local-plus-summary-projected-data-flow/38-10-PLAN.md
  </read_first>
  <action>Run the full Phase 38 gap-closure verification set after implementation. Capture failures as execution gaps instead of marking the phase complete. Do not advance to Phase 39 unless all commands pass and `38-VERIFICATION.md` can be updated to `status: passed`.</action>
  <verify>
    <automated>cargo fmt --all --check</automated>
    <automated>cargo test -p polint --lib analysis::data_flow --locked</automated>
    <automated>cargo test -p polint --lib eval_native_fixture_runner_data_flow_fixture_passes --locked</automated>
    <automated>cargo test -p polint --lib eval_data_flow_manifests_cover_required_taxonomy --locked</automated>
    <automated>cargo test -p polint --lib data_flow_public_no_leak --locked</automated>
    <automated>cargo test --workspace --locked</automated>
    <automated>cargo clippy --workspace --all-targets --locked -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - Every listed command exits 0.
    - The two eval filter commands each run at least one test.
    - `38-VERIFICATION.md` can be re-run and no longer reports the five gap entries from the 2026-05-25 report.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internal debug/eval -> public output | Internal data-flow detail is test-facing only until promotion. |
| public docs -> rule authors | Docs must not imply supported `DataFlow<'_>` semantics before Phase 41 promotion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T38-G07 | Information Disclosure | public docs and CLI | Mitigated | Add dedicated data-flow no-leak tests and remove internal provider ids from public docs. |
| T38-G08 | Tampering | eval fixtures | Mitigated | Use deterministic expected TOML rows and taxonomy coverage tests. |
| T38-G09 | Repudiation | verification evidence | Mitigated | Require non-zero focused eval tests plus full workspace test and clippy evidence. |
</threat_model>

<verification>
- `cargo fmt --all --check`
- `cargo test -p polint --lib analysis::data_flow --locked`
- `cargo test -p polint --lib eval_native_fixture_runner_data_flow_fixture_passes --locked`
- `cargo test -p polint --lib eval_data_flow_manifests_cover_required_taxonomy --locked`
- `cargo test -p polint --lib data_flow_public_no_leak --locked`
- `cargo test --workspace --locked`
- `cargo clippy --workspace --all-targets --locked -- -D warnings`
</verification>

<success_criteria>
Phase 38 has deterministic internal data-flow debug/eval proof, non-zero focused fixture tests, explicit public no-leak coverage, and enough evidence to rerun verification without the previously recorded gaps.
</success_criteria>
