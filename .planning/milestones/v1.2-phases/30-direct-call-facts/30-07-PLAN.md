---
phase: 30-direct-call-facts
plan: 07
type: execute
wave: 7
depends_on: ["30-06"]
files_modified:
  - crates/polint/src/eval/fixtures.rs
  - crates/polint/src/eval/mod.rs
  - tests/eval-fixtures/direct-calls/core/expected.polint-eval.toml
  - tests/eval-fixtures/direct-calls/core/repo/.polint.toml
  - tests/eval-fixtures/direct-calls/core/repo/go.mod
  - tests/eval-fixtures/direct-calls/core/repo/service.go
  - tests/eval-fixtures/direct-calls/core/repo/web/package.json
  - tests/eval-fixtures/direct-calls/core/repo/web/src/app.ts
autonomous: true
requirements: [SAE-SEM-05]
must_haves:
  truths:
    - "Internal eval fixtures cover direct functions, methods, constructors, member calls, imported calls, function values, unsupported dynamic calls, setup-sensitive cases, and precise statuses."
    - "Cold/warm/no-cache direct-call fixture output is deterministic."
    - "Fixture coverage exercises all D-10 index dimensions through debug/index count assertions."
  artifacts:
    - path: "crates/polint/src/eval/fixtures.rs"
      provides: "direct-calls fixture runner"
      contains: "run_direct_calls_core_fixture_for_test"
    - path: "tests/eval-fixtures/direct-calls/core/expected.polint-eval.toml"
      provides: "Expected direct-call fact and index coverage"
      contains: "direct_calls.current_determinism"
    - path: "tests/eval-fixtures/direct-calls/core/repo/service.go"
      provides: "Go direct and unresolved call taxonomy"
      contains: "interface"
  key_links:
    - from: "crates/polint/src/eval/fixtures.rs"
      to: "tests/eval-fixtures/direct-calls/core"
      via: "native fixture runner"
      pattern: "direct-calls"
    - from: "tests/eval-fixtures/direct-calls/core/expected.polint-eval.toml"
      to: "crates/polint/src/analysis_kernel/debug.rs"
      via: "fixture expected rows assert call debug counts and D-10 index counts"
      pattern: "outgoing_by_symbol"
---

<objective>
Add native eval fixtures and final coverage guards for direct call facts.

Purpose: Phase success depends on deterministic evidence that direct call facts, unresolved statuses, and required indexes work across Go and TS/JS fixture code.
Output: direct-calls native fixture repository, expected eval rows/invariants, and fixture dispatch/coverage guards.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-direct-call-facts/30-CONTEXT.md
@research/call-graphs/VALIDATION.md
@crates/polint/src/analysis_kernel/debug.rs
@crates/polint/src/eval/fixtures.rs
@crates/polint/src/eval/mod.rs
@tests/eval-fixtures/semantic-mir/core/expected.polint-eval.toml
@tests/eval-fixtures/cfg-core/expected.polint-eval.toml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add direct-calls native fixture coverage</name>
  <files>crates/polint/src/eval/fixtures.rs, tests/eval-fixtures/direct-calls/core/expected.polint-eval.toml, tests/eval-fixtures/direct-calls/core/repo/.polint.toml, tests/eval-fixtures/direct-calls/core/repo/go.mod, tests/eval-fixtures/direct-calls/core/repo/service.go, tests/eval-fixtures/direct-calls/core/repo/web/package.json, tests/eval-fixtures/direct-calls/core/repo/web/src/app.ts</files>
  <read_first>
    - `.planning/phases/30-direct-call-facts/30-CONTEXT.md`
    - `crates/polint/src/eval/fixtures.rs`
    - `tests/eval-fixtures/semantic-mir/core/expected.polint-eval.toml`
    - `tests/eval-fixtures/cfg-core/expected.polint-eval.toml`
    - planned new file: `tests/eval-fixtures/direct-calls/core/expected.polint-eval.toml`
    - planned new file: `tests/eval-fixtures/direct-calls/core/repo/service.go`
    - planned new file: `tests/eval-fixtures/direct-calls/core/repo/web/src/app.ts`
  </read_first>
  <behavior>
    - Test 1: fixture passes and observes `CallSite`, `CallTarget`, and `UnresolvedCall` rows.
    - Test 2: fixture manifest covers Go direct function, Go concrete method, Go function value unresolved, Go reflection/interface/goroutine unsupported evidence, TS imported function, TS class constructor, TS static/member call, TS callable value unresolved, TS dynamic property/eval/call-apply-bind unsupported evidence, and setup-sensitive statuses.
    - Test 3: fixture asserts `direct_calls.current_determinism = cold_warm_no_cache_equal`.
  </behavior>
  <action>Add `tests/eval-fixtures/direct-calls/core` using the existing TOML eval fixture format. Create a mixed Go and TS/JS repo: `service.go` should include direct function calls, a concrete method call, an interface-typed call or function value call that remains unresolved, reflection-like evidence, and a goroutine call that is not an interprocedural target. `web/src/app.ts` should include imported calls, class constructor calls, static/member calls with direct reference evidence, callable value calls, computed property call, `eval`, dynamic `import()`, and `call`/`apply`/`bind`. Add `run_direct_calls_core_fixture_for_test` to run cold, warm, and no-cache copies through `AnalysisKernel`, compare normalized output hashes, and emit `direct_calls.current_determinism`. Keep expected rows partial where stable keys are intentionally verbose, but require exact status/precision/provider labels for the success criteria per D-19.</action>
  <acceptance_criteria>
    - `rg -n "case_id = \"direct-calls-core\"|area = \"direct-calls\"|CallSite|CallTarget|UnresolvedCall|direct_calls.current_determinism" tests/eval-fixtures/direct-calls/core/expected.polint-eval.toml` finds fixture expectations.
    - `rg -n "func direct|interface|reflect|go .*\\(|function value|eval\\(|import\\(|\\.call\\(|\\.apply\\(|new .*\\(|static" tests/eval-fixtures/direct-calls/core/repo` finds source taxonomy.
    - `rg -n "run_direct_calls_core_fixture_for_test|direct_calls.current_determinism" crates/polint/src/eval/fixtures.rs` finds runner and invariant.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p polint --lib eval::fixtures::direct_calls_core --locked</automated>
  </verify>
  <done>Direct-call eval fixture proves required taxonomy, statuses, and deterministic output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add final eval coverage guards for counts and indexes</name>
  <files>crates/polint/src/eval/fixtures.rs, crates/polint/src/eval/mod.rs, tests/eval-fixtures/direct-calls/core/expected.polint-eval.toml</files>
  <read_first>
    - `.planning/phases/30-direct-call-facts/30-CONTEXT.md`
    - `crates/polint/src/eval/fixtures.rs`
    - `crates/polint/src/eval/mod.rs`
    - `tests/eval-fixtures/direct-calls/core/expected.polint-eval.toml`
  </read_first>
  <behavior>
    - Test 1: fixture observes non-zero counts by language, call kind, algorithm, status, unresolved reason, and provider.
    - Test 2: expected rows include both `Resolved` target evidence and unresolved/unsupported evidence.
    - Test 3: fixture expected rows or invariants name every D-10 index dimension: outgoing by function/symbol, incoming by target symbol/function, and unresolved by reason/status.
  </behavior>
  <action>Add focused tests and expected invariants that prove debug counts from D-18 are not empty on the fixture and that D-10 index coverage is live. Ensure native fixture discovery dispatches `area = "direct-calls"` / `case_id = "direct-calls-core"` to `run_direct_calls_core_fixture_for_test`. Add expected facts/invariants so review can see resolved direct targets, unresolved function values, unsupported dynamic calls, provider/count evidence, and index evidence for `outgoing_by_function`, `outgoing_by_symbol`, `incoming_by_symbol`, `incoming_by_function`, `unresolved_by_reason`, and `unresolved_by_status` without reading raw debug JSON.</action>
  <acceptance_criteria>
    - `rg -n "by_language|by_call_kind|by_algorithm|by_status|by_unresolved_reason|by_provider" crates/polint/src/eval/fixtures.rs crates/polint/src/eval/mod.rs tests/eval-fixtures/direct-calls/core/expected.polint-eval.toml` finds count coverage.
    - `rg -n "outgoing_by_function|outgoing_by_symbol|incoming_by_symbol|incoming_by_function|unresolved_by_reason|unresolved_by_status" crates/polint/src/eval/fixtures.rs crates/polint/src/eval/mod.rs tests/eval-fixtures/direct-calls/core/expected.polint-eval.toml` finds D-10 index coverage.
    - `rg -n "Resolved|Unresolved|Unsupported|SetupMissing|DirectReference|ImportBinding|StaticMember" tests/eval-fixtures/direct-calls/core/expected.polint-eval.toml` finds status/algorithm coverage.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p polint --lib eval::fixtures::direct_calls_core --locked</automated>
    <automated>cargo test -p polint --lib eval::direct_call_rows --locked</automated>
    <automated>cargo fmt --all -- --check</automated>
  </verify>
  <done>Eval coverage proves direct-call counts, statuses, algorithms, D-10 indexes, and deterministic fixture dispatch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture repo -> kernel | Checked-in fixture files are copied to temp dirs and analyzed. |
| debug JSON -> eval expected rows | Test-only debug data becomes fixture assertions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-07-01 | T | fixture copy | mitigate | Reuse existing native fixture copy path with traversal rejection; do not mutate checked-in fixture repo. |
| T-30-07-02 | R | fixture expected rows | mitigate | Assert exact provider/status/precision/count/index labels for resolved and unresolved facts. |
| T-30-07-03 | I | eval expected payloads | mitigate | Use compact payload fragments and fixture labels; forbid raw source, AST dumps, absolute paths, and dense IDs as identity. |
</threat_model>

<verification>
Run:
```bash
cargo test -p polint --lib eval::fixtures::direct_calls_core --locked
cargo test -p polint --lib eval::direct_call_rows --locked
cargo fmt --all -- --check
```
</verification>

<success_criteria>
- Native fixtures cover the Phase 30 call taxonomy across Go and TS/JS.
- Fixture output is deterministic across cold/warm/no-cache runs.
- Fixture coverage proves D-10 index dimensions and D-18 counts are populated.
</success_criteria>

<output>
After completion, create `.planning/phases/30-direct-call-facts/30-07-SUMMARY.md`
</output>
