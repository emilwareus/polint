---
phase: 30-direct-call-facts
plan: 06
type: execute
wave: 6
depends_on: ["30-05"]
files_modified:
  - crates/polint/src/eval/model.rs
  - crates/polint/src/eval/observed.rs
  - crates/polint/src/eval/metrics.rs
  - crates/polint/src/eval/matcher.rs
  - crates/polint/src/eval/report.rs
  - crates/polint/src/eval/mod.rs
autonomous: true
requirements: [SAE-SEM-05]
must_haves:
  truths:
    - "Direct-call debug rows normalize into deterministic eval fact observations."
    - "Eval status accounting treats unresolved, unsupported, and setup-missing call rows as unknown-like evidence."
    - "Eval observation preserves safe compact payloads without raw source, AST, absolute-path, or dense-ID identity."
  artifacts:
    - path: "crates/polint/src/eval/observed.rs"
      provides: "Call debug JSON to eval fact observation"
      contains: "call_facts_for_test"
    - path: "crates/polint/src/eval/metrics.rs"
      provides: "Unknown-like status accounting for call statuses"
      contains: "setup_missing"
  key_links:
    - from: "crates/polint/src/eval/observed.rs"
      to: "crates/polint/src/analysis_kernel/debug.rs"
      via: "reads `metadata_debug_json_for_test()[\"calls\"]` rows"
      pattern: "calls"
    - from: "crates/polint/src/eval/metrics.rs"
      to: "crates/polint/src/eval/model.rs"
      via: "call status labels are counted consistently"
      pattern: "setup_missing"
---

<objective>
Add internal eval observation for direct call facts.

Purpose: Phase success depends on deterministic evidence that direct call facts and unresolved statuses work across Go and TS/JS.
Output: eval model/observed/matcher/report updates for call fact rows and status accounting.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-direct-call-facts/30-CONTEXT.md
@research/call-graphs/VALIDATION.md
@crates/polint/src/analysis_kernel/debug.rs
@crates/polint/src/eval/model.rs
@crates/polint/src/eval/observed.rs
@crates/polint/src/eval/fixtures.rs
@tests/eval-fixtures/semantic-mir/core/expected.polint-eval.toml
@tests/eval-fixtures/cfg-core/expected.polint-eval.toml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Teach eval to observe call debug rows</name>
  <files>crates/polint/src/eval/model.rs, crates/polint/src/eval/observed.rs, crates/polint/src/eval/metrics.rs, crates/polint/src/eval/matcher.rs, crates/polint/src/eval/report.rs, crates/polint/src/eval/mod.rs</files>
  <read_first>
    - `.planning/phases/30-direct-call-facts/30-CONTEXT.md`
    - `crates/polint/src/analysis_kernel/debug.rs`
    - `crates/polint/src/eval/model.rs`
    - `crates/polint/src/eval/observed.rs`
    - `crates/polint/src/eval/metrics.rs`
    - `crates/polint/src/eval/matcher.rs`
    - `crates/polint/src/eval/report.rs`
  </read_first>
  <behavior>
    - Test 1: eval accepts expected fact families `CallSite`, `CallTarget`, and `UnresolvedCall` with producer `polint.calls`.
    - Test 2: observed call debug rows normalize to compact payload fact rows with status labels `resolved`, `ambiguous`, `unresolved`, `unsupported`, and `setup_missing`.
    - Test 3: unresolved/unsupported/setup-missing call statuses count as unknown-like evidence in metrics.
  </behavior>
  <action>Extend the crate-private eval model with fixture area `direct-calls`, call fact families, and call status labels. Add `call_facts_for_test` and internal `call_facts(debug_json)` in `eval/observed.rs`, reading `metadata_debug_json_for_test()["calls"]["sites"|"targets"|"unresolved"]`. Payload fragments should include relative path, span, kind, algorithm, status, reason, provider, and compact target identity; no raw source, AST dumps, absolute paths, or dense IDs as identity per D-18 and D-19. Update matcher/metrics/report so unresolved-like call statuses are counted as unknown evidence.</action>
  <acceptance_criteria>
    - `rg -n "DirectCalls|direct-calls|CallSite|CallTarget|UnresolvedCall|call_facts_for_test" crates/polint/src/eval` finds eval model and observation.
    - `rg -n "setup_missing|unresolved|unsupported" crates/polint/src/eval/metrics.rs crates/polint/src/eval/matcher.rs crates/polint/src/eval/report.rs` finds status accounting.
    - `rg -n "source_text|absolute_path|raw_source|ast" crates/polint/src/eval/observed.rs` returns no new call payload identity inputs.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p polint --lib eval::direct_call_rows --locked</automated>
  </verify>
  <done>Internal eval can observe and score call fact families from test-only debug JSON.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| debug JSON -> eval rows | Test-only debug data is normalized into fixture observations. |
| call status labels -> metrics | Internal status labels influence eval outcome classification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-06-01 | I | eval payloads | mitigate | Use compact payload fragments; forbid raw source, AST dumps, absolute paths, and dense IDs as identity. |
| T-30-06-02 | T | status accounting | mitigate | Treat unresolved, unsupported, and setup-missing labels as unknown-like evidence consistently in matcher/metrics/report. |
</threat_model>

<verification>
Run:
```bash
cargo test -p polint --lib eval::direct_call_rows --locked
cargo fmt --all -- --check
```
</verification>

<success_criteria>
- Internal eval supports call facts and unknown-like call statuses.
- Eval observation payloads are compact and deterministic.
</success_criteria>

<output>
After completion, create `.planning/phases/30-direct-call-facts/30-06-SUMMARY.md`
</output>
