---
phase: 30-direct-call-facts
plan: 04
type: execute
wave: 4
depends_on: ["30-03"]
files_modified:
  - crates/polint/src/analysis/calls/mod.rs
  - crates/polint/src/analysis/calls/extract.rs
  - crates/polint/src/analysis/calls/unresolved.rs
  - crates/polint/src/analysis/calls/provider.rs
  - crates/polint/src/analysis/calls/store.rs
autonomous: true
requirements: [SAE-SEM-05]
must_haves:
  truths:
    - "Every MIR call operation becomes a normalized call-site fact for Go and TS/JS."
    - "Function values and unsupported/dynamic call shapes produce first-class unresolved evidence."
    - "Call-site stable keys are deterministic and unrelated-file-stable."
  artifacts:
    - path: "crates/polint/src/analysis/calls/extract.rs"
      provides: "MIR call operation to CallSiteFact extraction"
      contains: "extract_call_sites"
    - path: "crates/polint/src/analysis/calls/unresolved.rs"
      provides: "Unresolved call evidence derivation"
      contains: "derive_unresolved_calls"
    - path: "crates/polint/src/analysis/calls/provider.rs"
      provides: "Provider output populated with call sites and unresolved rows"
      contains: "extract_call_sites"
  key_links:
    - from: "crates/polint/src/analysis/calls/extract.rs"
      to: "crates/polint/src/analysis/mir/op.rs"
      via: "reads `MirOperationKind::Call` rows"
      pattern: "MirOperationKind::Call"
    - from: "crates/polint/src/analysis/calls/unresolved.rs"
      to: "crates/polint/src/analysis/mir/op.rs"
      via: "maps `UnsupportedDomain::Calls` and call-shaped unknowns to unresolved rows"
      pattern: "UnsupportedDomain::Calls"
---

<objective>
Derive complete internal call-site facts and unresolved-call evidence from semantic MIR.

Purpose: Direct targets only make sense once call sites and missing-target evidence are complete and queryable.
Output: MIR-driven call-site extraction, stable-key construction, unresolved evidence rows, and provider publication.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-direct-call-facts/30-CONTEXT.md
@research/call-graphs/implementation/BOOTSTRAP-INTEGRATION.md
@research/call-graphs/languages/go.md
@research/call-graphs/languages/typescript-javascript.md
@crates/polint/src/analysis/calls/facts.rs
@crates/polint/src/analysis/calls/store.rs
@crates/polint/src/analysis/calls/provider.rs
@crates/polint/src/analysis/mir/op.rs
@crates/polint/src/analysis/mir/body.rs
@crates/polint/src/analysis/places.rs
@crates/polint/src/core/mod.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract call-site facts from MIR operations</name>
  <files>crates/polint/src/analysis/calls/mod.rs, crates/polint/src/analysis/calls/extract.rs, crates/polint/src/analysis/calls/provider.rs</files>
  <read_first>
    - `.planning/phases/30-direct-call-facts/30-CONTEXT.md`
    - `crates/polint/src/analysis/calls/facts.rs`
    - `crates/polint/src/analysis/calls/store.rs`
    - `crates/polint/src/analysis/calls/provider.rs`
    - `crates/polint/src/analysis/mir/op.rs`
    - `crates/polint/src/analysis/mir/body.rs`
    - `crates/polint/src/analysis/places.rs`
    - planned new file: `crates/polint/src/analysis/calls/extract.rs`
  </read_first>
  <behavior>
    - Test 1: Go and TS/JS MIR `MirOperationKind::Call` rows produce `CallSiteFact` rows with language, file, caller, body, operation, span, syntax kind, callee shape, arguments, result place, status, precision, and stable key.
    - Test 2: call-site stable keys include language, file stable key, caller stable key, span, callee shape, MIR operation stable key or same-span ordinal, and call kind.
    - Test 3: extraction output is deterministic when MIR operations arrive in different input orders.
  </behavior>
  <action>Create `analysis/calls/extract.rs` with `extract_call_sites(db: &AnalysisDb) -> Vec<CallSiteFact>`. Iterate stored MIR bodies/operations and select `MirOperationKind::Call` per D-07. Recover the owning `MirBody` and function/file/language; map `MirValue::Place`, `MirValue::Temporary`, `MirValue::CallReturn`, and `MirValue::Unknown` to `CallCallee` variants without reparsing source. Set call syntax kind conservatively from language plus callee/place shape: `Function`, `Method`, `Constructor`, `Member`, `DynamicImport`, `Require`, `FunctionValue`, or `Unknown`. Generate stable keys per D-09 using metadata stable keys for source file/caller/MIR op where available and deterministic same-span ordinal as fallback. Wire provider output to include extracted sites before unresolved/target derivation.</action>
  <acceptance_criteria>
    - `rg -n "extract_call_sites|MirOperationKind::Call|CallSiteFact" crates/polint/src/analysis/calls/extract.rs crates/polint/src/analysis/calls/provider.rs` finds extraction and provider wiring.
    - `rg -n "same_span|operation.*stable_key|callee_shape|CallSyntaxKind" crates/polint/src/analysis/calls/extract.rs` finds stable-key inputs.
    - `rg -n "tree_sitter|oxc_ast|AstNode|Node<'_|raw source" crates/polint/src/analysis/calls/extract.rs` returns no parser AST dependency.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p polint --lib analysis::calls::extract --locked</automated>
    <automated>cargo test -p polint --lib calls_provider --locked</automated>
  </verify>
  <done>MIR call operations are published as deterministic call-site facts with no raw parser dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit unresolved-call rows for function values and unsupported dynamic calls</name>
  <files>crates/polint/src/analysis/calls/mod.rs, crates/polint/src/analysis/calls/unresolved.rs, crates/polint/src/analysis/calls/provider.rs</files>
  <read_first>
    - `.planning/phases/30-direct-call-facts/30-CONTEXT.md`
    - `research/call-graphs/FINAL-REPORT.md`
    - `crates/polint/src/analysis/calls/facts.rs`
    - `crates/polint/src/analysis/calls/extract.rs`
    - `crates/polint/src/analysis/calls/provider.rs`
    - `crates/polint/src/analysis/mir/op.rs`
    - `crates/polint/src/analysis/mir/lower_go.rs`
    - `crates/polint/src/analysis/mir/lower_ts.rs`
    - planned new file: `crates/polint/src/analysis/calls/unresolved.rs`
  </read_first>
  <behavior>
    - Test 1: function-value calls produce unresolved rows with reason `FunctionValue` and status `Unresolved` or `Unknown`.
    - Test 2: TS/JS `eval`, dynamic property/index calls, dynamic import, `call`/`apply`/`bind`, proxies/getters/setters/decorators/framework dispatch evidence produce specific unsupported/unresolved reasons when represented in MIR/unsupported rows.
    - Test 3: Go interface dispatch, reflection, goroutine call edge requests, function values, and setup-missing package evidence produce specific unsupported/unresolved reasons when represented in MIR/unsupported rows.
  </behavior>
  <action>Create `analysis/calls/unresolved.rs` with `derive_unresolved_calls(db, sites) -> Vec<UnresolvedCallFact>`. For each call site with `CallCallee::FunctionValue`, `CallCallee::Unknown`, dynamic/index/member shapes without direct reference evidence, or related `UnsupportedSemanticFact` rows where `affected_domains` contains `UnsupportedDomain::Calls`, emit one unresolved row with source evidence, status, reason, missing input when known, and stable key per D-05, D-13, D-14, and D-15. Preserve specific reasons instead of a generic unknown: `FunctionValue`, `DynamicProperty`, `DynamicImport`, `Eval`, `CallApplyBind`, `InterfaceDispatch`, `Reflection`, `GoroutineBoundary`, `SetupMissing`, `FrameworkDispatch`, `UnsupportedSyntax`, and `UnknownCallee`. Ensure unresolved rows are countable/indexed via `CallStore` and included in provider output digest/debug snapshots.</action>
  <acceptance_criteria>
    - `rg -n "derive_unresolved_calls|FunctionValue|DynamicProperty|DynamicImport|Eval|CallApplyBind|InterfaceDispatch|Reflection|GoroutineBoundary|SetupMissing|FrameworkDispatch|UnsupportedSyntax|UnknownCallee" crates/polint/src/analysis/calls/unresolved.rs` finds required reasons.
    - `rg -n "UnsupportedDomain::Calls|UnsupportedSemanticFact" crates/polint/src/analysis/calls/unresolved.rs` confirms unsupported MIR evidence is consumed.
    - `rg -n "UnresolvedCallFact" crates/polint/src/analysis/calls/provider.rs crates/polint/src/analysis/calls/store.rs` confirms provider/store publication.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p polint --lib analysis::calls::unresolved --locked</automated>
    <automated>cargo test -p polint --lib analysis_kernel::validation::calls --locked</automated>
  </verify>
  <done>Unresolved/dynamic/unsupported calls are explicit facts with concrete reasons and source evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove call-site indexes and debug counts on populated output</name>
  <files>crates/polint/src/analysis/calls/store.rs, crates/polint/src/analysis/calls/provider.rs, crates/polint/src/analysis_kernel/debug.rs</files>
  <read_first>
    - `.planning/phases/30-direct-call-facts/30-CONTEXT.md`
    - `crates/polint/src/analysis/calls/store.rs`
    - `crates/polint/src/analysis/calls/provider.rs`
    - `crates/polint/src/analysis_kernel/debug.rs`
  </read_first>
  <behavior>
    - Test 1: provider output with populated sites/unresolved rows builds `sites_by_caller`, `targets_by_site`, `unresolved_by_reason`, and `unresolved_by_status`.
    - Test 2: debug counts include non-zero language/kind/status/reason counts for call-site-only and unresolved rows.
    - Test 3: repeated provider runs over the same fixture produce identical call output digests.
  </behavior>
  <action>Add focused provider/store/debug tests using synthetic MIR bodies and operations or an existing minimal temp DB fixture. Assert populated site and unresolved output flows through `db.replace_call_facts`, `CallStore` indexes, `calls_output_digest`, and `metadata_debug_json_for_test()["calls"]["counts"]` per D-10 and D-18. The store/debug assertions must explicitly cover `unresolved_by_reason` and `unresolved_by_status`; keep outgoing/incoming target index coverage for the direct-target plan where target rows exist. Fix any missing store/index/debug plumbing exposed by these tests.</action>
  <acceptance_criteria>
    - `rg -n "sites_by_caller|targets_by_site|unresolved_by_reason|unresolved_by_status|calls_debug_json.*counts|calls_output_digest" crates/polint/src/analysis/calls/store.rs crates/polint/src/analysis/calls/provider.rs crates/polint/src/analysis_kernel/debug.rs` finds exercised paths.
    - Test output from the automated commands includes `ok` for `analysis::calls` and `calls_debug_json`.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p polint --lib analysis::calls --locked</automated>
    <automated>cargo test -p polint --lib analysis_kernel::debug::calls_debug_json --locked</automated>
    <automated>cargo fmt --all -- --check</automated>
  </verify>
  <done>Populated call-site/unresolved outputs are indexed, digestible, and visible through internal debug counts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MIR -> calls | Call sites are derived from private MIR and place rows. |
| unsupported semantics -> unresolved calls | Unsupported rows become countable unresolved-call evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-04-01 | T | `extract_call_sites` | mitigate | Use MIR IDs plus stable keys and validate body/op/place references; do not parse raw AST text. |
| T-30-04-02 | R | unresolved reasons | mitigate | Preserve specific reason enums and source evidence so unsupported/dynamic calls are auditable. |
| T-30-04-03 | I | debug counts | mitigate | Counts expose only labels and aggregate numbers in test-only JSON; no source bodies or absolute paths. |
</threat_model>

<verification>
Run:
```bash
cargo test -p polint --lib analysis::calls --locked
cargo test -p polint --lib calls_provider --locked
cargo test -p polint --lib analysis_kernel::debug::calls_debug_json --locked
cargo fmt --all -- --check
```
</verification>

<success_criteria>
- Go and TS/JS MIR calls produce call-site facts.
- Function-value/dynamic/unsupported forms produce unresolved evidence with reasons.
- Call-site and unresolved indexes/debug counts work with populated provider output, including unresolved by reason and status.
</success_criteria>

<output>
After completion, create `.planning/phases/30-direct-call-facts/30-04-SUMMARY.md`
</output>
