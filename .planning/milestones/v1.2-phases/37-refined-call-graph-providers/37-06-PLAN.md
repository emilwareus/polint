---
phase: 37-refined-call-graph-providers
plan: 06
title: Validation, Debug, Eval Fixtures, And Public No-Leak Proof
type: execute
wave: 4
depends_on: [37-03, 37-04, 37-05]
files_modified:
  - crates/polint/src/analysis/refined_calls/validate.rs
  - crates/polint/src/analysis/refined_calls/debug.rs
  - crates/polint/src/analysis/refined_calls/mod.rs
  - crates/polint/src/analysis_kernel/validation.rs
  - crates/polint/src/analysis_kernel/debug.rs
  - crates/polint/src/analysis_kernel/mod.rs
  - crates/polint/src/eval/model.rs
  - crates/polint/src/eval/fixtures.rs
  - tests/eval-fixtures/refined-calls/direct-vs-refined/expected.polint-eval.toml
  - tests/eval-fixtures/refined-calls/extension-model/expected.polint-eval.toml
autonomous: true
requirements: [SAE-PREC-02]
must_haves:
  truths:
    - "D-21 is covered: validation checks dangling references, duplicate stable keys, precision claims, provenance, synthetic targets, extension precision, and graph/index cycles."
    - "D-22/D-23 are covered: debug and eval report direct-vs-refined edges, unresolved reductions, statuses, provenance, budgets, and extension/model deltas."
    - "D-25 is covered: refined call internals do not leak to public CLI JSON/help, SDK, runner, README, or docs/facts."
    - "All Phase 37 roadmap success criteria are covered by native fixtures and eval suites."
  artifacts:
    - path: "crates/polint/src/analysis/refined_calls/validate.rs"
      provides: "Refined call validation rules"
    - path: "tests/eval-fixtures/refined-calls/direct-vs-refined/expected.polint-eval.toml"
      provides: "Native direct-versus-refined eval proof"
    - path: "tests/eval-fixtures/refined-calls/extension-model/expected.polint-eval.toml"
      provides: "Default-vs-extended model delta proof"
---

<objective>
Close Phase 37 with validation, debug output, eval fixtures, provider determinism, and public no-leak proof.
</objective>

<execution_context>
@.planning/phases/37-refined-call-graph-providers/37-CONTEXT.md
@.planning/phases/37-refined-call-graph-providers/37-PATTERNS.md
@.agents/skills/rust-best-practices/SKILL.md
@AGENTS.md
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add refined call validation</name>
  <files>crates/polint/src/analysis/refined_calls/validate.rs, crates/polint/src/analysis/refined_calls/mod.rs, crates/polint/src/analysis_kernel/validation.rs</files>
  <read_first>
    - crates/polint/src/analysis_kernel/validation.rs
    - crates/polint/src/analysis/entrypoints/validate.rs
    - crates/polint/src/analysis/calls/validate.rs
    - crates/polint/src/analysis/refined_calls/facts.rs
  </read_first>
  <action>Create validation for refined call edges: dangling call-site/base-target/function/symbol/type/value/allocation/points-to/entrypoint/extension references, invalid spans if spans are stored, duplicate stable keys, impossible precision claims, missing provenance, malformed synthetic target keys, extension precision ceiling violations, unsupported exact dynamic dispatch, and graph/index cycles if graph nodes are materialized. Wire it into kernel validation.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::refined_calls::validate --locked</automated>
    <automated>cargo test -p polint --lib analysis_kernel::validation --locked</automated>
  </verify>
  <acceptance_criteria>
    - Validation catches a dangling call site.
    - Validation catches an exact edge from heuristic framework or generated-unvalidated extension provenance.
    - Validation catches duplicate stable keys deterministically.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add debug rows and eval observation</name>
  <files>crates/polint/src/analysis/refined_calls/debug.rs, crates/polint/src/analysis_kernel/debug.rs, crates/polint/src/eval/model.rs, crates/polint/src/eval/fixtures.rs</files>
  <read_first>
    - crates/polint/src/analysis_kernel/debug.rs
    - crates/polint/src/eval/model.rs
    - crates/polint/src/eval/fixtures.rs
    - crates/polint/src/analysis/refined_calls/facts.rs
  </read_first>
  <action>Add test-facing debug rows and eval observation for refined calls. Debug output must include counts by language if available, algorithm, tier, status, precision, provenance, unresolved reason, budget status, and default-vs-extended delta. Eval model must include a refined-calls fixture area or graph-edge/fact family labels that can assert direct-versus-refined edges without public CLI exposure.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis_kernel::debug --locked</automated>
    <automated>cargo test -p polint --lib eval --locked</automated>
  </verify>
  <acceptance_criteria>
    - Debug JSON contains refined edge counts by algorithm/status/provenance.
    - Debug JSON contains no absolute paths, raw source bodies, timestamps, or parser object IDs.
    - Eval observation can assert refined call edges and unresolved/budget invariants.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add native and extension eval fixtures</name>
  <files>tests/eval-fixtures/refined-calls/direct-vs-refined/expected.polint-eval.toml, tests/eval-fixtures/refined-calls/extension-model/expected.polint-eval.toml, crates/polint/src/eval/fixtures.rs</files>
  <read_first>
    - tests/eval-fixtures/direct-calls/core/expected.polint-eval.toml
    - tests/eval-fixtures/framework-entrypoints/mixed-go-ts/expected.polint-eval.toml
    - tests/eval-fixtures/type-value-alias/ts-js-core/expected.polint-eval.toml
    - tests/eval-fixtures/extension/real-sink/expected.polint-eval.toml
  </read_first>
  <action>Add refined call eval fixtures. `direct-vs-refined` must include Go and TS/JS cases that measure direct edge count versus refined edge count, precision/status on every refined edge, framework dispatch provenance, function-token/value refinement, receiver/type filtering, and unresolved/budget rows. `extension-model` must include one accepted extension/model refined edge and one rejected malformed target, showing default-vs-extended edge delta and no native override.</action>
  <verify>
    <automated>cargo test -p polint --lib eval_native_fixture_runner_refined_calls_fixture_passes --locked</automated>
    <automated>cargo test -p polint --lib eval_native_fixture_suite_covers_required_categories --locked</automated>
  </verify>
  <acceptance_criteria>
    - Native fixture asserts at least one direct-versus-refined edge delta.
    - Native fixture asserts precision and status for every refined edge.
    - Extension fixture asserts accepted and rejected model facts separately.
    - Suite category coverage includes refined calls or equivalent graph/facts category coverage.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Add public no-leak proof and full validation</name>
  <files>crates/polint/src/analysis_kernel/mod.rs</files>
  <read_first>
    - crates/polint/src/analysis_kernel/mod.rs
    - crates/polint/src/analysis/refined_calls/facts.rs
    - crates/polint/src/analysis_kernel/provider.rs
    - docs/API-VISIBILITY-PLAN.md
    - AGENTS.md
  </read_first>
  <action>Add or extend no-leak tests to assert public surfaces do not contain private refined-call markers: `polint.refined_calls`, `RefinedCallEdgeFact`, `RefinedCallTier`, `RefinedCallReason`, `RefinedCallGraph`, `refined_call_edges`, `direct_plus_framework`, `points_to_assisted`, `extension_model`, and any module/function names introduced by this phase. Scan normal `polint check --format json`, CLI help, `lib.rs`, SDK prelude/module, runner, README, and `docs/facts`. Then run final package/workspace validation commands.</action>
  <verify>
    <automated>cargo fmt --all --check</automated>
    <automated>cargo test -p polint --lib refined_call --locked</automated>
    <automated>cargo test -p polint --test cli --locked -- checked_in_examples_are_runnable_cli_fixtures</automated>
    <automated>cargo clippy --workspace --all-targets --locked -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - No-leak test scans CLI JSON/help, SDK, runner, README, and docs/facts.
    - Public outputs do not contain refined call provider IDs or private type names.
    - `cargo fmt --all --check`, refined-call tests, CLI fixture test, and workspace clippy pass.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| private refined graph -> public surfaces | Refined graph internals must remain private until Phase 41 promotion. |
| untrusted extension/model facts -> eval/debug | Rejected facts must be observable internally but not merged as accepted refined edges. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T37-12 | Information Disclosure | public CLI/SDK/docs | Mitigated | Add no-leak tests over all public surfaces. |
| T37-13 | Repudiation | refined edge provenance | Mitigated | Require provenance and evidence on every edge. |
| T37-14 | Tampering | extension refined edges | Mitigated | Validate and quarantine extension/model contributions. |
</threat_model>

<verification>
- `cargo fmt --all --check`
- `cargo test -p polint --lib refined_call --locked`
- `cargo test -p polint --lib eval_native_fixture_suite_covers_required_categories --locked`
- `cargo test -p polint --test cli --locked -- checked_in_examples_are_runnable_cli_fixtures`
- `cargo clippy --workspace --all-targets --locked -- -D warnings`
</verification>

<success_criteria>
- Native fixtures and eval suites measure direct versus refined edges.
- Precision and status are attached to every refined edge.
- Dynamic dispatch and framework edges retain provenance.
- Unresolved and budget-exceeded statuses remain explicit.
- Public surfaces do not expose private refined call internals.
</success_criteria>
