---
phase: 37-refined-call-graph-providers
plan: 01
title: Private Refined Call Fact Contracts And Store
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/polint/src/analysis/ids.rs
  - crates/polint/src/analysis/mod.rs
  - crates/polint/src/analysis/refined_calls/mod.rs
  - crates/polint/src/analysis/refined_calls/facts.rs
  - crates/polint/src/analysis/refined_calls/store.rs
  - crates/polint/src/analysis_kernel/metadata.rs
autonomous: true
requirements: [SAE-PREC-02]
must_haves:
  truths:
    - "D-01/D-02 are covered: refined calls are a crate-private layer over call_sites/call_targets/unresolved_calls, not a replacement for polint.calls."
    - "D-03/D-04 are covered: every refined edge preserves CallSiteId and carries algorithm, tier, status, precision, provenance, validation/confidence, reason, and evidence."
    - "D-05/D-17/D-18 are covered: refined call output normalizes by stable key and graph indexes are rebuildable views over stored facts."
    - "D-11 is covered: store/query APIs expose internal tier filters for direct-only, direct-plus-framework, type/value/function-token, summary-assisted, points-to-assisted, extension/model, and all accepted refined edges."
    - "SAE-PREC-02 is covered for fact shape: every edge can carry precision/status/provenance and unresolved/budget states."
  artifacts:
    - path: "crates/polint/src/analysis/refined_calls/facts.rs"
      provides: "RefinedCallEdgeFact and graph/tier/status vocabulary"
    - path: "crates/polint/src/analysis/refined_calls/store.rs"
      provides: "Normalized output and indexes by site, caller, callee, status, algorithm, provenance, and tier"
---

<objective>
Define the private refined call fact layer, deterministic output container, store indexes, IDs, and metadata families.

Purpose: give later Phase 37 plans a stable internal vocabulary for refined edges without changing the existing direct call provider or public API.
</objective>

<execution_context>
@.planning/phases/37-refined-call-graph-providers/37-CONTEXT.md
@.planning/phases/37-refined-call-graph-providers/37-PATTERNS.md
@.agents/skills/rust-best-practices/SKILL.md
@AGENTS.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@crates/polint/src/analysis/calls/facts.rs
@crates/polint/src/analysis/calls/store.rs
@crates/polint/src/analysis/entrypoints/facts.rs
@crates/polint/src/analysis/types/facts.rs
@crates/polint/src/analysis/values/facts.rs
@crates/polint/src/analysis/points_to/facts.rs
@crates/polint/src/analysis_kernel/metadata.rs
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add refined call IDs and module registration</name>
  <files>crates/polint/src/analysis/ids.rs, crates/polint/src/analysis/mod.rs, crates/polint/src/analysis/refined_calls/mod.rs</files>
  <read_first>
    - .agents/skills/rust-best-practices/SKILL.md
    - AGENTS.md
    - crates/polint/src/analysis/ids.rs
    - crates/polint/src/analysis/mod.rs
    - crates/polint/src/analysis/calls/mod.rs
  </read_first>
  <action>Add crate-private dense IDs `RefinedCallEdgeId` and, if needed for graph views, `RefinedCallGraphNodeId` using the existing `pub(crate) struct X(pub(crate) u64)` pattern. Register `pub(crate) mod refined_calls;` in `analysis/mod.rs`. Create `analysis/refined_calls/mod.rs` exposing crate-private `facts` and `store` modules only. Do not modify `lib.rs`, `sdk`, `runner`, CLI, README, or `docs/facts`.</action>
  <verify>
    <automated>cargo test -p polint --lib refined_call --locked</automated>
    <automated>rg -n "pub mod refined_calls|RefinedCalls&lt;'_|CallGraph&lt;'_" crates/polint/src/lib.rs crates/polint/src/sdk crates/polint/src/runner README.md docs || true</automated>
  </verify>
  <acceptance_criteria>
    - `crates/polint/src/analysis/ids.rs` contains `RefinedCallEdgeId`.
    - `crates/polint/src/analysis/mod.rs` contains `pub(crate) mod refined_calls;`.
    - No public SDK, runner, README, or docs file exposes `RefinedCalls<'_>` or `CallGraph<'_>`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define refined edge vocabulary</name>
  <files>crates/polint/src/analysis/refined_calls/facts.rs</files>
  <read_first>
    - crates/polint/src/analysis/calls/facts.rs
    - crates/polint/src/analysis/entrypoints/facts.rs
    - crates/polint/src/analysis/types/facts.rs
    - crates/polint/src/analysis/values/facts.rs
    - crates/polint/src/analysis/points_to/facts.rs
  </read_first>
  <action>Define `RefinedCallEdgeFact` with fields for `id`, `site: CallSiteId`, `base_target: Option<CallTargetId>`, `caller: FunctionId`, optional target function/symbol/synthetic target key, edge kind, algorithm, tier, status, reason, provenance, precision, validation/confidence, evidence strings, input stable keys, and `stable_key`. Define enums for `RefinedCallTier` with direct_plus_framework, type_value_function_token, summary_assisted, points_to_assisted, extension_model, and all_accepted-equivalent variants; `RefinedCallReason` covering framework dispatch, function value, receiver type filter, summary projection, points-to, extension model, setup missing, unsupported, unknown, and budget exceeded; and status/precision/provenance enums that map cleanly to existing `CallTargetStatus`, `CallPrecision`, and `CallProvenance`.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::refined_calls::facts --locked</automated>
  </verify>
  <acceptance_criteria>
    - `RefinedCallEdgeFact` has `site: CallSiteId` and `base_target: Option<CallTargetId>`.
    - The tier enum includes framework, type/value/function-token, summary-assisted, points-to-assisted, and extension/model tiers.
    - Status vocabulary includes resolved, ambiguous, unresolved, unsupported, setup-missing, budget-exceeded, and rejected-equivalent states.
    - Unit tests prove dense IDs and stable keys remain separate.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add normalized output and store indexes</name>
  <files>crates/polint/src/analysis/refined_calls/store.rs</files>
  <read_first>
    - crates/polint/src/analysis/calls/store.rs
    - crates/polint/src/analysis/refined_calls/facts.rs
  </read_first>
  <action>Create `RefinedCallOutput { edges: Vec<RefinedCallEdgeFact> }` with `normalized()` sorting by stable key, site, tier, algorithm, provenance, status, and id. Create `RefinedCallStore::from_output` that validates duplicate stable keys are deterministic, builds indexes by site, caller function, target function, target symbol, status, algorithm, provenance, and tier, and exposes query helpers returning `Vec<&RefinedCallEdgeFact>`. Keep graph indexes as rebuildable views over edge facts.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::refined_calls::store --locked</automated>
  </verify>
  <acceptance_criteria>
    - Store tests prove output order is stable regardless of insertion order.
    - Store tests prove indexes return framework and points-to-assisted edges by tier.
    - Store tests prove duplicate stable key or dangling base call reference errors are deterministic.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Register metadata fact families</name>
  <files>crates/polint/src/analysis_kernel/metadata.rs</files>
  <read_first>
    - crates/polint/src/analysis_kernel/metadata.rs
    - crates/polint/src/analysis/refined_calls/facts.rs
  </read_first>
  <action>Add a `FactFamily` variant and label for refined call edges, and graph node/view families only if Plan 01 introduces stored node facts. Ensure metadata stable-key helpers can create producer metadata for `polint.refined_calls` without widening public APIs.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis_kernel::metadata --locked</automated>
  </verify>
  <acceptance_criteria>
    - `FactFamily` includes refined call edge metadata support.
    - Metadata tests pass and no `unreachable_pub` is introduced.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| private facts -> public API | Refined call internals must remain crate-private and absent from SDK/CLI/docs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T37-01 | Information Disclosure | refined call contracts | Mitigated | Keep modules `pub(crate)` and defer public views to Phase 41. |
| T37-02 | Tampering | graph indexes | Mitigated | Rebuild indexes from normalized facts and reject duplicate/dangling references. |
</threat_model>

<verification>
- `cargo fmt --all --check`
- `cargo test -p polint --lib analysis::refined_calls --locked`
- `cargo test -p polint --lib analysis_kernel::metadata --locked`
</verification>

<success_criteria>
- Private refined call contracts and stores exist.
- Direct call facts remain unchanged as the baseline substrate.
- The plan covers D-01 through D-05, D-17, D-18, and SAE-PREC-02 fact-shape requirements.
</success_criteria>
