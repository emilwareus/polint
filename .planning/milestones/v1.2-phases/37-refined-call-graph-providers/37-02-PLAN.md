---
phase: 37-refined-call-graph-providers
plan: 02
title: Refined Call Provider, Cache Identity, And Kernel Wiring
type: execute
wave: 2
depends_on: [37-01]
files_modified:
  - crates/polint/src/analysis/refined_calls/cache_key.rs
  - crates/polint/src/analysis/refined_calls/provider.rs
  - crates/polint/src/analysis/refined_calls/mod.rs
  - crates/polint/src/core/mod.rs
  - crates/polint/src/analysis_kernel/provider.rs
  - crates/polint/src/analysis_kernel/mod.rs
  - tests/eval-fixtures/kernel/provider-order/expected.polint-eval.toml
autonomous: true
requirements: [SAE-PREC-02]
must_haves:
  truths:
    - "D-05 is covered: refined call provider output has deterministic digest and metadata before validation/debug/eval use."
    - "D-06/D-20 are covered: refined provider runs after direct calls, summaries, entrypoints, extensions, and type_value_alias without making refined calls an upstream requirement for direct summaries."
    - "D-21 is covered: provider manifest/order is explicit and deterministic."
  artifacts:
    - path: "crates/polint/src/analysis/refined_calls/provider.rs"
      provides: "derive_refined_calls_with_cache_stats provider entrypoint"
    - path: "crates/polint/src/analysis_kernel/provider.rs"
      provides: "polint.refined_calls manifest after polint.type_value_alias and before polint.metrics"
---

<objective>
Wire the refined call provider into `AnalysisDb`, provider manifests, kernel execution, cache identity, and provider-order eval expectations.
</objective>

<execution_context>
@.planning/phases/37-refined-call-graph-providers/37-CONTEXT.md
@.planning/phases/37-refined-call-graph-providers/37-PATTERNS.md
@.agents/skills/rust-best-practices/SKILL.md
@AGENTS.md
</execution_context>

<context>
@crates/polint/src/analysis/calls/provider.rs
@crates/polint/src/analysis/types/provider.rs
@crates/polint/src/analysis_kernel/provider.rs
@crates/polint/src/analysis_kernel/mod.rs
@crates/polint/src/core/mod.rs
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add AnalysisDb storage and accessors</name>
  <files>crates/polint/src/core/mod.rs</files>
  <read_first>
    - crates/polint/src/core/mod.rs
    - crates/polint/src/analysis/calls/store.rs
    - crates/polint/src/analysis/refined_calls/store.rs
  </read_first>
  <action>Add a private refined call store field to `AnalysisDb`, plus `replace_refined_call_facts`, `refined_call_edges`, and index accessor helpers mirroring the calls store pattern. The replacement method must refresh metadata for refined call edge facts through the metadata family added in Plan 37-01 and return `AnalysisError` on invalid store output.</action>
  <verify>
    <automated>cargo test -p polint --lib core:: --locked</automated>
  </verify>
  <acceptance_criteria>
    - `AnalysisDb` exposes crate-private refined edge accessors.
    - `replace_refined_call_facts` exists and is used by the provider in this plan.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add provider cache key and empty deterministic provider</name>
  <files>crates/polint/src/analysis/refined_calls/cache_key.rs, crates/polint/src/analysis/refined_calls/provider.rs, crates/polint/src/analysis/refined_calls/mod.rs</files>
  <read_first>
    - crates/polint/src/analysis/calls/provider.rs
    - crates/polint/src/analysis/types/cache_key.rs
    - crates/polint/src/analysis/refined_calls/facts.rs
    - crates/polint/src/analysis/refined_calls/store.rs
  </read_first>
  <action>Create `REFINED_CALLS_SCHEMA_LABEL` and provider parameter digest including enabled tiers, per-tier budgets, provider/schema version, config digest, upstream output digests, model components, extension components, lifecycle/toolchain components, and absent sentinels. Implement `derive_refined_calls_with_cache_stats` that initially can emit an empty normalized output but computes a deterministic digest from all required inputs and writes the output through `replace_refined_call_facts`.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::refined_calls::provider --locked</automated>
  </verify>
  <acceptance_criteria>
    - Provider digest changes when a tier/budget parameter changes.
    - Provider digest includes extension and model input components.
    - Empty output records `call_output=empty` or refined-call equivalent in the digest input.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Register manifest and kernel order</name>
  <files>crates/polint/src/analysis_kernel/provider.rs, crates/polint/src/analysis_kernel/mod.rs, tests/eval-fixtures/kernel/provider-order/expected.polint-eval.toml</files>
  <read_first>
    - crates/polint/src/analysis_kernel/provider.rs
    - crates/polint/src/analysis_kernel/mod.rs
    - tests/eval-fixtures/kernel/provider-order/expected.polint-eval.toml
  </read_first>
  <action>Add a `polint.refined_calls` provider manifest after `polint.type_value_alias` and before `polint.metrics`. Inputs must include direct call facts, summary facts, entrypoint/dispatch/unresolved framework facts, extension facts/rejections, type facts, value facts, access paths, points-to sets, alias answers, source/config/lifecycle, and model/extension/tool sentinels. Outputs must include `refined_call_edges` and any graph-view fact labels created in Plan 37-01. Add the kernel run step using the output digests from upstream providers and push provider output metadata to `KernelRunReport`. Update provider-order eval fixture expectations.</action>
  <verify>
    <automated>cargo test -p polint --lib provider_order_matches_behavior_preserving_kernel_sequence --locked</automated>
    <automated>cargo test -p polint --lib eval_observed_kernel_collects_provider_order_invariants_from_real_kernel --locked</automated>
  </verify>
  <acceptance_criteria>
    - Provider order includes `polint.refined_calls` after `polint.type_value_alias`.
    - Kernel report includes refined call provider output metadata.
    - Existing provider order tests and eval fixture pass after fixture update.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| extension/model inputs -> refined provider | Extension-influenced refined calls must remain provenance-rich and quarantine-aware. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T37-03 | Tampering | cache identity | Mitigated | Include upstream digests, extension/model components, tier settings, and budgets. |
| T37-04 | Denial of Service | provider execution | Mitigated | Keep provider empty/deterministic first and add budgeted algorithms in later plans. |
</threat_model>

<verification>
- `cargo fmt --all --check`
- `cargo test -p polint --lib analysis::refined_calls --locked`
- `cargo test -p polint --lib provider_order --locked`
</verification>

<success_criteria>
- `polint.refined_calls` is a deterministic private provider.
- Provider order avoids dependency cycles.
- Cache identity includes all inputs that affect refined call behavior.
</success_criteria>
