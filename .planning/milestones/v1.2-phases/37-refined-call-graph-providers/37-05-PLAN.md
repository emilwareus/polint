---
phase: 37-refined-call-graph-providers
plan: 05
title: TS/JS Function-Token, Points-To, And Extension Model Refinements
type: execute
wave: 3
depends_on: [37-02]
files_modified:
  - crates/polint/src/analysis/refined_calls/ts_js.rs
  - crates/polint/src/analysis/refined_calls/extensions.rs
  - crates/polint/src/analysis/refined_calls/provider.rs
  - crates/polint/src/analysis/refined_calls/mod.rs
  - crates/polint/src/analysis/extensions/sinks.rs
autonomous: true
requirements: [SAE-PREC-02]
must_haves:
  truths:
    - "D-09 is covered: TS/JS refinements consume function-token/value facts, allocation facts, import/export bindings, framework dispatch facts, call-return facts, and bounded points-to information."
    - "D-12 through D-16 are covered: repo-local model edges flow through extension validation, provenance, precision ceilings, and quarantine-aware input identity."
    - "D-13 is covered: extension/model edges carry distinct Extension, Model, or RepoModel provenance plus extension id/provider id/model id where available, and eval reports default-vs-extended changed edges plus unresolved reduction or explicit no-change."
    - "D-14 is covered: extension facts may add refined target edges, framework dispatch edges, function/API summaries, no-alias/must-alias evidence, receiver/type hints, or synthetic callable targets only after binding validation or declared synthetic identity validation."
    - "D-15 is covered: extension/model conflicts with native facts or other extensions become validation diagnostics or quarantine events rather than silent native overrides."
    - "SAE-PREC-02 is covered for function tokens, bounded points-to constraints, extension/model facts, and explicit unresolved/budget statuses."
  artifacts:
    - path: "crates/polint/src/analysis/refined_calls/ts_js.rs"
      provides: "TS/JS function-token and value-flow refined call candidates"
    - path: "crates/polint/src/analysis/refined_calls/extensions.rs"
      provides: "Validated extension/model refined call edge candidates"
---

<objective>
Add TS/JS function-token/value refinements and repo-local extension/model refined call participation with validation and precision ceilings.
</objective>

<execution_context>
@.planning/phases/37-refined-call-graph-providers/37-CONTEXT.md
@.planning/phases/37-refined-call-graph-providers/37-PATTERNS.md
@.agents/skills/rust-best-practices/SKILL.md
@AGENTS.md
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add TS/JS function-token refinement</name>
  <files>crates/polint/src/analysis/refined_calls/ts_js.rs, crates/polint/src/analysis/refined_calls/mod.rs</files>
  <read_first>
    - crates/polint/src/analysis/calls/facts.rs
    - crates/polint/src/analysis/values/facts.rs
    - crates/polint/src/analysis/types/facts.rs
    - crates/polint/src/analysis/access_paths/facts.rs
    - crates/polint/src/analysis/refined_calls/facts.rs
  </read_first>
  <action>Create a TS/JS refinement function that resolves `FunctionValue` and callable-object call sites using `ValueKind::FunctionObject`, `ValueKind::PlaceRef`, `ValueKind::CallReturn`, callable type shapes, and import/export binding evidence. Emit `FunctionTokenFlow` tier edges when evidence supports a callable target. For dynamic property keys, `eval`, proxies/accessors, `call/apply/bind`, unresolved imports, and setup gaps, emit explicit unknown/unresolved rows instead of exact targets.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::refined_calls::ts_js --locked</automated>
  </verify>
  <acceptance_criteria>
    - A function value assigned then called can produce a function-token refined edge.
    - Dynamic property and `eval` cases remain unresolved/unknown with reason evidence.
    - TS/JS refined edges carry function-token or type/value tier labels, not direct-call labels.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add bounded points-to assistance for TS/JS</name>
  <files>crates/polint/src/analysis/refined_calls/ts_js.rs</files>
  <read_first>
    - crates/polint/src/analysis/points_to/facts.rs
    - crates/polint/src/analysis/points_to/vars.rs
    - crates/polint/src/analysis/values/facts.rs
    - crates/polint/src/analysis/refined_calls/ts_js.rs
  </read_first>
  <action>Use within-budget points-to sets to refine callable object/property targets only when object/function token evidence binds to known stable facts. For absent, unsupported, unknown, or budget-exceeded points-to rows, produce explicit unknown/budgeted refined rows and do not broaden to every possible function.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::refined_calls::ts_js --locked</automated>
  </verify>
  <acceptance_criteria>
    - Within-budget points-to rows can improve a TS/JS callable target.
    - Budget-exceeded rows produce budget evidence and no exact target.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add extension/model refined call fact payloads</name>
  <files>crates/polint/src/analysis/extensions/sinks.rs, crates/polint/src/analysis/refined_calls/extensions.rs</files>
  <read_first>
    - crates/polint/src/analysis/extensions/sinks.rs
    - crates/polint/src/analysis/extensions/validate.rs
    - crates/polint/src/analysis/refined_calls/facts.rs
  </read_first>
  <action>Add a refined-call extension fact family label such as `refined_calls.edge` or `call_graph.refined_edge`. Validate required payload labels: `site=call_site:<id-or-stable-key>`, one of `target_function=`, `target_symbol=`, or `synthetic_target=`, `algorithm=repo_model|framework_model|function_token|points_to`, and `status=resolved|ambiguous|unresolved|budget_exceeded|rejected`. Create `refined_calls::extensions` to translate accepted extension candidates into refined edge candidates with extension/model provenance and precision ceilings. Rejected malformed candidates must remain rejected and not enter the refined store.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::extensions::sinks --locked</automated>
    <automated>cargo test -p polint --lib analysis::refined_calls::extensions --locked</automated>
  </verify>
  <acceptance_criteria>
    - Extension sink validation rejects missing site or missing target payloads.
    - Accepted extension refined edges carry extension id/provider id evidence.
    - Generated-unvalidated or heuristic extension facts cannot become exact refined edges.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Wire TS/JS and extension refinements into provider</name>
  <files>crates/polint/src/analysis/refined_calls/provider.rs</files>
  <read_first>
    - crates/polint/src/analysis/refined_calls/provider.rs
    - crates/polint/src/analysis/refined_calls/ts_js.rs
    - crates/polint/src/analysis/refined_calls/extensions.rs
  </read_first>
  <action>Merge TS/JS and extension/model refined outputs into the provider. Include extension/model stable keys in output digest input and keep default-vs-extended deltas distinguishable by provenance and tier.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::refined_calls --locked</automated>
  </verify>
  <acceptance_criteria>
    - Provider emits TS/JS function-token refined edges when inputs exist.
    - Provider emits accepted extension/model refined edges and excludes rejected candidates.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo-local extension -> refined call facts | Extension code is untrusted until protocol, payload, precision, and binding validation pass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T37-09 | Spoofing | extension model targets | Mitigated | Require existing stable call-site/target bindings or declared synthetic identities. |
| T37-10 | Tampering | extension precision | Mitigated | Apply extension precision ceilings and reject malformed payloads. |
| T37-11 | Denial of Service | TS/JS points-to refinement | Mitigated | Use bounded points-to only and preserve budget-exceeded rows. |
</threat_model>

<verification>
- `cargo fmt --all --check`
- `cargo test -p polint --lib analysis::refined_calls::ts_js --locked`
- `cargo test -p polint --lib analysis::refined_calls::extensions --locked`
- `cargo test -p polint --lib analysis::extensions::sinks --locked`
</verification>

<success_criteria>
- TS/JS function-token refinements work for concrete value facts.
- Extension/model refined call facts are validated and provenance-rich.
- Dynamic TS/JS uncertainty remains explicit.
</success_criteria>
