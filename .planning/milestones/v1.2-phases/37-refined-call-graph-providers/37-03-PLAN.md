---
phase: 37-refined-call-graph-providers
plan: 03
title: Framework Dispatch And Summary-Assisted Refinements
type: execute
wave: 3
depends_on: [37-02]
files_modified:
  - crates/polint/src/analysis/refined_calls/framework.rs
  - crates/polint/src/analysis/refined_calls/summaries.rs
  - crates/polint/src/analysis/refined_calls/provider.rs
  - crates/polint/src/analysis/refined_calls/mod.rs
autonomous: true
requirements: [SAE-PREC-02]
must_haves:
  truths:
    - "D-07/D-12/D-19 are covered: framework dispatch edges and summary facts can add refined edges while preserving direct-vs-refined deltas."
    - "D-10/D-24 are covered: unresolved, unsupported, setup-missing, and budget-exceeded statuses remain explicit."
    - "SAE-PREC-02 dynamic/framework provenance criterion is covered for framework edges."
  artifacts:
    - path: "crates/polint/src/analysis/refined_calls/framework.rs"
      provides: "FrameworkDispatchEdgeFact to refined call edge projection"
    - path: "crates/polint/src/analysis/refined_calls/summaries.rs"
      provides: "Summary-assisted refined call candidates"
---

<objective>
Add the first non-empty refined call vertical: framework dispatch edges and direct-summary hints become refined call edges with explicit provenance, tier, status, and evidence.
</objective>

<execution_context>
@.planning/phases/37-refined-call-graph-providers/37-CONTEXT.md
@.planning/phases/37-refined-call-graph-providers/37-PATTERNS.md
@.agents/skills/rust-best-practices/SKILL.md
@AGENTS.md
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Project framework dispatch facts into refined edges</name>
  <files>crates/polint/src/analysis/refined_calls/framework.rs, crates/polint/src/analysis/refined_calls/provider.rs, crates/polint/src/analysis/refined_calls/mod.rs</files>
  <read_first>
    - crates/polint/src/analysis/entrypoints/facts.rs
    - crates/polint/src/analysis/entrypoints/store.rs
    - crates/polint/src/analysis/calls/facts.rs
    - crates/polint/src/analysis/refined_calls/facts.rs
    - crates/polint/src/analysis/refined_calls/store.rs
  </read_first>
  <action>Create a framework refinement function that reads `FrameworkDispatchEdgeFact` rows and emits refined edges with `FrameworkModel` or equivalent algorithm/provenance, `direct_plus_framework` tier, source evidence containing the dispatch edge stable key, target function/symbol where available, and explicit unresolved rows for unresolved framework facts that cannot bind to a target. Do not mark heuristic framework edges exact.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::refined_calls::framework --locked</automated>
  </verify>
  <acceptance_criteria>
    - A framework dispatch fact with a target function produces one refined call edge.
    - An unresolved framework fact produces an explicit unresolved or unknown refined row.
    - Framework refined edges carry framework/model provenance and non-exact precision.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add summary-assisted candidates</name>
  <files>crates/polint/src/analysis/refined_calls/summaries.rs, crates/polint/src/analysis/refined_calls/provider.rs</files>
  <read_first>
    - crates/polint/src/analysis/summaries/facts.rs
    - crates/polint/src/analysis/summaries/store.rs
    - crates/polint/src/analysis/calls/facts.rs
    - crates/polint/src/analysis/refined_calls/facts.rs
  </read_first>
  <action>Create a summary-assisted refinement pass that uses existing summary call/control/TITO or summary event facts only when they point to concrete function/call stable keys already present in the call store. Emit `SummaryAssisted` tier edges with evidence containing summary stable keys. If summary data is absent or cannot bind to a known call site, emit no edge and no optimistic fallback.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::refined_calls::summaries --locked</automated>
  </verify>
  <acceptance_criteria>
    - Summary-assisted tests prove only bindable summary facts create refined edges.
    - Missing summary facts do not create broad guessed edges.
    - Summary-assisted edges are distinguishable from direct and framework edges.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Merge framework and summary outputs deterministically</name>
  <files>crates/polint/src/analysis/refined_calls/provider.rs</files>
  <read_first>
    - crates/polint/src/analysis/refined_calls/provider.rs
    - crates/polint/src/analysis/refined_calls/framework.rs
    - crates/polint/src/analysis/refined_calls/summaries.rs
  </read_first>
  <action>Update the provider to collect framework and summary-assisted outputs, normalize them through `RefinedCallOutput`, include their stable keys in the output digest, and write them through `replace_refined_call_facts`. Duplicate edge candidates for the same site/target/tier/algorithm should dedupe or validate deterministically, with evidence merged in sorted order if merging is chosen.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::refined_calls::provider --locked</automated>
  </verify>
  <acceptance_criteria>
    - Provider tests prove framework and summary edges are sorted deterministically.
    - Duplicate evidence is sorted and deduped or rejected deterministically.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| framework recognizer facts -> refined call graph | Framework recognizers are heuristic/setup-aware and must not claim exactness. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T37-05 | Spoofing | synthetic framework edges | Mitigated | Require evidence stable keys and target binding checks. |
| T37-06 | Information Disclosure | debug evidence | Mitigated | Store stable keys and metadata, not raw source bodies. |
</threat_model>

<verification>
- `cargo fmt --all --check`
- `cargo test -p polint --lib analysis::refined_calls --locked`
</verification>

<success_criteria>
- Framework dispatch and summary-assisted refined edges exist.
- Dynamic/framework provenance is explicit.
- Unknown/unresolved framework behavior remains observable.
</success_criteria>
