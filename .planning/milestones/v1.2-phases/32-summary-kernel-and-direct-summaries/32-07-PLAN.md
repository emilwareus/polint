---
phase: 32-summary-kernel-and-direct-summaries
plan: 07
type: execute
wave: 5
depends_on: [32-06]
files_modified:
  - crates/polint/tests/cli.rs
  - crates/polint/src/analysis_kernel/incremental/run_report.rs
autonomous: true
requirements: [SAE-INT-02]

must_haves:
  truths:
    - "Public CLI check JSON does not contain summary domain names, summary stable keys, or summary internal vocabulary"
    - "polint inspect and polint test JSON do not expose summary internals"
    - "CLI help text does not advertise summary commands or flags"
    - "SDK prelude, runner, README, and docs/facts do not export summary types"
    - "External temp-repo rule using polint::sdk::prelude::* compiles without summary access"
    - "Provider order expectations include polint.direct_summaries between abstract_domains and metrics"
  artifacts:
    - path: "crates/polint/tests/cli.rs"
      provides: "Public no-leak integration test for direct summary internals"
  key_links:
    - from: "crates/polint/tests/cli.rs"
      to: "crates/polint/src/analysis/summaries/"
      via: "proves private internals do not leak to public surfaces"
      pattern: "summary.*stay.*private|direct_summaries_internals"
---

<objective>
Prove that direct summary internals remain private: public CLI JSON, inspect, test, help, SDK exports, runner, README, and docs do not expose or advertise summary vocabulary. Update provider-order expectations for run-report tests.

Purpose: This is the public-boundary proof required by D-04 and D-17. It ensures that summary internals can evolve freely without breaking any public contract.
Output: External CLI integration test proving no-leak boundary, updated provider-order assertions.
</objective>

<execution_context>
@/Users/emilwareus/conductor/workspaces/exlint/islamabad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/conductor/workspaces/exlint/islamabad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-summary-kernel-and-direct-summaries/32-CONTEXT.md
@.planning/phases/32-summary-kernel-and-direct-summaries/32-PATTERNS.md
@.planning/phases/32-summary-kernel-and-direct-summaries/32-06-SUMMARY.md
@.planning/phases/31-p0-abstract-domain-kernel/31-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update provider-order expectations for polint.direct_summaries</name>
  <files>
    crates/polint/src/analysis_kernel/incremental/run_report.rs,
    crates/polint/src/analysis_kernel/mod.rs
  </files>
  <read_first>
    crates/polint/src/analysis_kernel/incremental/run_report.rs,
    crates/polint/src/analysis_kernel/mod.rs
  </read_first>
  <action>
    Search for existing provider-order assertions or manifest-list expectations in run_report.rs and mod.rs that list provider IDs (the pattern from Phase 31 Plan 05 where polint.abstract_domains was added between calls and metrics).

    Update all provider-order test assertions to include "polint.direct_summaries" between "polint.abstract_domains" and "polint.metrics". This includes:
    - Any test that asserts the full provider manifest order list
    - Any test that asserts index positions of providers
    - Any test that checks provider_outputs count

    The expected order should now be: polint.source, polint.go.syntax, polint.ts.syntax, polint.module_graph, polint.symbol_graph, polint.module_topology, polint.semantic_mir, polint.cfg, polint.calls, polint.abstract_domains, polint.direct_summaries, polint.metrics.

    Run focused tests to verify provider-order assertions pass.
  </action>
  <verify>
    <automated>cargo test -p polint --lib kernel_run_report --locked 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All provider-order assertions include "polint.direct_summaries" between "polint.abstract_domains" and "polint.metrics"
    - cargo test -p polint --lib kernel_run_report --locked exits 0
  </acceptance_criteria>
  <done>Provider-order expectations updated to include polint.direct_summaries in the correct position.</done>
</task>

<task type="auto">
  <name>Task 2: Add public no-leak boundary proof for direct summary internals</name>
  <files>
    crates/polint/tests/cli.rs
  </files>
  <read_first>
    crates/polint/tests/cli.rs
  </read_first>
  <action>
    Add integration test direct_summaries_internals_stay_private to tests/cli.rs following the abstract_domain_internals_stay_private pattern (per D-17):

    1. Create a temp-repo with a minimal .polint.toml, a Go file with a simple function, and a TS file with a simple function.
    2. Run polint check --format json and capture output.
    3. Assert public JSON output does NOT contain:
       - "direct_summaries" or "DirectSummaries"
       - "summary_control" or "summary_call" or "summary_memory" or "summary_tito"
       - "SummaryDomain" or "SummaryStore" or "SummaryBuilder"
       - "control_effects" as a summary domain label
       - "memory_effects" as a summary domain label
       - "data_flow_tito" as a summary domain label
       - "ControlEffects" or "CallEffects" or "MemoryEffects" or "DataFlowTito" (Rust type names)
    4. Run polint inspect rule --format json (if available) and assert no summary vocabulary.
    5. Run polint check --help and assert no summary-related flags or commands.
    6. Scan public SDK source files (crates/polint/src/sdk/) for summary type exports. Assert none found.
    7. Scan runner source files for summary type exports. Assert none found.
    8. Scan README.md and docs/facts/ for summary vocabulary. Assert none found.

    Also create a minimal external temp-repo rule test that imports only polint::sdk::prelude::* and polint::runner::run_cli, compiles, and runs without accessing any summary types. This proves the rule-author workflow remains compatible.

    If any summary vocabulary leaks into public output, the test must fail, and the fix must tighten visibility or filter output.
  </action>
  <verify>
    <automated>cargo test -p polint --test cli direct_summaries_internals_stay_private --locked 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/cli.rs contains direct_summaries_internals_stay_private test
    - Public check JSON does not contain summary domain names or internal vocabulary
    - CLI help does not advertise summary commands
    - SDK and runner sources do not export summary types
    - README and docs/facts do not mention summary internals
    - External temp-repo rule compiles using only polint::sdk::prelude::*
    - cargo test -p polint --test cli direct_summaries_internals_stay_private --locked exits 0
  </acceptance_criteria>
  <done>Public boundary proof confirms direct summary internals do not leak to CLI, SDK, runner, docs, or external rule consumers.</done>
</task>

<task type="auto">
  <name>Task 3: Run full verification across all targets</name>
  <files></files>
  <read_first>
    crates/polint/src/analysis/summaries/mod.rs
  </read_first>
  <action>
    Run the comprehensive verification suite to confirm Phase 32 does not break existing behavior:
    1. cargo test -p polint --all-targets --locked
    2. cargo fmt --all -- --check
    3. Verify all existing CLI integration tests still pass (especially checked_in_examples_are_runnable_cli_fixtures, syntax_cache_ignores_unrelated_rule_edits, semantic_mir_internals_stay_private, cfg_public_no_leak, direct_calls_internals_stay_private, abstract_domain_internals_stay_private).

    If any existing test fails due to the new provider (e.g., provider count changes, stale provider-order assertions missed in earlier plans), fix them before completing.
  </action>
  <verify>
    <automated>cargo test -p polint --all-targets --locked 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - cargo test -p polint --all-targets --locked exits 0
    - cargo fmt --all -- --check exits 0
    - All prior phase CLI integration tests pass
    - No summary vocabulary in public output
  </acceptance_criteria>
  <done>Full test suite passes, all prior phase behaviors preserved, direct summary internals confirmed private.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public CLI output | Untrusted consumers see only public JSON contract |
| SDK prelude | Rule authors import only curated public types |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-10 | Information Disclosure | public CLI JSON | mitigate | No-leak test asserts summary vocabulary absent from public output |
| T-32-11 | Information Disclosure | SDK exports | mitigate | Source scan confirms no summary types in sdk/prelude or runner |
| T-32-SC | Tampering | no package installs | accept | No new dependencies added |
</threat_model>

<verification>
cargo test -p polint --test cli direct_summaries_internals_stay_private --locked
cargo test -p polint --all-targets --locked
cargo fmt --all -- --check
</verification>

<success_criteria>
- Public boundary proof test passes
- All existing tests pass with new provider
- No regression in CLI, SDK, or eval behavior
</success_criteria>

<output>
Create `.planning/phases/32-summary-kernel-and-direct-summaries/32-07-SUMMARY.md` when done
</output>
