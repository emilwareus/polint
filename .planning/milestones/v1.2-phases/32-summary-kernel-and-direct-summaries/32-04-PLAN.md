---
phase: 32-summary-kernel-and-direct-summaries
plan: 04
type: execute
wave: 3
depends_on: [32-02, 32-03]
files_modified:
  - crates/polint/src/analysis/summaries/provider.rs
  - crates/polint/src/analysis/summaries/cache_key.rs
  - crates/polint/src/analysis_kernel/provider.rs
  - crates/polint/src/analysis_kernel/mod.rs
  - crates/polint/src/analysis_kernel/incremental/keys.rs
autonomous: true
requirements: [SAE-INT-02]

must_haves:
  truths:
    - "polint.direct_summaries provider runs after polint.abstract_domains and before polint.metrics"
    - "Provider output digest includes upstream MIR, CFG, calls, domains, symbol graph, module topology, syntax digests"
    - "Cache identity includes provider/schema version, domain versions, config, lifecycle, absent extension/model/toolchain slots per D-14"
    - "Provider manifest declares private outputs for summary facts"
    - "LayerKind::DirectSummaries exists in the layer vocabulary"
  artifacts:
    - path: "crates/polint/src/analysis/summaries/provider.rs"
      provides: "derive_direct_summaries_with_cache_stats function"
    - path: "crates/polint/src/analysis/summaries/cache_key.rs"
      provides: "Summary provider parameter digest"
    - path: "crates/polint/src/analysis_kernel/provider.rs"
      provides: "polint.direct_summaries manifest entry"
    - path: "crates/polint/src/analysis_kernel/mod.rs"
      provides: "Kernel run wiring for summary provider"
    - path: "crates/polint/src/analysis_kernel/incremental/keys.rs"
      provides: "LayerKind::DirectSummaries and LayerKey constructor"
  key_links:
    - from: "crates/polint/src/analysis/summaries/provider.rs"
      to: "crates/polint/src/analysis/summaries/builder.rs"
      via: "calls DirectSummaryBuilder::build"
      pattern: "DirectSummaryBuilder::build"
    - from: "crates/polint/src/analysis_kernel/mod.rs"
      to: "crates/polint/src/analysis/summaries/provider.rs"
      via: "kernel run sequence calls derive_direct_summaries_with_cache_stats"
      pattern: "derive_direct_summaries_with_cache_stats"
---

<objective>
Wire the summary provider into the kernel: add the polint.direct_summaries provider manifest, derive function, output digest computation, cache identity (parameter + layer key), kernel run-sequence integration, and provider order tests.

Purpose: This connects the summary builder to the kernel execution pipeline so summaries are computed after domains and before metrics, with correct cache identity including all upstream digest inputs.
Output: provider.rs, cache_key.rs, updated provider manifest, kernel run wiring, LayerKind::DirectSummaries.
</objective>

<execution_context>
@/Users/emilwareus/conductor/workspaces/exlint/islamabad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/conductor/workspaces/exlint/islamabad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-summary-kernel-and-direct-summaries/32-CONTEXT.md
@.planning/phases/32-summary-kernel-and-direct-summaries/32-PATTERNS.md
@.planning/phases/32-summary-kernel-and-direct-summaries/32-02-SUMMARY.md
@.planning/phases/32-summary-kernel-and-direct-summaries/32-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add summary provider manifest, parameter digest, and LayerKind::DirectSummaries</name>
  <files>
    crates/polint/src/analysis/summaries/cache_key.rs,
    crates/polint/src/analysis/summaries/mod.rs,
    crates/polint/src/analysis_kernel/provider.rs,
    crates/polint/src/analysis_kernel/incremental/keys.rs
  </files>
  <read_first>
    crates/polint/src/analysis/domains/cache_key.rs,
    crates/polint/src/analysis_kernel/provider.rs,
    crates/polint/src/analysis_kernel/incremental/keys.rs,
    crates/polint/src/analysis/summaries/mod.rs,
    crates/polint/src/analysis/summaries/core.rs,
    crates/polint/src/analysis/summaries/domain.rs
  </read_first>
  <action>
    Create analysis/summaries/cache_key.rs following domains/cache_key.rs pattern (per D-14):
    - pub(crate) fn direct_summaries_provider_parameter_digest() -> Digest: include schema label "direct-summary-facts-1:1", all four domain IDs and versions (ControlEffects::ID, ControlEffects::VERSION, CallEffects::ID, CallEffects::VERSION, MemoryEffects::ID, MemoryEffects::VERSION, DataFlowTito::ID, DataFlowTito::VERSION). Use Digest::from_parts(DigestKind::ProviderParameters, "direct_summaries_parameters", &refs).
    - Add test: parameters_change_when_domain_versions_change.

    Add pub(crate) mod cache_key to summaries/mod.rs.

    Add LayerKind::DirectSummaries variant to the LayerKind enum in analysis_kernel/incremental/keys.rs. Place it after AbstractDomains and before Metrics. Add serde rename to "direct_summaries".

    Add LayerKey::direct_summaries_layer_key constructor following the abstract_domains_layer_key pattern: takes manifest, source digests, config digest, lifecycle digests, upstream MIR/CFG/calls/domains/symbol_graph/module_topology output digests, parameter digest. Constructs LayerKey with LayerKind::DirectSummaries.

    Add polint.direct_summaries provider manifest entry to PROVIDER_MANIFESTS in analysis_kernel/provider.rs. Place it after polint.abstract_domains and before polint.metrics.
    - id: "polint.direct_summaries"
    - kind: ProviderKind::WholeRepoDerived
    - inputs: source_files, functions, mir_bodies, mir_operations, places, unsupported_semantics, cfg_functions, basic_blocks, cfg_edges, call_sites, call_targets, unresolved_calls, domain_observations, domain_events
    - outputs: summary_control, summary_call, summary_memory, summary_tito, summary_events
    - language_scope: LanguageScope::MultiLanguage
    - cache_policy: CachePolicy::InMemoryDerived
    - schema_versions: DIRECT_SUMMARIES_SCHEMA (define const "direct-summary-facts-1:1")
    - precision_ceiling: PrecisionCeiling::SetupAware

    Add test: direct_summaries_provider_manifest_declares_private_outputs.
  </action>
  <verify>
    <automated>cargo test -p polint --lib direct_summaries_provider --locked 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - crates/polint/src/analysis/summaries/cache_key.rs contains direct_summaries_provider_parameter_digest() returning Digest
    - LayerKind::DirectSummaries exists in incremental/keys.rs
    - PROVIDER_MANIFESTS contains polint.direct_summaries after polint.abstract_domains and before polint.metrics
    - Manifest outputs include summary_control, summary_call, summary_memory, summary_tito, summary_events
    - cargo test -p polint --lib direct_summaries_provider --locked exits 0
  </acceptance_criteria>
  <done>Summary provider manifest, parameter digest, LayerKind, and LayerKey constructor exist with passing tests.</done>
</task>

<task type="auto">
  <name>Task 2: Wire summary provider into kernel run sequence with output digest and run-report</name>
  <files>
    crates/polint/src/analysis/summaries/provider.rs,
    crates/polint/src/analysis/summaries/mod.rs,
    crates/polint/src/analysis_kernel/mod.rs
  </files>
  <read_first>
    crates/polint/src/analysis/domains/provider.rs,
    crates/polint/src/analysis_kernel/mod.rs,
    crates/polint/src/analysis/summaries/builder.rs,
    crates/polint/src/analysis/summaries/store.rs,
    crates/polint/src/analysis/summaries/cache_key.rs,
    crates/polint/src/analysis/summaries/mod.rs
  </read_first>
  <action>
    Create analysis/summaries/provider.rs following domains/provider.rs pattern:

    DirectSummariesProviderOutput struct: diagnostics: Vec of Diagnostic, cache_stats: CacheStats, output_digest: Option of Digest.

    pub(crate) fn derive_direct_summaries_with_cache_stats: takes &mut AnalysisDb, &InputSnapshot, &ProviderManifest, and upstream output digests: semantic_mir_output_digest, cfg_output_digest, calls_output_digest, abstract_domains_output_digest (NEW upstream), symbol_graph_output_digest, module_topology_output_digest, upstream_syntax_output_digests.
    1. Call DirectSummaryBuilder::build(db) to get SummaryOutput.
    2. Build callable stable key map from db.mir_bodies().
    3. Compute output digest via direct_summaries_output_digest using manifest, input_snapshot, upstream digests, and output rows.
    4. Record cache_stats recompute.
    5. Call db.replace_summary_facts(output).
    6. Return DirectSummariesProviderOutput.

    Implement direct_summaries_output_digest following abstract_domains_output_digest pattern: include provider_id, provider_version, schema, parameters (from direct_summaries_provider_parameter_digest), config, upstream digests (semantic_mir, cfg, calls, abstract_domains, symbol_graph, module_topology), lifecycle components, absent model/extension/tool components, syntax digests, and normalized summary row stable keys/domain/status/precision/provenance/payload_digest.

    Add pub(crate) mod provider to summaries/mod.rs.

    Wire into analysis_kernel/mod.rs run method:
    - After the abstract_domains block (around line 299), before metrics.
    - Create abstract_domains_dependency_output_digest from abstract_domains_output_digest (or absent).
    - Call derive_direct_summaries_with_cache_stats with &mut db, &input_snapshot, Self::provider_manifest("polint.direct_summaries"), semantic_mir_dependency_output_digest, cfg_dependency_output_digest, calls_dependency_output_digest, abstract_domains_dependency_output_digest, symbol_dependency_output_digest, module_topology_dependency_output_digest, syntax digests.
    - Record cache stats, extend diagnostics, push provider output.

    Add tests:
    - direct_summaries_runs_after_abstract_domains_and_before_metrics (provider order test)
    - provider_accepts_empty_output_with_deterministic_digest
  </action>
  <verify>
    <automated>cargo test -p polint --lib direct_summaries_provider --locked 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - crates/polint/src/analysis/summaries/provider.rs contains derive_direct_summaries_with_cache_stats
    - Provider calls DirectSummaryBuilder::build and db.replace_summary_facts
    - Output digest includes abstract_domains_output_digest as upstream input
    - analysis_kernel/mod.rs calls derive_direct_summaries_with_cache_stats after abstract_domains and before metrics
    - Provider order test confirms: calls < abstract_domains < direct_summaries < metrics
    - cargo test -p polint --lib direct_summaries_provider --locked exits 0
  </acceptance_criteria>
  <done>Summary provider executes in kernel run sequence, computes output digest with all upstream inputs, stores results in AnalysisDb, and provider order tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kernel -> provider | Provider receives trusted internal AnalysisDb and InputSnapshot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-05 | Tampering | cache identity | mitigate | Cache identity includes all upstream digests plus absent future slots per D-14 |
| T-32-06 | Information Disclosure | provider output | accept | Output stays crate-private, no public JSON or CLI surface per D-04 |
| T-32-SC | Tampering | no package installs | accept | No new dependencies added |
</threat_model>

<verification>
cargo test -p polint --lib direct_summaries_provider --locked
cargo test -p polint --lib direct_summaries_layer_key --locked
cargo test -p polint --lib analysis::summaries --locked
cargo fmt --all -- --check
</verification>

<success_criteria>
- polint.direct_summaries provider runs after abstract_domains and before metrics
- Cache identity includes all upstream digests and absent future slots
- Output digest is deterministic for the same input
</success_criteria>

<output>
Create `.planning/phases/32-summary-kernel-and-direct-summaries/32-04-SUMMARY.md` when done
</output>
