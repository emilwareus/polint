---
phase: 32-summary-kernel-and-direct-summaries
plan: 06
type: execute
wave: 4
depends_on: [32-05]
files_modified:
  - crates/polint/src/eval/model.rs
  - crates/polint/src/eval/observed.rs
  - crates/polint/src/eval/fixtures.rs
  - crates/polint/src/eval/mod.rs
  - tests/eval-fixtures/direct-summaries/core/expected.polint-eval.toml
  - tests/eval-fixtures/direct-summaries/core/repo/.polint.toml
  - tests/eval-fixtures/direct-summaries/core/repo/summary.go
  - tests/eval-fixtures/direct-summaries/core/repo/go.mod
  - tests/eval-fixtures/direct-summaries/core/repo/web/package.json
  - tests/eval-fixtures/direct-summaries/core/repo/web/tsconfig.json
  - tests/eval-fixtures/direct-summaries/core/repo/web/src/summary.ts
autonomous: true
requirements: [SAE-INT-02]

must_haves:
  truths:
    - "Eval observation extracts summary facts from debug JSON into normalized eval rows"
    - "Native fixture covers Go and TS/JS functions with known control effects, call effects, memory effects, and TITO"
    - "Fixture covers unknown/top summaries for functions with unresolved calls"
    - "Determinism invariant passes: repeated runs produce identical summary output"
    - "Summary eval facts are internal, not public SDK or CLI contract"
  artifacts:
    - path: "crates/polint/src/eval/model.rs"
      provides: "FixtureArea::DirectSummaries and summary fact families"
    - path: "crates/polint/src/eval/observed.rs"
      provides: "Summary debug JSON normalization into eval facts"
    - path: "tests/eval-fixtures/direct-summaries/core/expected.polint-eval.toml"
      provides: "Native fixture manifest for direct summaries"
  key_links:
    - from: "crates/polint/src/eval/observed.rs"
      to: "crates/polint/src/analysis_kernel/debug.rs"
      via: "reads summaries section from metadata_debug_json_for_test"
      pattern: "summaries"
    - from: "crates/polint/src/eval/fixtures.rs"
      to: "tests/eval-fixtures/direct-summaries/core/"
      via: "native fixture runner loads and observes the fixture repo"
      pattern: "direct-summaries"
---

<objective>
Add eval observation for direct summary facts and a native mixed Go/TS fixture proving control effects, call effects, memory effects, TITO, unknown summaries, and determinism.

Purpose: Eval fixtures are the evidence that summary computation is correct and deterministic. They exercise the full pipeline from source through MIR/CFG/calls/domains to summary output.
Output: Eval model extensions, observed.rs normalization, native fixture repo with expected manifest.
</objective>

<execution_context>
@/Users/emilwareus/conductor/workspaces/exlint/islamabad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/conductor/workspaces/exlint/islamabad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-summary-kernel-and-direct-summaries/32-CONTEXT.md
@.planning/phases/32-summary-kernel-and-direct-summaries/32-PATTERNS.md
@.planning/phases/32-summary-kernel-and-direct-summaries/32-05-SUMMARY.md
@.planning/phases/31-p0-abstract-domain-kernel/31-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add direct-summary eval observation, fixture area, and fact-family vocabulary</name>
  <files>
    crates/polint/src/eval/model.rs,
    crates/polint/src/eval/observed.rs,
    crates/polint/src/eval/mod.rs
  </files>
  <read_first>
    crates/polint/src/eval/model.rs,
    crates/polint/src/eval/observed.rs,
    crates/polint/src/eval/mod.rs,
    crates/polint/src/analysis_kernel/debug.rs,
    crates/polint/src/analysis/summaries/facts.rs
  </read_first>
  <action>
    Extend eval/model.rs:
    - Add FixtureArea::DirectSummaries variant to the FixtureArea enum.
    - Add DIRECT_SUMMARY_FACT_FAMILIES const array: ["summary_control", "summary_call", "summary_memory", "summary_tito", "summary_event"]. Follow the ABSTRACT_DOMAIN_FACT_FAMILIES pattern.
    - Add parsing support for area = "direct-summaries" in the fixture manifest parser.

    Extend eval/observed.rs:
    - Add fn observe_direct_summaries(report: &serde_json::Value) -> Vec of ObservedItem:
      1. Read report["summaries"]["summaries"] array.
      2. For each row, extract callable_stable_key, domain, status, precision, provenance, payload_digest, stable_key.
      3. Map domain to fact family: "control_effects" -> "summary_control", "call_effects" -> "summary_call", "memory_effects" -> "summary_memory", "data_flow_tito" -> "summary_tito".
      4. Build ObservedFact with family, payload (compact semicolon format: "domain;status;precision;provenance;payload_digest_prefix"), and stable_key.
      5. Read report["summaries"]["events"] array similarly.
      6. Read report["summaries"]["counts"] and emit ObservedInvariant items for total, present, unknown counts.
      7. Read report["summaries"]["domain_counts"] and emit ObservedInvariant for each domain count.
    - Wire observe_direct_summaries into the fixture observation path: when fixture.manifest.area == FixtureArea::DirectSummaries, call observe_direct_summaries.

    Add eval/mod.rs tests:
    - direct_summary_fixture_area_parses_from_toml
    - direct_summary_fact_families_are_recognized
  </action>
  <verify>
    <automated>cargo test -p polint --lib eval::direct_summary --locked 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - FixtureArea::DirectSummaries exists in eval/model.rs
    - observe_direct_summaries reads summaries section from debug JSON and produces ObservedFact and ObservedInvariant items
    - Fact families map correctly: control_effects -> summary_control, etc.
    - cargo test -p polint --lib eval::direct_summary --locked exits 0
  </acceptance_criteria>
  <done>Eval observation normalizes summary debug JSON into compact eval rows with fact families and invariants.</done>
</task>

<task type="auto">
  <name>Task 2: Add native mixed Go/TS direct-summary fixture with expected manifest</name>
  <files>
    tests/eval-fixtures/direct-summaries/core/expected.polint-eval.toml,
    tests/eval-fixtures/direct-summaries/core/repo/.polint.toml,
    tests/eval-fixtures/direct-summaries/core/repo/summary.go,
    tests/eval-fixtures/direct-summaries/core/repo/go.mod,
    tests/eval-fixtures/direct-summaries/core/repo/web/package.json,
    tests/eval-fixtures/direct-summaries/core/repo/web/tsconfig.json,
    tests/eval-fixtures/direct-summaries/core/repo/web/src/summary.ts,
    crates/polint/src/eval/fixtures.rs
  </files>
  <read_first>
    tests/eval-fixtures/abstract-domains/core/expected.polint-eval.toml,
    tests/eval-fixtures/abstract-domains/core/repo/domain.go,
    tests/eval-fixtures/abstract-domains/core/repo/web/src/domain.ts,
    crates/polint/src/eval/fixtures.rs,
    crates/polint/src/eval/observed.rs
  </read_first>
  <action>
    Create the native fixture repo (per D-16):

    tests/eval-fixtures/direct-summaries/core/repo/go.mod: module example.com/summarytest, go 1.22.

    tests/eval-fixtures/direct-summaries/core/repo/summary.go: Go functions covering:
    - func AlwaysPanics() that calls panic("fail") -- control effect: Panics, DoesNotReturn.
    - func ReturnsParam(x int) int that returns x -- TITO: Param(0) -> Return Value.
    - func MutatesReceiver(s *State) that writes to s.field -- memory: receiver Write.
    - func ReadsGlobal() int that reads a package-level var -- memory: global Read.
    - func CallsUnresolved(fn func()) that calls fn() -- call effects: unresolved, unknown summaries.
    - func PureFunction(a, b int) int that returns a + b -- minimal effects.

    tests/eval-fixtures/direct-summaries/core/repo/web/package.json: minimal {"name": "summary-web"}.
    tests/eval-fixtures/direct-summaries/core/repo/web/tsconfig.json: minimal {"compilerOptions": {"strict": true}}.

    tests/eval-fixtures/direct-summaries/core/repo/web/src/summary.ts: TS functions covering:
    - function throwsError(): never { throw new Error("fail"); } -- control: Throws, DoesNotReturn.
    - function identity(x: number): number { return x; } -- TITO: Param(0) -> Return Value.
    - function writesParam(arr: number[], val: number): void { arr.push(val); } -- memory: param Write.
    - function callsDynamic(cb: () => void): void { cb(); } -- call effects: callback invoked.
    - function noEffects(): string { return "hello"; } -- minimal effects.

    tests/eval-fixtures/direct-summaries/core/repo/.polint.toml: empty config.

    tests/eval-fixtures/direct-summaries/core/expected.polint-eval.toml: follow TOML fixture format:
    - area = "direct-summaries"
    - assertion_mode = "subset" (subset matching since exact counts may vary)
    - Expected facts for: control effects with exit kinds, TITO with param-to-return flow, memory effects with access kinds, call effects with unresolved evidence, and unknown/top events.
    - Invariants: summary_count > 0, domain_count.control_effects > 0, domain_count.memory_effects > 0, domain_count.data_flow_tito > 0, domain_count.call_effects > 0.
    - Determinism invariant: output_deterministic = true.

    Wire the fixture in eval/fixtures.rs:
    - Add the direct-summaries fixture area to the fixture runner dispatch (same pattern as abstract-domains fixture: when FixtureArea::DirectSummaries, run kernel observation and extract summaries).
    - Add the fixture to the required fixture area coverage in suite_has_required_area_coverage (add FixtureArea::DirectSummaries to the required areas list).

    Add test:
    - direct_summaries_core_fixture_passes (integration test running the full fixture)
  </action>
  <verify>
    <automated>cargo test -p polint --lib eval::fixtures::direct_summaries_core --locked 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/eval-fixtures/direct-summaries/core/ directory exists with expected.polint-eval.toml, repo/.polint.toml, repo/summary.go, repo/go.mod, repo/web/src/summary.ts
    - Go fixture includes panic, param-return, receiver-mutation, global-read, unresolved-call, and pure functions
    - TS fixture includes throw, identity, param-write, dynamic-callback, and no-effects functions
    - Fixture manifest has area = "direct-summaries" and invariants for summary counts and domain counts
    - eval/fixtures.rs dispatches FixtureArea::DirectSummaries
    - cargo test -p polint --lib eval::fixtures::direct_summaries_core --locked exits 0
  </acceptance_criteria>
  <done>Native mixed Go/TS fixture exercises all four summary domains including unknown/top for unresolved calls and passes determinism invariant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture repo -> kernel | Fixture repos are test-controlled, no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-09 | Information Disclosure | eval observation | accept | Eval stays crate-private and test-facing, no public surface per D-04 |
| T-32-SC | Tampering | no package installs | accept | No new dependencies added |
</threat_model>

<verification>
cargo test -p polint --lib eval::direct_summary --locked
cargo test -p polint --lib eval::fixtures::direct_summaries_core --locked
cargo fmt --all -- --check
</verification>

<success_criteria>
- Eval observation normalizes summary debug JSON into compact eval rows
- Native fixture covers Go and TS functions for all four domains
- Unknown summaries produced for unresolved calls
- Determinism invariant passes
</success_criteria>

<output>
Create `.planning/phases/32-summary-kernel-and-direct-summaries/32-06-SUMMARY.md` when done
</output>
