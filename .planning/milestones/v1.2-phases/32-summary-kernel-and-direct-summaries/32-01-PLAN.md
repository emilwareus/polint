---
phase: 32-summary-kernel-and-direct-summaries
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/polint/src/analysis/summaries/mod.rs
  - crates/polint/src/analysis/summaries/domain.rs
  - crates/polint/src/analysis/summaries/core.rs
  - crates/polint/src/analysis/summaries/facts.rs
  - crates/polint/src/analysis/mod.rs
  - crates/polint/src/analysis/ids.rs
autonomous: true
requirements: [SAE-INT-02]

must_haves:
  truths:
    - "SummaryDomain trait exists with bottom, unknown_top, join, leq, stable_digest_parts"
    - "Four core summary domains implement SummaryDomain: ControlEffects, CallEffects, MemoryEffects, DataFlowTito"
    - "Each domain has explicit bottom and unknown_top(reason) constructors"
    - "Summary fact structs carry dense IDs, status, precision, stable keys"
    - "SummaryId and SummaryEventId newtypes exist with the standard ID contract"
  artifacts:
    - path: "crates/polint/src/analysis/summaries/mod.rs"
      provides: "Module root declaring all summary submodules"
    - path: "crates/polint/src/analysis/summaries/domain.rs"
      provides: "SummaryDomain trait and SummaryTopReason enum"
    - path: "crates/polint/src/analysis/summaries/core.rs"
      provides: "Four core domain implementations"
    - path: "crates/polint/src/analysis/summaries/facts.rs"
      provides: "Summary fact structs with enums for status, precision, provenance, flow kinds, access kinds, exit kinds"
  key_links:
    - from: "crates/polint/src/analysis/summaries/core.rs"
      to: "crates/polint/src/analysis/summaries/domain.rs"
      via: "impl SummaryDomain for each core domain type"
      pattern: "impl SummaryDomain for ControlEffects"
---

<objective>
Define the private summary kernel contracts: SummaryDomain trait, four core direct-summary domain types (ControlEffects, CallEffects, MemoryEffects, DataFlowTito), summary fact structs, summary ID newtypes, and all supporting enums.

Purpose: Establish the type vocabulary that the summary store, builder, provider, validation, and debug systems depend on. This plan defines contracts only -- no store, builder, or provider wiring.
Output: New analysis::summaries module with domain.rs, core.rs, facts.rs, and supporting types in ids.rs.
</objective>

<execution_context>
@/Users/emilwareus/conductor/workspaces/exlint/islamabad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/conductor/workspaces/exlint/islamabad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-summary-kernel-and-direct-summaries/32-CONTEXT.md
@.planning/phases/32-summary-kernel-and-direct-summaries/32-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add summary module root, SummaryDomain trait, SummaryTopReason, summary ID newtypes, and fact vocabulary enums</name>
  <files>
    crates/polint/src/analysis/summaries/mod.rs,
    crates/polint/src/analysis/summaries/domain.rs,
    crates/polint/src/analysis/summaries/facts.rs,
    crates/polint/src/analysis/mod.rs,
    crates/polint/src/analysis/ids.rs
  </files>
  <read_first>
    crates/polint/src/analysis/domains/lattice.rs,
    crates/polint/src/analysis/domains/mod.rs,
    crates/polint/src/analysis/domains/facts.rs,
    crates/polint/src/analysis/mod.rs,
    crates/polint/src/analysis/ids.rs,
    crates/polint/src/analysis_kernel/incremental/keys.rs
  </read_first>
  <action>
    Create analysis/summaries/mod.rs declaring pub(crate) submodules: domain, core, facts (plus future store, builder, provider, validate, cache_key which will be added in later plans). Use cfg_attr dead_code expectation as in analysis/domains/mod.rs since consumers arrive in later plans.

    Create analysis/summaries/domain.rs (per D-02):
    - SummaryTopReason enum mirroring TopReason from domains/lattice.rs: UnresolvedCallee, UnsupportedSemantic, DynamicWrite, SetupMissing, BudgetExceeded, MissingDependency, ConflictingFacts. Add as_str method.
    - SummaryDomain trait: const ID: &'static str, const VERSION: u32, fn bottom() -> Self, fn unknown_top(reason: SummaryTopReason) -> Self, fn is_bottom(&self) -> bool, fn is_top(&self) -> bool, fn leq(&self, other: &Self) -> bool, fn join(&self, other: &Self) -> Self, fn join_into(&mut self, incoming: &Self) -> Changed (with default impl like AbstractDomain), fn stable_digest_parts(&self) -> Vec of String. Trait bounds: Clone + Send + Sync + Eq + 'static.
    - Changed enum (Yes/No) reused or re-declared locally.
    - Add trait law tests: bottom leq everything, top geq everything, join is commutative, join is idempotent, join_into returns Changed::No when result unchanged.

    Create analysis/summaries/facts.rs (per D-05, D-06, D-07, D-08, D-09):
    - SummaryDomainKind enum: ControlEffects, CallEffects, MemoryEffects, DataFlowTito. With as_str method.
    - SummaryStatus enum: Present, Unknown, Unsupported, SetupMissing, BudgetExceeded. With as_str.
    - SummaryPrecision enum: Local, SetupAware, Heuristic, UnknownTop. With as_str.
    - SummaryProvenance enum: NativeLocal, LiftedFromDomain. With as_str.
    - ExitKind enum: Returns, Throws, Panics, ExitsProcess, DoesNotReturn, Unknown. Derive Ord for BTreeSet usage.
    - AsyncKind enum: Sync, Async, Generator, Unknown.
    - AccessKind enum: None, Read, Write, ReadWrite.
    - ExternalEffectKind enum: FileSystem, Network, Database, Env, Process, Time.
    - MemoryResource enum: Receiver, Param(u16), Return, Local, Global, Module.
    - FlowKind enum: Value, BySideEffect, Taint, Barrier, Sanitizer (per D-08, Taint/Barrier/Sanitizer defined but not populated by Phase 32 builders).
    - FlowRoot enum: Param(u16), Receiver, Return.
    - SummaryFact struct: id: SummaryId, callable_stable_key: String, function: FunctionId, domain: SummaryDomainKind, status: SummaryStatus, precision: SummaryPrecision, provenance: SummaryProvenance, payload_digest: String, stable_key: String.
    - SummaryEventFact struct: id: SummaryEventId, callable_stable_key: String, function: FunctionId, domain: SummaryDomainKind, event_kind: String, reason: String, status: SummaryStatus, precision: SummaryPrecision, stable_key: String.
    - All enums derive Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash, Serialize, Deserialize. Structs derive Debug, Clone, PartialEq, Eq, Serialize, Deserialize.
    - Add unit tests verifying enum as_str round-trips and fact field separation.

    Add to analysis/ids.rs: SummaryId(pub(crate) u64) and SummaryEventId(pub(crate) u64) with the standard derive set. Add both to the assert_small_id_contract test.

    Add pub(crate) mod summaries to analysis/mod.rs.
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis::summaries --locked 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - crates/polint/src/analysis/summaries/mod.rs exists and declares pub(crate) submodules
    - crates/polint/src/analysis/summaries/domain.rs contains pub(crate) trait SummaryDomain with const ID, const VERSION, fn bottom, fn unknown_top, fn is_bottom, fn is_top, fn leq, fn join, fn stable_digest_parts
    - crates/polint/src/analysis/summaries/facts.rs contains SummaryDomainKind with four variants, SummaryStatus with five variants, FlowKind with five variants
    - crates/polint/src/analysis/ids.rs contains SummaryId(pub(crate) u64) and SummaryEventId(pub(crate) u64)
    - crates/polint/src/analysis/mod.rs contains pub(crate) mod summaries
    - cargo test -p polint --lib analysis::summaries --locked exits 0
  </acceptance_criteria>
  <done>Summary module root, SummaryDomain trait with law tests, fact vocabulary enums, and ID newtypes exist and compile.</done>
</task>

<task type="auto">
  <name>Task 2: Implement four core summary domain types with lattice semantics and law tests</name>
  <files>
    crates/polint/src/analysis/summaries/core.rs
  </files>
  <read_first>
    crates/polint/src/analysis/domains/core.rs,
    crates/polint/src/analysis/summaries/domain.rs,
    crates/polint/src/analysis/summaries/facts.rs
  </read_first>
  <action>
    Create analysis/summaries/core.rs (per D-05, D-06, D-07, D-08, D-09, D-10):

    Use expect(dead_code) module attribute as in domains/core.rs since builders arrive in a later plan.

    ControlEffects (per D-05 item 1):
    - Enum with Bottom, Effects { exits: BTreeSet of ExitKind, async_kind: AsyncKind, has_cleanup: bool }, Top(SummaryTopReason).
    - bottom() returns Bottom. unknown_top returns Top(reason).
    - join merges exit sets (union), async_kind (Unknown if different), has_cleanup (or).
    - leq: Bottom leq all, Effects leq Effects if exits subset and async compatible and cleanup subset, Top always greatest.
    - stable_digest_parts: sorted exit kinds, async_kind, cleanup flag.
    - ID = "summary.control_effects", VERSION = 1.

    CallEffects (per D-05 item 2):
    - Struct variants: Bottom, Effects { direct_callees: BTreeSet of String (callee stable keys), unresolved_count: u32, has_callback_invoked: bool, has_callback_stored: bool }, Top(SummaryTopReason).
    - join merges callee sets, sums unresolved counts (capped), ors callback flags.
    - ID = "summary.call_effects", VERSION = 1.

    MemoryEffects (per D-05 item 3, D-09):
    - Struct variants: Bottom, Effects { receiver: AccessKind, params: BTreeMap of u16 to AccessKind, return_access: AccessKind, local: AccessKind, global: AccessKind, module: AccessKind, may_have_external_effects: bool }, Top(SummaryTopReason).
    - join per-resource is AccessKind::join (None < Read < Write < ReadWrite). External flag ors.
    - ID = "summary.memory_effects", VERSION = 1.

    DataFlowTito (per D-05 item 4, D-07, D-08):
    - FlowEdge struct: from: FlowRoot, to: FlowRoot, kind: FlowKind. Derive Ord for BTreeSet.
    - Struct variants: Bottom, Flows { edges: BTreeSet of FlowEdge, has_source_return: bool, has_sink_param: bool }, Top(SummaryTopReason).
    - join merges edge sets, ors source/sink flags.
    - ID = "summary.data_flow_tito", VERSION = 1.
    - Per D-07: only Value and BySideEffect flow kinds are populated by Phase 32 builders. Taint, Barrier, Sanitizer are defined in FlowKind but not used.

    AccessKind should have a join method: None join X = X, Read join Write = ReadWrite, ReadWrite join anything = ReadWrite.

    Add comprehensive trait law tests for each domain: bottom_leq_all, top_geq_all, join_commutative, join_idempotent, unknown_top_is_top, bottom_is_bottom, stable_digest_deterministic.
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis::summaries::core --locked 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - crates/polint/src/analysis/summaries/core.rs contains ControlEffects, CallEffects, MemoryEffects, DataFlowTito types
    - Each type implements SummaryDomain (verified by compilation)
    - Each type has Bottom, Effects/Flows, and Top(SummaryTopReason) variants
    - AccessKind::join exists: None join Read = Read, Read join Write = ReadWrite
    - FlowEdge has from: FlowRoot, to: FlowRoot, kind: FlowKind
    - cargo test -p polint --lib analysis::summaries::core --locked exits 0 with law tests passing
  </acceptance_criteria>
  <done>Four core summary domains implement SummaryDomain with correct lattice semantics, and law tests prove partial order, join, idempotence, and top/bottom behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None in this plan | Pure internal type definitions, no I/O or external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Information Disclosure | summary domain types | accept | All types are pub(crate), no public API surface per D-04 |
| T-32-SC | Tampering | no package installs | accept | No new dependencies added in this plan |
</threat_model>

<verification>
cargo test -p polint --lib analysis::summaries --locked
cargo fmt --all -- --check
</verification>

<success_criteria>
- analysis::summaries module exists with domain trait, four core domains, fact vocabulary, and ID newtypes
- All law tests pass: bottom leq all, top geq all, join commutative, join idempotent
- cargo test -p polint --lib analysis::summaries --locked exits 0
</success_criteria>

<output>
Create `.planning/phases/32-summary-kernel-and-direct-summaries/32-01-SUMMARY.md` when done
</output>
