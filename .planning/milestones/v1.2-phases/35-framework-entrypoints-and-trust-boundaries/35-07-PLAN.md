---
phase: 35-framework-entrypoints-and-trust-boundaries
plan: 07
type: execute
wave: 4
depends_on: [35-05, 35-06]
files_modified:
  - crates/polint/src/analysis/entrypoints/debug.rs
  - crates/polint/src/analysis/entrypoints/mod.rs
  - crates/polint/src/analysis_kernel/mod.rs
  - tests/eval-fixtures/framework-entrypoints/mixed-go-ts/expected.polint-eval.toml
  - tests/eval-fixtures/framework-entrypoints/mixed-go-ts/repo/main.go
  - tests/eval-fixtures/framework-entrypoints/mixed-go-ts/repo/main_test.go
  - tests/eval-fixtures/framework-entrypoints/mixed-go-ts/repo/go.mod
  - tests/eval-fixtures/framework-entrypoints/mixed-go-ts/repo/server.ts
  - tests/eval-fixtures/framework-entrypoints/mixed-go-ts/repo/mcp-server.ts
  - tests/eval-fixtures/framework-entrypoints/mixed-go-ts/repo/package.json
  - tests/eval-fixtures/framework-entrypoints/mixed-go-ts/repo/tsconfig.json
autonomous: true
requirements: [SAE-INT-05]

must_haves:
  truths:
    - "Debug snapshots show entrypoint counts by language, framework, kind, status, and precision"
    - "Eval fixtures cover HTTP (Go net/http, chi; TS/JS Express), MCP (TS/JS), test (Go, TS/JS), CLI (Go), trust boundary source kinds, and unresolved framework"
    - "Cold/warm/no-cache three-way equality holds for framework entrypoint output"
  artifacts:
    - path: "crates/polint/src/analysis/entrypoints/debug.rs"
      provides: "metadata_debug_json_for_test producing debug snapshot with counts and fact summaries"
      min_lines: 40
    - path: "tests/eval-fixtures/framework-entrypoints/mixed-go-ts/expected.polint-eval.toml"
      provides: "Eval fixture manifest with entrypoint expected observations"
      min_lines: 20
  key_links:
    - from: "crates/polint/src/analysis_kernel/mod.rs"
      to: "crates/polint/src/analysis/entrypoints/debug.rs"
      via: "metadata_debug_json_for_test call in kernel debug output"
      pattern: "entrypoints.*debug"
---

<objective>
Add internal debug snapshots for framework facts and create deterministic eval fixtures covering the required framework recognition scenarios per D-24 and D-25.

Purpose: Debug snapshots provide internal visibility into framework fact output without public exposure. Eval fixtures prove that recognizers produce correct facts and that output is deterministic across cold/warm/no-cache runs.
Output: Debug JSON for test use, eval fixture with Go and TS/JS sources exercising all scoped framework patterns.
</objective>

<execution_context>
@.planning/phases/35-framework-entrypoints-and-trust-boundaries/35-CONTEXT.md
@.planning/phases/35-framework-entrypoints-and-trust-boundaries/35-PATTERNS.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/35-framework-entrypoints-and-trust-boundaries/35-05-SUMMARY.md
@.planning/phases/35-framework-entrypoints-and-trust-boundaries/35-06-SUMMARY.md

<interfaces>
From crates/polint/src/analysis/calls/provider.rs (debug pattern):
- Debug output typically implemented as pub(crate) fn metadata_debug_json_for_test(db: &AnalysisDb) -> serde_json::Value
- Uses relative paths, stable keys, spans, status/precision labels, counts
- Avoids raw source, absolute paths, parser IDs, timestamps, run-local dense IDs

From tests/eval-fixtures/ (fixture pattern):
- Directory structure: area/fixture-name/repo/ (source files) + expected.polint-eval.toml (manifest)
- TOML manifest specifies: area, name, expected_observations, invariants
- Observations list: family, status, precision, count ranges, payload fragments

From crates/polint/src/eval/ (observation pattern):
- eval observation maps provider outputs to per-family observation rows
- framework facts map to families: entrypoint, trust_boundary, dispatch_edge, unresolved_framework
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create debug snapshot module for framework facts</name>
  <files>crates/polint/src/analysis/entrypoints/debug.rs, crates/polint/src/analysis/entrypoints/mod.rs, crates/polint/src/analysis_kernel/mod.rs</files>
  <read_first>
    - crates/polint/src/analysis/calls/provider.rs
    - crates/polint/src/analysis/domains/debug.rs
    - crates/polint/src/analysis/summaries/debug.rs
    - crates/polint/src/analysis_kernel/mod.rs
    - crates/polint/src/analysis/entrypoints/facts.rs
  </read_first>
  <action>
    Create analysis/entrypoints/debug.rs (per D-24):

    #[cfg(test)]
    pub(crate) fn metadata_debug_json_for_test(db: &AnalysisDb) -> serde_json::Value.

    The debug snapshot should include:

    1. Summary counts:
       - total_entrypoints: count
       - total_trust_boundaries: count
       - total_dispatch_edges: count
       - total_unresolved: count

    2. Entrypoint breakdown by language, framework_id, kind, status, precision as nested counts.

    3. Trust boundary breakdown by source_kind and associated entrypoint_stable_key prefix (truncated).

    4. Dispatch edge breakdown by edge_kind.

    5. Unresolved framework breakdown by reason.

    6. Per-entrypoint detail rows (sorted by stable_key): relative file path (from db files), function name (from db functions), framework_id, kind label, trigger_metadata summary (method+path or tool_name or test_name), precision label, status label, stable_key.

    7. Per-trust-boundary detail rows (sorted by stable_key): entrypoint_stable_key (truncated to 40 chars), source_kind label, precision label, stable_key.

    Avoid: raw source bodies, absolute paths, parser IDs, timestamps, run-local dense IDs as identity.

    Wire the debug output into the kernel's test-facing debug JSON path. In analysis_kernel/mod.rs, add a #[cfg(test)] block that calls entrypoints::debug::metadata_debug_json_for_test(db) alongside the existing debug JSON calls for calls, domains, summaries. Store the result in the kernel's debug output structure (following the pattern of #[cfg(test)] let calls_debug, #[cfg(test)] let domains_debug, etc.).

    Update analysis/entrypoints/mod.rs to declare pub(crate) mod debug.

    Add tests verifying: empty db produces zero counts, populated db produces correct count breakdowns.
  </action>
  <verify>
    <automated>cd /Users/emilwareus/conductor/workspaces/exlint/sydney && cargo test -p polint --lib analysis::entrypoints::debug -- 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - analysis/entrypoints/debug.rs contains #[cfg(test)] pub(crate) fn metadata_debug_json_for_test
    - Debug JSON includes total_entrypoints, total_trust_boundaries, total_dispatch_edges, total_unresolved counts
    - Debug JSON includes per-entrypoint detail rows with relative path, function name, framework_id, kind, precision, status, stable_key
    - Debug JSON avoids absolute paths, raw source, timestamps, run-local dense IDs
    - analysis_kernel/mod.rs has #[cfg(test)] entrypoints debug output wiring
    - cargo test -p polint --lib analysis::entrypoints::debug passes
  </acceptance_criteria>
  <done>Debug snapshots provide internal visibility into framework fact output with counts and detail rows</done>
</task>

<task type="auto">
  <name>Task 2: Create eval fixture with mixed Go and TS/JS framework sources</name>
  <files>tests/eval-fixtures/framework-entrypoints/mixed-go-ts/expected.polint-eval.toml, tests/eval-fixtures/framework-entrypoints/mixed-go-ts/repo/main.go, tests/eval-fixtures/framework-entrypoints/mixed-go-ts/repo/main_test.go, tests/eval-fixtures/framework-entrypoints/mixed-go-ts/repo/go.mod, tests/eval-fixtures/framework-entrypoints/mixed-go-ts/repo/server.ts, tests/eval-fixtures/framework-entrypoints/mixed-go-ts/repo/mcp-server.ts, tests/eval-fixtures/framework-entrypoints/mixed-go-ts/repo/package.json, tests/eval-fixtures/framework-entrypoints/mixed-go-ts/repo/tsconfig.json</files>
  <read_first>
    - tests/eval-fixtures/direct-calls/mixed-go-ts/expected.polint-eval.toml
    - tests/eval-fixtures/direct-calls/mixed-go-ts/repo/main.go
    - tests/eval-fixtures/direct-summaries/mixed-go-ts/expected.polint-eval.toml
    - tests/eval-fixtures/abstract-domains/mixed-go-ts/expected.polint-eval.toml
  </read_first>
  <action>
    Create a mixed Go and TS/JS eval fixture (per D-25) at tests/eval-fixtures/framework-entrypoints/mixed-go-ts/:

    Go source files:

    main.go: A Go HTTP server using net/http and chi.
    - import "net/http" and "github.com/go-chi/chi/v5"
    - http.HandleFunc("/health", healthHandler) — HTTP route entrypoint
    - r := chi.NewRouter(); r.Get("/api/users", getUsersHandler); r.Post("/api/users", createUserHandler) — chi route entrypoints
    - r.Use(loggingMiddleware) — chi middleware entrypoint
    - Handlers are named functions with http.ResponseWriter and *http.Request parameters.
    - Include a cobra.Command registration for CLI entrypoint.

    main_test.go: Test functions.
    - import "testing"
    - func TestHealthHandler(t *testing.T) { ... } — test entrypoint
    - func BenchmarkGetUsers(b *testing.B) { ... } — benchmark entrypoint

    go.mod: module directive with go-chi/chi/v5 and cobra dependencies.

    TS/JS source files:

    server.ts: Express HTTP server.
    - import express from "express"
    - const app = express()
    - app.get("/api/items", getItems) — Express route
    - app.post("/api/items", createItem) — Express route
    - app.use(corsMiddleware) — Express middleware
    - Handler functions defined as named functions.

    mcp-server.ts: MCP TypeScript SDK server.
    - import { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js" (or equivalent import path)
    - const server = new McpServer(...)
    - server.tool("calculate", calculateHandler) — MCP tool
    - server.resource("config://settings", settingsHandler) — MCP resource
    - server.prompt("summarize", summarizeHandler) — MCP prompt

    package.json: with express, @modelcontextprotocol/sdk, jest dependencies.
    tsconfig.json: minimal config.

    TOML eval manifest (expected.polint-eval.toml):

    area = "framework-entrypoints"
    name = "mixed-go-ts"

    Expected observations (use the established TOML format from other fixtures):
    - Entrypoint facts with counts: at least 4 Go HTTP routes + 1 Go middleware + 2 Go tests + 1 Go CLI + 3 Express routes + 1 Express middleware + 3 MCP tools/resources/prompts = at least 15 entrypoints.
    - Trust boundary facts: HTTP entrypoints produce multiple boundaries each (PathParam, QueryString, RequestBody, RequestHeader), MCP entrypoints produce McpArguments/McpResourceUri boundaries, CLI produces CliArgs+CliFlags. Count at least 20 trust boundaries.
    - Dispatch edge facts: one per resolved entrypoint, at least 12.
    - Unresolved framework facts: may be 0 if all patterns are recognized (no unrecognized framework imports).

    Invariants:
    - determinism.cold_warm_equality = true
    - determinism.cold_nocache_equality = true
    - determinism.warm_nocache_equality = true (three-way equality)

    Add the fixture to the eval test runner. Wire observation of framework fact families into the eval harness if not already present: map db.entrypoint_facts() to "entrypoint" family observations, db.trust_boundary_facts() to "trust_boundary", db.dispatch_edge_facts() to "dispatch_edge", db.unresolved_framework_facts() to "unresolved_framework".
  </action>
  <verify>
    <automated>cd /Users/emilwareus/conductor/workspaces/exlint/sydney && cargo test -p polint eval_fixture -- framework 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - tests/eval-fixtures/framework-entrypoints/mixed-go-ts/ directory exists with repo/ containing Go and TS/JS source files
    - main.go has net/http HandleFunc, chi router methods, cobra.Command patterns
    - main_test.go has Test* and Benchmark* functions
    - server.ts has Express app.get/post/use patterns
    - mcp-server.ts has MCP server.tool/resource/prompt patterns
    - expected.polint-eval.toml specifies area "framework-entrypoints" with entrypoint, trust_boundary, dispatch_edge observations
    - Determinism invariants: cold_warm_equality, cold_nocache_equality, warm_nocache_equality all true
    - Eval fixture test passes (or is wired for future pass once observation is complete)
  </acceptance_criteria>
  <done>Eval fixture exercises all Phase 35 framework recognition scenarios with deterministic three-way equality</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval fixture sources | Fixture source files are checked-in test data, not external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-11 | Information Disclosure | debug snapshots | mitigate | Debug output gated behind #[cfg(test)]; no public exposure per D-24 |
| T-35-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies added in this plan |
</threat_model>

<verification>
cargo test -p polint --lib analysis::entrypoints::debug passes
cargo test -p polint eval_fixture -- framework passes (or is ready for wiring)
cargo check -p polint succeeds
</verification>

<success_criteria>
Debug snapshots show framework fact counts and detail rows. Eval fixture covers HTTP, MCP, test, CLI entrypoints for both Go and TS/JS with deterministic output.
</success_criteria>

<output>
Create `.planning/phases/35-framework-entrypoints-and-trust-boundaries/35-07-SUMMARY.md` when done
</output>
