---
phase: 26-semantic-index-deepening
plan: 05
type: execute
wave: 5
depends_on: ["26-04"]
files_modified:
  - crates/polint/src/analysis_kernel/incremental/keys.rs
  - crates/polint/src/symbol_graph/model.rs
  - crates/polint/src/symbol_graph/mod.rs
  - crates/polint/src/core/mod.rs
autonomous: true
requirements: [SAE-SEM-01]
must_haves:
  truths:
    - "Semantic layer keys include source, syntax, module, config, lifecycle, provider/schema, parameters, and absent extension digest inputs."
    - "Semantic payloads store normalized facts and metadata only, not raw source text, ASTs, absolute paths, timestamps, or run-local IDs as identity."
    - "Stable export identities and semantic stable keys survive deterministic cache restore."
  artifacts:
    - path: "crates/polint/src/analysis_kernel/incremental/keys.rs"
      provides: "Semantic-aware symbol graph layer key identity"
      contains: "semantic_provider_parameter_digest"
    - path: "crates/polint/src/symbol_graph/model.rs"
      provides: "Symbol graph payload containing semantic rows"
      contains: "semantic_index"
    - path: "crates/polint/src/symbol_graph/mod.rs"
      provides: "Cache read/write and restore for semantic rows"
      contains: "restore_symbol_graph_layer_payload"
  key_links:
    - from: "crates/polint/src/symbol_graph/mod.rs"
      to: "crates/polint/src/core/mod.rs"
      via: "AnalysisDb semantic replacement on cache hit"
      pattern: "replace_semantic_index_facts"
    - from: "crates/polint/src/analysis_kernel/incremental/keys.rs"
      to: "crates/polint/src/analysis_kernel/provider.rs"
      via: "provider schema/version in LayerKey"
      pattern: "symbol_graph_layer_key"
---

<objective>
Persist and restore semantic-index rows through the existing symbol graph layer cache with complete deterministic key inputs.

Purpose: This implements D-05, D-07, D-18, D-19, D-20, D-21, D-22, and D-23 for cache behavior and stable export identities.
Output: Semantic-aware symbol graph cache keys, payloads, validators, restore paths, and cache-stability tests.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/26-semantic-index-deepening/26-CONTEXT.md
@research/semantic-index/RECOMMENDED_IMPLEMENTATION.md
@research/semantic-index/VALIDATION.md
@AGENTS.md
@.agents/skills/rust-best-practices/SKILL.md
@crates/polint/src/analysis_kernel/incremental/keys.rs
@crates/polint/src/analysis_kernel/incremental/layer_cache.rs
@crates/polint/src/symbol_graph/mod.rs
@crates/polint/src/symbol_graph/model.rs
@crates/polint/src/core/mod.rs
@.planning/phases/24-persistent-layer-cache-for-existing-cheap-facts/24-CONTEXT.md

<interfaces>
Existing cache restore shape:

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
pub(crate) struct SymbolGraphLayerPayload {
    pub(crate) schema: String,
    pub(crate) diagnostics: Vec<Diagnostic>,
    pub(crate) capability_support: Vec<CapabilitySupport>,
    pub(crate) symbols: Vec<SymbolFact>,
    pub(crate) definitions: Vec<DefinitionFact>,
    pub(crate) references: Vec<ReferenceFact>,
}

fn restore_symbol_graph_layer_payload(db: &mut AnalysisDb, payload: &SymbolGraphLayerPayload);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add semantic inputs to symbol graph layer identity</name>
  <files>crates/polint/src/analysis_kernel/incremental/keys.rs, crates/polint/src/symbol_graph/mod.rs</files>
  <read_first>
    - crates/polint/src/analysis_kernel/incremental/keys.rs
    - crates/polint/src/symbol_graph/mod.rs
    - crates/polint/src/analysis_kernel/provider.rs
    - crates/polint/src/analysis_kernel/incremental/input_snapshot.rs
    - .planning/phases/24-persistent-layer-cache-for-existing-cheap-facts/24-CONTEXT.md
    - .planning/phases/26-semantic-index-deepening/26-CONTEXT.md
  </read_first>
  <behavior>
    - Test: Symbol graph layer key changes when semantic provider parameters change.
    - Test: Symbol graph layer key changes when provider schema changes.
    - Test: Symbol graph layer key includes absent extension digest and upstream module/syntax output digests.
  </behavior>
  <action>Update symbol graph layer key construction to include semantic provider parameters in the `parameter_digest`. Add a helper named `semantic_provider_parameter_digest` that contributes the exact parts `scopes=enabled`, `semantic_imports=enabled`, `exports=enabled`, `aliases=enabled`, `resolution_facts=enabled`, `generated_symbols=enabled`, `stable_exports=enabled`, `alias_closure=max_input_plus_one`, and `generated_hooks=native_rows_only`. Keep existing source/function/package/import/config/lifecycle/module-output/syntax-output inputs. Keep `Digest::absent(DigestKind::ExtensionCode, "extension_digest_absent")` in extension digests. Do not include rule code digest, rule options, raw source text, absolute paths, timestamps, or run-local IDs in layer identity.</action>
  <verify>
    <automated>cargo test -p polint --lib incremental::keys::symbol_graph_semantic_layer_key --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg "semantic_provider_parameter_digest|scopes=enabled|stable_exports=enabled|alias_closure=max_input_plus_one|generated_hooks=native_rows_only" crates/polint/src/analysis_kernel/incremental/keys.rs crates/polint/src/symbol_graph/mod.rs` finds semantic parameter inputs.
    - `rg "extension_digest_absent" crates/polint/src/analysis_kernel/incremental/keys.rs crates/polint/src/symbol_graph/mod.rs` confirms absent extension digest remains in the key.
    - `rg "rule_hash|rule_digest|rule_options|absolute|timestamp|SystemTime" crates/polint/src/analysis_kernel/incremental/keys.rs crates/polint/src/symbol_graph/mod.rs` returns no new symbol-graph semantic layer key inputs.
  </acceptance_criteria>
  <done>Symbol graph layer identity changes for semantic provider/schema inputs and remains independent of rules, raw paths, and transient runtime data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist and restore semantic rows in symbol graph payloads</name>
  <files>crates/polint/src/symbol_graph/model.rs, crates/polint/src/symbol_graph/mod.rs, crates/polint/src/core/mod.rs</files>
  <read_first>
    - crates/polint/src/symbol_graph/model.rs
    - crates/polint/src/symbol_graph/mod.rs
    - crates/polint/src/core/mod.rs
    - crates/polint/src/analysis_kernel/incremental/layer_cache.rs
    - crates/polint/src/symbol_graph/semantic.rs
    - .planning/phases/26-semantic-index-deepening/26-CONTEXT.md
  </read_first>
  <behavior>
    - Test: A cold symbol graph run writes semantic rows into `SymbolGraphLayerPayload`.
    - Test: A warm symbol graph run restores symbols, definitions, references, semantic rows, and metadata through AnalysisDb replacement paths.
    - Test: Cache payload JSON does not contain raw source bodies, absolute temp roots, or run-local ID fields named `run_id_identity`.
  </behavior>
  <action>Extend `SymbolGraphLayerPayload` with a field named `semantic_index` containing sorted vectors for scopes, semantic imports, exports, aliases, resolution facts, generated symbols, and stable exports. Update payload creation to copy normalized semantic rows from `AnalysisDb` after validation-ready derivation. Update `restore_symbol_graph_layer_payload` to call `db.replace_symbol_graph_facts(...)` and `db.replace_semantic_index_facts(...)` on cache hits, so metadata refresh paths run for both old and new fact families. Update payload validation to require schema `symbol-graph-facts-2`, non-empty semantic stable keys for every semantic row, no absolute path strings under row path fields, and no duplicate stable export identity keys with conflicting target symbol keys.</action>
  <verify>
    <automated>cargo test -p polint --lib symbol_graph::semantic_layer_payload --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg "semantic_index" crates/polint/src/symbol_graph/model.rs crates/polint/src/symbol_graph/mod.rs` finds the payload field and restore logic.
    - `rg "replace_semantic_index_facts" crates/polint/src/symbol_graph/mod.rs crates/polint/src/core/mod.rs` finds restore through AnalysisDb.
    - `rg "symbol-graph-facts-2" crates/polint/src/symbol_graph/model.rs crates/polint/src/symbol_graph/mod.rs` finds schema validation.
    - `rg "source\\.as_ref\\(\\)|source_text|absolute temp|run_id_identity" crates/polint/src/symbol_graph/model.rs crates/polint/src/symbol_graph/mod.rs` returns no raw-source or transient-identity payload fields.
  </acceptance_criteria>
  <done>Semantic rows persist and restore through the existing layer cache using normalized payloads and metadata-refreshing DB APIs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove stable export identities survive cache restore</name>
  <files>crates/polint/src/symbol_graph/mod.rs</files>
  <read_first>
    - crates/polint/src/symbol_graph/mod.rs
    - crates/polint/src/analysis_kernel/mod.rs
    - crates/polint/src/analysis_kernel/incremental/layer_cache.rs
    - crates/polint/src/symbol_graph/semantic.rs
    - tests/eval-fixtures/cache/layer-cache/expected.polint-eval.toml
    - .planning/phases/26-semantic-index-deepening/26-CONTEXT.md
  </read_first>
  <behavior>
    - Test: Cold and warm runs over the same TS fixture produce byte-identical sorted stable export keys.
    - Test: Cold and warm runs over the same Go fixture produce byte-identical sorted stable export keys when Go setup succeeds.
    - Test: Warm run reports a symbol graph layer-cache hit and verified reuse while semantic rows are present.
  </behavior>
  <action>Add focused symbol graph cache tests that run `AnalysisKernel::run` twice with layer cache enabled and a plan requesting `symbols` plus `references`. Collect sorted `db.stable_exports().iter().map(|row| row.stable_key.clone())` from cold and warm outputs and assert equality. Also assert the warm `polint.symbol_graph` provider output row has `cache_stats.hits == 1`, `verified_reuse == 1`, and `recomputes == 0`. Add separate fixtures or inline temp repos for TS/JS and Go; if the test environment lacks Go setup, assert setup-missing semantic rows and skip only the Go stable-export equality assertion through an explicit test branch based on the existing Go lifecycle error, not by hiding diagnostics.</action>
  <verify>
    <automated>cargo test -p polint --lib symbol_graph::semantic_cache_restore --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg "semantic_cache_restore|stable_exports\\(\\).*stable_key|verified_reuse|cache_stats\\.hits" crates/polint/src/symbol_graph/mod.rs` finds cache restore assertions.
    - `rg "recomputes, 0|recomputes == 0" crates/polint/src/symbol_graph/mod.rs` finds warm-run no-recompute assertion.
    - `rg "skip|ignore" crates/polint/src/symbol_graph/mod.rs` returns no unconditional skipped cache restore tests.
  </acceptance_criteria>
  <done>Stable export identities and semantic keys survive deterministic symbol graph cache restore with verified reuse counters.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| layer cache files to AnalysisDb | Cache manifests and payloads are filesystem data read back into trusted in-memory facts. |
| semantic stable keys to cache identity | Stable identity controls reuse and must not depend on transient or attacker-controlled path escapes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-05-01 | Tampering | `SymbolGraphLayerPayload` | mitigate | Validate schema, payload digest, non-empty stable keys, path normalization, and duplicate stable export conflicts before Hit. |
| T-26-05-02 | Information Disclosure | cache payload JSON | mitigate | Persist normalized facts only; grep acceptance blocks raw source text, absolute temp roots, timestamps, and transient identity fields. |
| T-26-05-03 | Spoofing | cache stable export identity | mitigate | Restore through `replace_semantic_index_facts` so metadata stable-key ownership validation runs after cache hits. |
</threat_model>

<verification>
Run:
- `cargo test -p polint --lib incremental::keys::symbol_graph_semantic_layer_key --locked`
- `cargo test -p polint --lib symbol_graph::semantic_layer_payload --locked`
- `cargo test -p polint --lib symbol_graph::semantic_cache_restore --locked`
- `cargo fmt --all -- --check`
</verification>

<success_criteria>
Plan succeeds when semantic facts participate in symbol graph cache identity, payload persistence, restore validation, and stable export identities survive warm cache reuse.
</success_criteria>

<output>
After completion, create `.planning/phases/26-semantic-index-deepening/26-05-SUMMARY.md`.
</output>
