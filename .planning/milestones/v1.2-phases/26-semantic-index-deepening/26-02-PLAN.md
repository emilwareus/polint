---
phase: 26-semantic-index-deepening
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - crates/polint/src/symbol_graph/ts.rs
  - crates/polint/src/symbol_graph/semantic.rs
autonomous: true
requirements: [SAE-SEM-01]
must_haves:
  truths:
    - "TS/JS semantic extraction emits normalized internal scopes, imports, exports, aliases, resolution steps, unknowns, and stable exports."
    - "Oxc remains the TS/JS semantic source; no TypeScript compiler sidecar is required."
    - "Dynamic/CommonJS/unsupported TS/JS semantics are visible as uncertain rows instead of exact claims."
  artifacts:
    - path: "crates/polint/src/symbol_graph/ts.rs"
      provides: "TS/JS language-owned semantic extraction"
      contains: "derive_ts_semantic_index"
    - path: "crates/polint/src/symbol_graph/semantic.rs"
      provides: "Shared builder helpers consumed by TS/JS extraction"
      contains: "SemanticIndexBuilder"
  key_links:
    - from: "crates/polint/src/symbol_graph/ts.rs"
      to: "crates/polint/src/symbol_graph/semantic.rs"
      via: "SemanticIndexBuilder insertion APIs"
      pattern: "SemanticIndexBuilder"
---

<objective>
Deepen the TS/JS side of the semantic index using Oxc-owned scopes, imports, exports, aliases, and reference resolution rows.

Purpose: This implements D-03, D-08, D-09, D-12, D-13, D-15, D-16, D-20, and D-21 for TypeScript and JavaScript without adding a public TS semantic API.
Output: TS/JS semantic rows behind `polint.symbol_graph`, with explicit uncertain states for dynamic and unsupported patterns.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/26-semantic-index-deepening/26-CONTEXT.md
@research/semantic-index/RECOMMENDED_IMPLEMENTATION.md
@research/semantic-index/VALIDATION.md
@AGENTS.md
@.agents/skills/rust-best-practices/SKILL.md
@crates/polint/src/symbol_graph/ts.rs
@crates/polint/src/symbol_graph/semantic.rs
@crates/polint/src/module_graph/ts.rs
@crates/polint/src/core/mod.rs

<interfaces>
Use the existing TS entrypoint and import-alias join:

```rust
pub(crate) fn derive_ts_symbols(
    builder: &mut SymbolGraphBuilder,
    db: &AnalysisDb,
    _loaded: &LoadedConfig,
    plan: &AnalysisPlan,
) -> LanguageSymbolOutput;
```

Existing TS precision boundaries:

```rust
SymbolPrecision::ExactLocal
SymbolPrecision::ModuleLinked
SymbolPrecision::Unresolved
SymbolPrecision::Ambiguous
SymbolPrecision::SetupMissing
SymbolPrecision::Unsupported
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Emit TS/JS scopes and declaration ownership</name>
  <files>crates/polint/src/symbol_graph/ts.rs, crates/polint/src/symbol_graph/semantic.rs</files>
  <read_first>
    - crates/polint/src/symbol_graph/ts.rs
    - crates/polint/src/symbol_graph/semantic.rs
    - crates/polint/src/symbol_graph/model.rs
    - crates/polint/src/core/mod.rs
    - research/semantic-index/RECOMMENDED_IMPLEMENTATION.md
    - .planning/phases/26-semantic-index-deepening/26-CONTEXT.md
  </read_first>
  <behavior>
    - Test: A TS file with module, function, block, class, and catch scopes emits deterministic `ScopeFact` rows with parent-child links.
    - Test: Shadowed identifiers in nested scopes have distinct owning scope stable keys.
    - Test: Each emitted local reference row includes an enclosing scope stable key where Oxc provides a scope id.
  </behavior>
  <action>First define `pub(crate) struct SemanticIndexBuilder` in `crates/polint/src/symbol_graph/semantic.rs` as the shared cross-plan semantic row collector. It must own stored vectors for scopes, semantic imports, exports/stable export identities, aliases, resolutions, generated symbols, and stable exports, and expose concrete APIs named `SemanticIndexBuilder::new`, `add_scope`, `add_semantic_import`, `add_export_identity`, `add_alias`, `add_resolution`, `add_generated_symbol`, and `finish`. `finish` must return a deterministic semantic output value sorted by stable key/path/span/name/status so the symbol graph derivation path can pass one normalized provider-owned output into alias/reexport closure, generated-symbol hook emission, validation, cache payloads, and `AnalysisDb::replace_semantic_index_facts` in later plans.

Add a TS helper named `derive_ts_semantic_index` that runs inside the existing `derive_ts_file_symbols` path after Oxc semantic construction and instantiates `SemanticIndexBuilder::new` for each TS/JS semantic extraction unit. It must insert `ScopeFact` rows through `SemanticIndexBuilder::add_scope` for `Program`, function/arrow function, class, block, catch, loop, switch, interface, type alias, enum, and namespace-like scopes using `ScopeKind` values with concrete labels `module`, `function`, `class`, `block`, `catch`, `loop`, `switch`, `type`, and `namespace`. Add `scope_stable_key` and `reference_scope_stable_key` helpers that use file path plus Oxc scope ancestor path. Pass owning scope stable keys into semantic symbol/declaration rows without changing public `SymbolFact` fields. Preserve current `Symbols<'_>` and `References<'_>` behavior.</action>
  <verify>
    <automated>cargo test -p polint --lib symbol_graph::ts::semantic_scopes --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg "derive_ts_semantic_index" crates/polint/src/symbol_graph/ts.rs` finds the helper.
    - `rg -F "pub(crate) struct SemanticIndexBuilder" crates/polint/src/symbol_graph/semantic.rs` finds the shared builder definition.
    - `rg -F "fn add_scope" crates/polint/src/symbol_graph/semantic.rs` finds the scope insertion API.
    - `rg -F "fn add_resolution" crates/polint/src/symbol_graph/semantic.rs` finds the resolution insertion API.
    - `rg -F "fn finish" crates/polint/src/symbol_graph/semantic.rs` finds the deterministic output API.
    - `rg "SemanticIndexBuilder::new|\\.add_scope\\(|\\.finish\\(" crates/polint/src/symbol_graph/ts.rs` proves TS/JS extraction uses the builder path.
    - `rg "ScopeKind::(Module|Function|Class|Block|Catch|Loop|Switch|Type|Namespace)" crates/polint/src/symbol_graph/ts.rs crates/polint/src/symbol_graph/semantic.rs` finds scope-kind use or definitions.
    - `rg "reference_scope_stable_key|scope_stable_key" crates/polint/src/symbol_graph/ts.rs` finds both helper names.
    - `rg "pub struct Scope|pub enum Scope" crates/polint/src/sdk crates/polint/src/lib.rs` returns no matches.
  </acceptance_criteria>
  <done>TS/JS scope rows and declaration/reference scope ownership are emitted internally while existing public symbol/reference APIs remain compatible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit TS/JS imports, exports, reexports, and aliases</name>
  <files>crates/polint/src/symbol_graph/ts.rs</files>
  <read_first>
    - crates/polint/src/symbol_graph/ts.rs
    - crates/polint/src/module_graph/ts.rs
    - crates/polint/src/symbol_graph/semantic.rs
    - docs/facts/imports.md
    - docs/facts/module-graph.md
    - .planning/phases/26-semantic-index-deepening/26-CONTEXT.md
  </read_first>
  <behavior>
    - Test: TS static import forms emit semantic import rows for named, default, namespace, side-effect, and type-only imports.
    - Test: TS export forms emit export rows for named exports, default exports, `export * from`, and reexports.
    - Test: CommonJS `require`, `module.exports`, `exports.name`, and dynamic `import(expr)` emit conservative `Dynamic` or `Unsupported` rows rather than `Resolved`.
  </behavior>
  <action>Extend TS extraction to add `SemanticImportFact`, `ExportFact`, and `AliasFact` rows through the `SemanticIndexBuilder` APIs created in Task 1: `add_semantic_import`, `add_export_identity`, and `add_alias`. Required `SemanticImportKind` labels: `static_named`, `static_default`, `static_namespace`, `side_effect`, `type_only`, `commonjs_require`, and `dynamic_import`. Required `ExportKind` labels: `named`, `default`, `namespace`, `star_reexport`, `commonjs_module_exports`, and `commonjs_exports_property`. Static Oxc import/export syntax uses `SemanticStatus::Resolved` only when backed by existing module graph resolution or local Oxc symbol identity; otherwise it uses `Unresolved`, `External`, or `SetupMissing`. `require(...)`, `module.exports`, `exports.name`, and non-literal dynamic imports must use `Dynamic` or `Unsupported` with precision not higher than heuristic. Keep current `ImportAlias` public reference behavior intact.</action>
  <verify>
    <automated>cargo test -p polint --lib symbol_graph::ts::semantic_imports_exports --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg "static_named|static_default|static_namespace|side_effect|type_only|commonjs_require|dynamic_import" crates/polint/src/symbol_graph/ts.rs crates/polint/src/symbol_graph/semantic.rs` finds every import-kind label.
    - `rg "star_reexport|commonjs_module_exports|commonjs_exports_property" crates/polint/src/symbol_graph/ts.rs crates/polint/src/symbol_graph/semantic.rs` finds every export-kind label.
    - `rg "SemanticStatus::(Dynamic|Unsupported|External|SetupMissing|Unresolved)" crates/polint/src/symbol_graph/ts.rs` finds conservative status handling.
    - `rg "typescript|tsserver|tsc --" crates/polint/src/symbol_graph/ts.rs Cargo.toml` returns no new runtime dependency invocation.
  </acceptance_criteria>
  <done>TS/JS imports, exports, reexports, aliases, CommonJS, and dynamic patterns are represented as normalized internal facts with honest uncertainty.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Record TS/JS resolution ladder rows and stable export identities</name>
  <files>crates/polint/src/symbol_graph/ts.rs</files>
  <read_first>
    - crates/polint/src/symbol_graph/ts.rs
    - crates/polint/src/symbol_graph/semantic.rs
    - crates/polint/src/symbol_graph/model.rs
    - docs/facts/symbols-and-references.md
    - research/semantic-index/VALIDATION.md
    - .planning/phases/26-semantic-index-deepening/26-CONTEXT.md
  </read_first>
  <behavior>
    - Test: Local lexical references record `LexicalLookup` resolution steps and `Resolved` status.
    - Test: Import alias references record `ImportAliasLookup` and candidates for ambiguous exports.
    - Test: Stable export identities are unchanged across cold and warm runs for unchanged TS/JS fixture source.
  </behavior>
  <action>For TS/JS references, emit `ResolutionFact` rows through `SemanticIndexBuilder::add_resolution` with step kinds exactly `LexicalLookup`, `ImportAliasLookup`, `ModuleLookup`, `MemberLookup`, `GeneratedHintLookup`, and `UnknownFallback`. Existing Oxc-resolved local references produce a `LexicalLookup` row with `SemanticStatus::Resolved`. Existing import alias references produce `ImportAliasLookup` plus `ModuleLookup` rows and retain `Ambiguous` rows when multiple exported candidates exist. Root unresolved references produce `UnknownFallback` rows with `SemanticStatus::Unresolved`. For every exported TS/JS symbol already marked `is_exported`, emit `StableExportIdentity` through `SemanticIndexBuilder::add_export_identity`, keyed by language, file path, module node stable key when available, export name, namespace, symbol stable key, and generated discriminator `native`. Do not change public `ReferenceFact.status` names or add public resolution-step accessors.</action>
  <verify>
    <automated>cargo test -p polint --lib symbol_graph::ts::semantic_resolution --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg "LexicalLookup|ImportAliasLookup|ModuleLookup|MemberLookup|GeneratedHintLookup|UnknownFallback" crates/polint/src/symbol_graph/ts.rs crates/polint/src/symbol_graph/semantic.rs` finds every resolution step.
    - `rg "StableExportIdentity|generated_discriminator.*native|stable_exports" crates/polint/src/symbol_graph/ts.rs crates/polint/src/symbol_graph/semantic.rs` finds stable export identity emission.
    - `rg "pub fn .*resolution|pub struct Resolution" crates/polint/src/sdk crates/polint/src/lib.rs` returns no matches for a new public resolution API.
  </acceptance_criteria>
  <done>TS/JS reference resolution is explainable internally and exported symbols get cache-stable identities without public API expansion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TS/JS source to Oxc semantic facts | Untrusted repository source is parsed and converted into semantic rows. |
| Internal semantic rows to existing public symbol/reference facts | New internal precision must not overstate public certainty. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-02-01 | Tampering | TS/JS semantic row construction | mitigate | Use Oxc ids only as inputs to normalized stable keys; sort rows by stable key before DB replacement. |
| T-26-02-02 | Information Disclosure | TS/JS internal resolution rows | mitigate | Keep resolution rows crate-private and block SDK/public module additions with grep criteria. |
| T-26-02-03 | Repudiation | Dynamic/CommonJS references | mitigate | Emit `Dynamic` or `Unsupported` resolution rows with low precision instead of silently omitting them. |
</threat_model>

<verification>
Run:
- `cargo test -p polint --lib symbol_graph::ts::semantic_scopes --locked`
- `cargo test -p polint --lib symbol_graph::ts::semantic_imports_exports --locked`
- `cargo test -p polint --lib symbol_graph::ts::semantic_resolution --locked`
- `cargo fmt --all -- --check`
</verification>

<success_criteria>
Plan succeeds when TS/JS emits internal semantic rows for scopes, imports, exports, aliases, resolution steps, unknowns, and stable exports while keeping the existing SDK and CLI surfaces unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/26-semantic-index-deepening/26-02-SUMMARY.md`.
</output>
