---
phase: 36-p0-type-value-place-alias-substrate
plan: 05
title: Bounded Points-To Constraints And Alias Query Service
type: execute
wave: 4
depends_on: [36-03, 36-04]
files_modified:
  - crates/polint/src/analysis/points_to/constraints.rs
  - crates/polint/src/analysis/points_to/solver.rs
  - crates/polint/src/analysis/points_to/store.rs
  - crates/polint/src/analysis/aliases/query.rs
  - crates/polint/src/analysis/aliases/provider_stack.rs
  - crates/polint/src/analysis/aliases/store.rs
  - crates/polint/src/analysis/types/provider.rs
autonomous: true
requirements: [SAE-PREC-01]
must_haves:
  truths:
    - "D-15/D-16 are covered: points-to is bounded and optional/query-scoped, not a mandatory whole-repo baseline pass."
    - "D-17/D-20 are covered: alias answers come from a provider stack, not a primary alias graph."
    - "D-18/D-19 are covered: every alias answer uses NoAlias/MayAlias/MustAlias/PartialAlias/Unknown with evidence and reason codes."
---

<objective>
Implement the bounded points-to constraint substrate and alias query service over the Phase 36 type/value/allocation/access-path facts.

Purpose: Provide the explicit alias statuses required by SAE-PREC-01 while keeping expensive points-to optional and budgeted.
Output: Deterministic constraint solver, provider-stack alias query, and tests for address/copy/field flows, budget exhaustion, and all five alias statuses.
</objective>

<execution_context>
@.planning/phases/36-p0-type-value-place-alias-substrate/36-CONTEXT.md
@.planning/phases/36-p0-type-value-place-alias-substrate/36-PATTERNS.md
@research/type-alias-points-to/algorithms/andersen-solver.md
@research/type-alias-points-to/algorithms/core-algorithms.md
@.agents/skills/rust-best-practices/SKILL.md
@AGENTS.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Build deterministic points-to constraints and solver core</name>
  <files>crates/polint/src/analysis/points_to/constraints.rs, crates/polint/src/analysis/points_to/solver.rs, crates/polint/src/analysis/points_to/store.rs</files>
  <read_first>
    - crates/polint/src/analysis/points_to/facts.rs
    - crates/polint/src/analysis/values/facts.rs
    - crates/polint/src/analysis/access_paths/facts.rs
    - research/type-alias-points-to/algorithms/andersen-solver.md
  </read_first>
  <action>
    Implement a bounded inclusion-style points-to solver over dense `PtVarId` and `ObjectTokenId` IDs. Support constraints for AddressOf, Copy, Load, Store, FieldLoad, FieldStore, ElementLoad, ElementStore, CallReturn, and SummaryFlow where facts exist. Use deterministic BTreeMap/BTreeSet or stable bitsets, delta propagation, copy-graph SCC collapse if practical in this plan, type filters where type facts exist, and an explicit `BudgetExceeded`/Unknown result when limits are hit. Expose a function that can solve for all facts in a small fixture or answer a query-scoped subset without forcing whole-repo solving.
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis::points_to --locked</automated>
  </verify>
  <acceptance_criteria>
    - Unit tests cover AddressOf, Copy, FieldLoad, FieldStore, ElementLoad/Store, CallReturn, and SummaryFlow constraints.
    - Solver output order is deterministic across repeated runs.
    - Budget exhaustion produces Unknown/BudgetExceeded facts instead of panic or partial exact output.
    - No baseline kernel path requires whole-repo points-to to run for every check.
  </acceptance_criteria>
  <done>Bounded points-to constraints can produce deterministic sets or explicit budget unknowns.</done>
</task>

<task type="auto">
  <name>Task 2: Implement alias provider stack</name>
  <files>crates/polint/src/analysis/aliases/query.rs, crates/polint/src/analysis/aliases/provider_stack.rs, crates/polint/src/analysis/aliases/store.rs</files>
  <read_first>
    - crates/polint/src/analysis/aliases/facts.rs
    - crates/polint/src/analysis/points_to/solver.rs
    - crates/polint/src/analysis/access_paths/facts.rs
    - crates/polint/src/analysis/types/facts.rs
    - crates/polint/src/analysis/values/facts.rs
  </read_first>
  <action>
    Implement an internal alias query service with provider order from D-20: identity/disjointness, language ownership, local flow/narrowing, validated extension alias facts, bounded points-to, future sparse refinement placeholder. Return `AliasAnswerFact` values with `AliasStatus`, `AliasReason`, evidence labels, precision, and status. Implement definitive `MustAlias` only for same stable place/access path or singleton-equal object cases with evidence. Implement `NoAlias` for provably disjoint locals/allocations or disjoint points-to sets. Return `MayAlias` for overlapping points-to sets and `Unknown` for missing points-to, setup missing, unsupported constructs, or budget exhaustion. Implement `PartialAlias` for common base with different known projections or object/field overlap where exactness is not proven.
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis::aliases --locked</automated>
  </verify>
  <acceptance_criteria>
    - Tests assert all five statuses: NoAlias, MayAlias, MustAlias, PartialAlias, Unknown.
    - Every alias answer includes non-empty evidence or reason text.
    - Unknown is returned for missing points-to and budget-exceeded paths.
    - MustAlias tests cover same stable access path and singleton-equal object only.
  </acceptance_criteria>
  <done>Alias queries derive honest evidence-backed answers from the provider stack.</done>
</task>

<task type="auto">
  <name>Task 3: Wire constraints and alias answers into Phase 36 provider output</name>
  <files>crates/polint/src/analysis/types/provider.rs, crates/polint/src/analysis/points_to/store.rs, crates/polint/src/analysis/aliases/store.rs</files>
  <read_first>
    - crates/polint/src/analysis/types/provider.rs
    - crates/polint/src/analysis/points_to/constraints.rs
    - crates/polint/src/analysis/points_to/solver.rs
    - crates/polint/src/analysis/aliases/provider_stack.rs
  </read_first>
  <action>
    Have the Phase 36 provider derive points-to constraints from value/allocation/access-path facts and run only bounded/local/query-scoped solving needed by fixtures. Store points-to set facts and alias answer facts in normalized output. Record solver budget settings in provider parameter digest and debug output. Ensure solver failure or budget exhaustion produces diagnostics/events plus Unknown facts, not panics.
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis::types::provider analysis::points_to analysis::aliases --locked</automated>
  </verify>
  <acceptance_criteria>
    - Provider output contains points-to constraints and alias answers for fixture cases.
    - Provider debug output includes solver budget status counts.
    - Changing solver budget changes provider parameter/output digest.
    - `cargo test -p polint --lib analysis::types::provider analysis::points_to analysis::aliases --locked` passes.
  </acceptance_criteria>
  <done>Provider stores points-to and alias facts without mandatory whole-repo solving.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| solver budget | Solver must fail closed when precision cost exceeds configured budget. |
| alias answer consumers | Downstream providers may treat alias answers as precision evidence, so statuses must be honest. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-09 | Denial of Service | points-to solver | mitigate | Add explicit budgets and budget-exceeded Unknown facts; test budget exhaustion. |
| T-36-10 | Tampering | alias precision | mitigate | Alias provider stack returns evidence-backed statuses and avoids exact claims without proof. |
| T-36-SC | Tampering | dependency installation | accept | Use standard collections or existing dependencies; do not add arbitrary solver crates. |
</threat_model>

<verification>
Run:
- `cargo test -p polint --lib analysis::points_to analysis::aliases --locked`
- `cargo test -p polint --lib analysis::types::provider --locked`
- `cargo check -p polint --locked`
</verification>

<success_criteria>
Alias facts distinguish `NoAlias`, `MayAlias`, `MustAlias`, `PartialAlias`, and `Unknown`, with bounded optional points-to and explicit budget behavior.
</success_criteria>

<output>
Create `.planning/phases/36-p0-type-value-place-alias-substrate/36-05-SUMMARY.md` when done.
</output>
