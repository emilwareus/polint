---
phase: 36-p0-type-value-place-alias-substrate
plan: 04
title: TS/JS Type, Value, Allocation, Access Path, And Narrowing Facts
type: execute
wave: 3
depends_on: [36-02]
files_modified:
  - crates/polint/src/analysis/types/ts_js.rs
  - crates/polint/src/analysis/types/provider.rs
  - crates/polint/src/analysis/types/cache_key.rs
  - crates/polint/src/analysis/mir/lower_ts.rs
  - tests/eval-fixtures/type-value-alias/ts-js-core/expected.polint-eval.toml
autonomous: true
requirements: [SAE-PREC-01]
must_haves:
  truths:
    - "D-08/D-10 are covered for TS/JS: nullish/truthiness/typeof/instanceof/in/equality narrowing exists, while dynamic constructs produce Unknown/Unsupported rows."
    - "D-11/D-12/D-13 are covered for TS/JS: function/class/module/object/array allocations and access paths are explicit facts."
    - "D-14 is covered for TS/JS: unknown dynamic keys remain unknown or budgeted access-path/points-to facts and never collapse to exact field-insensitive precision."
    - "TypeScript-compatible semantic input is normalized into polint facts and participates in cache identity when used."
---

<objective>
Populate the Phase 36 provider with first-tier TypeScript/JavaScript type, value, allocation, access-path, and local narrowing facts over existing TS/JS MIR/CFG/place/domain data.

Purpose: Prove the precision substrate on dynamic JS/TS patterns while preserving honest unknowns for dynamic properties, proxies, eval, decorators, and setup gaps.
Output: TS/JS facts for nullish/truthiness/typeof/instanceof/in/equality narrowing, function/class/object/module allocations, known/unknown property keys, and dynamic unknown rows.
</objective>

<execution_context>
@.planning/phases/36-p0-type-value-place-alias-substrate/36-CONTEXT.md
@.planning/phases/36-p0-type-value-place-alias-substrate/36-PATTERNS.md
@research/type-alias-points-to/languages/typescript-javascript.md
@.agents/skills/rust-best-practices/SKILL.md
@AGENTS.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Extract TS/JS type and narrowing facts</name>
  <files>crates/polint/src/analysis/types/ts_js.rs, crates/polint/src/analysis/types/provider.rs</files>
  <read_first>
    - crates/polint/src/analysis/mir/lower_ts.rs
    - crates/polint/src/analysis/places.rs
    - crates/polint/src/analysis/domains/store.rs
    - crates/polint/src/symbol_graph/semantic.rs
    - research/type-alias-points-to/languages/typescript-javascript.md
  </read_first>
  <action>
    Add `analysis/types/ts_js.rs` with an extractor that walks TS/JS MIR bodies, places, operations, CFG/domain observations, semantic references, and unsupported semantic rows. Emit type facts for parameters, locals, imports/exports, `this`, call returns, module namespace objects, function/class values, nullish/unknown/any-like states, and setup-missing cases. Emit narrowed type facts for branch/control locations covering nullish checks, truthiness, strict equality, `typeof`, `instanceof`, `in`/property checks where evidence is available, optional chaining, and known discriminant/literal checks.
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis::types::ts_js --locked</automated>
  </verify>
  <acceptance_criteria>
    - `analysis/types/ts_js.rs` contains an extractor invoked by the Phase 36 provider.
    - Tests cover nullish, truthiness, `typeof`, `instanceof`, `in`, strict equality, optional chaining, and setup-missing cases.
    - Dynamic property/eval/proxy/unsupported rows produce Unknown/Unsupported facts with evidence.
    - No facts claim Exact precision for unsupported dynamic constructs.
  </acceptance_criteria>
  <done>TS/JS type and narrowing facts are emitted with explicit dynamic unknowns.</done>
</task>

<task type="auto">
  <name>Task 2: Emit TS/JS value, allocation, and access-path facts</name>
  <files>crates/polint/src/analysis/types/ts_js.rs, crates/polint/src/analysis/values/facts.rs, crates/polint/src/analysis/access_paths/facts.rs, tests/eval-fixtures/type-value-alias/ts-js-core/repo/src/app.ts</files>
  <read_first>
    - crates/polint/src/analysis/mir/op.rs
    - crates/polint/src/analysis/mir/lower_ts.rs
    - crates/polint/src/analysis/values/facts.rs
    - crates/polint/src/analysis/access_paths/facts.rs
    - tests/eval-fixtures/semantic-mir/core/repo/web/src/app.ts
  </read_first>
  <action>
    Emit TS/JS value facts for null, undefined, booleans/truthiness, string/number literals where already visible, function objects, arrow functions, class constructors, module namespace objects, object literals, array literals, call returns, and unknown values. Emit allocation tokens for functions/arrows/classes/object literals/array literals/module namespaces/closures. Emit access-path facts for property, field-like, index-known, index-unknown, optional-chain, await result, and call-return projections. Known string keys and unknown dynamic keys must be represented differently. Per D-14, unknown dynamic keys must produce unknown/budgeted access-path or points-to facts with evidence; they must not collapse the object to exact field-insensitive precision.
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis::types::ts_js --locked</automated>
  </verify>
  <acceptance_criteria>
    - Object literal fixture produces `AllocationKind::ObjectLiteral`.
    - Function/arrow fixture produces `ValueKind::FunctionObject`.
    - Class fixture produces `ValueKind::ClassObject` or `AllocationKind::ClassObject`.
    - Module namespace fixture produces module object value/allocation facts.
    - Dynamic property key fixture produces unknown access-path evidence instead of exact property identity.
  </acceptance_criteria>
  <done>TS/JS value, allocation, and access-path facts exist for high-yield dynamic language cases.</done>
</task>

<task type="auto">
  <name>Task 3: Include TS/JS semantic input digests when used</name>
  <files>crates/polint/src/analysis/types/cache_key.rs, tests/eval-fixtures/type-value-alias/ts-js-core/expected.polint-eval.toml</files>
  <read_first>
    - crates/polint/src/analysis_kernel/incremental/input_snapshot.rs
    - crates/polint/src/analysis/types/cache_key.rs
    - crates/polint/src/ts/mod.rs
    - crates/polint/src/config.rs
  </read_first>
  <action>
    If TypeScript-compatible semantic setup or TS/JS lifecycle inputs are used, include tsconfig/package/language-mode/toolchain/config digests in the Phase 36 provider digest. If not activated yet, add explicit absent/off sentinels and setup-missing/unknown facts where type precision depends on unavailable setup. Add cache-key tests proving tsconfig/package/lifecycle changes alter output identity when TS/JS type facts depend on them.
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis::types::cache_key --locked</automated>
  </verify>
  <acceptance_criteria>
    - Cache key tests prove TS/JS lifecycle/config digest changes affect Phase 36 output identity when active.
    - Absent TS semantic setup is represented by explicit digest sentinel and SetupMissing/Unknown facts where applicable.
    - No raw TypeScript compiler or Oxc internal object is exposed in public output.
  </acceptance_criteria>
  <done>TS/JS semantic input participation is explicit and cache-safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TS/JS dynamic constructs | Dynamic properties, eval, proxies, decorators, and generated APIs can invalidate exact claims. |
| TS/JS setup inputs | tsconfig/package/language-mode inputs affect semantic precision and cache correctness. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-07 | Tampering | dynamic property precision | mitigate | Represent unknown dynamic keys as Unknown access-path/type/value facts with evidence. |
| T-36-08 | Spoofing | TS/JS setup state | mitigate | Include setup/config digests and setup-missing status rows. |
| T-36-SC | Tampering | dependency installation | accept | Use existing Oxc/TS setup boundaries; do not add arbitrary analyzers. |
</threat_model>

<verification>
Run:
- `cargo test -p polint --lib analysis::types::ts_js analysis::types::cache_key --locked`
- `cargo test -p polint --test cli --locked`
- `cargo check -p polint --locked`
</verification>

<success_criteria>
TS/JS fixtures cover narrowing, function/class/object/module allocations, property access limits, unresolved aliases, dynamic unknowns, and setup digest behavior where used.
</success_criteria>

<output>
Create `.planning/phases/36-p0-type-value-place-alias-substrate/36-04-SUMMARY.md` when done.
</output>
