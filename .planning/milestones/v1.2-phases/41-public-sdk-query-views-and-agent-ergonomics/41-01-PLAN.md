---
phase: 41-public-sdk-query-views-and-agent-ergonomics
plan: 01
title: Promotion Audit And Reserved Capability Disposition
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/API-VISIBILITY-PLAN.md
  - docs/facts/README.md
  - docs/facts/capability-plans.md
  - docs/facts/data-flow.md
  - docs/facts/evidence.md
  - crates/polint/src/analysis_plan.rs
  - crates/polint-macros/src/lib.rs
  - crates/polint/tests/cli.rs
autonomous: true
requirements: [SAE-PROM-02]
must_haves:
  truths:
    - "D-01/D-04: public promotion is evidence-gated; reserved capabilities stay unsupported until real public facts, docs, tests, and setup behavior exist."
    - "D-03/D-21: advanced internal families are promoted selectively and private vocabularies must remain guarded by no-leak tests."
  artifacts:
    - path: "docs/API-VISIBILITY-PLAN.md"
      provides: "Phase 41 promotion audit and disposition policy"
    - path: "crates/polint/src/analysis_plan.rs"
      provides: "Reserved capability support status aligned to Phase 41 disposition"
---

<objective>
Create the Phase 41 promotion audit and align reserved capability diagnostics, docs, and no-leak tests so public promotion starts from an explicit disposition table rather than accidental exports.
</objective>

<execution_context>
@.planning/phases/41-public-sdk-query-views-and-agent-ergonomics/41-CONTEXT.md
@docs/API-VISIBILITY-PLAN.md
@AGENTS.md
@.agents/skills/rust-best-practices/SKILL.md
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Add promotion audit table</name>
  <files>docs/API-VISIBILITY-PLAN.md, docs/facts/README.md</files>
  <read_first>
    - docs/API-VISIBILITY-PLAN.md
    - docs/facts/README.md
    - docs/facts/data-flow.md
    - docs/facts/evidence.md
    - .planning/phases/41-public-sdk-query-views-and-agent-ergonomics/41-CONTEXT.md
  </read_first>
  <action>Add a "Phase 41 promotion audit" section that lists `ResolvedImports`, `ModuleGraphFacts`, `Symbols`, `References`, metric views, `Cfg`, `CallGraph`, `DataFlow`, `Evidence`, `Effects/Summaries`, `Types/Values/Aliases`, model packs, provider extensions, `polint inspect rule`, `polint test`, `polint facts`, `polint unknowns`, `polint explain`, `polint diff`, and `polint eval`. For each row, record current disposition as `stable`, `candidate`, `preview`, `internal`, or `defer`, plus required gates: docs, temp-repo tests, stable JSON, bounded query behavior, setup behavior, cache/input behavior, and no-leak proof.</action>
  <verify>
    <automated>rg -n "Phase 41 promotion audit|ResolvedImports|CallGraph|polint eval" docs/API-VISIBILITY-PLAN.md docs/facts/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/API-VISIBILITY-PLAN.md` contains a promotion audit table with every candidate public surface named above.
    - The table marks broad raw graph/database APIs and stable public `polint eval` as deferred or internal unless a narrow contract is explicitly documented.
    - `docs/facts/README.md` points readers to reserved/internal docs without implying unsupported views are stable.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align reserved capability diagnostics with audit</name>
  <files>crates/polint/src/analysis_plan.rs, crates/polint-macros/src/lib.rs, crates/polint/tests/cli.rs</files>
  <read_first>
    - crates/polint/src/analysis_plan.rs
    - crates/polint-macros/src/lib.rs
    - crates/polint/tests/cli.rs
    - docs/facts/capability-plans.md
  </read_first>
  <action>Update unsupported reserved capability reasons and docs paths so `cfg`, `call_graph`, `dataflow`, `coverage_facts`, and `test_suite_metrics` point to current public docs instead of stale roadmap paths. Keep macro recognition for canonical reserved views only if capability diagnostics remain honest; otherwise remove unsupported aliases from macro mapping and update compile-error tests accordingly.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis_plan::tests::reserved_capabilities_remain_unsupported --locked</automated>
    <automated>cargo test -p polint-macros --locked</automated>
  </verify>
  <acceptance_criteria>
    - Rules requesting unsupported reserved views still receive `polint/capability` diagnostics before execution.
    - Unsupported diagnostics cite an existing docs path such as `docs/facts/capability-plans.md`, `docs/facts/data-flow.md`, or `docs/facts/evidence.md`.
    - Macro tests still reject non-canonical fact-view paths and unsupported local lookalikes.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Strengthen public no-leak tests for Phase 41 baseline</name>
  <files>crates/polint/tests/cli.rs, docs/facts/data-flow.md, docs/facts/evidence.md</files>
  <read_first>
    - crates/polint/tests/cli.rs
    - crates/polint/src/sdk/mod.rs
    - crates/polint/src/runner/mod.rs
    - README.md
    - docs/facts/data-flow.md
    - docs/facts/evidence.md
  </read_first>
  <action>Add or update a public-boundary regression that checks normal `polint check --format json`, public CLI help, `polint::sdk::prelude`, `polint::runner`, README, and docs/facts for accidental exposure of private provider ids and raw internals from calls, refined calls, summaries, demand queries, extensions, framework facts, type/value/alias, data flow, evidence, benchmarks, and eval.</action>
  <verify>
    <automated>cargo test -p polint --test cli phase41_public_promotion_baseline_no_leak --locked</automated>
  </verify>
  <acceptance_criteria>
    - The test fails if private provider IDs such as `polint.data_flow`, `polint.evidence`, `polint.refined_calls`, or eval adapter internals appear in stable SDK/runner/docs surfaces.
    - Reserved docs continue to say unsupported views are not public SDK fact views.
    - Existing public examples remain consumers of `polint::sdk::prelude::*` only.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| private analysis internals -> public SDK/docs | Reserved v1.2 internals must not become public contracts through docs or exports before gates pass. |
| unsupported capability -> rule execution | Rules requesting unsupported facts must not run with placeholder data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T41-01 | Information Disclosure | SDK/docs/help | Mitigated | Add explicit no-leak tests and docs audit table. |
| T41-02 | Tampering | capability planning | Mitigated | Keep unsupported capabilities blocked until backed by real public facts and tests. |
</threat_model>

<verification>
- `cargo test -p polint --lib analysis_plan::tests::reserved_capabilities_remain_unsupported --locked`
- `cargo test -p polint-macros --locked`
- `cargo test -p polint --test cli phase41_public_promotion_baseline_no_leak --locked`
</verification>

<success_criteria>
Phase 41 starts with an explicit public-promotion audit, honest reserved capability diagnostics, and no accidental public leakage of advanced private internals.
</success_criteria>
