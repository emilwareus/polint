---
phase: 41-public-sdk-query-views-and-agent-ergonomics
plan: 05
title: Final Public Docs Skills And Promotion Verification
type: execute
wave: 4
depends_on: [41-01, 41-02, 41-03, 41-04]
files_modified:
  - README.md
  - docs/facts
  - docs/API-VISIBILITY-PLAN.md
  - docs/schemas
  - .claude/skills/polint/SKILL.md
  - .agents/skills/polint/SKILL.md
  - crates/polint/tests/cli.rs
  - .planning/phases/41-public-sdk-query-views-and-agent-ergonomics/41-VERIFICATION.md
autonomous: true
requirements: [SAE-PROM-02]
must_haves:
  truths:
    - "D-19/D-20/D-22: every promoted public view or command has docs, at least one temp-repo public-SDK test, stable JSON checks, no-leak proof, and compatibility protection."
    - "D-13/D-17: generated skills/docs advertise only completed supportable workflows and avoid placeholder advanced APIs."
  artifacts:
    - path: ".planning/phases/41-public-sdk-query-views-and-agent-ergonomics/41-VERIFICATION.md"
      provides: "Final promotion evidence and closeout proof for SAE-PROM-02"
---

<objective>
Close Phase 41 by aligning public documentation, generated skills, schemas, no-leak tests, and verification evidence with the exact SDK views and commands promoted during the phase.
</objective>

<execution_context>
@.planning/phases/41-public-sdk-query-views-and-agent-ergonomics/41-CONTEXT.md
@docs/API-VISIBILITY-PLAN.md
@AGENTS.md
@.agents/skills/rust-best-practices/SKILL.md
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Update public docs and generated skill text</name>
  <files>README.md, docs/facts, .claude/skills/polint/SKILL.md, .agents/skills/polint/SKILL.md</files>
  <read_first>
    - README.md
    - docs/facts/README.md
    - docs/facts/resolved-imports.md
    - docs/facts/symbols-and-references.md
    - docs/facts/metrics.md
    - docs/facts/data-flow.md
    - docs/facts/evidence.md
    - .claude/skills/polint/SKILL.md
    - .agents/skills/polint/SKILL.md
  </read_first>
  <action>Update public docs and generated skill text to describe only the SDK views and CLI JSON commands actually promoted by Plans 41-02 through 41-04. Include examples using `polint::sdk::prelude::*`, `#[polint::rule]`, typed fact-view parameters, `polint::runner::run_cli`, `polint inspect rule --format json`, `polint test --format json`, and any newly stable `facts`/`unknowns`/`explain` commands. Keep `CallGraph<'_>`, `DataFlow<'_>`, `Cfg<'_>`, `Evidence<'_>`, model packs, provider extensions, and `polint eval` marked reserved/preview/internal unless implemented as stable in prior plans.</action>
  <verify>
    <automated>rg -n "use polint::sdk::prelude::\\*|polint inspect rule --format json|polint test --format json" README.md docs/facts .claude/skills/polint/SKILL.md .agents/skills/polint/SKILL.md</automated>
    <automated>cargo test -p polint --test cli generated_skills_describe_phase41_public_surface --locked</automated>
  </verify>
  <acceptance_criteria>
    - Docs and skills include promoted helper methods and stable commands with exact names.
    - Docs and skills do not tell agents to import `polint::core`, parser adapters, `AnalysisDb`, provider modules, or internal eval/debug APIs.
    - Reserved advanced views remain described as reserved or future unless tests prove promotion.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add schema and compatibility regression coverage</name>
  <files>docs/schemas, crates/polint/tests/cli.rs</files>
  <read_first>
    - docs/schemas
    - crates/polint/tests/cli.rs
    - crates/polint/src/cli/mod.rs
    - crates/polint/src/rule_manifest.rs
    - crates/polint/src/rule_test.rs
  </read_first>
  <action>Add or update tests that parse every stable Phase 41 public JSON command into `serde_json::Value`, validate required schema/version/tool fields, assert deterministic ordering across two runs, and assert no private provider/debug/eval vocabulary appears in stable JSON fields. Per D-20, include at least one temp-repo public-SDK test for each new rule-authoring feature, compiling generated `.polint/rules`, importing only `polint::sdk::prelude::*`, consuming real typed facts, running `polint check --format json`, and asserting diagnostics or command JSON. Include inspect/test and any newly stable facts/unknowns/explain commands.</action>
  <verify>
    <automated>cargo test -p polint --test cli phase41_public_json_contracts_are_stable --locked</automated>
  </verify>
  <acceptance_criteria>
    - Every stable public JSON command has a checked-in schema or explicit schema URL.
    - Repeated command runs produce stable row ordering.
    - Tests fail on private provider IDs or internal debug/eval field names in stable JSON.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Final workspace verification and closeout evidence</name>
  <files>.planning/phases/41-public-sdk-query-views-and-agent-ergonomics/41-VERIFICATION.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/phases/41-public-sdk-query-views-and-agent-ergonomics/41-CONTEXT.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - docs/API-VISIBILITY-PLAN.md
  </read_first>
  <action>Run the final verification command set, write `41-VERIFICATION.md` with promoted surfaces, deferred surfaces, commands run, temp-repo proof, docs/schema proof, and no-leak proof. Update roadmap/state through GSD tooling when closing the phase; do not directly hand-edit state progress if a GSD handler exists.</action>
  <verify>
    <automated>cargo fmt --all --check</automated>
    <automated>cargo clippy --workspace --all-targets --locked -- -D warnings</automated>
    <automated>cargo test --workspace --all-targets --locked</automated>
    <automated>cargo doc -p polint --all-features --no-deps --locked</automated>
  </verify>
  <acceptance_criteria>
    - `41-VERIFICATION.md` maps SAE-PROM-02 to concrete public SDK views, command JSON contracts, docs, and tests.
    - Deferred surfaces are listed with reasons and required future gates.
    - Full workspace tests, clippy, fmt, and docs pass or failures are captured with exact remediation.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated skills -> agent behavior | Skills must not instruct agents to use unsupported APIs or internals. |
| public docs -> product contract | README/docs/facts/schema text become support commitments. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T41-09 | Information Disclosure | docs and generated skills | Mitigated | Source-surface tests for internal imports, provider IDs, and eval/debug terms. |
| T41-10 | Repudiation | phase closeout | Mitigated | Verification maps every promoted/deferred surface to evidence and commands. |
</threat_model>

<verification>
- `cargo test -p polint --test cli generated_skills_describe_phase41_public_surface --locked`
- `cargo test -p polint --test cli phase41_public_json_contracts_are_stable --locked`
- `cargo fmt --all --check`
- `cargo clippy --workspace --all-targets --locked -- -D warnings`
- `cargo test --workspace --all-targets --locked`
- `cargo doc -p polint --all-features --no-deps --locked`
</verification>

<success_criteria>
Phase 41 closes v1.2 promotion with documented, tested, stable public ergonomics and an explicit list of advanced surfaces that remain deferred.
</success_criteria>
