---
phase: 33-demand-queries-and-summary-scc-cache
plan: 06
type: execute
wave: 4
depends_on: [33-03, 33-04, 33-05]
files_modified:
  - crates/polint/src/analysis/summaries/validate.rs
  - crates/polint/src/analysis_kernel/validation.rs
  - crates/polint/src/analysis_kernel/debug.rs
autonomous: true
requirements: [SAE-INT-03]
must_haves:
  truths:
    - "Demand query results are validated for correct query keys, precision tiers, and provenance"
    - "SCC scheduling debug output includes number of SCCs, sizes, ordering, iteration counts, and backdating events"
    - "Query trace debug output includes query kind, cache status, compute time, and result digest"
    - "Validation catches invalid demand query results"
  artifacts:
    - path: "crates/polint/src/analysis/summaries/validate.rs"
      provides: "Extended validation covering SCC closure results and demand query consistency"
    - path: "crates/polint/src/analysis_kernel/debug.rs"
      provides: "Debug JSON with SCC scheduling and demand query trace sections"
  key_links:
    - from: "crates/polint/src/analysis/summaries/validate.rs"
      to: "crates/polint/src/analysis/summaries/closure.rs"
      via: "SCC closure result validation"
      pattern: "SccClosureResult|budget_exceeded"
    - from: "crates/polint/src/analysis_kernel/debug.rs"
      to: "crates/polint/src/analysis_kernel/incremental/demand.rs"
      via: "DemandQueryTrace rendered to debug JSON"
      pattern: "DemandQueryTrace|demand_query"
---

<objective>
Extend validation and debug output for demand queries, SCC scheduling, and quarantine.

Purpose: Per D-16, validate that demand query results have valid keys, consistent precision tiers, non-stale digests, and correct provenance. Per D-14, SCC scheduling debug output records number of SCCs, sizes, processing order, iteration counts for recursive SCCs, backdating events, and total functions. Per D-13, query trace records query kind, precision tier, cache status, compute time, and result digest. All debug output stays crate-private and test-facing per established pattern.

Output: Extended validate.rs with SCC/demand checks. Extended debug.rs with SCC and demand query trace debug JSON sections.
</objective>

<execution_context>
@/Users/emilwareus/conductor/workspaces/exlint/islamabad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/conductor/workspaces/exlint/islamabad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/33-demand-queries-and-summary-scc-cache/33-CONTEXT.md
@.planning/phases/33-demand-queries-and-summary-scc-cache/33-PATTERNS.md

@crates/polint/src/analysis/summaries/validate.rs
@crates/polint/src/analysis_kernel/validation.rs
@crates/polint/src/analysis_kernel/debug.rs

<interfaces>
<!-- Key types for validation and debug -->

From summaries/validate.rs:
validate_summaries(db: &AnalysisDb, diagnostics: &mut Vec<Diagnostic>)

From validation.rs:
validate_fact_metadata(db, manifests) -> Vec<Diagnostic>
Validation orchestration calls: validate_semantic_mir, validate_cfg, validate_calls, validate_abstract_domains, validate_summaries, validate_metadata_providers, validate_precision_ceilings

From debug.rs:
metadata_debug_json_for_test(db: &AnalysisDb) -> Value
MetadataDebugReport { files, imports, symbols, references, semantic, mir, cfg, calls, abstract_domains, summaries }
SummaryDebugReport { summaries, events, counts, domain_counts }

From closure.rs (Plan 33-04):
SccClosureResult { total_sccs_processed, non_recursive_sccs, recursive_sccs, budget_exceeded_sccs, backdated_sccs, total_iterations, updated_summaries, scc_iteration_counts }

From demand.rs (Plan 33-02):
DemandQueryTrace with entries() -> &[DemandQueryTraceEntry]
DemandQueryTraceEntry { query_kind, cache_status, result_digest, compute_duration_micros, ... }

From run_report.rs:
KernelRunReport with demand_query_trace() -> &DemandQueryTrace
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend summary validation for SCC closure and demand query consistency</name>
  <files>crates/polint/src/analysis/summaries/validate.rs, crates/polint/src/analysis_kernel/validation.rs</files>
  <read_first>
    crates/polint/src/analysis/summaries/validate.rs
    crates/polint/src/analysis_kernel/validation.rs
    crates/polint/src/analysis/summaries/facts.rs
    crates/polint/src/analysis/summaries/closure.rs
    crates/polint/src/analysis/summaries/domain.rs
  </read_first>
  <action>
    In validate.rs, add validation checks that run within the existing validate_summaries function (or as a new validate_scc_closure_results helper called from validate_summaries):

    1. Check that summary facts with SummaryProvenance::InterproceduralClosure (or equivalent provenance label for SCC-produced summaries, to be defined alongside the closure module) have valid callable references — each function ID must exist in AnalysisDb.
    2. Check that BudgetExceeded summaries carry SummaryTopReason::BudgetExceeded evidence. Push a diagnostic if a BudgetExceeded status summary has no corresponding event with BudgetExceeded reason.
    3. Check that SCC-produced summaries do not claim FactPrecision::Exact — they should be at most SetupAware, since interprocedural closure introduces callee dependency uncertainty. This parallels the existing precision ceiling check for polint.direct_summaries.
    4. Check for orphaned SCC summaries — if a summary claims interprocedural provenance but its function has no direct call targets (CallStore outgoing is empty), push a warning diagnostic.

    In validation.rs, no structural changes needed — validate_summaries is already called in the validation sequence. The new checks run within validate_summaries.

    Add test in validate.rs that creates a malformed BudgetExceeded summary without a corresponding event and verifies a diagnostic is produced.
  </action>
  <verify>
    <automated>cd /Users/emilwareus/conductor/workspaces/exlint/kabul-v2 && cargo test --lib -p polint -- validate_summaries 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - validate_summaries checks BudgetExceeded summaries have corresponding events
    - validate_summaries rejects Exact precision on SCC-produced summaries
    - Malformed summary produces a diagnostic (not silently accepted)
    - Existing validation tests still pass
  </acceptance_criteria>
  <done>Summary validation extended to cover SCC closure result consistency, budget-exceeded evidence, and precision ceiling enforcement.</done>
</task>

<task type="auto">
  <name>Task 2: Extend debug JSON with SCC scheduling and demand query trace sections</name>
  <files>crates/polint/src/analysis_kernel/debug.rs</files>
  <read_first>
    crates/polint/src/analysis_kernel/debug.rs
    crates/polint/src/analysis/summaries/scc.rs
    crates/polint/src/analysis/summaries/closure.rs
    crates/polint/src/analysis_kernel/incremental/demand.rs
    crates/polint/src/analysis_kernel/incremental/run_report.rs
  </read_first>
  <action>
    In debug.rs, extend the MetadataDebugReport struct to include two new fields:
    - scc_schedule: SccScheduleDebugSection
    - demand_queries: DemandQueryDebugSection

    Define SccScheduleDebugSection (Serialize) with fields: total_sccs (usize), total_functions (usize), recursive_scc_count (usize), max_scc_size (usize), scc_sizes (Vec of usize), processing_order (Vec of SccDebugEntry). Define SccDebugEntry (Serialize) with fields: member_stable_keys (Vec of String), is_recursive (bool), size (usize).

    Define DemandQueryDebugSection (Serialize) with fields: total_queries (usize), cache_hits (usize), cache_misses (usize), entries (Vec of DemandQueryDebugEntry). Define DemandQueryDebugEntry (Serialize) with fields: query_kind (String), cache_status (String), result_digest_prefix (String — first 16 chars), compute_duration_micros (u64).

    Update metadata_debug_json_for_test to accept the KernelRunReport (or separate SccSchedule and DemandQueryTrace parameters) to populate these sections. Since metadata_debug_json_for_test currently takes only AnalysisDb, add an overloaded version or extend the signature to accept optional SCC/demand data. If the data is absent, emit empty default sections. The SCC schedule data should be extracted from AnalysisDb's summary/call stores by re-running compute_scc_schedule inside the debug function (since it's test-only and cheap).

    The demand query trace comes from KernelRunReport. Add a new test-only accessor to AnalysisKernel that includes the KernelRunReport data in debug JSON — or use the established pattern where debug JSON is built from AnalysisDb plus supplementary data passed as parameters.

    Add a test that verifies the debug JSON includes "scc_schedule" and "demand_queries" keys with the expected structure.
  </action>
  <verify>
    <automated>cd /Users/emilwareus/conductor/workspaces/exlint/kabul-v2 && cargo test --lib -p polint -- metadata_debug 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - MetadataDebugReport has scc_schedule and demand_queries fields
    - SCC debug section includes total_sccs, recursive_scc_count, scc_sizes, and processing_order with member stable keys
    - Demand query debug section includes total_queries, cache_hits, cache_misses, and per-entry query_kind and cache_status
    - Debug JSON output includes "scc_schedule" and "demand_queries" keys
    - All debug output is crate-private (behind #[cfg(test)])
    - All tests pass
  </acceptance_criteria>
  <done>Debug JSON extended with SCC scheduling statistics and demand query trace per D-13 and D-14. All output is crate-private and test-facing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Validation and debug are internal-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-09 | Information Disclosure | Debug JSON | mitigate | Debug JSON behind #[cfg(test)], no public surface |
| T-33-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies added |
</threat_model>

<verification>
cargo test --lib -p polint -- validate_summaries 2>&1 | tail -20
cargo test --lib -p polint -- metadata_debug 2>&1 | tail -20
cargo clippy -p polint -- -D warnings 2>&1 | tail -10
</verification>

<success_criteria>
- Validation catches invalid SCC closure results (BudgetExceeded without event, Exact precision)
- Debug JSON includes SCC scheduling with sizes, order, iteration counts
- Debug JSON includes demand query trace with kind, status, digest
- All output is crate-private and test-facing
- All tests pass
</success_criteria>

<output>
Create `.planning/phases/33-demand-queries-and-summary-scc-cache/33-06-SUMMARY.md` when done
</output>
