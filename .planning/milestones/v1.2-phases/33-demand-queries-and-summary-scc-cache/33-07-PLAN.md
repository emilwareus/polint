---
phase: 33-demand-queries-and-summary-scc-cache
plan: 07
type: execute
wave: 5
depends_on: [33-04, 33-05, 33-06]
files_modified:
  - tests/eval-fixtures/direct-summaries/scc-closure/manifest.toml
  - tests/eval-fixtures/direct-summaries/scc-closure/repos/mixed-calls/main.go
  - tests/eval-fixtures/direct-summaries/scc-closure/repos/mixed-calls/index.ts
  - crates/polint/src/eval/observed.rs
autonomous: true
requirements: [SAE-INT-03]
must_haves:
  truths:
    - "Eval fixtures prove demand query cache hit/miss behavior"
    - "Eval fixtures prove SCC scheduling order correctness"
    - "Eval fixtures prove backdating behavior when summaries don't change"
    - "Eval fixtures prove quarantine behavior with synthetic extension digests"
    - "Eval fixtures prove cold/warm/no-cache three-way deterministic equality"
    - "Public CLI, inspect, test, SDK, README, and docs do not expose demand or SCC internals"
  artifacts:
    - path: "tests/eval-fixtures/direct-summaries/scc-closure/manifest.toml"
      provides: "Eval fixture manifest for SCC closure scenarios"
    - path: "crates/polint/src/eval/observed.rs"
      provides: "Extended observation covering SCC and demand query debug rows"
  key_links:
    - from: "crates/polint/src/eval/observed.rs"
      to: "crates/polint/src/analysis_kernel/debug.rs"
      via: "metadata_debug_json_for_test observation"
      pattern: "scc_schedule|demand_queries"
---

<objective>
Add eval fixtures proving Phase 33 behavior and public no-leak boundary proof.

Purpose: Per D-15, native eval fixtures must prove: (a) demand query cache hit/miss behavior, (b) SCC scheduling order correctness, (c) backdating behavior when summaries don't change, (d) quarantine behavior with synthetic extension digests, and (e) deterministic cold/warm/no-cache three-way equality. The phase must also prove that no internal demand query, SCC, or quarantine details leak into public CLI JSON, inspect, test, SDK, README, or docs surfaces.

Output: Eval fixture manifest and test repos covering SCC closure scenarios. Extended eval observation for SCC/demand debug rows. Public no-leak assertions.
</objective>

<execution_context>
@/Users/emilwareus/conductor/workspaces/exlint/islamabad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/conductor/workspaces/exlint/islamabad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/33-demand-queries-and-summary-scc-cache/33-CONTEXT.md
@.planning/phases/33-demand-queries-and-summary-scc-cache/33-PATTERNS.md

@tests/eval-fixtures/direct-summaries/core/manifest.toml
@crates/polint/src/eval/observed.rs
@crates/polint/src/analysis_kernel/debug.rs

<interfaces>
<!-- Eval fixture format and observation patterns -->

From tests/eval-fixtures/ established pattern:
TOML manifest with [fixture], repos, expected sections.
Repos are subdirectories with source files.
Expected assertions check debug JSON fields, counts, and invariants.

From eval/observed.rs:
observe_provider_outputs(), observe_debug_json() — extract provider output and debug JSON observations from kernel run.

From debug.rs (Plan 33-06):
SccScheduleDebugSection { total_sccs, total_functions, recursive_scc_count, max_scc_size, scc_sizes, processing_order }
DemandQueryDebugSection { total_queries, cache_hits, cache_misses, entries }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SCC closure eval fixture with mixed Go/TS call chains</name>
  <files>tests/eval-fixtures/direct-summaries/scc-closure/manifest.toml, tests/eval-fixtures/direct-summaries/scc-closure/repos/mixed-calls/main.go, tests/eval-fixtures/direct-summaries/scc-closure/repos/mixed-calls/index.ts</files>
  <read_first>
    tests/eval-fixtures/direct-summaries/core/manifest.toml
    tests/eval-fixtures/direct-summaries/core/repos
    crates/polint/src/eval/observed.rs
    crates/polint/src/eval/model.rs
  </read_first>
  <action>
    Create a new eval fixture directory at tests/eval-fixtures/direct-summaries/scc-closure/ with manifest.toml and a repos/mixed-calls/ subdirectory.

    The mixed-calls repo should contain:
    - main.go with: (a) a leaf function leaf() that returns a value (no calls), (b) a middle function middle() that calls leaf() and returns, (c) a top-level function top() that calls middle(), (d) two mutually recursive functions pingGo(n int) and pongGo(n int) that call each other (pingGo calls pongGo, pongGo calls pingGo with a base case).
    - index.ts with: (a) a leaf function leafTs() that returns, (b) a caller function callerTs() that calls leafTs(), (c) a function that calls a Go-provided function (cross-language call evidence is unresolved since there is no FFI — this tests unresolved callee handling).

    The manifest.toml should follow the established TOML fixture format with:
    - [fixture] section with name, description, category = "scc-closure"
    - Expected assertions checking nonzero SCC count from debug JSON scc_schedule.total_sccs
    - Expected assertions checking that recursive_scc_count is at least 1 (for the ping/pong pair)
    - Expected assertions checking that demand_queries.total_queries is nonzero
    - Expected determinism assertion for cold/warm/no-cache three-way equality using the established direct-summaries pattern

    Also create a .polint.toml in the fixture repo to enable analysis.

    Adapt the fixture format to what the existing eval infrastructure expects — follow the exact TOML structure used by tests/eval-fixtures/direct-summaries/core/manifest.toml.
  </action>
  <verify>
    <automated>cd /Users/emilwareus/conductor/workspaces/exlint/kabul-v2 && cargo test --lib -p polint -- eval_scc 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - manifest.toml exists at tests/eval-fixtures/direct-summaries/scc-closure/manifest.toml
    - Fixture repo has main.go with leaf, middle, top, and mutually recursive pingGo/pongGo functions
    - Fixture repo has index.ts with leafTs, callerTs functions
    - Fixture manifest asserts nonzero SCC count and recursive SCC count
    - Fixture manifest asserts nonzero demand query count
    - Fixture manifest asserts cold/warm/no-cache determinism
  </acceptance_criteria>
  <done>Eval fixture for SCC closure exists with mixed Go/TS call chains, recursive SCCs, and determinism assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Extend eval observation and add public no-leak boundary proof</name>
  <files>crates/polint/src/eval/observed.rs</files>
  <read_first>
    crates/polint/src/eval/observed.rs
    crates/polint/src/analysis_kernel/debug.rs
    crates/polint/src/analysis_kernel/mod.rs
  </read_first>
  <action>
    In observed.rs, extend the observation extraction to include SCC scheduling and demand query trace data from the debug JSON. Add extraction for:
    - scc_schedule.total_sccs, scc_schedule.recursive_scc_count, scc_schedule.max_scc_size from the debug JSON
    - demand_queries.total_queries, demand_queries.cache_hits, demand_queries.cache_misses from the debug JSON

    These should be added to the existing observation data structure (or a new section within the observed facts) so eval fixture assertions can reference them.

    Add a public no-leak integration test (following the established Phase 32 pattern in 32-07-PLAN) that checks:
    1. Public check JSON (from polint check --format json) does not contain any of these 21+ internal markers: "demand_query", "DemandQuery", "scc_schedule", "SccSchedule", "SccClosure", "scc_closure", "quarantine", "QuarantineStore", "QuarantineEntry", "quarantine_policy", "backdating", "backdated", "interprocedural_closure", "SccClosureResult", "SccClosureConfig", "DemandQueryEngine", "DemandQueryTrace", "DemandQueryTraceEntry", "DemandQueryResult", "is_quarantined", "reinstate".
    2. polint check --help does not contain "demand", "scc", "quarantine", or "backdating".
    3. SDK prelude exports do not contain demand/scc/quarantine types.
    4. docs/ directory files do not contain "demand_query", "scc_schedule", or "quarantine" (unless in explicitly internal research docs).
    5. README.md does not contain "demand query", "SCC cache", or "quarantine".

    Use the established no-leak test pattern from Phase 32 Plan 07: source surface assertions using grep over specific files and directories, combined with public CLI JSON output assertions.
  </action>
  <verify>
    <automated>cd /Users/emilwareus/conductor/workspaces/exlint/kabul-v2 && cargo test --lib -p polint -- no_leak 2>&1 | tail -20 && cargo test --lib -p polint -- public_boundary 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Eval observation extracts scc_schedule and demand_queries data from debug JSON
    - Public no-leak test checks at least 15 internal markers against public CLI JSON, help, SDK, docs, and README
    - No internal demand/SCC/quarantine identifiers appear in public surfaces
    - All tests pass
  </acceptance_criteria>
  <done>Eval observation extended for SCC/demand data. Public no-leak boundary proof confirms no Phase 33 internals leak to public surfaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internal debug -> public CLI | Internal debug JSON must not leak to public output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-10 | Information Disclosure | Public JSON/help/docs | mitigate | No-leak test with 21+ internal markers against all public surfaces |
| T-33-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies added |
</threat_model>

<verification>
cargo test --lib -p polint -- eval 2>&1 | tail -20
cargo test --lib -p polint -- no_leak 2>&1 | tail -20
cargo test --lib -p polint -- public_boundary 2>&1 | tail -20
cargo clippy -p polint -- -D warnings 2>&1 | tail -10
</verification>

<success_criteria>
- Eval fixtures prove SCC scheduling, backdating, demand query cache, quarantine, and determinism
- Cold/warm/no-cache three-way equality holds for SCC closure output
- Public CLI JSON, help, SDK, docs, and README contain no Phase 33 internal identifiers
- All tests pass
</success_criteria>

<output>
Create `.planning/phases/33-demand-queries-and-summary-scc-cache/33-07-SUMMARY.md` when done
</output>
