---
phase: 33-demand-queries-and-summary-scc-cache
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/polint/src/analysis_kernel/incremental/keys.rs
  - crates/polint/src/analysis/summaries/provider.rs
  - crates/polint/src/analysis_kernel/mod.rs
autonomous: true
requirements: [SAE-INT-03]
must_haves:
  truths:
    - "Direct summaries layer cache persists summary output to disk on cold runs"
    - "Direct summaries layer cache restores summary output from disk on warm runs"
    - "Summary layer cache identity includes all upstream provider output digests"
  artifacts:
    - path: "crates/polint/src/analysis/summaries/provider.rs"
      provides: "Layer cache read/write wiring for direct summaries"
    - path: "crates/polint/src/analysis_kernel/incremental/keys.rs"
      provides: "Activated direct_summaries_layer_key without dead_code attribute"
  key_links:
    - from: "crates/polint/src/analysis/summaries/provider.rs"
      to: "crates/polint/src/analysis_kernel/incremental/layer_cache.rs"
      via: "LayerCacheStore::read_json_validated and write_json"
      pattern: "store\\.read_json_validated|store\\.write_json"
    - from: "crates/polint/src/analysis/summaries/provider.rs"
      to: "crates/polint/src/analysis_kernel/incremental/keys.rs"
      via: "LayerKey::direct_summaries_layer_key"
      pattern: "direct_summaries_layer_key"
---

<objective>
Activate the reserved `direct_summaries_layer_key()` function and wire layer cache persistence into the direct summaries provider.

Purpose: This is the fastest Phase 33 win per D-11 and D-12. Summaries computed in Phase 32 are currently always recomputed. Adding layer cache read/write gives immediate warm-run performance improvement and establishes the cache identity pattern that SCC-level caching builds upon.

Output: Direct summaries provider persists/restores summary layer payloads through the established layer cache infrastructure. Cold runs write, warm runs read. Cache identity includes all upstream digests per D-12.
</objective>

<execution_context>
@/Users/emilwareus/conductor/workspaces/exlint/islamabad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/conductor/workspaces/exlint/islamabad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-demand-queries-and-summary-scc-cache/33-CONTEXT.md
@.planning/phases/33-demand-queries-and-summary-scc-cache/33-PATTERNS.md

@crates/polint/src/analysis/summaries/provider.rs
@crates/polint/src/analysis_kernel/incremental/keys.rs
@crates/polint/src/analysis_kernel/mod.rs
@crates/polint/src/metrics.rs

<interfaces>
<!-- Key types and contracts the executor needs. -->

From crates/polint/src/analysis_kernel/incremental/keys.rs (lines 675-747):
LayerKey::direct_summaries_layer_key() — fully implemented, marked #[expect(dead_code)]. Takes manifest, 11 digest parameters, returns LayerKey with LayerKind::DirectSummaries.

From crates/polint/src/analysis_kernel/incremental/layer_cache.rs:
LayerCacheStore::read_json_validated::<T, F>(key, validator) -> LayerCacheReadOutcome<T>
LayerCacheStore::write_json(manifest, payload) -> Result<LayerCacheWriteStatus, Error>
LayerCacheStore::payload_digest_for_json(payload) -> Result<Digest, Error>
LayerCacheReadStatus::{Hit, Miss, InvalidEvicted, BypassedDisabled}
LayerCacheWriteStatus::{Written, BypassedDisabled}
LayerCacheManifest::new(key, output_digest, payload_digest, dependencies, precision, validation, warnings)

From crates/polint/src/analysis/summaries/store.rs:
SummaryOutput { summaries: Vec<SummaryFact>, events: Vec<SummaryEventFact> }
SummaryOutput::normalized() -> Self

From crates/polint/src/analysis/summaries/provider.rs:
derive_direct_summaries_with_cache_stats() — currently always recomputes, never uses layer cache.
DirectSummariesProviderOutput { diagnostics, cache_stats, output_digest }

From crates/polint/src/metrics.rs (layer cache analog — lines 58-120):
Pattern: compute layer_key -> store.read_json_validated -> match Hit/Miss/InvalidEvicted -> restore or recompute+write.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Activate direct_summaries_layer_key and add layer payload type</name>
  <files>crates/polint/src/analysis_kernel/incremental/keys.rs, crates/polint/src/analysis/summaries/provider.rs</files>
  <read_first>
    crates/polint/src/analysis_kernel/incremental/keys.rs
    crates/polint/src/analysis/summaries/provider.rs
    crates/polint/src/analysis/summaries/store.rs
    crates/polint/src/analysis/summaries/facts.rs
    crates/polint/src/metrics.rs
  </read_first>
  <action>
    In keys.rs, remove the two #[expect(...)] attributes from direct_summaries_layer_key (lines 676-680). Keep the function body unchanged. The function is already fully implemented.

    In provider.rs, add a DirectSummariesLayerPayload struct (Serialize, Deserialize, Clone, Debug, PartialEq, Eq) wrapping the SummaryOutput fields needed for cache restore: summaries (Vec of SummaryFact) and events (Vec of SummaryEventFact). Add a validate_direct_summaries_layer_payload function following the metrics.rs validate_metrics_layer_payload pattern — check that the payload schema_version matches the expected schema label from the provider manifest.

    Add a build_direct_summaries_layer_payload function that extracts the current SummaryOutput from AnalysisDb into a DirectSummariesLayerPayload. Add a restore_direct_summaries_layer_payload function that calls db.replace_summary_facts with a SummaryOutput reconstructed from the payload.

    Add a write_direct_summaries_layer_payload helper following the write_metrics_layer_payload pattern from metrics.rs: compute payload_digest, build output_digest, construct LayerCacheManifest with PrecisionTier::SetupAware and "native_trusted" validation label, call store.write_json, record stats. Return Option of Digest.

    Add a direct_summaries_layer_dependency_edges function that builds DependencyEdge entries from the layer key to upstream layer digests (semantic_mir, cfg, calls, abstract_domains, symbol_graph, module_topology, syntax digests), following the metrics_layer_dependency_edges pattern.

    Restructure derive_direct_summaries_with_cache_stats to: (1) accept cache: &Cache as a new parameter, (2) build the LayerKey via LayerKey::direct_summaries_layer_key using all provided upstream digests plus source_function_digests from InputSnapshot, (3) attempt cache read via store.read_json_validated, (4) on Hit restore payload and return early with cache_stats.record_hit/record_verified_reuse, (5) on Miss/InvalidEvicted build from DirectSummaryBuilder::build, write payload, return with appropriate stats. Per D-12, cache identity must include semantic_mir, cfg, calls, abstract_domains, symbol_graph, module_topology, syntax, config, lifecycle, and absent extension/model/toolchain digests.

    The DirectSummariesLayerPayload must include a schema_version field set to the provider's primary_schema_label to enable future schema migration validation.
  </action>
  <verify>
    <automated>cd /Users/emilwareus/conductor/workspaces/exlint/kabul-v2 && cargo test --lib -p polint -- direct_summaries_provider 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - keys.rs: The #[expect(dead_code, ...)] and #[expect(clippy::too_many_arguments, ...)] attributes are removed from direct_summaries_layer_key. The clippy::too_many_arguments allow can be kept with a non-dead-code reason.
    - provider.rs: DirectSummariesLayerPayload struct exists with Serialize+Deserialize derives and a schema_version: String field.
    - provider.rs: derive_direct_summaries_with_cache_stats takes a cache: &Cache parameter.
    - provider.rs: LayerKey::direct_summaries_layer_key is called with all upstream digest parameters.
    - provider.rs: LayerCacheStore::read_json_validated is called and the result is matched on Hit/Miss/InvalidEvicted.
    - provider.rs: On cache miss, LayerCacheStore::write_json is called to persist the payload.
    - Existing test provider_accepts_empty_output_with_deterministic_digest compiles and passes.
  </acceptance_criteria>
  <done>Direct summaries layer cache read/write is wired. Cold runs recompute and write. The direct_summaries_layer_key function is no longer dead code.</done>
</task>

<task type="auto">
  <name>Task 2: Wire cache parameter through kernel and verify warm-run restore</name>
  <files>crates/polint/src/analysis_kernel/mod.rs, crates/polint/src/analysis/summaries/provider.rs</files>
  <read_first>
    crates/polint/src/analysis_kernel/mod.rs
    crates/polint/src/analysis/summaries/provider.rs
    crates/polint/src/metrics.rs
  </read_first>
  <action>
    In analysis_kernel/mod.rs, update the call to derive_direct_summaries_with_cache_stats (around lines 308-323) to pass input.cache as the new cache parameter. Also rename _direct_summaries_output_digest to direct_summaries_output_digest (remove the leading underscore) since the output digest is now used for cache identity and will be consumed by later plans for SCC cache inputs.

    Construct the source_function_digests parameter for the layer key by collecting function-body digests from MIR bodies in AnalysisDb. Use a pattern similar to how the metrics provider builds its source-level input digests — iterate db.mir_bodies(), collect (stable_key, body content digest or body shape evidence) into sorted digest parts, and produce a Vec<Digest>. If MIR bodies are empty, use an empty Vec.

    Add a test in provider.rs that creates a temp directory with .polint.toml, runs derive_direct_summaries_with_cache_stats twice with the same inputs and a real Cache, and asserts that: (a) the first call records cache_stats.misses == 1 or cache_stats.recomputes == 1, (b) the second call records cache_stats.hits == 1, (c) both calls produce the same output_digest. This proves warm-run restore works end-to-end.
  </action>
  <verify>
    <automated>cd /Users/emilwareus/conductor/workspaces/exlint/kabul-v2 && cargo test --lib -p polint -- direct_summaries_provider 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - analysis_kernel/mod.rs: derive_direct_summaries_with_cache_stats is called with input.cache as the cache argument.
    - analysis_kernel/mod.rs: The variable is named direct_summaries_output_digest (no leading underscore).
    - provider.rs: A test exists that exercises cold-then-warm cache behavior and asserts hits == 1 on the second run.
    - cargo test passes with no failures.
  </acceptance_criteria>
  <done>Summary layer cache is fully activated end-to-end. Cold runs write payloads. Warm runs restore from cache. The output digest is live and available for downstream consumption.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| layer_cache disk -> provider | Cached payload read from .polint/cache/layers may be stale or malformed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Tampering | layer_cache payload | mitigate | validate_direct_summaries_layer_payload checks schema_version before restore |
| T-33-02 | Information Disclosure | layer_cache payloads | accept | Cache files are local to the repo working directory, same trust as source |
| T-33-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies added in this plan |
</threat_model>

<verification>
cargo test --lib -p polint -- direct_summaries 2>&1 | tail -20
cargo clippy -p polint -- -D warnings 2>&1 | tail -10
</verification>

<success_criteria>
- direct_summaries_layer_key is activated (no dead_code attribute)
- Layer cache read/write is wired into the summaries provider
- Cold run writes summary payload to .polint/cache/layers
- Warm run restores summary payload from cache
- Output digest is deterministic across cold and warm runs
- All existing tests pass
</success_criteria>

<output>
Create `.planning/phases/33-demand-queries-and-summary-scc-cache/33-01-SUMMARY.md` when done
</output>
