---
phase: 40-external-benchmark-adapters-and-promotion-gates
plan: 05
title: Supported Language Smoke Suites And Tier Runner
type: execute
wave: 5
depends_on: [40-02, 40-03, 40-04]
files_modified:
  - crates/polint/src/eval/runner.rs
  - crates/polint/src/eval/tiers.rs
  - crates/polint/src/eval/external/secbench_js.rs
  - crates/polint/src/eval/external/gosec.rs
  - research/evaluation-harness/suites/secbench-js-smoke.toml
  - research/evaluation-harness/suites/gosec-samples.toml
autonomous: true
requirements: [SAE-PROM-01]
must_haves:
  truths:
    - "D-07/D-10: first real external analysis targets should match current Go and TS/JS support."
    - "D-25: fast/nightly/release/research tiers use deterministic case selection."
  artifacts:
    - path: "crates/polint/src/eval/runner.rs"
      provides: "Internal suite/tier runner over manifests"
    - path: "crates/polint/src/eval/external/secbench_js.rs"
      provides: "TS/JS smoke suite adapter shape"
    - path: "crates/polint/src/eval/external/gosec.rs"
      provides: "Go sample suite adapter shape"
---

<objective>
Add an internal tier runner and first supported-language external smoke suite adapters for TS/JS and Go, using deterministic small subsets rather than full release suites in normal planning/execution.
</objective>

<execution_context>
@.planning/phases/40-external-benchmark-adapters-and-promotion-gates/40-CONTEXT.md
@research/evaluation-harness/REPO-INDEX.md
@research/evaluation-harness/oss/benchmark-comparison.md
@research/evaluation-harness/implementation/polint-eval-path.md
@.agents/skills/rust-best-practices/SKILL.md
@AGENTS.md
</execution_context>

<context>
@crates/polint/src/eval/adapter.rs
@crates/polint/src/eval/suite.rs
@crates/polint/src/eval/report.rs
@crates/polint/src/analysis_kernel/mod.rs
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add deterministic tier and case selection runner</name>
  <files>crates/polint/src/eval/runner.rs, crates/polint/src/eval/tiers.rs, crates/polint/src/eval/mod.rs</files>
  <read_first>
    - crates/polint/src/eval/suite.rs
    - crates/polint/src/eval/adapter.rs
    - research/evaluation-harness/algorithms/benchmark-scheduling.md
  </read_first>
  <action>Add an internal runner that loads suite manifests, resolves `fast`, `nightly`, `release`, or `research` tiers, deterministically selects cases, prepares temporary workspaces, runs supported-language polint analysis where enabled, and writes normalized eval reports. Keep this callable from tests/internal tooling, not documented as stable public CLI.</action>
  <verify>
    <automated>cargo test -p polint --lib eval::runner --locked</automated>
    <automated>cargo test -p polint --lib eval::tiers --locked</automated>
  </verify>
  <acceptance_criteria>
    - Same suite, tier, seed, and source commit produce the same selected case ids.
    - Adapter-only suites do not run polint analysis.
    - Temporary workspaces cannot escape suite-owned or fixture-owned paths.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add SecBench.js smoke adapter shape</name>
  <files>crates/polint/src/eval/external/secbench_js.rs, research/evaluation-harness/suites/secbench-js-smoke.toml</files>
  <read_first>
    - research/evaluation-harness/REPO-INDEX.md
    - research/evaluation-harness/oss/benchmark-comparison.md
    - crates/polint/src/eval/adapter.rs
  </read_first>
  <action>Add a TS/JS smoke adapter that can enumerate a small deterministic subset from a local SecBench.js clone when present, map cases into canonical diagnostics/path expectations where metadata exists, and otherwise mark unsupported expected labels as limitations. The adapter should tolerate absent local clones by producing a clear skipped/fixture-only status for unit tests.</action>
  <verify>
    <automated>cargo test -p polint --lib eval::external::secbench_js --locked</automated>
  </verify>
  <acceptance_criteria>
    - Manifest records `bc3156219138` source commit from `REPO-INDEX.md`.
    - Unit tests do not require the full local clone.
    - Report limitations distinguish executable package setup gaps from polint analysis failures.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add gosec sample adapter shape</name>
  <files>crates/polint/src/eval/external/gosec.rs, research/evaluation-harness/suites/gosec-samples.toml</files>
  <read_first>
    - research/evaluation-harness/REPO-INDEX.md
    - crates/polint/src/eval/adapter.rs
    - crates/polint/src/eval/runner.rs
  </read_first>
  <action>Add a Go sample adapter that can enumerate pinned gosec sample/test cases where a local clone exists, map available expected finding metadata into canonical diagnostics, and record that gosec is a practical Go baseline but not broad independent ground truth. Include deterministic smoke subset selection.</action>
  <verify>
    <automated>cargo test -p polint --lib eval::external::gosec --locked</automated>
  </verify>
  <acceptance_criteria>
    - Manifest records `de65614d10a6` source commit from `REPO-INDEX.md`.
    - Adapter can skip gracefully when clone is absent.
    - Reports identify gosec samples as Go-specific practical baseline, not a universal benchmark.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external clone -> runner workspace | Local benchmark repos are untrusted input and must not escape temp workspaces. |
| smoke subset -> product claim | Small subsets must not be represented as full-suite release results. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T40-10 | Elevation of Privilege | benchmark preparation | Mitigated | Reject symlink/path escape and avoid executing benchmark package scripts by default. |
| T40-11 | Repudiation | subset reports | Mitigated | Store tier, selector, seed, suite commit, and selected case ids. |
</threat_model>

<verification>
- `cargo fmt --all --check`
- `cargo test -p polint --lib eval::runner --locked`
- `cargo test -p polint --lib eval::external::secbench_js --locked`
- `cargo test -p polint --lib eval::external::gosec --locked`
</verification>

<success_criteria>
Phase 40 has an internal tier runner and at least one supported-language smoke path for real polint analysis, while larger/full external suites remain tiered.
</success_criteria>
