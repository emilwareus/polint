---
phase: 40-external-benchmark-adapters-and-promotion-gates
plan: 07
title: Competitor Baseline Records And Promotion Gate Baselines
type: execute
wave: 7
depends_on: [40-03, 40-05, 40-06]
files_modified:
  - crates/polint/src/eval/competitors.rs
  - crates/polint/src/eval/baseline.rs
  - crates/polint/src/eval/gates.rs
  - research/evaluation-harness/baselines/README.md
autonomous: true
requirements: [SAE-PROM-01]
must_haves:
  truths:
    - "D-02/D-03: competitor baselines are sourced, versioned, and marked imported or locally reproduced."
    - "D-25/D-27: promotion gates compare current reports against explicit baselines and thresholds."
  artifacts:
    - path: "crates/polint/src/eval/baseline.rs"
      provides: "Baseline read/write/compare logic for eval reports"
    - path: "research/evaluation-harness/baselines/README.md"
      provides: "Policy for committed summaries vs non-vendored large benchmark outputs"
---

<objective>
Add competitor baseline records and baseline comparison gates so Phase 40 can show other-product results, current polint results, and regressions against prior polint reports without overclaiming unsupported suites.
</objective>

<execution_context>
@.planning/phases/40-external-benchmark-adapters-and-promotion-gates/40-CONTEXT.md
@research/evaluation-harness/FINAL-REPORT.md
@research/evaluation-harness/oss/benchmark-comparison.md
@research/evaluation-harness/algorithms/benchmark-scheduling.md
@.agents/skills/rust-best-practices/SKILL.md
@AGENTS.md
</execution_context>

<context>
@crates/polint/src/eval/competitors.rs
@crates/polint/src/eval/gates.rs
@crates/polint/src/eval/report.rs
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Validate competitor baseline records</name>
  <files>crates/polint/src/eval/competitors.rs</files>
  <read_first>
    - crates/polint/src/eval/competitors.rs
    - research/evaluation-harness/oss/benchmark-comparison.md
  </read_first>
  <action>Add validation for competitor records: product name, product version or published source, suite id/version, metric map, imported-vs-local reproduction status, command/config/artifact path for local reproductions, and limitations. Add constructors for known comparison categories such as Semgrep, CodeQL, gosec, and suite-native reference results without hardcoding their scores.</action>
  <verify>
    <automated>cargo test -p polint --lib eval::competitors --locked</automated>
  </verify>
  <acceptance_criteria>
    - Imported competitor records require source citation metadata.
    - Locally reproduced competitor records require command/config/version/artifact metadata.
    - Competitor records sort deterministically by suite/product/version/source.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add baseline read/write and regression comparison</name>
  <files>crates/polint/src/eval/baseline.rs, crates/polint/src/eval/mod.rs</files>
  <read_first>
    - crates/polint/src/eval/report.rs
    - crates/polint/src/eval/gates.rs
    - research/evaluation-harness/algorithms/benchmark-scheduling.md
  </read_first>
  <action>Add crate-private baseline files over normalized eval reports. Support writing a baseline, loading a prior baseline, comparing scanner/graph/path/performance/cache/adaptation metrics, and producing thresholded gate findings for recall drop, precision drop, new false-positive traps, determinism drift, runtime regression, cache regression, and extension rejection changes.</action>
  <verify>
    <automated>cargo test -p polint --lib eval::baseline --locked</automated>
  </verify>
  <acceptance_criteria>
    - Baseline comparison is deterministic and path-normalized.
    - Threshold tests cover pass, warning, and fail cases.
    - Adapter-only baselines cannot be compared as real polint analysis results.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Document baseline artifact policy</name>
  <files>research/evaluation-harness/baselines/README.md</files>
  <read_first>
    - research/evaluation-harness/REPO-INDEX.md
    - research/evaluation-harness/FINAL-REPORT.md
  </read_first>
  <action>Add a research README explaining which baseline artifacts may be committed: small normalized summaries, pinned manifest metadata, and generated Markdown summaries. Explicitly forbid committing large benchmark corpora, raw third-party outputs without license review, or machine-local paths.</action>
  <verify>
    <automated>rg -n "Do not commit|source commit|license" research/evaluation-harness/baselines/README.md</automated>
  </verify>
  <acceptance_criteria>
    - README states source commit/version requirements.
    - README distinguishes local generated reports from committed baseline summaries.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| competitor claims -> polint report | Imported/public results must not be silently treated as locally reproduced. |
| baseline files -> regression gate | Baselines must be normalized and suite-version-specific. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T40-14 | Spoofing | competitor records | Mitigated | Require source/reproduction metadata and product versions. |
| T40-15 | Tampering | baselines | Mitigated | Compare normalized reports and include output hashes/suite commits. |
</threat_model>

<verification>
- `cargo fmt --all --check`
- `cargo test -p polint --lib eval::competitors --locked`
- `cargo test -p polint --lib eval::baseline --locked`
- `cargo test -p polint --lib eval::gates --locked`
</verification>

<success_criteria>
Reports can include comparable competitor/product records and compare current polint results against normalized baselines with explicit thresholded promotion gates.
</success_criteria>
