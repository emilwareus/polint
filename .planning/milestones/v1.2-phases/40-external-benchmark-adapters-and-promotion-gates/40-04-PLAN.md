---
phase: 40-external-benchmark-adapters-and-promotion-gates
plan: 04
title: Native Graph Fact Path Promotion Gates
type: execute
wave: 4
depends_on: [40-03]
files_modified:
  - crates/polint/src/eval/gates.rs
  - crates/polint/src/eval/observed.rs
  - tests/eval-fixtures/promotion/cfg-call-flow-evidence/expected.polint-eval.toml
  - tests/eval-fixtures/promotion/cfg-call-flow-evidence/repo
autonomous: true
requirements: [SAE-PROM-01]
must_haves:
  truths:
    - "D-06/D-09: benchmark gates cover scanner outcomes plus graph/fact/path and engine/adaptation quality."
    - "D-24: native fixtures remain the first promotion gate before external suites."
  artifacts:
    - path: "crates/polint/src/eval/gates.rs"
      provides: "Promotion gate verdicts over native fixture metrics"
    - path: "tests/eval-fixtures/promotion/cfg-call-flow-evidence/expected.polint-eval.toml"
      provides: "Native promotion fixture covering CFG/call/data-flow/evidence rows"
---

<objective>
Add native promotion gates and fixtures that prove CFG/call/data-flow/evidence facts, graph/path matching, unknown accounting, budgets, and deterministic report behavior before external benchmark suites are trusted.
</objective>

<execution_context>
@.planning/phases/40-external-benchmark-adapters-and-promotion-gates/40-CONTEXT.md
@research/evaluation-harness/FINAL-REPORT.md
@research/data-flow/VALIDATION.md
@research/call-graphs/VALIDATION.md
@research/program-slicing-evidence/VALIDATION.md
@.agents/skills/rust-best-practices/SKILL.md
@AGENTS.md
</execution_context>

<context>
@crates/polint/src/eval/observed.rs
@crates/polint/src/eval/matcher.rs
@crates/polint/src/eval/metrics.rs
@tests/eval-fixtures/README.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add promotion gate verdict model</name>
  <files>crates/polint/src/eval/gates.rs, crates/polint/src/eval/mod.rs</files>
  <read_first>
    - crates/polint/src/eval/metrics.rs
    - crates/polint/src/eval/report.rs
    - research/evaluation-harness/algorithms/benchmark-scheduling.md
  </read_first>
  <action>Add crate-private gate types for pass/fail/warn verdicts with thresholds for determinism, required native fixture pass rate, graph required-edge misses, path misses, unknown budget limits, rejected facts, runtime budget failures, and cache determinism. Include suite/tier-specific threshold config structs.</action>
  <verify>
    <automated>cargo test -p polint --lib eval::gates --locked</automated>
  </verify>
  <acceptance_criteria>
    - Gate tests cover pass, fail, and warning verdicts.
    - Gate output names the failing metric and threshold.
    - Gate input is normalized report data, not ad hoc string parsing.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add native promotion fixture for graph/fact/path families</name>
  <files>tests/eval-fixtures/promotion/cfg-call-flow-evidence/expected.polint-eval.toml, tests/eval-fixtures/promotion/cfg-call-flow-evidence/repo</files>
  <read_first>
    - tests/eval-fixtures/README.md
    - tests/eval-fixtures/data-flow/core/expected.polint-eval.toml
    - tests/eval-fixtures/cfg-core/expected.polint-eval.toml
  </read_first>
  <action>Create a tiny Go or TS/JS fixture repo that exercises branch-controlled flow, a direct/refined call edge, source-to-sink data-flow, sanitizer/barrier behavior, evidence/path rows, unknown or budget marker, and deterministic provider/cache rows. Keep the fixture source owned by this repo, not copied from external benchmarks.</action>
  <verify>
    <automated>cargo test -p polint --lib eval_observed --locked</automated>
  </verify>
  <acceptance_criteria>
    - Fixture expected rows include at least one graph edge, one path, one fact family row, one unknown/budget row, and one runtime/cache invariant.
    - Fixture passes through the existing native fixture runner.
    - No external benchmark content is committed.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Classify partial-truth graph/path extras as unconfirmed</name>
  <files>crates/polint/src/eval/matcher.rs, crates/polint/src/eval/metrics.rs</files>
  <read_first>
    - crates/polint/src/eval/matcher.rs
    - research/call-graphs/VALIDATION.md
    - research/evaluation-harness/algorithms/scoring-and-matching.md
  </read_first>
  <action>Ensure graph and path matchers can distinguish complete truth from partial truth. Extra static edges or paths against partial dynamic truth should be `Unconfirmed` unless an expected forbidden item proves they are false.</action>
  <verify>
    <automated>cargo test -p polint --lib eval::matcher --locked</automated>
    <automated>cargo test -p polint --lib eval::metrics --locked</automated>
  </verify>
  <acceptance_criteria>
    - Matcher tests cover required edge found, required edge missing, forbidden edge found, extra confirmed false, and extra unconfirmed.
    - Path matcher prioritizes endpoint/evidence correctness over exact internal path equality when truth is partial.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| partial truth -> metric classification | Dynamic or sampled truth must not over-penalize valid static extras. |
| native fixtures -> promotion claims | Native fixtures prove engine invariants, not broad external scanner quality. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T40-08 | Tampering | gate thresholds | Mitigated | Store thresholds as explicit config and report them with verdicts. |
| T40-09 | Repudiation | graph/path extras | Mitigated | Distinguish false positives from unconfirmed partial-truth extras. |
</threat_model>

<verification>
- `cargo fmt --all --check`
- `cargo test -p polint --lib eval::gates --locked`
- `cargo test -p polint --lib eval::matcher --locked`
- `cargo test -p polint --lib eval_observed --locked`
</verification>

<success_criteria>
Native promotion gates prove graph/fact/path/evidence correctness and partial-truth matching before external scanner suites are used for claims.
</success_criteria>
