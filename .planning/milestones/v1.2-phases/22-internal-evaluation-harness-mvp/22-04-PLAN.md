---
phase: 22-internal-evaluation-harness-mvp
plan: 04
type: execute
wave: 4
depends_on:
  - 22-03
files_modified:
  - crates/polint/src/eval/fixtures.rs
  - tests/eval-fixtures/provenance/metadata/expected.polint-eval.toml
  - tests/eval-fixtures/provenance/metadata/repo/.polint.toml
  - tests/eval-fixtures/provenance/metadata/repo/src/app.ts
  - tests/eval-fixtures/cache/current-determinism/expected.polint-eval.toml
  - tests/eval-fixtures/cache/current-determinism/repo/.polint.toml
  - tests/eval-fixtures/cache/current-determinism/repo/component.tsx
  - tests/eval-fixtures/cache/current-determinism/repo/payment.go
autonomous: true
requirements:
  - SAE-FND-03
must_haves:
  truths:
    - "Native fixtures cover Phase 21 provenance metadata using `producer_id` fields that match existing metadata debug JSON."
    - "Native fixtures assert current cache determinism through cold, warm, and no-cache normalized output comparisons."
    - "Cache determinism is represented as an observed invariant only after deterministic JSON and output hash comparisons pass."
    - "The cache fixture does not introduce typed layer cache keys, persistent layer cache behavior, or layer hit/miss semantics."
  artifacts:
    - path: "tests/eval-fixtures/provenance/metadata/expected.polint-eval.toml"
      provides: "Provenance/metadata native fixture"
      contains: "producer_id"
    - path: "tests/eval-fixtures/cache/current-determinism/expected.polint-eval.toml"
      provides: "Current cache determinism fixture without typed layer cache semantics"
      contains: "cache.current_determinism"
    - path: "crates/polint/src/eval/fixtures.rs"
      provides: "Provenance and cache fixture tests plus cache determinism invariant derivation"
      contains: "eval_cache_current_determinism_fixture_passes"
  key_links:
    - from: "tests/eval-fixtures/provenance/metadata/expected.polint-eval.toml"
      to: "crates/polint/src/analysis_kernel/debug.rs"
      via: "expected metadata rows match observed metadata debug facts with producer_id"
      pattern: "metadata_debug_json_for_test"
    - from: "tests/eval-fixtures/cache/current-determinism/expected.polint-eval.toml"
      to: "crates/polint/src/eval/fixtures.rs"
      via: "cold/warm/no-cache output comparison derives cache.current_determinism observed invariant"
      pattern: "cold_warm_no_cache_equal"
---

<objective>
Add the provenance and current-cache native fixtures required by Phase 22.

Purpose: prove the harness can consume existing Phase 21 provenance metadata and current cache behavior without widening public contracts or adding Phase 23/24 cache semantics.
Output: provenance and cache fixtures under `tests/eval-fixtures/` plus focused fixture-runner tests in `eval/fixtures.rs`.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/22-internal-evaluation-harness-mvp/22-CONTEXT.md
@research/evaluation-harness/FINAL-REPORT.md
@research/evaluation-harness/RECOMMENDED_IMPLEMENTATION.md
@research/evaluation-harness/VALIDATION.md
@AGENTS.md
@docs/API-VISIBILITY-PLAN.md
@.agents/skills/rust-best-practices/SKILL.md
@.planning/phases/21-provenance-precision-and-validation-metadata/21-04-SUMMARY.md
@.planning/phases/07-cache-and-performance/07-04-SUMMARY.md
@.planning/phases/22-internal-evaluation-harness-mvp/22-01-SUMMARY.md
@.planning/phases/22-internal-evaluation-harness-mvp/22-02-SUMMARY.md
@.planning/phases/22-internal-evaluation-harness-mvp/22-03-SUMMARY.md
@crates/polint/src/eval/fixtures.rs
@crates/polint/src/eval/observed.rs
@crates/polint/src/eval/matcher.rs
@crates/polint/src/eval/report.rs
@crates/polint/src/analysis_kernel/debug.rs
@crates/polint/src/cache/mod.rs

<interfaces>
Model and runner contracts from earlier Phase 22 plans:
```rust
pub(crate) enum ExpectedItem { Fact(..), Invariant(..), RuntimeBudget(..), /* other item kinds */ }
pub(crate) enum ObservedItem { Fact(..), Invariant(..), RuntimeBudget(..), /* other item kinds */ }
pub(crate) struct ObservedRuntimeBudget { name: String, budget_passed: bool, observed_runtime_ms: Option<u64> }
pub(crate) fn run_native_fixture_for_test(fixture_dir: &Path) -> anyhow::Result<EvaluationRun>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add provenance metadata native fixture</name>
  <files>crates/polint/src/eval/fixtures.rs, tests/eval-fixtures/provenance/metadata/expected.polint-eval.toml, tests/eval-fixtures/provenance/metadata/repo/.polint.toml, tests/eval-fixtures/provenance/metadata/repo/src/app.ts</files>
  <read_first>
    <file>.planning/phases/22-internal-evaluation-harness-mvp/22-CONTEXT.md</file>
    <file>research/evaluation-harness/RECOMMENDED_IMPLEMENTATION.md</file>
    <file>.planning/phases/21-provenance-precision-and-validation-metadata/21-04-SUMMARY.md</file>
    <file>crates/polint/src/eval/fixtures.rs</file>
    <file>crates/polint/src/eval/observed.rs</file>
    <file>crates/polint/src/eval/matcher.rs</file>
    <file>crates/polint/src/eval/report.rs</file>
    <file>crates/polint/src/analysis_kernel/debug.rs</file>
    <file>tests/eval-fixtures/provenance/metadata/expected.polint-eval.toml</file>
  </read_first>
  <behavior>
    - Test 1: the provenance fixture passes and observes fact rows with `producer_id = "polint.source"` and `producer_id = "polint.ts.syntax"`, stable keys, precision labels, and relative paths only.
    - Test 2: provenance expected facts use exact `producer_id` fields matching Phase 21 metadata debug JSON.
    - Test 3: the provenance fixture includes at least one runtime budget expected item and only the observed budget pass/fail affects deterministic hashes.
  </behavior>
  <action>Add fixture test `eval_provenance_fixture_passes` in `eval/fixtures.rs`. Add `tests/eval-fixtures/provenance/metadata/` with a small TS repo containing `.polint.toml` include/exclude settings and `src/app.ts` that imports and calls one exported function. Its manifest must expect fact rows for at least one source file and one TS syntax fact using exact `family`, exact `producer_id`, exact `relative_path`, and partial stable-key matching where the full stable key contains content hashes; `producer_id` is the only accepted provenance producer field name in this fixture. This implements D-07, D-09 provenance coverage, D-16, D-17, and D-19 using existing Phase 21 metadata debug JSON.</action>
  <acceptance_criteria>
    <criterion>`rg -n "eval_provenance_fixture_passes" crates/polint/src/eval/fixtures.rs` returns the provenance fixture test.</criterion>
    <criterion>`rg -n "producer_id|polint.source|polint.ts.syntax|relative_path" tests/eval-fixtures/provenance/metadata/expected.polint-eval.toml` returns provenance expectations.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p polint --lib eval_provenance_fixture_passes --locked</automated>
  </verify>
  <done>Native provenance fixture passes deterministically and uses `producer_id` consistently with Phase 21 metadata debug JSON.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add current-cache determinism fixture and invariant observation</name>
  <files>crates/polint/src/eval/fixtures.rs, tests/eval-fixtures/cache/current-determinism/expected.polint-eval.toml, tests/eval-fixtures/cache/current-determinism/repo/.polint.toml, tests/eval-fixtures/cache/current-determinism/repo/component.tsx, tests/eval-fixtures/cache/current-determinism/repo/payment.go</files>
  <read_first>
    <file>.planning/phases/22-internal-evaluation-harness-mvp/22-CONTEXT.md</file>
    <file>research/evaluation-harness/RECOMMENDED_IMPLEMENTATION.md</file>
    <file>.planning/phases/07-cache-and-performance/07-04-SUMMARY.md</file>
    <file>crates/polint/src/eval/fixtures.rs</file>
    <file>crates/polint/src/eval/observed.rs</file>
    <file>crates/polint/src/eval/matcher.rs</file>
    <file>crates/polint/src/eval/report.rs</file>
    <file>crates/polint/src/cache/mod.rs</file>
    <file>tests/eval-fixtures/cache/current-determinism/expected.polint-eval.toml</file>
  </read_first>
  <behavior>
    - Test 1: the cache fixture passes only when cold, warm, and no-cache runs have byte-identical deterministic JSON and identical `output_hash`.
    - Test 2: after cold/warm/no-cache comparisons pass, the cache fixture emits `ObservedItem::Invariant { name: "cache.current_determinism", value: "cold_warm_no_cache_equal" }` for expected invariant matching.
    - Test 3: the cache fixture asserts `cache.current_determinism` and does not assert typed `LayerKey`, `InputSnapshot`, provider output metadata, or persistent layer cache hit/miss semantics.
    - Test 4: the cache fixture includes at least one runtime budget expected item and only the observed budget pass/fail affects deterministic hashes.
  </behavior>
  <action>Add fixture test `eval_cache_current_determinism_fixture_passes` in `eval/fixtures.rs`. Add `tests/eval-fixtures/cache/current-determinism/` with the same small mixed TS/Go shape as `write_phase7_cache_fixture`: `.polint.toml`, `component.tsx`, and `payment.go`. Its manifest must assert invariant `cache.current_determinism = "cold_warm_no_cache_equal"` and runtime budget `cache.current_determinism` with `max_runtime_ms = 5000`. The fixture runner test must run the cache fixture cold, warm, and no-cache; compare deterministic JSON and `output_hash` for all three runs; only after those comparisons pass, derive or inject `ObservedItem::Invariant { name: "cache.current_determinism", value: "cold_warm_no_cache_equal" }` before calling the matcher. If any deterministic JSON or `output_hash` comparison fails, do not emit that observed invariant and let the expected invariant fail. Do not add typed cache-key vocabulary such as `InputSnapshot`, `LayerKey`, `QueryKey`, `SummaryKey`, or `DiagnosticKey`; do not add layer cache hit/miss counters or persistent cache behavior beyond current cache determinism per D-09 and D-11.</action>
  <acceptance_criteria>
    <criterion>`rg -n "eval_cache_current_determinism_fixture_passes" crates/polint/src/eval/fixtures.rs` returns the cache fixture test.</criterion>
    <criterion>`rg -n "cache.current_determinism|cold_warm_no_cache_equal|max_runtime_ms = 5000" tests/eval-fixtures/cache/current-determinism/expected.polint-eval.toml crates/polint/src/eval/fixtures.rs` returns cache expectations and observed invariant derivation.</criterion>
    <criterion>`rg -n "InputSnapshot|LayerKey|QueryKey|SummaryKey|DiagnosticKey|provider output metadata|layer cache|hit/miss" crates/polint/src/eval tests/eval-fixtures/cache/current-determinism` returns no implementation matches.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p polint --lib eval_cache_current_determinism_fixture_passes --locked</automated>
  </verify>
  <done>Native current-cache fixture emits the cache determinism invariant only after cold/warm/no-cache output equality is proven and does not introduce Phase 23 or Phase 24 cache semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Provenance fixture -> metadata debug JSON | Fixture manifests assert facts produced by existing Phase 21 debug rows and must use the same `producer_id` field names. |
| Cache fixture -> current cache behavior | Fixture claims about cache determinism must stay limited to current deterministic output, not Phase 23/24 cache semantics. |
| Runtime budget -> deterministic hash | Wall-clock measurement can influence pass/fail but must not make hashes machine-dependent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-04-01 | Tampering | provenance fixture manifest | mitigate | Require `producer_id` fields matching Phase 21 metadata debug JSON and reject any alternate producer field name during manifest loading. |
| T-22-04-02 | Repudiation | cache determinism invariant | mitigate | Derive `cache.current_determinism` observed invariant only after cold/warm/no-cache deterministic JSON and `output_hash` comparisons pass. |
| T-22-04-03 | Tampering | cache fixture claims | mitigate | Acceptance checks grep for absence of Phase 23/24 typed cache vocabulary and layer cache semantics. |
| T-22-04-04 | Information Disclosure | fixture reports | mitigate | Reuse Plan 22-03 path normalization and assert fixture data uses relative paths only. |
</threat_model>

<verification>
Run the automated task commands, then run:
```bash
cargo test -p polint --lib eval --locked
cargo test --workspace --all-features --locked
cargo fmt --all -- --check
```
</verification>

<success_criteria>
Plan 22-04 is complete when provenance and current-cache fixtures pass, provenance fields consistently use `producer_id`, cache determinism emits an observed invariant only after cold/warm/no-cache equality is proven, and no typed layer-cache or persistent-cache semantics are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/22-internal-evaluation-harness-mvp/22-04-SUMMARY.md`.
</output>
