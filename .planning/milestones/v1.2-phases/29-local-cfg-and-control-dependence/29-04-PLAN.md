---
phase: 29-local-cfg-and-control-dependence
plan: 04
type: execute
wave: 4
depends_on: [29-01, 29-02, 29-03]
files_modified:
  - crates/polint/src/analysis/cfg/mod.rs
  - crates/polint/src/analysis/cfg/lower_go.rs
  - crates/polint/src/analysis/cfg/provider.rs
  - crates/polint/src/analysis/mir/lower_go.rs
autonomous: true
requirements: [SAE-SEM-04]
requirements_addressed: [SAE-SEM-04]
must_haves:
  truths:
    - "Go CFG lowering consumes private MIR and emits polint-owned CFG facts."
    - "Go branches, loops, returns, short-circuiting, panic, defer, go, switch/select, labels/goto/fallthrough are represented or explicitly unsupported."
    - "Go lifecycle/setup limitations remain explicit and do not require repository lifecycle files."
  artifacts:
    - path: "crates/polint/src/analysis/cfg/lower_go.rs"
      provides: "Go MIR-to-CFG lowering"
      contains: "lower_go_cfg"
    - path: "crates/polint/src/analysis/cfg/provider.rs"
      provides: "Provider merge path for Go CFG output"
      contains: "lower_go_cfg"
  key_links:
    - from: "crates/polint/src/analysis/cfg/lower_go.rs"
      to: "crates/polint/src/analysis/mir/lower_go.rs"
      via: "Go CFG follows MIR operation/control-shape rows"
      pattern: "MirOperationKind"
---

<objective>
Implement the first Go CFG lowering path over private semantic MIR.

Purpose: Phase 29 must cover supported Go constructs with truthful CFG/control-dependence inputs while preserving Go lifecycle and public API constraints.
Output: Go CFG lowering module, provider merge integration, Go CFG unit tests, and unsupported-control-flow rows for gaps.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-local-cfg-and-control-dependence/29-CONTEXT.md
@.planning/phases/29-local-cfg-and-control-dependence/29-01-PLAN.md
@.planning/phases/29-local-cfg-and-control-dependence/29-02-PLAN.md
@.planning/phases/29-local-cfg-and-control-dependence/29-03-PLAN.md
@research/cfg-control-flow/FINAL-REPORT.md
@research/cfg-control-flow/RECOMMENDED_IMPLEMENTATION.md
@research/cfg-control-flow/VALIDATION.md
@.agents/skills/rust-best-practices/SKILL.md
@crates/polint/src/analysis/cfg/builder.rs
@crates/polint/src/analysis/cfg/facts.rs
@crates/polint/src/analysis/cfg/store.rs
@crates/polint/src/analysis/cfg/provider.rs
@crates/polint/src/analysis/mir/lower_go.rs
@crates/polint/src/go/adapter.rs
@crates/polint/src/symbol_graph/go.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Go CFG lowering for straight-line, branch, loop, return, and short-circuit shapes</name>
  <files>crates/polint/src/analysis/cfg/mod.rs, crates/polint/src/analysis/cfg/lower_go.rs, crates/polint/src/analysis/cfg/provider.rs</files>
  <read_first>
    - crates/polint/src/analysis/cfg/builder.rs
    - crates/polint/src/analysis/cfg/facts.rs
    - crates/polint/src/analysis/cfg/provider.rs
    - crates/polint/src/analysis/mir/lower_go.rs
    - crates/polint/src/analysis/mir/op.rs
    - research/cfg-control-flow/RECOMMENDED_IMPLEMENTATION.md
    - .planning/phases/29-local-cfg-and-control-dependence/29-CONTEXT.md
  </read_first>
  <behavior>
    - Test Go straight-line functions produce entry, operation nodes, normal edges, one normal exit, reachability, and dominator rows.
    - Test Go `if` and `for` shapes produce true/false, loop enter/back/exit, and control-dependence rows.
    - Test Go `&&` and `||` produce short-circuit edges rather than one opaque expression node.
    - Test return edges prevent following statements from being marked reachable.
  </behavior>
  <action>Create `analysis::cfg::lower_go` with `pub(crate) fn lower_go_cfg(db: &AnalysisDb) -> CfgOutput`. Iterate stored MIR bodies where `MirBody.language == Language::Go`. For each body, drive `CfgBuilder` from that body's `MirOperation` rows in ordinal order. Map `MirOperationKind::Branch` to condition nodes and true/false edges using available MIR/source evidence, `Return` to return edges into the normal exit, calls/read/write/assign/bind/storage-live to operation nodes with normal fallthrough, and unsupported MIR rows affecting `UnsupportedDomain::Cfg` to `UnsupportedControlFlowFact`. Add initial Go tests by constructing MIR rows directly where AST/MIR coverage is not enough, then add at least one kernel-level fixture using real Go source if existing helpers make it practical. Ensure derived rows are produced by the common provider path, not inside `lower_go_cfg`.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::cfg::lower_go --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "lower_go_cfg|Language::Go|MirOperationKind::Branch|MirOperationKind::Return|ShortCircuit" crates/polint/src/analysis/cfg/lower_go.rs` returns matches.
    - `rg -n "derive_reachability|derive_dominators|derive_control_dependence" crates/polint/src/analysis/cfg/lower_go.rs` returns no matches because derived analyses run in the shared provider path.
    - `cargo test -p polint --lib analysis::cfg::lower_go --locked` passes.
  </acceptance_criteria>
  <done>Go MIR bodies lower into deterministic CFG nodes/edges for core local control-flow shapes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Represent Go abrupt, spawn, switch/select, defer, panic, and unsupported precision honestly</name>
  <files>crates/polint/src/analysis/cfg/lower_go.rs, crates/polint/src/analysis/mir/lower_go.rs</files>
  <read_first>
    - crates/polint/src/analysis/cfg/lower_go.rs
    - crates/polint/src/analysis/mir/lower_go.rs
    - crates/polint/src/analysis/mir/op.rs
    - crates/polint/src/analysis/cfg/facts.rs
    - research/cfg-control-flow/FINAL-REPORT.md
    - research/cfg-control-flow/VALIDATION.md
    - AGENTS.md
  </read_first>
  <behavior>
    - Test `panic` maps to `Panic` or `Unknown`/unsupported CFG evidence with non-exact precision unless fully modeled.
    - Test `defer` maps to `Defer`/`Cleanup`/unsupported evidence rather than normal-only fake fallthrough.
    - Test `go f()` emits a `Spawn` boundary fact/edge and does not create an intraprocedural successor into `f`.
    - Test `switch`, `select`, `goto`, labels, and `fallthrough` are either modeled with typed edges or emitted as unsupported control-flow facts with source evidence and affected domain `Cfg`.
  </behavior>
  <action>Extend Go CFG lowering and, only where necessary, Go MIR unsupported/control-shape emission so Go-specific abrupt and concurrency constructs are truthful. Map supported `break`, `continue`, `goto`, labels, `fallthrough`, `switch`, and loop control to typed CFG edges when the existing MIR evidence can support it. For `defer`, `panic`, `recover`, `select`, and `go`, prefer explicit `CfgEdgeKind::Defer`, `Cleanup`, `Panic`, `Recover`, `Spawn`, `Unknown`, or `UnsupportedControlFlowFact` rows with `CfgPrecision::Conservative`, `Unknown`, or `Unsupported` as appropriate. Do not require a root `go.mod`, do not write lifecycle files, and do not claim SSA-grade exactness unless tests prove it. Add tests that assert exact edge-kind strings for modeled constructs and unsupported rows for gaps.</action>
  <verify>
    <automated>cargo test -p polint --lib go_cfg_abrupt_and_unsupported --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "Spawn|Defer|Panic|Recover|UnsupportedControlFlow|select|fallthrough|goto" crates/polint/src/analysis/cfg/lower_go.rs crates/polint/src/analysis/mir/lower_go.rs` returns matches.
    - `rg -n "go\\.mod|go.work|std::fs::write|File::create" crates/polint/src/analysis/cfg/lower_go.rs` returns no lifecycle-file-writing matches.
    - `cargo test -p polint --lib go_cfg_abrupt_and_unsupported --locked` passes.
  </acceptance_criteria>
  <done>Go-specific abrupt, cleanup, spawn, and unsupported control-flow semantics are represented honestly for downstream CFG consumers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Go syntax/MIR -> CFG | Go parser/MIR evidence must become polint-owned CFG facts without raw AST leakage. |
| Go lifecycle -> CFG precision | Missing setup or syntactic-only evidence must lower precision/status rather than claiming exact SSA parity. |
| Go concurrency/panic/defer -> local CFG | Spawn and cleanup semantics must not be collapsed into misleading sequential edges. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-04-01 | T | Go CFG precision | mitigate | Use conservative/unknown/unsupported rows for semantics not fully modeled. |
| T-29-04-02 | I | Go parser/lifecycle leakage | mitigate | Consume MIR and existing lifecycle state; do not store AST objects or write repo lifecycle files. |
| T-29-04-03 | R | Spawn/defer/panic evidence | mitigate | Add tests requiring typed `Spawn`, `Defer`, `Panic`, or unsupported rows. |
| T-29-04-04 | T | Reachability after return | mitigate | Add tests proving return edges prevent following statements from being reachable. |
</threat_model>

<verification>
Run `cargo test -p polint --lib analysis::cfg::lower_go --locked`, `cargo test -p polint --lib go_cfg_abrupt_and_unsupported --locked`, and `cargo fmt --all -- --check`.
</verification>

<success_criteria>
Go CFG lowering emits deterministic CFG rows for supported local constructs and explicit unsupported/control uncertainty rows for semantics not yet modeled exactly.
</success_criteria>

<output>
After completion, create `.planning/phases/29-local-cfg-and-control-dependence/29-04-SUMMARY.md`.
</output>
