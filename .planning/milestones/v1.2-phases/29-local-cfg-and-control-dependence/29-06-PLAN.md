---
phase: 29-local-cfg-and-control-dependence
plan: 06
type: execute
wave: 6
depends_on: [29-05]
files_modified:
  - crates/polint/src/eval/model.rs
  - crates/polint/src/eval/observed.rs
  - crates/polint/src/eval/fixtures.rs
  - tests/eval-fixtures/cfg-core/manifest.json
  - tests/eval-fixtures/cfg-core/repo/go/cfg.go
  - tests/eval-fixtures/cfg-core/repo/ts/cfg.ts
  - tests/eval-fixtures/cfg-core/expected.json
  - crates/polint/tests/cli.rs
  - crates/polint/src/sdk/facts.rs
  - crates/polint/src/analysis_plan.rs
  - docs/facts/module-graph.md
autonomous: true
requirements: [SAE-SEM-04]
requirements_addressed: [SAE-SEM-04]
must_haves:
  truths:
    - "CFG eval snapshots are deterministic for Go and TS/JS branches, loops, returns, short-circuiting, panics/throws, cleanup/unsupported constructs, and control dependence."
    - "Public CLI, docs, SDK, inspect/test JSON, and external rule workflows do not expose private CFG internals."
    - "The existing public `cfg` capability remains unsupported unless this phase deliberately promotes it with docs and tests."
  artifacts:
    - path: "tests/eval-fixtures/cfg-core/manifest.json"
      provides: "CFG eval fixture manifest"
      contains: "\"area\": \"cfg\""
    - path: "crates/polint/src/eval/observed.rs"
      provides: "Internal observed CFG rows for eval"
      contains: "cfg_facts"
    - path: "crates/polint/tests/cli.rs"
      provides: "Public no-leak and compatibility proof"
      contains: "cfg"
  key_links:
    - from: "crates/polint/src/eval/observed.rs"
      to: "crates/polint/src/analysis_kernel/debug.rs"
      via: "CFG observed facts from test-only metadata debug JSON"
      pattern: "cfg"
    - from: "crates/polint/tests/cli.rs"
      to: "crates/polint/src/sdk/facts.rs"
      via: "Reserved public CFG capability remains honest"
      pattern: "unsupported capability"
---

<objective>
Add CFG eval snapshots, deterministic fixture coverage, and public-boundary proof for Phase 29.

Purpose: The phase is not complete unless CFG facts are validated through Go/TS fixture evidence and private internals remain absent from public rule-author surfaces.
Output: internal CFG eval fixture category/observed rows, Go/TS fixture snapshots, public no-leak tests, capability-honesty tests, and final docs alignment if existing docs need limit wording.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-local-cfg-and-control-dependence/29-CONTEXT.md
@.planning/phases/29-local-cfg-and-control-dependence/29-04-PLAN.md
@.planning/phases/29-local-cfg-and-control-dependence/29-05-PLAN.md
@research/cfg-control-flow/STANDARD.md
@research/cfg-control-flow/VALIDATION.md
@.planning/phases/22-internal-evaluation-harness-mvp/22-CONTEXT.md
@.planning/phases/28-private-semantic-mir-and-place-identity/28-07-SUMMARY.md
@.agents/skills/rust-best-practices/SKILL.md
@crates/polint/src/eval/model.rs
@crates/polint/src/eval/observed.rs
@crates/polint/src/eval/fixtures.rs
@crates/polint/src/analysis_kernel/debug.rs
@crates/polint/tests/cli.rs
@crates/polint/src/sdk/facts.rs
@crates/polint/src/analysis_plan.rs
@docs/API-VISIBILITY-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CFG observed rows and fixture category</name>
  <files>crates/polint/src/eval/model.rs, crates/polint/src/eval/observed.rs, crates/polint/src/eval/fixtures.rs</files>
  <read_first>
    - crates/polint/src/eval/model.rs
    - crates/polint/src/eval/observed.rs
    - crates/polint/src/eval/fixtures.rs
    - crates/polint/src/analysis_kernel/debug.rs
    - .planning/phases/22-internal-evaluation-harness-mvp/22-CONTEXT.md
    - .planning/phases/29-local-cfg-and-control-dependence/29-CONTEXT.md
  </read_first>
  <behavior>
    - Test observed CFG facts include function, node, block, edge, reachability, dominator, postdominator, control-dependence, and unsupported rows.
    - Test observed CFG payloads use compact fragments such as `view=...;kind=...;from=...;to=...;status=...` and do not include raw source or absolute paths.
    - Test CFG fixture category runs through the existing native fixture runner and participates in deterministic output hashing.
  </behavior>
  <action>Extend the internal eval model with a CFG fixture area/category label using exact string `cfg`. Extend `eval::observed` with `cfg_facts(debug_json: &Value) -> Vec<ObservedItem>` sourced from `metadata_debug_json_for_test` CFG arrays. Observed fact families must use labels `CfgFunction`, `CfgNode`, `BasicBlock`, `CfgEdge`, `CfgReachability`, `CfgDominator`, `CfgPostDominator`, `CfgControlDependence`, and `UnsupportedControlFlow`. Payloads must be compact semicolon-delimited fragments containing stable key, graph view, kind, status, precision, and endpoint/controller stable-key fragments where relevant. Add fixture runner support analogous to semantic MIR fixtures so `run_cfg_core_fixture_for_test` executes a native repo fixture, observes CFG facts, and normalizes/hashes deterministically.</action>
  <verify>
    <automated>cargo test -p polint --lib eval::observed::cfg eval::fixtures::cfg_core --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "CfgFunction|CfgNode|BasicBlock|CfgEdge|CfgControlDependence|UnsupportedControlFlow|cfg_facts" crates/polint/src/eval/observed.rs` returns matches.
    - `rg -n "Cfg|cfg_core|run_cfg_core_fixture_for_test|area.*cfg" crates/polint/src/eval/model.rs crates/polint/src/eval/fixtures.rs` returns matches.
    - `cargo test -p polint --lib eval::observed::cfg eval::fixtures::cfg_core --locked` passes.
  </acceptance_criteria>
  <done>Internal eval can observe CFG facts deterministically from test-only debug JSON.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Go and TS/JS CFG core eval fixture snapshots</name>
  <files>tests/eval-fixtures/cfg-core/manifest.json, tests/eval-fixtures/cfg-core/repo/go/cfg.go, tests/eval-fixtures/cfg-core/repo/ts/cfg.ts, tests/eval-fixtures/cfg-core/expected.json, crates/polint/src/eval/fixtures.rs</files>
  <read_first>
    - crates/polint/src/eval/fixtures.rs
    - tests/eval-fixtures/semantic-mir-core/manifest.json
    - tests/eval-fixtures/semantic-mir-core/expected.json
    - research/cfg-control-flow/VALIDATION.md
    - .planning/phases/29-local-cfg-and-control-dependence/29-CONTEXT.md
  </read_first>
  <behavior>
    - Fixture covers Go branches, loops, returns, short-circuiting, panic/defer or unsupported evidence, unreachable code, and control dependence.
    - Fixture covers TS/JS branches, loops, returns, throws, short-circuiting/nullish/optional behavior where supported, try/finally or unsupported evidence, unreachable code, and control dependence.
    - Repeated fixture runs produce identical normalized JSON and identical output hash.
  </behavior>
  <action>Create `tests/eval-fixtures/cfg-core/` with a manifest using area `cfg`, at least one Go file under `repo/go/cfg.go`, at least one TS/JS file under `repo/ts/cfg.ts`, and expected observed facts in `expected.json`. The fixture source must include branches, loops, returns, short-circuiting, panic/throw behavior, cleanup/unsupported constructs where supported, and unreachable code. Expected rows must assert at least one `CfgFunction`, `CfgNode`, `BasicBlock`, `CfgEdge`, `CfgReachability`, `CfgDominator`, `CfgPostDominator`, `CfgControlDependence`, and `UnsupportedControlFlow` or documented unsupported-equivalent row if a language-specific construct is conservatively modeled. Add tests proving all required fact families are observed and `cfg.current_determinism` invariant passes across repeated cold/warm/no-cache style runs using existing eval harness patterns.</action>
  <verify>
    <automated>cargo test -p polint --lib eval::fixtures::cfg_core --locked</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/eval-fixtures/cfg-core/manifest.json && test -f tests/eval-fixtures/cfg-core/expected.json` exits 0.
    - `rg -n "if|for|return|panic|defer|&&|\\|\\||throw|finally|unreachable" tests/eval-fixtures/cfg-core/repo` returns matches.
    - `rg -n "CfgFunction|CfgNode|BasicBlock|CfgEdge|CfgControlDependence|UnsupportedControlFlow|cfg.current_determinism" tests/eval-fixtures/cfg-core crates/polint/src/eval/fixtures.rs` returns matches.
    - `cargo test -p polint --lib eval::fixtures::cfg_core --locked` passes.
  </acceptance_criteria>
  <done>CFG core eval fixtures prove deterministic Go/TS CFG and control-dependence observations over real source inputs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove public no-leak and capability honesty</name>
  <files>crates/polint/tests/cli.rs, crates/polint/src/sdk/facts.rs, crates/polint/src/analysis_plan.rs, docs/facts/module-graph.md</files>
  <read_first>
    - crates/polint/tests/cli.rs
    - crates/polint/src/sdk/facts.rs
    - crates/polint/src/analysis_plan.rs
    - docs/API-VISIBILITY-PLAN.md
    - docs/facts/module-graph.md
    - .planning/phases/29-local-cfg-and-control-dependence/29-CONTEXT.md
    - AGENTS.md
  </read_first>
  <behavior>
    - Test public `polint check --format json`, `polint inspect`, `polint test`, CLI help, README/docs scans, and SDK source scans do not expose `polint.cfg`, CFG fact family internals, provider manifests, debug JSON, or eval schemas.
    - Test a temp-repo external rule that requests only supported public fact views still runs successfully after CFG provider wiring.
    - Test a rule requesting reserved `Cfg<'_>` or `cfg` capability still receives an unsupported capability diagnostic unless this phase intentionally promotes public CFG.
    - Test docs do not advertise CFG as a supported public fact family.
  </behavior>
  <action>Add or extend public-boundary tests following Phase 28 no-leak patterns. Public check JSON and inspect/test JSON must not contain `polint.cfg`, `CfgFunction`, `CfgNode`, `BasicBlock`, `CfgEdge`, `CfgControlDependence`, `UnsupportedControlFlow`, `cfg_debug`, `cfg-facts-1`, or provider cache stats. Source-surface tests must confirm `crates/polint/src/sdk`, `crates/polint/src/runner.rs`, README, and `docs/facts/` do not present CFG internals as supported facts. Preserve existing reserved `Cfg<'_>` and `Capabilities::cfg()` behavior: requesting it should produce an unsupported capability diagnostic with capability `cfg`, and rules should not execute with placeholder CFG facts. If existing docs mention advanced graph capabilities, update only limit wording to say CFG remains internal/unsupported in Phase 29; do not create `docs/facts/cfg.md`.</action>
  <verify>
    <automated>cargo test -p polint --test cli cfg_public_no_leak cfg_capability_remains_unsupported --locked</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p polint --test cli cfg_public_no_leak cfg_capability_remains_unsupported --locked` passes.
    - `rg -n "polint.cfg|CfgFunction|CfgNode|BasicBlock|CfgEdge|CfgControlDependence|UnsupportedControlFlow|cfg-facts-1" README.md docs crates/polint/src/sdk crates/polint/src/runner.rs` returns no matches except explicit negative/unsupported test strings if those are intentionally included in tests.
    - `test ! -f docs/facts/cfg.md` exits 0.
    - `rg -n "unsupported capability.*cfg|capability.*cfg" crates/polint/tests/cli.rs crates/polint/src/analysis_plan.rs` returns matches proving capability honesty.
  </acceptance_criteria>
  <done>CFG internals remain private and public `cfg` capability behavior stays truthful until an intentional promotion phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CFG internals -> eval fixtures | Eval may observe private rows only through test-only debug JSON. |
| CFG internals -> public outputs | Public CLI, SDK, docs, inspect, and test output must not leak private fact families. |
| reserved public cfg capability -> rule execution | Unsupported CFG capability must stop rule execution with diagnostics rather than placeholder facts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-06-01 | R | Fixture coverage | mitigate | Require Go/TS fixture rows across CFG functions, nodes, blocks, edges, derived facts, and unsupported evidence. |
| T-29-06-02 | T | Determinism | mitigate | Add repeated fixture run invariant and output-hash checks. |
| T-29-06-03 | I | Public leakage | mitigate | Add public JSON/help/docs/SDK no-leak tests for CFG internals. |
| T-29-06-04 | S | Unsupported capability bypass | mitigate | Assert `cfg` capability remains unsupported and rules do not execute with placeholder CFG facts. |
</threat_model>

<verification>
Run `cargo test -p polint --lib eval::observed::cfg eval::fixtures::cfg_core --locked`, `cargo test -p polint --test cli cfg_public_no_leak cfg_capability_remains_unsupported --locked`, `cargo test -p polint --all-targets --locked`, and `cargo fmt --all -- --check`.
</verification>

<success_criteria>
CFG core fixtures deterministically observe Go/TS CFG and control-dependence facts, while public CLI, SDK, docs, inspect/test JSON, and reserved capability behavior remain honest and private.
</success_criteria>

<output>
After completion, create `.planning/phases/29-local-cfg-and-control-dependence/29-06-SUMMARY.md`.
</output>
