---
phase: 29-local-cfg-and-control-dependence
plan: 03
type: execute
wave: 3
depends_on: [29-01, 29-02]
files_modified:
  - crates/polint/src/analysis/cfg/mod.rs
  - crates/polint/src/analysis/cfg/provider.rs
  - crates/polint/src/analysis/cfg/cache_key.rs
  - crates/polint/src/analysis/cfg/validate.rs
  - crates/polint/src/analysis_kernel/provider.rs
  - crates/polint/src/analysis_kernel/mod.rs
  - crates/polint/src/analysis_kernel/incremental/keys.rs
  - crates/polint/src/analysis_kernel/validation.rs
  - crates/polint/src/analysis_kernel/debug.rs
autonomous: true
requirements: [SAE-SEM-04]
requirements_addressed: [SAE-SEM-04]
must_haves:
  truths:
    - "A private `polint.cfg` provider runs after `polint.semantic_mir` and before metrics/future consumers."
    - "CFG validation rejects malformed graphs before derived rows are trusted by downstream phases."
    - "CFG cache identity includes semantic MIR output and graph-view/provider parameters."
  artifacts:
    - path: "crates/polint/src/analysis/cfg/provider.rs"
      provides: "CFG provider derivation"
      contains: "derive_cfg_with_cache_stats"
    - path: "crates/polint/src/analysis/cfg/validate.rs"
      provides: "CFG graph invariant validation"
      contains: "validate_cfg"
    - path: "crates/polint/src/analysis_kernel/provider.rs"
      provides: "polint.cfg provider manifest"
      contains: "polint.cfg"
  key_links:
    - from: "crates/polint/src/analysis_kernel/mod.rs"
      to: "crates/polint/src/analysis/cfg/provider.rs"
      via: "kernel provider call after semantic MIR"
      pattern: "derive_cfg_with_cache_stats"
    - from: "crates/polint/src/analysis_kernel/debug.rs"
      to: "crates/polint/src/core/mod.rs"
      via: "test-only CFG debug rows from stored facts"
      pattern: "cfg"
---

<objective>
Wire CFG into the private analysis kernel with provider metadata, cache identity, validation, and test-only debug JSON.

Purpose: CFG must become a real internal provider product before Go/TS lowerers and eval fixtures depend on it.
Output: `polint.cfg` manifest, kernel execution slot, provider output digest/cache stats, cache-key vocabulary, validation diagnostics, and test-only debug rows.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-local-cfg-and-control-dependence/29-CONTEXT.md
@.planning/phases/29-local-cfg-and-control-dependence/29-01-PLAN.md
@.planning/phases/29-local-cfg-and-control-dependence/29-02-PLAN.md
@research/cfg-control-flow/STANDARD.md
@research/cfg-control-flow/RECOMMENDED_IMPLEMENTATION.md
@.planning/phases/21-provenance-precision-and-validation-metadata/21-CONTEXT.md
@.planning/phases/23-input-snapshots-and-cache-key-vocabulary/23-CONTEXT.md
@.planning/phases/28-private-semantic-mir-and-place-identity/28-CONTEXT.md
@.agents/skills/rust-best-practices/SKILL.md
@crates/polint/src/analysis/provider.rs
@crates/polint/src/analysis/cfg/facts.rs
@crates/polint/src/analysis/cfg/store.rs
@crates/polint/src/analysis/cfg/builder.rs
@crates/polint/src/analysis/cfg/derived.rs
@crates/polint/src/analysis_kernel/provider.rs
@crates/polint/src/analysis_kernel/mod.rs
@crates/polint/src/analysis_kernel/incremental/keys.rs
@crates/polint/src/analysis_kernel/validation.rs
@crates/polint/src/analysis_kernel/debug.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CFG provider shell and kernel execution slot</name>
  <files>crates/polint/src/analysis/cfg/mod.rs, crates/polint/src/analysis/cfg/provider.rs, crates/polint/src/analysis_kernel/provider.rs, crates/polint/src/analysis_kernel/mod.rs</files>
  <read_first>
    - crates/polint/src/analysis_kernel/provider.rs
    - crates/polint/src/analysis_kernel/mod.rs
    - crates/polint/src/analysis/provider.rs
    - crates/polint/src/analysis/cfg/store.rs
    - crates/polint/src/analysis/cfg/derived.rs
    - .planning/phases/29-local-cfg-and-control-dependence/29-CONTEXT.md
  </read_first>
  <behavior>
    - Test provider order is `polint.source`, `polint.go.syntax`, `polint.ts.syntax`, `polint.module_graph`, `polint.symbol_graph`, `polint.module_topology`, `polint.semantic_mir`, `polint.cfg`, `polint.metrics`.
    - Test `KernelRunReport.provider_outputs` contains a `polint.cfg` row with schema label, output digest, and cache stats.
    - Test empty CFG provider output is accepted before language lowerers add real CFG rows, while still producing a deterministic output digest.
  </behavior>
  <action>Create `analysis::cfg::provider` with `pub(crate) struct CfgProviderOutput { diagnostics: Vec<Diagnostic>, cache_stats: CacheStats, output_digest: Option<Digest> }` and `pub(crate) fn derive_cfg_with_cache_stats(db: &mut AnalysisDb, input_snapshot: &InputSnapshot, manifest: &ProviderManifest, semantic_mir_output_digest: Digest, upstream_syntax_output_digests: Vec<Digest>) -> CfgProviderOutput`. In this plan, the provider may build an empty `CfgOutput` through a `derive_cfg_output(db)` helper that later language plans extend. It must normalize output, derive reachability/dominators/postdominators/control dependence for requested internal views when functions exist, call `db.replace_cfg_facts(output)`, and compute a deterministic output digest from provider id/version/schema, input snapshot config/lifecycle/model/extension/tool slots, semantic MIR output digest, upstream syntax output digests, and all normalized CFG stable keys. Add provider manifest `polint.cfg` with inputs `source_files`, `functions`, `mir_bodies`, `mir_operations`, `places`, and `unsupported_semantics`, outputs `cfg_functions`, `cfg_nodes`, `basic_blocks`, `cfg_edges`, `cfg_reachability`, `cfg_dominators`, `cfg_postdominators`, `cfg_control_dependence`, and `unsupported_control_flow`, schema `cfg-facts-1:1`, language scope `MultiLanguage`, cache policy `InMemoryDerived`, and precision ceiling `SetupAware`. Wire `AnalysisKernel::run` to call `polint.cfg` after `polint.semantic_mir` and before metrics.</action>
  <verify>
    <automated>cargo test -p polint --lib cfg_provider provider_order kernel_run_report_cfg_row_carries_output_digest --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "derive_cfg_with_cache_stats|CfgProviderOutput|replace_cfg_facts|polint.cfg" crates/polint/src/analysis/cfg/provider.rs crates/polint/src/analysis_kernel/mod.rs` returns matches.
    - `rg -n "cfg-facts-1|cfg_functions|cfg_nodes|basic_blocks|cfg_edges|cfg_control_dependence|unsupported_control_flow" crates/polint/src/analysis_kernel/provider.rs` returns matches.
    - `cargo test -p polint --lib provider_order --locked` passes with `polint.cfg` between `polint.semantic_mir` and `polint.metrics`.
    - `cargo test -p polint --lib cfg_provider kernel_run_report_cfg_row_carries_output_digest --locked` passes.
  </acceptance_criteria>
  <done>The private kernel has a deterministic `polint.cfg` provider slot and run-report metadata row.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add CFG cache-key vocabulary</name>
  <files>crates/polint/src/analysis/cfg/cache_key.rs, crates/polint/src/analysis/cfg/mod.rs, crates/polint/src/analysis_kernel/incremental/keys.rs</files>
  <read_first>
    - crates/polint/src/analysis_kernel/incremental/keys.rs
    - crates/polint/src/analysis_kernel/incremental/input_snapshot.rs
    - crates/polint/src/analysis_kernel/provider.rs
    - crates/polint/src/analysis/cfg/provider.rs
    - crates/polint/src/analysis/cache_key.rs
    - .planning/phases/23-input-snapshots-and-cache-key-vocabulary/23-CONTEXT.md
    - .planning/phases/29-local-cfg-and-control-dependence/29-CONTEXT.md
  </read_first>
  <behavior>
    - Test `cfg_layer_key` changes when source digests, Go lifecycle, TS/JS lifecycle, config digest, provider parameters, semantic MIR output digest, or upstream syntax digest changes.
    - Test graph-view/provider parameters participate in the key.
    - Test absent extension, model, and toolchain digests are included.
  </behavior>
  <action>Create `analysis::cfg::cache_key` with `cfg_provider_parameter_digest()` over exact strings `cfg_functions`, `cfg_nodes`, `basic_blocks`, `cfg_edges`, `cfg_reachability`, `cfg_dominators`, `cfg_postdominators`, `cfg_control_dependence`, `unsupported_control_flow`, `normal_control_view`, `abrupt_aware_view`, and schema `cfg-facts-1`. Extend `LayerKind` with `Cfg` and add `LayerKey::cfg_layer_key(manifest, source_function_digests, config_digest, go_lifecycle_digest, ts_js_lifecycle_digest, upstream_syntax_output_digests, semantic_mir_output_digest, cfg_parameter_digest)` in `analysis_kernel/incremental/keys.rs`. The key must include provider id/version/schema, source/function input digests, lifecycle digests, config digest, parameter digest, semantic MIR provider output digest, Go/TS syntax dependency digests, absent extension/model/toolchain slots, and graph view labels. Add key-diff tests under a `cfg_layer_key` test module.</action>
  <verify>
    <automated>cargo test -p polint --lib cfg_layer_key --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "Cfg|cfg_layer_key|cfg_provider_parameter_digest|cfg-facts-1" crates/polint/src/analysis/cfg/cache_key.rs crates/polint/src/analysis_kernel/incremental/keys.rs` returns matches.
    - `rg -n "semantic_mir_output_digest|normal_control_view|abrupt_aware_view|ExtensionCode|toolchain|model" crates/polint/src/analysis/cfg/cache_key.rs crates/polint/src/analysis_kernel/incremental/keys.rs` returns matches.
    - `cargo test -p polint --lib cfg_layer_key --locked` passes.
  </acceptance_criteria>
  <done>CFG provider identity has future-fit deterministic cache-key vocabulary tied to semantic MIR and graph-view parameters.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add CFG validation and test-only debug JSON</name>
  <files>crates/polint/src/analysis/cfg/validate.rs, crates/polint/src/analysis/cfg/mod.rs, crates/polint/src/analysis_kernel/validation.rs, crates/polint/src/analysis_kernel/debug.rs</files>
  <read_first>
    - crates/polint/src/analysis_kernel/validation.rs
    - crates/polint/src/analysis_kernel/debug.rs
    - crates/polint/src/analysis/cfg/facts.rs
    - crates/polint/src/analysis/cfg/store.rs
    - crates/polint/src/analysis/cfg/derived.rs
    - crates/polint/src/core/mod.rs
    - research/cfg-control-flow/STANDARD.md
    - .planning/phases/29-local-cfg-and-control-dependence/29-CONTEXT.md
  </read_first>
  <behavior>
    - Test validation rejects dangling body/node/block/edge references, duplicate stable keys, duplicate normalized edges, invalid spans, missing entry/exit, and cross-function edge endpoints.
    - Test reachable labels agree with graph reachability and unreachable nodes do not poison dominator facts.
    - Test provider precision-ceiling validation rejects `polint.cfg` rows that claim unsupported exactness.
    - Test metadata debug JSON includes deterministic `cfg.functions`, `cfg.nodes`, `cfg.blocks`, `cfg.edges`, `cfg.reachability`, `cfg.dominators`, `cfg.postdominators`, `cfg.control_dependence`, and `cfg.unsupported` arrays.
  </behavior>
  <action>Create `analysis::cfg::validate` with `pub(crate) fn validate_cfg(db: &AnalysisDb, diagnostics: &mut Vec<Diagnostic>)`. Call it from `analysis_kernel::validation::validate_fact_metadata` after metadata validation can see stored CFG rows. Diagnostics must use rule id `polint/internal` and evidence labels `family`, `stable_key`, `field`, and `reason`. Validate one entry per CFG function, at least one selected exit or explicit unsupported/infinite marker, every edge endpoint exists and belongs to the same function, block node ranges are non-empty except synthetic entry/exit blocks, no duplicate identical edges after normalization, exceptional/cleanup/finally edges use non-normal kinds, derived rows reference existing blocks/edges, and unsupported control-flow rows include construct, source evidence, affected domain, conservative action, precision, and status. Extend test-only `metadata_debug_json_for_test` with CFG rows sorted by path/span/function/view/stable key/run id. Debug rows must include compact edge/node/block/dependence payload fragments and must exclude raw source, AST dumps, absolute paths, temp roots, and wall-clock data.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis_kernel::validation::cfg analysis_kernel::debug::cfg_debug_json --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "validate_cfg|duplicate identical edges|dangling|cfg_control_dependence|UnsupportedControlFlow" crates/polint/src/analysis/cfg/validate.rs crates/polint/src/analysis_kernel/validation.rs` returns matches.
    - `rg -n "cfg\\.functions|cfg\\.nodes|cfg\\.blocks|cfg\\.edges|cfg\\.control_dependence|CfgDebug" crates/polint/src/analysis_kernel/debug.rs` returns matches.
    - `cargo test -p polint --lib analysis_kernel::validation::cfg --locked` passes.
    - `cargo test -p polint --lib analysis_kernel::debug::cfg_debug_json --locked` passes.
  </acceptance_criteria>
  <done>CFG validation and debug rows make malformed graph state visible internally before downstream consumers use it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| semantic MIR provider -> CFG provider | CFG must be keyed and ordered after MIR so stale or missing MIR cannot create misleading graph facts. |
| CFG provider -> kernel run report | Provider output metadata must be deterministic and private. |
| CFG facts -> validation/debug | Malformed graph rows must become internal diagnostics, not silent downstream assumptions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-03-01 | T | Provider order | mitigate | Add provider-order and run-report tests for `polint.cfg` after `polint.semantic_mir`. |
| T-29-03-02 | R | Cache identity | mitigate | Add `cfg_layer_key` tests covering source, lifecycle, config, MIR output, graph views, and absent slots. |
| T-29-03-03 | T | Graph validity | mitigate | Validate structural invariants before derived rows are trusted. |
| T-29-03-04 | I | Debug JSON leakage | mitigate | Keep CFG debug JSON cfg(test)/crate-private and exclude raw source, ASTs, absolute paths, and timestamps. |
</threat_model>

<verification>
Run `cargo test -p polint --lib cfg_provider provider_order kernel_run_report_cfg_row_carries_output_digest --locked`, `cargo test -p polint --lib cfg_layer_key --locked`, `cargo test -p polint --lib analysis_kernel::validation::cfg analysis_kernel::debug::cfg_debug_json --locked`, and `cargo fmt --all -- --check`.
</verification>

<success_criteria>
`polint.cfg` is a private provider with deterministic run-report metadata, cache-key vocabulary, structural validation, and test-only debug visibility.
</success_criteria>

<output>
After completion, create `.planning/phases/29-local-cfg-and-control-dependence/29-03-SUMMARY.md`.
</output>
