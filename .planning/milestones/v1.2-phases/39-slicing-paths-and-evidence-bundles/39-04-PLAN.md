---
phase: 39-slicing-paths-and-evidence-bundles
plan: 04
title: Summary Expansion And Interprocedural Context
type: execute
wave: 4
depends_on: [39-03]
files_modified:
  - crates/polint/src/analysis/evidence/facts.rs
  - crates/polint/src/analysis/evidence/query.rs
  - crates/polint/src/analysis/slicing/interprocedural.rs
  - crates/polint/src/analysis/slicing/paths.rs
  - crates/polint/src/analysis/slicing/mod.rs
autonomous: true
requirements: [SAE-PREC-04]
must_haves:
  truths:
    - "D-11: Interprocedural evidence paths enforce call-site stack matching; entering a callee pushes the call site and returning must pop the matching call site."
    - "D-12: Unknown dynamic calls, unsupported edges, setup-missing summaries, and budgeted expansion create explicit unknown/havoc/omitted evidence instead of silent missing paths."
    - "D-13: Summary-projected edges render as compressed evidence steps by default with summary id/stable key, subject/callable, domain, endpoints, status, precision, provenance, and expansion status."
    - "D-14: Summary evidence supports expandable, opaque, and external/model-backed expansion states."
  artifacts:
    - path: "crates/polint/src/analysis/slicing/interprocedural.rs"
      provides: "Context-matched interprocedural traversal"
---

<objective>
Add summary compression/expansion handles and context-matched interprocedural traversal for evidence paths.
</objective>

<execution_context>
@.planning/phases/39-slicing-paths-and-evidence-bundles/39-CONTEXT.md
@.planning/phases/39-slicing-paths-and-evidence-bundles/39-PATTERNS.md
@.agents/skills/rust-best-practices/SKILL.md
@AGENTS.md
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Represent summary evidence compression and expansion state</name>
  <files>crates/polint/src/analysis/evidence/facts.rs, crates/polint/src/analysis/evidence/query.rs, crates/polint/src/analysis/slicing/paths.rs</files>
  <read_first>
    - crates/polint/src/analysis/summaries/facts.rs
    - crates/polint/src/analysis/data_flow/summary_edges.rs
    - crates/polint/src/analysis/evidence/facts.rs
    - research/program-slicing-evidence/RECOMMENDED_IMPLEMENTATION.md
  </read_first>
  <action>Add summary evidence payload fields or rows that record summary stable key/id, callable stable key, domain, input endpoint, output endpoint, status, precision, provenance, and expansion state. Expansion states must include `Expandable` with an expansion/replay key, `Opaque` with reason, and `ExternalModel` or equivalent with model evidence. Path rendering/query rows should show summary edges as one compressed step by default.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::slicing::paths::summary --locked</automated>
  </verify>
  <acceptance_criteria>
    - A test proves a Phase 38 summary-projected data-flow edge becomes one compressed summary evidence step.
    - A test proves expandable summaries carry a stable expansion key.
    - A test proves opaque summaries carry a non-empty reason and downgraded/unknown status.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce call-site stack matching for interprocedural paths</name>
  <files>crates/polint/src/analysis/slicing/interprocedural.rs, crates/polint/src/analysis/slicing/paths.rs, crates/polint/src/analysis/slicing/mod.rs</files>
  <read_first>
    - crates/polint/src/analysis/calls/facts.rs
    - crates/polint/src/analysis/refined_calls/facts.rs
    - crates/polint/src/analysis/data_flow/direct_calls.rs
    - crates/polint/src/path_context.rs
    - research/program-slicing-evidence/VALIDATION.md
  </read_first>
  <action>Add a crate-private `PathContext` or equivalent with call-site stack and max interprocedural depth. When traversing call/parameter-in edges, push the call site. When traversing return/parameter-out edges, require the matching call site to pop. Summary edges may bypass stack push/pop but must keep an expansion key or opaque reason. Unresolved dynamic calls create unknown evidence nodes/edges.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::slicing::interprocedural --locked</automated>
  </verify>
  <acceptance_criteria>
    - A test with two callers of one callee proves only the matching caller's source can reach its sink.
    - A negative test proves a path that enters through call site A and returns through call site B is rejected.
    - Recursive or over-depth traversal returns explicit budget/omitted evidence.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| call graph -> evidence path | Mismatched call/return context would create false evidence. |
| summary compression -> renderer | Opaque summaries must not be presented as fully explained paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T39-08 | Tampering | interprocedural path context | Mitigated | Require call-site stack matching and add mismatched-call negative tests. |
| T39-09 | Repudiation | summary evidence | Mitigated | Include summary status, precision, provenance, and expansion/opaque reason on every summary edge. |
</threat_model>

<verification>
- `cargo test -p polint --lib analysis::slicing::interprocedural --locked`
- `cargo test -p polint --lib analysis::slicing::paths --locked`
</verification>

<success_criteria>
Interprocedural evidence respects calling context and summary edges remain compact but expandable or honestly opaque.
</success_criteria>
