---
phase: 39-slicing-paths-and-evidence-bundles
plan: 02
title: Local Evidence Graph And Thin Full Slice Queries
type: execute
wave: 2
depends_on: [39-01]
files_modified:
  - crates/polint/src/analysis/evidence/provider.rs
  - crates/polint/src/analysis/evidence/query.rs
  - crates/polint/src/analysis/slicing/mod.rs
  - crates/polint/src/analysis/slicing/local.rs
  - crates/polint/src/analysis/mod.rs
autonomous: true
requirements: [SAE-PREC-04]
must_haves:
  truths:
    - "D-05: Local evidence graph rows support the first useful local backward/forward slice inside one function over operation, place, data-flow, and control-dependence evidence."
    - "D-06: Thin slices are the default diagnostic explanation subset, while full local/interprocedural slices remain internal debug/eval modes."
    - "D-07: Internal query modes include thin backward, full backward, forward impact, chop/source-to-sink, path, and expansion, with mode and edge-filter differences visible in cache keys, debug, and eval output."
    - "D-08: Budgeted or filtered slices report omitted regions, unknowns, and truncation explicitly."
  artifacts:
    - path: "crates/polint/src/analysis/slicing/local.rs"
      provides: "Local thin/full slice queries"
---

<objective>
Build the first useful local evidence graph and local slice query layer over data-flow and control-dependence facts.
</objective>

<execution_context>
@.planning/phases/39-slicing-paths-and-evidence-bundles/39-CONTEXT.md
@.planning/phases/39-slicing-paths-and-evidence-bundles/39-PATTERNS.md
@.agents/skills/rust-best-practices/SKILL.md
@AGENTS.md
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Materialize local evidence graph from existing facts</name>
  <files>crates/polint/src/analysis/evidence/provider.rs, crates/polint/src/analysis/evidence/query.rs</files>
  <read_first>
    - crates/polint/src/analysis/evidence/facts.rs
    - crates/polint/src/analysis/evidence/store.rs
    - crates/polint/src/analysis/data_flow/facts.rs
    - crates/polint/src/analysis/cfg/facts.rs
    - crates/polint/src/analysis/mir/op.rs
    - crates/polint/src/analysis/places.rs
  </read_first>
  <action>Extend the evidence provider to create local evidence nodes for MIR operations, source statements/spans, places, and synthetic unknown/omitted anchors. Project Phase 38 data-flow edges into evidence `DataValue`, `DataAddress`, `Model`, `Summary`, and `Unknown` edges as appropriate. Project CFG/control-dependence rows into evidence `Control` edges. Each evidence edge must carry source fact stable keys, provenance, status, precision, validation, confidence, and compact labels.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::evidence::provider --locked</automated>
  </verify>
  <acceptance_criteria>
    - A focused test proves local data-flow rows become evidence nodes and `DataValue` edges.
    - A focused test proves control-dependence rows become `Control` evidence edges.
    - Evidence rows preserve source fact stable keys and do not include raw source bodies or absolute paths.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement thin and full local slice queries</name>
  <files>crates/polint/src/analysis/slicing/mod.rs, crates/polint/src/analysis/slicing/local.rs, crates/polint/src/analysis/evidence/query.rs</files>
  <read_first>
    - crates/polint/src/analysis/evidence/facts.rs
    - crates/polint/src/analysis/evidence/store.rs
    - crates/polint/src/analysis/data_flow/query.rs
    - research/program-slicing-evidence/RECOMMENDED_IMPLEMENTATION.md
    - research/program-slicing-evidence/VALIDATION.md
  </read_first>
  <action>Add crate-private `SliceQuery`, `SliceDirection`, `SliceMode`, `EdgeFilter`, `SliceBudget`, and `SliceResult` or exact equivalents. Implement local backward and forward traversal over evidence edges. `ThinBackward` follows value-producing data edges and selected summary/model edges. `FullBackward` and `FullLocal` include data, address, control, call/return, summary, model, alias, and unknown edges. Results must include nodes, edges, omitted regions, unknowns, status, precision, and stats.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::slicing::local --locked</automated>
  </verify>
  <acceptance_criteria>
    - A test proves `ThinBackward` is a subset of `FullBackward` for the same criterion.
    - A test proves thin slices include direct value producers.
    - A test proves full local slices include selected control dependencies.
    - A test proves edge filters or budgets produce explicit omitted-region metadata.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| slice filters -> output | A filtered slice must not be presented as complete. |
| control dependence -> diagnostic evidence | Control edges must remain distinguishable from value-flow edges. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T39-04 | Tampering | slice completeness | Mitigated | Emit omitted-region rows whenever budgets or filters remove reachable evidence. |
| T39-05 | Information Disclosure | rendered nodes | Mitigated | Store spans and stable keys, not raw source bodies. |
</threat_model>

<verification>
- `cargo test -p polint --lib analysis::evidence::provider --locked`
- `cargo test -p polint --lib analysis::slicing::local --locked`
- `cargo test -p polint --lib analysis::evidence --locked`
</verification>

<success_criteria>
Local thin/full slice queries are deterministic, bounded, and explicit about omitted or unknown evidence.
</success_criteria>
