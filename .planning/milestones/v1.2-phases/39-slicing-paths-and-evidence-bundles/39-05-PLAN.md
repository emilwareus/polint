---
phase: 39-slicing-paths-and-evidence-bundles
plan: 05
title: Diagnostic Evidence Bundles JSON And SARIF Rendering
type: execute
wave: 5
depends_on: [39-02, 39-03, 39-04]
files_modified:
  - crates/polint/src/diagnostics/mod.rs
  - crates/polint/src/analysis/evidence/render.rs
  - crates/polint/src/reporting.rs
  - crates/polint/src/cli/mod.rs
  - crates/polint/src/runner/mod.rs
autonomous: true
requirements: [SAE-PREC-04]
must_haves:
  truths:
    - "D-02: Diagnostics can carry structured internal evidence bundles without breaking scalar evidence compatibility."
    - "D-15: JSON evidence is the primary structured renderer; SARIF is a lossy projection through codeFlows, threadFlows, related locations, and messages."
    - "D-16: JSON/SARIF evidence rendering is deterministic and bounded while preserving status, precision, provenance, unknowns, hidden-node counts, summary data, replay keys, and omitted-region information."
  artifacts:
    - path: "crates/polint/src/analysis/evidence/render.rs"
      provides: "Evidence JSON/SARIF rendering helpers"
---

<objective>
Connect evidence bundles to diagnostics and render deterministic JSON/SARIF evidence while preserving existing report compatibility.
</objective>

<execution_context>
@.planning/phases/39-slicing-paths-and-evidence-bundles/39-CONTEXT.md
@.planning/phases/39-slicing-paths-and-evidence-bundles/39-PATTERNS.md
@.agents/skills/rust-best-practices/SKILL.md
@AGENTS.md
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add internal evidence bundle attachment for diagnostics</name>
  <files>crates/polint/src/diagnostics/mod.rs, crates/polint/src/analysis/evidence/render.rs</files>
  <read_first>
    - crates/polint/src/diagnostics/mod.rs
    - crates/polint/src/reporting.rs
    - crates/polint/src/analysis/evidence/facts.rs
    - research/program-slicing-evidence/RECOMMENDED_IMPLEMENTATION.md
  </read_first>
  <action>Add an internal optional evidence bundle reference or side-table bridge for diagnostics. Preserve existing `Evidence { label, value }` serialization and builder behavior. Add helpers that can convert selected bundle labels into scalar evidence when existing public renderers need compatibility. Do not add public SDK methods or require rule authors to construct evidence bundles in this phase.</action>
  <verify>
    <automated>cargo test -p polint --lib diagnostics --locked</automated>
  </verify>
  <acceptance_criteria>
    - Existing diagnostic JSON tests still pass without schema breakage for diagnostics that have only scalar evidence.
    - A new test proves a diagnostic can reference an internal evidence bundle id or bridge object without changing stable fingerprint behavior unless configured to include evidence details.
    - Existing `with_evidence(label, value)` behavior remains available.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render structured JSON evidence deterministically</name>
  <files>crates/polint/src/analysis/evidence/render.rs, crates/polint/src/reporting.rs, crates/polint/src/cli/mod.rs, crates/polint/src/runner/mod.rs</files>
  <read_first>
    - crates/polint/src/reporting.rs
    - crates/polint/src/cli/mod.rs
    - crates/polint/src/runner/mod.rs
    - crates/polint/src/analysis/evidence/facts.rs
    - crates/polint/src/analysis/evidence/store.rs
  </read_first>
  <action>Add a deterministic structured evidence JSON representation for diagnostics/reporting that includes candidate paths up to configured limit, edge kinds, provenance, precision/status, hidden node counts, unknowns, omitted regions, replay key, and summary expansion keys. If the existing public JSON schema cannot carry all fields safely, add the structure under an explicitly versioned optional object such as `evidence_v1` and keep it bounded by config/default limits.</action>
  <verify>
    <automated>cargo test -p polint --lib reporting --locked</automated>
    <automated>cargo test -p polint --test cli check_json_output_is_deterministic_across_repeated_runs --locked</automated>
  </verify>
  <acceptance_criteria>
    - JSON rendering sorts paths, nodes, edges, unknowns, and omitted regions deterministically.
    - JSON output includes status, precision, provenance, replay key, hidden node count, and summary expansion keys for evidence-bearing diagnostics.
    - JSON output excludes absolute workspace roots, raw source bodies, parser object ids, and timestamps in evidence payloads.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Map evidence paths to SARIF codeFlows/threadFlows</name>
  <files>crates/polint/src/analysis/evidence/render.rs, crates/polint/src/reporting.rs</files>
  <read_first>
    - crates/polint/src/reporting.rs
    - crates/polint/tests/cli.rs
    - research/program-slicing-evidence/RECOMMENDED_IMPLEMENTATION.md
    - research/program-slicing-evidence/VALIDATION.md
  </read_first>
  <action>Extend SARIF rendering so selected evidence paths map to `codeFlows`, `threadFlows`, `threadFlowLocations`, related locations, and messages. SARIF must carry enough labels to show partial/unknown/omitted evidence and must not claim completeness when internal evidence status is partial, unknown, opaque, or budget exceeded. Keep SARIF as a lossy projection from the internal evidence model.</action>
  <verify>
    <automated>cargo test -p polint --lib reporting::sarif --locked</automated>
    <automated>cargo test -p polint --test cli sarif_output_includes_ci_fields_and_honors_fail_threshold --locked</automated>
  </verify>
  <acceptance_criteria>
    - SARIF output for an evidence-bearing diagnostic contains at least one `codeFlows` entry with ordered `threadFlowLocations`.
    - Partial/unknown/budgeted evidence produces SARIF text that includes `partial`, `unknown`, `opaque`, `omitted`, or `budget` wording as applicable.
    - Existing SARIF help URI behavior remains intact.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internal evidence -> public JSON | Optional structured evidence must stay bounded and versioned. |
| internal evidence -> SARIF | SARIF lossiness must not overstate completeness. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T39-10 | Information Disclosure | report rendering | Mitigated | Assert evidence JSON/SARIF omit absolute paths, raw source bodies, timestamps, and parser ids. |
| T39-11 | Spoofing | SARIF completeness | Mitigated | Render partial/unknown/budget wording when internal evidence is incomplete. |
</threat_model>

<verification>
- `cargo test -p polint --lib diagnostics --locked`
- `cargo test -p polint --lib reporting --locked`
- `cargo test -p polint --test cli sarif_output_includes_ci_fields_and_honors_fail_threshold --locked`
- `cargo test -p polint --test cli check_json_output_is_deterministic_across_repeated_runs --locked`
</verification>

<success_criteria>
Diagnostics can carry internal evidence and JSON/SARIF render bounded deterministic evidence with provenance and uncertainty.
</success_criteria>
