---
phase: 34-rust-extension-provider-sink
plan: 05
type: execute
wave: 5
depends_on: [34-01, 34-02, 34-03, 34-04]
files_modified:
  - crates/polint/src/analysis/extensions/cache_key.rs
  - crates/polint/src/analysis/extensions/provider.rs
  - crates/polint/src/analysis_kernel/incremental/keys.rs
  - crates/polint/src/analysis_kernel/incremental/quarantine.rs
  - crates/polint/src/analysis_kernel/incremental/layer_cache.rs
autonomous: true
requirements: [SAE-INT-04]
must_haves:
  truths:
    - "D-14: Extension cache identity includes source, dependency, manifest, protocol, options/config, declared inputs, input fact digests, and output digest"
    - "D-15: Phase 33 quarantine semantics are reused for extension-influenced cache nodes"
    - "D-16: Eval proves extension digest changes affect cache keys"
    - "D-17: --no-cache still runs extensions but bypasses analysis cache reads/writes"
  artifacts:
    - path: "crates/polint/src/analysis/extensions/cache_key.rs"
      provides: "Extension layer/provider key identity and digest construction"
    - path: "crates/polint/src/analysis_kernel/incremental/quarantine.rs"
      provides: "Real extension digest quarantine coverage"
---

<objective>
Wire extension digests into cache identity, layer cache behavior, and quarantine.

Purpose: SAE-INT-04 requires extension code and output to participate in deterministic cache invalidation. Phase 33 already created quarantine infrastructure; Phase 34 must feed it with real extension digests.

Output: Extension-influenced cache keys change when extension code/manifest/options/inputs change, and stale extension-influenced nodes are quarantined rather than served as hits.
</objective>

<context>
@.planning/phases/34-rust-extension-provider-sink/34-CONTEXT.md
@crates/polint/src/analysis_kernel/incremental/keys.rs
@crates/polint/src/analysis_kernel/incremental/quarantine.rs
@crates/polint/src/analysis_kernel/incremental/layer_cache.rs
@crates/polint/src/analysis/extensions/discovery.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add extension cache key construction</name>
  <files>crates/polint/src/analysis/extensions/cache_key.rs, crates/polint/src/analysis/extensions/mod.rs</files>
  <read_first>
    crates/polint/src/analysis_kernel/incremental/keys.rs
    crates/polint/src/analysis/summaries/cache_key.rs
    crates/polint/src/analysis/calls/cache_key.rs
    crates/polint/src/analysis/extensions/discovery.rs
  </read_first>
  <action>
    Add extension cache key helpers that build `LayerKey` values with `LayerKind::Extension`, provider id derived from extension/provider ids, schema/protocol labels, extension source/dependency digests, config/options digest, declared read digest, relevant input fact digests, and dependency layer digests. Include absent model/toolchain slots consistently with existing key constructors.
  </action>
  <acceptance_criteria>
    - `cache_key.rs` has tests proving source edit, manifest edit, protocol version change, declared read change, and options change produce different keys.
    - Tests prove identical inputs produce identical keys across repeated construction.
    - Extension layer keys contain at least one non-absent `DigestKind::ExtensionCode` digest when an extension is configured.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Apply quarantine to real extension-influenced nodes</name>
  <files>crates/polint/src/analysis_kernel/incremental/quarantine.rs, crates/polint/src/analysis/extensions/provider.rs</files>
  <read_first>
    crates/polint/src/analysis_kernel/incremental/quarantine.rs
    crates/polint/src/analysis_kernel/incremental/invalidation.rs
    crates/polint/src/analysis/demand/quarantine.rs
    crates/polint/src/analysis/extensions/cache_key.rs
  </read_first>
  <action>
    Connect extension provider key changes and validation failures to existing quarantine actions. Real extension layer/query/summary/diagnostic nodes should be quarantine-eligible when they contain a non-absent extension digest. Native-only nodes must remain rejected by quarantine, preserving Phase 33 D-09.
  </action>
  <acceptance_criteria>
    - A test with a real extension layer key proves `QuarantineStore::quarantine` accepts the node.
    - A test proves a native layer key with only absent extension digests is not quarantined.
    - A test proves reinstatement works when the extension digest returns to a previous value.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Preserve no-cache execution semantics</name>
  <files>crates/polint/src/analysis/extensions/provider.rs, crates/polint/src/analysis/extensions/host.rs</files>
  <read_first>
    crates/polint/src/cache/mod.rs
    crates/polint/src/analysis/summaries/provider.rs
    crates/polint/src/metrics.rs
  </read_first>
  <action>
    Ensure `--no-cache` or disabled cache modes do not skip extension discovery, handshake, provider execution, validation, or merge. They should only bypass extension layer reads/writes and record bypass stats consistently with native layer cache providers.
  </action>
  <acceptance_criteria>
    - A test proves cache-disabled runs still invoke the fake extension provider once.
    - A test proves cache-disabled runs record a bypass or disabled-cache stat and do not write extension layer payloads.
    - Existing `--no-cache` CLI behavior remains unchanged for normal public check output.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| extension cache -> analysis reuse | Stale extension output may be served after extension code or manifest changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-13 | Tampering | extension cache keys | mitigate | Include source/dependency/manifest/protocol/options/input digests |
| T-34-14 | Tampering | stale cache reuse | mitigate | Quarantine extension-influenced nodes on digest or validation changes |
| T-34-15 | Denial of Service | cache-disabled mode | mitigate | Keep provider execution independent from cache reads/writes |
</threat_model>

<verification>
cargo test --lib -p polint -- extensions::cache_key
cargo test --lib -p polint -- quarantine
cargo test --lib -p polint -- extension_no_cache
</verification>

<success_criteria>
- Extension digest changes affect cache keys.
- Extension-influenced nodes use existing quarantine.
- `--no-cache` still runs extension providers without cache reuse.
</success_criteria>

<output>
Create `.planning/phases/34-rust-extension-provider-sink/34-05-SUMMARY.md` when done.
</output>
