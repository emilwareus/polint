---
quick_id: 260719-t2i
status: complete
description: Parallelize eval fixture coverage tests and inspect remaining serial test bottlenecks without running tests locally
---

# Quick Task 260719-t2i Plan

Reduce eval-suite wall-clock time using the supplied GitHub Actions profiling data while preserving fixture coverage and leaving CI configuration unchanged.

## Task 1: Make native fixture coverage independently schedulable

**Files:** `crates/polint/src/eval/fixtures.rs`

**Action:** Replace the single loop over all native fixtures with a declarative list that emits one `#[test]` per fixture directory. Keep required-area discovery as a fast manifest-only test and preserve the Windows runtime-extension skip.

**Verify:** Format the changed Rust file, inspect the generated test list against every checked-in fixture manifest, and run whitespace/static diff checks only. Do not build, typecheck, or run tests locally; push for CI validation.

**Done:** Cargo can schedule all 53 native fixture coverage runs independently across its test threads.

## Task 2: Remove unasserted framework fixture passes

**Files:** `crates/polint/src/eval/fixtures.rs`

**Action:** Reduce the framework-entrypoints fixture helper from cold, warm, and no-cache executions to one isolated no-cache execution because its manifest asserts framework observations and runtime only, not cache determinism. Remove the now-unused determinism-only helper and redundant standalone pass test while retaining its useful assertions in shared coverage validation.

**Verify:** Format the changed Rust file and inspect all framework fixture expectations and call sites. Do not build, typecheck, or run tests locally; push for CI validation.

**Done:** Framework entrypoint coverage performs one kernel pass instead of six duplicated passes across two tests.

## Task 3: Reuse the direct-calls fixture evaluation

**Files:** `crates/polint/src/eval/fixtures.rs`

**Action:** Cache the immutable direct-calls core evaluation in a thread-safe `OnceLock` so its generated coverage test and four focused assertion tests share one cold/warm/no-cache result.

**Verify:** Format the changed Rust file, inspect every call site, and rely on GitHub Actions for compilation and tests. Do not build, typecheck, or run tests locally.

**Done:** The five tests identified above 60 seconds by CI execute one fixture evaluation instead of five.
