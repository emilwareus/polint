# Quick Task 260520-a6t: Fix Pnpm Workspace Package-Manager Review Findings

**Date:** 2026-05-20
**Status:** Complete

## Scope

Fix the follow-up review findings in TS/JS package-manager topology support:

1. Treat root `pnpm-workspace.yaml` as workspace membership evidence when `package.json#workspaces` is absent.
2. Keep a root explicit `packageManager` authoritative for workspace members, even if a member has a stale conflicting lockfile.
3. Emit controlled unsupported evidence when a selected JS lockfile parses but produces no exact selected entries.

## Tasks

### 1. Workspace Selection
- Files: `crates/polint/src/module_graph/ts.rs`
- Action: Include pnpm workspace members in manifest collection and workspace-root detection, and prefer explicit workspace-root package managers.
- Verify: Add pnpm workspace inheritance and stale member lockfile regression tests.

### 2. Lockfile Parse Gaps
- Files: `crates/polint/src/module_graph/formats/js_lockfile.rs`, `crates/polint/src/module_graph/ts.rs`
- Action: Surface empty selected lockfile coverage as unsupported topology evidence instead of silent absence.
- Verify: Add Bun selected-lockfile regression for unparseable git/path-style exact entries.

### 3. Verification
- Run focused TS topology and JS lockfile tests, formatting, clippy, and the full workspace test suite.
