---
quick_id: 260604-tz2
status: completed
created: 2026-06-04T19:34:53.302Z
description: Fix CI failure from CLI macos-x86_64 log
---

# Quick Task: Fix CI Artifact Upload Failure

## Finding

The attached `CLI macos-x86_64` log shows the Rust build and packaging completed successfully. The job failed only at `actions/upload-artifact@v7` with `Failed to CreateArtifact: Unable to make request: ENOTFOUND` in the `release-dry-run` workflow running on a pull request.

## Scope

- Keep PR release dry-run coverage for building and packaging CLI archives.
- Avoid making PR checks depend on GitHub artifact service availability when no downstream PR job consumes those artifacts.
- Do not weaken real release artifact upload behavior.

## Acceptance

- `release-dry-run` PR jobs build and package archives but skip artifact upload.
- Real release workflow still uploads CLI artifacts for release asset publication.
- Workflow syntax remains valid.

## Result

- Added a PR-only guard around the `release-dry-run` packaged CLI artifact upload.
- PR release dry-runs still build, package, and checksum the CLI archive.
- Real release artifact upload remains unchanged in `release.yml`.
- Verified `.github/workflows/release-dry-run.yml` parses as YAML.
