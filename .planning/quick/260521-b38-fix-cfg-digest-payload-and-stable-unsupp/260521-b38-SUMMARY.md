# Quick Task 260521-b38 Summary: Fix CFG Digest Payload And Stable Unsupported Control-Flow Keys

## Outcome

Fixed both follow-up PR review findings for CFG digest correctness and unsupported control-flow stable keys.

## Changes

- Expanded CFG provider output digests to include behavior-relevant payload fields for functions, nodes, blocks, edges, reachability, dominators, postdominators, control dependence, and unsupported control-flow rows.
- Resolved CFG references to stable CFG row keys in digest parts and added a regression that dense CFG/MIR ID shifts do not change the digest.
- Removed run-local `MirBodyId` and `MirOpId` values from Go and TS/JS `UnsupportedControlFlow` stable keys.
- Added regressions proving unsupported control-flow stable keys are tied to source unsupported-semantic stable identity rather than dense body/operation IDs.

## Verification

- `cargo test -p polint --lib analysis::cfg::provider --locked`
- `cargo test -p polint --lib analysis::cfg::lower_go --locked`
- `cargo test -p polint --lib analysis::cfg::lower_ts --locked`
- `cargo test -p polint --lib analysis::cfg --locked`
- `cargo test -p polint --lib analysis_kernel::validation::cfg --locked`
- `cargo fmt --all -- --check`
- `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings`
