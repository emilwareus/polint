# Quick Task 260521-b38: Fix CFG Digest Payload And Stable Unsupported Control-Flow Keys

## Goal

Fix the follow-up PR review findings for Phase 29 CFG facts:

- CFG provider output digests must change when behavior-relevant fact payload changes, not only when stable keys change.
- Unsupported control-flow stable keys must not include run-local MIR body or operation IDs.

## Tasks

### 1. Make CFG Output Digest Payload-Sensitive

Files:
- `crates/polint/src/analysis/cfg/provider.rs`

Action:
- Include canonical payload parts for CFG functions, nodes, blocks, edges, derived rows, and unsupported control-flow rows.
- Resolve CFG references to stable keys in digest parts where possible so run-local dense handles do not become persistent identity.
- Add regression tests proving changed `reachable` and `immediate` payload fields change the digest.

Verify:
- `cargo test -p polint --lib analysis::cfg::provider --locked`

### 2. Remove Run-Local IDs From Unsupported Control-Flow Stable Keys

Files:
- `crates/polint/src/analysis/cfg/lower_go.rs`
- `crates/polint/src/analysis/cfg/lower_ts.rs`

Action:
- Build `UnsupportedControlFlow` stable keys from language, construct, span, and the source unsupported semantic stable key.
- Do not include numeric `MirBodyId` or `MirOpId` values in the stable key.
- Add/adjust regression assertions to prove the key is stable across run-local body/operation IDs when the source unsupported semantic key is stable.

Verify:
- `cargo test -p polint --lib analysis::cfg::lower_go analysis::cfg::lower_ts --locked` using separate Cargo filters.

## Final Verification

- `cargo test -p polint --lib analysis::cfg --locked`
- `cargo test -p polint --lib analysis_kernel::validation::cfg --locked`
- `cargo fmt --all -- --check`
- `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings`
