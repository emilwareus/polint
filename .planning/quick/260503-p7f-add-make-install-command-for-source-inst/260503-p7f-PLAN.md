# Quick Task 260503-p7f: Add make install command for source installs

**Date:** 2026-05-03
**Status:** Complete

## Goal

Add a root-level `make install` command that installs the `polint` CLI from the
current source checkout.

## Tasks

1. Add a minimal root `Makefile` with an `install` target for `polint`.
2. Document the new local source install command in the README.
3. Verify the target with a temporary Cargo install root and commit the change.
