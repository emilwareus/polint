---
quick_id: 260829-q4d
status: in_progress
description: Fix the Windows rules-store absolute override test, check neighboring overrides, run requested tests and formatting, and commit without pushing
---

# Quick Task 260829-q4d Plan

1. Inspect `resolve_root` tests for unguarded Unix-only absolute override inputs.
2. Replace the affected override with an absolute path derived from `std::env::temp_dir()` while preserving production semantics and off-value assertions.
3. Run the requested rules-store unit-test subset and formatting check, then commit the complete quick task with the requested message.
