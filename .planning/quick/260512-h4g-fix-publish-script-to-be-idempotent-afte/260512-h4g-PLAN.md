# Quick Task 260512-h4g Plan

## Goal

Make `scripts/publish-crates.sh` safe to resume after a partial crates.io publish and resilient to crates.io search/index lag.

## Steps

1. Replace the `cargo search` exact-version check with a direct Cargo registry metadata lookup from outside the workspace.
2. Skip any package whose exact version is already published.
3. Keep waiting for `polint-macros` before publishing `polint`, but base the wait on Cargo-resolvable registry metadata.
4. Stop using deprecated `cargo publish --token`; map `CRATES_IO_TOKEN` to `CARGO_REGISTRY_TOKEN`.
5. Verify with shell syntax checking and dry-run packaging.
