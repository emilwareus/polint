# Quick Task 260520-9jr: Fix Package Manager Topology Review Findings

**Date:** 2026-05-20
**Status:** In progress

## Scope

Fix the three review findings from the package-manager topology PR:

1. Allow valid `ExactLockfile` topology rows during metadata validation.
2. Avoid attributing inherited root JS lockfile entries to the wrong workspace package.
3. Prevent Bun lockfile parsing from treating cache/artifact strings as exact package versions.

## Tasks

### 1. Validation
- Files: `crates/polint/src/analysis_kernel/validation.rs`
- Action: Make type-specific exact-lockfile validation authoritative for allowed topology row kinds.
- Verify: Add positive and negative metadata validation tests.

### 2. JS Lockfile Attribution and Bun Parsing
- Files: `crates/polint/src/module_graph/ts.rs`, `crates/polint/src/module_graph/formats/js_lockfile.rs`
- Action: Scope lockfile rows conservatively by package path/importer, and only extract Bun versions from supported resolution slots.
- Verify: Add workspace attribution and Bun parser regression tests.

### 3. Test and Summarize
- Files: relevant Rust tests and `.planning/STATE.md`
- Action: Run formatting, targeted tests, clippy, and workspace tests where feasible.
- Verify: Record commands and outcomes in the quick summary.
