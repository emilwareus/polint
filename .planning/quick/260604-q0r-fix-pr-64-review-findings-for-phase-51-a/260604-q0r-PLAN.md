---
quick_id: 260604-q0r
status: completed
created: 2026-06-04T16:44:06.781Z
description: Fix PR 64 review findings for Phase 51 adaptation model ingestion, budgets, and reporting
---

# Quick Task: Fix PR 64 Review Findings

## Scope

Fix the three PR review findings without changing public SDK/runner surfaces:

1. Production semantic graph provider must discover and load repo-local adaptation model files instead of always passing an empty `AdaptationModelStore`.
2. Adaptation model budgets must be enforced for file count, target fanout, and derived model edges, not only included in digests.
3. Adapted delta reporting must not double-count extension adaptation facts as model facts.

## Acceptance

- Repo-local `.polint/models/**/*.polint-model.toml` and `.polint/models/**/*.toml` files are loaded deterministically, validated against existing semantic graph node keys, and passed into model-edge lowering.
- Model file parse/load failures surface as provider diagnostics.
- Model budgets explicitly reject over-budget files/facts/fanout and cap solver-derived model edges with an honest budget signal.
- Extension facts with `producer_id = "polint.extension.adaptation"` count only as extension facts; model facts count only when they come from the adaptation model family/provider/provenance.
- Focused tests cover production ingestion, budget caps, and reporting classification.

## Result

- Implemented production adaptation model discovery/loading in `polint.semantic_graph`.
- Enforced adaptation file, fact/fanout, and solver-derived edge budgets.
- Fixed adaptation delta classification so extension adaptation facts are not counted as model facts.
- Verified with `cargo test -p polint`.
