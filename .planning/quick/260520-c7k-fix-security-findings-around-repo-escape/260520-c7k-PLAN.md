# Quick Task 260520-c7k: Fix Security Findings Around Repo Escape Reads

**Date:** 2026-05-20
**Status:** In progress

## Goal

Close the reported security findings for untrusted repository inputs:
- prevent repo-controlled topology/cache paths and tsconfig extends from reading outside the repository,
- prevent workspace glob expansion from probing outside the repository,
- reject Go package patterns that can be interpreted as `go list` flags and run package loading with `-mod=readonly`,
- bound topology manifest and lockfile reads,
- replace predictable synthetic `go.work` writes with exclusive temp files.

## Tasks

1. Add centralized safe repo file/path helpers and bounded topology reads.
   - Files: `crates/polint/src/module_graph/paths.rs`, TS topology and incremental modules.
   - Verify: symlink escapes, absolute/ancestor paths, and oversized files are skipped or represented as unsupported facts.

2. Harden workspace glob and tsconfig resolution call sites.
   - Files: `crates/polint/src/module_graph/ts.rs`, `crates/polint/src/analysis_kernel/incremental/keys.rs`, `crates/polint/src/analysis_kernel/incremental/input_snapshot.rs`.
   - Verify: absolute and `../*` workspace patterns do not call `read_dir`; absolute or escaping tsconfig extends do not enter digests/facts.

3. Harden Go lifecycle and package loading.
   - Files: `crates/polint/src/go/lifecycle.rs`, `crates/polint/src/module_graph/go.rs`.
   - Verify: package patterns beginning with `-` are rejected, `go list` uses `-mod=readonly`, and synthetic workspaces use exclusive temp files.

4. Add regression tests and run focused checks.
   - Verify: targeted module tests plus `cargo fmt`.
