---
quick_id: 260604-g7q
status: in_progress
date: 2026-06-04
---

# Quick Task 260604-g7q: Fix PR review findings for TS object-model eval gates

## Goal

Address the PR review findings in the TypeScript object-model extractor, solver, provider path, and eval coverage without widening public API surface.

## Tasks

1. Fix production object-model behavior.
   - Ensure duplicate fact validation runs before normalization in the provider path.
   - Ensure inline allocation expressions resolve to the same stable keys recorded for allocations.
   - Seed callable tokens for object literal method/function values.
   - Consume receiver bindings when resolving `this`-based property calls.

2. Add regression coverage.
   - Cover inline object/new allocation key reuse.
   - Cover object literal method calls and receiver-derived `this` calls.
   - Keep assertions at the public/eval surface where possible.

3. Verify and commit.
   - Run focused TS object-model tests, eval gates, formatting, clippy, and full crate tests.
   - Write SUMMARY.md and update `.planning/STATE.md`.
