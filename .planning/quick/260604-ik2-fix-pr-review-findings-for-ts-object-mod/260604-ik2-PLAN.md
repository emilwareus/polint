---
quick_id: 260604-ik2
status: in_progress
date: 2026-06-04
---

# Quick Task 260604-ik2: Fix final TS object-model review findings

## Goal

Address the final PR review findings in the private TypeScript object-model driver before merge.

## Tasks

1. Fix scoped object/class lookup.
   - Prevent same-name declarations in different lexical scopes from aliasing object allocations.
   - Make class prototype lookup independent of source order for valid `new C()` uses.

2. Fix exact property-key matching.
   - Canonicalize exact static/string/numeric property names so equivalent JS property forms match.
   - Preserve bounded computed/unknown bucket behavior.

3. Add regression coverage and re-review.
   - Cover same-name shadowing, forward class references, and cross-form exact property calls.
   - Run focused tests, formatting/lint checks, and a final source review pass.
