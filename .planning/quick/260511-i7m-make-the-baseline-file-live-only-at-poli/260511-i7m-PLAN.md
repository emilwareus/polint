# Quick Task 260511-i7m: Make the baseline file live only at .polint/baseline.yaml and remove user-selectable baseline paths

**Date:** 2026-05-11
**Status:** Complete

## Goal

Make `.polint/baseline.yaml` the only supported baseline location. Remove public CLI options that let users choose the file name or path, and update docs, generated skill text, and tests to match.

## Tasks

1. Update baseline CLI behavior
   - Files: `crates/polint/src/baseline.rs`, `crates/polint/src/cli/mod.rs`
   - Action: Change the default baseline path to `.polint/baseline.yaml`; remove `--output` from `baseline create`, remove `--baseline PATH` from `baseline update`, and make `check --baseline` a boolean flag.
   - Verify: Help text and existing baseline behavior use only the canonical path.

2. Update tests and docs
   - Files: `crates/polint/tests/cli.rs`, `README.md`, `docs/AGENT-PLAYBOOK.md`, `crates/polint/src/cli/skill.rs`, `.claude/skills/polint/SKILL.md`
   - Action: Replace root-level `.polint-baseline.yaml` examples with `.polint/baseline.yaml` and add/adjust tests that reject removed path flags.
   - Verify: CLI integration tests cover create/check/update/ignore/malformed YAML at the canonical location.

3. Run focused verification
   - Action: Run formatting plus baseline-focused tests, then broader `polint` checks if the focused suite passes.
   - Verify: `cargo fmt --all`, `cargo test -p polint baseline --locked`, and `cargo check -p polint --locked` pass.
