# Quick Task 260509: Comment Ignores Feature

**Date:** 2026-05-09
**Mode:** quick local execution

## Goal

Implement engine-level comment ignores and `polint ignores` so policy
diagnostics can be suppressed by source comments and ignore debt can be reported
by rule/file for humans and coding agents.

## Scope

- Add `[ignores].require_reason` config.
- Parse Go and TS/JS/TSX/JSX comment directives.
- Suppress policy-rule diagnostics after rule execution.
- Emit unused, malformed, and missing-reason ignore diagnostics.
- Add `polint ignores` with `--stat`, `--shortstat`, `--filter`, and JSON.
- Update docs, generated skill text, and agent playbook.
- Add focused unit/integration tests and run verification.

## Tasks

- [x] Build ignore directive parser and language-aware comment scanner.
- [x] Add suppression report generation and diagnostics.
- [x] Integrate suppression into parent CLI and local rule host paths without double suppression.
- [x] Add `polint ignores` output modes.
- [x] Add config hashing/schema/docs/skill updates.
- [x] Add tests for comments, config, output, filtering, and local rule host behavior.
- [x] Run focused and workspace verification.
