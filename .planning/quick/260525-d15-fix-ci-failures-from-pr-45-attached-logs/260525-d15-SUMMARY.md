---
quick_id: 260525-d15
slug: fix-ci-failures-from-pr-45-attached-logs
status: complete
completed: 2026-05-25
---

# Quick Task 260525-d15 Summary

## Outcome

Fixed the PR #45 CI failures from the attached logs:

- Updated the remaining provider-order assertions to include `polint.refined_calls` before `polint.metrics`.
- Replaced the refined-call extension fixture's line-ending-sensitive `file_span` site binding with `file_callee:<path>:<callee>`.
- Added `file_callee` validation and translation for refined-call extension site references, requiring a unique matching call site.

## Verification

- `cargo test -p polint --lib analysis_kernel::tests::kernel_run_report_records_input_snapshot_and_provider_outputs --locked`
- `cargo test -p polint --lib analysis_kernel::tests::provider_manifests_cover_existing_kernel_providers --locked`
- `cargo test -p polint --lib eval_native_fixture_runner_refined_calls_fixture_passes --locked`
- `cargo test -p polint --lib analysis::extensions::validate --locked`
- `cargo test -p polint --lib refined_call --locked`
- `cargo test -p polint --lib --locked`
- `cargo fmt --all --check`
- `cargo clippy --workspace --all-targets --locked -- -D warnings`
