---
quick_id: 260525-d15
slug: fix-ci-failures-from-pr-45-attached-logs
status: complete
created: 2026-05-25
---

# Quick Task 260525-d15: Fix CI failures from PR 45 attached logs

## Goal

Fix the failing PR #45 CI actions reported by the attached Ubuntu, Windows, and macOS logs.

## Findings From Logs

- Ubuntu/macOS fail `analysis_kernel::tests::kernel_run_report_records_input_snapshot_and_provider_outputs` because expected provider output order omits `polint.refined_calls`.
- Ubuntu/macOS fail `analysis_kernel::tests::provider_manifests_cover_existing_kernel_providers` for the same provider list mismatch.
- Windows additionally fails `eval_native_fixture_runner_refined_calls_fixture_passes`; inspect whether the new `file_span` extension fixture binding is path separator or byte-offset sensitive.

## Tasks

1. Reproduce the failing tests locally.
2. Update provider-order tests/manifests to include the refined-call provider where intended.
3. Fix the Windows refined-call fixture mismatch with a deterministic, cross-platform binding.
4. Rerun focused checks plus formatting/clippy as appropriate.

## Verification

- `cargo test -p polint --lib analysis_kernel::tests::kernel_run_report_records_input_snapshot_and_provider_outputs --locked`
- `cargo test -p polint --lib analysis_kernel::tests::provider_manifests_cover_existing_kernel_providers --locked`
- `cargo test -p polint --lib eval_native_fixture_runner_refined_calls_fixture_passes --locked`
- `cargo test -p polint --lib refined_call --locked`
- `cargo test -p polint --lib --locked`
- `cargo fmt --all --check`
- `cargo clippy --workspace --all-targets --locked -- -D warnings`
