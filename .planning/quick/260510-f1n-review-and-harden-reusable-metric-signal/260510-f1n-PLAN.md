# Review and harden reusable metric signal changes

## Goal

Review the PR against `main` using the local Rust and polint skills, fix issues
found during review, and verify the branch is in good shape.

## Findings to fix

- `polint add-skill` prompts before overwrite, but a declined overwrite exits as
  an error and prevents `--all` from installing other missing skills.
- `.claude/skills/polint/SKILL.md` is tracked but is missing the reusable metric
  signal guidance generated by `polint add-skill`.
- Metric signal view helpers are exercised through end-to-end tests, but their
  direct SDK query methods deserve focused unit coverage.

## Verification

- Targeted unit/integration tests for changed behavior.
- `cargo fmt --all -- --check`
- `cargo clippy --all-targets --all-features --locked -- -D warnings`
- `cargo test --workspace --all-targets --locked --quiet`
- CLI smoke on the code-quality metric example and add-skill overwrite prompts.
