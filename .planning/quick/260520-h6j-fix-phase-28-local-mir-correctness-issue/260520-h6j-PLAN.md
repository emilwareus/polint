# Quick Task 260520-h6j: Fix Phase 28 local MIR correctness issues and add edge-case tests

**Date:** 2026-05-20
**Status:** Complete

## Goal

Fix the local Phase 28 semantic MIR issues found during review, with emphasis on correctness and regression coverage rather than remote CI status.

## Tasks

1. Fix changed-code local quality issues
   - Files: `crates/polint/src/analysis/mir/lower_go.rs`, `crates/polint/src/analysis/mir/lower_ts.rs`, `crates/polint/src/analysis/places.rs`, `crates/polint/src/analysis/validate.rs`, `crates/polint/src/analysis_kernel/mod.rs`, `crates/polint/src/analysis_kernel/incremental/keys.rs`, `crates/polint/src/eval/fixtures.rs`
   - Action: Remove redundant clones and refactor oversized helper signatures into small context/spec structs where doing so improves maintainability.
   - Verify: `cargo clippy --all-targets --all-features --locked -- -D warnings`

2. Make TS/JS MIR lowering explicit for common unsupported or nested constructs
   - Files: `crates/polint/src/analysis/mir/lower_ts.rs`
   - Action: Preserve nested call/read/write evidence in common expression and statement forms; emit structured unsupported rows for constructs the private MIR cannot model yet instead of silently dropping them.
   - Verify: focused semantic MIR tests cover binary/comparison expressions, templates, unary/update forms, switch, try/catch/finally, throw, do-while, for-in, and for-of cases.

3. Prevent missing place references from disappearing
   - Files: Go/TS MIR lowerers and validation/tests as needed.
   - Action: Replace silent `filter_map` loss for drafted MIR place references with controlled fallback evidence or validation-visible behavior.
   - Verify: tests assert call arguments and unsupported affected places are retained or represented explicitly.

4. Deep review and iterate
   - Files: all changed source and tests.
   - Action: Review the new diff after implementation, fix follow-up findings, and rerun local tests.
   - Verify: `cargo test --workspace --all-features --locked` plus targeted tests pass.
