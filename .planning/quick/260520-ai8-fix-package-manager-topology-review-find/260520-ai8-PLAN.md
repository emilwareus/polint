# Quick Task 260520-ai8: Fix Package-Manager Topology Review Findings

**Date:** 2026-05-20
**Status:** Complete

## Scope

Fix the follow-up deep-review findings with TDD coverage:

1. Do not let root `pnpm-workspace.yaml` make every repo package inherit the root package manager.
2. Parse `pnpm-workspace.yaml` as structured YAML, not ad-hoc line text.
3. Include pnpm workspace member topology files in module-graph cache inputs.
4. Do not emit missing `go.sum` evidence for Go requirements replaced by local paths.

## Verification

- Add failing regression tests first.
- Run focused module graph and incremental key tests.
- Run `cargo fmt --check`.
- Run `cargo clippy --workspace --all-targets --all-features -- -D warnings`.
- Run `cargo test --workspace --all-features`.
- Perform another deep local review before finalizing.
