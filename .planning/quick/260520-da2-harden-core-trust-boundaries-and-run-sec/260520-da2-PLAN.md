# Quick Task 260520-da2: Harden core trust boundaries and run secondary deep security review

**Date:** 2026-05-20
**Status:** Complete

## Goal

Harden polint core against untrusted repository data while preserving repo-local rules as a trusted, maximally powerful extension boundary.

## Tasks

1. Add shared repo-confined filesystem helpers for bounded reads, safe directory creation, and atomic no-symlink writes.
2. Wire remaining risky core paths through those helpers: config, package metadata labels, AI-friendly output, baseline, source snippets, and analysis/layer caches.
3. Make Go semantic loading readonly by default and provide an offline mode, then add regression tests and run a secondary security review.

## Verification

- `cargo test -p polint`
- `cargo clippy --all-targets --all-features --locked -- -D warnings`
- Targeted security scan of Rust/Go command, filesystem, and parser-input boundaries.
