# Quick Task 260503-ba9: Add multi-rule example with one local rule-pack Cargo manifest

**Date:** 2026-05-03
**Status:** Complete

## Goal

Add a new example that demonstrates the recommended real-project structure for
multiple repo-local rules: one Cargo package for the whole local rule set, with
multiple rule modules registered by the same runner.

## Tasks

1. Add `examples/multiple-rules` with mixed Go/TS fixtures, `.polint.toml`, and
   one rule-pack crate at `.polint/rules/Cargo.toml`.
2. Implement two minimal local rules in that crate and register both from one
   `main.rs`.
3. Document the example in its README and the top-level README.
4. Add CLI e2e coverage proving the example emits both rule IDs from the single
   rule-pack crate.
5. Run formatting, metadata, and targeted CLI tests.
