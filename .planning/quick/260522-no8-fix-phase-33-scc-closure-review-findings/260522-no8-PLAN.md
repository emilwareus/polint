---
quick_id: 260522-no8
slug: fix-phase-33-scc-closure-review-findings
status: complete
created: 2026-05-22
---

# Quick Plan: Fix Phase 33 SCC Closure Review Findings

## Goal

Fix the three critical review findings from the Phase 33 SCC closure feature:

1. Non-recursive SCC chains must observe already-closed callee summaries.
2. Leaf/no-callee SCCs must not be rewritten as interprocedural updates.
3. Production SCC backdating must read/write previous SCC output digests and surface hit rows.

## Tasks

1. Add failing regression tests for transitive non-recursive propagation, no-op leaf preservation, and warm-run backdating.
2. Update SCC closure to publish each SCC result before processing callers and skip no-callee rewrites.
3. Add an internal cache record for previous SCC output digests and wire it from `AnalysisKernel`.
4. Run focused tests, full `polint` tests, format/check, commit, and perform a final deep review.

## Outcome

Completed. SCC closure now merges each processed SCC immediately, preserves local leaf summaries, persists SCC output digests through the internal cache path, records warm-run backdating as demand-query cache hits, and computes persisted digests from final SCC summaries rather than only updated rows.
