---
quick_id: 260525-c1a
slug: fix-final-review-findings-for-phase-37-r
status: complete
created: 2026-05-25
---

# Quick Task 260525-c1a: Fix final review findings for Phase 37 refined calls

## Goal

Close the final review findings before merge:

- Reject dangling `refined_calls.edge` extension call-site/function/symbol references during extension validation.
- Use stable call-site binding in the extension eval fixture.
- Strengthen direct-vs-refined eval coverage so Go refined-call output is proven by the fixture.

## Tasks

1. Add failing regression coverage for refined-call extension reference validation and Go fixture coverage.
2. Implement refined-call reference resolution inside `analysis::extensions::validate`.
3. Update refined-call eval fixtures and run focused plus workspace checks.

## Verification

- `cargo test -p polint --lib analysis::extensions::validate --locked`
- `cargo test -p polint --lib analysis::refined_calls::go --locked`
- `cargo test -p polint --lib refined_call --locked`
- `cargo test -p polint --lib eval_native_fixture_runner_refined_calls_fixture_passes --locked`
- `cargo test -p polint --test cli --locked -- checked_in_examples_are_runnable_cli_fixtures`
- `cargo fmt --all --check`
- `cargo clippy --workspace --all-targets --locked -- -D warnings`
