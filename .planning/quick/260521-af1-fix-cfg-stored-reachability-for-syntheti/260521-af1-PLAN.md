# Quick Task 260521-af1: Fix CFG Stored Reachability For Synthetic Exits

## Goal

Fix the follow-up review finding where optional synthetic exits are stored as reachable even when no CFG edge reaches them.

## Tasks

### 1. Recompute Builder Reachability

Files:
- `crates/polint/src/analysis/cfg/builder.rs`

Action:
- Before `CfgBuilder::finish` returns, recompute stored block reachability from the entry block over `NormalControl` edges for every builder-owned function.
- Add a regression test proving synthetic exits without incoming paths are stored as unreachable.

Verify:
- `cargo test -p polint --lib analysis::cfg::builder --locked`

### 2. Validate Abrupt Lowering

Files:
- `crates/polint/src/analysis/cfg/lower_go.rs`
- `crates/polint/src/analysis/cfg/lower_ts.rs`

Action:
- Add regression assertions that abrupt-only CFG paths no longer leave stale reachable flags on untargeted exits.

Verify:
- `cargo test -p polint --lib analysis::cfg::lower_go analysis::cfg::lower_ts --locked` using separate Cargo filters.

## Final Verification

- `cargo test -p polint --lib analysis::cfg --locked`
- `cargo test -p polint --lib analysis_kernel::validation::cfg --locked`
- `cargo fmt --all -- --check`
- `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings`
