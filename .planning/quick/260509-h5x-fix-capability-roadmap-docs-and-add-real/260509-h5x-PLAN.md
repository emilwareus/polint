# Quick Task 260509-h5x: Fix capability roadmap docs and add realistic CLI coverage for explain plan

**Date:** 2026-05-09
**Mode:** quick local execution

## Goal

Make the human-readable capability roadmap reflect that Phase 11's analysis-plan item is complete, and add realistic test coverage that proves a checked-in multi-rule local rule pack exposes its plan through `polint explain plan`.

## Tasks

1. Update `docs/roadmap/00_ROADMAP.md` so entry 1 is checked and explains what Phase 11 delivered without implying later capability families are done.
2. Add an integration test in `crates/polint/tests/cli.rs` using `examples/multiple-rules` to verify `polint explain plan --format json` reports both realistic local rules, supported capabilities, and an empty setup-check list.
3. Run focused tests plus formatting and record the result.

## Verification

- `cargo test -p polint --test cli checked_in_multiple_rules_example_explain_plan_reports_real_capabilities --locked`
- `cargo test -p polint --test cli explain_plan --locked`
- `cargo fmt --all -- --check`
