---
phase: 37-refined-call-graph-providers
plan: 04
title: Go Receiver And Type-Aware Refinements
type: execute
wave: 3
depends_on: [37-02]
files_modified:
  - crates/polint/src/analysis/refined_calls/go.rs
  - crates/polint/src/analysis/refined_calls/provider.rs
  - crates/polint/src/analysis/refined_calls/mod.rs
autonomous: true
requirements: [SAE-PREC-02]
must_haves:
  truths:
    - "D-08 is covered: Go refinements use existing functions, symbols/references, receiver, type/value, direct calls, entrypoints, summaries, and lifecycle/setup states."
    - "D-10 is covered: interface/dynamic dispatch remains explicit when setup or budget cannot support a refined answer."
    - "SAE-PREC-02 is covered for receiver types and bounded points-to constraints on Go inputs."
  artifacts:
    - path: "crates/polint/src/analysis/refined_calls/go.rs"
      provides: "Go receiver/type-aware refined call candidates"
---

<objective>
Add Go-specific refined call candidates using receiver places, type facts, value facts, and bounded points-to data while preserving setup-missing and unresolved interface dispatch states.
</objective>

<execution_context>
@.planning/phases/37-refined-call-graph-providers/37-CONTEXT.md
@.planning/phases/37-refined-call-graph-providers/37-PATTERNS.md
@.agents/skills/rust-best-practices/SKILL.md
@AGENTS.md
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add Go receiver/type candidate filtering</name>
  <files>crates/polint/src/analysis/refined_calls/go.rs, crates/polint/src/analysis/refined_calls/mod.rs</files>
  <read_first>
    - crates/polint/src/analysis/calls/facts.rs
    - crates/polint/src/analysis/types/facts.rs
    - crates/polint/src/analysis/values/facts.rs
    - crates/polint/src/analysis/places.rs
    - crates/polint/src/analysis/refined_calls/facts.rs
  </read_first>
  <action>Create a Go refinement function that inspects Go call sites with receivers or interface-dispatch unresolved reasons, uses available type facts and receiver/value facts to emit type-filtered refined edges, and leaves calls unresolved when type facts are unknown, unsupported, setup-missing, or ambiguous. Use `GoCha`, `GoRta`, `GoVta`, `TypeHierarchy`, or a narrower equivalent algorithm label only when the implemented evidence matches the label.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::refined_calls::go --locked</automated>
  </verify>
  <acceptance_criteria>
    - A Go receiver with one concrete type fact can create a resolved or ambiguous refined edge to a matching method target.
    - A Go interface call with setup-missing type facts remains setup-missing or unresolved.
    - The algorithm label is not broader than the evidence used.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Consume bounded points-to sets for Go</name>
  <files>crates/polint/src/analysis/refined_calls/go.rs</files>
  <read_first>
    - crates/polint/src/analysis/points_to/facts.rs
    - crates/polint/src/analysis/points_to/vars.rs
    - crates/polint/src/analysis/aliases/facts.rs
    - crates/polint/src/analysis/refined_calls/go.rs
  </read_first>
  <action>When receiver or function-value points-to sets exist and `PointsToBudgetStatus::WithinBudget`, allow them to narrow candidate targets with `points_to_assisted` tier evidence. If the points-to set is absent, `NotRun`, `BudgetExceeded`, unsupported, or unknown, emit an explicit unknown/budgeted row where appropriate and do not invent exact edges.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::refined_calls::go --locked</automated>
  </verify>
  <acceptance_criteria>
    - Within-budget points-to input can produce a points-to-assisted refined edge.
    - Budget-exceeded points-to input produces a budget-exceeded or unknown row, not an exact edge.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Wire Go refinement into provider</name>
  <files>crates/polint/src/analysis/refined_calls/provider.rs</files>
  <read_first>
    - crates/polint/src/analysis/refined_calls/provider.rs
    - crates/polint/src/analysis/refined_calls/go.rs
  </read_first>
  <action>Add Go refinement output to the provider merge path. Include Go lifecycle/config/toolchain input snapshot components in digest parts already created in Plan 37-02. Ensure Go-specific output is normalized together with framework and summary outputs.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::refined_calls --locked</automated>
  </verify>
  <acceptance_criteria>
    - Provider output includes Go refined edges when Go inputs exist.
    - Digest tests prove Go lifecycle input changes affect refined provider identity.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Go lifecycle/setup -> refined edges | Missing module roots, build tags, or test inclusion must not produce placeholder exact edges. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T37-07 | Tampering | Go target candidates | Mitigated | Bind targets to existing function/symbol facts and type evidence. |
| T37-08 | Denial of Service | Go refined algorithms | Mitigated | Keep RTA/VTA-like behavior opt-in/budgeted and report budget status. |
</threat_model>

<verification>
- `cargo fmt --all --check`
- `cargo test -p polint --lib analysis::refined_calls::go --locked`
- `cargo test -p polint --lib analysis::refined_calls::provider --locked`
</verification>

<success_criteria>
- Go receiver/type refinements produce evidence-backed refined edges.
- Go setup gaps and budget exhaustion remain explicit.
- Direct call behavior remains unchanged.
</success_criteria>
