---
phase: 52-refined-calls-rework-unknown-taxonomy-consolidation
plan: 04
type: execute
wave: 2
depends_on: [52-01, 52-02, 52-03]
files_modified:
  - crates/polint/src/cli/mod.rs
  - crates/polint/tests/cli.rs
  - docs/schemas/polint-unknowns-v1.json
  - docs/CONSUMER-SETUP.md
  - crates/polint/src/cli/skill.rs
  - docs/API-VISIBILITY-PLAN.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [GRAPH-05, TAX-01]

must_haves:
  truths:
    - "D-19/D-20/D-21/D-22/D-23: `polint inspect unknowns --format json` is canonical; existing `polint unknowns --cap ... --format json` remains compatible; JSON schema stays versioned and stable."
    - "D-27/D-30/D-31: public unknown output is deterministic, schema-backed, CLI-tested, and verified with focused and full regression gates."
    - "Roadmap SC1-SC4: refined-call projection, taxonomy consolidation, public inspect command, and removal/demotion of v1.2 heuristic refiners are all closed before final phase status."
  artifacts:
    - path: "crates/polint/src/cli/mod.rs"
      provides: "canonical inspect unknowns command and compatibility alias"
      contains: "InspectCommand::Unknowns"
    - path: "docs/schemas/polint-unknowns-v1.json"
      provides: "stable public unknowns JSON schema"
      contains: "category"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 52 plan list and closeout bookkeeping"
      contains: "52-04"
---

<objective>
Add the public `polint inspect unknowns --format json` command, preserve current unknowns command compatibility, update schema/docs/tests, run final verification, and mark Phase 52 planned/closed in roadmap artifacts.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/52-refined-calls-rework-unknown-taxonomy-consolidation/52-CONTEXT.md
@.planning/phases/52-refined-calls-rework-unknown-taxonomy-consolidation/52-01-PLAN.md
@.planning/phases/52-refined-calls-rework-unknown-taxonomy-consolidation/52-02-PLAN.md
@.planning/phases/52-refined-calls-rework-unknown-taxonomy-consolidation/52-03-PLAN.md
@crates/polint/src/cli/mod.rs
@crates/polint/tests/cli.rs
@docs/schemas/polint-unknowns-v1.json
@docs/API-VISIBILITY-PLAN.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `polint inspect unknowns --format json`</name>
  <files>crates/polint/src/cli/mod.rs, crates/polint/tests/cli.rs</files>
  <read_first>
    - crates/polint/src/cli/mod.rs
    - crates/polint/tests/cli.rs
    - crates/polint/src/analysis/unknown_taxonomy/{mod.rs,facts.rs,collect.rs}
  </read_first>
  <action>
    Add `Unknowns` to `InspectCommand` with `--format json`, optional `--cap`, optional paths, and `--no-cache` arguments as needed. Route it through the shared unknown-taxonomy renderer. Default no-`--cap` behavior should return the consolidated workspace queue; `--cap` should filter or delegate to the compatibility capability path.
  </action>
  <verify>
    <automated>cargo test -p polint --test cli inspect_unknowns 2>&1 | tail -60</automated>
  </verify>
  <acceptance_criteria>
    - `polint inspect unknowns --format json` exits 0 and prints valid JSON.
    - JSON contains `version`, `schema`, `tool`, and deterministic `rows`.
    - No-cap output represents the consolidated queue, not only one public fact view.
    - `--cap references` or equivalent cap filtering remains deterministic.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Preserve top-level `polint unknowns` compatibility</name>
  <files>crates/polint/src/cli/mod.rs, crates/polint/tests/cli.rs</files>
  <read_first>
    - crates/polint/src/cli/mod.rs
    - crates/polint/tests/cli.rs
    - docs/CONSUMER-SETUP.md
    - crates/polint/src/cli/skill.rs
  </read_first>
  <action>
    Keep `Command::Unknowns` and route it through the same renderer used by `inspect unknowns`. Preserve existing `polint unknowns --cap resolved_imports|symbols|references|dataflow --format json` behavior and exit codes unless a test documents a deliberate schema-compatible extension.
  </action>
  <verify>
    <automated>cargo test -p polint --test cli unknowns_json_reports_public_setup_and_resolution_gaps 2>&1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - Existing unknowns CLI tests pass.
    - Reserved unsupported capability still exits with the existing nonzero code and unsupported row shape.
    - Docs snippets that still use `polint unknowns --cap references --format json` remain true.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update schema, docs, and generated skill text</name>
  <files>docs/schemas/polint-unknowns-v1.json, docs/CONSUMER-SETUP.md, docs/API-VISIBILITY-PLAN.md, crates/polint/src/cli/skill.rs</files>
  <read_first>
    - docs/schemas/polint-unknowns-v1.json
    - docs/CONSUMER-SETUP.md
    - docs/API-VISIBILITY-PLAN.md
    - crates/polint/src/cli/skill.rs
    - .planning/ROADMAP.md
  </read_first>
  <action>
    Update the unknowns schema only as needed for consolidated taxonomy rows, preferring optional fields over breaking changes. Update consumer docs and generated skill text to prefer `polint inspect unknowns --format json` for consolidated inspection while noting `polint unknowns --cap ...` remains supported for cap-filtered compatibility.
  </action>
  <verify>
    <automated>cargo test -p polint --test cli docs 2>&1 | tail -60</automated>
  </verify>
  <acceptance_criteria>
    - Schema validates rows with category/provider/family/source fields if those fields are added.
    - Docs no longer imply the old top-level command is the only unknown inspection path.
    - Docs do not advertise public solver, semantic graph, or refined-call APIs.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Final verification and roadmap closeout</name>
  <files>.planning/ROADMAP.md, crates/polint/tests/public_surface_leak.rs</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/phases/52-refined-calls-rework-unknown-taxonomy-consolidation/52-CONTEXT.md
    - crates/polint/tests/public_surface_leak.rs
  </read_first>
  <action>
    Update `.planning/ROADMAP.md` Phase 52 `**Plans:**` from `TBD` to the four plan list. Run focused tests for refined calls, unknown taxonomy, CLI, data-flow/evidence compatibility, public surface leak, formatting, diff whitespace, full `cargo test -p polint`, and clippy. Record any intentionally changed fixture snapshots as provenance-improving or unknown-taxonomy-consolidating changes, not recall flooding.
  </action>
  <verify>
    <automated>cargo fmt --all -- --check && git diff --check && cargo test -p polint --lib refined_calls unknown_taxonomy data_flow evidence && cargo test -p polint --test cli && cargo test -p polint --test public_surface_leak && cargo test -p polint && cargo clippy -p polint --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` lists 52-01 through 52-04.
    - Focused refined-call, taxonomy, data-flow, evidence, CLI, and public leak tests pass.
    - `cargo fmt --all -- --check` passes.
    - `git diff --check` passes.
    - `cargo test -p polint` passes.
    - `cargo clippy -p polint --all-targets -- -D warnings` passes.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Risk | Mitigation |
|----------|------|------------|
| Public CLI schema | Consolidated unknown rows could break existing consumers. | Preserve existing fields, add optional fields, keep compatibility alias tests. |
| Public docs | Docs could imply solver/refined-call API promotion. | Explicitly document only unknown inspection, not graph/query APIs. |
| Full verification | Focused tests could miss public-output regressions. | Run full test, clippy, format, diff check, CLI, and public leak gates. |

## STRIDE Notes

- Spoofing: public rows include provider/family/source fields where available.
- Tampering: schema and CLI tests pin stable output shape.
- Information disclosure: no absolute temp paths or internal solver schemas are exposed.
</threat_model>

<verification>
- `cargo test -p polint --test cli`
- `cargo test -p polint --test public_surface_leak`
- `cargo test -p polint --lib refined_calls unknown_taxonomy data_flow evidence`
- `cargo test -p polint`
- `cargo clippy -p polint --all-targets -- -D warnings`
- `cargo fmt --all -- --check`
- `git diff --check`
</verification>

<success_criteria>
- `polint inspect unknowns --format json` is implemented and schema-backed.
- Existing `polint unknowns --cap ... --format json` remains compatible.
- Docs and generated skill text align with the new canonical command.
- Phase 52 roadmap plan list is updated.
- Full verification gates pass.
</success_criteria>
