---
phase: 52-refined-calls-rework-unknown-taxonomy-consolidation
plan: 02
type: execute
wave: 2
depends_on: [52-01]
files_modified:
  - crates/polint/src/analysis/data_flow/direct_calls.rs
  - crates/polint/src/analysis/data_flow/{provider.rs,facts.rs,store.rs,validate.rs}
  - crates/polint/src/analysis/evidence/{provider.rs,facts.rs,render.rs,store.rs,validate.rs}
  - crates/polint/src/eval/{go_rta.rs,ts_tokens.rs,ts_object_model.rs,adaptation.rs,determinism_gate.rs}
  - tests/eval-fixtures/refined-calls/solver-projection/
  - tests/eval-fixtures/polyglot-canary/go-ts/
  - crates/polint/tests/public_surface_leak.rs
autonomous: true
requirements: [GRAPH-05]

must_haves:
  truths:
    - "D-09/D-10: data-flow and evidence remain insulated from solver internals and consume refined calls through `db.refined_call_edges()`."
    - "D-11/D-12/D-13: fixtures cover Go RTA, TS token, TS object-model, and adaptation-derived solver edges; public no-leak remains green."
    - "D-28/D-29: mapping and integration fixtures prove downstream compatibility with v1.2 behavior or explicitly documented improvements."
  artifacts:
    - path: "tests/eval-fixtures/refined-calls/solver-projection/"
      provides: "solver-projected refined-call fixture coverage"
      contains: "expected.polint-eval.toml"
    - path: "crates/polint/src/analysis/data_flow/direct_calls.rs"
      provides: "downstream compatibility consumer"
      contains: "derive_direct_call_edges"
    - path: "crates/polint/tests/public_surface_leak.rs"
      provides: "public boundary proof"
      contains: "ALLOWED_PRELUDE"
---

<objective>
Prove that solver-projected refined calls preserve downstream data-flow/evidence behavior, cover all Phase 48-51 solver edge families, and do not leak refined-call or solver internals into public surfaces.
</objective>

<context>
@.planning/phases/52-refined-calls-rework-unknown-taxonomy-consolidation/52-CONTEXT.md
@.planning/phases/52-refined-calls-rework-unknown-taxonomy-consolidation/52-01-PLAN.md
@crates/polint/src/analysis/data_flow/direct_calls.rs
@crates/polint/src/analysis/evidence/provider.rs
@crates/polint/src/eval/{go_rta.rs,ts_tokens.rs,ts_object_model.rs,adaptation.rs,determinism_gate.rs}
@tests/eval-fixtures/polyglot-canary/go-ts/
@crates/polint/tests/public_surface_leak.rs
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add solver-projection refined-call fixture</name>
  <files>tests/eval-fixtures/refined-calls/solver-projection/, crates/polint/src/eval/{go_rta.rs,ts_tokens.rs,ts_object_model.rs,adaptation.rs}</files>
  <read_first>
    - tests/eval-fixtures/go-rta/interface-dispatch/expected.polint-eval.toml
    - tests/eval-fixtures/ts-tokens/alias-parameter-return/expected.polint-eval.toml
    - tests/eval-fixtures/ts-object-model/object-literal/expected.polint-eval.toml
    - tests/eval-fixtures/adaptation-model/accepted-model/expected.polint-eval.toml
    - crates/polint/src/eval/model.rs
  </read_first>
  <action>
    Create a native refined-call solver-projection fixture or extend existing eval gates to assert that one Go RTA edge, one TS token edge, one TS object-model edge with object model enabled, and one accepted adaptation `ModelEdge` all appear as refined-call rows with solver provenance.
  </action>
  <verify>
    <automated>cargo test -p polint eval::refined_calls 2>&1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - Fixture asserts at least four projected refined-call rows, one from each Phase 48-51 source family.
    - Every projected row carries solver/provenance evidence and a non-empty `input_stable_keys` list.
    - No fixture expectation relies on public SDK access to refined-call internals.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Prove data-flow direct-call compatibility</name>
  <files>crates/polint/src/analysis/data_flow/direct_calls.rs, crates/polint/src/analysis/data_flow/{provider.rs,validate.rs}, tests/eval-fixtures/refined-calls/solver-projection/</files>
  <read_first>
    - crates/polint/src/analysis/data_flow/direct_calls.rs
    - crates/polint/src/analysis/data_flow/provider.rs
    - crates/polint/src/analysis/refined_calls/facts.rs
    - crates/polint/src/analysis/solver/facts.rs
  </read_first>
  <action>
    Add assertions proving `derive_direct_call_edges` consumes solver-projected refined calls through `db.refined_call_edges()` and produces the same call boundary node/edge shapes as current direct/refined rows. Avoid adding direct solver-store reads to data-flow code.
  </action>
  <verify>
    <automated>cargo test -p polint --lib data_flow::direct_calls eval::refined_calls 2>&1 | tail -60</automated>
  </verify>
  <acceptance_criteria>
    - Data-flow tests pass with solver-projected refined calls.
    - `analysis::data_flow::direct_calls.rs` does not import `analysis::solver`.
    - Resolved projected calls produce `CallArgumentToParameter` and `CallReturnToUse` paths when summaries/callsite data are available.
    - Non-resolved projected calls produce unresolved/budget/unsupported data-flow evidence rather than disappearing.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Prove evidence compatibility and deterministic reporting</name>
  <files>crates/polint/src/analysis/evidence/{provider.rs,facts.rs,render.rs,validate.rs}, tests/eval-fixtures/refined-calls/solver-projection/</files>
  <read_first>
    - crates/polint/src/analysis/evidence/provider.rs
    - crates/polint/src/analysis/evidence/render.rs
    - crates/polint/src/analysis/data_flow/facts.rs
    - crates/polint/src/diagnostics/mod.rs
  </read_first>
  <action>
    Add fixture/test coverage showing evidence rendering continues to derive from data-flow/evidence rows and can include refined-call provenance through existing source fact stable keys without exposing raw solver graph internals.
  </action>
  <verify>
    <automated>cargo test -p polint --lib evidence eval::refined_calls 2>&1 | tail -60</automated>
  </verify>
  <acceptance_criteria>
    - Evidence fixture output remains deterministic.
    - Evidence rows reference data-flow/refined-call source keys, not public solver internals.
    - Existing evidence validation still passes.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Run public no-leak and v1.2 compatibility sweep</name>
  <files>crates/polint/tests/public_surface_leak.rs, crates/polint/tests/cli.rs, tests/eval-fixtures/refined-calls/solver-projection/</files>
  <read_first>
    - crates/polint/tests/public_surface_leak.rs
    - crates/polint/tests/cli.rs
    - docs/API-VISIBILITY-PLAN.md
    - docs/CONSUMER-SETUP.md
  </read_first>
  <action>
    Run and update focused tests so public surfaces do not expose `RefinedCallEdgeFact`, solver types, semantic graph internals, or unknown-taxonomy internals. Document any intentional fixture deltas caused by stricter solver provenance rather than broad heuristic recall.
  </action>
  <verify>
    <automated>cargo test -p polint --test public_surface_leak 2>&1 | tail -40 && cargo test -p polint --test cli 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `public_surface_leak` passes without adding refined-call or solver internals to `ALLOWED_PRELUDE`.
    - CLI public JSON tests still pass.
    - Any changed v1.2 refined-call fixture expectation has an explanation in test names/comments or snapshot notes.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Risk | Mitigation |
|----------|------|------------|
| Downstream analysis | Data-flow/evidence could couple to solver internals. | Keep refined calls as the only compatibility boundary and test imports/behavior. |
| Fixture scoring | New solver rows could inflate recall by flooding. | Assert representative precise rows and document intentional deltas. |
| Public output | Internal refined-call markers could leak through JSON/docs. | Run public no-leak and CLI tests unchanged. |

## STRIDE Notes

- Tampering: deterministic fixture assertions catch changed source fact keys and edge families.
- Repudiation: evidence rows retain source stable keys for projected paths.
- Information disclosure: public tests block internal type/schema leaks.
</threat_model>

<verification>
- `cargo test -p polint eval::refined_calls`
- `cargo test -p polint --lib data_flow::direct_calls evidence`
- `cargo test -p polint --test public_surface_leak`
- `cargo test -p polint --test cli`
</verification>

<success_criteria>
- Solver-projected refined calls are consumed by data-flow and evidence.
- Go RTA, TS token, TS object-model, and adaptation model edge families are represented.
- Public no-leak and CLI compatibility tests pass.
</success_criteria>
