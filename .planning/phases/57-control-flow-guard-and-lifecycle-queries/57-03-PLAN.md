# Phase 57-03 Plan: Capability, Docs, and External Rule Proof

## Goal

Promote `control_flow` to a supported capability for Phase 57 while keeping
public docs, generated skill text, tests, and roadmap requirements honest about
the supported scope.

## Tasks

1. Mark `control_flow` supported in `analysis_plan.rs`.
2. Add `control_flow` to the semantic pipeline trigger set.
3. Update CLI capability regressions so `dataflow` remains fail-closed while
   `control_flow` runs.
4. Add an external temp-repo CLI test where a repo-local rule imports only
   `polint::sdk::prelude::*`, requests `ControlFlow<'_>`, and reports guard plus
   cleanup diagnostics.
5. Update `docs/facts/control-flow.md`, `.agents/skills/polint/SKILL.md`,
   `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, and `.planning/STATE.md`
   to record the same-function call-event boundary.

## Verification

- `cargo test -p polint --test cli phase57_control_flow_rule_reports_json --locked`
- `cargo test -p polint --test cli phase55_preview_rule_syntax_compiles_and_fails_closed --locked`
- `cargo test -p polint --lib policy_capabilities_report_phase_support_boundaries --locked`
- `cargo test -p polint --lib --locked`
- `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings`
- `cargo fmt --all --check`
- `cargo doc -p polint --no-deps --locked`
- `git diff --check`
