# Phase 57-02 Plan: Lifecycle Query Semantics

## Goal

Back `ControlFlow::missing_cleanup(LifecycleQuery)` for same-function call-event
lifecycle policies with conservative, deterministic diagnostics.

## Tasks

1. Reuse the ordered call-event projection from Plan 57-01.
2. Implement `missing_cleanup` for call start plus call cleanup patterns.
3. Treat cleanup evidence as same-function ordered proof only; document that
   error-exit and resource-identity proof are not exact in this phase.
4. Add focused unit tests for missing cleanup, satisfied cleanup, zero-budget
   behavior, and evidence labels.

## Verification

- `cargo test -p polint --lib control_flow_missing_cleanup --locked`

