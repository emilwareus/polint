---
phase: 09-plugin-skeleton
plan: "03"
type: execute
wave: 3
depends_on:
  - "09-01"
  - "09-02"
files_modified:
  - README.md
  - crates/polint-plugin/src/lib.rs
  - .planning/phases/09-plugin-skeleton/09-03-SUMMARY.md
requirements:
  - PLUG-01
  - PLUG-02
requirements_addressed:
  - PLUG-01
  - PLUG-02
autonomous: true
must_haves:
  truths:
    - "Docs clearly mark Wasm repo-local rules as experimental."
    - "Docs describe the stable-ID host API and state that plugins do not receive full AST/source payloads."
    - "Full workspace verification passes after plugin skeleton changes."
  artifacts:
    - path: "README.md"
      provides: "user-facing experimental plugin documentation"
    - path: "crates/polint-plugin/src/lib.rs"
      provides: "crate-level plugin skeleton docs"
    - path: ".planning/phases/09-plugin-skeleton/09-03-SUMMARY.md"
      provides: "final Phase 9 evidence"
  key_links:
    - from: ".planning/phases/09-plugin-skeleton/09-CONTEXT.md"
      to: "README.md"
      via: "experimental docs and out-of-scope runtime claims"
      pattern: "experimental"
---

<objective>
Document the experimental Wasm plugin skeleton honestly and close Phase 9 with targeted plus full workspace verification.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-plugin-skeleton/09-CONTEXT.md
@README.md
@crates/polint-plugin/src/lib.rs
@crates/polint-plugin/src/rule.wit
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add honest plugin skeleton docs</name>
  <files>README.md, crates/polint-plugin/src/lib.rs</files>
  <read_first>
    - README.md
    - crates/polint-plugin/src/lib.rs
    - .planning/phases/09-plugin-skeleton/09-CONTEXT.md
  </read_first>
  <action>Add documentation that states:
  - Wasm repo-local rules are experimental.
  - `polint-plugin` currently provides WIT files, manifest validation, and optional Wasmtime component-byte validation.
  - `polint check` does not automatically compile, cache, or execute repo-local Wasm rules in v1.
  - Future plugins should query host-owned facts through stable IDs such as file IDs, function IDs, and branch IDs.
  - Plugins should not receive full AST JSON, full source text, or large graph payloads.
  Put user-facing docs in `README.md` under a concise heading such as `Experimental Wasm plugins`. Put crate-level docs at the top of `crates/polint-plugin/src/lib.rs` using `//!` comments.</action>
  <verify>
    <automated>rg -n "Experimental Wasm plugins|experimental|stable IDs|full AST|polint check" README.md crates/polint-plugin/src/lib.rs</automated>
  </verify>
  <acceptance_criteria>
    - README contains the phrase `Experimental Wasm plugins`.
    - Docs contain the phrase `stable IDs`.
    - Docs state that `polint check` does not automatically compile, cache, or execute Wasm rules in v1.
    - Docs state that plugins should not receive full AST/source payloads.
  </acceptance_criteria>
  <done>Plugin skeleton is documented honestly for users and maintainers.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run Phase 9 verification matrix</name>
  <files>crates/polint-plugin/src/lib.rs, crates/polint-plugin/src/rule.wit, README.md</files>
  <read_first>
    - crates/polint-plugin/src/lib.rs
    - crates/polint-plugin/src/rule.wit
    - README.md
  </read_first>
  <action>Run targeted and full verification after all plugin skeleton changes. Fix only failures directly caused by Phase 9 changes.</action>
  <verify>
    <automated>cargo test -p polint-plugin --lib</automated>
    <automated>cargo test -p polint-plugin --features wasmtime-host --lib invalid_component_bytes_are_rejected</automated>
    <automated>cargo fmt -- --check</automated>
    <automated>cargo clippy --workspace --all-targets -- -D warnings</automated>
    <automated>cargo test --workspace</automated>
  </verify>
  <acceptance_criteria>
    - Targeted plugin tests pass.
    - Feature-specific Wasmtime validation test passes.
    - `cargo fmt -- --check` passes.
    - `cargo clippy --workspace --all-targets -- -D warnings` passes.
    - `cargo test --workspace` passes.
  </acceptance_criteria>
  <done>Phase 9 code and docs pass full workspace verification.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Record final Phase 9 evidence</name>
  <files>.planning/phases/09-plugin-skeleton/09-03-SUMMARY.md</files>
  <read_first>
    - .planning/phases/09-plugin-skeleton/09-CONTEXT.md
    - .planning/phases/09-plugin-skeleton/09-01-SUMMARY.md
    - .planning/phases/09-plugin-skeleton/09-02-SUMMARY.md
  </read_first>
  <action>Create `09-03-SUMMARY.md` using the GSD summary template. Include evidence for all Phase 9 success criteria: WIT rule interface, Wasmtime host-loading skeleton, structured path/error validation, experimental docs, stable-ID host API docs, and full verification. Explicitly state that automatic repo-local Wasm compilation, plugin artifact caching, and `polint check` plugin execution remain out of scope.</action>
  <verify>
    <automated>rg -n "WIT|Wasmtime|structured|experimental|stable-ID|out of scope|cargo test --workspace" .planning/phases/09-plugin-skeleton/09-03-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - Summary lists pass status for targeted plugin tests, feature test, fmt, clippy, and workspace tests.
    - Summary contains `experimental` and does not claim production plugin execution.
    - Summary lists out-of-scope automatic compilation, artifact caching, and `polint check` plugin execution.
  </acceptance_criteria>
  <done>Phase 9 has final evidence ready for phase-level review and security closeout.</done>
</task>

</tasks>

<threat_model>
ASVS L1 posture: no high severity threat is accepted; documentation must not overclaim plugin runtime support.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs -> user expectations | Docs define what users believe plugins can do in v1. |
| plugin crate -> future runtime | Crate-level docs guide future execution work. |
| verification -> release confidence | Phase summary records what actually passed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Spoofing | user docs | mitigate | Docs explicitly say Wasm plugins are experimental and not run by `polint check` in v1. |
| T-09-03-02 | Information Disclosure | plugin API docs | mitigate | Docs explain stable-ID host queries and no full AST/source payload transfer. |
| T-09-03-03 | Repudiation | phase evidence | mitigate | Summary records exact commands and pass status. |
| T-09-03-04 | Tampering | future expectations | mitigate | Summary and docs list out-of-scope automatic compilation, caching, and execution. |
</threat_model>

<verification>
Run after completing all tasks in this plan:

```bash
cargo test -p polint-plugin --lib
cargo test -p polint-plugin --features wasmtime-host --lib invalid_component_bytes_are_rejected
cargo fmt -- --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
```
</verification>

<success_criteria>
- Plugin docs are honest and explicitly experimental.
- Stable-ID host API direction is documented.
- Full workspace verification passes.
</success_criteria>

<output>
After completion, create `.planning/phases/09-plugin-skeleton/09-03-SUMMARY.md`.
</output>
