---
phase: 35-framework-entrypoints-and-trust-boundaries
plan: 08
type: execute
wave: 5
depends_on: [35-07]
files_modified:
  - crates/polint/src/analysis_kernel/mod.rs
autonomous: true
requirements: [SAE-INT-05]

must_haves:
  truths:
    - "Normal polint check JSON does not contain private framework fact internals, provider IDs, or debug terms"
    - "SDK exports do not expose entrypoints, trust boundaries, dispatch edges, or unresolved framework types"
    - "CLI help text does not advertise framework analysis"
    - "README and docs do not document private framework internals"
  artifacts:
    - path: "crates/polint/src/analysis_kernel/mod.rs"
      provides: "Public no-leak test assertions for 21+ internal markers"
  key_links:
    - from: "crates/polint/src/analysis_kernel/mod.rs"
      to: "crates/polint/src/analysis/entrypoints/mod.rs"
      via: "no-leak assertion: internal markers must not appear in public surfaces"
      pattern: "polint\\.entrypoints.*assert"
---

<objective>
Prove that private framework fact internals do not leak into public CLI JSON, SDK exports, help text, README, or docs per D-26 and D-27. Keep the Entrypoints SDK view deferred to Phase 41.

Purpose: Public API discipline requires that new internals stay private unless deliberately promoted. This plan proves the boundary with source-surface assertions.
Output: No-leak test covering 21+ internal markers across all public surfaces.
</objective>

<execution_context>
@.planning/phases/35-framework-entrypoints-and-trust-boundaries/35-CONTEXT.md
@.planning/phases/35-framework-entrypoints-and-trust-boundaries/35-PATTERNS.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/35-framework-entrypoints-and-trust-boundaries/35-07-SUMMARY.md

<interfaces>
From crates/polint/src/analysis_kernel/provider.rs (no-leak pattern):
- Existing no-leak tests scan: public check JSON output, CLI help text, SDK prelude exports, runner exports, README, docs
- Test asserts absence of internal terms like provider IDs, type names, debug function names
- Pattern: for term in ["internal_term_1", "internal_term_2"] { assert!(!public_output.contains(term)); }

From previous phase no-leak tests (Phase 30-08, 31-05, 32-07):
- Source-surface assertions grep crate-root, sdk, runner, README, docs for internal marker strings
- The test is a #[test] function in analysis_kernel/mod.rs or a dedicated no-leak test file
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add public no-leak boundary proof for framework internals</name>
  <files>crates/polint/src/analysis_kernel/mod.rs</files>
  <read_first>
    - crates/polint/src/analysis_kernel/mod.rs
    - crates/polint/src/analysis_kernel/provider.rs
    - crates/polint/src/analysis/entrypoints/mod.rs
    - crates/polint/src/analysis/entrypoints/facts.rs
  </read_first>
  <action>
    Add a #[test] function in analysis_kernel/mod.rs (or extend the existing no-leak test pattern) that proves Phase 35 framework internals do not leak into public surfaces per D-26 and D-27.

    The test should scan these public surface files for absence of internal markers:
    1. SDK prelude (crates/polint/src/sdk/prelude.rs or equivalent)
    2. SDK module (crates/polint/src/sdk/mod.rs)
    3. Runner module (crates/polint/src/runner.rs or equivalent)
    4. Crate root (crates/polint/src/lib.rs)
    5. README.md
    6. docs/ directory files (docs/facts/*.md, docs/API-VISIBILITY-PLAN.md)
    7. CLI help generation (if applicable)

    Internal markers to check (21+ markers per D-26):
    Provider IDs: "polint.entrypoints"
    Type names: "EntrypointFact", "TrustBoundaryFact", "FrameworkDispatchEdgeFact", "UnresolvedFrameworkFact"
    Enum names: "EntrypointKind", "TrustBoundarySourceKind", "DispatchEdgeKind", "UnresolvedFrameworkReason"
    Precision names: "EntrypointPrecision", "EntrypointProvenance", "EntrypointConfidence", "EntrypointStatus"
    Module names: "recognizers_go", "recognizers_ts", "trust_boundaries", "dispatch"
    Function names: "derive_entrypoints_with_cache_stats", "extract_entrypoints", "recognize_go_entrypoints", "recognize_ts_entrypoints"
    Debug function: "entrypoints_debug", "metadata_debug_json_for_test" (in non-test context)
    Store types: "EntrypointStore", "EntrypointOutput"

    For each marker, assert it does NOT appear in any of the public surface files. Use the same file-reading and string-searching pattern as the existing Phase 30/31/32 no-leak tests.

    If the existing no-leak test is a single comprehensive function, extend it with the new markers. If it is per-phase, add a new framework_entrypoints_internals_do_not_leak_into_public_surfaces test.

    Also verify that normal polint check JSON output (from an integration test or fixture run) does not contain any of these markers in the JSON output string.
  </action>
  <verify>
    <automated>cd /Users/emilwareus/conductor/workspaces/exlint/sydney && cargo test -p polint --lib -- no_leak 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Test function exists asserting 21+ internal framework markers are absent from public surfaces
    - Scanned surfaces include: SDK prelude, SDK module, runner, crate root, README, docs
    - Markers include provider ID "polint.entrypoints", all fact type names, all enum names, all function names
    - Normal polint check JSON output does not contain framework internal terms
    - Entrypoints SDK view remains deferred (no Entrypoints<'_> in SDK)
    - cargo test -p polint --lib -- no_leak passes
  </acceptance_criteria>
  <done>Public no-leak proof confirms framework internals are crate-private with no public surface exposure</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internal -> public | Framework fact internals must not cross into public API surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-12 | Information Disclosure | public surface leak | mitigate | No-leak test scans all public files for 21+ internal markers and fails if any are found |
| T-35-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies added in this plan |
</threat_model>

<verification>
cargo test -p polint --lib -- no_leak passes
cargo test -p polint passes (full test suite)
cargo check -p polint succeeds
</verification>

<success_criteria>
21+ internal framework markers confirmed absent from all public surfaces. Entrypoints SDK view remains deferred to Phase 41.
</success_criteria>

<output>
Create `.planning/phases/35-framework-entrypoints-and-trust-boundaries/35-08-SUMMARY.md` when done
</output>
