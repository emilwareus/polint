---
phase: 35-framework-entrypoints-and-trust-boundaries
plan: 02
type: execute
wave: 2
depends_on: [35-01]
files_modified:
  - crates/polint/src/analysis/entrypoints/provider.rs
  - crates/polint/src/analysis/entrypoints/cache_key.rs
  - crates/polint/src/analysis/entrypoints/mod.rs
  - crates/polint/src/analysis_kernel/provider.rs
  - crates/polint/src/analysis_kernel/mod.rs
autonomous: true
requirements: [SAE-INT-05]

must_haves:
  truths:
    - "polint.entrypoints provider exists in the kernel run sequence after polint.direct_summaries and before polint.extensions"
    - "Provider produces deterministic output digest from upstream inputs and framework fact stable keys"
    - "Cache identity includes upstream output digests, lifecycle, config, and absent extension/model/toolchain slots"
  artifacts:
    - path: "crates/polint/src/analysis/entrypoints/provider.rs"
      provides: "derive_entrypoints_with_cache_stats function, EntrypointsProviderOutput, output digest computation"
      min_lines: 80
    - path: "crates/polint/src/analysis/entrypoints/cache_key.rs"
      provides: "entrypoints_provider_parameter_digest function"
      min_lines: 15
  key_links:
    - from: "crates/polint/src/analysis_kernel/mod.rs"
      to: "crates/polint/src/analysis/entrypoints/provider.rs"
      via: "derive_entrypoints_with_cache_stats call in kernel run sequence"
      pattern: "derive_entrypoints_with_cache_stats"
    - from: "crates/polint/src/analysis_kernel/provider.rs"
      to: "crates/polint/src/analysis/entrypoints/provider.rs"
      via: "polint.entrypoints manifest entry"
      pattern: "polint\\.entrypoints"
---

<objective>
Wire the polint.entrypoints provider into the kernel: manifest entry, provider function skeleton, cache key, output digest, and kernel run-sequence integration. The provider initially produces empty output; recognizers are added in Plans 03-04.

Purpose: Establish the provider slot so that recognizers, validation, and debug can be added incrementally. Per D-11, D-12, D-13, D-14 the provider runs after polint.calls (actually after polint.direct_summaries/SCC closure) and before polint.extensions.
Output: Working provider skeleton that produces deterministic empty output in the kernel run sequence.
</objective>

<execution_context>
@.planning/phases/35-framework-entrypoints-and-trust-boundaries/35-CONTEXT.md
@.planning/phases/35-framework-entrypoints-and-trust-boundaries/35-PATTERNS.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-framework-entrypoints-and-trust-boundaries/35-01-SUMMARY.md

<interfaces>
From crates/polint/src/analysis/calls/provider.rs:
- CallsProviderOutput { diagnostics: Vec<Diagnostic>, cache_stats: CacheStats, output_digest: Option<Digest> }
- derive_calls_with_cache_stats(db, input_snapshot, manifest, upstream_digests...) -> CallsProviderOutput
- calls_output_digest(db, manifest, input_snapshot, upstream_digests, output) -> Digest
- calls_provider_parameter_digest() -> Digest using Digest::from_parts(DigestKind::ProviderParameters, ...)

From crates/polint/src/analysis_kernel/provider.rs:
- ProviderManifest { id, kind, inputs, outputs, language_scope, cache_policy, schema_versions, precision_ceiling }
- ProviderKind::WholeRepoDerived, LanguageScope::MultiLanguage, CachePolicy::InMemoryDerived, PrecisionCeiling::SetupAware
- Schema constant pattern: const CALLS_SCHEMA: &[SchemaVersion] = &[SchemaVersion { name: "calls-facts-1", version: 1 }]

From crates/polint/src/analysis_kernel/mod.rs:
- Provider execution order: ...polint.calls -> polint.abstract_domains -> polint.direct_summaries -> SCC closure -> polint.extensions -> polint.metrics
- Each provider call: derive_X_with_cache_stats -> extract cache_stats, output_digest, diagnostics -> push provider_output
- Dependency digest pattern: let X_dependency_output_digest = X_output_digest.unwrap_or_else(|| Digest::absent(...))

From crates/polint/src/analysis_kernel/incremental:
- Digest, DigestKind, CacheStats, InputSnapshot
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create provider manifest entry, cache key, and provider function skeleton</name>
  <files>crates/polint/src/analysis/entrypoints/provider.rs, crates/polint/src/analysis/entrypoints/cache_key.rs, crates/polint/src/analysis/entrypoints/mod.rs, crates/polint/src/analysis_kernel/provider.rs</files>
  <read_first>
    - crates/polint/src/analysis/calls/provider.rs
    - crates/polint/src/analysis/calls/cache_key.rs
    - crates/polint/src/analysis_kernel/provider.rs
    - crates/polint/src/analysis/entrypoints/mod.rs
  </read_first>
  <action>
    Create analysis/entrypoints/cache_key.rs with entrypoints_provider_parameter_digest() -> Digest. Use Digest::from_parts(DigestKind::ProviderParameters, "entrypoints_provider_parameters", &[...]) with parts: "entrypoints-facts-1", "entrypoints", "trust_boundaries", "dispatch_edges", "unresolved_framework", "go_net_http", "go_chi", "go_cobra", "go_testing", "ts_express", "ts_mcp_sdk", "ts_commander_yargs", "ts_jest_vitest_mocha". Include a pin test.

    Create analysis/entrypoints/provider.rs with:
    - EntrypointsProviderOutput struct: diagnostics (Vec of Diagnostic), cache_stats (CacheStats), output_digest (Option of Digest). Derive Debug, Clone, Default.
    - pub(crate) const ENTRYPOINTS_PROVIDER_ID: &str = "polint.entrypoints"
    - derive_entrypoints_with_cache_stats function taking: db (&mut AnalysisDb), input_snapshot (&InputSnapshot), manifest (&ProviderManifest), and upstream digest arguments matching the calls provider pattern (semantic_mir_output_digest, cfg_output_digest, calls_output_digest, symbol_output_digest, module_topology_output_digest, syntax_output_digests as Vec of Digest). For now the function constructs an empty EntrypointOutput, normalizes it, computes entrypoints_output_digest, records cache_stats.record_recompute(), calls db.replace_entrypoint_facts(output) (which needs AnalysisDb extension — use a placeholder that stores on db if the method exists, otherwise store locally), and returns EntrypointsProviderOutput.
    - entrypoints_output_digest function following the calls_output_digest pattern: include provider_id, provider_version, primary_schema_label, parameter digest, config digest, upstream digests, lifecycle components (go_lifecycle, ts_js_lifecycle), model/extension/tool components, and per-fact stable payload lines. Sort all parts, use Digest::from_parts(DigestKind::ProviderOutput, "entrypoints_output", &refs).
    - provider_error_diagnostic helper producing Diagnostic::error("polint/internal", ...) with "Entrypoints provider failed: ..." message.

    Add ENTRYPOINTS_SCHEMA constant to analysis_kernel/provider.rs: const ENTRYPOINTS_SCHEMA: &[SchemaVersion] = &[SchemaVersion { name: "entrypoints-facts-1", version: 1 }].

    Insert a new ProviderManifest entry in PROVIDER_MANIFESTS after polint.direct_summaries and before polint.extensions (per D-11). The manifest: id "polint.entrypoints", kind WholeRepoDerived, inputs ["source_files", "functions", "symbols", "references", "semantic_imports", "resolved_imports", "import_to_package_edges", "mir_bodies", "mir_operations", "places", "call_sites", "call_targets", "unresolved_calls"], outputs ["entrypoints", "trust_boundaries", "dispatch_edges", "unresolved_framework"], language_scope MultiLanguage, cache_policy InMemoryDerived, schema_versions ENTRYPOINTS_SCHEMA, precision_ceiling SetupAware (per D-13).

    Update the provider_order_matches_behavior_preserving_kernel_sequence test to include "polint.entrypoints" after "polint.direct_summaries" and before "polint.extensions".

    Update analysis/entrypoints/mod.rs to declare pub(crate) mod cache_key and pub(crate) mod provider.
  </action>
  <verify>
    <automated>cd /Users/emilwareus/conductor/workspaces/exlint/sydney && cargo test -p polint --lib analysis_kernel::provider -- --exact provider_order_matches_behavior_preserving_kernel_sequence 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - analysis_kernel/provider.rs contains ProviderManifest with id "polint.entrypoints" positioned between polint.direct_summaries and polint.extensions
    - Provider order test expects vec containing "polint.entrypoints" after "polint.direct_summaries" and before "polint.extensions"
    - analysis/entrypoints/provider.rs contains pub(crate) fn derive_entrypoints_with_cache_stats
    - analysis/entrypoints/provider.rs contains fn entrypoints_output_digest computing Digest::from_parts(DigestKind::ProviderOutput, "entrypoints_output", ...)
    - analysis/entrypoints/cache_key.rs contains pub(crate) fn entrypoints_provider_parameter_digest returning Digest
    - cargo test -p polint --lib analysis_kernel::provider passes
  </acceptance_criteria>
  <done>polint.entrypoints manifest entry exists in provider order, provider function skeleton compiles, parameter digest and output digest functions exist</done>
</task>

<task type="auto">
  <name>Task 2: Wire entrypoints provider into kernel run sequence</name>
  <files>crates/polint/src/analysis_kernel/mod.rs</files>
  <read_first>
    - crates/polint/src/analysis_kernel/mod.rs
    - crates/polint/src/analysis/entrypoints/provider.rs
  </read_first>
  <action>
    Insert the entrypoints provider call in AnalysisKernel::run after the SCC closure block and before the polint.extensions provider call. Follow the established pattern:

    1. Compute dependency digest: let direct_summaries_dependency_output_digest = _direct_summaries_output_digest.unwrap_or_else(|| Digest::absent(DigestKind::ProviderOutput, "polint.direct_summaries")). Note: the variable is currently named _direct_summaries_output_digest with a leading underscore — remove the underscore so it is used.
    2. Call derive_entrypoints_with_cache_stats with db, input_snapshot, Self::provider_manifest("polint.entrypoints"), and upstream digest arguments: semantic_mir_dependency_output_digest.clone(), cfg_dependency_output_digest.clone(), calls_dependency_output_digest.clone(), symbol_dependency_output_digest.clone(), module_topology_dependency_output_digest.clone(), syntax digests vec.
    3. Extract cache_stats, output_digest, diagnostics. Push provider_output using Self::provider_output_for_with_optional_digest("polint.entrypoints", &db, stats, digest).

    Also add AnalysisDb support: if db does not already have a replace_entrypoint_facts method, add one to the AnalysisDb or to the store module (following the replace_call_facts pattern). The method should store EntrypointOutput into the db's internal state. If AnalysisDb uses a pattern of Vec fields for each fact family, add entrypoint_facts, trust_boundary_facts, dispatch_edge_facts, unresolved_framework_facts fields. Add accessor methods: entrypoint_facts() -> &[EntrypointFact], trust_boundary_facts() -> &[TrustBoundaryFact], dispatch_edge_facts() -> &[FrameworkDispatchEdgeFact], unresolved_framework_facts() -> &[UnresolvedFrameworkFact].

    Update any kernel integration tests that assert on provider output counts, provider_output vec length, or provider ID lists to include "polint.entrypoints".
  </action>
  <verify>
    <automated>cd /Users/emilwareus/conductor/workspaces/exlint/sydney && cargo test -p polint --lib analysis_kernel -- 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - analysis_kernel/mod.rs calls derive_entrypoints_with_cache_stats after SCC closure and before polint.extensions
    - analysis_kernel/mod.rs pushes "polint.entrypoints" provider output entry
    - AnalysisDb has replace_entrypoint_facts method and accessor methods for all four fact vectors
    - cargo test -p polint --lib analysis_kernel passes
    - cargo check -p polint succeeds
  </acceptance_criteria>
  <done>Entrypoints provider runs in kernel sequence producing empty output; all existing tests pass with the new provider in the pipeline</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider -> db | Provider output enters AnalysisDb through validated store |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-02 | Tampering | entrypoints_output_digest | mitigate | Include provider version, schema, and all upstream digests in output digest to prevent cache poisoning |
| T-35-03 | Information Disclosure | provider output | mitigate | Provider output stays crate-private; no public JSON fields added |
| T-35-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies added in this plan |
</threat_model>

<verification>
cargo test -p polint --lib analysis_kernel passes
cargo test -p polint --lib analysis::entrypoints passes
cargo check -p polint succeeds
</verification>

<success_criteria>
polint.entrypoints provider runs in the kernel sequence after SCC closure and before extensions. Empty output produces deterministic digest. All existing tests pass.
</success_criteria>

<output>
Create `.planning/phases/35-framework-entrypoints-and-trust-boundaries/35-02-SUMMARY.md` when done
</output>
