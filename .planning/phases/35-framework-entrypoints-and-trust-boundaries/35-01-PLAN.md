---
phase: 35-framework-entrypoints-and-trust-boundaries
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/polint/src/analysis/entrypoints/mod.rs
  - crates/polint/src/analysis/entrypoints/facts.rs
  - crates/polint/src/analysis/entrypoints/store.rs
  - crates/polint/src/analysis/ids.rs
  - crates/polint/src/analysis_kernel/metadata.rs
  - crates/polint/src/analysis/mod.rs
autonomous: true
requirements: [SAE-INT-05]

must_haves:
  truths:
    - "Four framework fact types exist with dense IDs, stable keys, vocabulary enums, and serialization"
    - "EntrypointOutput container normalizes by stable key and EntrypointStore builds validated indexes"
    - "FactFamily enum includes Entrypoint, TrustBoundary, DispatchEdge, and UnresolvedFramework variants"
  artifacts:
    - path: "crates/polint/src/analysis/entrypoints/facts.rs"
      provides: "EntrypointFact, TrustBoundaryFact, FrameworkDispatchEdgeFact, UnresolvedFrameworkFact, vocabulary enums"
      min_lines: 150
    - path: "crates/polint/src/analysis/entrypoints/store.rs"
      provides: "EntrypointOutput with normalized(), EntrypointStore with from_output validation and indexes"
      min_lines: 80
    - path: "crates/polint/src/analysis/entrypoints/mod.rs"
      provides: "Module declarations for entrypoints submodules"
    - path: "crates/polint/src/analysis/ids.rs"
      provides: "EntrypointId, TrustBoundaryId, DispatchEdgeId, UnresolvedFrameworkId newtypes"
  key_links:
    - from: "crates/polint/src/analysis/entrypoints/facts.rs"
      to: "crates/polint/src/analysis/ids.rs"
      via: "import EntrypointId, TrustBoundaryId, DispatchEdgeId, UnresolvedFrameworkId"
      pattern: "use crate::analysis::ids"
    - from: "crates/polint/src/analysis/entrypoints/store.rs"
      to: "crates/polint/src/analysis/entrypoints/facts.rs"
      via: "import fact types"
      pattern: "use super::facts"
    - from: "crates/polint/src/analysis/mod.rs"
      to: "crates/polint/src/analysis/entrypoints/mod.rs"
      via: "pub(crate) mod entrypoints"
      pattern: "pub\\(crate\\) mod entrypoints"
---

<objective>
Define the four framework fact families (EntrypointFact, TrustBoundaryFact, FrameworkDispatchEdgeFact, UnresolvedFrameworkFact), their vocabulary enums, dense IDs, normalized output container, validated store with indexes, and FactFamily integration.

Purpose: Establish the type contracts that all downstream plans (recognizers, provider, validation, eval) depend on. Per D-01, D-02, D-03, D-04, D-05 these are the minimum fact families needed for the framework boundary layer.
Output: Compiling `analysis::entrypoints` module with fact types, store, and IDs; extended FactFamily enum.
</objective>

<execution_context>
@.planning/phases/35-framework-entrypoints-and-trust-boundaries/35-CONTEXT.md
@.planning/phases/35-framework-entrypoints-and-trust-boundaries/35-PATTERNS.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From crates/polint/src/analysis/ids.rs:
- Dense ID pattern: `#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash, Serialize, Deserialize)] pub(crate) struct CallSiteId(pub(crate) u64);`
- Test: `assert_small_id_contract::<T>()` verifies Debug+Clone+Copy+PartialEq+Eq+PartialOrd+Ord+Hash+Serialize+DeserializeOwned

From crates/polint/src/analysis_kernel/metadata.rs:
- `pub(crate) enum FactFamily` with variants and `fn label(self) -> &'static str`
- Current last variants before test expectations: ExtensionFact, MirStatement, MirTerminator, UnsupportedSemantic

From crates/polint/src/analysis/calls/facts.rs:
- Fact struct pattern: id, language, domain fields, precision, stable_key
- Enum derive set: `Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash, Serialize, Deserialize`

From crates/polint/src/analysis/calls/store.rs:
- CallOutput with normalized() sorting by (stable_key, id)
- CallStore with from_output() doing normalize + validate referential integrity + build BTreeMap indexes
- AnalysisError::InvalidFact for validation failures

From crates/polint/src/core:
- FileId, FunctionId, Language, ReferenceId, Span, SymbolId
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define dense IDs, fact types, vocabulary enums, and FactFamily variants</name>
  <files>crates/polint/src/analysis/ids.rs, crates/polint/src/analysis/entrypoints/facts.rs, crates/polint/src/analysis_kernel/metadata.rs, crates/polint/src/analysis/entrypoints/mod.rs, crates/polint/src/analysis/mod.rs</files>
  <read_first>
    - crates/polint/src/analysis/ids.rs
    - crates/polint/src/analysis/calls/facts.rs
    - crates/polint/src/analysis_kernel/metadata.rs
    - crates/polint/src/analysis/mod.rs
  </read_first>
  <action>
    Add four dense ID newtypes to analysis/ids.rs following the existing pattern (per D-01):
    - EntrypointId(pub(crate) u64)
    - TrustBoundaryId(pub(crate) u64)
    - DispatchEdgeId(pub(crate) u64)
    - UnresolvedFrameworkId(pub(crate) u64)
    All must derive Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash, Serialize, Deserialize. Add assert_small_id_contract calls in the existing test.

    Create analysis/entrypoints/facts.rs with four fact structs and their vocabulary enums (per D-02, D-03, D-04, D-05):

    EntrypointFact: id (EntrypointId), language (Language), framework_id (String), kind (EntrypointKind), target_function (FunctionId), target_symbol (Option of SymbolId), registration_span (Span), registration_file (FileId), trigger_metadata (TriggerMetadata — struct with optional method, path, tool_name, event_name, test_name fields as Option of String), trust_boundary_link (Option of String for entrypoint stable_key linkage), precision (EntrypointPrecision), provenance (EntrypointProvenance), confidence (EntrypointConfidence), status (EntrypointStatus), provider_id (String), stable_key (String).

    EntrypointKind enum (per D-02): HttpRoute, HttpMiddleware, McpTool, McpResource, McpPrompt, CliCommand, Test, Job, QueueConsumer, ServerlessHandler, LifecycleCallback, EventListener, GeneratedDispatch.

    EntrypointPrecision enum: ResolvedStatic, SetupAware, Heuristic, Conservative, Unknown.
    EntrypointProvenance enum: NativeRecognizer, Extension, Generated.
    EntrypointConfidence enum: High, Medium, Low.
    EntrypointStatus enum: Resolved, Partial, Unresolved, SetupMissing, Unsupported.

    TrustBoundaryFact (per D-03, D-19, D-20): id (TrustBoundaryId), entrypoint_stable_key (String), source_kind (TrustBoundarySourceKind), target_parameter (Option of FunctionId), target_parameter_index (Option of usize), access_path (Option of String), protocol (Option of String), language (Language), file (FileId), span (Span), precision (EntrypointPrecision), provider_id (String), stable_key (String).

    TrustBoundarySourceKind enum (per D-20): PathParam, QueryString, RequestBody, RequestHeader, Cookie, McpArguments, McpResourceUri, CliArgs, CliFlags, EnvVar, Stdin, QueuePayload, ExternalReturn, Unknown.

    FrameworkDispatchEdgeFact (per D-04): id (DispatchEdgeId), from_source (String — dispatch root stable key), to_target (FunctionId), to_symbol (Option of SymbolId), edge_kind (DispatchEdgeKind), guard_metadata (Option of String), ordering (Option of u32), language (Language), file (FileId), span (Span), precision (EntrypointPrecision), provider_id (String), stable_key (String).

    DispatchEdgeKind enum: RouteDispatch, MiddlewareChain, LifecycleHook, EventDispatch, McpDispatch, TestRunner, JobScheduler.

    UnresolvedFrameworkFact (per D-05): id (UnresolvedFrameworkId), language (Language), file (FileId), span (Span), framework_id (String), reason (UnresolvedFrameworkReason), evidence (String), scope_description (String), precision (EntrypointPrecision), provider_id (String), stable_key (String).

    UnresolvedFrameworkReason enum: DynamicRoute, UnknownWrapper, UnresolvedHandler, MissingSetup, UnsupportedFrameworkVersion, BudgetExceeded, DynamicRegistration, UnrecognizedPattern.

    All enums derive the full trait set. Add a #[cfg(test)] module verifying ID contract traits and stable key separation.

    Add four FactFamily variants to metadata.rs after ExtensionFact: Entrypoint, TrustBoundary, DispatchEdge, UnresolvedFramework. Add labels in the label() match: "Entrypoint", "TrustBoundary", "DispatchEdge", "UnresolvedFramework".

    Create analysis/entrypoints/mod.rs declaring pub(crate) submodules: facts, store (and later: extract, provider, cache_key, validate, unresolved). Start with just facts and store.

    Add pub(crate) mod entrypoints to analysis/mod.rs in alphabetical position.
  </action>
  <verify>
    <automated>cd /Users/emilwareus/conductor/workspaces/exlint/sydney && cargo test -p polint --lib analysis::ids -- --exact 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - analysis/ids.rs contains pub(crate) struct EntrypointId(pub(crate) u64), TrustBoundaryId, DispatchEdgeId, UnresolvedFrameworkId
    - analysis/entrypoints/facts.rs contains pub(crate) struct EntrypointFact with id, language, framework_id, kind, target_function, registration_span, precision, stable_key fields
    - analysis/entrypoints/facts.rs contains pub(crate) enum EntrypointKind with 13 variants (HttpRoute through GeneratedDispatch)
    - analysis/entrypoints/facts.rs contains pub(crate) struct TrustBoundaryFact with entrypoint_stable_key, source_kind, precision, stable_key fields
    - analysis/entrypoints/facts.rs contains pub(crate) enum TrustBoundarySourceKind with 14 variants (PathParam through Unknown)
    - analysis/entrypoints/facts.rs contains pub(crate) struct FrameworkDispatchEdgeFact with from_source, to_target, edge_kind, stable_key fields
    - analysis/entrypoints/facts.rs contains pub(crate) struct UnresolvedFrameworkFact with reason, evidence, stable_key fields
    - analysis/entrypoints/facts.rs contains pub(crate) enum UnresolvedFrameworkReason with 8 variants
    - analysis_kernel/metadata.rs FactFamily enum contains Entrypoint, TrustBoundary, DispatchEdge, UnresolvedFramework variants with correct labels
    - analysis/mod.rs contains pub(crate) mod entrypoints
    - cargo test -p polint --lib analysis::ids passes
    - cargo test -p polint --lib analysis::entrypoints::facts passes
  </acceptance_criteria>
  <done>Four fact types with vocabulary enums, four dense IDs, FactFamily variants with labels, and module wiring all compile and pass unit tests</done>
</task>

<task type="auto">
  <name>Task 2: Create EntrypointOutput container and EntrypointStore with validated indexes</name>
  <files>crates/polint/src/analysis/entrypoints/store.rs</files>
  <read_first>
    - crates/polint/src/analysis/calls/store.rs
    - crates/polint/src/analysis/entrypoints/facts.rs
    - crates/polint/src/analysis/error.rs
  </read_first>
  <action>
    Create analysis/entrypoints/store.rs following the CallOutput/CallStore pattern (per D-12):

    EntrypointOutput: pub(crate) struct with entrypoints (Vec of EntrypointFact), trust_boundaries (Vec of TrustBoundaryFact), dispatch_edges (Vec of FrameworkDispatchEdgeFact), unresolved (Vec of UnresolvedFrameworkFact). Derive Debug, Clone, Default, PartialEq, Eq.

    Implement normalized(mut self) -> Self: sort entrypoints by (stable_key, id), sort trust_boundaries by (entrypoint_stable_key, stable_key, id), sort dispatch_edges by (from_source, stable_key, id), sort unresolved by (stable_key, id). Reassign IDs sequentially within each vector after sorting, starting from 0.

    EntrypointStore: pub(crate) struct wrapping normalized output plus BTreeMap indexes:
    - entrypoints_by_kind: BTreeMap of EntrypointKind to Vec of usize
    - entrypoints_by_file: BTreeMap of FileId to Vec of usize
    - entrypoints_by_framework: BTreeMap of String to Vec of usize
    - trust_boundaries_by_entrypoint_key: BTreeMap of String to Vec of usize
    - dispatch_edges_by_entrypoint_key: BTreeMap of String to Vec of usize
    - unresolved_by_reason: BTreeMap of UnresolvedFrameworkReason to Vec of usize

    Implement from_output(output: EntrypointOutput) -> Result of Self or AnalysisError:
    1. Call output.normalized()
    2. Validate referential integrity: every trust_boundary.entrypoint_stable_key must exist in entrypoints stable keys. Every dispatch_edge.from_source must exist in entrypoints stable keys. On failure return AnalysisError::InvalidFact with provider "polint.entrypoints".
    3. Build BTreeMap indexes.

    Add accessor methods: entrypoints() -> &[EntrypointFact], trust_boundaries() -> &[TrustBoundaryFact], dispatch_edges() -> &[FrameworkDispatchEdgeFact], unresolved() -> &[UnresolvedFrameworkFact], output() -> &EntrypointOutput.

    Add a #[cfg(test)] module verifying: normalization sorts by stable key, from_output rejects dangling trust boundary references, from_output rejects dangling dispatch edge references, indexes are correctly built.
  </action>
  <verify>
    <automated>cd /Users/emilwareus/conductor/workspaces/exlint/sydney && cargo test -p polint --lib analysis::entrypoints -- 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - analysis/entrypoints/store.rs contains pub(crate) struct EntrypointOutput with four Vec fields and normalized() method
    - analysis/entrypoints/store.rs contains pub(crate) struct EntrypointStore with from_output returning Result
    - from_output rejects dangling trust_boundary.entrypoint_stable_key (test proves AnalysisError::InvalidFact is returned)
    - from_output rejects dangling dispatch_edge.from_source (test proves AnalysisError::InvalidFact is returned)
    - Normalization sorts entrypoints by (stable_key, id) and reassigns sequential IDs
    - Indexes are built correctly (entrypoints_by_kind, trust_boundaries_by_entrypoint_key, etc.)
    - cargo test -p polint --lib analysis::entrypoints passes
  </acceptance_criteria>
  <done>EntrypointOutput normalizes deterministically, EntrypointStore validates referential integrity and builds indexes, all unit tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fact contracts | Internal fact types with no external input in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Tampering | EntrypointStore from_output | mitigate | Validate referential integrity of trust boundary and dispatch edge references before building indexes |
| T-35-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies added in this plan |
</threat_model>

<verification>
cargo test -p polint --lib analysis::entrypoints -- passes
cargo test -p polint --lib analysis::ids -- passes
cargo check -p polint succeeds with no errors
</verification>

<success_criteria>
Four framework fact types compile with correct vocabulary enums. EntrypointStore validates referential integrity. FactFamily enum is extended with four new variants. All tests pass.
</success_criteria>

<output>
Create `.planning/phases/35-framework-entrypoints-and-trust-boundaries/35-01-SUMMARY.md` when done
</output>
