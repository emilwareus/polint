---
phase: 34-rust-extension-provider-sink
plan: 02
type: execute
wave: 2
depends_on: [34-01]
files_modified:
  - crates/polint/src/analysis/extensions/host.rs
  - crates/polint/src/analysis/extensions/protocol.rs
  - crates/polint/src/analysis/extensions/manifest.rs
  - crates/polint/src/diagnostics/mod.rs
autonomous: true
requirements: [SAE-INT-04]
must_haves:
  truths:
    - "D-02: Extensions run as process-isolated executables through a versioned protocol"
    - "D-04: Extension build, handshake, panic, timeout, and protocol failures become controlled diagnostics"
    - "D-17: --no-cache still runs extension discovery and provider execution while avoiding analysis cache reads/writes"
  artifacts:
    - path: "crates/polint/src/analysis/extensions/host.rs"
      provides: "Process-isolated extension handshake and run-provider host"
    - path: "crates/polint/src/analysis/extensions/protocol.rs"
      provides: "Versioned request/response protocol structs with validation-friendly schemas"
---

<objective>
Add the process-isolated extension host and versioned protocol handshake.

Purpose: Phase 34 must prove extension code cannot crash the polint host and that extension setup gaps are reported as diagnostics before any extension facts are trusted.

Output: A crate-private extension host can run local extension executables for handshake/provider commands, classify failures deterministically, and return activation status plus diagnostics.
</objective>

<context>
@.planning/phases/34-rust-extension-provider-sink/34-CONTEXT.md
@crates/polint/src/analysis/extensions/manifest.rs
@crates/polint/src/diagnostics/mod.rs
@crates/polint/src/rule_error.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define extension protocol payloads</name>
  <files>crates/polint/src/analysis/extensions/protocol.rs, crates/polint/src/analysis/extensions/mod.rs</files>
  <read_first>
    crates/polint/src/analysis/extensions/manifest.rs
    crates/polint/src/eval/model.rs
    crates/polint/src/analysis_kernel/incremental/input_snapshot.rs
  </read_first>
  <action>
    Add serde protocol structs for `ExtensionHandshakeRequest`, `ExtensionHandshakeResponse`, `ExtensionProviderRunRequest`, `ExtensionProviderRunResponse`, `ExtensionProtocolDiagnostic`, and schema constants `polint-extension-handshake-v1` and `polint-extension-provider-run-v1`. Include protocol version, extension id, provider id, declared inputs, declared outputs, activation status, diagnostics, and output digest inputs. Deny unknown fields on wire structs.
  </action>
  <acceptance_criteria>
    - Protocol structs use `#[serde(deny_unknown_fields)]` where they cross the process boundary.
    - Tests reject unknown JSON fields for handshake and provider responses.
    - Tests reject unsupported protocol schema labels with a deterministic error enum.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement process host with bounded command execution</name>
  <files>crates/polint/src/analysis/extensions/host.rs</files>
  <read_first>
    crates/polint/src/analysis/extensions/discovery.rs
    crates/polint/src/analysis/extensions/protocol.rs
    crates/polint/src/cache/mod.rs
    crates/polint/src/diagnostics/mod.rs
  </read_first>
  <action>
    Implement a crate-private host that runs local extension crates through `cargo run --manifest-path <path> -- handshake` and `cargo run --manifest-path <path> -- run-provider <provider>`. Set `CARGO_TARGET_DIR` to `.polint/cache/extensions-target` under the analyzed repo. Bound stdout/stderr capture and timeouts with a small internal default. Convert nonzero exit, invalid JSON, timeout, missing binary, and panic-like stderr into typed extension host errors.
  </action>
  <acceptance_criteria>
    - Host code uses `std::process::Command` with explicit args; it does not invoke a shell string.
    - Tests cover successful fake command output, nonzero exit, invalid JSON, and timeout classification.
    - Error classification includes labels for build_failed, handshake_failed, provider_failed, timeout, malformed_response, and unsupported_protocol.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Convert host failures into controlled diagnostics</name>
  <files>crates/polint/src/analysis/extensions/host.rs, crates/polint/src/diagnostics/mod.rs</files>
  <read_first>
    crates/polint/src/analysis/cfg/provider.rs
    crates/polint/src/analysis/calls/provider.rs
    crates/polint/src/diagnostics/mod.rs
  </read_first>
  <action>
    Add diagnostic helpers for extension setup failures with rule ids `polint/extension` or `polint/capability`, stable messages, extension/provider evidence fields, and no raw stderr body beyond a bounded sanitized summary. Ensure failed extensions produce activation status failed and no emitted facts.
  </action>
  <acceptance_criteria>
    - A unit test proves a malformed extension response produces a `polint/extension` diagnostic and zero provider facts.
    - A unit test proves a failed extension has activation status failed or validation_failed.
    - Diagnostics do not include absolute temp paths or full unbounded stderr.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| extension process -> polint host | Extension stdout/stderr and exit status are untrusted |
| cargo build/run -> local machine | Extension code is executable repo-local Rust |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-04 | Denial of Service | extension process | mitigate | Timeouts and bounded output capture |
| T-34-05 | Tampering | protocol response | mitigate | Versioned serde structs with deny_unknown_fields and validation before merge |
| T-34-06 | Information Disclosure | stderr diagnostics | mitigate | Bounded sanitized summaries only |
</threat_model>

<verification>
cargo test --lib -p polint -- extensions::protocol
cargo test --lib -p polint -- extensions::host
cargo clippy -p polint -- -D warnings
</verification>

<success_criteria>
- Extension process isolation exists.
- Handshake/provider failures are controlled diagnostics.
- No extension facts are trusted during host/protocol failures.
</success_criteria>

<output>
Create `.planning/phases/34-rust-extension-provider-sink/34-02-SUMMARY.md` when done.
</output>
