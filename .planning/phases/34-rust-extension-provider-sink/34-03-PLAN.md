---
phase: 34-rust-extension-provider-sink
plan: 03
type: execute
wave: 3
depends_on: [34-01, 34-02]
files_modified:
  - crates/polint/src/analysis/extensions/sinks.rs
  - crates/polint/src/analysis/extensions/store.rs
  - crates/polint/src/analysis/extensions/validate.rs
  - crates/polint/src/core/mod.rs
  - crates/polint/src/analysis_kernel/metadata.rs
autonomous: true
requirements: [SAE-INT-04]
must_haves:
  truths:
    - "D-06: Extensions emit through typed sinks and never mutate AnalysisDb directly"
    - "D-07: Declared read/output sets are enforced against observed extension behavior"
    - "D-08: Extension output binds to stable existing ids or explicit synthetic ids with evidence"
    - "D-09: Extension facts merge only after validation and cannot silently override native facts"
    - "D-10: Invalid extension facts are rejected before merge"
  artifacts:
    - path: "crates/polint/src/analysis/extensions/sinks.rs"
      provides: "Typed extension sink API for the first real extension fact vertical slice"
    - path: "crates/polint/src/analysis/extensions/validate.rs"
      provides: "Manifest, declared output, binding, span, and conflict validation"
---

<objective>
Add the first typed extension sink, validation, and merge boundary.

Purpose: Phase 34 must move from synthetic eval rows to a real host-validated extension output path while preventing invalid facts from entering normal analysis stores.

Output: Accepted extension facts are normalized and stored with metadata; rejected facts are reported for debug/eval but are not merged into `AnalysisDb` as trusted analysis rows.
</objective>

<context>
@.planning/phases/34-rust-extension-provider-sink/34-CONTEXT.md
@crates/polint/src/analysis/extensions/protocol.rs
@crates/polint/src/analysis_kernel/metadata.rs
@crates/polint/src/core/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed sink payloads for the first extension vertical slice</name>
  <files>crates/polint/src/analysis/extensions/sinks.rs, crates/polint/src/analysis/extensions/mod.rs</files>
  <read_first>
    crates/polint/src/analysis/summaries/facts.rs
    crates/polint/src/analysis/calls/facts.rs
    crates/polint/src/eval/model.rs
    research/agent-extension-surface/RECOMMENDED_IMPLEMENTATION.md
  </read_first>
  <action>
    Define a small typed sink surface for Phase 34's first vertical slice. The sink may be a generic internal extension-fact sink or a narrow preview sink selected by the planner, but it must have concrete typed fields for fact family, stable key, binding refs, precision, confidence/status, evidence refs, and payload labels. Do not expose `AnalysisDb` mutation or raw parser ASTs to sink authors.
  </action>
  <acceptance_criteria>
    - `sinks.rs` contains a typed builder or payload type with fields for fact_family, stable_key, precision, confidence/status, and evidence.
    - Sink payloads are serializable and sorted deterministically.
    - Tests prove sink output normalization is stable independent of emission order.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Validate declared outputs, bindings, spans, and conflicts</name>
  <files>crates/polint/src/analysis/extensions/validate.rs</files>
  <read_first>
    crates/polint/src/analysis_kernel/validation.rs
    crates/polint/src/analysis_kernel/metadata.rs
    crates/polint/src/core/mod.rs
    crates/polint/src/path_context.rs
  </read_first>
  <action>
    Add validation that rejects facts whose family was not declared by the provider, whose referenced files/symbols/calls/functions do not exist, whose spans exceed file bounds, whose precision/provenance is missing, whose synthetic ids lack evidence, or whose stable key conflicts with native facts without an explicit merge policy. Validation results must separate accepted and rejected facts with stable rejection reasons.
  </action>
  <acceptance_criteria>
    - Tests cover undeclared output family, missing symbol/file binding, invalid span, missing precision, duplicate stable key, and native conflict rejection.
    - Accepted/rejected output is deterministic and sorted by extension id, provider id, family, stable key, and reason.
    - Rejected facts are not inserted into trusted stores.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Store accepted extension facts with metadata</name>
  <files>crates/polint/src/analysis/extensions/store.rs, crates/polint/src/core/mod.rs, crates/polint/src/analysis_kernel/metadata.rs</files>
  <read_first>
    crates/polint/src/analysis/summaries/store.rs
    crates/polint/src/analysis/calls/store.rs
    crates/polint/src/analysis_kernel/metadata.rs
  </read_first>
  <action>
    Add an extension store or AnalysisDb sidecar for accepted Phase 34 extension facts and rejected-fact audit rows. Accepted facts must get `FactMeta` rows with producer id derived from the extension/provider ids, `FactPrecision` mapped from extension precision, confidence/status, stable key, and evidence. Use a deterministic producer id scheme such as `polint.extension.<extension_id>.<provider_id>`.
  </action>
  <acceptance_criteria>
    - Accepted extension facts have metadata rows with producer id starting with `polint.extension.`
    - Rejected facts are observable through a separate audit/debug path but not through normal accepted fact iterators.
    - Metadata validation can distinguish extension producer ids from native provider ids.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| extension output -> AnalysisDb | Untrusted emitted facts must be validated before merge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-07 | Tampering | emitted facts | mitigate | Validate declared outputs, stable bindings, spans, precision, provenance, and conflicts before merge |
| T-34-08 | Spoofing | producer identity | mitigate | Derive producer ids from manifest extension/provider ids, not extension-provided arbitrary strings |
| T-34-09 | Repudiation | rejected facts | mitigate | Preserve deterministic rejection reasons in internal audit/debug rows |
</threat_model>

<verification>
cargo test --lib -p polint -- extensions::sinks
cargo test --lib -p polint -- extensions::validate
cargo test --lib -p polint -- extensions::store
</verification>

<success_criteria>
- A typed sink path exists.
- Invalid extension facts are rejected before merge.
- Accepted extension facts carry metadata with provenance and precision ceilings.
</success_criteria>

<output>
Create `.planning/phases/34-rust-extension-provider-sink/34-03-SUMMARY.md` when done.
</output>
