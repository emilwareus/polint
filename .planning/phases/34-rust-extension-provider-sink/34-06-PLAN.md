---
phase: 34-rust-extension-provider-sink
plan: 06
type: execute
wave: 6
depends_on: [34-01, 34-02, 34-03, 34-04, 34-05]
files_modified:
  - crates/polint/src/eval/model.rs
  - crates/polint/src/eval/observed.rs
  - crates/polint/src/eval/fixtures.rs
  - crates/polint/tests/cli.rs
  - tests/eval-fixtures/extension/real-sink/expected.polint-eval.toml
  - tests/eval-fixtures/extension/real-sink/repo/.polint.toml
  - tests/eval-fixtures/extension/real-sink/repo/.polint/extensions/demo/Cargo.toml
  - tests/eval-fixtures/extension/real-sink/repo/.polint/extensions/demo/src/main.rs
autonomous: true
requirements: [SAE-INT-04]
must_haves:
  truths:
    - "SAE-INT-04: Invalid extension facts are rejected before merge"
    - "SAE-INT-04: Extension facts carry provenance and precision ceilings"
    - "SAE-INT-04: Default-vs-extended eval reports changed facts and unknown reduction or bounded surrogate evidence"
    - "D-18: Internal extension protocol terms do not leak through normal public surfaces"
    - "D-19: Framework entrypoints, refined calls, data flow, slicing, evidence, and public query views remain deferred"
  artifacts:
    - path: "tests/eval-fixtures/extension/real-sink/expected.polint-eval.toml"
      provides: "Real extension sink fixture manifest with accepted/rejected facts and delta invariants"
    - path: "crates/polint/src/eval/observed.rs"
      provides: "Eval observation for extension activation, validation, cache, and delta rows"
    - path: "crates/polint/tests/cli.rs"
      provides: "Public no-leak compatibility proof"
---

<objective>
Add real extension eval fixtures, default-vs-extended delta proof, and final public-boundary tests.

Purpose: Phase 34 closes only when a real repo-local extension exercises the sink path and the eval harness proves accepted/rejected facts, provenance/precision, cache participation, and public no-leak behavior.

Output: A native eval fixture with `extension.real_sink_active = true`, deterministic default-vs-extended delta rows, rejected fact evidence, cache digest proof, and public compatibility tests.
</objective>

<context>
@.planning/phases/34-rust-extension-provider-sink/34-CONTEXT.md
@tests/eval-fixtures/extension/rejection-delta/expected.polint-eval.toml
@crates/polint/src/eval/model.rs
@crates/polint/src/eval/fixtures.rs
@crates/polint/src/eval/observed.rs
@crates/polint/tests/cli.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a real extension fixture crate</name>
  <files>tests/eval-fixtures/extension/real-sink/repo/.polint/extensions/demo/Cargo.toml, tests/eval-fixtures/extension/real-sink/repo/.polint/extensions/demo/src/main.rs, tests/eval-fixtures/extension/real-sink/repo/.polint.toml</files>
  <read_first>
    tests/eval-fixtures/extension/rejection-delta/expected.polint-eval.toml
    research/agent-extension-surface/RECOMMENDED_IMPLEMENTATION.md
    crates/polint/src/analysis/extensions/protocol.rs
  </read_first>
  <action>
    Add a small repo fixture with one extension crate that implements handshake and run-provider protocol commands. The extension should emit at least one valid fact through the Phase 34 typed sink and one invalid fact that the host rejects. Keep the fixture source minimal and deterministic.
  </action>
  <acceptance_criteria>
    - Fixture extension has `Cargo.toml` and `src/main.rs` under `.polint/extensions/demo/`.
    - Extension output includes one accepted candidate fact and one rejected candidate fact.
    - Running the fixture through the extension host sets `extension.real_sink_active` to true in internal eval observation.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Extend eval observation and fixture manifest</name>
  <files>crates/polint/src/eval/model.rs, crates/polint/src/eval/observed.rs, crates/polint/src/eval/fixtures.rs, tests/eval-fixtures/extension/real-sink/expected.polint-eval.toml</files>
  <read_first>
    crates/polint/src/eval/model.rs
    crates/polint/src/eval/observed.rs
    crates/polint/src/eval/fixtures.rs
    tests/eval-fixtures/extension/rejection-delta/expected.polint-eval.toml
  </read_first>
  <action>
    Add eval observation rows for extension activation status, accepted fact count, rejected fact count, validation failure count, extension output digest, cache key/digest change, and default-vs-extended delta. The delta must include changed facts and either unknown reduction or an explicitly named bounded surrogate when the first sink cannot reduce real unknowns yet. Update the fixture manifest to assert these invariants.
  </action>
  <acceptance_criteria>
    - `expected.polint-eval.toml` contains `extension.real_sink_active` with value `true`.
    - Manifest asserts at least one accepted fact and one rejected fact.
    - Manifest asserts an extension digest/cache invariant that changes when extension source changes.
    - Manifest asserts `extension.default_vs_extension_delta` with changed fact evidence.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add public no-leak and final compatibility proof</name>
  <files>crates/polint/tests/cli.rs, README.md, docs/API-VISIBILITY-PLAN.md</files>
  <read_first>
    crates/polint/tests/cli.rs
    docs/API-VISIBILITY-PLAN.md
    README.md
    AGENTS.md
  </read_first>
  <action>
    Add no-leak assertions for normal `polint check --format json`, CLI help, SDK exports, runner exports, README, docs/facts, and docs/API-VISIBILITY-PLAN.md. The tests should forbid internal protocol names, raw extension debug rows, and preview sink names from normal public surfaces unless the implementation deliberately promoted a documented extension surface with tests.
  </action>
  <acceptance_criteria>
    - Public no-leak tests fail on `polint-extension-handshake-v1`, `polint-extension-provider-run-v1`, raw `ExtensionProviderRunResponse`, and internal sink/debug marker strings in normal public output.
    - Existing external temp-repo rule tests still compile using only `polint::sdk::prelude::*` and `polint::runner::run_cli`.
    - No README or docs/facts page advertises broad extension capabilities as stable unless explicitly introduced and tested in this phase.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture extension -> eval trust | A passing fixture could accidentally assert synthetic behavior instead of the real sink |
| internal debug -> public output | Protocol/debug details could leak into supported CLI or SDK surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-16 | Repudiation | extension eval evidence | mitigate | Assert real sink active, accepted/rejected counts, and output digest rows |
| T-34-17 | Information Disclosure | public output | mitigate | Public no-leak marker sweep across CLI JSON/help, SDK, runner, README, and docs |
| T-34-18 | Tampering | synthetic fixture shortcut | mitigate | Fixture must run a real repo-local extension crate, not only synthetic observed rows |
</threat_model>

<verification>
cargo test --lib -p polint -- eval_extension
cargo test --lib -p polint -- real_sink
cargo test -p polint --test cli -- extension_no_leak
cargo test --workspace
cargo clippy --workspace --all-targets -- -D warnings
</verification>

<success_criteria>
- Real extension fixture proves accepted and rejected extension facts.
- Default-vs-extended eval reports extension delta evidence.
- Public compatibility and no-leak tests pass.
- Phase 34 remains scoped; Phase 35+ capabilities stay deferred.
</success_criteria>

<output>
Create `.planning/phases/34-rust-extension-provider-sink/34-06-SUMMARY.md` when done.
</output>
