---
phase: 23-input-snapshots-and-cache-key-vocabulary
plan: 02
type: execute
wave: 2
depends_on:
  - 23-01
files_modified:
  - crates/polint/src/analysis_kernel/incremental/mod.rs
  - crates/polint/src/analysis_kernel/incremental/input_snapshot.rs
autonomous: true
requirements:
  - SAE-FND-04
must_haves:
  truths:
    - "InputSnapshot represents one coherent run input view over discovered files, config, lifecycle, rules, models, extensions, tools, provider schema versions, provider language scopes, and provider cache policies."
    - "Source text digests are identity inputs while mtime-like filesystem data is recorded only as deterministic hint presence."
    - "Go and TS/JS lifecycle inputs are represented with explicit present, absent, unsupported, or setup-missing identity components."
    - "Snapshot debug serialization is deterministic and excludes raw source text, absolute machine paths, temp roots, timestamps, and nondeterministic map order."
  artifacts:
    - path: "crates/polint/src/analysis_kernel/incremental/input_snapshot.rs"
      provides: "InputSnapshot and lifecycle snapshot construction"
      contains: "pub(crate) struct InputSnapshot"
    - path: "crates/polint/src/analysis_kernel/incremental/mod.rs"
      provides: "Crate-private input snapshot module registration"
      contains: "mod input_snapshot"
  key_links:
    - from: "crates/polint/src/analysis_kernel/incremental/input_snapshot.rs"
      to: "crates/polint/src/core/mod.rs"
      via: "reads SourceFile.relative_path, language, content_hash, and source length"
      pattern: "SourceFile"
    - from: "crates/polint/src/analysis_kernel/incremental/input_snapshot.rs"
      to: "crates/polint/src/go/lifecycle.rs"
      via: "uses GoAnalysisConfig::from_loaded for module roots, build tags, include_tests, package_patterns, and setup gaps"
      pattern: "GoAnalysisConfig"
    - from: "crates/polint/src/analysis_kernel/incremental/input_snapshot.rs"
      to: "crates/polint/src/analysis_kernel/provider.rs"
      via: "reads ProviderManifest id, schema_versions, language_scope, cache_policy, precision_ceiling, inputs, and outputs into provider schema snapshot rows"
      pattern: "ProviderManifest"
---

<objective>
Add deterministic internal input snapshots for Phase 23.

Purpose: make the full set of cache-relevant run inputs explicit before changing cache reuse behavior.
Output: crate-private `InputSnapshot` construction with unit tests for source, config, lifecycle, rule, model, extension, provider manifest, and tool identity coverage.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/23-input-snapshots-and-cache-key-vocabulary/23-CONTEXT.md
@research/incremental-query-engine/FINAL-REPORT.md
@research/incremental-query-engine/RECOMMENDED_IMPLEMENTATION.md
@research/module-graph/RECOMMENDED_IMPLEMENTATION.md
@research/semantic-index/RECOMMENDED_IMPLEMENTATION.md
@AGENTS.md
@docs/API-VISIBILITY-PLAN.md
@.agents/skills/rust-best-practices/SKILL.md
@.planning/phases/20-private-analysis-kernel-facade/20-02-SUMMARY.md
@crates/polint/src/core/mod.rs
@crates/polint/src/config/mod.rs
@crates/polint/src/go/lifecycle.rs
@crates/polint/src/fs/mod.rs
@crates/polint/src/analysis_kernel/provider.rs
@crates/polint/src/analysis_kernel/incremental/digest.rs
@crates/polint/src/analysis_kernel/incremental/keys.rs

<interfaces>
Input data already available after source loading:
```rust
pub struct SourceFile {
    pub id: FileId,
    pub path: PathBuf,
    pub relative_path: String,
    pub language: Language,
    pub source: Arc<str>,
    pub content_hash: String,
}

pub(crate) struct LoadedConfig {
    pub(crate) root: PathBuf,
    pub(crate) config: PolintConfig,
    pub(crate) missing: bool,
}

pub(crate) struct GoAnalysisConfig {
    pub(crate) module_roots: Vec<String>,
    pub(crate) package_patterns: Vec<String>,
    pub(crate) build_tags: Vec<String>,
    pub(crate) include_tests: bool,
    pub(crate) files_without_module_root: Vec<String>,
}

pub(crate) struct ProviderManifest {
    pub(crate) id: &'static str,
    pub(crate) inputs: &'static [&'static str],
    pub(crate) outputs: &'static [&'static str],
    pub(crate) language_scope: LanguageScope,
    pub(crate) cache_policy: CachePolicy,
    pub(crate) schema_versions: &'static [SchemaVersion],
    pub(crate) precision_ceiling: PrecisionCeiling,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build deterministic source, config, rule, model, extension, and provider-schema snapshots</name>
  <files>crates/polint/src/analysis_kernel/incremental/mod.rs, crates/polint/src/analysis_kernel/incremental/input_snapshot.rs</files>
  <read_first>
    <file>.planning/phases/23-input-snapshots-and-cache-key-vocabulary/23-CONTEXT.md</file>
    <file>research/incremental-query-engine/RECOMMENDED_IMPLEMENTATION.md</file>
    <file>AGENTS.md</file>
    <file>docs/API-VISIBILITY-PLAN.md</file>
    <file>.agents/skills/rust-best-practices/SKILL.md</file>
    <file>crates/polint/src/core/mod.rs</file>
    <file>crates/polint/src/config/mod.rs</file>
    <file>crates/polint/src/analysis_kernel/provider.rs</file>
    <file>crates/polint/src/analysis_kernel/incremental/mod.rs</file>
    <file>crates/polint/src/analysis_kernel/incremental/digest.rs</file>
    <file>crates/polint/src/analysis_kernel/incremental/input_snapshot.rs</file>
  </read_first>
  <behavior>
    - Test 1: two snapshots built from the same `LoadedConfig`, `AnalysisDb`, config digest, rule digest, plan digest, and provider manifests serialize to byte-identical pretty JSON.
    - Test 2: file rows contain `relative_path`, `language`, `source_text_digest`, `size_bytes`, and `mtime_hint_present`; they do not contain `source`, `SourceFile.path`, absolute temp roots, or raw file text.
    - Test 3: config, rule, plan, model, extension, and provider-schema components are present as typed `Digest` values or explicit absent/unsupported components.
    - Test 4: source file rows are sorted by normalized `relative_path`.
    - Test 5: each provider schema row records `provider_id`, `schema_versions`, `language_scope`, `cache_policy`, `precision_ceiling`, and a `provider_manifest_digest`; changing only `language_scope` or `cache_policy` changes the digest.
  </behavior>
  <action>Create `input_snapshot.rs` and register it from `incremental/mod.rs` with `mod input_snapshot;` and crate-private re-exports required by later plans. Define `INPUT_SNAPSHOT_SCHEMA_VERSION` exactly as `"polint-input-snapshot-1"`. Define `InputSnapshot` with exact fields `schema_version`, `files`, `config`, `go_lifecycle`, `ts_js_lifecycle`, `rules`, `models`, `extensions`, `tool_invocations`, and `provider_schemas`. Define `FileSnapshot` with exact fields `relative_path`, `language`, `source_text_digest`, `size_bytes`, and `mtime_hint_present`; set `source_text_digest` from `SourceFile.content_hash` wrapped as `DigestKind::SourceText`, set `size_bytes` from `SourceFile.source.len()`, and set `mtime_hint_present` from filesystem metadata availability without serializing the timestamp value per D-06 and D-10. Define `InputComponentStatus` with exact variants `Present`, `Absent`, `Unsupported`, and `SetupMissing`. Define `InputComponent { name, status, digest, detail }` where `detail` is sorted, root-relative, and machine-path-free. Build config digest from the `config_digest` argument as `DigestKind::Config`; build rules from `rule_digest` as `DigestKind::RuleCode` and `plan_digest` as `DigestKind::RuleOptions`; build models and extensions as named absent components `model.files` and `extension.providers` with `DigestKind::ModelFile` and `DigestKind::ExtensionCode`. Define `ProviderSchemaSnapshot` with exact fields `provider_id`, `schema_versions`, `language_scope`, `cache_policy`, `precision_ceiling`, and `provider_manifest_digest`; build one row from every `ProviderManifest` using manifest id, sorted schema labels, language scope label, cache policy label, precision ceiling label, sorted inputs, and sorted outputs. The `provider_manifest_digest` must use `DigestKind::ProviderParameters` and include `language_scope` and `cache_policy` as real digest parts, replacing the Phase 20 synthetic manifest-consumption debt per D-14 and D-18. This implements D-01, D-03, D-05, D-06, D-09, D-10, D-14, D-18, D-21, and D-23.</action>
  <acceptance_criteria>
    <criterion>`rg -n "polint-input-snapshot-1|struct InputSnapshot|struct FileSnapshot|enum InputComponentStatus" crates/polint/src/analysis_kernel/incremental/input_snapshot.rs` returns all snapshot type names and schema version.</criterion>
    <criterion>`rg -n "relative_path|source_text_digest|size_bytes|mtime_hint_present" crates/polint/src/analysis_kernel/incremental/input_snapshot.rs` returns all file snapshot fields.</criterion>
    <criterion>`rg -n "model.files|extension.providers|provider_schemas|RuleCode|RuleOptions|SourceText|Config" crates/polint/src/analysis_kernel/incremental/input_snapshot.rs` returns all required snapshot component names or digest kinds.</criterion>
    <criterion>`rg -n "struct ProviderSchemaSnapshot|provider_id|schema_versions|language_scope|cache_policy|precision_ceiling|provider_manifest_digest|ProviderParameters" crates/polint/src/analysis_kernel/incremental/input_snapshot.rs` returns real provider manifest snapshot inputs, including cache policy and language scope.</criterion>
    <criterion>`rg -n "SourceFile\\.path|SystemTime|modified\\(|created\\(|accessed\\(|source:" crates/polint/src/analysis_kernel/incremental/input_snapshot.rs` returns no matches outside comments that explicitly reject those fields.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p polint --lib input_snapshot::source_config_rule_model_extension --locked</automated>
  </verify>
  <done>InputSnapshot records source/config/rule/model/extension/provider manifest inputs, including cache policy and language scope, deterministically without raw source, absolute paths, or timestamp identity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Go and TS/JS lifecycle and official-tool identity components</name>
  <files>crates/polint/src/analysis_kernel/incremental/input_snapshot.rs</files>
  <read_first>
    <file>.planning/phases/23-input-snapshots-and-cache-key-vocabulary/23-CONTEXT.md</file>
    <file>research/incremental-query-engine/RECOMMENDED_IMPLEMENTATION.md</file>
    <file>research/module-graph/RECOMMENDED_IMPLEMENTATION.md</file>
    <file>AGENTS.md</file>
    <file>crates/polint/src/config/mod.rs</file>
    <file>crates/polint/src/go/lifecycle.rs</file>
    <file>crates/polint/src/fs/mod.rs</file>
    <file>crates/polint/src/analysis_kernel/incremental/digest.rs</file>
    <file>crates/polint/src/analysis_kernel/incremental/input_snapshot.rs</file>
  </read_first>
  <behavior>
    - Test 1: a repo with `go.mod`, `go.sum`, configured `[languages.go] module_roots`, `build_tags`, `include_tests`, and `package_patterns` records those exact values in sorted Go lifecycle components.
    - Test 2: a repo with `package.json`, `package-lock.json`, `tsconfig.json`, and `[languages.ts]` settings records TS/JS lifecycle components for package manifests, lockfiles, config files, resolver options, and source-set membership.
    - Test 3: when official Go or TS/JS tool invocations are not run by polint, the snapshot records `go.tool_invocation` and `ts_js.tool_invocation` with status `Unsupported`, not an exact digest.
    - Test 4: setup gaps such as Go files outside module roots are represented with status `SetupMissing` and root-relative file paths.
  </behavior>
  <action>Extend `input_snapshot.rs` with `GoLifecycleSnapshot` and `TsJsLifecycleSnapshot`. For Go, use `crate::go::lifecycle::GoAnalysisConfig::from_loaded(loaded, db)` and record exact components named `go.module_roots`, `go.files_without_module_root`, `go.mod`, `go.sum`, `go.work`, `go.build_tags`, `go.include_tests`, `go.package_patterns`, `go.tool_invocation`, and `go.environment_policy`; digest file components by reading root-relative `go.mod`, `go.sum`, and `go.work` files under every configured or inferred module root when present, use `InputComponentStatus::Absent` for missing optional files, and use `InputComponentStatus::SetupMissing` for `files_without_module_root`. For TS/JS, scan only root-relative lifecycle filenames `package.json`, `package-lock.json`, `pnpm-lock.yaml`, `yarn.lock`, `bun.lock`, `bun.lockb`, `tsconfig.json`, and `jsconfig.json` under the repository root and discovered TS/JS parent directories; record exact components named `ts_js.package_manifests`, `ts_js.lockfiles`, `ts_js.config_files`, `ts_js.resolver_options`, `ts_js.source_set_membership`, and `ts_js.tool_invocation`. Build `ts_js.resolver_options` from sorted `[languages.ts]` TOML settings and `ts_js.source_set_membership` from sorted discovered TS/JS relative paths. Set both official tool invocation components to `Unsupported` unless this code path actually invokes a tool; this phase must not invoke `go`, `tsc`, package managers, or external resolvers. This implements D-07, D-08, D-10, D-21, D-22, and D-23.</action>
  <acceptance_criteria>
    <criterion>`rg -n "struct GoLifecycleSnapshot|go\\.module_roots|go\\.mod|go\\.sum|go\\.work|go\\.build_tags|go\\.include_tests|go\\.package_patterns|go\\.tool_invocation|go\\.environment_policy" crates/polint/src/analysis_kernel/incremental/input_snapshot.rs` returns all Go lifecycle names.</criterion>
    <criterion>`rg -n "struct TsJsLifecycleSnapshot|ts_js\\.package_manifests|ts_js\\.lockfiles|ts_js\\.config_files|ts_js\\.resolver_options|ts_js\\.source_set_membership|ts_js\\.tool_invocation" crates/polint/src/analysis_kernel/incremental/input_snapshot.rs` returns all TS/JS lifecycle names.</criterion>
    <criterion>`rg -n "Command::new|std::process::Command|go env|tsc|npm|pnpm|yarn|bun" crates/polint/src/analysis_kernel/incremental/input_snapshot.rs` returns no matches.</criterion>
    <criterion>`rg -n "SetupMissing|Unsupported|Absent|Present" crates/polint/src/analysis_kernel/incremental/input_snapshot.rs` returns explicit lifecycle status handling.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p polint --lib input_snapshot::lifecycle --locked</automated>
  </verify>
  <done>InputSnapshot covers Go and TS/JS lifecycle/tool identity with deterministic present/absent/unsupported/setup-missing components and no tool execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem -> InputSnapshot | Mutable repo files and lifecycle manifests are normalized into a coherent run snapshot. |
| Source text -> snapshot JSON | Source content influences digests but raw text must not be serialized into test/debug snapshots. |
| Language lifecycle -> cache identity | Go and TS/JS lifecycle config must be explicit so setup gaps do not look like exact inputs. |
| Provider manifests -> snapshot identity | Provider schema, language scope, cache policy, precision, inputs, and outputs become real snapshot/digest inputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-02-01 | Information Disclosure | `FileSnapshot` | mitigate | Serialize only `relative_path`, `language`, `source_text_digest`, `size_bytes`, and deterministic hint presence; never serialize raw source or `SourceFile.path`. |
| T-23-02-02 | Tampering | lifecycle digest lists | mitigate | Sort all discovered lifecycle files and source-set rows before digest construction. |
| T-23-02-03 | Spoofing | unsupported official tool input | mitigate | Record `go.tool_invocation` and `ts_js.tool_invocation` as `Unsupported` when no tool was invoked, avoiding false exactness. |
| T-23-02-04 | Repudiation | setup-missing lifecycle state | mitigate | Represent Go files outside module roots as `SetupMissing` input components with root-relative paths. |
| T-23-02-05 | Repudiation | provider manifest snapshot rows | mitigate | Include manifest `language_scope` and `cache_policy` labels in `ProviderSchemaSnapshot` and `provider_manifest_digest`, with tests proving digest participation. |
</threat_model>

<verification>
Run the automated task commands, then run:
```bash
cargo test -p polint --lib input_snapshot --locked
cargo fmt --all -- --check
```
</verification>

<success_criteria>
Plan 23-02 is complete when `InputSnapshot` deterministically covers file text digests, config, Go lifecycle, TS/JS lifecycle, rule/options digests, model/extension absent components, provider schema versions, provider language scopes, provider cache policies, and official tool invocation status without public exposure or source/path/timestamp leakage.
</success_criteria>

<output>
After completion, create `.planning/phases/23-input-snapshots-and-cache-key-vocabulary/23-02-SUMMARY.md`.
</output>
