---
phase: 26-semantic-index-deepening
plan: 06
type: execute
wave: 6
depends_on: ["26-05"]
files_modified:
  - crates/polint/src/eval/model.rs
  - crates/polint/src/eval/observed.rs
  - crates/polint/src/eval/matcher.rs
  - tests/eval-fixtures/semantic-index/core/expected.polint-eval.toml
  - tests/eval-fixtures/semantic-index/core/repo/.polint.toml
  - tests/eval-fixtures/semantic-index/core/repo/src/app.ts
  - tests/eval-fixtures/semantic-index/core/repo/src/lib.ts
  - tests/eval-fixtures/semantic-index/core/repo/go.mod
  - tests/eval-fixtures/semantic-index/core/repo/service.go
  - crates/polint/tests/cli.rs
  - docs/facts/symbols-and-references.md
autonomous: true
requirements: [SAE-SEM-01]
must_haves:
  truths:
    - "Fixtures cover resolved, ambiguous, unresolved, generated, alias, import/export, and cross-file references."
    - "Unknowns are visible and precision-labeled in internal eval/debug output."
    - "Public check JSON, SDK views, runner behavior, inspect/test surfaces, docs, and schemas do not leak internal semantic providers or rows."
  artifacts:
    - path: "tests/eval-fixtures/semantic-index/core/expected.polint-eval.toml"
      provides: "Semantic-index fixture expectations"
      contains: "semantic-index-core"
    - path: "crates/polint/src/eval/observed.rs"
      provides: "Observed semantic rows for internal eval"
      contains: "semantic"
    - path: "crates/polint/tests/cli.rs"
      provides: "Public no-leak and external-consumer compatibility tests"
      contains: "semantic_index_internals_stay_private"
  key_links:
    - from: "crates/polint/src/eval/observed.rs"
      to: "crates/polint/src/analysis_kernel/debug.rs"
      via: "metadata_debug_json_for_test semantic rows"
      pattern: "metadata_debug_json_for_test"
    - from: "crates/polint/tests/cli.rs"
      to: "public polint check JSON"
      via: "external temp-repo rule using SDK prelude only"
      pattern: "polint::sdk::prelude::\\*"
---

<objective>
Close Phase 26 with internal eval fixture coverage, public compatibility proof, and honest public docs updates where existing symbol/reference behavior changed.

Purpose: This implements D-20, D-21, D-23, D-24, D-25, and D-27 and proves all Phase 26 success criteria.
Output: Semantic-index eval fixture, eval observation/matching support, public no-leak tests, and bounded docs updates for existing `Symbols<'_>` / `References<'_>` semantics.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/26-semantic-index-deepening/26-CONTEXT.md
@research/semantic-index/VALIDATION.md
@AGENTS.md
@.agents/skills/rust-best-practices/SKILL.md
@.claude/skills/polint/SKILL.md
@crates/polint/src/eval/model.rs
@crates/polint/src/eval/observed.rs
@crates/polint/src/eval/matcher.rs
@tests/eval-fixtures/README.md
@crates/polint/tests/cli.rs
@docs/facts/symbols-and-references.md
@docs/facts/imports.md
@docs/facts/module-graph.md

<interfaces>
Existing eval fixture shape:

```toml
schema_version = "polint-eval-fixture-1"
case_id = "..."
area = "..."

[[expected]]
fact = { family = "...", stable_key = "...", mode = "partial", producer_id = "...", precision = "...", status = "present" }
```

Existing public no-leak pattern:

```rust
for marker in ["AnalysisKernel", "LayerCache", "ProviderOutputMeta"] {
    assert!(!public_json.contains(marker));
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Teach internal eval to observe semantic rows</name>
  <files>crates/polint/src/eval/model.rs, crates/polint/src/eval/observed.rs, crates/polint/src/eval/matcher.rs</files>
  <read_first>
    - crates/polint/src/eval/model.rs
    - crates/polint/src/eval/observed.rs
    - crates/polint/src/eval/matcher.rs
    - crates/polint/src/analysis_kernel/debug.rs
    - tests/eval-fixtures/provenance/metadata/expected.polint-eval.toml
    - .planning/phases/22-internal-evaluation-harness-mvp/22-CONTEXT.md
    - .planning/phases/26-semantic-index-deepening/26-CONTEXT.md
  </read_first>
  <behavior>
    - Test: Eval expected facts can use semantic families `Scope`, `SemanticImport`, `Export`, `Alias`, `Resolution`, `GeneratedSymbol`, and `StableExport`.
    - Test: Observed semantic rows include status, precision, stable key, producer id, path, and span.
    - Test: Partial and exact matching work for semantic stable keys and unknown statuses.
  </behavior>
  <action>Extend crate-private eval models to accept semantic fact families exactly `Scope`, `SemanticImport`, `Export`, `Alias`, `Resolution`, `GeneratedSymbol`, and `StableExport`. Update observed collection to read `AnalysisKernel::metadata_debug_json_for_test(output.db).semantic` arrays and normalize each row into existing observed fact rows with `family`, `stable_key`, `producer_id`, `precision`, `status`, `path`, `span`, and `payload` fields. Map semantic statuses `Unresolved`, `Ambiguous`, `Dynamic`, `Unsupported`, `SetupMissing`, `External`, `Cycle`, and `Generated` into matcher status values so unknown-rate metrics count them. Keep eval crate-private and do not add a `polint eval` command.</action>
  <verify>
    <automated>cargo test -p polint --lib eval::semantic_rows --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg "Scope|SemanticImport|StableExport|GeneratedSymbol" crates/polint/src/eval/model.rs crates/polint/src/eval/observed.rs crates/polint/src/eval/matcher.rs` finds semantic family support.
    - `rg "metadata_debug_json_for_test|\\.semantic|generated_symbols|stable_exports" crates/polint/src/eval/observed.rs` finds observed semantic collection.
    - `rg "polint eval|Eval" crates/polint/src/cli crates/polint/src/sdk` returns no new public eval command or SDK type.
  </acceptance_criteria>
  <done>Internal eval can assert semantic rows and unknown/status precision without creating a public eval surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add semantic-index native fixture coverage</name>
  <files>tests/eval-fixtures/semantic-index/core/expected.polint-eval.toml, tests/eval-fixtures/semantic-index/core/repo/.polint.toml, tests/eval-fixtures/semantic-index/core/repo/src/app.ts, tests/eval-fixtures/semantic-index/core/repo/src/lib.ts, tests/eval-fixtures/semantic-index/core/repo/go.mod, tests/eval-fixtures/semantic-index/core/repo/service.go</files>
  <read_first>
    - tests/eval-fixtures/README.md
    - tests/eval-fixtures/provenance/metadata/expected.polint-eval.toml
    - tests/eval-fixtures/cache/layer-cache/expected.polint-eval.toml
    - research/semantic-index/VALIDATION.md
    - .planning/phases/26-semantic-index-deepening/26-CONTEXT.md
  </read_first>
  <behavior>
    - Test: Fixture expected rows cover resolved, ambiguous, unresolved, generated, alias, import/export, cross-file reference, and shadowing cases.
    - Test: Fixture expected rows assert stable export keys across cold/warm cache runs.
    - Test: Fixture expected rows include unknown statuses with precision labels.
  </behavior>
  <action>Create `tests/eval-fixtures/semantic-index/core/` with `case_id = "semantic-index-core"` and `area = "semantic-index"`. The TS fixture must include `src/lib.ts` exporting a named value `handler`, a default export, a reexport, a namespace import, a default import, a named import alias, a dynamic import expression, a CommonJS `require`, an unresolved global `missingSymbol`, and nested shadowing of `handler`. The Go fixture must include `go.mod` for module `example.com/semanticfixture` and `service.go` with exported `Handler`, named import alias, dot import, blank import, method receiver, local shadowing, and an unresolved identifier in a function body. The expected TOML must include at least one expected fact for each semantic family and exact expected markers with these family/status combinations: `Reference` + `Ambiguous`, `Reference` + `Unresolved`, `SemanticImport` + `Dynamic`, `Alias` + `Resolved`, `Export` + `Resolved`, `GeneratedSymbol` + `Generated`, and `StableExport` + `Resolved`. Include cache invariants for `polint.symbol_graph` cold miss, warm hit, and warm verified reuse.</action>
  <verify>
    <automated>cargo test -p polint --lib eval::fixtures::semantic_index_core --locked</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/eval-fixtures/semantic-index/core/expected.polint-eval.toml` passes.
    - `rg "semantic-index-core|area = \"semantic-index\"" tests/eval-fixtures/semantic-index/core/expected.polint-eval.toml` finds fixture identity.
    - `rg "Ambiguous|Unresolved|Dynamic|Generated|StableExport|SemanticImport|Alias|Export" tests/eval-fixtures/semantic-index/core/expected.polint-eval.toml` finds required status and family coverage.
    - `rg "dynamic import|require\\(|missingSymbol|namespace|reexport|shadow" tests/eval-fixtures/semantic-index/core/repo/src tests/eval-fixtures/semantic-index/core/repo/service.go` finds fixture source patterns.
    - `rg "polint\\.symbol_graph.*warm|verified_reuse|cold" tests/eval-fixtures/semantic-index/core/expected.polint-eval.toml` finds cache restore expectations.
  </acceptance_criteria>
  <done>Native eval fixture coverage proves the semantic-index taxonomy, visible unknowns, cross-file references, generated hooks, aliases, imports/exports, and cache restore behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove public compatibility and update supported fact docs</name>
  <files>crates/polint/tests/cli.rs, docs/facts/symbols-and-references.md</files>
  <read_first>
    - crates/polint/tests/cli.rs
    - docs/facts/symbols-and-references.md
    - docs/facts/imports.md
    - docs/facts/module-graph.md
    - .claude/skills/polint/SKILL.md
    - AGENTS.md
    - .planning/phases/25-rule-manifest-inspect-and-test-skeleton/25-CONTEXT.md
    - .planning/phases/26-semantic-index-deepening/26-CONTEXT.md
  </read_first>
  <behavior>
    - Test: External temp-repo rule using only `polint::sdk::prelude::*` can consume `Symbols<'_>` and `References<'_>` after semantic deepening.
    - Test: Public `check`, `inspect rule`, and `test` JSON do not contain semantic internal markers.
    - Test: Public docs mention only existing `Symbols<'_>` / `References<'_>` behavior and do not document new public semantic views, semantic commands, generic graph APIs, or semantic debug commands.
  </behavior>
  <action>Add a CLI integration test named `semantic_index_internals_stay_private`. Build a temp repo with a local rule importing only `polint::sdk::prelude::*` and requesting `Symbols<'_>` and `References<'_>`. Run `polint check --format json --fail-on none`, `polint inspect rule --format json`, and `polint test --format json`; parse each as JSON where applicable. Assert public outputs do not contain markers `SemanticIndex`, `ScopeFact`, `SemanticImportFact`, `ResolutionFact`, `GeneratedSymbolFact`, `StableExportIdentity`, `alias_reexport_closure`, `metadata.precision`, `ProviderOutputMeta`, `KernelRunReport`, raw sidecar schema `polint-go-symbols-semantic-1`, or `tests/eval-fixtures`. Update `docs/facts/symbols-and-references.md` only to clarify that precision/status may reflect lexical, module-linked, ambiguous, unresolved, setup-missing, unsupported, generated, or external semantic evidence, and that scopes/import closure/resolution-step rows remain internal. Do not add new public semantic views, commands, export-format docs, or generic graph API docs.</action>
  <verify>
    <automated>cargo test -p polint --test cli semantic_index_internals_stay_private --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg "semantic_index_internals_stay_private" crates/polint/tests/cli.rs` finds the test.
    - `rg "SemanticIndex|ScopeFact|SemanticImportFact|ResolutionFact|GeneratedSymbolFact|StableExportIdentity|alias_reexport_closure|polint-go-symbols-semantic-1" crates/polint/tests/cli.rs` finds no-leak markers.
    - `rg "polint::sdk::prelude::\\*|Symbols<'_>|References<'_>|polint::runner::run_cli" crates/polint/tests/cli.rs` finds external-consumer proof.
    - `rg "scopes/import closure/resolution-step rows remain internal|generated|external semantic evidence" docs/facts/symbols-and-references.md` finds the bounded doc update.
    - `rg "public semantic view|semantic command|export-format|generic graph API" docs/facts docs/CONSUMER-SETUP.md README.md` returns no new public semantic promotion from this plan.
  </acceptance_criteria>
  <done>Existing public rule-author and CLI surfaces still work, internal semantic markers do not leak, and docs honestly describe only supported symbol/reference behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internal eval/debug output to public compatibility tests | Internal semantic evidence is used for validation but must not become public JSON/schema. |
| external rule host to SDK views | Temp-repo rules consume only supported SDK/runner APIs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-06-01 | Information Disclosure | public JSON outputs | mitigate | Add no-leak tests for check, inspect, and test outputs against semantic/debug/cache/internal markers. |
| T-26-06-02 | Spoofing | fixture-generated semantic facts | mitigate | Fixture expectations assert generated rows remain labeled `Generated` and do not replace native exact facts silently. |
| T-26-06-03 | Repudiation | unknown semantic states | mitigate | Eval rows include status and precision for unresolved, ambiguous, dynamic, setup-missing, unsupported, external, cycle, and generated states. |
</threat_model>

<verification>
Run:
- `cargo test -p polint --lib eval::semantic_rows --locked`
- `cargo test -p polint --lib eval::fixtures::semantic_index_core --locked`
- `cargo test -p polint --test cli semantic_index_internals_stay_private --locked`
- `cargo test -p polint --test cli inspect_rule_manifest_json_is_stable_for_local_rules --locked`
- `cargo test -p polint --test cli polint_test_runs_temp_repo_fixtures --locked`
- `cargo fmt --all -- --check`
</verification>

<success_criteria>
Plan succeeds when semantic-index fixtures cover the required taxonomy, unknowns are visible and precision-labeled internally, stable exports survive cache restore, public surfaces do not leak internals, and docs stay limited to existing supported symbol/reference facts.
</success_criteria>

<output>
After completion, create `.planning/phases/26-semantic-index-deepening/26-06-SUMMARY.md`.
</output>
