---
phase: 26-semantic-index-deepening
plan: 04
type: execute
wave: 3
depends_on: ["26-02", "26-03"]
files_modified:
  - crates/polint/src/symbol_graph/semantic.rs
  - crates/polint/src/symbol_graph/model.rs
  - crates/polint/src/analysis_kernel/validation.rs
  - crates/polint/src/analysis_kernel/debug.rs
autonomous: true
requirements: [SAE-SEM-01]
must_haves:
  truths:
    - "Alias/reexport closure terminates deterministically and reports cycles or unsupported edges as rows."
    - "Generated symbols and hooks are validated internal rows with provenance and stable identity."
    - "Internal debug output exposes unknowns and precision labels for semantic rows without becoming public CLI JSON."
  artifacts:
    - path: "crates/polint/src/symbol_graph/semantic.rs"
      provides: "Alias/reexport closure and generated-symbol merge helpers"
      contains: "alias_reexport_closure"
    - path: "crates/polint/src/analysis_kernel/validation.rs"
      provides: "Semantic referential, span, precision, generated-row, and cache-shape validation"
      contains: "validate_semantic_index"
    - path: "crates/polint/src/analysis_kernel/debug.rs"
      provides: "Test-only semantic debug JSON rows"
      contains: "semantic"
  key_links:
    - from: "crates/polint/src/analysis_kernel/validation.rs"
      to: "crates/polint/src/core/mod.rs"
      via: "AnalysisDb semantic row accessors"
      pattern: "db\\.scopes\\(\\)"
    - from: "crates/polint/src/analysis_kernel/debug.rs"
      to: "crates/polint/src/symbol_graph/semantic.rs"
      via: "semantic debug row serialization"
      pattern: "resolution_facts"
---

<objective>
Add deterministic alias/reexport closure, generated symbol hooks, validation, and internal debug visibility for semantic rows.

Purpose: This implements D-10, D-11, D-12, D-13, D-15, D-20, D-21, D-22, and D-24 across normalized facts from the Go and TS/JS providers.
Output: Bounded closure helper, generated semantic row validation, fail-closed kernel diagnostics, and test-only semantic debug JSON.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/26-semantic-index-deepening/26-CONTEXT.md
@research/semantic-index/RECOMMENDED_IMPLEMENTATION.md
@research/semantic-index/VALIDATION.md
@AGENTS.md
@.agents/skills/rust-best-practices/SKILL.md
@crates/polint/src/symbol_graph/semantic.rs
@crates/polint/src/symbol_graph/model.rs
@crates/polint/src/analysis_kernel/validation.rs
@crates/polint/src/analysis_kernel/debug.rs
@crates/polint/src/analysis_kernel/metadata.rs

<interfaces>
Validation entrypoint:

```rust
pub(crate) fn validate_fact_metadata(
    db: &AnalysisDb,
    manifests: &[ProviderManifest],
) -> Vec<Diagnostic>;
```

Debug JSON entrypoint:

```rust
#[cfg(test)]
pub(crate) fn metadata_debug_json_for_test(db: &AnalysisDb) -> serde_json::Value;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement bounded alias and reexport closure</name>
  <files>crates/polint/src/symbol_graph/semantic.rs, crates/polint/src/symbol_graph/model.rs</files>
  <read_first>
    - crates/polint/src/symbol_graph/semantic.rs
    - crates/polint/src/symbol_graph/model.rs
    - crates/polint/src/symbol_graph/ts.rs
    - crates/polint/src/symbol_graph/go.rs
    - research/semantic-index/RECOMMENDED_IMPLEMENTATION.md
    - .planning/phases/26-semantic-index-deepening/26-CONTEXT.md
  </read_first>
  <behavior>
    - Test: Acyclic import alias chains resolve to deterministic alias target rows.
    - Test: Reexport cycles terminate within a bounded iteration count and emit `SemanticStatus::Cycle`.
    - Test: Star exports create conservative closure rows rather than exact rows when the exported set is incomplete.
  </behavior>
  <action>Add `alias_reexport_closure` in `semantic.rs` that consumes `AliasFact`, `ExportFact`, and `StableExportIdentity` rows and returns sorted closure aliases plus resolution rows. Use a deterministic fixpoint with `max_iterations = aliases.len() + exports.len() + stable_exports.len() + 1`. If the loop would continue after that bound, emit one `ResolutionFact` per unresolved cycle with `ResolutionStepKind::ImportAliasLookup` and `SemanticStatus::Cycle`. Reexport edges with unknown star targets must emit `SemanticStatus::Ambiguous`, not `Resolved`. Wire the closure call into the symbol graph finish path so closure rows are included before `AnalysisDb::replace_semantic_index_facts` is called.</action>
  <verify>
    <automated>cargo test -p polint --lib symbol_graph::semantic::alias_reexport_closure --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg "alias_reexport_closure|max_iterations|SemanticStatus::Cycle" crates/polint/src/symbol_graph/semantic.rs` finds the bounded closure implementation.
    - `rg "replace_semantic_index_facts|alias_reexport_closure" crates/polint/src/symbol_graph/model.rs crates/polint/src/symbol_graph/mod.rs` finds closure wiring before DB replacement.
    - `rg "while true|loop \\{" crates/polint/src/symbol_graph/semantic.rs` returns no unbounded closure loop unless it is paired with `max_iterations` in the same function.
  </acceptance_criteria>
  <done>Alias and reexport closure resolves deterministic chains, terminates on cycles, and records uncertainty instead of dropping edges.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate semantic rows fail closed</name>
  <files>crates/polint/src/analysis_kernel/validation.rs</files>
  <read_first>
    - crates/polint/src/analysis_kernel/validation.rs
    - crates/polint/src/analysis_kernel/metadata.rs
    - crates/polint/src/core/mod.rs
    - crates/polint/src/symbol_graph/semantic.rs
    - .planning/phases/21-provenance-precision-and-validation-metadata/21-CONTEXT.md
    - .planning/phases/26-semantic-index-deepening/26-CONTEXT.md
  </read_first>
  <behavior>
    - Test: Invalid semantic spans produce deterministic `polint/internal` diagnostics.
    - Test: Alias, resolution, generated-symbol, and stable-export rows referencing missing file/scope/symbol IDs produce diagnostics.
    - Test: Semantic rows that exceed provider precision ceiling produce diagnostics.
  </behavior>
  <action>Add `validate_semantic_index` and call it from `validate_fact_metadata` after existing span/reference checks. Validate: every semantic row file reference exists; every parent scope exists when set; every alias source and target scope/symbol reference exists unless status is `Generated`, `External`, `Unresolved`, `Dynamic`, `SetupMissing`, or `Unsupported`; every span lies inside the referenced source file; every generated symbol has non-empty `generated_discriminator`, producer id `polint.symbol_graph`, and `SemanticStatus::Generated`; every stable export identity points to an existing symbol stable key or has status `Generated`; no semantic metadata row from `polint.symbol_graph` has `FactPrecision::Exact` because the provider ceiling is `SetupAware`. Diagnostics must use rule id `polint/internal`, file `<workspace>` for non-file-specific errors, deterministic evidence keys `family`, `stable_key`, and `reason`, and sorted output.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis_kernel::validation::semantic_index --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg "fn validate_semantic_index|generated_discriminator|provider precision ceiling|SemanticStatus::Generated" crates/polint/src/analysis_kernel/validation.rs` finds validation code.
    - `rg "polint/internal" crates/polint/src/analysis_kernel/validation.rs` finds internal diagnostics.
    - `rg "family.*stable_key.*reason|stable_key.*reason.*family" crates/polint/src/analysis_kernel/validation.rs` finds deterministic evidence keys.
    - `rg "FactPrecision::Exact" crates/polint/src/analysis_kernel/validation.rs` finds a semantic precision-ceiling rejection path.
  </acceptance_criteria>
  <done>Semantic validation catches malformed references, spans, generated rows, stable export rows, and over-precise metadata before rule execution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose semantic unknowns in test-only debug JSON</name>
  <files>crates/polint/src/analysis_kernel/debug.rs</files>
  <read_first>
    - crates/polint/src/analysis_kernel/debug.rs
    - crates/polint/src/analysis_kernel/mod.rs
    - crates/polint/src/core/mod.rs
    - crates/polint/src/symbol_graph/semantic.rs
    - crates/polint/tests/cli.rs
    - .planning/phases/26-semantic-index-deepening/26-CONTEXT.md
  </read_first>
  <behavior>
    - Test: `metadata_debug_json_for_test` includes `semantic.scopes`, `semantic.imports`, `semantic.exports`, `semantic.aliases`, `semantic.resolutions`, `semantic.generated_symbols`, and `semantic.stable_exports`.
    - Test: Debug rows include `status`, `precision`, `stable_key`, `producer_id`, `path`, and `span` where applicable.
    - Test: Public `polint check --format json` output contains none of the semantic debug keys.
  </behavior>
  <action>Extend test-only `metadata_debug_json_for_test` to include a top-level object key `semantic` with arrays named exactly `scopes`, `imports`, `exports`, `aliases`, `resolutions`, `generated_symbols`, and `stable_exports`. Rows must be sorted by path, span start byte, name/export name, status, stable key, and run id. Include row fields `status`, `fact_precision`, `metadata.precision`, `metadata.confidence`, `stable_key`, `producer_id`, `layer_id`, and `validation`. Keep this behind `#[cfg(test)]`; do not add a CLI flag, public inspect output, SDK accessor, schema file, or docs page for semantic debug rows.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis_kernel::debug::semantic_debug_json --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg "semantic|generated_symbols|stable_exports|resolution_facts" crates/polint/src/analysis_kernel/debug.rs` finds semantic debug serialization.
    - `rg "#\\[cfg\\(test\\)\\]" crates/polint/src/analysis_kernel/debug.rs crates/polint/src/analysis_kernel/mod.rs` confirms debug entrypoint remains test-only.
    - `rg "semantic debug|semantic.*format|stable_exports" crates/polint/src/cli crates/polint/src/sdk docs/schemas` returns no public CLI, SDK, or schema promotion.
  </acceptance_criteria>
  <done>Internal tests and eval fixtures can observe semantic unknowns and precision labels without exposing a public semantic debug contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| language semantic rows to closure helper | Provider-owned facts feed a derived alias/reexport relation. |
| internal debug data to public surfaces | Debug JSON must remain cfg(test) and cannot leak through CLI or SDK. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04-01 | Denial of Service | `alias_reexport_closure` | mitigate | Use deterministic `max_iterations` bound and emit `Cycle` rows on non-convergence. |
| T-26-04-02 | Tampering | generated semantic rows | mitigate | Validate generated discriminator, producer id, references, spans, and precision ceiling before rule execution. |
| T-26-04-03 | Information Disclosure | semantic debug JSON | mitigate | Keep debug entrypoints under `#[cfg(test)]` and add grep checks against CLI/SDK/schema promotion. |
</threat_model>

<verification>
Run:
- `cargo test -p polint --lib symbol_graph::semantic::alias_reexport_closure --locked`
- `cargo test -p polint --lib analysis_kernel::validation::semantic_index --locked`
- `cargo test -p polint --lib analysis_kernel::debug::semantic_debug_json --locked`
- `cargo fmt --all -- --check`
</verification>

<success_criteria>
Plan succeeds when alias/reexport closure is bounded, generated rows are validated, semantic validation fails closed, and internal debug JSON exposes unknown/status/precision rows only to tests.
</success_criteria>

<output>
After completion, create `.planning/phases/26-semantic-index-deepening/26-04-SUMMARY.md`.
</output>
