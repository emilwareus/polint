---
phase: 26-semantic-index-deepening
plan: 03
type: execute
wave: 3
depends_on: ["26-01", "26-02"]
files_modified:
  - crates/polint/src/symbol_graph/go.rs
  - crates/polint/src/symbol_graph/semantic.rs
  - crates/go-sidecar/polint-go-symbols/main.go
  - crates/go-sidecar/polint-go-symbols/internal/symbols/emit.go
autonomous: true
requirements: [SAE-SEM-01]
must_haves:
  truths:
    - "Go semantic extraction emits normalized internal scopes, imports, aliases, resolution steps, generated hooks, unknowns, and stable exports."
    - "Go lifecycle/setup gaps remain deterministic capability diagnostics rather than synthetic semantic certainty."
    - "Go module roots are inferred or configured through existing lifecycle inputs without generated repository files."
  artifacts:
    - path: "crates/polint/src/symbol_graph/go.rs"
      provides: "Go conversion into semantic rows"
      contains: "derive_go_semantic_index"
    - path: "crates/go-sidecar/polint-go-symbols/internal/symbols/emit.go"
      provides: "Go sidecar semantic output fields"
      contains: "Scopes"
  key_links:
    - from: "crates/polint/src/symbol_graph/go.rs"
      to: "crates/polint/src/symbol_graph/semantic.rs"
      via: "SemanticIndexBuilder insertion APIs"
      pattern: "SemanticIndexBuilder"
---

<objective>
Deepen the Go side of the semantic index using the existing Go lifecycle and sidecar-backed typed extraction path.

Purpose: This implements D-03, D-08, D-09, D-12, D-13, D-14, D-15, D-17, D-20, and D-21 for Go while preserving monorepo lifecycle behavior.
Output: Go semantic rows behind `polint.symbol_graph`, with setup-missing and unsupported states represented honestly.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/26-semantic-index-deepening/26-CONTEXT.md
@research/semantic-index/RECOMMENDED_IMPLEMENTATION.md
@research/semantic-index/VALIDATION.md
@AGENTS.md
@.agents/skills/rust-best-practices/SKILL.md
@crates/polint/src/symbol_graph/go.rs
@crates/go-sidecar/polint-go-symbols/main.go
@crates/go-sidecar/polint-go-symbols/internal/symbols/emit.go
@crates/polint/src/go/lifecycle.rs

<interfaces>
Existing Go sidecar conversion path:

```rust
fn derive_go_symbols_with_runner(
    builder: &mut SymbolGraphBuilder,
    db: &AnalysisDb,
    loaded: &LoadedConfig,
    plan: &AnalysisPlan,
    runner: impl FnOnce(&GoAnalysisConfig) -> Result<Vec<u8>, GoSidecarFailure>,
) -> LanguageSymbolOutput;
```

Go setup gaps must keep using:

```rust
CapabilitySupportStatus::SetupMissing
Diagnostic::error("polint/capability", ...)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Go sidecar semantic output</name>
  <files>crates/go-sidecar/polint-go-symbols/main.go, crates/go-sidecar/polint-go-symbols/internal/symbols/emit.go, crates/polint/src/symbol_graph/go.rs</files>
  <read_first>
    - crates/go-sidecar/polint-go-symbols/main.go
    - crates/go-sidecar/polint-go-symbols/internal/symbols/emit.go
    - crates/polint/src/symbol_graph/go.rs
    - crates/polint/src/go/lifecycle.rs
    - .planning/phases/26-semantic-index-deepening/26-CONTEXT.md
    - research/semantic-index/RECOMMENDED_IMPLEMENTATION.md
  </read_first>
  <behavior>
    - Test: Sidecar JSON includes package, file, function, method, block, and type scopes for a Go fixture.
    - Test: Sidecar JSON includes import alias kind for named, dot, and blank imports.
    - Test: Sidecar JSON includes stable object path values for exported package-level declarations.
  </behavior>
  <action>Extend the embedded Go sidecar output schema string to `polint-go-symbols-semantic-1`. Add JSON arrays named exactly `scopes`, `imports`, `exports`, and `resolution_steps` beside existing `symbols`, `definitions`, and `references`. Each scope row must include `key`, `parent_key`, `kind`, `package_path`, `file`, and `span`. Each Go import row must include `path`, `local_name`, `alias_kind` with values `named`, `dot`, `blank`, or `implicit`, plus `file` and `span`. Each export row must include `symbol_key`, `export_name`, `namespace`, `object_path`, `package_path`, and `generated` boolean. Each resolution step row must include `reference_key`, `step`, `status`, `target_key`, and `candidate_keys`. Update Rust `GoSidecarOutput` deserialization to accept these arrays with default-empty handling and to reject unknown schema strings except the new schema string. Do not write or require generated `go.work` or other lifecycle files in repository roots.</action>
  <verify>
    <automated>cargo test -p polint --lib symbol_graph::go::sidecar_semantic_output --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg "polint-go-symbols-semantic-1" crates/go-sidecar/polint-go-symbols crates/polint/src/symbol_graph/go.rs` finds the new sidecar schema string.
    - `rg "scopes|imports|exports|resolution_steps" crates/go-sidecar/polint-go-symbols/internal/symbols/emit.go crates/polint/src/symbol_graph/go.rs` finds every semantic array name.
    - `rg "named|dot|blank|implicit" crates/go-sidecar/polint-go-symbols/internal/symbols/emit.go crates/polint/src/symbol_graph/go.rs` finds every Go alias kind.
    - `rg "go\\.work|os\\.WriteFile|ioutil\\.WriteFile" crates/go-sidecar/polint-go-symbols/internal/symbols/emit.go crates/polint/src/symbol_graph/go.rs` returns no new lifecycle-file write path.
  </acceptance_criteria>
  <done>The Go sidecar emits semantic rows needed by polint without changing the Go lifecycle contract or writing generated repo files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert Go semantic output into normalized facts</name>
  <files>crates/polint/src/symbol_graph/go.rs, crates/polint/src/symbol_graph/semantic.rs</files>
  <read_first>
    - crates/polint/src/symbol_graph/go.rs
    - crates/polint/src/symbol_graph/semantic.rs
    - crates/polint/src/symbol_graph/model.rs
    - crates/polint/src/core/mod.rs
    - docs/facts/symbols-and-references.md
    - .planning/phases/26-semantic-index-deepening/26-CONTEXT.md
  </read_first>
  <behavior>
    - Test: Go sidecar scope rows become `ScopeFact` rows with deterministic parent links.
    - Test: Go alias, dot, and blank imports become `SemanticImportFact` and `AliasFact` rows with honest statuses.
    - Test: Exported Go declarations become `StableExportIdentity` rows keyed by package path and object path.
  </behavior>
  <action>Add a Rust helper named `derive_go_semantic_index` called from `convert_sidecar_output`. It must use the same `SemanticIndexBuilder` APIs defined by Plan 26-02: instantiate `SemanticIndexBuilder::new`, route scopes through `add_scope`, imports through `add_semantic_import`, aliases through `add_alias`, stable exports through `add_export_identity`, and return the deterministic builder `finish` output to the symbol graph derivation path. Convert sidecar scopes into `ScopeFact` rows using `ScopeKind` labels `package`, `file`, `function`, `method`, `block`, and `type`. Convert sidecar imports into `SemanticImportFact` rows with `SemanticImportKind` labels `go_named`, `go_dot`, `go_blank`, and `go_implicit`. Convert aliases for named imports to `AliasFact` rows with `AliasKind::ImportAlias`; dot imports must produce alias rows with `SemanticStatus::Ambiguous` unless the sidecar target list has exactly one exported candidate. Blank imports must produce semantic import rows but no alias target. Convert exports into `StableExportIdentity` rows using generated discriminator `native`. Preserve existing `SymbolFact` and `ReferenceFact` conversion and public query behavior.</action>
  <verify>
    <automated>cargo test -p polint --lib symbol_graph::go::semantic_conversion --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg "derive_go_semantic_index" crates/polint/src/symbol_graph/go.rs` finds the helper.
    - `rg "SemanticIndexBuilder::new|add_scope|add_semantic_import|add_alias|add_export_identity|finish" crates/polint/src/symbol_graph/go.rs` proves Go extraction uses the shared builder APIs.
    - `rg "go_named|go_dot|go_blank|go_implicit" crates/polint/src/symbol_graph/go.rs crates/polint/src/symbol_graph/semantic.rs` finds every Go semantic import kind.
    - `rg "StableExportIdentity|object_path|generated_discriminator.*native" crates/polint/src/symbol_graph/go.rs crates/polint/src/symbol_graph/semantic.rs` finds stable export conversion.
    - `rg "pub struct Scope|pub fn scopes\\(" crates/polint/src/sdk crates/polint/src/lib.rs` returns no matches for public scope promotion.
  </acceptance_criteria>
  <done>Go sidecar semantic output is normalized into internal semantic rows with stable identities and no public API expansion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Preserve Go setup-missing and unresolved semantic states</name>
  <files>crates/polint/src/symbol_graph/go.rs</files>
  <read_first>
    - crates/polint/src/symbol_graph/go.rs
    - crates/polint/src/go/lifecycle.rs
    - crates/polint/src/analysis_plan.rs
    - docs/facts/symbols-and-references.md
    - AGENTS.md
    - .planning/phases/26-semantic-index-deepening/26-CONTEXT.md
  </read_first>
  <behavior>
    - Test: Files outside module roots still produce `polint/capability` setup diagnostics and setup-missing reference rows.
    - Test: Go references with no sidecar target produce internal `UnknownFallback` rows and public `SetupMissing` or `Unresolved` status as appropriate.
    - Test: Configured multi-module roots continue to avoid generated repository files.
  </behavior>
  <action>Keep the existing `setup_missing_output` flow as the only setup-failure path. When setup fails for requested symbols/references, add semantic `ResolutionFact` rows through `SemanticIndexBuilder::add_resolution` only if a file-level setup-missing reference row is already emitted; use `ResolutionStepKind::UnknownFallback` and `SemanticStatus::SetupMissing`. For sidecar references with no `target_key` and no candidates, emit `UnknownFallback` with `Unresolved`. For sidecar references with more than one candidate, emit `Ambiguous`. Ensure all setup diagnostics keep rule id `polint/capability`, include docs path `docs/facts/symbols-and-references.md`, and include hints from the Go lifecycle contract. Do not suppress setup diagnostics behind semantic rows.</action>
  <verify>
    <automated>cargo test -p polint --lib symbol_graph::go::semantic_setup_missing --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg "polint/capability" crates/polint/src/symbol_graph/go.rs` still finds capability diagnostics.
    - `rg "UnknownFallback|SemanticStatus::SetupMissing|SemanticStatus::Unresolved|SemanticStatus::Ambiguous" crates/polint/src/symbol_graph/go.rs` finds explicit unknown handling.
    - `rg "docs/facts/symbols-and-references.md" crates/polint/src/symbol_graph/go.rs` finds the docs path.
    - `rg "go\\.work|WriteFile|create_dir_all.*go" crates/polint/src/symbol_graph/go.rs crates/go-sidecar/polint-go-symbols` returns no new generated lifecycle-file path.
  </acceptance_criteria>
  <done>Go semantic deepening preserves deterministic setup diagnostics, monorepo lifecycle rules, and explicit unknown/ambiguous rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Go sidecar process to Rust host | Sidecar JSON is external process output consumed by polint. |
| Go lifecycle config to semantic extraction | User repo layout and `.polint.toml` lifecycle inputs determine package loading. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03-01 | Tampering | Go sidecar JSON | mitigate | Validate schema string, normalize/reject unsafe paths through existing path validation, and default missing optional arrays to empty. |
| T-26-03-02 | Denial of Service | alias/candidate conversion | mitigate | Sort and deduplicate sidecar candidate keys and do not run unbounded lookup loops in Go conversion. |
| T-26-03-03 | Repudiation | setup-missing Go lifecycle | mitigate | Preserve `polint/capability` diagnostics and add semantic setup-missing rows only as evidence, never as a replacement. |
</threat_model>

<verification>
Run:
- `cargo test -p polint --lib symbol_graph::go::sidecar_semantic_output --locked`
- `cargo test -p polint --lib symbol_graph::go::semantic_conversion --locked`
- `cargo test -p polint --lib symbol_graph::go::semantic_setup_missing --locked`
- `cargo fmt --all -- --check`
</verification>

<success_criteria>
Plan succeeds when Go semantic rows cover scopes, imports, aliases, exports, resolution unknowns, and stable export identities while Go setup failures remain visible capability diagnostics and no generated lifecycle files are required.
</success_criteria>

<output>
After completion, create `.planning/phases/26-semantic-index-deepening/26-03-SUMMARY.md`.
</output>
