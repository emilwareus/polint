---
phase: 26-semantic-index-deepening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/polint/src/symbol_graph/semantic.rs
  - crates/polint/src/symbol_graph/mod.rs
  - crates/polint/src/core/mod.rs
  - crates/polint/src/analysis_kernel/metadata.rs
  - crates/polint/src/analysis_kernel/provider.rs
autonomous: true
requirements: [SAE-SEM-01]
must_haves:
  truths:
    - "Internal semantic rows can represent scopes, imports, exports, aliases, resolution steps, generated symbols, unknowns, and stable export identities."
    - "New semantic rows are crate-private and do not create a public semantic graph API."
    - "Semantic row stable keys are deterministic and separate from run-local IDs."
  artifacts:
    - path: "crates/polint/src/symbol_graph/semantic.rs"
      provides: "Internal semantic fact model and stable-key helpers"
      contains: "pub(crate) struct ScopeFact"
    - path: "crates/polint/src/core/mod.rs"
      provides: "AnalysisDb storage and crate-private accessors for semantic rows"
      contains: "replace_semantic_index_facts"
    - path: "crates/polint/src/analysis_kernel/metadata.rs"
      provides: "FactFamily variants for semantic metadata rows"
      contains: "Scope"
  key_links:
    - from: "crates/polint/src/symbol_graph/mod.rs"
      to: "crates/polint/src/symbol_graph/semantic.rs"
      via: "crate-private module registration"
      pattern: "pub\\(crate\\) mod semantic"
    - from: "crates/polint/src/core/mod.rs"
      to: "crates/polint/src/analysis_kernel/metadata.rs"
      via: "FactFamily metadata recording"
      pattern: "FactFamily::Scope"
---

<objective>
Create the internal semantic-index contracts that later language providers, validation, cache restore, and fixtures build on.

Purpose: Phase 26 needs richer semantic facts without promoting a public graph/query surface. This plan establishes the crate-private model and database boundary per D-01, D-02, D-06, D-07, D-08, D-09, D-12, D-15, D-24, and D-27.
Output: Internal semantic fact structs, stable identity helpers, AnalysisDb storage/accessors, metadata family vocabulary, and updated symbol provider manifest schema.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/26-semantic-index-deepening/26-CONTEXT.md
@research/semantic-index/FINAL-REPORT.md
@research/semantic-index/RECOMMENDED_IMPLEMENTATION.md
@research/semantic-index/VALIDATION.md
@AGENTS.md
@.agents/skills/rust-best-practices/SKILL.md
@docs/API-VISIBILITY-PLAN.md
@crates/polint/src/symbol_graph/model.rs
@crates/polint/src/symbol_graph/stable_id.rs
@crates/polint/src/core/mod.rs
@crates/polint/src/analysis_kernel/metadata.rs
@crates/polint/src/analysis_kernel/provider.rs

<interfaces>
Existing contracts to preserve:

```rust
pub(crate) fn stable_key_from_parts(family: FactFamily, parts: &[(&str, String)]) -> String;
pub(crate) fn replace_symbol_graph_facts(
    &mut self,
    symbols: Vec<SymbolFact>,
    definitions: Vec<DefinitionFact>,
    references: Vec<ReferenceFact>,
);
```

The current symbol graph layer schema constant lives in `crates/polint/src/symbol_graph/model.rs` and must be advanced by this phase when semantic payload fields are added.

Current symbol provider manifest:

```rust
ProviderManifest {
    id: "polint.symbol_graph",
    kind: ProviderKind::WholeRepoDerived,
    outputs: &["symbols", "definitions", "references"],
    language_scope: LanguageScope::MultiLanguage,
    cache_policy: CachePolicy::InMemoryDerived,
    precision_ceiling: PrecisionCeiling::SetupAware,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add crate-private semantic fact contracts</name>
  <files>crates/polint/src/symbol_graph/semantic.rs, crates/polint/src/symbol_graph/mod.rs</files>
  <read_first>
    - crates/polint/src/symbol_graph/model.rs
    - crates/polint/src/symbol_graph/stable_id.rs
    - crates/polint/src/symbol_graph/mod.rs
    - .planning/phases/26-semantic-index-deepening/26-CONTEXT.md
    - research/semantic-index/RECOMMENDED_IMPLEMENTATION.md
    - AGENTS.md
  </read_first>
  <behavior>
    - Test: Stable keys for two equivalent scope facts are identical across insertion order.
    - Test: Stable export identities include language, module/package context, export name, namespace, symbol stable key, and generated discriminator when present.
    - Test: Alias closure status rows can represent `Resolved`, `Ambiguous`, `Unresolved`, `Cycle`, `Generated`, `Dynamic`, `External`, `SetupMissing`, and `Unsupported`.
  </behavior>
  <action>Create `crates/polint/src/symbol_graph/semantic.rs` with only `pub(crate)` types. Define ID newtypes `ScopeId(u64)`, `SemanticImportId(u64)`, `ExportId(u64)`, `AliasId(u64)`, `ResolutionId(u64)`, `GeneratedSymbolId(u64)`, and `StableExportId(u64)`. Define structs `ScopeFact`, `SemanticImportFact`, `ExportFact`, `AliasFact`, `ResolutionFact`, `GeneratedSymbolFact`, and `StableExportIdentity`. Define enums `ScopeKind`, `SemanticImportKind`, `ExportKind`, `AliasKind`, `ResolutionStepKind`, `SemanticStatus`, and `GeneratedSymbolKind`. Required `SemanticStatus` variants are exactly `Resolved`, `Ambiguous`, `Unresolved`, `Cycle`, `Generated`, `Dynamic`, `External`, `SetupMissing`, and `Unsupported`. Add stable-key constructors using `stable_key_from_parts` with concrete labels: `language`, `scope_path`, `file`, `package`, `module`, `name`, `namespace`, `kind`, `symbol_stable_key`, `export_name`, `generated_discriminator`, and `status`. Register the module from `symbol_graph/mod.rs` as `pub(crate) mod semantic;`. Do not add any `pub mod semantic`, SDK prelude exports, CLI command, docs page that describes these rows as supported public facts, or rule-author examples importing this module.</action>
  <verify>
    <automated>cargo test -p polint --lib symbol_graph::semantic --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg "pub\\(crate\\) mod semantic" crates/polint/src/symbol_graph/mod.rs` finds the module registration.
    - `rg "pub struct|pub enum|pub mod semantic" crates/polint/src/symbol_graph/semantic.rs` returns no matches.
    - `rg "pub\\(crate\\) struct (ScopeFact|SemanticImportFact|ExportFact|AliasFact|ResolutionFact|GeneratedSymbolFact|StableExportIdentity)" crates/polint/src/symbol_graph/semantic.rs` finds every listed struct.
    - `rg "Resolved|Ambiguous|Unresolved|Cycle|Generated|Dynamic|External|SetupMissing|Unsupported" crates/polint/src/symbol_graph/semantic.rs` finds every status variant.
    - `rg "stable_key_from_parts" crates/polint/src/symbol_graph/semantic.rs` finds semantic stable-key construction.
  </acceptance_criteria>
  <done>Semantic contracts exist internally, deterministic stable-key tests pass, and no public semantic module or SDK export is introduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Store semantic rows inside AnalysisDb with metadata families</name>
  <files>crates/polint/src/core/mod.rs, crates/polint/src/analysis_kernel/metadata.rs</files>
  <read_first>
    - crates/polint/src/core/mod.rs
    - crates/polint/src/analysis_kernel/metadata.rs
    - crates/polint/src/analysis_kernel/validation.rs
    - crates/polint/src/symbol_graph/semantic.rs
    - .planning/phases/21-provenance-precision-and-validation-metadata/21-CONTEXT.md
    - .planning/phases/26-semantic-index-deepening/26-CONTEXT.md
  </read_first>
  <behavior>
    - Test: `replace_semantic_index_facts` replaces all semantic rows, rebuilds deterministic ID indexes, and removes stale metadata for replaced semantic families.
    - Test: Missing metadata detection reports semantic rows if the refresh path is intentionally bypassed in a test.
  </behavior>
  <action>Add crate-private semantic storage to `AnalysisDb`: vectors for scopes, semantic imports, exports, aliases, resolution facts, generated symbols, and stable export identities. Add crate-private borrowed accessors named exactly `scopes()`, `semantic_imports()`, `exports()`, `aliases()`, `resolution_facts()`, `generated_symbols()`, and `stable_exports()`. Add `replace_semantic_index_facts(scopes, semantic_imports, exports, aliases, resolutions, generated_symbols, stable_exports)` that assigns deterministic run IDs from sorted stable keys and records metadata for each row. Extend `FactFamily` with variants exactly `Scope`, `SemanticImport`, `Export`, `Alias`, `Resolution`, `GeneratedSymbol`, and `StableExport`. Metadata for these rows must use producer/layer id `polint.symbol_graph`, validation `NativeTrusted`, and precision/confidence mapped from each row's `SemanticStatus`: `Resolved` and `Generated` use `SetupAware`/`High`, `Ambiguous` uses `Ambiguous`/`Medium`, `Unresolved` uses `Unresolved`/`Medium`, `Cycle`, `Dynamic`, and `Unsupported` use `Unsupported` or `Heuristic` with `Low`, `External` uses `SetupAware`/`Medium`, and `SetupMissing` uses `SetupMissing`/`High`. Keep accessors crate-private; do not expose these rows through `sdk/facts.rs`.</action>
  <verify>
    <automated>cargo test -p polint --lib semantic_index_storage --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg "replace_semantic_index_facts" crates/polint/src/core/mod.rs` finds the replacement API.
    - `rg "pub\\(crate\\) fn (scopes|semantic_imports|exports|aliases|resolution_facts|generated_symbols|stable_exports)" crates/polint/src/core/mod.rs` finds all seven crate-private accessors.
    - `rg "Scope|SemanticImport|Export|Alias|Resolution|GeneratedSymbol|StableExport" crates/polint/src/analysis_kernel/metadata.rs` finds the new `FactFamily` variants and labels.
    - `rg "pub struct Scope|pub struct SemanticImport|pub struct ExportFact" crates/polint/src/core/mod.rs crates/polint/src/sdk/facts.rs` returns no matches for newly internal semantic structs.
  </acceptance_criteria>
  <done>AnalysisDb can hold and metadata-track internal semantic rows without widening the public SDK or crate-root API.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update symbol provider manifest for semantic outputs</name>
  <files>crates/polint/src/analysis_kernel/provider.rs</files>
  <read_first>
    - crates/polint/src/analysis_kernel/provider.rs
    - crates/polint/src/analysis_kernel/incremental/input_snapshot.rs
    - crates/polint/src/analysis_kernel/mod.rs
    - .planning/phases/20-private-analysis-kernel-facade/20-CONTEXT.md
    - .planning/phases/26-semantic-index-deepening/26-CONTEXT.md
  </read_first>
  <behavior>
    - Test: Provider order remains exactly `polint.source`, `polint.go.syntax`, `polint.ts.syntax`, `polint.module_graph`, `polint.symbol_graph`, `polint.metrics`.
    - Test: `polint.symbol_graph` manifest outputs include old rows plus `scopes`, `semantic_imports`, `exports`, `aliases`, `resolution_facts`, `generated_symbols`, and `stable_exports`.
  </behavior>
  <action>Change only the existing `polint.symbol_graph` manifest per D-02 and D-04. Keep provider id `polint.symbol_graph`, kind `WholeRepoDerived`, language scope `MultiLanguage`, cache policy `InMemoryDerived`, and precision ceiling `SetupAware`. Change `SYMBOL_GRAPH_SCHEMA` to schema name `symbol-graph-facts-2` with version `2`. Expand manifest outputs to exactly include the existing `symbols`, `definitions`, `references` plus `scopes`, `semantic_imports`, `exports`, `aliases`, `resolution_facts`, `generated_symbols`, and `stable_exports`. Do not split provider order, do not add `polint.semantic.*` provider ids, and do not expose provider inspection outside existing cfg(test) helpers.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis_kernel::provider --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg "symbol-graph-facts-2" crates/polint/src/analysis_kernel/provider.rs` finds the updated schema name.
    - `rg "\"scopes\"|\"semantic_imports\"|\"exports\"|\"aliases\"|\"resolution_facts\"|\"generated_symbols\"|\"stable_exports\"" crates/polint/src/analysis_kernel/provider.rs` finds all semantic output labels.
    - `rg "polint\\.semantic|semantic_graph" crates/polint/src/analysis_kernel/provider.rs` returns no matches.
    - The provider order test asserts the six existing provider ids in the existing order.
  </acceptance_criteria>
  <done>The provider manifest truthfully declares semantic outputs while preserving the current kernel execution order and internal-only inspection boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parser/provider facts to AnalysisDb | Language adapters and future generated hooks produce semantic rows that enter shared kernel storage. |
| AnalysisDb internals to public SDK/JSON | Internal semantic rows must not leak into stable public rule-author or CLI contracts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01-01 | Tampering | `replace_semantic_index_facts` | mitigate | Sort by stable key, assign deterministic run IDs, record metadata for every row, and reuse stable-key conflict validation. |
| T-26-01-02 | Information Disclosure | `symbol_graph::semantic` | mitigate | Keep all new fact structs and accessors `pub(crate)` and add grep acceptance criteria blocking SDK/prelude/public module exposure. |
| T-26-01-03 | Spoofing | semantic stable keys | mitigate | Stable keys include language, context, namespace/kind, symbol/export name, and generated discriminator instead of transient parser IDs or spans alone. |
</threat_model>

<verification>
Run:
- `cargo test -p polint --lib symbol_graph::semantic --locked`
- `cargo test -p polint --lib semantic_index_storage --locked`
- `cargo test -p polint --lib analysis_kernel::provider --locked`
- `cargo fmt --all -- --check`
</verification>

<success_criteria>
Plan succeeds when semantic contracts, AnalysisDb storage, metadata families, and provider manifest outputs exist internally; provider order is unchanged; and grep checks prove no public semantic API was introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/26-semantic-index-deepening/26-01-SUMMARY.md`.
</output>
