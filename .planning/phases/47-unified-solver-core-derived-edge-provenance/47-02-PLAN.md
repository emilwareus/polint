---
phase: 47-unified-solver-core-derived-edge-provenance
plan: 02
type: execute
wave: 2
depends_on: ["47-01"]
files_modified:
  - crates/polint/src/analysis/ids.rs
  - crates/polint/src/analysis_kernel/metadata.rs
  - crates/polint/src/analysis/solver/provenance.rs
  - crates/polint/src/analysis/solver/facts.rs
  - crates/polint/src/analysis/solver/store.rs
  - crates/polint/src/analysis/solver/mod.rs
  - crates/polint/src/cli/mod.rs
autonomous: true
requirements: [GRAPH-04]
must_haves:
  truths:
    - "Every solver-derived edge carries a DerivedEdgeProvenance with contributing fact IDs totally ordered by stable ID, the producing constraint kind, and the solver step."
    - "Derived-edge facts have a dedicated FactFamily, dense IDs assigned after stable-key sort, and reject FactPrecision::Exact."
    - "The solver store normalizes derived edges deterministically (shuffle-stable)."
    - "polint explain can surface, for a derived edge, its contributing facts + constraint kind + solver step via existing private plumbing (no new public CLI/JSON surface)."
    - "A property test asserts deleting any single contributing fact invalidates the derived edge."
    - "Implements 47-CONTEXT.md decisions D-08, D-09, and D-10: the DerivedEdgeProvenance struct (contributing fact IDs totally ordered by stable ID, constraint kind, solver step), the deletion-invalidation property test, and polint explain consumption via existing private plumbing."
  artifacts:
    - path: "crates/polint/src/analysis/solver/provenance.rs"
      provides: "DerivedEdgeProvenance with the three roadmap-named fields"
      contains: "DerivedEdgeProvenance"
    - path: "crates/polint/src/analysis/solver/facts.rs"
      provides: "Derived-edge fact family"
      contains: "pub(crate)"
    - path: "crates/polint/src/analysis/solver/store.rs"
      provides: "Deterministic derived-edge output + dense-IDs-after-sort"
      contains: "normalized"
  key_links:
    - from: "crates/polint/src/analysis/solver/provenance.rs"
      to: "crates/polint/src/analysis/semantic_graph/constraints.rs"
      via: "reuse ConstraintKind::as_str() as the producing-constraint-kind field"
      pattern: "ConstraintKind"
    - from: "crates/polint/src/cli/mod.rs"
      to: "crates/polint/src/analysis/solver/provenance.rs"
      via: "explain private plumbing surfaces DerivedEdgeProvenance"
      pattern: "[Dd]erived"
---

<objective>
Make every solver-derived edge carry a `DerivedEdgeProvenance` (GRAPH-04): contributing
fact IDs totally ordered by stable ID, the producing `ConstraintKind`, and the monotonic
solver step. Add the derived-edge fact family + deterministic store, wire `polint explain`
to surface provenance through existing private plumbing, and prove provenance is
load-bearing with a deletion property test.

Purpose: provenance must be sound and load-bearing, not decorative — the property test
(D-09) is the proof. Contributing facts are total-ordered by stable ID (the Phase 42 dedup
rule) so provenance is itself byte-stable.

Output: `solver/{provenance,facts,store}.rs`, new ID newtypes + FactFamily entries, and an
`explain` seam. Provider/cache/validate/fixture wiring lands in Plan 03.
</objective>

<execution_context>
@/Users/emilwareus/conductor/workspaces/exlint/islamabad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/conductor/workspaces/exlint/islamabad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-unified-solver-core-derived-edge-provenance/47-CONTEXT.md
@.planning/phases/47-unified-solver-core-derived-edge-provenance/47-PATTERNS.md
@.planning/phases/47-unified-solver-core-derived-edge-provenance/47-01-SUMMARY.md

<interfaces>
<!-- Contracts the executor builds on. Extracted from the codebase; do not re-explore. -->

From crates/polint/src/analysis/ids.rs (lines 155-178):
- Dense ID newtype pattern `pub(crate) struct XId(pub(crate) u64)`. Any ID whose owning fact carries `#[serde(skip)]` on its `id` field MUST derive `Default` (the `SemanticConstraintId` block lines 170-178 shows this). New newtypes register in `assert_small_id_contract` (lines 204-258).

From crates/polint/src/analysis_kernel/metadata.rs:
- `pub(crate) enum FactFamily { ... PointsToSet ... }` (PointsToSet line 87, its `label()` arm line 190). Reserved-but-unproduced families use `#[expect(dead_code, reason = "...")]` naming the phase (lines 89-92).
- `stable_key_from_parts(family, &[(&str, String)]) -> String` (lines 370-384): length-prefixed, label-sorted, family-tagged, backslash-normalized. The recipe for ordering contributing fact IDs by stable key.

From crates/polint/src/analysis/semantic_graph/constraints.rs:
- `pub(crate) enum ConstraintKind { CopyEdge, Alloc, FieldLoad, FieldStore, CallConstraint, ModelEdge, TypeConstraint }` with `fn as_str(&self) -> &'static str` (stable snake_case, line 84) — reuse as the provenance constraint-kind field.
- `ConstraintFact { #[serde(skip)] id, kind, status, precision, stable_key }` (lines 178-188) — the fact shape to mirror for derived edges.

From crates/polint/src/analysis/semantic_graph/store.rs:
- `SemanticGraphOutput` (line 19), `normalized()` (line 35: stable-key sort then assign dense IDs), `from_output` (line 108: deterministic kind indexes + referential validation), shuffle-stability tests (`normalized_is_shuffle_stable` line 320, `normalized_constraints_are_shuffle_stable` line 460), `SEMANTIC_GRAPH_PROVIDER_ID` const (line 10).

From crates/polint/src/analysis/points_to/facts.rs:
- Shared `PointsToStatus` / `PointsToPrecision` vocabulary to reuse (do not invent redundant status/precision enums). `PointsToStatus::BudgetExceeded` line 82.

From crates/polint/src/analysis/solver/engine.rs (Plan 01):
- The engine exposes a monotonic `u64` step counter per worklist step (the provenance solver-step source).

From crates/polint/src/cli/mod.rs (lines 1331-1370):
- `explain(...)` is a RULE-CAPABILITY planner today (discovers local rule hosts, emits `ExplainReport`/`ExplainRuleRow` lines 1574-1584), NOT a fact/edge inspector. D-10 extends the EXISTING PRIVATE plumbing reached from this command; it is NOT a new public CLI surface (the only v1.3 public CLI surface is `polint inspect unknowns`, Phase 52). Confirm the exact private seam; add no public JSON schema field.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ID newtypes + FactFamily entries + DerivedEdgeProvenance (D-08)</name>
  <files>crates/polint/src/analysis/ids.rs, crates/polint/src/analysis_kernel/metadata.rs, crates/polint/src/analysis/solver/provenance.rs, crates/polint/src/analysis/solver/mod.rs</files>
  <read_first>
    - crates/polint/src/analysis/ids.rs (lines 155-178 newtype + Default pattern; lines 204-258 `assert_small_id_contract`)
    - crates/polint/src/analysis_kernel/metadata.rs (FactFamily enum lines 6-115, PointsToSet line 87, label() arm line 190, dead-code reserve pattern lines 89-92, stable_key_from_parts lines 370-384)
    - crates/polint/src/analysis/semantic_graph/constraints.rs (lines 84-94 `as_str()`; lines 178-188 ConstraintFact shape)
    - crates/polint/src/analysis/stable_key.rs (lines 16-18 stable-key wrapper recipe)
  </read_first>
  <behavior>
    - DerivedEdgeProvenance stores contributing fact IDs in a total order by stable ID; two provenances built from the same contributing facts in different input order are equal/byte-identical.
    - The constraint-kind field equals `ConstraintKind::as_str()` for the producing kind.
    - The solver-step field is the monotonic u64 from the engine.
    - A new FactFamily variant tags derived-edge (and provenance, if separate) facts; its `label()` arm returns a stable string.
  </behavior>
  <action>
    Add solver dense-ID newtypes to `crates/polint/src/analysis/ids.rs` (e.g. a derived-edge
    ID and, if needed, a provenance ID) following the `SemanticConstraintId` block (lines
    170-178): derive `Default` for any ID whose owning fact `#[serde(skip)]`s its `id`, and
    register each newtype in `assert_small_id_contract` (lines 204-258).
    Add a `FactFamily` variant per new solver fact family (derived edge; and provenance if it
    is a separate family) in `crates/polint/src/analysis_kernel/metadata.rs`, each with a
    `label()` arm; if a family is reserved-but-unproduced until Plan 03 use the
    `#[expect(dead_code, reason = "...")]` pattern naming Phase 47.
    Create `crates/polint/src/analysis/solver/provenance.rs` defining `pub(crate) struct
    DerivedEdgeProvenance` with the three roadmap-named fields (D-08): (1) contributing fact
    IDs TOTALLY ORDERED BY STABLE ID — reference EXISTING stable identities (composition over
    duplication), ordering them via the `stable_key_from_parts` length-prefixed recipe (Phase
    42 dedup total-order rule); (2) the producing constraint kind — reuse
    `ConstraintKind::as_str()` (do not mint a parallel enum); (3) the solver step — the
    monotonic `u64` from the engine. Add a unit test asserting provenance built from the same
    contributing facts in shuffled input order is equal (total-order is shuffle-stable).
    Declare `pub(crate) mod provenance;` in `solver/mod.rs`.
  </action>
  <verify>
    <automated>cargo test -p polint --lib solver::provenance 2>&1 | tail -15 && cargo test -p polint --lib ids:: 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `crates/polint/src/analysis/solver/provenance.rs` defines `pub(crate) struct DerivedEdgeProvenance` carrying contributing-fact IDs (total-ordered by stable ID), a constraint-kind field sourced from `ConstraintKind::as_str()`, and a `u64` solver step.
    - New ID newtypes are registered in `assert_small_id_contract` (test passes).
    - New `FactFamily` variant(s) have `label()` arms; `cargo test -p polint --lib metadata` (or the FactFamily label test) passes.
    - The provenance shuffle-stability unit test passes: `cargo test -p polint --lib solver::provenance`.
    - All new types are `pub(crate)`.
  </acceptance_criteria>
  <done>DerivedEdgeProvenance carries the three roadmap-named fields, is total-ordered byte-stable, has dense IDs + FactFamily tags registered, and reuses existing ConstraintKind/stable-key machinery.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Derived-edge fact family + deterministic solver store (facts.rs, store.rs)</name>
  <files>crates/polint/src/analysis/solver/facts.rs, crates/polint/src/analysis/solver/store.rs, crates/polint/src/analysis/solver/mod.rs</files>
  <read_first>
    - crates/polint/src/analysis/semantic_graph/constraints.rs (lines 178-188 ConstraintFact `{ #[serde(skip)] id, kind, status, precision, stable_key }`)
    - crates/polint/src/analysis/semantic_graph/store.rs (line 10 PROVIDER_ID const; line 19 Output; line 35 `normalized()`; line 108 `from_output`; lines 320 + 460 shuffle-stability tests)
    - crates/polint/src/analysis/points_to/facts.rs (PointsToStatus/PointsToPrecision to reuse; PointsToStatus::BudgetExceeded line 82)
    - crates/polint/src/analysis/solver/provenance.rs (DerivedEdgeProvenance from Task 1)
  </read_first>
  <behavior>
    - A derived-edge fact carries its DerivedEdgeProvenance, a status/precision reusing the shared points-to vocabulary, and a stable key; its dense `id` is `#[serde(skip)]` and never enters the output digest.
    - Derived edges reject `FactPrecision::Exact` (honest precision ceiling, D-06).
    - `normalized()` sorts by stable key then assigns dense IDs; shuffling input rows yields byte-identical normalized output.
  </behavior>
  <action>
    Create `crates/polint/src/analysis/solver/facts.rs` defining the derived-edge fact family,
    mirroring `ConstraintFact` (lines 178-188): `{ #[serde(skip)] id, ..., status, precision,
    stable_key, provenance: DerivedEdgeProvenance }`. Reuse the shared `PointsToStatus` /
    `PointsToPrecision` vocabulary (do NOT invent redundant status/precision enums). The dense
    `id` carries `#[serde(skip)]` so it never enters the digest; serde restores via `Default`.
    Derived edges must reject `FactPrecision::Exact` (D-06) — represent at most a SetupAware/
    conservative precision.
    Create `crates/polint/src/analysis/solver/store.rs` mirroring `semantic_graph/store.rs`:
    a `SolverOutput` carrying derived-edge rows, `normalized()` (stable-key sort then assign
    dense IDs), and a `SolverStore::from_output` building deterministic indexes with
    referential validation. Add a `SOLVER_PROVIDER_ID = "polint.solver"` const (mirroring
    `SEMANTIC_GRAPH_PROVIDER_ID` line 10). Add shuffle-stability tests mirroring
    `normalized_is_shuffle_stable` (line 320) and `normalized_constraints_are_shuffle_stable`
    (line 460): shuffled input rows produce byte-identical normalized output. Declare
    `pub(crate) mod facts;` and `pub(crate) mod store;` in `solver/mod.rs`. Wire the engine
    (Plan 01) to emit derived-edge facts with provenance into `SolverOutput` (each derived
    edge records its contributing facts, producing ConstraintKind, and the worklist step).
  </action>
  <verify>
    <automated>cargo test -p polint --lib solver::store 2>&1 | tail -15 && cargo test -p polint --lib solver::facts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `facts.rs` defines a `pub(crate)` derived-edge fact carrying `DerivedEdgeProvenance`, reusing `PointsToStatus`/`PointsToPrecision`, with `#[serde(skip)]` on the dense `id`.
    - Derived edges never carry `FactPrecision::Exact` (a test or validation asserts the precision ceiling).
    - `store.rs` defines `SOLVER_PROVIDER_ID = "polint.solver"`, `normalized()` (sort-then-dense-ID), and `from_output` indexes.
    - Shuffle-stability tests pass: `cargo test -p polint --lib solver::store`.
    - The engine emits derived-edge facts with provenance into SolverOutput.
  </acceptance_criteria>
  <done>Derived-edge facts + a shuffle-stable solver store exist; edges carry provenance, reuse shared status/precision, reject Exact, and are byte-deterministic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: polint explain consumption (D-10) + deletion property test (D-09)</name>
  <files>crates/polint/src/cli/mod.rs, crates/polint/src/analysis/solver/provenance.rs</files>
  <read_first>
    - crates/polint/src/cli/mod.rs (lines 1331-1370 `explain(...)`; lines 1574-1584 `ExplainReport`/`ExplainRuleRow`) — confirm the private seam; this is a PARTIAL analog (no current edge-provenance consumer)
    - crates/polint/src/analysis/solver/store.rs + provenance.rs (the data explain surfaces)
    - crates/polint/src/analysis/points_to/solver.rs (lines 392-395 `assert_eq!(first, second)` — the determinism assertion shape for the property test)
    - crates/polint/tests/public_surface_leak.rs (ALLOWED_PRELUDE const line 42 — must NOT be extended)
  </read_first>
  <behavior>
    - Given a derived edge, explain's private plumbing can return its contributing facts + constraint kind + solver step.
    - No new field appears in the public ExplainReport JSON schema (provenance stays pub(crate)/internal).
    - Deletion property test: re-running the solver with any single contributing fact removed does NOT reproduce the derived edge (provenance is load-bearing).
  </behavior>
  <action>
    Wire `DerivedEdgeProvenance` into the EXISTING private plumbing reached from the `explain`
    command in `crates/polint/src/cli/mod.rs` (lines 1331-1370), per D-10: for a derived edge,
    surface its contributing facts + constraint kind + solver step. Keep ALL provenance types
    `pub(crate)`; add NO new public JSON schema field to `ExplainReport`/`ExplainRuleRow`
    (this is NOT a new public CLI surface — the only v1.3 public CLI surface is `polint
    inspect unknowns`, Phase 52). Confirm the exact private seam first (PATTERNS.md flags this
    as the one partial analog); the consumption can be an internal accessor / cfg(test)-facing
    surface if no production explain path consumes edges yet, provided the plumbing exists and
    is exercised by a test.
    Add the GRAPH-04 acceptance property test (D-09): construct a constraint set the solver
    derives an edge from, capture the derived edge; then delete any ONE contributing fact (as
    recorded in its DerivedEdgeProvenance) and re-run the solver — assert the derived edge is
    NOT reproduced (use the `assert_eq`/`assert_ne` determinism-assertion shape from
    points_to/solver.rs lines 392-395). This proves provenance is sound and load-bearing.
  </action>
  <verify>
    <automated>cargo test -p polint provenance 2>&1 | tail -15 && cargo test -p polint public_surface_leak 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - The `explain` private plumbing can surface a derived edge's contributing facts + constraint kind + solver step; a test exercises it.
    - No new public field in `ExplainReport`/`ExplainRuleRow`; `ALLOWED_PRELUDE` in `public_surface_leak.rs` is unchanged.
    - The deletion property test passes: removing any single contributing fact prevents the derived edge from being reproduced (`cargo test -p polint provenance`).
    - `cargo test -p polint public_surface_leak` passes (all solver/provenance types stay pub(crate)).
  </acceptance_criteria>
  <done>polint explain consumes DerivedEdgeProvenance via existing private plumbing with no public surface added, and the deletion property test proves provenance is load-bearing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| solver edges → explain private plumbing | Provenance flows from in-process solver output to the existing private explain seam. No new public CLI/JSON surface; consumers are internal/test-facing. No untrusted input crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-05 | Information Disclosure | `cli/mod.rs` explain provenance seam | mitigate | D-10 keeps all provenance types `pub(crate)` and adds NO public JSON schema field; leak gate (`public_surface_leak.rs`) asserts no solver type reaches `polint::sdk::prelude::*`. ALLOWED_PRELUDE untouched. |
| T-47-06 | Tampering | provenance total-order / derived-edge determinism | mitigate | Contributing facts total-ordered by stable ID via `stable_key_from_parts`; dense IDs after sort; `#[serde(skip)]` on dense `id` keeps it out of digests. Shuffle-stability + deletion property tests lock soundness. |
| T-47-07 | Repudiation | provenance soundness (decorative-only risk) | mitigate | Deletion property test (D-09) proves provenance is load-bearing: removing any contributing fact invalidates the derived edge, so provenance cannot silently misattribute. |
| T-47-SC | Tampering | npm/pip/cargo installs | mitigate | No new package-manager installs in this plan. No `[ASSUMED]`/`[SUS]` packages; no install task. |
</threat_model>

<verification>
- `cargo build -p polint` succeeds.
- `cargo test -p polint --lib solver::` passes (provenance, facts, store).
- `cargo test -p polint provenance` passes (deletion property test).
- `cargo test -p polint public_surface_leak` passes; `ALLOWED_PRELUDE` unchanged.
- `cargo test -p polint points_to` still byte-identical.
</verification>

<success_criteria>
- DerivedEdgeProvenance carries contributing fact IDs (total-ordered by stable ID), the producing ConstraintKind, and the solver step.
- Derived-edge facts + a shuffle-stable store exist; edges reject Exact precision.
- polint explain surfaces provenance via existing private plumbing with no public surface added.
- The deletion property test proves provenance is load-bearing.
</success_criteria>

<output>
Create `.planning/phases/47-unified-solver-core-derived-edge-provenance/47-02-SUMMARY.md` when done.
</output>
