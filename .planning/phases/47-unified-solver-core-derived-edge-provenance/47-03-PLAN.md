---
phase: 47-unified-solver-core-derived-edge-provenance
plan: 03
type: execute
wave: 3
depends_on: ["47-02"]
files_modified:
  - crates/polint/src/analysis/solver/cache_key.rs
  - crates/polint/src/analysis/solver/validate.rs
  - crates/polint/src/analysis/solver/provider.rs
  - crates/polint/src/analysis/solver/mod.rs
  - crates/polint/src/analysis_kernel/provider.rs
  - crates/polint/src/analysis_kernel/mod.rs
  - crates/polint/src/analysis/semantic_graph/provider.rs
  - tests/eval-fixtures/provenance/cycle-detection
autonomous: true
requirements: [GRAPH-03, GRAPH-04]
must_haves:
  truths:
    - "A private polint.solver provider is registered in the kernel manifest, slotted after polint.semantic_graph and before polint.refined_calls."
    - "The polint.solver cache key digests upstream provider output digests (semantic_graph + points-to source families), provider/schema version, AND the SolverBudget."
    - "Validation rejects derived edges with FactPrecision::Exact and dangling/duplicate rows; a cycle-detection check/fixture proves no solver to summary loop is admitted."
    - "All ~7 provider-order snapshot/ordering assertions are updated to include polint.solver and pass."
    - "The Phase 43 determinism gate stays green (10-shuffle byte-identical observed JSON) and the public-surface-leak gate stays green."
    - "Implements 47-CONTEXT.md decisions D-11, D-12, D-13, D-14, and D-15: the dependency-contract enforcement, the cycle-detection fixture proving no solver-to-summary loop, the polint.solver provider slot, the determinism-gate inheritance, and the budget-digesting cache key."
  artifacts:
    - path: "crates/polint/src/analysis/solver/provider.rs"
      provides: "polint.solver provider: derive derived edges + provenance, output digest"
      contains: "polint.solver"
    - path: "crates/polint/src/analysis/solver/cache_key.rs"
      provides: "SOLVER_SCHEMA_LABEL + parameter digest including SolverBudget"
      contains: "SOLVER_SCHEMA_LABEL"
    - path: "crates/polint/src/analysis/solver/validate.rs"
      provides: "Validation pass + cycle-detection / precision-ceiling checks"
      contains: "pub(crate)"
    - path: "tests/eval-fixtures/provenance/cycle-detection"
      provides: "Fixture proving no solver to summary loop is admitted"
  key_links:
    - from: "crates/polint/src/analysis_kernel/provider.rs"
      to: "crates/polint/src/analysis/solver/provider.rs"
      via: "polint.solver ProviderManifest after semantic_graph, before refined_calls"
      pattern: "polint\\.solver"
    - from: "crates/polint/src/analysis_kernel/mod.rs"
      to: "crates/polint/src/analysis/solver/provider.rs"
      via: "dispatch block calling solver::provider::derive_* with semantic_graph_output_digest"
      pattern: "solver::provider"
---

<objective>
Wire the `analysis::solver` core into the kernel: register a private `polint.solver`
provider (slotted after `polint.semantic_graph`, before `polint.refined_calls` — the slot
Phase 44 reserved), give it a cache key that digests upstream + budgets (D-15), add a
validation pass with the cycle-detection check + precision ceiling, ship the
cycle-detection fixture (D-12), update all ~7 provider-order snapshot assertions, and prove
the Phase 43 determinism gate and Phase 42 leak gate stay green.

Purpose: completes GRAPH-03 (provider/cache/determinism/leak acceptance criteria SC2/SC4/SC5)
and GRAPH-04 (provider emits derived edges + provenance into the reserved slot Phase 52
reads). This plan makes the solver a live, deterministic, cached, validated kernel provider.

Output: `solver/{provider,cache_key,validate}.rs`, kernel manifest/dispatch edits, ~7 updated
snapshot sites, and the cycle-detection fixture.
</objective>

<execution_context>
@/Users/emilwareus/conductor/workspaces/exlint/islamabad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/conductor/workspaces/exlint/islamabad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-unified-solver-core-derived-edge-provenance/47-CONTEXT.md
@.planning/phases/47-unified-solver-core-derived-edge-provenance/47-PATTERNS.md
@.planning/phases/47-unified-solver-core-derived-edge-provenance/47-01-SUMMARY.md
@.planning/phases/47-unified-solver-core-derived-edge-provenance/47-02-SUMMARY.md

<interfaces>
<!-- Contracts the executor builds on. Extracted from the codebase; do not re-explore. -->

From crates/polint/src/analysis/semantic_graph/cache_key.rs:
- `SEMANTIC_GRAPH_SCHEMA_LABEL` (line 4 pattern), `*_provider_parameter_digest()` listing frozen algorithm-version strings (lines 49-67), a locked "parts list" trip-wire test (lines 73-95), and an "algorithm-version bump invalidates" test (lines 97-116).

From crates/polint/src/analysis/semantic_graph/provider.rs:
- `derive_*_with_cache_stats` returns `*ProviderRunOutput { diagnostics, cache_stats, output_digest }`; on store error returns `output_digest: None` (lines 23-108).
- `*_output_digest` folds provider/version/schema/parameter digests + every consumed upstream provider output digest + per-row stable-key parts, then `parts.sort()` + `Digest::from_parts` (lines 127-234).
- Slot-assertion test `provider_manifests_list_semantic_graph_between_type_value_alias_and_refined_calls` (lines 564-573) — copy to add the solver neighbor assertion AND update this existing one (it now asserts tva+2 == polint.solver, tva+3 == polint.refined_calls).

From crates/polint/src/analysis/semantic_graph/validate.rs:
- Emits an evidence-bearing `Diagnostic` per problem (duplicate stable keys, dangling endpoints, non-contiguous dense IDs, precision-ceiling violations). The precision-ceiling rejection of the exact tier (doc lines 18-21) is the "derived edges reject FactPrecision::Exact" template.

From crates/polint/src/analysis_kernel/provider.rs:
- `polint.type_value_alias` manifest id line 610; `polint.semantic_graph` entry lines 657-687 (id line 658); `polint.refined_calls` entry lines 688-711 (id line 689). `SEMANTIC_GRAPH_SCHEMA` const lines 220-223.
- The ~7 snapshot/order sites (from 47-PATTERNS.md): (1) `provider_order_matches_behavior_preserving_kernel_sequence` lines 827-855 (insert after line 848 "polint.semantic_graph"); (2) `symbol_graph_manifest_declares_semantic_outputs_without_reordering_providers` line 879; (3) `module_graph_manifest_declares_base_topology_outputs_without_reordering_providers` line 937; (4) `provider_order_report_for_test` ProviderOrderRow snapshot lines 1349-1385 (insert between 1350 semantic_graph and 1366 refined_calls); (5) semantic_graph's own slot assertion `semantic_graph/provider.rs` lines 564-573; (6) `provider_manifests_cover_existing_kernel_providers` in `analysis_kernel/mod.rs` ~line 1990 (add polint.solver to expected id set); (7) any further `provider_order_for_test()` / required-metadata loops — run full `cargo test -p polint` and fix every failing ordered vec. Treat ~7 as a floor.

From crates/polint/src/analysis_kernel/mod.rs:
- semantic_graph dispatch block lines 542-572; `semantic_graph_output_digest` produced line 565 is the primary upstream digest the solver consumes. Add a parallel solver block after it: `derive_*_with_cache_stats(&mut db, ..., Self::provider_manifest("polint.solver"), <upstream digests incl. semantic_graph_output_digest>)` then `provider_outputs.push(Self::provider_output_for_with_optional_digest("polint.solver", ...))`. Thread the SolverBudget into the call (D-15).

From crates/polint/src/eval/determinism_gate.rs:
- `PERMUTATION_RUNS = 10` (line 62); the shuffled provider set comes from `AnalysisKernel::provider_manifests()`, so `polint.solver` AUTO-ENROLLS once registered (D-14). NO harness edit needed. Fixtures: `tests/eval-fixtures/determinism/{go,ts}_reachable` must stay green on Linux + macOS.

From crates/polint/tests/public_surface_leak.rs:
- `ALLOWED_PRELUDE` const (line 42) — must NOT be extended (D-16).

From crates/polint/src/analysis/solver/store.rs (Plan 02):
- `SOLVER_PROVIDER_ID = "polint.solver"`, `SolverOutput`, `SolverStore::from_output`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: solver cache_key + validate (D-12, D-15, precision ceiling)</name>
  <files>crates/polint/src/analysis/solver/cache_key.rs, crates/polint/src/analysis/solver/validate.rs, crates/polint/src/analysis/solver/mod.rs</files>
  <read_first>
    - crates/polint/src/analysis/semantic_graph/cache_key.rs (line 4 SCHEMA_LABEL; lines 49-67 parameter digest; lines 73-95 parts-list trip-wire test; lines 97-116 algorithm-bump test)
    - crates/polint/src/analysis/semantic_graph/validate.rs (lines 1-30 head; doc lines 18-21 precision-ceiling pattern)
    - crates/polint/src/analysis/solver/budget.rs + store.rs + facts.rs (Plans 01/02: SolverBudget, SolverStore, derived-edge facts)
    - crates/polint/src/analysis/summaries (awareness only: summaries are an INPUT to the solver, never re-fed into the same fixpoint — the cycle the D-12 check rejects)
  </read_first>
  <behavior>
    - The solver parameter digest includes SolverBudget knobs, so a budget-default change changes the digest (algorithm-bump-style test passes).
    - Validation emits a diagnostic for any derived edge carrying FactPrecision::Exact (precision ceiling, D-06).
    - Validation emits diagnostics for duplicate stable keys / dangling endpoints / non-contiguous dense IDs.
    - The cycle-detection check detects a constraint set that would create a solver to summary to solver cycle and rejects/bounds it rather than diverging (D-12).
  </behavior>
  <action>
    Create `crates/polint/src/analysis/solver/cache_key.rs` mirroring
    `semantic_graph/cache_key.rs`: define `SOLVER_SCHEMA_LABEL`, a
    `solver_provider_parameter_digest()` listing frozen algorithm-version strings, and ADD the
    SolverBudget knobs to the parameter parts so a budget-default change is captured (D-15).
    Add the locked "parts list" trip-wire test and the "algorithm-version bump invalidates"
    test, plus a test proving a SolverBudget change changes the parameter digest.
    Create `crates/polint/src/analysis/solver/validate.rs` mirroring
    `semantic_graph/validate.rs`: emit an evidence-bearing `Diagnostic` per problem —
    duplicate stable keys, dangling derived-edge endpoints, non-contiguous dense IDs, and the
    precision-ceiling violation (reject `FactPrecision::Exact` on derived edges, D-06).
    Implement the D-12 cycle-detection check here: prove no solver to summary loop is admitted
    — summaries are an INPUT snapshot to the solver, never re-fed into the same fixpoint as
    they are produced; a constraint set that would create a solver to summary to solver cycle
    is detected/bounded (or rejected with a diagnostic) rather than diverging. Add a unit test
    exercising the cycle case (the fixture in Task 3 is the native-tree counterpart).
    Declare `pub(crate) mod cache_key;` and `pub(crate) mod validate;` in `solver/mod.rs`.
  </action>
  <verify>
    <automated>cargo test -p polint --lib solver::cache_key 2>&1 | tail -10 && cargo test -p polint --lib solver::validate 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cache_key.rs` defines `SOLVER_SCHEMA_LABEL` and a parameter digest that includes SolverBudget; the parts-list trip-wire, algorithm-bump, and budget-change tests pass.
    - `validate.rs` emits diagnostics for Exact-precision derived edges, duplicate keys, dangling endpoints, and non-contiguous IDs.
    - The cycle-detection check rejects/bounds a solver to summary to solver constraint set (unit test passes): `cargo test -p polint --lib solver::validate`.
    - All new types are `pub(crate)`.
  </acceptance_criteria>
  <done>solver cache_key digests budgets (D-15) and validate enforces the precision ceiling + cycle detection (D-12) with locked tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: polint.solver provider + kernel manifest/dispatch + update ~7 snapshot sites (D-13, D-15)</name>
  <files>crates/polint/src/analysis/solver/provider.rs, crates/polint/src/analysis_kernel/provider.rs, crates/polint/src/analysis_kernel/mod.rs, crates/polint/src/analysis/semantic_graph/provider.rs, crates/polint/src/analysis/solver/mod.rs</files>
  <read_first>
    - crates/polint/src/analysis/semantic_graph/provider.rs (entire file: lines 23-108 run entry + ProviderRunOutput; lines 127-234 output digest; lines 564-573 slot-assertion test)
    - crates/polint/src/analysis_kernel/provider.rs (id line 610 type_value_alias; lines 657-687 semantic_graph manifest entry; lines 688-711 refined_calls; SCHEMA const lines 220-223; the ~7 snapshot sites: lines 827-855, 879, 937, 1349-1385)
    - crates/polint/src/analysis_kernel/mod.rs (lines 542-572 semantic_graph dispatch; `semantic_graph_output_digest` line 565; `provider_manifests_cover_existing_kernel_providers` ~line 1990)
    - crates/polint/src/analysis/solver/{store.rs,cache_key.rs,validate.rs} (SOLVER_PROVIDER_ID, schema label, validation)
  </read_first>
  <behavior>
    - The kernel registers `polint.solver` between `polint.semantic_graph` and `polint.refined_calls`; provider-order assertions reflect this.
    - The solver provider's output digest folds provider/version/schema/parameter digests + consumed upstream output digests (semantic_graph + points-to source families) + SolverBudget + per-row stable-key parts; on store error it returns `output_digest: None`.
    - `cargo test -p polint` passes with every provider-order/snapshot site updated.
  </behavior>
  <action>
    Create `crates/polint/src/analysis/solver/provider.rs` mirroring
    `semantic_graph/provider.rs`: a `derive_*_with_cache_stats` entry that builds the
    `SolverOutput` (drives the engine over the closed input snapshot, emits derived edges +
    provenance), validates via `solver::validate`, persists, and returns
    `ProviderRunOutput { diagnostics, cache_stats, output_digest }` (output_digest: None on
    store error). Implement `*_output_digest` folding (a) provider/version/schema/parameter
    digests, (b) every consumed upstream provider output digest — at minimum
    `polint.semantic_graph` plus the points-to source families (`points_to_constraints` /
    `points_to_sets` from `polint.type_value_alias`), (c) the SolverBudget (D-15), (d) per-row
    stable-key parts, then `parts.sort()` + `Digest::from_parts`.
    In `crates/polint/src/analysis_kernel/provider.rs`: add a `polint.solver`
    `ProviderManifest` AFTER `polint.semantic_graph` (line 687) and BEFORE
    `polint.refined_calls` (line 688), and a `SOLVER_SCHEMA` const referencing
    `solver::cache_key::SOLVER_SCHEMA_LABEL`.
    In `crates/polint/src/analysis_kernel/mod.rs`: add a dispatch block AFTER the
    semantic_graph block (lines 542-572) calling
    `crate::analysis::solver::provider::derive_*_with_cache_stats(&mut db, ...,
    Self::provider_manifest("polint.solver"), <upstream digests incl.
    semantic_graph_output_digest>)`, threading the SolverBudget (D-15), then
    `provider_outputs.push(Self::provider_output_for_with_optional_digest("polint.solver",
    ...))`.
    Update ALL ~7 provider-order/snapshot sites to include `polint.solver` after
    `polint.semantic_graph`: sites 1-4 are hand-maintained ordered vecs in `provider.rs`
    (lines 848, 879, 937, and the ProviderOrderRow snapshot 1349-1385); site 5 is the
    neighbor slot-assertion in `semantic_graph/provider.rs` lines 564-573 (now tva+2 ==
    polint.solver, tva+3 == polint.refined_calls) PLUS add a parallel
    `provider_manifests_list_solver_between_semantic_graph_and_refined_calls` test; site 6 is
    `provider_manifests_cover_existing_kernel_providers` in `analysis_kernel/mod.rs` (~line
    1990, add polint.solver). Treat ~7 as a floor: run the FULL `cargo test -p polint` and fix
    every failing ordered-vec/snapshot assertion until green. Declare `pub(crate) mod
    provider;` in `solver/mod.rs`.
  </action>
  <verify>
    <automated>cargo test -p polint 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - A `polint.solver` ProviderManifest is registered after `polint.semantic_graph` and before `polint.refined_calls`.
    - The solver output digest folds upstream digests (semantic_graph + points-to source families) + SolverBudget + schema/version + per-row stable keys; store error yields `output_digest: None`.
    - The new `provider_manifests_list_solver_between_semantic_graph_and_refined_calls` test passes and the updated semantic_graph neighbor assertion passes.
    - All ~7 ordered-vec/snapshot sites are updated; FULL `cargo test -p polint` passes.
    - All new types are `pub(crate)`.
  </acceptance_criteria>
  <done>polint.solver is a live kernel provider in the reserved slot, digests upstream + budgets, and every provider-order snapshot assertion passes.</done>
</task>

<task type="auto">
  <name>Task 3: cycle-detection fixture + determinism gate green + leak gate green (D-12, D-14, D-16; SC2/SC4/SC5)</name>
  <files>tests/eval-fixtures/provenance/cycle-detection</files>
  <read_first>
    - tests/eval-fixtures/provenance/ and tests/eval-fixtures/determinism/ (existing native fixture-tree layout + README) and tests/eval-fixtures/semantic-graph/ (a peer fixture to mirror structure)
    - crates/polint/src/eval/determinism_gate.rs (lines 1-44 doc; PERMUTATION_RUNS=10 line 62; auto-enrollment via provider_manifests())
    - crates/polint/tests/public_surface_leak.rs (ALLOWED_PRELUDE line 42 — must stay unchanged)
    - crates/polint/src/analysis/solver/validate.rs (the cycle-detection check from Task 1 the fixture exercises)
  </read_first>
  <action>
    Create the D-12 cycle-detection fixture under
    `tests/eval-fixtures/provenance/cycle-detection` (mirror the existing native fixture-tree
    structure under `tests/eval-fixtures/provenance/` / `semantic-graph/`): a constraint set
    that would create a solver to summary to solver cycle, with expectations proving the
    solver DETECTS/BOUNDS it (summaries are an input snapshot, never re-fed into the same
    fixpoint) rather than diverging — the concrete mechanism behind D-11 closed-input-set /
    single-fixpoint-per-run. Wire it into the eval fixture runner the same way peer
    provenance/semantic-graph fixtures are wired.
    Verify the Phase 43 determinism gate stays green: the `polint.solver` provider
    AUTO-ENROLLS via `provider_manifests()` (D-14, NO harness edit) — run the determinism gate
    and confirm `tests/eval-fixtures/determinism/{go,ts}_reachable` produce byte-identical
    observed JSON across the 10 seeded provider-order shuffles, with identical solver step
    counts and budget-exceeded reasons. If the gate is red, the fix is in the solver's BTree
    accumulation / dense-IDs-after-sort discipline (Plan 01/02), NOT the gate harness.
    Verify the Phase 42 public-surface-leak gate stays green: run `public_surface_leak` and
    confirm NO solver type (`SolverBudget`, `BudgetStatus`, `SolverPolicy`,
    `DerivedEdgeProvenance`, derived-edge facts, the provider) is reachable from
    `polint::sdk::prelude::*`; `ALLOWED_PRELUDE` is UNCHANGED (D-16).
  </action>
  <verify>
    <automated>cargo test -p polint determinism 2>&1 | tail -12 && cargo test -p polint cycle 2>&1 | tail -8 && cargo test -p polint public_surface_leak 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `tests/eval-fixtures/provenance/cycle-detection` exists and its test proves no solver to summary loop is admitted (detected/bounded, not divergent).
    - The Phase 43 determinism gate passes: 10-shuffle byte-identical observed JSON with `polint.solver` enrolled; `determinism/{go,ts}_reachable` green. No edit to `determinism_gate.rs`.
    - The Phase 42 leak gate passes: `cargo test -p polint public_surface_leak` green; `ALLOWED_PRELUDE` unchanged.
    - FULL `cargo test -p polint` passes.
  </acceptance_criteria>
  <done>The cycle-detection fixture proves the closed-input contract, and both the determinism and leak gates stay green with the solver provider live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream provider digests → solver cache key | The solver cache key consumes in-process upstream output digests + budgets. No external/untrusted input; cache identity is deterministic and internal. |
| solver provider output → reserved provider slot | Emits derived edges + provenance into the `polint.refined_calls`-preceding slot for Phase 52 to read later. No public surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-08 | Denial of Service | solver provider on adversarial/cyclic constraint sets | mitigate | SolverBudget bounds outer iterations (D-11) and the validate.rs cycle-detection check + fixture (D-12) prove no solver to summary loop diverges; summaries are an input snapshot, never re-fed into the same fixpoint. |
| T-47-09 | Tampering | cache identity correctness | mitigate | Output digest folds upstream digests + SolverBudget + schema/version + sorted per-row stable keys (D-15); a budget change invalidates downstream. Parts-list trip-wire + algorithm-bump tests lock the recipe. |
| T-47-10 | Tampering | non-deterministic observed JSON | mitigate | Provider auto-enrolls in the Phase 43 determinism gate (D-14); 10-shuffle byte-identical observed JSON is a named acceptance criterion. BTree accumulation + dense-IDs-after-sort make it hold. |
| T-47-11 | Information Disclosure | solver types leaking to public SDK | mitigate | Phase 42 leak gate asserts no solver type reaches `polint::sdk::prelude::*`; all types `pub(crate)`; `ALLOWED_PRELUDE` unchanged (D-16). |
| T-47-SC | Tampering | npm/pip/cargo installs | mitigate | No new package-manager installs in this plan. No `[ASSUMED]`/`[SUS]` packages; no install task. |
</threat_model>

<verification>
- FULL `cargo test -p polint` passes (all ~7 provider-order snapshot sites updated).
- `cargo test -p polint determinism` passes; `determinism_gate.rs` unedited.
- `cargo test -p polint cycle` passes (cycle-detection fixture).
- `cargo test -p polint public_surface_leak` passes; `ALLOWED_PRELUDE` unchanged.
- `cargo test -p polint points_to` still byte-identical (fold preserved through provider integration).
- `cargo clippy -p polint --all-targets 2>&1 | tail -5` clean.
</verification>

<success_criteria>
- polint.solver provider is registered in the reserved slot (after semantic_graph, before refined_calls).
- Cache key digests upstream + SolverBudget; validation enforces precision ceiling + cycle detection.
- All ~7 provider-order snapshot assertions updated and passing.
- Phase 43 determinism gate green (10-shuffle byte-identical); Phase 42 leak gate green.
- Cycle-detection fixture proves no solver to summary loop is admitted.
</success_criteria>

<output>
Create `.planning/phases/47-unified-solver-core-derived-edge-provenance/47-03-SUMMARY.md` when done.
</output>
