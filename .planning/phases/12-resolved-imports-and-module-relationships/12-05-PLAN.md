---
phase: 12-resolved-imports-and-module-relationships
plan: 05
type: execute
wave: 4
depends_on: ["12-03", "12-04"]
files_modified:
  - crates/polint/tests/cli.rs
  - docs/facts/resolved-imports.md
  - docs/facts/README.md
  - crates/polint/src/cli/skill.rs
autonomous: true
requirements: [MOD-01, MOD-02, MOD-03, MOD-04]
must_haves:
  truths:
    - "An external repo-local rule can consume ResolvedImports<'_> and ModuleGraphFacts<'_> using only polint::sdk::prelude::*."
    - "Unresolved and setup-missing imports are visible in JSON diagnostics with explicit reasons."
    - "Docs explain public fields, query methods, setup-sensitive behavior, and limits without claiming exact semantic coverage."
  artifacts:
    - path: "crates/polint/tests/cli.rs"
      provides: "temp-repo external SDK proof for resolved imports and module graph facts"
      contains: "ResolvedImports<'_>"
    - path: "docs/facts/resolved-imports.md"
      provides: "public fact documentation for resolved imports and module graph facts"
      contains: "ResolutionStatus"
    - path: "docs/facts/README.md"
      provides: "fact reference link"
      contains: "Resolved imports"
    - path: "crates/polint/src/cli/skill.rs"
      provides: "generated skill guidance for the new public fact views"
      contains: "ResolvedImports<'_>"
  key_links:
    - from: "crates/polint/tests/cli.rs"
      to: "polint::sdk::prelude::*"
      via: "generated .polint/rules temp repo"
      pattern: "use polint::sdk::prelude::\\*;"
    - from: "docs/facts/README.md"
      to: "docs/facts/resolved-imports.md"
      via: "public docs link"
      pattern: "resolved-imports.md"
    - from: "crates/polint/src/cli/skill.rs"
      to: "SDK fact views"
      via: "generated skill text names ResolvedImports and ModuleGraphFacts"
      pattern: "ModuleGraphFacts<'_>"
---

<objective>
Prove the completed Phase 12 capability through external rule consumption and public documentation.

Purpose: MOD-01 through MOD-04 are only complete when repo-local rules can use the new typed views as external SDK consumers and unresolved/setup-sensitive behavior is documented honestly.
Output: CLI integration tests, fact docs, fact README link, generated skill text updates, and full phase verification commands.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-resolved-imports-and-module-relationships/12-CONTEXT.md
@docs/roadmap/12_RESOLVED_IMPORTS_MODULE_GRAPH_ARCHITECTURE.md
@AGENTS.md
@.planning/phases/12-resolved-imports-and-module-relationships/12-01-SUMMARY.md
@.planning/phases/12-resolved-imports-and-module-relationships/12-02-SUMMARY.md
@.planning/phases/12-resolved-imports-and-module-relationships/12-03-SUMMARY.md
@.planning/phases/12-resolved-imports-and-module-relationships/12-04-SUMMARY.md
@crates/polint/tests/cli.rs
@crates/polint/tests/common/mod.rs
@docs/facts/README.md
@crates/polint/src/cli/skill.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add external temp-repo SDK proof for module relationships</name>
  <read_first>
    - crates/polint/tests/cli.rs
    - crates/polint/tests/common/mod.rs
    - crates/polint/src/sdk/mod.rs
    - crates/polint/src/sdk/facts.rs
    - .planning/phases/12-resolved-imports-and-module-relationships/12-CONTEXT.md
    - AGENTS.md
  </read_first>
  <files>crates/polint/tests/cli.rs</files>
  <behavior>
    - Test: generated `.polint/rules` imports only `polint::sdk::prelude::*` and registers through `polint::runner::run_cli`.
    - Test: a rule requesting `ResolvedImports<'_>` reports a diagnostic for an unresolved import reason.
    - Test: a rule requesting `ModuleGraphFacts<'_>` reports a diagnostic for a forbidden local dependency edge.
    - Test: JSON output from `polint check --format json --fail-on none` contains rule-owned diagnostics with evidence from real relationship facts.
  </behavior>
  <action>Add a helper `write_module_relationship_rule_repo(root: &Path)` following existing temp-repo helpers. The generated local rule crate must use only `use polint::sdk::prelude::*;`, define one `#[polint::rule]` that requests `ResolvedImports<'_>` and one that requests `ModuleGraphFacts<'_>`, and register both through `polint::runner::run_cli(vec![...])` per D-02, D-04, and D-23. The fixture should include TS files where `src/ui/view.ts` imports `../domain/model` and `./missing`, plus a `tsconfig.json` alias case if Plan 12-03 exposed it through CLI. The resolved-import rule reports `local/unresolved-import` when `status != ResolutionStatus::Resolved && status != ResolutionStatus::External`, with evidence `status` and `reason`. The graph rule reports `local/no-ui-to-domain` when an outgoing edge from a file label under `src/ui/` reaches a file label under `src/domain/`. The test must run `polint check --format json --fail-on none`, assert parseable JSON, assert both local diagnostics exist, and assert diagnostic evidence includes `reason=NotFound` for the missing import and a target label containing `src/domain/model.ts` for the graph edge.</action>
  <verify>
    <automated>cargo test -p polint --test cli external_rule_consumes_module_relationship_facts_through_public_sdk --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "write_module_relationship_rule_repo|ResolvedImports<'_>|ModuleGraphFacts<'_>|local/unresolved-import|local/no-ui-to-domain" crates/polint/tests/cli.rs` finds the helper and both rules.
    - `rg -n "use polint::sdk::prelude::\\*;|polint::runner::run_cli" crates/polint/tests/cli.rs` finds public SDK imports and runner registration in the generated rule source.
    - `! rg -n "polint::core|crate::core|module_graph::|AnalysisDb" crates/polint/tests/cli.rs` succeeds for the generated rule source block.
    - `cargo test -p polint --test cli external_rule_consumes_module_relationship_facts_through_public_sdk --locked` passes.
  </acceptance_criteria>
  <done>Phase 12 has external-consumer proof that real resolved import and module graph facts are usable through the public SDK only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove setup-missing and deterministic output at CLI level</name>
  <read_first>
    - crates/polint/tests/cli.rs
    - crates/polint/src/module_graph/go.rs
    - crates/polint/src/runner/mod.rs
    - .planning/phases/12-resolved-imports-and-module-relationships/12-CONTEXT.md
  </read_first>
  <files>crates/polint/tests/cli.rs</files>
  <behavior>
    - Test: Go repo without go.mod and a rule requesting `ResolvedImports<'_>` emits `polint/capability` setup-missing diagnostics.
    - Test: the requesting rule does not emit its own diagnostic when Go setup is missing.
    - Test: repeated `polint check --format json --fail-on none` runs produce byte-identical JSON for the module relationship fixture.
  </behavior>
  <action>Add CLI tests covering D-12, D-17, D-24, and D-25. For setup missing, create a temp repo with `.polint/rules` requesting `ResolvedImports<'_>`, one Go file importing `example.com/app/internal/worker`, and no root `go.mod`; run `polint check --format json --fail-on none`, assert at least one `polint/capability` diagnostic has evidence `status=setup_missing`, and assert the local rule ID has zero diagnostics. For determinism, run the TS module relationship fixture from Task 1 three times with `--format json --fail-on none` and compare the parsed `diagnostics` arrays exactly. Keep assertions on JSON fields and evidence labels, not stdout substrings.</action>
  <verify>
    <automated>cargo test -p polint --test cli module_relationship_setup_missing_and_determinism --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "module_relationship_setup_missing_and_determinism|setup_missing|polint/capability|assert_eq!\\(first.*second|assert_eq!\\(second.*third" crates/polint/tests/cli.rs` finds the setup and determinism assertions.
    - `rg -n "diagnostic_has_evidence\\(.*status.*setup_missing|diagnostics_for_rule\\(&json, \"local/" crates/polint/tests/cli.rs` finds evidence-based checks.
    - `cargo test -p polint --test cli module_relationship_setup_missing_and_determinism --locked` passes.
  </acceptance_criteria>
  <done>CLI behavior proves setup-missing capabilities are deterministic and do not execute requesting rules with fabricated facts.</done>
</task>

<task type="auto">
  <name>Task 3: Document public fact contract and generated skill guidance</name>
  <read_first>
    - docs/facts/resolved-imports.md (create)
    - docs/facts/README.md
    - docs/facts/imports.md
    - docs/facts/capability-plans.md
    - crates/polint/src/cli/skill.rs
    - .planning/phases/12-resolved-imports-and-module-relationships/12-CONTEXT.md
  </read_first>
  <files>docs/facts/resolved-imports.md, docs/facts/README.md, crates/polint/src/cli/skill.rs</files>
  <action>Create `docs/facts/resolved-imports.md` documenting `ResolvedImports<'_>`, `ModuleGraphFacts<'_>`, `ResolvedImportFact`, `ModuleNode`, `ModuleEdge`, `ResolutionStatus`, `ResolutionPrecision`, `UnresolvedReason`, query methods, and setup-sensitive limits per D-03, D-05, D-11, D-13, D-20, and D-22. The docs must explicitly state: TS/JS resolution uses `oxc_resolver`; Go resolution uses `go list -json ./...` only when root `go.mod` exists; standard library and package-manager dependencies are `External`; unresolved/setup-missing/dynamic/unsupported imports remain visible; no TypeScript type checking, Go type checking, symbols, call graph, CFG, coverage, or project-level graph cache is included in this phase. Add a link named `Resolved imports and module graph` to `docs/facts/README.md`. Update generated skill text in `crates/polint/src/cli/skill.rs` to mention that architecture rules can request `ResolvedImports<'_>` and `ModuleGraphFacts<'_>` through `polint::sdk::prelude::*`, and keep the rule-authoring guidance free of manual `Capabilities::new()` examples.</action>
  <verify>
    <automated>cargo test -p polint --test cli add_skill_installs_claude_skill_non_interactively --locked
rg -n "ResolvedImports<'_>|ModuleGraphFacts<'_>|ResolutionStatus|UnresolvedReason|oxc_resolver|go list -json ./\\.\\.\\.|no TypeScript type checking|no project-level graph cache" docs/facts/resolved-imports.md
rg -n "resolved-imports\\.md|Resolved imports and module graph" docs/facts/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/facts/resolved-imports.md` succeeds.
    - `rg -n "ResolvedImports<'_>|ModuleGraphFacts<'_>|ResolvedImportFact|ModuleNode|ModuleEdge|ResolutionStatus|ResolutionPrecision|UnresolvedReason" docs/facts/resolved-imports.md` finds all public names.
    - `rg -n "oxc_resolver|go list -json ./\\.\\.\\.|External|SetupMissing|Dynamic|Unsupported|no TypeScript type checking|no Go type checking|no symbols|no call graph|no CFG|no coverage|no project-level graph cache" docs/facts/resolved-imports.md` finds honest limits and setup behavior.
    - `rg -n "Resolved imports and module graph.*resolved-imports\\.md" docs/facts/README.md` finds the fact reference link.
    - `rg -n "ResolvedImports<'_>|ModuleGraphFacts<'_>|polint::sdk::prelude::\\*" crates/polint/src/cli/skill.rs` finds the two views and public prelude guidance.
    - `! rg -n "Capabilities::new\\(\\)" crates/polint/src/cli/skill.rs` succeeds.
    - `cargo test -p polint --test cli add_skill_installs_claude_skill_non_interactively --locked` passes.
  </acceptance_criteria>
  <done>Public docs and generated skill guidance explain the Phase 12 fact contract honestly and point rule authors to typed SDK views.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external rule crate -> public SDK | Temp repos compile against `polint::sdk::prelude::*` as downstream consumers. |
| setup-missing diagnostics -> JSON report | Capability diagnostics become machine-readable output consumed by agents. |
| docs/skill text -> user behavior | Generated guidance shapes how rule authors use the new capability. |
| Go metadata command -> diagnostics | Setup failures from Plan 12-04 are asserted at CLI level. |
| resolver path handling -> reported evidence | Path labels from relationship facts appear in rule diagnostics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05-01 | S | external temp-repo tests | mitigate | Generated rules import only `polint::sdk::prelude::*` and register through `polint::runner::run_cli`; tests reject `polint::core` and module internals. |
| T-12-05-02 | R | setup-missing CLI behavior | mitigate | JSON tests assert `polint/capability` evidence `status=setup_missing` and absence of local rule diagnostics. |
| T-12-05-03 | I | documentation and skill output | mitigate | Docs describe normalized public facts and limits, not raw resolver output, absolute paths, or command stdout. |
| T-12-05-04 | T | generated diagnostics/evidence | mitigate | Tests assert evidence labels and deterministic JSON arrays across repeated runs. |
| T-12-05-05 | D | external rule compilation cost | accept | Temp-repo rule-host tests are slower but limited to focused external-consumer proof. |
| T-12-05-06 | E | Go command execution | mitigate | CLI proof relies on Plan 12-04 fixed-argument command path and verifies setup-missing behavior when command setup is absent. |
</threat_model>

<verification>
Run the task-level commands plus full phase gates:

```bash
cargo test -p polint --test cli external_rule_consumes_module_relationship_facts_through_public_sdk --locked
cargo test -p polint --test cli module_relationship_setup_missing_and_determinism --locked
cargo fmt --all -- --check
cargo clippy --workspace --all-targets --all-features --locked -- -D warnings
cargo test --workspace --all-features --locked
```
</verification>

<success_criteria>
- External rule-host tests prove `ResolvedImports<'_>` and `ModuleGraphFacts<'_>` work through public SDK imports only.
- Setup-missing capabilities are visible as deterministic diagnostics and do not run requesting rules with fabricated facts.
- Public docs and generated skill text describe the supported behavior, setup requirements, and limits truthfully.
</success_criteria>

<output>
After completion, create `.planning/phases/12-resolved-imports-and-module-relationships/12-05-SUMMARY.md`.
</output>
