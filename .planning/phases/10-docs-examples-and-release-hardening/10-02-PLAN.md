---
phase: 10-docs-examples-and-release-hardening
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/basic/README.md
  - examples/custom-rule-go/README.md
  - examples/custom-rule-ts/README.md
  - examples/go-branch-obligations/README.md
  - examples/go-branch-obligations/.polint.toml
  - examples/ts-design-tokens/README.md
  - examples/ts-design-tokens/.polint.toml
requirements:
  - FND-03
  - TEST-02
requirements_addressed:
  - FND-03
  - TEST-02
autonomous: true
must_haves:
  truths:
    - "`examples/` contains basic, Go custom rule, TS custom rule, Go branch obligation, and TS design token examples."
    - "Example docs are copyable and do not claim dynamic repo-local Rust rule loading in v1."
    - "Runnable example directories include minimal `.polint.toml` files where `polint check` is intended to run."
  artifacts:
    - path: "examples/"
      provides: "v1 user-facing examples"
  key_links:
    - from: ".planning/phases/10-docs-examples-and-release-hardening/10-CONTEXT.md"
      to: "examples/"
      via: "D-05 through D-08 examples decisions"
      pattern: "Expand the existing top-level `examples/` layout"
---

<objective>
Harden the top-level examples so each roadmap-required example has clear documentation and runnable/configured behavior where appropriate.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-docs-examples-and-release-hardening/10-CONTEXT.md
@README.md
@examples/
@crates/polint-cli/tests/cli.rs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Expand quickstart and custom-rule example docs</name>
  <files>examples/basic/README.md, examples/custom-rule-go/README.md, examples/custom-rule-ts/README.md</files>
  <read_first>
    - examples/basic/README.md
    - examples/custom-rule-go/README.md
    - examples/custom-rule-ts/README.md
    - README.md
  </read_first>
  <action>Update the three existing example READMEs:
  - `examples/basic/README.md` must include `polint init`, `polint check`, `polint check --profile full --format sarif`, and `cargo run -p polint-cli --`.
  - `examples/custom-rule-go/README.md` must include `polint new-rule go require-payment-error-tests`, `polint test-rules`, `ctx.go_tests()`, and `ctx.branch_obligations(function.id)`.
  - `examples/custom-rule-ts/README.md` must include `polint new-rule ts no-product-hex-colors`, `polint test-rules`, `ctx.string_literals()`, and `ctx.jsx_attributes()`.
  Each README must state that generated repo-local Rust rules are scaffolded for authoring/testing and are not automatically compiled or dynamically loaded by `polint check` in v1.</action>
  <verify>
    <automated>rg -n "polint init|polint check --profile full --format sarif|cargo run -p polint-cli --|polint new-rule go require-payment-error-tests|polint new-rule ts no-product-hex-colors|polint test-rules|ctx.go_tests\\(\\)|ctx.branch_obligations\\(function.id\\)|ctx.string_literals\\(\\)|ctx.jsx_attributes\\(\\)|not automatically compiled|dynamically loaded" examples/basic/README.md examples/custom-rule-go/README.md examples/custom-rule-ts/README.md</automated>
  </verify>
  <acceptance_criteria>
    - Basic README contains `polint check --profile full --format sarif`.
    - Go custom rule README contains `ctx.go_tests()` and `ctx.branch_obligations(function.id)`.
    - TS custom rule README contains `ctx.string_literals()` and `ctx.jsx_attributes()`.
    - Both custom-rule READMEs contain the v1 dynamic-loading limitation.
  </acceptance_criteria>
  <done>Existing example docs are useful and honest.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add Go branch obligation example docs and config</name>
  <files>examples/go-branch-obligations/README.md, examples/go-branch-obligations/.polint.toml</files>
  <read_first>
    - examples/go-branch-obligations/authorize.go
    - README.md
    - crates/polint-rules/src/lib.rs
  </read_first>
  <action>Create `examples/go-branch-obligations/README.md` and `examples/go-branch-obligations/.polint.toml`.
  The config must contain:
  ```toml
  [workspace]
  include = ["*.go"]
  exclude = []

  [profiles.fast]
  rules = ["examples/go-branch-obligations"]
  ```
  The README must include `examples/go-branch-obligations`, `polint check --profile fast --format json --fail-on none`, `No nearby test evidence found`, and `heuristic`.
  It must explain that this example intentionally has error branches without companion tests so the heuristic branch-obligation rule reports diagnostics.</action>
  <verify>
    <automated>test -f examples/go-branch-obligations/README.md && test -f examples/go-branch-obligations/.polint.toml</automated>
    <automated>rg -n "examples/go-branch-obligations|polint check --profile fast --format json --fail-on none|No nearby test evidence found|heuristic" examples/go-branch-obligations/README.md</automated>
    <automated>rg -n "include = \\[\"\\*.go\"\\]|rules = \\[\"examples/go-branch-obligations\"\\]" examples/go-branch-obligations/.polint.toml</automated>
  </verify>
  <acceptance_criteria>
    - Go branch example has README and `.polint.toml`.
    - Config includes `rules = ["examples/go-branch-obligations"]`.
    - README includes `heuristic` and `No nearby test evidence found`.
  </acceptance_criteria>
  <done>Go branch obligation example is documented and configured.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add TS design token example docs and config</name>
  <files>examples/ts-design-tokens/README.md, examples/ts-design-tokens/.polint.toml</files>
  <read_first>
    - examples/ts-design-tokens/Button.tsx
    - README.md
    - crates/polint-rules/src/lib.rs
  </read_first>
  <action>Create `examples/ts-design-tokens/README.md` and `examples/ts-design-tokens/.polint.toml`.
  The config must contain:
  ```toml
  [workspace]
  include = ["*.tsx"]
  exclude = []

  [profiles.fast]
  rules = ["examples/ts-no-raw-colors"]

  [[rules.config]]
  id = "examples/ts-no-raw-colors"
  severity = "error"
  files = ["*.tsx"]
  ```
  The README must include `examples/ts-design-tokens`, `examples/ts-no-raw-colors`, `polint check --profile fast --format json`, `#ff00aa`, and `data-color="#00ff00"`.
  It must explain that the rule detects syntax-level string/JSX color literals and does not prove design-token semantic correctness.</action>
  <verify>
    <automated>test -f examples/ts-design-tokens/README.md && test -f examples/ts-design-tokens/.polint.toml</automated>
    <automated>rg -n "examples/ts-design-tokens|examples/ts-no-raw-colors|polint check --profile fast --format json|#ff00aa|data-color=\"#00ff00\"|syntax-level" examples/ts-design-tokens/README.md</automated>
    <automated>rg -n "include = \\[\"\\*.tsx\"\\]|rules = \\[\"examples/ts-no-raw-colors\"\\]|id = \"examples/ts-no-raw-colors\"" examples/ts-design-tokens/.polint.toml</automated>
  </verify>
  <acceptance_criteria>
    - TS design-token example has README and `.polint.toml`.
    - Config includes `rules = ["examples/ts-no-raw-colors"]`.
    - README includes both literal examples and the syntax-level limitation.
  </acceptance_criteria>
  <done>TS design token example is documented and configured.</done>
</task>

</tasks>

<threat_model>
ASVS L1 posture: no high severity threat is accepted; examples must be safe to copy and must not misrepresent v1 capabilities.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| example docs -> user repository | Users may copy commands and config directly. |
| example source -> diagnostics | Example source intentionally triggers diagnostics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | Spoofing | custom-rule examples | mitigate | State dynamic loading limitation in custom-rule READMEs. |
| T-10-02-02 | Tampering | `.polint.toml` examples | mitigate | Keep configs minimal and explicit, with exact rule IDs. |
| T-10-02-03 | Repudiation | heuristic examples | mitigate | Go branch example README must include `heuristic`. |
| T-10-02-04 | Information Disclosure | examples | mitigate | Use only synthetic local source and no secrets. |
</threat_model>

<verification>
Run after completing all tasks in this plan:

```bash
find examples -maxdepth 2 -type f | sort
rg -n "not automatically compiled|heuristic|syntax-level|polint check --profile fast" examples
```
</verification>

<success_criteria>
- All five roadmap-required examples exist.
- Runnable examples have minimal configs.
- Example docs are honest and copyable.
</success_criteria>

<output>
After completion, create `.planning/phases/10-docs-examples-and-release-hardening/10-02-SUMMARY.md`.
</output>
