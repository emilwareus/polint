---
phase: 10-docs-examples-and-release-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
requirements:
  - FND-03
requirements_addressed:
  - FND-03
autonomous: true
must_haves:
  truths:
    - "README explains what polint is, why it exists, non-goals, quickstart, config, SDK authoring, capabilities, rule testing, CI usage, examples, experimental plugins, and roadmap."
    - "README keeps built-in rules framed as SDK examples, not a comprehensive lint pack."
    - "README does not claim dynamic repo-local Rust rule loading or Wasm plugin execution in `polint check` v1."
  artifacts:
    - path: "README.md"
      provides: "complete v1 user-facing documentation"
  key_links:
    - from: "docs/INITIAL_PROMPT.md"
      to: "README.md"
      via: "README requirements section"
      pattern: "README requirements"
---

<objective>
Complete the primary README so a first-time user can understand what polint is, install/run it, configure it, author a rule with the SDK, test rules, use CI output, and understand the honest v1 roadmap.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-docs-examples-and-release-hardening/10-CONTEXT.md
@docs/INITIAL_PROMPT.md
@README.md
@examples/
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fill README quickstart and command path</name>
  <files>README.md</files>
  <read_first>
    - README.md
    - docs/INITIAL_PROMPT.md
    - .planning/phases/10-docs-examples-and-release-hardening/10-CONTEXT.md
  </read_first>
  <action>Update `README.md` so it contains these exact user-facing headings or phrases:
  - `## Installation`
  - `## Quickstart`
  - `cargo install --path crates/polint-cli`
  - `polint init`
  - `polint new-rule go require-payment-error-tests`
  - `polint check`
  Keep the existing project overview and non-goal language. Add short text explaining that the repository is named `exlint` while the CLI/crates use `polint`.</action>
  <verify>
    <automated>rg -n "## Installation|## Quickstart|cargo install --path crates/polint-cli|polint init|polint new-rule go require-payment-error-tests|polint check" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README contains `## Installation`.
    - README contains `## Quickstart`.
    - README contains `cargo install --path crates/polint-cli`.
    - README contains the command sequence `polint init`, `polint new-rule go require-payment-error-tests`, and `polint check`.
  </acceptance_criteria>
  <done>README explains the initial user path.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Document configuration, SDK authoring, capabilities, and rule testing</name>
  <files>README.md</files>
  <read_first>
    - README.md
    - crates/polint-sdk/src/lib.rs
    - crates/polint-cli/tests/cli.rs
  </read_first>
  <action>Update `README.md` with concise sections covering:
  - `## Example Config`
  - `## Rule Authoring`
  - `## Capabilities`
  - `## Testing Rules`
  In `Rule Authoring`, keep a small Rust snippet using `use polint_sdk::prelude::*;`, `impl Rule`, `RuleMeta`, `Capabilities::new()`, and `fn run(&self, ctx: &mut RuleCtx<'_>) -> Result<()>`.
  In `Capabilities`, state that rules declare facts they need so the engine computes only relevant analysis.
  In `Testing Rules`, include `polint test-rules --format json` and state that generated repo-local Rust rules are scaffolded but not automatically compiled or dynamically loaded by `polint check` in v1.</action>
  <verify>
    <automated>rg -n "## Example Config|## Rule Authoring|## Capabilities|## Testing Rules|polint test-rules --format json|use polint_sdk::prelude::\\*|Capabilities::new\\(\\)" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README contains `## Capabilities`.
    - README contains `## Testing Rules`.
    - README contains `polint test-rules --format json`.
    - README contains `use polint_sdk::prelude::*`.
    - README contains `Capabilities::new()`.
    - README states that generated repo-local Rust rules are not automatically compiled or dynamically loaded by `polint check` in v1.
  </acceptance_criteria>
  <done>README explains rule authoring and testing honestly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Document CI, examples, roadmap, and release readiness</name>
  <files>README.md</files>
  <read_first>
    - README.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>Update `README.md` so it contains:
  - `## Examples`
  - `## CI`
  - `## Development`
  - `## Release Readiness`
  - `## Roadmap`
  The CI section must recommend `polint check --profile full --format sarif`.
  The Examples section must link or list `examples/basic`, `examples/custom-rule-go`, `examples/custom-rule-ts`, `examples/go-branch-obligations`, and `examples/ts-design-tokens`.
  The Release Readiness section must list `cargo fmt -- --check`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo test --workspace`.
  The Roadmap section must list exact Go semantic sidecar, dynamic branch coverage instrumentation, repo-local Wasm rule compilation/caching, and more language adapters as future work.</action>
  <verify>
    <automated>rg -n "## Examples|## CI|## Development|## Release Readiness|## Roadmap|polint check --profile full --format sarif|examples/basic|examples/custom-rule-go|examples/custom-rule-ts|examples/go-branch-obligations|examples/ts-design-tokens|cargo fmt -- --check|cargo clippy --workspace --all-targets -- -D warnings|cargo test --workspace" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README contains all five expected example paths.
    - README CI section contains `polint check --profile full --format sarif`.
    - README Release Readiness section contains the three exact cargo commands.
    - README Roadmap section lists future work without claiming it already exists.
  </acceptance_criteria>
  <done>README covers examples, CI, release checks, and future work.</done>
</task>

</tasks>

<threat_model>
ASVS L1 posture: no high severity threat is accepted; documentation must not mislead users into trusting unsupported runtime behavior.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README -> user expectations | README defines what users believe v1 can do. |
| examples -> copied local policy | Users may copy example commands/config into their repositories. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | Spoofing | README capability claims | mitigate | Explicitly state repo-local Rust rules are scaffolded but not dynamically compiled/loaded in v1. |
| T-10-01-02 | Tampering | CI docs | mitigate | Keep CI commands exact and machine-parseable, using existing `polint check --profile full --format sarif`. |
| T-10-01-03 | Repudiation | release readiness | mitigate | List exact verification commands in README and final summary. |
| T-10-01-04 | Information Disclosure | examples/docs | mitigate | Do not include secrets, tokens, or real customer data in examples. |
</threat_model>

<verification>
Run after completing all tasks in this plan:

```bash
rg -n "## Installation|## Quickstart|## Rule Authoring|## Capabilities|## Testing Rules|## Examples|## Release Readiness" README.md
```
</verification>

<success_criteria>
- README satisfies `FND-03`.
- README is honest about v1 limits and future work.
- README contains copyable commands for quickstart, rule testing, CI, and verification.
</success_criteria>

<output>
After completion, create `.planning/phases/10-docs-examples-and-release-hardening/10-01-SUMMARY.md`.
</output>
