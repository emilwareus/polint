---
phase: 10-docs-examples-and-release-hardening
plan: "03"
type: execute
wave: 2
depends_on:
  - "10-02"
files_modified:
  - crates/polint-cli/tests/cli.rs
requirements:
  - TEST-01
  - TEST-02
  - TEST-04
requirements_addressed:
  - TEST-01
  - TEST-02
  - TEST-04
autonomous: true
must_haves:
  truths:
    - "CLI integration tests cover Go, TS, and mixed repository fixtures."
    - "CLI integration tests prove the configured example directories run through `polint check`."
    - "Final testing traceability can rely on existing unit, integration, snapshot, and property tests plus the added Phase 10 smoke tests."
  artifacts:
    - path: "crates/polint-cli/tests/cli.rs"
      provides: "mixed fixture and example smoke integration tests"
  key_links:
    - from: "tests/fixtures/mixed"
      to: "crates/polint-cli/tests/cli.rs"
      via: "mixed repository CLI integration proof"
      pattern: "mixed"
---

<objective>
Add focused executable proof that the final fixtures and examples work through the CLI, especially the mixed Go/TS repository fixture and configured example directories.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-docs-examples-and-release-hardening/10-CONTEXT.md
@crates/polint-cli/tests/cli.rs
@tests/fixtures/
@examples/
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add mixed repository fixture test</name>
  <files>crates/polint-cli/tests/cli.rs</files>
  <read_first>
    - crates/polint-cli/tests/cli.rs
    - tests/fixtures/mixed/main.go
    - tests/fixtures/mixed/view.ts
  </read_first>
  <action>Add a CLI integration test named `check_mixed_fixture_handles_go_and_ts_sources`.
  The test must:
  - Create a temp repo.
  - Write `tests/fixtures/mixed/main.go` to `mixed/main.go`.
  - Write `tests/fixtures/mixed/view.ts` to `mixed/view.ts`.
  - Write `.polint.toml` with:
    ```toml
    [workspace]
    include = ["mixed/**"]
    exclude = []

    [profiles.fast]
    rules = ["examples/go-cyclomatic-complexity", "examples/ts-cyclomatic-complexity"]
    ```
  - Run `polint check --profile fast --format json --fail-on none`.
  - Assert exit success and parsed JSON array output.
  - Run `polint graph imports --format dot`.
  - Assert DOT output contains `digraph` and `mixed/view.ts`.</action>
  <verify>
    <automated>cargo test -p polint-cli --test cli check_mixed_fixture_handles_go_and_ts_sources</automated>
    <automated>rg -n "check_mixed_fixture_handles_go_and_ts_sources|tests/fixtures/mixed|mixed/view.ts|graph imports" crates/polint-cli/tests/cli.rs</automated>
  </verify>
  <acceptance_criteria>
    - Targeted test compiles and passes.
    - Test function name is `check_mixed_fixture_handles_go_and_ts_sources`.
    - Test includes `mixed/view.ts` and `graph imports`.
  </acceptance_criteria>
  <done>Mixed Go/TS fixture has CLI integration proof.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add example directory smoke tests</name>
  <files>crates/polint-cli/tests/cli.rs</files>
  <read_first>
    - crates/polint-cli/tests/cli.rs
    - examples/go-branch-obligations/authorize.go
    - examples/go-branch-obligations/.polint.toml
    - examples/ts-design-tokens/Button.tsx
    - examples/ts-design-tokens/.polint.toml
  </read_first>
  <action>Add two CLI integration tests:
  - `example_go_branch_obligations_reports_expected_diagnostic`
  - `example_ts_design_tokens_reports_raw_colors`
  The Go example test must write `authorize.go` and `.polint.toml` from `examples/go-branch-obligations/`, run `polint check --profile fast --format json --fail-on none`, and assert a diagnostic with `rule_id == "examples/go-branch-obligations"`.
  The TS example test must write `Button.tsx` and `.polint.toml` from `examples/ts-design-tokens/`, run `polint check --profile fast --format json --fail-on none`, and assert a diagnostic with `rule_id == "examples/ts-no-raw-colors"`.
  Use existing helper patterns in `cli.rs` for temp file writes and parsed JSON.</action>
  <verify>
    <automated>cargo test -p polint-cli --test cli example_go_branch_obligations_reports_expected_diagnostic</automated>
    <automated>cargo test -p polint-cli --test cli example_ts_design_tokens_reports_raw_colors</automated>
    <automated>rg -n "example_go_branch_obligations_reports_expected_diagnostic|example_ts_design_tokens_reports_raw_colors|examples/go-branch-obligations|examples/ts-design-tokens" crates/polint-cli/tests/cli.rs</automated>
  </verify>
  <acceptance_criteria>
    - Both targeted example tests pass.
    - Go example test asserts `examples/go-branch-obligations`.
    - TS example test asserts `examples/ts-no-raw-colors`.
  </acceptance_criteria>
  <done>Configured example directories have CLI smoke proof.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Verify final test categories remain covered</name>
  <files>crates/polint-cli/tests/cli.rs</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - crates/polint-cli/tests/cli.rs
    - crates/polint-rules/tests/snapshots.rs
    - crates/polint-cache/src/lib.rs
    - crates/polint-core/src/lib.rs
    - crates/polint-diagnostics/src/lib.rs
    - crates/polint-fs/src/lib.rs
  </read_first>
  <action>Run targeted CLI tests and inspect property/snapshot coverage so Phase 10 can close test traceability honestly.
  Do not add artificial tests only to inflate counts. If existing property tests already cover span roundtrips, diagnostic sorting determinism, file discovery exclusions, and cache key participation, record that in the summary.</action>
  <verify>
    <automated>cargo test -p polint-cli --test cli check_mixed_fixture_handles_go_and_ts_sources example_</automated>
    <automated>rg -n "proptest!|span_from_byte_range_is_monotonic|sort_diagnostics_is_input_order_independent|discovery_include_exclude_decision_is_stable|cache_key_for_file_path_participates" crates/polint-core/src/lib.rs crates/polint-diagnostics/src/lib.rs crates/polint-fs/src/lib.rs crates/polint-cache/src/lib.rs</automated>
  </verify>
  <acceptance_criteria>
    - Targeted Phase 10 CLI tests pass.
    - Existing property tests for span, diagnostic sorting, file discovery, and cache key participation are located with `rg`.
    - Summary records whether any remaining testing scope is documentation-only or verified.
  </acceptance_criteria>
  <done>Final fixture/example testing proof is ready for release verification.</done>
</task>

</tasks>

<threat_model>
ASVS L1 posture: no high severity threat is accepted; example and fixture tests must not hide broken commands behind docs-only claims.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| examples -> CLI | Example directories are executed through `polint check`. |
| fixtures -> CLI | Mixed language fixtures verify file discovery and parsing behavior. |
| test output -> release confidence | Tests provide evidence for final release readiness. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | Spoofing | examples | mitigate | Execute configured examples through CLI integration tests. |
| T-10-03-02 | Tampering | mixed fixture proof | mitigate | Assert graph output references `mixed/view.ts` and parsed JSON succeeds. |
| T-10-03-03 | Repudiation | test traceability | mitigate | Summary records exact targeted commands and property-test evidence. |
| T-10-03-04 | Denial of Service | tests | mitigate | Keep examples and fixtures small; avoid broad slow fixture suites. |
</threat_model>

<verification>
Run after completing all tasks in this plan:

```bash
cargo test -p polint-cli --test cli check_mixed_fixture_handles_go_and_ts_sources
cargo test -p polint-cli --test cli example_go_branch_obligations_reports_expected_diagnostic
cargo test -p polint-cli --test cli example_ts_design_tokens_reports_raw_colors
```
</verification>

<success_criteria>
- Mixed fixture has executable CLI proof.
- Configured example directories have executable CLI proof.
- Final testing traceability has concrete evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/10-docs-examples-and-release-hardening/10-03-SUMMARY.md`.
</output>
