---
phase: 63-ground-truth-and-performance-baseline
plan: 02
type: execute
wave: 2
depends_on: [63-01]
files_modified:
  - crates/polint/src/eval/bench/mod.rs
  - crates/polint/src/eval/bench/runner.rs
  - crates/polint/src/eval/bench/report.rs
  - crates/polint/src/eval/bench/sweep.rs
  - crates/polint/src/eval/markdown.rs
autonomous: true
requirements: [BENCH-01]

must_haves:
  truths:
    - "The harness runs polint check and polint review over a checked-out real repo and records one curve-point per (repo size, diff size) measurement."
    - "The harness assembles a real multi-point CurveSeries by sweeping the locked benchmark repo set and/or diff sizes, not just a synthetic 2-point fixture."
    - "The harness emits the measured curve series as machine-readable JSON."
    - "The markdown benchmark report renders peak RSS, cold/warm wall-clock, cache/store size, and budget-exhaustion columns per curve point."
  artifacts:
    - path: "crates/polint/src/eval/bench/runner.rs"
      provides: "Whole-repo perf runner driving polint check + review, capturing curve points via the Plan 01 substrate"
      min_lines: 60
    - path: "crates/polint/src/eval/bench/report.rs"
      provides: "Curve-series JSON emission + markdown benchmark-report rendering"
      min_lines: 40
    - path: "crates/polint/src/eval/bench/sweep.rs"
      provides: "Benchmark entry-point: iterates the locked manifest set / sweeps diff sizes into a real multi-point CurveSeries and writes JSON + markdown"
      min_lines: 40
  key_links:
    - from: "crates/polint/src/eval/bench/runner.rs"
      to: "crates/polint/src/eval/bench/measure.rs"
      via: "cold_then_warm timing + peak_rss_bytes capture around a check/review run"
      pattern: "cold_then_warm|peak_rss_bytes"
    - from: "crates/polint/src/eval/bench/runner.rs"
      to: "crates/polint/src/git/mod.rs"
      via: "changeset_for_ref to derive diff size for the review measurement"
      pattern: "changeset_for_ref"
    - from: "crates/polint/src/eval/bench/report.rs"
      to: "crates/polint/src/eval/bench/curve.rs"
      via: "CurveSeries serialized to JSON and rendered to markdown"
      pattern: "CurveSeries"
---

<objective>
Make the scale/latency problem observable (BENCH-01 success criteria 2): a whole-repo perf runner drives `polint check` and `polint review` over a checked-out benchmark repo, capturing one curve-point per (repo size, diff size) measurement, and emits the results as machine-readable JSON curves plus a markdown benchmark report with peak RSS, cold/warm wall-clock, cache/store size, and budget-exhaustion columns.

Purpose: Baselines (Plan 03) and regression gates (Plan 04) need a runner that actually produces measured curve points and a report to record them in.
Output: `bench/runner.rs` (whole-repo perf runner) and `bench/report.rs` (curve JSON + markdown), plus a markdown benchmark-report section.
</objective>

<execution_context>
@/Users/emilwareus/conductor/workspaces/exlint/islamabad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/conductor/workspaces/exlint/islamabad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/63-ground-truth-and-performance-baseline/63-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 and existing harness. Use directly; do not re-explore. -->

From crates/polint/src/eval/bench/curve.rs (Plan 01):
```rust
pub(crate) struct CurvePoint { repo_id, repo_file_count, repo_source_bytes, diff_files, diff_hunk_lines, cold_wall_clock_ms, warm_wall_clock_ms, peak_rss_bytes, size: StoreSizeBytes, budget: BudgetExhaustionCounters }
pub(crate) struct CurveSeries { schema_version: String, points: Vec<CurvePoint> }
```
From crates/polint/src/eval/bench/measure.rs (Plan 01):
```rust
pub(crate) fn peak_rss_bytes() -> u64;
pub(crate) fn cold_then_warm<F: FnMut()>(run: F) -> (u64 /*cold_ms*/, u64 /*warm_ms*/, u64 /*peak_rss*/);
```
From crates/polint/src/git/mod.rs (diff sizing for review):
```rust
pub(crate) fn changeset_for_ref(root: &Path, target: &str) -> Result<ReviewChangeset>; // name-status + unified=0 hunks
```
From crates/polint/src/eval/runner.rs (how a kernel run is driven over a workspace — mirror this, do not duplicate scoring):
```rust
// crate::config::load_config(root) -> crate::analysis_kernel::AnalysisKernel::run(KernelInput{ loaded, cache, config_digest, rule_digest, plan, parallel })
// crate::analysis_plan::AnalysisPlan::full_pipeline_for_test() for deep runs.
```
From crates/polint/src/eval/markdown.rs:
```rust
pub(crate) fn render_markdown(run: &EvaluationRun) -> String; // existing "## RSS Thresholds" table pattern to mirror
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Whole-repo perf runner driving check + review</name>
  <files>crates/polint/src/eval/bench/runner.rs, crates/polint/src/eval/bench/mod.rs</files>
  <read_first>
    - crates/polint/src/eval/bench/measure.rs (Plan 01 timing/RSS helpers)
    - crates/polint/src/eval/bench/curve.rs (Plan 01 CurvePoint/CurveSeries)
    - crates/polint/src/eval/runner.rs (run_polint_for_prepared_case: the canonical way to load config + drive AnalysisKernel::run over a workspace, and safe_join_workspace)
    - crates/polint/src/git/mod.rs (changeset_for_ref for diff size)
    - crates/polint/src/analysis_kernel/debug.rs (the per-provider summary walk that counts SummaryStatus::BudgetExceeded — the source for budget/token/iteration counters)
  </read_first>
  <action>
    In crates/polint/src/eval/bench/runner.rs create a pub(crate) `run_repo_perf_point(repo_root: &Path, review_ref: Option<&str>) -> anyhow::Result<CurvePoint>` (test-facing, #[cfg(test)] where it drives the kernel, consistent with runner.rs conventions).
    It must: (1) compute repo_file_count and repo_source_bytes from the discovered source set (reuse the config/source-loading path used by run_polint_for_prepared_case; do NOT load whole-repo source eagerly outside the capability-gated pipeline — respect PERF-01 discipline); (2) drive a `polint check` equivalent through crate::analysis_kernel::AnalysisKernel::run wrapped in measure::cold_then_warm to capture cold/warm wall-clock and peak RSS; (3) when review_ref is Some, call changeset_for_ref to derive diff_files and diff_hunk_lines and drive the review-kind run (RuleKind::Review path) as the diff-size measurement; (4) read cache/store size from the repo's `.polint/cache` on disk into StoreSizeBytes (store_bytes = 0 for now — the store lands in Phase 64); (5) fold budget-exhaustion telemetry into BudgetExhaustionCounters. Source these counters explicitly: KernelRunReport does NOT expose budget/token/iteration counters — they live as per-provider `SummaryStatus::BudgetExceeded` counts (see crates/polint/src/analysis_kernel/debug.rs, where a per-provider counts struct increments `budget_exceeded` on `SummaryStatus::BudgetExceeded`). Walk the provider summaries the same way debug.rs does and sum `budget_exceeded` into BudgetExhaustionCounters.budget_exceeded; map token-explosion and iteration-cap evidence (the too-many-tokens sentinel / iteration-cap BudgetExceeded surfaced via provider summaries or CallTargetStatus::BudgetExceeded / DomainStatus::BudgetExceeded) into tokens_exhausted and iteration_capped. If a given counter has no reachable source in the current pipeline, set it to 0 AND record that explicit-zero decision in the SUMMARY (do not silently zero-stub without noting it). Return a fully-populated CurvePoint.
    Provide a #[cfg(test)] test that runs against a small in-repo fixture directory (e.g. tests/eval-fixtures/... or a tempdir with a tiny Go/TS file), asserting cold_wall_clock_ms > 0, peak_rss_bytes > 0, and repo_file_count > 0. Skip (early return) if a required external repo checkout is absent, mirroring external::tests.
  </action>
  <verify>
    <automated>cargo test -p polint --lib eval::bench::runner --locked</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p polint --lib eval::bench::runner --locked` exits 0.
    - grep -E "cold_then_warm|peak_rss_bytes" crates/polint/src/eval/bench/runner.rs matches.
    - grep -c "changeset_for_ref" crates/polint/src/eval/bench/runner.rs is >= 1 (diff-size measurement wired for review).
    - A test asserts the returned CurvePoint has cold_wall_clock_ms > 0 AND peak_rss_bytes > 0 AND repo_file_count > 0.
    - The runner does not introduce a whole-repo eager source load: grep -v '^//' crates/polint/src/eval/bench/runner.rs shows the run goes through AnalysisKernel::run / the existing config load path (no new fs::read_dir-and-read-all of the entire repo).
    - BudgetExhaustionCounters is populated from a provider-summary walk (grep -E "SummaryStatus::BudgetExceeded|budget_exceeded" crates/polint/src/eval/bench/runner.rs matches), NOT hardcoded to zero; any counter with no reachable source is documented as an explicit-zero decision in the SUMMARY.
  </acceptance_criteria>
  <done>The runner produces a fully-populated CurvePoint from a real check+review run over a checked-out repo, with cold/warm timing, peak RSS, diff size, cache size, and budget-exhaustion counters.</done>
</task>

<task type="auto">
  <name>Task 2: Curve JSON emission + markdown benchmark report</name>
  <files>crates/polint/src/eval/bench/report.rs, crates/polint/src/eval/markdown.rs</files>
  <read_first>
    - crates/polint/src/eval/bench/curve.rs (CurveSeries shape + schema_version constant)
    - crates/polint/src/eval/markdown.rs (existing render_markdown and the "## RSS Thresholds" table style to mirror)
    - crates/polint/src/eval/report.rs (to_deterministic_json_pretty for byte-stable JSON emission)
  </read_first>
  <action>
    In crates/polint/src/eval/bench/report.rs add pub(crate) `write_curve_series(path: &Path, series: &CurveSeries) -> anyhow::Result<()>` that sorts points deterministically and writes byte-stable pretty JSON (reuse the deterministic-JSON approach from eval::report). Add pub(crate) `render_curve_markdown(series: &CurveSeries) -> String` producing a "## Benchmark Curves" table with columns: Repo, Files, Source bytes, Diff files, Diff hunk lines, Cold ms, Warm ms, Peak RSS (MiB), Cache bytes, Store bytes, Budget exceeded. Rows sorted by (repo_id, repo_file_count, diff_files).
    In crates/polint/src/eval/markdown.rs add a helper (or extend render) so the benchmark report can include the curve section; keep the existing render_markdown signature intact (add a new pub(crate) function rather than breaking callers).
    Add tests: a two-point CurveSeries emits byte-identical JSON across two calls; the markdown contains a "Peak RSS" column header and a "Cold ms" column header and one row per point.
  </action>
  <verify>
    <automated>cargo test -p polint --lib eval::bench::report --locked</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p polint --lib eval::bench::report --locked` exits 0.
    - A test writes a CurveSeries JSON twice and asserts the two files are byte-identical.
    - render_curve_markdown output contains the substrings "Peak RSS", "Cold ms", "Warm ms", "Diff files", "Budget", and "Cache bytes".
    - The markdown renders exactly one row per CurvePoint (a two-point series yields two data rows).
  </acceptance_criteria>
  <done>Curve series serialize to deterministic JSON and render to a markdown report with peak RSS, cold/warm, cache/store size, and budget-exhaustion columns.</done>
</task>

<task type="auto">
  <name>Task 3: Benchmark entry-point — sweep the locked suite set into a real multi-point CurveSeries</name>
  <files>crates/polint/src/eval/bench/sweep.rs, crates/polint/src/eval/bench/mod.rs</files>
  <read_first>
    - crates/polint/src/eval/bench/runner.rs (Task 1 run_repo_perf_point — the per-point measurement this task aggregates)
    - crates/polint/src/eval/bench/report.rs (Task 2 write_curve_series + render_curve_markdown — the emit path)
    - crates/polint/src/eval/runner.rs (run_external_suite_for_test ~line 211-224: the pattern for writing json_path + markdown_path via std::fs::write to an output_dir)
    - crates/polint/src/eval/suite.rs (SuiteManifest / SuiteManifest::validate — loading the committed manifests)
    - research/evaluation-harness/suites/BENCHMARK-SUITE.md (the locked repo set to iterate)
  </read_first>
  <action>
    Register `pub(crate) mod sweep;` in crates/polint/src/eval/bench/mod.rs and create crates/polint/src/eval/bench/sweep.rs. Add a pub(crate) #[cfg(test)] entry point `run_benchmark_sweep(output_dir: &Path) -> anyhow::Result<CurveSeries>` that: (1) loads the committed CI benchmark manifests (grafana-grafana-scale.toml, gohugoio-hugo-scale.toml, excalidraw-excalidraw-scale.toml) from research/evaluation-harness/suites/; (2) for each manifest whose checkout.path exists, calls run_repo_perf_point once for the cold/warm baseline point AND additional points across a small fixed diff-size sweep (e.g. review against 2-3 review refs / synthetic diff sizes) so the series has multiple points varying by repo size AND diff size; (3) SKIPS (does not fail) any manifest whose checkout is absent, mirroring external::tests, so the sweep is runnable without the large clones present; (4) assembles all points into one CurveSeries (schema_version from curve.rs), and writes both `<output_dir>/benchmark-curves.json` (via write_curve_series) and `<output_dir>/benchmark-report.md` (via render_curve_markdown) using std::fs::write, mirroring runner.rs's json_path/markdown_path pattern. (Plan 03 later extends benchmark-report.md with the persisted-graph accuracy section; do not build that section here.) Return the CurveSeries.
    Add a #[cfg(test)] test that runs the sweep against a tempdir output using a small in-repo fixture repo (or a manifest pointed at a vendored fixture) so at least 2 points are produced WITHOUT the large clones, and asserts: the returned CurveSeries has >= 2 points, benchmark-curves.json and benchmark-report.md both exist and are non-empty, and re-running the sweep produces byte-identical benchmark-curves.json (deterministic). Do not add any public/SDK/CLI surface.
  </action>
  <verify>
    <automated>cargo test -p polint --lib eval::bench::sweep --locked</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p polint --lib eval::bench::sweep --locked` exits 0.
    - crates/polint/src/eval/bench/sweep.rs defines a `run_benchmark_sweep` entry point that iterates the committed benchmark manifests and returns a CurveSeries.
    - A test asserts the returned CurveSeries has >= 2 points (multi-point, not the synthetic 2-point report fixture from Task 2) and that both `benchmark-curves.json` and `benchmark-report.md` are written to the output dir and are non-empty.
    - A test asserts re-running the sweep yields byte-identical `benchmark-curves.json` (deterministic emission).
    - Absent large-repo checkouts cause skips, not test failures (grep shows an existence check on checkout.path before run_repo_perf_point).
    - grep -c "mod sweep" crates/polint/src/eval/bench/mod.rs is >= 1.
  </acceptance_criteria>
  <done>A benchmark entry-point sweeps the locked suite set (and diff sizes) into a real multi-point CurveSeries and writes JSON + markdown to an output path; the curves-versus-size deliverable has an owning task.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checked-out repo → perf runner | Untrusted repo source + git history read during check/review measurement |
| git ref (review_ref) → git binary | Diff base string passed to `git diff` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-02-01 | Tampering | changeset_for_ref review_ref argument | mitigate | Reuse existing crate::git::changeset_for_ref, which passes refs as fixed positional args to the git binary (no shell); the ref is developer/manifest-supplied, not repo-controlled. No new shell invocation is added. |
| T-63-02-02 | Elevation | workspace path resolution during measurement | mitigate | Reuse safe_join_workspace / the existing config-load workspace root; no new path joining that could escape the repo root. |
| T-63-02-03 | Denial of Service | measuring a large repo consumes host memory | accept | Intended workload; the run uses the capability-gated kernel pipeline (PERF-01) and records budget-exhaustion counters rather than unbounded expansion. |
| T-63-02-04 | Information Disclosure | curve JSON / report leaking absolute host paths | mitigate | CurvePoint keys by repo_id, not absolute paths; report rendering emits repo_id and relative fields only (asserted by test — no "/Users/" or "/home/" in output). |
</threat_model>

<verification>
- `cargo test -p polint --lib eval::bench::runner --locked` and `eval::bench::report` pass.
- `cargo build -p polint --locked` passes.
- Public-surface leak gate unaffected (bench stays pub(crate)).
</verification>

<success_criteria>
- A whole-repo perf runner produces measured CurvePoints (cold/warm, peak RSS, diff size, cache/store size, budget-exhaustion) from check + review runs.
- Curve series emit deterministic JSON and a markdown benchmark report with the required columns.
</success_criteria>

<output>
Create `.planning/phases/63-ground-truth-and-performance-baseline/63-02-SUMMARY.md` when done.
</output>
