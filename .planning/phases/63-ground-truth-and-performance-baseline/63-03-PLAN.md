---
phase: 63-ground-truth-and-performance-baseline
plan: 03
type: execute
wave: 3
depends_on: [63-02]
files_modified:
  - crates/polint/src/eval/baseline.rs
  - crates/polint/src/eval/bench/report.rs
  - crates/polint/src/eval/markdown.rs
  - research/evaluation-harness/baselines/store-disabled-check.json
  - research/evaluation-harness/baselines/store-disabled-review.json
  - research/evaluation-harness/baselines/persisted-graph-accuracy.json
autonomous: true
requirements: [BENCH-02, BENCH-04]

must_haves:
  truths:
    - "A committed store-disabled baseline records peak RSS and cold/warm latency for polint check."
    - "A committed store-disabled baseline records peak RSS and cold/warm latency for polint review, plus a diagnostics-parity marker."
    - "The persisted-graph (pre-store) recall/precision baseline from the Jelly and Go x/tools adapters is recorded and appears in the benchmark report."
  artifacts:
    - path: "research/evaluation-harness/baselines/store-disabled-check.json"
      provides: "Committed store-disabled baseline for polint check (peak RSS, cold/warm, diagnostics digest)"
      contains: "store_disabled"
    - path: "research/evaluation-harness/baselines/store-disabled-review.json"
      provides: "Committed store-disabled baseline for polint review"
      contains: "store_disabled"
    - path: "research/evaluation-harness/baselines/persisted-graph-accuracy.json"
      provides: "Committed pre-store graph recall/precision baseline from Jelly + Go x/tools"
      contains: "recall"
    - path: "crates/polint/src/eval/baseline.rs"
      provides: "Distinct StoreDisabledBaseline type (own schema constant) with peak RSS + cold/warm wall-clock + store-disabled marker + diagnostics digest"
      contains: "store_disabled"
  key_links:
    - from: "crates/polint/src/eval/baseline.rs"
      to: "crates/polint/src/eval/bench/curve.rs"
      via: "baseline records peak RSS + cold/warm wall-clock from a measured CurvePoint"
      pattern: "peak_rss|cold_wall_clock"
    - from: "research/evaluation-harness/baselines/persisted-graph-accuracy.json"
      to: "crates/polint/src/eval/external"
      via: "recall/precision sourced from JellyCallgraphAdapter + GoXToolsCallgraphAdapter runs"
      pattern: "jelly|go_x_tools|recall"
---

<objective>
Record and commit the reference baselines the whole milestone measures against (BENCH-02, BENCH-04): the store-disabled peak-RSS / cold-warm-latency / diagnostics-parity baseline for `polint check` and `polint review`, and the pre-store persisted-graph recall/precision baseline from the Jelly and Go x/tools callgraph adapters, surfaced in the benchmark report.

Purpose: The store does not exist yet (it lands Phase 64), so "store-disabled" == current polint. These committed baselines are the fixed reference the Phase 64+ regression gates (Plan 04) compare against, and the accuracy baseline keeps `polint graph` answers honest (accuracy-visibility gate).
Output: Extended `EvalBaseline` carrying peak RSS + cold/warm wall-clock + a store-disabled marker; three committed baseline JSON artifacts; an accuracy section in the benchmark report.
</objective>

<execution_context>
@/Users/emilwareus/conductor/workspaces/exlint/islamabad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/conductor/workspaces/exlint/islamabad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/63-ground-truth-and-performance-baseline/63-02-SUMMARY.md

<interfaces>
<!-- Existing baseline schema + adapters. Extracted; use directly. -->

From crates/polint/src/eval/baseline.rs:
```rust
pub(crate) const BASELINE_SCHEMA_VERSION: &str = "polint-eval-baseline-0";
pub(crate) struct EvalBaseline { schema_version, suite_id, suite_commit, mode: EvaluationMode, output_hash, run: EvaluationRun }
pub(crate) fn write_baseline(path: &Path, run: &EvaluationRun) -> Result<EvalBaseline>;
pub(crate) fn load_baseline(path: &Path) -> Result<EvalBaseline>;
// EvalBaseline::validate() enforces schema_version + real-polint mode.
```
From crates/polint/src/eval/bench/curve.rs (Plan 01): CurvePoint { cold_wall_clock_ms, warm_wall_clock_ms, peak_rss_bytes, ... }.
From crates/polint/src/eval/external/mod.rs (Plan-existing test): run_external_suite_for_test drives JellyCallgraphAdapter + GoXToolsCallgraphAdapter and yields EvaluationRun with metrics.recall / metrics.precision / graph_edges_expected/observed.
From crates/polint/src/eval/report.rs: MetricSummary { precision: Option<f64>, recall: Option<f64>, graph_edges_expected, graph_edges_observed, ... }; to_deterministic_json_pretty.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add StoreDisabledBaseline type and record committed store-disabled check/review baselines</name>
  <files>crates/polint/src/eval/baseline.rs, research/evaluation-harness/baselines/store-disabled-check.json, research/evaluation-harness/baselines/store-disabled-review.json</files>
  <read_first>
    - crates/polint/src/eval/baseline.rs (EvalBaseline, BASELINE_SCHEMA_VERSION, write_baseline/load_baseline/validate)
    - crates/polint/src/eval/bench/runner.rs (Plan 02 run_repo_perf_point producing a CurvePoint)
    - crates/polint/src/eval/bench/curve.rs (CurvePoint fields)
  </read_first>
  <action>
    Create a DISTINCT pub(crate) type `StoreDisabledBaseline` in baseline.rs (do NOT overload the existing EvalBaseline; Plan 04 hard-depends on a type literally named `StoreDisabledBaseline` with these fields) carrying `schema_version: String`, `store_disabled: bool`, `repo_id: String`, `suite_id: String`, `peak_rss_bytes: u64`, `cold_wall_clock_ms: u64`, `warm_wall_clock_ms: u64`, and `diagnostics_digest: String` (the deterministic output/diagnostics hash used as the parity marker). Give it its OWN schema constant `STORE_DISABLED_BASELINE_SCHEMA_VERSION = "polint-store-disabled-baseline-0"`; do NOT bump or reuse the shared `BASELINE_SCHEMA_VERSION` (EvalBaseline::validate() asserts exact-equality on that constant, so touching it would break existing EvalBaseline artifacts). Derive Serialize/Deserialize with `deny_unknown_fields`. Add a pub(crate) constructor `StoreDisabledBaseline::from_curve_point(repo_id, suite_id, point: &CurvePoint, diagnostics_digest)` and a `write(path)` / `load(path)` pair emitting deterministic pretty JSON with a `validate()` that checks its own schema constant + store_disabled == true + non-empty diagnostics_digest.
    Record the two committed artifacts research/evaluation-harness/baselines/store-disabled-check.json and store-disabled-review.json with `store_disabled = true`, a suite_id referencing the benchmark suite (e.g. "grafana-grafana-scale" or a documented small stand-in when the large clone is absent), the measured peak RSS + cold/warm ms, and the diagnostics_digest. If the large repo checkout is absent in the executor environment, generate the committed JSON from a small vendored fixture repo and record `repo_id` honestly so the artifact is reproducible; document this in the SUMMARY. Add a test that load()s both committed baselines and asserts store_disabled == true and non-empty diagnostics_digest.
  </action>
  <verify>
    <automated>cargo test -p polint --lib eval::baseline --locked</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p polint --lib eval::baseline --locked` exits 0.
    - crates/polint/src/eval/baseline.rs defines a distinct `struct StoreDisabledBaseline` (grep -c "struct StoreDisabledBaseline" is 1) with fields `store_disabled`, `peak_rss_bytes`, `cold_wall_clock_ms`, `warm_wall_clock_ms`, and `diagnostics_digest`.
    - baseline.rs defines `STORE_DISABLED_BASELINE_SCHEMA_VERSION = "polint-store-disabled-baseline-0"` and the existing `BASELINE_SCHEMA_VERSION` constant is UNCHANGED (still "polint-eval-baseline-0"); the pre-existing eval::baseline tests still pass.
    - research/evaluation-harness/baselines/store-disabled-check.json and store-disabled-review.json both exist and each contain the string `"store_disabled": true` (or `store_disabled = true` equivalent) and a non-empty diagnostics/output digest field.
    - A test loads both committed baseline files and asserts store_disabled and a non-empty diagnostics_digest.
  </acceptance_criteria>
  <done>A distinct StoreDisabledBaseline type (own schema constant, shared BASELINE_SCHEMA_VERSION untouched) carries RSS + cold/warm + store-disabled marker + diagnostics parity digest; committed check/review baselines exist and load/validate.</done>
</task>

<task type="auto">
  <name>Task 2: Record persisted-graph recall/precision baseline and surface it in the benchmark report</name>
  <files>crates/polint/src/eval/bench/report.rs, crates/polint/src/eval/markdown.rs, research/evaluation-harness/baselines/persisted-graph-accuracy.json</files>
  <read_first>
    - crates/polint/src/eval/external/mod.rs (external_graph_baseline_reports_can_be_generated: how Jelly + Go x/tools adapters produce recall/precision EvaluationRuns)
    - crates/polint/src/eval/report.rs (MetricSummary recall/precision/graph_edges fields; to_deterministic_json_pretty)
    - crates/polint/src/eval/bench/report.rs (Plan 02 report/markdown surface to extend)
  </read_first>
  <action>
    Add a pub(crate) `GraphAccuracyBaseline` type (in bench/report.rs) with a schema_version and per-suite rows { suite_id, suite_commit, recall, precision, graph_edges_expected, graph_edges_observed, unknown_count } for the Jelly micro suite and the Go x/tools RTA suite. Provide a builder that consumes the EvaluationRuns produced by the existing external adapters (do NOT reimplement scoring — read metrics.recall/precision/graph_edges_* off the runs). Emit the committed artifact research/evaluation-harness/baselines/persisted-graph-accuracy.json via deterministic JSON.
    Extend the benchmark markdown report (render_curve_markdown or a sibling) to include a "## Persisted-Graph Accuracy Baseline" section with columns Suite, Commit, Recall, Precision, Edges expected, Edges observed, Unknowns, sourced from GraphAccuracyBaseline. This satisfies BENCH-04's "appears in the benchmark report" requirement and CLI-07's calibration context.
    Because the persistent store does not exist yet, label the baseline explicitly as the pre-store reference (a comment/field noting "pre-store graph, Phase 63 reference"). Add a test that builds a GraphAccuracyBaseline from two synthetic EvaluationRuns and asserts the markdown contains "Recall", "Precision", and one row per suite; and that the committed JSON round-trips byte-identically.
  </action>
  <verify>
    <automated>cargo test -p polint --lib eval::bench::report --locked</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p polint --lib eval::bench::report --locked` exits 0.
    - research/evaluation-harness/baselines/persisted-graph-accuracy.json exists and contains rows for both the Jelly and Go x/tools suites with `recall` and `precision` fields.
    - The benchmark markdown output contains the substrings "Persisted-Graph Accuracy", "Recall", and "Precision".
    - A test asserts the accuracy markdown renders exactly one row per suite and the committed JSON round-trips byte-identically across two emissions.
    - The baseline artifact carries an explicit pre-store label field or comment.
  </acceptance_criteria>
  <done>A committed pre-store graph recall/precision baseline exists for Jelly + Go x/tools and renders in the benchmark report.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed baseline JSON → later-phase gate | Baseline files are the trusted reference future phases compare against |
| adapter EvaluationRun → accuracy baseline | Recall/precision numbers copied from existing adapter runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-03-01 | Tampering | committed baseline JSON silently edited | mitigate | Baselines are committed artifacts under version control; load()/validate() enforce schema_version, and a test asserts store_disabled + non-empty digest so a hand-edited/incomplete file fails CI. |
| T-63-03-02 | Repudiation | baseline recorded from an unrepresentative repo without disclosure | mitigate | The baseline records repo_id/suite_id honestly and the SUMMARY documents whether the large clone or a fixture stand-in was used, so the reference is reproducible and auditable. |
| T-63-03-03 | Information Disclosure | absolute host paths / private devloupe data in committed JSON | mitigate | Committed baselines carry repo_id + metrics only, no absolute paths or private source; the devloupe local-only reference is NOT committed (it stays local per Plan 01). Test asserts no "/Users/"|"/home/" substrings. |
</threat_model>

<verification>
- `cargo test -p polint --lib eval::baseline --locked` and `eval::bench::report --locked` pass.
- The three committed baseline JSON files exist and load/validate.
- `cargo build -p polint --locked` passes.
</verification>

<success_criteria>
- Store-disabled baselines for polint check and polint review are recorded (peak RSS, cold/warm latency, diagnostics parity) and committed.
- The pre-store persisted-graph recall/precision baseline from Jelly + Go x/tools is committed and appears in the benchmark report.
</success_criteria>

<output>
Create `.planning/phases/63-ground-truth-and-performance-baseline/63-03-SUMMARY.md` when done.
</output>
