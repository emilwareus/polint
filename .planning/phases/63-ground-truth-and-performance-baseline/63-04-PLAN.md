---
phase: 63-ground-truth-and-performance-baseline
plan: 04
type: execute
wave: 4
depends_on: [63-03]
files_modified:
  - crates/polint/src/eval/baseline.rs
  - crates/polint/src/eval/bench/gate.rs
  - crates/polint/src/eval/bench/mod.rs
autonomous: true
requirements: [BENCH-03]

must_haves:
  truths:
    - "The regression gate compares a measured run against the committed store-disabled baseline on peak RSS and cold wall-clock."
    - "A run exceeding +20% peak RSS OR +25% cold wall-clock yields a Fail verdict, not a silent pass."
    - "A run within budget yields a Pass verdict."
    - "The gate exposes a non-zero-exit signal a later phase (64+) can wire at its phase boundary."
  artifacts:
    - path: "crates/polint/src/eval/bench/gate.rs"
      provides: "Regression-budget gate: peak-RSS +20% and cold-wall-clock +25% checks vs store-disabled baseline, with Fail/Pass verdict"
      min_lines: 50
    - path: "crates/polint/src/eval/baseline.rs"
      provides: "BaselineThresholds extended with peak-RSS and cold-wall-clock budget ratios"
      contains: "peak_rss"
  key_links:
    - from: "crates/polint/src/eval/bench/gate.rs"
      to: "crates/polint/src/eval/baseline.rs"
      via: "reads the committed store-disabled baseline (peak RSS + cold wall-clock) as the reference"
      pattern: "StoreDisabledBaseline|load_baseline|store_disabled"
    - from: "crates/polint/src/eval/bench/gate.rs"
      to: "crates/polint/src/eval/gates.rs"
      via: "reuses GateVerdict / GateCheck so the verdict aggregates like existing gates"
      pattern: "GateVerdict|GateCheck"
---

<objective>
Wire the regression gate that makes the locked budgets enforceable (BENCH-03): a later store phase whose measured run exceeds +20% peak RSS or +25% cold wall-clock versus the committed store-disabled baseline fails its gate rather than passing silently.

Purpose: This is the mechanism that turns Plan 03's committed baseline into an enforced gate from Phase 64 onward. Without it, later phases could regress scale/latency undetected.
Output: `BaselineThresholds` extended with peak-RSS and cold-wall-clock budget ratios; a `bench/gate.rs` regression-budget gate producing a Fail/Pass verdict with a non-zero-exit signal; tests proving over-budget fails and within-budget passes.
</objective>

<execution_context>
@/Users/emilwareus/conductor/workspaces/exlint/islamabad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/conductor/workspaces/exlint/islamabad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/63-ground-truth-and-performance-baseline/63-03-SUMMARY.md

<interfaces>
<!-- Existing gate + threshold vocabulary. Extracted; use directly. -->

From crates/polint/src/eval/baseline.rs (existing thresholds — extend, do not replace):
```rust
pub(crate) struct BaselineThresholds {
    max_precision_drop, max_recall_drop,
    warn_runtime_overhead_ratio: f64 /* 1.10 */, fail_runtime_overhead_ratio: f64 /* 1.25 */,
    max_new_false_positive_traps, require_same_output_hash, max_cache_miss_delta, max_rejected_fact_delta,
}
// runtime_overhead_check already exists for observed_runtime_ms; peak RSS + cold wall-clock have NO budget yet.
```
From crates/polint/src/eval/gates.rs (reuse the verdict vocabulary):
```rust
pub(crate) enum GateVerdict { Pass, Warn, Fail } // derives Ord; aggregate via .max()
pub(crate) struct GateCheck { metric: String, observed: String, threshold: String, verdict: GateVerdict }
```
From crates/polint/src/eval/baseline.rs (Plan 03): pub(crate) struct StoreDisabledBaseline { schema_version, store_disabled, repo_id, suite_id, peak_rss_bytes, cold_wall_clock_ms, warm_wall_clock_ms, diagnostics_digest } with its own STORE_DISABLED_BASELINE_SCHEMA_VERSION constant and a load(path) reader.
Locked budgets (REQUIREMENTS.md, non-negotiable): peak RSS <= +20% (ratio 1.20), cold wall-clock <= +25% (ratio 1.25) vs the store-disabled baseline, until warm reuse lands (Phase 67).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend BaselineThresholds with peak-RSS and cold-wall-clock budget ratios</name>
  <files>crates/polint/src/eval/baseline.rs</files>
  <read_first>
    - crates/polint/src/eval/baseline.rs (BaselineThresholds, Default impl, runtime_overhead_check, max_f64_check — mirror these for the new budgets)
    - .planning/REQUIREMENTS.md lines 24-49 (Milestone Outcome Gates + Locked Milestone Decisions: the +20% RSS / +25% cold wall-clock budgets)
  </read_first>
  <action>
    Add two fields to BaselineThresholds: `max_peak_rss_ratio: f64` (default 1.20) and `max_cold_wall_clock_ratio: f64` (default 1.25), with serde defaults so existing serialized thresholds still deserialize. Update the Default impl. These encode the locked regression budgets per D (REQUIREMENTS.md Locked Milestone Decisions) — reference that they are revisable only with a recorded decision. Add unit tests asserting the default ratios are exactly 1.20 and 1.25.
    Do NOT repurpose the existing runtime_overhead_ratio fields — cold wall-clock is a distinct signal from the eval-runner runtime; keep both.
  </action>
  <verify>
    <automated>cargo test -p polint --lib eval::baseline --locked</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p polint --lib eval::baseline --locked` exits 0.
    - grep -E "max_peak_rss_ratio" crates/polint/src/eval/baseline.rs matches, and the Default impl sets it to 1.20.
    - grep -E "max_cold_wall_clock_ratio" crates/polint/src/eval/baseline.rs matches, and the Default impl sets it to 1.25.
    - A test asserts BaselineThresholds::default().max_peak_rss_ratio == 1.20 and max_cold_wall_clock_ratio == 1.25.
    - The existing warn/fail_runtime_overhead_ratio fields are unchanged (still present).
  </acceptance_criteria>
  <done>BaselineThresholds carries the locked +20% peak-RSS and +25% cold-wall-clock budgets with correct defaults, backward-compatible deserialization.</done>
</task>

<task type="auto">
  <name>Task 2: Regression-budget gate producing Fail/Pass with a phase-boundary signal</name>
  <files>crates/polint/src/eval/bench/gate.rs, crates/polint/src/eval/bench/mod.rs</files>
  <read_first>
    - crates/polint/src/eval/gates.rs (GateVerdict, GateCheck, max_u64_check pattern, verdict aggregation via .max())
    - crates/polint/src/eval/baseline.rs (StoreDisabledBaseline from Plan 03 + the new ratio thresholds from Task 1)
    - crates/polint/src/eval/bench/curve.rs (CurvePoint fields the measured run supplies)
  </read_first>
  <action>
    Register `pub(crate) mod gate;` in crates/polint/src/eval/bench/mod.rs and create crates/polint/src/eval/bench/gate.rs. Implement pub(crate) `evaluate_regression_budget(baseline: &StoreDisabledBaseline, measured: &CurvePoint, thresholds: &BaselineThresholds) -> RegressionGateReport`, where RegressionGateReport { verdict: GateVerdict, checks: Vec<GateCheck> }. Produce two checks: peak_rss_ratio = measured.peak_rss_bytes / baseline.peak_rss_bytes vs max_peak_rss_ratio, and cold_wall_clock_ratio = measured.cold_wall_clock_ms / baseline.cold_wall_clock_ms vs max_cold_wall_clock_ratio; each Fail if ratio exceeds its budget, else Pass. Aggregate the verdict via .max() (Fail dominates). Add a pub(crate) helper `is_blocking(report: &RegressionGateReport) -> bool` returning true on Fail so a later phase can convert it to a non-zero exit at its phase boundary (do NOT wire it into a public CLI — this is the crate-internal gate later store phases invoke).
    Add tests: (1) a measured run at 1.25x baseline peak RSS -> peak_rss check Fail and overall Fail and is_blocking true; (2) a run at 1.10x peak RSS and 1.15x cold wall-clock -> Pass and is_blocking false; (3) a run at 1.30x cold wall-clock -> cold check Fail. Include a doc comment stating this gate is invoked at every store-phase boundary from Phase 64 onward (BENCH-03).
  </action>
  <verify>
    <automated>cargo test -p polint --lib eval::bench::gate --locked</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p polint --lib eval::bench::gate --locked` exits 0.
    - A test asserts a measured run at 1.25x baseline peak RSS yields GateVerdict::Fail and is_blocking == true.
    - A test asserts a measured run within both budgets (e.g. 1.10x RSS, 1.15x cold) yields GateVerdict::Pass and is_blocking == false.
    - A test asserts a run at 1.30x cold wall-clock yields a failing cold_wall_clock check.
    - grep -E "GateVerdict|GateCheck" crates/polint/src/eval/bench/gate.rs matches (reuses the existing verdict vocabulary).
    - gate.rs carries a doc comment naming BENCH-03 / the Phase 64+ phase-boundary invocation.
  </acceptance_criteria>
  <done>A regression-budget gate fails a run that exceeds +20% peak RSS or +25% cold wall-clock and passes a within-budget run, exposing a blocking signal later phases wire at their boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store-disabled baseline → gate | Trusted committed reference the gate divides against |
| measured CurvePoint → gate | Measured run under evaluation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-04-01 | Tampering | budget ratios silently loosened to hide a regression | mitigate | Defaults are pinned to the locked values (1.20 / 1.25) and tested for exact equality; REQUIREMENTS.md marks them revisable only with a recorded decision, so a silent change fails the default-value test. |
| T-63-04-02 | Denial of Service | divide-by-zero when baseline peak RSS or cold ms is 0 | mitigate | The gate guards zero baseline denominators (treat as no-signal / explicit Fail with a "missing baseline" check) rather than panicking; covered by a test. |
| T-63-04-03 | Repudiation | a real regression passes silently (the core failure this plan prevents) | mitigate | is_blocking returns true on Fail and a test proves an over-budget run is blocking; later phases convert this to a non-zero exit, so BENCH-03's "fails its gate rather than passing silently" is enforced. |
</threat_model>

<verification>
- `cargo test -p polint --lib eval::baseline --locked` and `eval::bench::gate --locked` pass.
- `cargo build -p polint --locked` passes.
- Public-surface leak gate unaffected (gate stays pub(crate) under eval::bench).
</verification>

<success_criteria>
- The regression gate enforces the locked +20% peak-RSS and +25% cold-wall-clock budgets against the committed store-disabled baseline.
- An over-budget run fails (blocking); a within-budget run passes; later phases can wire the blocking signal at their boundary.
</success_criteria>

<output>
Create `.planning/phases/63-ground-truth-and-performance-baseline/63-04-SUMMARY.md` when done.
</output>
