---
phase: 08-ci-output-and-graph-commands
plan: "03"
type: execute
wave: 3
depends_on:
  - "08-01"
files_modified:
  - crates/polint-graph/src/lib.rs
  - crates/polint-cli/tests/cli.rs
requirements:
  - CLI-04
  - TEST-02
requirements_addressed:
  - CLI-04
  - TEST-02
autonomous: true
must_haves:
  truths:
    - "`polint graph imports --format dot` emits deterministic valid DOT for import facts."
    - "`polint graph function <name> --format dot` emits deterministic valid DOT for available call facts."
    - "Empty function graph behavior is deterministic and nonfatal."
  artifacts:
    - path: "crates/polint-graph/src/lib.rs"
      provides: "DOT graph construction and graph unit tests"
    - path: "crates/polint-cli/tests/cli.rs"
      provides: "CLI graph command integration tests"
  key_links:
    - from: "crates/polint-cli/src/main.rs"
      to: "crates/polint-graph/src/lib.rs"
      via: "`graph` command builds `ImportGraph` and `FunctionGraph` from `AnalysisDb`"
      pattern: "GraphCommand::Imports"
---

<objective>
Harden DOT graph export behavior for import and function graph commands.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-ci-output-and-graph-commands/08-CONTEXT.md
@crates/polint-graph/src/lib.rs
@crates/polint-cli/src/main.rs
@crates/polint-cli/tests/cli.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic graph unit tests</name>
  <files>crates/polint-graph/src/lib.rs</files>
  <read_first>
    - crates/polint-graph/src/lib.rs
    - crates/polint-core/src/lib.rs
  </read_first>
  <action>Add unit tests in `polint-graph`:
  - `import_graph_dot_is_deterministic` builds an `AnalysisDb` with two files and import facts, renders `ImportGraph::from_db(&db).to_dot()` twice, and asserts exact equality plus `digraph`, source file label, and imported module label are present.
  - `function_graph_dot_includes_available_calls` builds a function fact named `Authorize` with calls `validateUser` and `charge`, renders `FunctionGraph::from_db(&db, "Authorize").to_dot()`, and asserts all three labels plus `digraph` are present.
  - `function_graph_dot_empty_match_is_valid_dot` renders a missing function and asserts the output contains `digraph`.</action>
  <verify>
    <automated>cargo test -p polint-graph --lib graph_dot</automated>
    <automated>rg -n "import_graph_dot_is_deterministic|function_graph_dot_includes_available_calls|function_graph_dot_empty_match_is_valid_dot" crates/polint-graph/src/lib.rs</automated>
  </verify>
  <acceptance_criteria>
    - Graph unit tests pass.
    - DOT output contains `digraph`.
    - Determinism is asserted by comparing two renders exactly.
  </acceptance_criteria>
  <done>Graph helper DOT output has focused unit proof.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add CLI graph command integration tests</name>
  <files>crates/polint-cli/tests/cli.rs</files>
  <read_first>
    - crates/polint-cli/tests/cli.rs
    - crates/polint-cli/src/main.rs
    - crates/polint-graph/src/lib.rs
  </read_first>
  <action>Add CLI integration tests:
  - `graph_imports_outputs_deterministic_dot` creates a temp repo with a TS or Go import, runs `polint graph imports --format dot` twice, asserts exact equality, and asserts stdout contains `digraph`, the source file path, and the import target.
  - `graph_function_outputs_deterministic_dot` creates a temp repo with a function calling another function, runs `polint graph function <name> --format dot` twice, asserts exact equality, and asserts stdout contains `digraph`, the function name, and a called symbol.
  - `graph_function_missing_name_is_nonfatal_valid_dot` runs a missing function name and expects exit 0 with stdout containing `digraph`.</action>
  <verify>
    <automated>cargo test -p polint-cli --test cli graph</automated>
    <automated>rg -n "graph_imports_outputs_deterministic_dot|graph_function_outputs_deterministic_dot|graph_function_missing_name_is_nonfatal_valid_dot" crates/polint-cli/tests/cli.rs</automated>
  </verify>
  <acceptance_criteria>
    - CLI graph tests pass.
    - Import and function graph outputs are byte-identical across repeated runs.
    - Missing function graph is nonfatal and valid DOT.
  </acceptance_criteria>
  <done>Graph commands have CLI-level deterministic DOT proof.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Verify graph command contract</name>
  <files>crates/polint-graph/src/lib.rs, crates/polint-cli/tests/cli.rs</files>
  <read_first>
    - crates/polint-graph/src/lib.rs
    - crates/polint-cli/tests/cli.rs
  </read_first>
  <action>Run graph unit and CLI tests. If DOT output lacks determinism, fix graph construction with sorted inputs/labels only; do not add new graph formats or semantic module resolution.</action>
  <verify>
    <automated>cargo test -p polint-graph --lib</automated>
    <automated>cargo test -p polint-cli --test cli graph</automated>
  </verify>
  <acceptance_criteria>
    - Graph unit tests pass.
    - Graph CLI tests pass.
    - No non-DOT graph format is added.
  </acceptance_criteria>
  <done>DOT graph export is ready for final Phase 8 verification.</done>
</task>

</tasks>

<threat_model>
ASVS L1 posture: graph command output is local metadata; determinism and valid DOT are the main security-relevant controls.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source facts -> DOT output | Local syntactic facts become graph text. |
| graph stdout -> user/CI tools | DOT output may be compared or consumed by graph tooling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | Repudiation | graph output | mitigate | Add repeated-run exact equality tests for import and function graph commands. |
| T-08-03-02 | Tampering | graph labels/order | mitigate | Use deterministic graph construction and tests for expected labels. |
| T-08-03-03 | Denial of Service | missing function graph | mitigate | Missing function names produce valid empty DOT and exit 0 rather than panicking. |
| T-08-03-04 | Information Disclosure | graph output | accept | DOT output contains local file/module/function metadata already analyzed by the local CLI. |
</threat_model>

<verification>
```bash
cargo test -p polint-graph --lib
cargo test -p polint-cli --test cli graph
```
</verification>

<success_criteria>
- Import graph DOT command is deterministic.
- Function graph DOT command is deterministic.
- Empty graph behavior is nonfatal and valid DOT.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ci-output-and-graph-commands/08-03-SUMMARY.md`.
</output>
