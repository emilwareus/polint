---
phase: 08-ci-output-and-graph-commands
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/polint-cli/src/main.rs
  - crates/polint-cli/tests/cli.rs
requirements:
  - CLI-04
  - CLI-05
  - TEST-02
requirements_addressed:
  - CLI-04
  - CLI-05
  - TEST-02
autonomous: true
must_haves:
  truths:
    - "`polint explain <rule-id>` has deterministic known-rule and unknown-rule behavior."
    - "`polint test-rules --format json|sarif` does not print human prelude text before machine-readable output."
    - "`--fail-on warn|error|none` exit behavior is proven for diagnostic-running commands."
    - "Fatal CLI/tool/config errors return exit code 2."
  artifacts:
    - path: "crates/polint-cli/src/main.rs"
      provides: "command dispatch and exit-code behavior"
    - path: "crates/polint-cli/tests/cli.rs"
      provides: "CLI command surface and exit code integration tests"
  key_links:
    - from: "crates/polint-cli/src/main.rs"
      to: "crates/polint-cli/tests/cli.rs"
      via: "`test-rules` delegates to `check` without corrupting machine output"
      pattern: "test_rules_json_output_is_parseable"
---

<objective>
Harden the Phase 8 CLI command contracts and exit-code semantics for `explain`, `test-rules`, `profile-rules`, and fatal command failures.

Purpose: close the command-surface and exit-code parts of `CLI-04`, `CLI-05`, and `TEST-02`.
Execution policy: work directly in `/Users/emilwareus/Development/exlint` on `main`; do not create or use worktrees.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-ci-output-and-graph-commands/08-CONTEXT.md
@crates/polint-cli/src/main.rs
@crates/polint-cli/tests/cli.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CLI command contract and exit-code tests</name>
  <files>crates/polint-cli/tests/cli.rs</files>
  <read_first>
    - crates/polint-cli/tests/cli.rs
    - crates/polint-cli/src/main.rs
    - .planning/phases/08-ci-output-and-graph-commands/08-CONTEXT.md
  </read_first>
  <action>Add focused integration tests for Phase 8 command contracts:
  - `explain_known_rule_prints_metadata` runs `polint explain examples/ts-no-raw-colors`, expects exit 0, and asserts stdout contains the exact rule ID and `Severity:`.
  - `explain_unknown_rule_is_nonfatal_and_clear` runs `polint explain custom/missing`, expects exit 0, and asserts stdout contains `No built-in rule found`.
  - `test_rules_json_output_is_parseable` creates a temp repo with a TS raw-color finding, runs `polint test-rules --profile phase8 --format json --fail-on none`, parses stdout as JSON, and asserts no leading human prelude corrupted stdout.
  - `check_fail_on_warn_error_none_exit_codes_are_stable` proves warning diagnostics return code 1 for `--fail-on warn`, return code 0 for `--fail-on error`, and return code 0 for `--fail-on none`.
  - `fatal_config_parse_error_exits_two` writes invalid `.polint.toml`, runs `polint check`, and expects exit code 2.</action>
  <verify>
    <automated>cargo test -p polint-cli --test cli explain_known_rule_prints_metadata explain_unknown_rule_is_nonfatal_and_clear test_rules_json_output_is_parseable check_fail_on_warn_error_none_exit_codes_are_stable fatal_config_parse_error_exits_two</automated>
    <automated>rg -n "explain_known_rule_prints_metadata|test_rules_json_output_is_parseable|fatal_config_parse_error_exits_two|fail_on_warn_error_none" crates/polint-cli/tests/cli.rs</automated>
  </verify>
  <acceptance_criteria>
    - The new tests compile.
    - The JSON test parses stdout with `serde_json::from_str` or the existing `stdout_json` helper.
    - The fail-on test asserts exit codes for warn, error, and none thresholds.
    - The fatal config test asserts `.code(2)`.
  </acceptance_criteria>
  <done>CLI command behavior and exit-code gaps are captured by failing or passing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make test-rules preserve machine-readable stdout</name>
  <files>crates/polint-cli/src/main.rs, crates/polint-cli/tests/cli.rs</files>
  <read_first>
    - crates/polint-cli/src/main.rs
    - crates/polint-cli/tests/cli.rs
  </read_first>
  <action>Update `Command::TestRules(args)` so it only prints the human prelude `Running rule tests against the current repository fixtures.` when `args.format` is `FormatArg::Human`. For `FormatArg::Json` and `FormatArg::Sarif`, it must delegate directly to `check` without writing any extra stdout before the renderer output. Preserve existing `check` and `exit_code_for` semantics.</action>
  <verify>
    <automated>cargo test -p polint-cli --test cli test_rules_json_output_is_parseable</automated>
    <automated>cargo test -p polint-cli --test cli check_fail_on_warn_error_none_exit_codes_are_stable</automated>
    <automated>rg -n "Command::TestRules|FormatArg::Human|Running rule tests" crates/polint-cli/src/main.rs</automated>
  </verify>
  <acceptance_criteria>
    - `test_rules_json_output_is_parseable` passes.
    - `Command::TestRules` contains a `matches!(args.format, FormatArg::Human)` or equivalent branch before printing the prelude.
    - JSON and SARIF test-rules output contains only renderer output on stdout.
  </acceptance_criteria>
  <done>`test-rules` no longer corrupts machine-readable stdout.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Verify CLI command contracts</name>
  <files>crates/polint-cli/src/main.rs, crates/polint-cli/tests/cli.rs</files>
  <read_first>
    - crates/polint-cli/tests/cli.rs
  </read_first>
  <action>Run all targeted Phase 8 CLI command tests from this plan and fix only failures directly related to command dispatch, machine-readable stdout, or exit-code behavior. Do not redesign command names or add dynamic custom-rule loading.</action>
  <verify>
    <automated>cargo test -p polint-cli --test cli explain</automated>
    <automated>cargo test -p polint-cli --test cli test_rules</automated>
    <automated>cargo test -p polint-cli --test cli fail_on</automated>
    <automated>cargo test -p polint-cli --test cli fatal_config_parse_error_exits_two</automated>
  </verify>
  <acceptance_criteria>
    - All command contract tests pass.
    - `polint check`, `polint test-rules`, and `polint profile-rules` retain existing command names.
    - No tests rely on broad substring-only output when a structured parse is possible.
  </acceptance_criteria>
  <done>CLI command and exit-code contract is ready for SARIF/graph hardening.</done>
</task>

</tasks>

<threat_model>
ASVS L1 posture: no high severity threat is accepted; mitigated threats use deterministic, parseable output and explicit exit-code tests.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI stdout -> CI parser | JSON/SARIF stdout is consumed by CI tooling. |
| diagnostics -> process exit | Exit codes drive CI pass/fail behavior. |
| user command -> rule metadata | `explain` exposes built-in rule information. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Tampering | machine-readable stdout | mitigate | Add tests proving `test-rules --format json` emits parseable JSON without human prelude text. |
| T-08-01-02 | Spoofing | CI status | mitigate | Add exit-code tests for warn/error/none thresholds and fatal config errors. |
| T-08-01-03 | Repudiation | explain output | mitigate | Add deterministic known/unknown rule explain tests. |
| T-08-01-04 | Information Disclosure | explain output | accept | Built-in rule IDs, severities, and descriptions are public local CLI metadata. |
</threat_model>

<verification>
Run after completing all tasks in this plan:

```bash
cargo test -p polint-cli --test cli explain
cargo test -p polint-cli --test cli test_rules
cargo test -p polint-cli --test cli fail_on
cargo test -p polint-cli --test cli fatal_config_parse_error_exits_two
```
</verification>

<success_criteria>
- CLI command surface tests exist for explain/test-rules/fail thresholds/fatal error.
- Machine-readable `test-rules` output is parseable.
- Fatal tool/config errors return code 2.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ci-output-and-graph-commands/08-01-SUMMARY.md`.
</output>
