---
phase: 08-ci-output-and-graph-commands
plan: "02"
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - crates/polint-diagnostics/src/lib.rs
  - crates/polint-cli/tests/cli.rs
requirements:
  - DIAG-03
  - TEST-02
  - TEST-03
requirements_addressed:
  - DIAG-03
  - TEST-02
  - TEST-03
autonomous: true
must_haves:
  truths:
    - "SARIF-like output is valid JSON and includes rule IDs, locations, messages, severities, and fingerprints."
    - "SARIF-like renderer output has a representative snapshot."
    - "`polint check --format sarif` is tested through CLI integration."
  artifacts:
    - path: "crates/polint-diagnostics/src/lib.rs"
      provides: "SARIF-like renderer and renderer snapshots"
    - path: "crates/polint-cli/tests/cli.rs"
      provides: "CLI SARIF parseability and field assertions"
  key_links:
    - from: "crates/polint-diagnostics/src/lib.rs"
      to: "crates/polint-cli/tests/cli.rs"
      via: "`OutputFormat::Sarif` renderer consumed by CLI `--format sarif`"
      pattern: "render_sarif"
---

<objective>
Harden SARIF-like CI diagnostic output and add representative snapshot/CLI proof.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-ci-output-and-graph-commands/08-CONTEXT.md
@crates/polint-diagnostics/src/lib.rs
@crates/polint-cli/tests/cli.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SARIF-like renderer snapshot</name>
  <files>crates/polint-diagnostics/src/lib.rs</files>
  <read_first>
    - crates/polint-diagnostics/src/lib.rs
    - .planning/phases/08-ci-output-and-graph-commands/08-CONTEXT.md
  </read_first>
  <action>Add a unit test named `render_sarif_snapshot_includes_ci_fields` using the existing `contract_diagnostic()` helper. It must render `OutputFormat::Sarif`, parse it as `serde_json::Value`, assert:
  - `/version` equals `2.1.0`
  - `/runs/0/tool/driver/name` equals `polint`
  - `/runs/0/results/0/ruleId` equals `project/rule`
  - `/runs/0/results/0/level` equals `error`
  - `/runs/0/results/0/message/text` equals `policy failed`
  - `/runs/0/results/0/fingerprints/polint` equals `fingerprint-123`
  - `/runs/0/results/0/locations/0/physicalLocation/artifactLocation/uri` equals `src/lib.rs`
  Then snapshot the rendered SARIF string with `insta::assert_snapshot!`.</action>
  <verify>
    <automated>cargo test -p polint-diagnostics --lib render_sarif_snapshot_includes_ci_fields</automated>
    <automated>rg -n "render_sarif_snapshot_includes_ci_fields|fingerprints|artifactLocation|ruleId" crates/polint-diagnostics/src/lib.rs</automated>
  </verify>
  <acceptance_criteria>
    - SARIF renderer snapshot test passes.
    - Test asserts all roadmap-required SARIF-like fields through JSON pointers.
    - Snapshot includes `version`, `tool.driver.name`, `ruleId`, `level`, `message`, `locations`, and `fingerprints`.
  </acceptance_criteria>
  <done>SARIF-like renderer has CI field snapshot coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden CLI SARIF parseability and fail thresholds</name>
  <files>crates/polint-cli/tests/cli.rs</files>
  <read_first>
    - crates/polint-cli/tests/cli.rs
    - crates/polint-diagnostics/src/lib.rs
  </read_first>
  <action>Extend CLI SARIF integration coverage. Add or expand a test named `sarif_output_includes_ci_fields_and_honors_fail_threshold` that creates a temp repo with a warning diagnostic, runs:
  - `polint check --profile phase8 --format sarif --fail-on none` and parses stdout as JSON.
  - `polint check --profile phase8 --format sarif --fail-on warn` and expects exit code 1.
  Assert the SARIF JSON contains `ruleId`, `level`, `message.text`, `fingerprints.polint`, and `locations[0].physicalLocation.artifactLocation.uri` for the produced diagnostic.</action>
  <verify>
    <automated>cargo test -p polint-cli --test cli sarif_output_includes_ci_fields_and_honors_fail_threshold</automated>
    <automated>rg -n "sarif_output_includes_ci_fields_and_honors_fail_threshold|fingerprints|artifactLocation|fail-on" crates/polint-cli/tests/cli.rs</automated>
  </verify>
  <acceptance_criteria>
    - CLI SARIF test parses stdout as JSON.
    - CLI SARIF test asserts every roadmap-required field.
    - CLI SARIF fail-on warn path exits with code 1.
  </acceptance_criteria>
  <done>CLI SARIF output is proven parseable and CI-usable.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Verify SARIF-like output contract</name>
  <files>crates/polint-diagnostics/src/lib.rs, crates/polint-cli/tests/cli.rs</files>
  <read_first>
    - crates/polint-diagnostics/src/lib.rs
    - crates/polint-cli/tests/cli.rs
  </read_first>
  <action>Run targeted diagnostics and CLI SARIF tests. If the current renderer lacks a required field, add the smallest deterministic field to `render_sarif`; do not broaden into full SARIF certification.</action>
  <verify>
    <automated>cargo test -p polint-diagnostics --lib sarif</automated>
    <automated>cargo test -p polint-cli --test cli sarif</automated>
  </verify>
  <acceptance_criteria>
    - Targeted SARIF tests pass.
    - No human prelude text appears in SARIF stdout.
    - The renderer remains deterministic for the same ordered diagnostics.
  </acceptance_criteria>
  <done>SARIF-like CI output is ready for graph command hardening.</done>
</task>

</tasks>

<threat_model>
ASVS L1 posture: no high severity threat is accepted; SARIF-like output must be valid machine-readable JSON and must not overclaim full certification.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| diagnostics -> SARIF JSON | Local diagnostics become CI-ingested JSON. |
| stdout -> CI parser | CI tooling parses stdout as JSON. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Tampering | SARIF fields | mitigate | Add JSON-pointer assertions and snapshots for required fields. |
| T-08-02-02 | Spoofing | SARIF severity | mitigate | Assert severity-to-level mapping in renderer and CLI output. |
| T-08-02-03 | Repudiation | SARIF fingerprints | mitigate | Assert stable fingerprint is present in `fingerprints.polint`. |
| T-08-02-04 | Information Disclosure | SARIF payload | mitigate | Output only diagnostic metadata already present in human/JSON diagnostics; no source text is added. |
</threat_model>

<verification>
```bash
cargo test -p polint-diagnostics --lib sarif
cargo test -p polint-cli --test cli sarif
```
</verification>

<success_criteria>
- SARIF-like renderer snapshot exists.
- CLI SARIF output has structured field assertions.
- SARIF fail-threshold behavior is tested.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ci-output-and-graph-commands/08-02-SUMMARY.md`.
</output>
