---
phase: 08-ci-output-and-graph-commands
plan: "04"
type: execute
wave: 4
depends_on:
  - "08-02"
  - "08-03"
files_modified:
  - crates/polint-cli/tests/cli.rs
  - crates/polint-diagnostics/src/lib.rs
  - crates/polint-graph/src/lib.rs
requirements:
  - CLI-04
  - CLI-05
  - DIAG-03
  - TEST-02
  - TEST-03
requirements_addressed:
  - CLI-04
  - CLI-05
  - DIAG-03
  - TEST-02
  - TEST-03
autonomous: true
must_haves:
  truths:
    - "All Phase 8 command surfaces are covered by integration or unit tests."
    - "SARIF-like, JSON, and human diagnostic snapshots remain stable."
    - "Full workspace formatting, clippy, and tests pass."
    - "Phase 8 does not claim dynamic custom-rule loading, full SARIF certification, or semantic graph resolution."
  artifacts:
    - path: ".planning/phases/08-ci-output-and-graph-commands/08-04-SUMMARY.md"
      provides: "final Phase 8 verification evidence"
    - path: "crates/polint-cli/tests/cli.rs"
      provides: "end-to-end CI command coverage"
    - path: "crates/polint-diagnostics/src/lib.rs"
      provides: "diagnostic renderer snapshot coverage"
  key_links:
    - from: ".planning/phases/08-ci-output-and-graph-commands/08-CONTEXT.md"
      to: ".planning/phases/08-ci-output-and-graph-commands/08-04-SUMMARY.md"
      via: "final evidence covers every auto-selected decision D-01 through D-20"
      pattern: "D-01"
---

<objective>
Close Phase 8 with final CI output, graph command, exit-code, and snapshot verification evidence.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-ci-output-and-graph-commands/08-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@crates/polint-cli/tests/cli.rs
@crates/polint-diagnostics/src/lib.rs
@crates/polint-graph/src/lib.rs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run targeted Phase 8 verification matrix</name>
  <files>crates/polint-cli/tests/cli.rs, crates/polint-diagnostics/src/lib.rs, crates/polint-graph/src/lib.rs</files>
  <read_first>
    - .planning/phases/08-ci-output-and-graph-commands/08-CONTEXT.md
    - crates/polint-cli/tests/cli.rs
    - crates/polint-diagnostics/src/lib.rs
    - crates/polint-graph/src/lib.rs
  </read_first>
  <action>Run targeted tests that prove Phase 8 behavior. Fix only failures directly related to Phase 8 command surface, SARIF-like output, graph DOT output, snapshots, or exit codes.</action>
  <verify>
    <automated>cargo test -p polint-cli --test cli explain</automated>
    <automated>cargo test -p polint-cli --test cli test_rules</automated>
    <automated>cargo test -p polint-cli --test cli fail_on</automated>
    <automated>cargo test -p polint-cli --test cli sarif</automated>
    <automated>cargo test -p polint-cli --test cli graph</automated>
    <automated>cargo test -p polint-diagnostics --lib sarif</automated>
    <automated>cargo test -p polint-graph --lib</automated>
    <automated>cargo test -p polint-rules --test snapshots</automated>
  </verify>
  <acceptance_criteria>
    - All targeted Phase 8 tests pass.
    - Existing Phase 6 human/JSON snapshots still pass.
    - No test introduces fixed timing assertions for `profile-rules`.
  </acceptance_criteria>
  <done>Targeted Phase 8 verification matrix passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run full workspace verification</name>
  <files>workspace</files>
  <read_first>
    - Cargo.toml
  </read_first>
  <action>Run the full workspace verification commands after all Phase 8 changes. If formatting fails, run `cargo fmt`, commit the formatting-only change, and rerun `cargo fmt -- --check`.</action>
  <verify>
    <automated>cargo fmt -- --check</automated>
    <automated>cargo clippy --workspace --all-targets -- -D warnings</automated>
    <automated>cargo test --workspace</automated>
  </verify>
  <acceptance_criteria>
    - `cargo fmt -- --check` passes.
    - `cargo clippy --workspace --all-targets -- -D warnings` passes.
    - `cargo test --workspace` passes.
  </acceptance_criteria>
  <done>Workspace quality gates pass after Phase 8.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Record final Phase 8 evidence</name>
  <files>.planning/phases/08-ci-output-and-graph-commands/08-04-SUMMARY.md</files>
  <read_first>
    - .planning/phases/08-ci-output-and-graph-commands/08-CONTEXT.md
    - .planning/phases/08-ci-output-and-graph-commands/08-01-SUMMARY.md
    - .planning/phases/08-ci-output-and-graph-commands/08-02-SUMMARY.md
    - .planning/phases/08-ci-output-and-graph-commands/08-03-SUMMARY.md
  </read_first>
  <action>Create `08-04-SUMMARY.md` using the GSD summary template. Include exact evidence lines for these Phase 8 success criteria: command surface implemented, SARIF-like CI fields, fail-on exit codes, DOT graph exports, integration tests, and snapshots. Explicitly state that dynamic rule loading, full SARIF certification, and semantic graph resolution remain out of scope.</action>
  <verify>
    <automated>rg -n "command surface|SARIF-like|fail-on|DOT graph|snapshot|out of scope" .planning/phases/08-ci-output-and-graph-commands/08-04-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - Summary lists commands/tests proving all five Phase 8 roadmap success criteria.
    - Summary contains the phrase `SARIF-like` and does not claim full SARIF certification.
    - Summary lists full workspace verification commands and their pass status.
  </acceptance_criteria>
  <done>Phase 8 has final verification evidence ready for phase-level review.</done>
</task>

</tasks>

<threat_model>
ASVS L1 posture: no high severity threat is accepted; final verification must prove CI output and exit behavior are deterministic and parseable.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local diagnostics -> CI output | Human/JSON/SARIF-like diagnostics are consumed by users and CI. |
| command output -> automation | Exit codes and stdout are used by scripts. |
| planning evidence -> verification | Summary evidence guides phase verification and future audits. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | Repudiation | phase evidence | mitigate | Record exact commands and pass status in `08-04-SUMMARY.md`. |
| T-08-04-02 | Spoofing | CI pass/fail | mitigate | Run targeted exit-code tests and full workspace tests. |
| T-08-04-03 | Tampering | snapshots | mitigate | Run diagnostic snapshots after SARIF-like renderer changes. |
| T-08-04-04 | Information Disclosure | summary claims | mitigate | Explicitly state out-of-scope items and avoid overclaiming full SARIF or dynamic loading. |
</threat_model>

<verification>
```bash
cargo fmt -- --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
```
</verification>

<success_criteria>
- All Phase 8 roadmap success criteria have automated evidence.
- Full workspace quality gates pass.
- Phase 8 summary is ready for phase verification and security closeout.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ci-output-and-graph-commands/08-04-SUMMARY.md`.
</output>
