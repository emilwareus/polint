---
phase: 51-adaptation-model-layer
plan: 04
type: execute
wave: 4
depends_on: [51-01, 51-02, 51-03]
files_modified:
  - tests/eval-fixtures/adaptation-model/
  - crates/polint/src/eval/adaptation.rs
  - crates/polint/src/eval/delta.rs
  - crates/polint/src/eval/report.rs
  - crates/polint/src/analysis/adaptation/
  - crates/polint/src/analysis/semantic_graph/
  - crates/polint/src/analysis/solver/
  - crates/polint/tests/public_surface_leak.rs
  - .planning/ROADMAP.md
autonomous: true
requirements: [ADAPT-01, ADAPT-02]

must_haves:
  truths:
    - "D-24/D-25: native fixtures prove accepted model edge, rejected non-resolving target, broad pattern rejection, oracle-shaped RHS rejection, prompt sanitizer, cache invalidation, and held-out report behavior."
    - "D-26: public-surface leak gate remains unchanged; do not extend `ALLOWED_PRELUDE`."
    - "Roadmap SC1/SC2/SC3/SC4: Phase 51 closes only when private model validation, adapted reporting, sandboxing, and ModelEdge accept/reject proof are all documented."
  artifacts:
    - path: "tests/eval-fixtures/adaptation-model/"
      provides: "complete accepted/rejected/sandbox/cache/held-out fixture suite"
      contains: "adaptation"
    - path: "crates/polint/tests/public_surface_leak.rs"
      provides: "public API leak proof"
      contains: "ALLOWED_PRELUDE"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 51 plan list and closeout bookkeeping"
      contains: "51-04"
---

<objective>
Close Phase 51 with executable verification across accepted/rejected model facts, adapted reporting, sandbox leak prevention, cache invalidation, held-out deltas, public API discipline, full regression, and roadmap bookkeeping.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/51-adaptation-model-layer/51-CONTEXT.md
@.planning/phases/51-adaptation-model-layer/51-01-PLAN.md
@.planning/phases/51-adaptation-model-layer/51-02-PLAN.md
@.planning/phases/51-adaptation-model-layer/51-03-PLAN.md
@crates/polint/tests/public_surface_leak.rs
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run focused adaptation-model fixture gates</name>
  <files>tests/eval-fixtures/adaptation-model/, crates/polint/src/analysis/adaptation/, crates/polint/src/analysis/semantic_graph/, crates/polint/src/analysis/solver/</files>
  <action>
    Run the accepted/rejected model, `ModelEdge`, cache invalidation, budget, and polyglot fixture gates created by Plans 01 and 02. Fix any deterministic ordering, digest, or rejection-reason gaps.
  </action>
  <verify>
    <automated>cargo test -p polint adaptation_model 2>&1 | tail -80</automated>
  </verify>
  <acceptance_criteria>
    - Accepted facts emit `ModelEdge`; rejected facts emit none.
    - Cache invalidation changes on behavior-affecting model edits and stays stable on no-op reorder.
    - Budget hits are explicit.
    - No cross-language model leakage exists.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Run adapted benchmark reporting gates</name>
  <files>crates/polint/src/eval/{adaptation.rs,delta.rs,report.rs,markdown.rs,runner.rs}, tests/eval-fixtures/adaptation-model/</files>
  <action>
    Run adapted-report fixtures for prompt hash, changed model files, accepted/rejected facts, unknown delta, precision/recall delta, runtime/cache delta, sandbox forbidden paths, and held-out subset deltas. Keep claims local and truthful.
  </action>
  <verify>
    <automated>cargo test -p polint eval::adaptation eval::delta eval::markdown 2>&1 | tail -80</automated>
  </verify>
  <acceptance_criteria>
    - Adapted report includes every ADAPT-02 field.
    - Prompt/sandbox fixtures prove forbidden oracle paths and contents are unavailable.
    - Held-out deltas are separated from model-selection deltas.
    - New false positives and precision losses are visible.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Run public-surface, full test, and clippy gates</name>
  <files>crates/polint/tests/public_surface_leak.rs, crates/polint/src/</files>
  <action>
    Run the public-surface leak gate without extending `ALLOWED_PRELUDE`, then run the full polint test suite and clippy. Fix visibility warnings by tightening visibility, not weakening lints.
  </action>
  <verify>
    <automated>cargo test -p polint --test public_surface_leak 2>&1 | tail -30 && cargo test -p polint 2>&1 | tail -50 && cargo clippy -p polint --all-targets 2>&1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - Public leak gate passes with no adaptation internals exposed.
    - `cargo test -p polint` passes.
    - `cargo clippy -p polint --all-targets` passes with no new warnings.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Record verification and roadmap closeout</name>
  <files>.planning/phases/51-adaptation-model-layer/51-VERIFICATION.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
    Create Phase 51 verification with requirement coverage for ADAPT-01 and ADAPT-02, fixture/test evidence, benchmark reporting evidence, sandbox proof, public leak proof, and any honest caveats. Update roadmap plan list/progress and state through GSD commands where available.
  </action>
  <verify>
    <automated>gsd-sdk query find-phase 51 && gsd-sdk query audit-uat</automated>
  </verify>
  <acceptance_criteria>
    - Phase 51 has summaries for all four plans and a `51-VERIFICATION.md`.
    - ROADMAP lists 51-01 through 51-04 and Phase 51 progress is complete.
    - STATE points to the correct next phase after closeout.
    - No outstanding UAT items block Phase 51 completion.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Risk | Mitigation |
|----------|------|------------|
| Final verification | Green narrow tests can miss integration regressions. | Run focused gates, full `cargo test -p polint`, clippy, and public leak tests. |
| Public claims | Phase 51 can overstate benchmark quality before Phase 54. | Verification records local adapted/held-out evidence only and defers hard floors. |
| Planning state | Phase can appear complete without verification artifacts. | Require summaries, `51-VERIFICATION.md`, roadmap progress, and state update. |

## STRIDE Notes

- Repudiation: verification records commands and outcomes.
- Information disclosure: final leak and sandbox gates prove internal/oracle data stays private.
- Tampering: cache invalidation fixtures prove model edits affect adapted output.
</threat_model>

<verification>
- `cargo test -p polint adaptation_model`
- `cargo test -p polint eval::adaptation eval::delta eval::markdown`
- `cargo test -p polint --test public_surface_leak`
- `cargo test -p polint`
- `cargo clippy -p polint --all-targets`
- `gsd-sdk query find-phase 51`
- `gsd-sdk query audit-uat`
</verification>

<success_criteria>
- All Phase 51 success criteria are proven by code, fixtures, reports, and planning artifacts.
- ADAPT-01 and ADAPT-02 are covered and ready for GSD verification.
- Phase 51 closes without exposing adaptation internals publicly or leaking oracle labels.
</success_criteria>
