---
phase: 27-layered-module-package-topology-graph
plan: 06
type: execute
wave: 5
depends_on: [27-05]
files_modified:
  - crates/polint/src/eval/model.rs
  - crates/polint/src/eval/observed.rs
  - crates/polint/src/eval/fixtures.rs
  - crates/polint/src/eval/matcher.rs
  - crates/polint/src/eval/metrics.rs
  - crates/polint/tests/cli.rs
  - tests/eval-fixtures/module-topology/core/expected.polint-eval.toml
  - tests/eval-fixtures/module-topology/core/repo/.polint.toml
  - tests/eval-fixtures/module-topology/core/repo/services/api/go.mod
  - tests/eval-fixtures/module-topology/core/repo/services/api/go.sum
  - tests/eval-fixtures/module-topology/core/repo/services/api/main.go
  - tests/eval-fixtures/module-topology/core/repo/services/api/main_test.go
  - tests/eval-fixtures/module-topology/core/repo/web/package.json
  - tests/eval-fixtures/module-topology/core/repo/web/package-lock.json
  - tests/eval-fixtures/module-topology/core/repo/web/tsconfig.json
  - tests/eval-fixtures/module-topology/core/repo/web/src/app.ts
  - tests/eval-fixtures/module-topology/core/repo/web/src/app.test.ts
  - tests/eval-fixtures/module-topology/core/repo/web/generated/client.generated.ts
  - docs/facts/imports.md
autonomous: true
requirements: [SAE-SEM-02]
requirements_addressed: [SAE-SEM-02]
must_haves:
  truths:
    - "Internal eval fixtures assert roots, packages, source sets, declared requirements, resolved dependency edges, overlays, import-to-package rows, Go monorepos, TS/JS workspaces, and cache reuse."
    - "Public check, inspect rule, polint test JSON, SDK prelude, runner, and docs do not expose new topology internals."
    - "Existing public ResolvedImports and ModuleGraphFacts behavior remains compatible for external rule consumers."
  artifacts:
    - path: "tests/eval-fixtures/module-topology/core/expected.polint-eval.toml"
      provides: "Native topology fixture expected rows"
      contains: "module-topology-core"
    - path: "crates/polint/src/eval/observed.rs"
      provides: "Internal topology row observation for eval"
      contains: "topology"
    - path: "crates/polint/tests/cli.rs"
      provides: "Public no-leak and compatibility integration tests"
      contains: "module_topology_internals_stay_private"
  key_links:
    - from: "crates/polint/src/eval/observed.rs"
      to: "AnalysisDb topology accessors"
      via: "eval observation"
      pattern: "workspace_roots\\(\\)"
    - from: "crates/polint/tests/cli.rs"
      to: "public CLI JSON"
      via: "no-leak assertions"
      pattern: "module_topology_internals_stay_private"
---

<objective>
Add evaluation fixtures, cache regression coverage, public-boundary proof, and narrow docs alignment for Phase 27.

Purpose: The topology graph is private, but it must be objectively verified through internal eval and must not leak through supported public surfaces.
Output: Native eval fixture coverage, CLI public compatibility/no-leak tests, and existing public docs wording only where behavior limits need clarification.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-layered-module-package-topology-graph/27-CONTEXT.md
@research/module-graph/VALIDATION.md
@.planning/phases/22-internal-evaluation-harness-mvp/22-03-SUMMARY.md
@.planning/phases/24-persistent-layer-cache-for-existing-cheap-facts/24-05-SUMMARY.md
@.planning/phases/26-semantic-index-deepening/26-06-SUMMARY.md
@crates/polint/src/eval/model.rs
@crates/polint/src/eval/observed.rs
@crates/polint/src/eval/fixtures.rs
@crates/polint/tests/cli.rs
@docs/facts/imports.md
@docs/facts/resolved-imports.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Teach internal eval to observe topology rows</name>
  <files>crates/polint/src/eval/model.rs, crates/polint/src/eval/observed.rs, crates/polint/src/eval/matcher.rs, crates/polint/src/eval/metrics.rs</files>
  <read_first>
    - crates/polint/src/eval/model.rs
    - crates/polint/src/eval/observed.rs
    - crates/polint/src/eval/matcher.rs
    - crates/polint/src/eval/metrics.rs
    - crates/polint/src/module_graph/topology.rs
    - .planning/phases/26-semantic-index-deepening/26-06-SUMMARY.md
  </read_first>
  <behavior>
    - Test observed topology rows include families `WorkspaceRoot`, `TopologyPackage`, `SourceSet`, `DependencyRequirement`, `ResolvedDependencyEdge`, `ImportToPackage`, and `RepoTopologyOverlay`.
    - Test observed statuses include `resolved`, `external`, `missing_lockfile`, `unsupported`, `dynamic`, `ambiguous`, `undeclared`, and `outside_workspace`.
    - Test topology unknown-like statuses contribute to eval unknown metrics.
  </behavior>
  <action>Add `FixtureArea::ModuleTopology` serialized as `module-topology`. Add observed fact family names for all seven topology families. Extend eval observation to read `db.workspace_roots()`, `db.topology_packages()`, `db.source_sets()`, `db.dependency_requirements()`, `db.resolved_dependency_edges()`, `db.import_to_package_edges()`, and `db.repo_topology_overlays()`, emitting `ObservedFact` rows with producer id, precision labels, status labels, and compact payload strings containing only normalized stable-key parts. Add status labels exactly `resolved`, `external`, `missing_lockfile`, `unsupported`, `dynamic`, `ambiguous`, `undeclared`, and `outside_workspace`. Keep eval code under `#[cfg(test)]` and crate-private per D-03, D-20, D-23, and D-24.</action>
  <verify>
    <automated>cargo test -p polint --lib eval::topology_rows --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "ModuleTopology|WorkspaceRoot|TopologyPackage|ImportToPackage|outside_workspace|undeclared" crates/polint/src/eval/model.rs crates/polint/src/eval/observed.rs crates/polint/src/eval/metrics.rs` returns matches.
    - `rg -n "pub fn .*topology|pub struct .*Topology" crates/polint/src/eval crates/polint/src/sdk crates/polint/src/runner.rs` returns no public topology API matches.
    - `cargo test -p polint --lib eval::topology_rows --locked` passes.
  </acceptance_criteria>
  <done>Internal eval can observe and score topology row families and uncertainty statuses without public exposure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add module topology native fixture and cache invalidation proof</name>
  <files>crates/polint/src/eval/fixtures.rs, tests/eval-fixtures/module-topology/core/expected.polint-eval.toml, tests/eval-fixtures/module-topology/core/repo/.polint.toml, tests/eval-fixtures/module-topology/core/repo/services/api/go.mod, tests/eval-fixtures/module-topology/core/repo/services/api/go.sum, tests/eval-fixtures/module-topology/core/repo/services/api/main.go, tests/eval-fixtures/module-topology/core/repo/services/api/main_test.go, tests/eval-fixtures/module-topology/core/repo/web/package.json, tests/eval-fixtures/module-topology/core/repo/web/package-lock.json, tests/eval-fixtures/module-topology/core/repo/web/tsconfig.json, tests/eval-fixtures/module-topology/core/repo/web/src/app.ts, tests/eval-fixtures/module-topology/core/repo/web/src/app.test.ts, tests/eval-fixtures/module-topology/core/repo/web/generated/client.generated.ts</files>
  <read_first>
    - crates/polint/src/eval/fixtures.rs
    - tests/eval-fixtures/semantic-index/core/expected.polint-eval.toml
    - tests/eval-fixtures/cache/layer-cache/expected.polint-eval.toml
    - tests/eval-fixtures/README.md
    - .planning/phases/27-layered-module-package-topology-graph/27-CONTEXT.md
  </read_first>
  <behavior>
    - Test fixture passes with facts for Go module root, TS workspace root, source/test/generated source sets, declared requirements, selected lockfile edges, import-to-package source/test/generated contexts, and overlays.
    - Test cold/warm runs show `layer_cache.provider.polint.module_graph.hits` and `layer_cache.provider.polint.module_topology.hits` on warm.
    - Test editing `web/package.json` or `services/api/go.mod` invalidates the relevant topology layer and recomputes.
  </behavior>
  <action>Create `tests/eval-fixtures/module-topology/core` with a repo containing `.polint.toml`, `services/api/go.mod`, `services/api/go.sum`, `services/api/main.go`, `services/api/main_test.go`, `web/package.json`, `web/package-lock.json`, `web/tsconfig.json`, `web/src/app.ts`, `web/src/app.test.ts`, and `web/generated/client.generated.ts`. Configure `.polint.toml` with `[workspace] include = ["services/api/**/*.go", "web/**/*.ts"]` and `[languages.go] module_roots = ["services/api"]`. Add `run_module_topology_core_fixture_for_test` to `eval/fixtures.rs` using `AnalysisPlan::from_capability_names_for_test(&["resolved_imports", "module_graph", "symbols", "references"])` so module graph, symbol graph, and module topology run. The expected TOML must include partial fact assertions for family `WorkspaceRoot` stable key `services/api`, `TopologyPackage` stable key `web`, `SourceSet` stable keys `main_test.go` and `client.generated.ts`, `DependencyRequirement` stable keys `github.com/acme/lib` and `@scope/lib`, `ResolvedDependencyEdge` status `resolved`, `ImportToPackage` statuses `resolved` and `external`, `RepoTopologyOverlay` kinds `GeneratedZone` and `TestOnlyVisibility`, plus warm cache invariants for `polint.module_graph` and `polint.module_topology` per D-05, D-06, D-07, D-08, D-10, D-13, D-17, D-20, and D-21.</action>
  <verify>
    <automated>cargo test -p polint --lib eval::fixtures::module_topology_core --locked</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/eval-fixtures/module-topology/core/expected.polint-eval.toml && test -f tests/eval-fixtures/module-topology/core/repo/services/api/go.mod && test -f tests/eval-fixtures/module-topology/core/repo/web/package.json`
    - `rg -n "module-topology-core|WorkspaceRoot|TopologyPackage|SourceSet|DependencyRequirement|ResolvedDependencyEdge|ImportToPackage|RepoTopologyOverlay|polint.module_topology" tests/eval-fixtures/module-topology/core/expected.polint-eval.toml` returns matches.
    - `cargo test -p polint --lib eval::fixtures::module_topology_core --locked` passes.
  </acceptance_criteria>
  <done>Native eval proves Phase 27 topology behavior, Go monorepo support, TS workspace facts, import classification, overlays, and cache participation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove public compatibility and update existing docs only if needed</name>
  <files>crates/polint/tests/cli.rs, docs/facts/imports.md</files>
  <read_first>
    - crates/polint/tests/cli.rs
    - docs/facts/imports.md
    - docs/facts/resolved-imports.md
    - .claude/skills/polint/SKILL.md
    - AGENTS.md
    - docs/API-VISIBILITY-PLAN.md
  </read_first>
  <behavior>
    - Test external temp-repo rules importing only `polint::sdk::prelude::*` can still request `ResolvedImports<'_>` and `ModuleGraphFacts<'_>` and receive diagnostics.
    - Test `polint check --format json`, `polint inspect rule --format json`, and `polint test --format json` do not contain `workspace_roots`, `topology_packages`, `source_sets`, `dependency_requirements`, `resolved_dependency_edges`, `import_to_package_edges`, `repo_topology_overlays`, `polint.module_topology`, or `module-topology-facts`.
    - Test CLI help does not contain `topology`, `Packages`, `Dependencies`, `SourceSets`, or `RepoTopology`.
  </behavior>
  <action>Add CLI integration test `module_topology_internals_stay_private` using a temp repo with a local rule pack that imports only `polint::sdk::prelude::*`, registers through `polint::runner::run_cli`, and requests `ResolvedImports<'_>` plus `ModuleGraphFacts<'_>`. Assert public JSON from `check`, `inspect rule`, and `polint test` excludes the exact strings `workspace_roots`, `topology_packages`, `source_sets`, `dependency_requirements`, `resolved_dependency_edges`, `import_to_package_edges`, `repo_topology_overlays`, `polint.module_topology`, `module-topology-facts`, `Packages<'_>`, `Dependencies<'_>`, `SourceSets<'_>`, and `RepoTopology<'_>` per D-03, D-14, D-24, and D-26. Update `docs/facts/imports.md` only if a sentence is needed to clarify that public resolved-import/module-graph facts remain the supported relationship surface and richer package/topology internals are not SDK views per D-25; do not document package-manager internals, cache internals, provider manifests, or topology query APIs.</action>
  <verify>
    <automated>cargo test -p polint --test cli module_topology_internals_stay_private --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "module_topology_internals_stay_private|workspace_roots|polint.module_topology|module-topology-facts" crates/polint/tests/cli.rs` returns matches in assertions.
    - `rg -n "Packages<'_|Dependencies<'_|SourceSets<'_|RepoTopology<'_|polint topology|polint facts" docs README.md crates/polint/src/cli crates/polint/src/sdk` returns no matches outside the no-leak test assertions.
    - `cargo test -p polint --test cli external_rule_consumes_module_relationship_facts_through_public_sdk --locked` passes.
  </acceptance_criteria>
  <done>Existing public relationship views remain compatible and new topology internals stay out of public CLI, SDK, runner, crate-root, and docs surfaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internal eval -> repository fixtures | Fixture repos are copied and executed through the real kernel in temp directories. |
| internal topology facts -> public CLI/SDK/docs | New topology rows must remain private while existing public relationship views keep working. |
| cache observations -> deterministic reports | Runtime/cache evidence must not include machine-local paths or timestamps in hashes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-06-01 | I | Public check/inspect/test JSON | mitigate | Add explicit no-leak assertions for topology family names, provider ids, schemas, and proposed public view names. |
| T-27-06-02 | T | Eval fixture repository copy | mitigate | Reuse existing native fixture copy logic that rejects symlinks and parent traversal. |
| T-27-06-03 | R | Topology verification evidence | mitigate | Add native eval expected rows and cache invariants so topology behavior is reproducible and reviewable. |
| T-27-06-04 | I | Documentation boundary | mitigate | Update only existing relationship docs if needed; do not add public topology docs or query-builder names. |
</threat_model>

<verification>
Run `cargo test -p polint --lib eval::topology_rows --locked`, `cargo test -p polint --lib eval::fixtures::module_topology_core --locked`, `cargo test -p polint --test cli module_topology_internals_stay_private --locked`, `cargo test -p polint --test cli external_rule_consumes_module_relationship_facts_through_public_sdk --locked`, `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings`, `cargo test --workspace --all-features --locked`, and `cargo fmt --all -- --check`.
</verification>

<success_criteria>
Phase 27 has eval coverage for all four roadmap success criteria, cache participation proof, and public-boundary compatibility/no-leak proof. No public topology SDK, CLI, runner, crate-root, or broad docs surface is promoted.
</success_criteria>

<output>
After completion, create `.planning/phases/27-layered-module-package-topology-graph/27-06-SUMMARY.md`.
</output>
