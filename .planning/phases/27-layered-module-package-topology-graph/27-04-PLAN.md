---
phase: 27-layered-module-package-topology-graph
plan: 04
type: execute
wave: 4
depends_on: [27-02, 27-03]
files_modified:
  - crates/polint/src/module_graph/mod.rs
  - crates/polint/src/module_graph/model.rs
  - crates/polint/src/module_graph/topology.rs
  - crates/polint/src/analysis_kernel/incremental/keys.rs
  - crates/polint/src/analysis_kernel/incremental/dependency_index.rs
autonomous: true
requirements: [SAE-SEM-02]
requirements_addressed: [SAE-SEM-02]
must_haves:
  truths:
    - "The existing polint.module_graph derivation stores base topology rows with resolved imports, module nodes, and module edges."
    - "Module graph cache identity changes when topology-relevant manifests, lockfiles, workspace files, source-set classifications, or overlay inputs change."
    - "Repo topology overlays produce private facts for generated zones, test-only visibility, internal/public API boundaries, source-of-truth directories, ownership, architecture layers, and deploy units where known or explicitly unknown."
  artifacts:
    - path: "crates/polint/src/module_graph/mod.rs"
      provides: "Base topology wiring into module graph derivation and cache payloads"
      contains: "derive_base_topology"
    - path: "crates/polint/src/module_graph/model.rs"
      provides: "ModuleGraphLayerPayload schema with topology rows"
      contains: "workspace_roots"
    - path: "crates/polint/src/analysis_kernel/incremental/keys.rs"
      provides: "Topology-aware module graph layer key inputs"
      contains: "module_graph_topology_input_digests"
  key_links:
    - from: "crates/polint/src/module_graph/mod.rs"
      to: "AnalysisDb::replace_topology_facts"
      via: "module graph derivation"
      pattern: "replace_topology_facts"
---

<objective>
Wire Go and TS/JS base topology into the existing module graph provider and cache identity.

Purpose: Phase 27 base topology must be produced by normal kernel runs, restored through layer cache payloads, and invalidated by the manifests and lifecycle inputs that define topology.
Output: Base topology derivation, cache payload persistence, and topology-aware layer keys.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-layered-module-package-topology-graph/27-CONTEXT.md
@research/module-graph/RECOMMENDED_IMPLEMENTATION.md
@research/module-graph/VALIDATION.md
@.planning/phases/24-persistent-layer-cache-for-existing-cheap-facts/24-03-SUMMARY.md
@crates/polint/src/module_graph/mod.rs
@crates/polint/src/module_graph/model.rs
@crates/polint/src/module_graph/go.rs
@crates/polint/src/module_graph/ts.rs
@crates/polint/src/analysis_kernel/incremental/keys.rs
@crates/polint/src/analysis_kernel/incremental/dependency_index.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Merge base topology rows into module graph derivation</name>
  <files>crates/polint/src/module_graph/mod.rs, crates/polint/src/module_graph/topology.rs</files>
  <read_first>
    - crates/polint/src/module_graph/mod.rs
    - crates/polint/src/module_graph/go.rs
    - crates/polint/src/module_graph/ts.rs
    - crates/polint/src/module_graph/topology.rs
    - .planning/phases/27-layered-module-package-topology-graph/27-CONTEXT.md
  </read_first>
  <behavior>
    - Test requesting `module_graph` stores non-empty `workspace_roots`, `topology_packages`, `source_sets`, and dependency rows for a mixed Go/TS fixture.
    - Test repeated derivation on the same fixture produces byte-identical sorted topology stable keys.
    - Test declared dependency rows and actual resolved imports remain separate row families.
    - Test `RepoTopologyOverlayKind` includes exact variants `OwnershipZone`, `ArchitectureLayer`, `DeployUnit`, `GeneratedZone`, `TestOnlyVisibility`, `InternalPublicApiBoundary`, and `SourceOfTruthDirectory`.
    - Test base topology derivation emits private overlay rows or explicit unknown rows for every D-21 category: ownership, architecture layers, deploy units, generated zones, test-only visibility, internal/public API boundaries, and source-of-truth directories.
  </behavior>
  <action>Add `derive_base_topology(loaded: &LoadedConfig, db: &AnalysisDb, go_metadata: &go::GoPackageIndex, ts_resolver_context: Option<&ts::TsResolverContext>) -> TopologyOutput` inside `module_graph/mod.rs`. Call `go::collect_go_topology` and `ts::collect_ts_topology`, merge their outputs with `TopologyOutput::merge`, then add repository-level overlay rows from path facts. Define `RepoTopologyOverlayKind` with exact variants `OwnershipZone`, `ArchitectureLayer`, `DeployUnit`, `GeneratedZone`, `TestOnlyVisibility`, `InternalPublicApiBoundary`, and `SourceOfTruthDirectory`. Add `collect_repo_topology_overlays(root: &Path, db: &AnalysisDb, topology: &TopologyOutput) -> Vec<RepoTopologyOverlayFact>` that emits private overlay rows when path/package/source-set evidence is known and explicit `TopologyStatus::Unknown` rows when ownership, architecture layer, deploy unit, internal/public API boundary, or source-of-truth directory evidence is unavailable. The unknown rows must use stable keys such as `repo-topology:ownership-zone:unknown`, `repo-topology:architecture-layer:unknown`, `repo-topology:deploy-unit:unknown`, and `repo-topology:internal-public-api-boundary:unknown` so D-21 categories are represented without claiming exactness. After `db.replace_module_graph_facts(...)`, call `db.replace_topology_facts(base_topology)` so base topology is stored in the same provider run per D-01, D-02, D-08, and D-21. Do not alter `ResolvedImportFact`, `ModuleNode`, `ModuleEdge`, `ResolvedImports<'_>`, or `ModuleGraphFacts<'_>` public behavior per D-14.</action>
  <verify>
    <automated>cargo test -p polint --lib module_graph::base_topology --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "derive_base_topology|collect_go_topology|collect_ts_topology|replace_topology_facts" crates/polint/src/module_graph/mod.rs` returns matches.
    - `rg -n "OwnershipZone|ArchitectureLayer|DeployUnit|GeneratedZone|TestOnlyVisibility|InternalPublicApiBoundary|SourceOfTruthDirectory|collect_repo_topology_overlays" crates/polint/src/module_graph/topology.rs crates/polint/src/module_graph/mod.rs` returns matches.
    - `rg -n "repo-topology:ownership-zone:unknown|repo-topology:architecture-layer:unknown|repo-topology:deploy-unit:unknown|repo-topology:internal-public-api-boundary:unknown|TopologyStatus::Unknown" crates/polint/src/module_graph/topology.rs crates/polint/src/module_graph/mod.rs` returns matches.
    - `cargo test -p polint --lib module_graph::topology_overlay_categories --locked` passes.
    - `rg -n "ResolvedImportFact \\{|pub struct ModuleGraphFacts|pub struct ResolvedImports" crates/polint/src/module_graph/mod.rs crates/polint/src/sdk/facts.rs` shows no new public topology fields.
    - `cargo test -p polint --lib module_graph::base_topology --locked` passes.
  </acceptance_criteria>
  <done>Normal module graph derivation stores deterministic base topology rows while preserving existing public relationship facts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist base topology in module graph layer payloads</name>
  <files>crates/polint/src/module_graph/model.rs, crates/polint/src/module_graph/mod.rs</files>
  <read_first>
    - crates/polint/src/module_graph/model.rs
    - crates/polint/src/module_graph/mod.rs
    - crates/polint/src/analysis_kernel/incremental/layer_cache.rs
    - .planning/phases/24-persistent-layer-cache-for-existing-cheap-facts/24-03-SUMMARY.md
  </read_first>
  <behavior>
    - Test cold and warm module graph cache runs restore identical topology row stable keys.
    - Test module graph cache payload JSON contains `schema = "module-graph-facts-v2"` and topology arrays, but not raw source text or absolute temp paths.
    - Test corrupt topology payload rows produce invalid-evicted reads and recompute.
    - Test duplicate or conflicting stable keys in cached base topology families are rejected by `validate_module_graph_layer_payload` before restore.
  </behavior>
  <action>Change `MODULE_GRAPH_LAYER_SCHEMA` to `module-graph-facts-v2`. Extend `ModuleGraphLayerPayload` with `workspace_roots`, `topology_packages`, `source_sets`, `dependency_requirements`, `resolved_dependency_edges`, and `repo_topology_overlays`. Extend `module_graph_layer_payload`, `restore_module_graph_layer_payload`, `module_graph_layer_payload_parts`, and `validate_module_graph_layer_payload` so cached hits restore base topology through `AnalysisDb::replace_topology_facts`. In `validate_module_graph_layer_payload`, reject duplicate or conflicting `stable_key` values before restore for every base topology family in this payload: `WorkspaceRootFact`, `TopologyPackageFact`, `SourceSetFact`, `DependencyRequirementFact`, `ResolvedDependencyEdgeFact`, and `RepoTopologyOverlayFact`. Add a named regression test `module_graph_layer_cache_rejects_duplicate_topology_stable_keys` that constructs a cached `ModuleGraphLayerPayload` with duplicate/conflicting base topology stable keys, verifies the layer read status is `InvalidEvicted`, and verifies the run recomputes instead of calling `AnalysisDb::replace_topology_facts` with corrupted rows. Keep `import_to_package_edges` out of this payload because Plan 05 computes those after `polint.symbol_graph` per D-12. Ensure payload serialization contains normalized topology rows and metadata inputs only, not raw source text, absolute paths, timestamps, raw ASTs, or package-manager private blobs per D-18 and D-19.</action>
  <verify>
    <automated>cargo test -p polint --lib module_graph_layer_cache_topology --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "MODULE_GRAPH_LAYER_SCHEMA:.*module-graph-facts-v2|workspace_roots|dependency_requirements|repo_topology_overlays" crates/polint/src/module_graph/model.rs crates/polint/src/module_graph/mod.rs` returns matches.
    - `rg -n "validate_module_graph_layer_payload|WorkspaceRootFact|TopologyPackageFact|SourceSetFact|DependencyRequirementFact|ResolvedDependencyEdgeFact|RepoTopologyOverlayFact|duplicate.*stable_key|stable_key_conflict|module_graph_layer_cache_rejects_duplicate_topology_stable_keys" crates/polint/src/module_graph/model.rs crates/polint/src/module_graph/mod.rs` returns matches.
    - `rg -n "raw_source|source_text|absolute_path|SystemTime|timestamp|node_modules/.+contents" crates/polint/src/module_graph/model.rs crates/polint/src/module_graph/mod.rs` returns no new payload identity fields.
    - `cargo test -p polint --lib module_graph_layer_cache_rejects_duplicate_topology_stable_keys --locked` passes.
    - `cargo test -p polint --lib module_graph_layer_cache_topology --locked` passes.
  </acceptance_criteria>
  <done>Module graph layer cache payloads persist and restore normalized base topology rows with schema v2 and fail-closed validation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add topology inputs to module graph cache identity</name>
  <files>crates/polint/src/analysis_kernel/incremental/keys.rs, crates/polint/src/analysis_kernel/incremental/dependency_index.rs, crates/polint/src/module_graph/mod.rs</files>
  <read_first>
    - crates/polint/src/analysis_kernel/incremental/keys.rs
    - crates/polint/src/analysis_kernel/incremental/dependency_index.rs
    - crates/polint/src/module_graph/mod.rs
    - crates/polint/src/analysis_kernel/incremental/input_snapshot.rs
    - research/module-graph/VALIDATION.md
  </read_first>
  <behavior>
    - Test `LayerKey::module_graph_layer_key` changes when `package.json`, `package-lock.json`, `pnpm-workspace.yaml`, `go.mod`, `go.work`, `go.sum`, or `tsconfig.json` content digests change.
    - Test module graph dependency edges include `ShapeKind::ModuleTopology` for manifest, lockfile, workspace, source-set, and overlay inputs.
    - Test cache keys include absent extension placeholders but no extension activation, merge, or quarantine implementation runs in Phase 27.
    - Test unrelated rule digest changes do not change module graph topology layer keys.
  </behavior>
  <action>Add `module_graph_topology_input_digests(root: &Path, db: &AnalysisDb) -> Vec<Digest>` and include its unordered digest inside `module_graph_parameter_digest` or the `LayerKey::module_graph_layer_key` parameter inputs. The function must hash normalized relative path plus content hash for exactly these file names when they exist under topology-relevant roots: `go.mod`, `go.work`, `go.sum`, `package.json`, `package-lock.json`, `pnpm-lock.yaml`, `pnpm-workspace.yaml`, `yarn.lock`, `bun.lock`, `bun.lockb`, and `tsconfig.json`. Add dependency-index edges for these inputs with `DependencyKind::Input` and `ShapeKind::ModuleTopology`. Include absent extension and toolchain placeholders as existing module graph keys do, and persist only absent placeholder digest/metadata handling. Per D-22, do not implement real repo-local extension/provider activation, extension fact merge, extension conflict side tables, extension-aware cache quarantine, or accepted/rejected extension semantics in Phase 27; those remain later-phase work. Preserve rule-digest independence per D-17, D-18, and D-22.</action>
  <verify>
    <automated>cargo test -p polint --lib module_graph_layer_key_topology_inputs --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "module_graph_topology_input_digests|package-lock.json|pnpm-workspace.yaml|go.sum|bun.lockb|ShapeKind::ModuleTopology" crates/polint/src/analysis_kernel/incremental/keys.rs crates/polint/src/module_graph/mod.rs crates/polint/src/analysis_kernel/incremental/dependency_index.rs` returns matches.
    - `rg -n "absent.*extension|extension.*absent|ExtensionPlaceholder|AbsentExtension" crates/polint/src/analysis_kernel/incremental/keys.rs crates/polint/src/module_graph/mod.rs crates/polint/src/analysis_kernel/incremental/dependency_index.rs` returns matches proving absent placeholder digest/metadata handling.
    - `rg -n "activate.*extension|extension.*activation|merge.*extension|extension.*merge|quarantine|accepted.*extension|rejected.*extension|ExtensionConflict" crates/polint/src/module_graph/mod.rs crates/polint/src/analysis_kernel/incremental/keys.rs crates/polint/src/analysis_kernel/incremental/dependency_index.rs` returns no matches.
    - `cargo test -p polint --lib module_graph_layer_key_topology_inputs --locked` passes.
    - `cargo test -p polint --lib module_graph_layer_key_ignores_rule_digest_changes --locked` passes.
  </acceptance_criteria>
  <done>Base topology cache identity and dependency rows change on topology inputs, include only absent extension placeholders, avoid extension activation/merge/quarantine semantics, and stay independent from unrelated rule edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository topology inputs -> layer cache key | Manifest and lockfile contents influence cache reuse and invalidation. |
| layer cache payload -> AnalysisDb | Cached topology rows are restored from disk and must be validated before reuse. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-04-01 | T | Module graph payload restore | mitigate | Validate schema v2 and output digest before `replace_topology_facts`; invalid payloads recompute. |
| T-27-04-02 | I | Cache payload content | mitigate | Store normalized fact rows only; acceptance grep blocks raw source, absolute paths, timestamps, and package-manager blobs. |
| T-27-04-03 | D | Cache invalidation | mitigate | Add manifest/lockfile/workspace/source-set/topology digests and dependency edges so changed topology inputs invalidate module graph cache. |
| T-27-04-04 | E | Extension placeholders | mitigate | Per D-22, include only absent extension placeholder digest/metadata handling and grep-test that Phase 27 does not implement extension activation, merge, conflict side tables, accepted/rejected semantics, or cache quarantine. |
</threat_model>

<verification>
Run `cargo test -p polint --lib module_graph::base_topology --locked`, `cargo test -p polint --lib module_graph_layer_cache_topology --locked`, `cargo test -p polint --lib module_graph_layer_key_topology_inputs --locked`, the D-22 extension guard greps in Task 3 acceptance criteria, and `cargo fmt --all -- --check`.
</verification>

<success_criteria>
The existing module graph provider stores and caches base topology facts, preserves public relationship behavior, invalidates on topology-relevant inputs, and handles only absent extension placeholders without Phase 34 extension activation/merge/quarantine behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/27-layered-module-package-topology-graph/27-04-SUMMARY.md`.
</output>
