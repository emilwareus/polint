---
phase: 27-layered-module-package-topology-graph
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/polint/src/module_graph/mod.rs
  - crates/polint/src/module_graph/topology.rs
  - crates/polint/src/module_graph/formats/mod.rs
  - crates/polint/src/core/mod.rs
  - crates/polint/src/analysis_kernel/metadata.rs
  - crates/polint/src/analysis_kernel/provider.rs
autonomous: true
requirements: [SAE-SEM-02]
requirements_addressed: [SAE-SEM-02]
must_haves:
  truths:
    - "Internal topology rows can represent workspace roots, packages/projects, source sets, requirements, resolved edges, import-to-package edges, and overlays."
    - "Topology row IDs are deterministic run-local IDs assigned from sorted stable keys."
    - "New topology rows remain crate-private and do not add SDK, runner, crate-root, public CLI, or docs surface."
  artifacts:
    - path: "crates/polint/src/module_graph/topology.rs"
      provides: "Crate-private topology fact contracts and deterministic output sorting"
      contains: "WorkspaceRootFact"
    - path: "crates/polint/src/core/mod.rs"
      provides: "AnalysisDb topology storage, replacement APIs, accessors, and metadata refresh"
      contains: "replace_topology_facts"
    - path: "crates/polint/src/analysis_kernel/metadata.rs"
      provides: "FactFamily variants for topology metadata"
      contains: "ImportToPackage"
  key_links:
    - from: "crates/polint/src/core/mod.rs"
      to: "crates/polint/src/module_graph/topology.rs"
      via: "crate-private topology type imports"
      pattern: "use crate::module_graph::topology::"
    - from: "crates/polint/src/analysis_kernel/provider.rs"
      to: "topology provider outputs"
      via: "manifest output labels"
      pattern: "workspace_roots"
---

<objective>
Create the internal topology contract layer for Phase 27.

Purpose: Executors need concrete crate-private fact types, storage APIs, metadata families, and provider output labels before language-specific collectors can emit topology rows.
Output: `module_graph::topology` contracts plus `AnalysisDb` storage and metadata support.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/27-layered-module-package-topology-graph/27-CONTEXT.md
@research/module-graph/FINAL-REPORT.md
@research/module-graph/RECOMMENDED_IMPLEMENTATION.md
@research/module-graph/VALIDATION.md
@AGENTS.md
@docs/API-VISIBILITY-PLAN.md
@crates/polint/src/module_graph/model.rs
@crates/polint/src/core/mod.rs
@crates/polint/src/analysis_kernel/metadata.rs
@crates/polint/src/analysis_kernel/provider.rs

<interfaces>
Existing contracts to preserve:
```rust
pub enum ModuleNodeKind { File, Package, Module, External }
pub enum ModuleEdgeKind { Contains, Imports, DependsOn }
pub enum ResolutionStatus { Resolved, External, Unresolved, SetupMissing, Dynamic, Unsupported }
pub(crate) fn replace_module_graph_facts(
    &mut self,
    resolved_imports: Vec<ResolvedImportFact>,
    module_nodes: Vec<ModuleNode>,
    module_edges: Vec<ModuleEdge>,
)
```

New topology contracts to create as crate-private only:
```rust
pub(crate) struct WorkspaceRootId(pub(crate) u64);
pub(crate) struct TopologyPackageId(pub(crate) u64);
pub(crate) struct SourceSetId(pub(crate) u64);
pub(crate) struct DependencyRequirementId(pub(crate) u64);
pub(crate) struct ResolvedDependencyEdgeId(pub(crate) u64);
pub(crate) struct ImportToPackageId(pub(crate) u64);
pub(crate) struct RepoTopologyOverlayId(pub(crate) u64);

pub(crate) struct TopologyOutput {
    pub(crate) workspace_roots: Vec<WorkspaceRootFact>,
    pub(crate) packages: Vec<TopologyPackageFact>,
    pub(crate) source_sets: Vec<SourceSetFact>,
    pub(crate) dependency_requirements: Vec<DependencyRequirementFact>,
    pub(crate) resolved_dependency_edges: Vec<ResolvedDependencyEdgeFact>,
    pub(crate) import_to_package_edges: Vec<ImportToPackageFact>,
    pub(crate) overlays: Vec<RepoTopologyOverlayFact>,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add crate-private topology fact contracts</name>
  <files>crates/polint/src/module_graph/mod.rs, crates/polint/src/module_graph/topology.rs, crates/polint/src/module_graph/formats/mod.rs</files>
  <read_first>
    - crates/polint/src/module_graph/mod.rs
    - crates/polint/src/module_graph/model.rs
    - crates/polint/src/symbol_graph/semantic.rs
    - .planning/phases/27-layered-module-package-topology-graph/27-CONTEXT.md
    - research/module-graph/RECOMMENDED_IMPLEMENTATION.md
  </read_first>
  <behavior>
    - Test that `TopologyOutput::normalized` sorts every row family by `stable_key` and reassigns run-local IDs starting at 0.
    - Test that `ImportToPackageStatus` contains `Resolved`, `External`, `Unresolved`, `SetupMissing`, `Unsupported`, `Dynamic`, `Ambiguous`, `Undeclared`, and `OutsideWorkspace`.
    - Test that `SourceSetKind` contains `Source`, `Test`, `Generated`, `Vendor`, `External`, and `Unknown`.
  </behavior>
  <action>Create `crates/polint/src/module_graph/topology.rs` with crate-private IDs and row structs for `WorkspaceRootFact`, `TopologyPackageFact`, `SourceSetFact`, `DependencyRequirementFact`, `ResolvedDependencyEdgeFact`, `ImportToPackageFact`, and `RepoTopologyOverlayFact` per D-02, D-07, D-08, D-11, D-12, D-13, and D-21. Include enums named `WorkspaceRootKind`, `TopologyPackageKind`, `SourceSetKind`, `RequirementKind`, `ResolvedDependencyKind`, `ImportToPackageStatus`, `ImportContextKind`, `RepoTopologyOverlayKind`, `TopologyPrecision`, and `TopologyStatus`. Every row must contain `stable_key: String`, `producer_id: &'static str`, `precision: TopologyPrecision`, and `status: TopologyStatus` or the edge-specific status enum. Add `pub(crate) mod topology;` and `pub(crate) mod formats;` to `module_graph/mod.rs`. Create `module_graph/formats/mod.rs` as the namespace for later static manifest parsers. Keep all new symbols `pub(crate)` and add no `pub use` re-exports per D-03.</action>
  <verify>
    <automated>cargo test -p polint --lib module_graph::topology --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "pub\\(crate\\) struct WorkspaceRootFact|pub\\(crate\\) enum ImportToPackageStatus|pub\\(crate\\) struct TopologyOutput" crates/polint/src/module_graph/topology.rs` returns matches.
    - `rg -n "pub mod topology|pub use .*Topology|pub struct WorkspaceRootFact" crates/polint/src/lib.rs crates/polint/src/sdk crates/polint/src/runner.rs` returns no matches.
    - `rg -n "Undeclared|OutsideWorkspace|Generated|Vendor|RepoTopologyOverlayKind" crates/polint/src/module_graph/topology.rs` returns matches.
  </acceptance_criteria>
  <done>Topology contracts exist as crate-private internal rows with deterministic normalization tests and no public re-export.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Store topology rows in AnalysisDb with metadata</name>
  <files>crates/polint/src/core/mod.rs, crates/polint/src/analysis_kernel/metadata.rs, crates/polint/src/module_graph/topology.rs</files>
  <read_first>
    - crates/polint/src/core/mod.rs
    - crates/polint/src/analysis_kernel/metadata.rs
    - crates/polint/src/module_graph/topology.rs
    - .planning/phases/21-provenance-precision-and-validation-metadata/21-01-SUMMARY.md
    - .planning/phases/26-semantic-index-deepening/26-01-SUMMARY.md
  </read_first>
  <behavior>
    - Test that `AnalysisDb::replace_topology_facts` replaces stale topology rows and rebuilds metadata.
    - Test that `AnalysisDb::replace_import_to_package_facts` updates only import-to-package rows and preserves workspace roots, packages, source sets, requirements, resolved edges, and overlays.
    - Test that every stored topology row has a `FactMeta` row with provider `polint.module_graph` or `polint.module_topology`.
  </behavior>
  <action>Import topology row types into `core/mod.rs`. Add `Vec` storage fields for all seven topology families to `AnalysisDb`. Add crate-private accessors named `workspace_roots()`, `topology_packages()`, `source_sets()`, `dependency_requirements()`, `resolved_dependency_edges()`, `import_to_package_edges()`, and `repo_topology_overlays()`. Add `replace_topology_facts(output: TopologyOutput)` that normalizes rows, reassigns IDs, clears all seven families, and refreshes metadata. Add `replace_import_to_package_facts(edges: Vec<ImportToPackageFact>)` that normalizes only import-to-package rows and refreshes metadata for `FactFamily::ImportToPackage`. Add `FactFamily` variants `WorkspaceRoot`, `TopologyPackage`, `SourceSet`, `DependencyRequirement`, `ResolvedDependencyEdge`, `ImportToPackage`, and `RepoTopologyOverlay`. Map `TopologyPrecision::ExactStatic` and `ExactLockfile` to `FactPrecision::SetupAware`, `Heuristic` to `FactPrecision::Heuristic`, `Unknown` to `FactPrecision::Unresolved`, and `Unsupported` to `FactPrecision::Unsupported`. Use producer id `polint.module_graph` for base topology rows and `polint.module_topology` for import-to-package rows per D-01, D-03, D-17, and D-18.</action>
  <verify>
    <automated>cargo test -p polint --lib topology_storage --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "workspace_roots: Vec<WorkspaceRootFact>|replace_topology_facts|replace_import_to_package_facts" crates/polint/src/core/mod.rs` returns matches.
    - `rg -n "WorkspaceRoot|TopologyPackage|SourceSet|DependencyRequirement|ResolvedDependencyEdge|ImportToPackage|RepoTopologyOverlay" crates/polint/src/analysis_kernel/metadata.rs` returns matches.
    - `rg -n "polint.module_topology|MODULE_TOPOLOGY_PROVIDER_ID" crates/polint/src/core/mod.rs` returns matches.
  </acceptance_criteria>
  <done>AnalysisDb can store, replace, access, and metadata-track all internal topology row families deterministically.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Declare internal provider outputs without public promotion</name>
  <files>crates/polint/src/analysis_kernel/provider.rs, crates/polint/src/module_graph/topology.rs, crates/polint/src/core/mod.rs</files>
  <read_first>
    - crates/polint/src/analysis_kernel/provider.rs
    - crates/polint/src/analysis_kernel/mod.rs
    - crates/polint/src/module_graph/topology.rs
    - docs/API-VISIBILITY-PLAN.md
    - AGENTS.md
  </read_first>
  <behavior>
    - Test that provider order remains `polint.source`, `polint.go.syntax`, `polint.ts.syntax`, `polint.module_graph`, `polint.symbol_graph`, `polint.metrics` in this plan.
    - Test that `polint.module_graph` manifest outputs include `workspace_roots`, `topology_packages`, `source_sets`, `dependency_requirements`, `resolved_dependency_edges`, and `repo_topology_overlays`.
    - Test that no manifest output is visible through SDK prelude exports.
  </behavior>
  <action>Update `MODULE_GRAPH_SCHEMA` to include a schema label `module-graph-facts-2` with version `2`, and extend `polint.module_graph` outputs with exactly `workspace_roots`, `topology_packages`, `source_sets`, `dependency_requirements`, `resolved_dependency_edges`, and `repo_topology_overlays` per D-01, D-02, and D-17. Do not add `import_to_package_edges` to `polint.module_graph` yet; Plan 05 introduces the semantic-aware `polint.module_topology` pass after `polint.symbol_graph` because D-12 requires Phase 26 semantic rows. Add no SDK fact view, no runner export, no crate-root export, no CLI command, and no public docs per D-03 and D-24.</action>
  <verify>
    <automated>cargo test -p polint --lib provider_manifests --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "module-graph-facts-2|workspace_roots|topology_packages|resolved_dependency_edges|repo_topology_overlays" crates/polint/src/analysis_kernel/provider.rs` returns matches.
    - `rg -n "Packages<'_|Dependencies<'_|SourceSets<'_|RepoTopology<'_|polint topology|polint facts" crates/polint/src docs README.md` returns no matches.
    - `cargo test -p polint --lib provider_order --locked` passes with the six-provider order unchanged.
  </acceptance_criteria>
  <done>The existing module graph manifest advertises base topology internals for downstream cache/reporting code while public surfaces remain unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internal facts -> public API | New topology rows must not cross into SDK, runner, crate-root exports, public CLI commands, or docs. |
| cache payload -> AnalysisDb metadata | Future cached topology rows will be restored through AnalysisDb and must have metadata families ready. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01-01 | I | SDK/runner/crate-root boundaries | mitigate | Keep all topology contracts `pub(crate)` and verify `Packages<'_>`, `Dependencies<'_>`, `SourceSets<'_>`, `RepoTopology<'_>`, `polint topology`, and `polint facts` are absent. |
| T-27-01-02 | T | AnalysisDb topology replacement | mitigate | Normalize by stable key and reassign run-local IDs inside replacement APIs; add metadata conflict coverage. |
| T-27-01-03 | R | Provider output traceability | mitigate | Add explicit provider output labels and metadata families so later validation can attribute rows to `polint.module_graph` or `polint.module_topology`. |
</threat_model>

<verification>
Run `cargo test -p polint --lib module_graph::topology --locked`, `cargo test -p polint --lib topology_storage --locked`, `cargo test -p polint --lib provider_manifests --locked`, and `cargo fmt --all -- --check`.
</verification>

<success_criteria>
Topology contract rows, AnalysisDb storage, metadata families, and base module graph manifest outputs exist internally. No public SDK, runner, CLI, crate-root, or docs topology surface exists.
</success_criteria>

<output>
After completion, create `.planning/phases/27-layered-module-package-topology-graph/27-01-SUMMARY.md`.
</output>
