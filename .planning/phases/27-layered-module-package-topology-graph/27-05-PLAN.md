---
phase: 27-layered-module-package-topology-graph
plan: 05
type: execute
wave: 5
depends_on: [27-04]
files_modified:
  - crates/polint/src/analysis_kernel/provider.rs
  - crates/polint/src/analysis_kernel/mod.rs
  - crates/polint/src/analysis_kernel/incremental/keys.rs
  - crates/polint/src/module_graph/mod.rs
  - crates/polint/src/module_graph/model.rs
  - crates/polint/src/module_graph/topology.rs
  - crates/polint/src/analysis_kernel/validation.rs
autonomous: true
requirements: [SAE-SEM-02]
requirements_addressed: [SAE-SEM-02]
must_haves:
  truths:
    - "Import-to-package facts bridge syntax imports, resolved imports, Phase 26 semantic import rows, package/project/source-set ownership, and external package nodes."
    - "Import-to-package facts distinguish source, test, generated, vendor, external, unresolved, setup-missing, unsupported, dynamic, ambiguous, undeclared, and outside-workspace states."
    - "Semantic-aware topology facts have their own provider manifest, cache identity, payload restore, and validation after symbol graph execution."
  artifacts:
    - path: "crates/polint/src/module_graph/mod.rs"
      provides: "polint.module_topology derivation and import-to-package classification"
      contains: "derive_module_topology_with_cache_stats"
    - path: "crates/polint/src/analysis_kernel/provider.rs"
      provides: "crate-private polint.module_topology provider manifest"
      contains: "polint.module_topology"
    - path: "crates/polint/src/analysis_kernel/validation.rs"
      provides: "Topology referential integrity and precision validation"
      contains: "validate_topology_facts"
  key_links:
    - from: "crates/polint/src/analysis_kernel/mod.rs"
      to: "crates/polint/src/module_graph/mod.rs"
      via: "post-symbol topology provider call"
      pattern: "derive_module_topology_with_cache_stats"
    - from: "crates/polint/src/module_graph/mod.rs"
      to: "AnalysisDb::semantic_imports"
      via: "semantic-aware import-to-package classification"
      pattern: "semantic_imports\\(\\)"
---

<objective>
Add the semantic-aware import-to-package topology provider, cache, and validation.

Purpose: Base module graph runs before semantic rows exist. A crate-private `polint.module_topology` pass after `polint.symbol_graph` satisfies D-12 without changing existing public relationship facts.
Output: Import-to-package facts, provider manifest, cache identity/payload restore, kernel wiring, and validation.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-layered-module-package-topology-graph/27-CONTEXT.md
@research/module-graph/FINAL-REPORT.md
@research/module-graph/RECOMMENDED_IMPLEMENTATION.md
@research/module-graph/VALIDATION.md
@.planning/phases/26-semantic-index-deepening/26-06-SUMMARY.md
@crates/polint/src/analysis_kernel/mod.rs
@crates/polint/src/analysis_kernel/provider.rs
@crates/polint/src/analysis_kernel/incremental/keys.rs
@crates/polint/src/module_graph/mod.rs
@crates/polint/src/module_graph/topology.rs
@crates/polint/src/symbol_graph/semantic.rs
@crates/polint/src/analysis_kernel/validation.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add semantic-aware import-to-package classification</name>
  <files>crates/polint/src/module_graph/mod.rs, crates/polint/src/module_graph/topology.rs, crates/polint/src/module_graph/model.rs</files>
  <read_first>
    - crates/polint/src/module_graph/mod.rs
    - crates/polint/src/module_graph/topology.rs
    - crates/polint/src/symbol_graph/semantic.rs
    - crates/polint/src/core/mod.rs
    - .planning/phases/27-layered-module-package-topology-graph/27-CONTEXT.md
  </read_first>
  <behavior>
    - Test static TS import to workspace package emits `ImportToPackageStatus::Resolved` and `ImportContextKind::Source`.
    - Test Go `_test.go` import emits `ImportContextKind::Test`; generated/vendor paths emit `Generated` and `Vendor`.
    - Test dynamic import emits `Dynamic`; unresolved relative import emits `Unresolved`; external package without declaration emits `Undeclared`; target outside workspace emits `OutsideWorkspace`; multiple package candidates emit `Ambiguous`.
  </behavior>
  <action>Add `derive_import_to_package_edges(db: &AnalysisDb) -> Vec<ImportToPackageFact>` in `module_graph/mod.rs`. For each `ImportFact`, join by `ResolvedImportFact.import`, matching `SemanticImportFact.file` plus `SemanticImportFact.import_path` where available, source-set ownership, topology package ownership, and external module nodes. Set `ImportToPackageFact.syntax_import`, `resolved_import`, `semantic_import_stable_key`, `from_package_stable_key`, `to_package_stable_key`, `source_set_stable_key`, `import_path`, `context`, `status`, `precision`, and `stable_key`. Map statuses exactly: resolved local package -> `Resolved`; resolved external with declared requirement -> `External`; resolved external without requirement -> `Undeclared`; `ResolutionStatus::Dynamic` or `SemanticStatus::Dynamic` -> `Dynamic`; `SetupMissing` -> `SetupMissing`; `Unsupported` -> `Unsupported`; missing target -> `Unresolved`; target path outside workspace -> `OutsideWorkspace`; more than one package candidate -> `Ambiguous`. This implements D-12 and D-13 while preserving `ResolvedImportFact` and `ModuleNode` behavior per D-14.</action>
  <verify>
    <automated>cargo test -p polint --lib module_graph::import_to_package --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "derive_import_to_package_edges|semantic_imports\\(\\)|ImportToPackageStatus::Undeclared|ImportToPackageStatus::OutsideWorkspace|ImportContextKind::Generated|ImportContextKind::Vendor" crates/polint/src/module_graph/mod.rs` returns matches.
    - `rg -n "semantic_import_stable_key|from_package_stable_key|to_package_stable_key|source_set_stable_key" crates/polint/src/module_graph/topology.rs` returns matches.
    - `cargo test -p polint --lib module_graph::import_to_package --locked` passes.
  </acceptance_criteria>
  <done>Import-to-package rows connect imports, semantic rows, source sets, package ownership, and external nodes with explicit classification statuses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add polint.module_topology provider, kernel order, and cache</name>
  <files>crates/polint/src/analysis_kernel/provider.rs, crates/polint/src/analysis_kernel/mod.rs, crates/polint/src/analysis_kernel/incremental/keys.rs, crates/polint/src/module_graph/mod.rs, crates/polint/src/module_graph/model.rs</files>
  <read_first>
    - crates/polint/src/analysis_kernel/provider.rs
    - crates/polint/src/analysis_kernel/mod.rs
    - crates/polint/src/analysis_kernel/incremental/keys.rs
    - crates/polint/src/module_graph/mod.rs
    - crates/polint/src/module_graph/model.rs
    - .planning/phases/24-persistent-layer-cache-for-existing-cheap-facts/24-05-SUMMARY.md
  </read_first>
  <behavior>
    - Test provider order is `polint.source`, `polint.go.syntax`, `polint.ts.syntax`, `polint.module_graph`, `polint.symbol_graph`, `polint.module_topology`, `polint.metrics`.
    - Test `polint.module_topology` cache key changes when module graph output digest, symbol graph output digest, semantic provider parameter digest, topology package/source-set digest, or import shape digest changes.
    - Test warm cache restore calls `replace_import_to_package_facts` and preserves stable keys.
  </behavior>
  <action>Add provider manifest `polint.module_topology` with `ProviderKind::WholeRepoDerived`, inputs `source_files`, `imports`, `resolved_imports`, `module_nodes`, `module_edges`, `workspace_roots`, `topology_packages`, `source_sets`, `dependency_requirements`, `resolved_dependency_edges`, `semantic_imports`, outputs `import_to_package_edges`, language scope `MultiLanguage`, cache policy `InMemoryDerived`, schema `module-topology-facts-1:1`, and precision ceiling `SetupAware`. Wire `AnalysisKernel::run` to call `derive_module_topology_with_cache_stats` after `derive_requested_symbols_with_cache_stats` and before metrics. Add `LayerKey::module_topology_layer_key` and dependency edges over import shapes, base topology stable keys, module graph output digest, symbol graph output digest, config digest, lifecycle digests, absent toolchain/extension placeholders, and semantic provider parameter digest per D-01, D-12, D-17, and D-18. Add a `ModuleTopologyLayerPayload` containing schema `module-topology-facts-v1`, diagnostics, capability_support as an empty vec, and `import_to_package_edges`, with read/write validation through `LayerCacheStore`.</action>
  <verify>
    <automated>cargo test -p polint --lib module_topology_layer_cache --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "polint.module_topology|module-topology-facts-1|module-topology-facts-v1|derive_module_topology_with_cache_stats|module_topology_layer_key" crates/polint/src/analysis_kernel/provider.rs crates/polint/src/analysis_kernel/mod.rs crates/polint/src/analysis_kernel/incremental/keys.rs crates/polint/src/module_graph/mod.rs crates/polint/src/module_graph/model.rs` returns matches.
    - `cargo test -p polint --lib provider_order --locked` passes with `polint.module_topology` between symbol graph and metrics.
    - `cargo test -p polint --lib module_topology_layer_cache --locked` passes.
  </acceptance_criteria>
  <done>The semantic-aware module topology provider runs after symbol graph, reports internal cache stats, and persists normalized import-to-package rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Validate topology referential integrity and precision ceilings</name>
  <files>crates/polint/src/analysis_kernel/validation.rs, crates/polint/src/module_graph/topology.rs</files>
  <read_first>
    - crates/polint/src/analysis_kernel/validation.rs
    - crates/polint/src/module_graph/topology.rs
    - crates/polint/src/analysis_kernel/metadata.rs
    - research/module-graph/VALIDATION.md
  </read_first>
  <behavior>
    - Test invalid source-set file refs, package refs, import refs, resolved-import refs, and semantic stable-key refs emit `polint/internal` validation diagnostics.
    - Test malformed absolute or escaping paths in topology rows are rejected by validation.
    - Test `TopologyPrecision::ExactLockfile` is accepted only for parsed lockfile/checksum rows and rejected for unsupported dynamic package-manager rows.
  </behavior>
  <action>Add `validate_topology_facts(db: &AnalysisDb, ids: &IdSets, diagnostics: &mut Vec<Diagnostic>)` and call it from `validate_fact_metadata`. Validate that every optional `FileId`, `PackageId`, `ImportId`, `ResolvedImportId`, and `ModuleNodeId` in topology rows exists; every `semantic_import_stable_key` exists in `db.semantic_imports()` when present; every path-like field is repo-relative and does not contain `..`, an absolute prefix, or backslash-only identity; every import-to-package row with status `Resolved` has `to_package_stable_key`; `Undeclared` rows have no matching `DependencyRequirementFact`; and unsupported/dynamic rows never carry `TopologyPrecision::ExactLockfile` or `ExactStatic`. Emit internal diagnostics with evidence keys `family`, `stable_key`, `field`, and `reason` per D-11, D-19, and D-23.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis_kernel::validation::topology --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "validate_topology_facts|ImportToPackageFact|TopologyPrecision::ExactLockfile|semantic_import_stable_key|Undeclared" crates/polint/src/analysis_kernel/validation.rs` returns matches.
    - `cargo test -p polint --lib analysis_kernel::validation::topology --locked` passes.
    - `cargo test -p polint --lib analysis_kernel::validation --locked` passes.
  </acceptance_criteria>
  <done>Topology rows fail closed on referential-integrity, path, precision-ceiling, and unsupported-exactness violations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| semantic rows -> topology classification | Phase 26 semantic rows are internal facts but can be unresolved, dynamic, ambiguous, or setup-missing. |
| topology cache payload -> AnalysisDb | Import-to-package rows are restored from disk after symbol graph execution. |
| topology facts -> public output | New provider outputs must remain out of public JSON, SDK, runner, and docs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-05-01 | T | Import-to-package classification | mitigate | Preserve explicit statuses for dynamic, ambiguous, unresolved, setup-missing, unsupported, undeclared, and outside-workspace states. |
| T-27-05-02 | I | Module topology provider output | mitigate | Keep `polint.module_topology` crate-private and add public no-leak proof in Plan 06. |
| T-27-05-03 | T | Cached import-to-package rows | mitigate | Validate schema, output digest, repo-relative paths, refs, and precision ceilings before restore. |
| T-27-05-04 | D | Provider cycle risk | mitigate | Run `polint.module_topology` only after `polint.symbol_graph`, and do not feed topology output back into symbol graph in the same run. |
</threat_model>

<verification>
Run `cargo test -p polint --lib module_graph::import_to_package --locked`, `cargo test -p polint --lib module_topology_layer_cache --locked`, `cargo test -p polint --lib analysis_kernel::validation::topology --locked`, `cargo test -p polint --lib provider_order --locked`, and `cargo fmt --all -- --check`.
</verification>

<success_criteria>
Import-to-package topology rows are semantic-aware, status-rich, cache-backed, validated, and internally scheduled after symbol graph without public topology promotion.
</success_criteria>

<output>
After completion, create `.planning/phases/27-layered-module-package-topology-graph/27-05-SUMMARY.md`.
</output>
