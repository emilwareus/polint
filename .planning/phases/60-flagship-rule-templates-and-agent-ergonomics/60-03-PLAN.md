# Phase 60-03 Plan: Docs, Skill Text, and Closeout

## Goal

Document the template workflow as repo-local scaffolding and close Phase 60 with
focused verification.

## Tasks

1. Update README usage to show `--template` and the flagship template IDs.
2. Update generated agent skill text and checked-in skills so agents prefer
   policy templates when creating v1.4 rule examples.
3. Update relevant `docs/facts/` pages with template examples that use the
   normalized Phase 59 evidence semantics.
4. Mark TPL-01 through TPL-05 complete in requirements and roadmap/state.
5. Add Phase 60 summaries and verification notes.
6. Run focused CLI tests, formatting, clippy, docs, and public-surface checks
   before committing locally.

## Verification

- `cargo test -p polint --test cli new_rule_policy_template --locked`
- `cargo test -p polint --test cli phase5 --locked`
- `cargo test -p polint --test public_surface_leak --locked`
- `cargo fmt --all --check`
- `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings`
- `cargo doc -p polint --no-deps --locked`

