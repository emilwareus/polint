---
phase: 36-p0-type-value-place-alias-substrate
plan: 02
title: Type/Value/Alias Provider, AnalysisDb Storage, Cache Identity, And Metadata
type: execute
wave: 2
depends_on: [36-01]
files_modified:
  - crates/polint/src/analysis/types/provider.rs
  - crates/polint/src/analysis/types/cache_key.rs
  - crates/polint/src/analysis/types/debug.rs
  - crates/polint/src/analysis/provider.rs
  - crates/polint/src/analysis_kernel/provider.rs
  - crates/polint/src/analysis_kernel/mod.rs
  - crates/polint/src/analysis_kernel/metadata.rs
  - crates/polint/src/analysis_kernel/incremental/keys.rs
  - crates/polint/src/core/mod.rs
autonomous: true
requirements: [SAE-PREC-01]
must_haves:
  truths:
    - "D-21/D-22/D-23 are covered: a deterministic provider manifest and kernel run step exist for type/value/alias output."
    - "D-24 is covered: provider output digest includes schema, config, lifecycle, upstream output digests, extension/model/tool sentinels, budget, and precision settings."
    - "D-27 is started: metadata is attached to all Phase 36 fact families and validation can reason about provider/schema identity."
  artifacts:
    - path: "crates/polint/src/analysis/types/provider.rs"
      provides: "derive_type_value_alias_with_cache_stats or equivalent provider entrypoint"
    - path: "crates/polint/src/analysis/types/cache_key.rs"
      provides: "schema label and provider parameter digest for Phase 36 output"
    - path: "crates/polint/src/analysis_kernel/provider.rs"
      provides: "polint.type_value_alias manifest with inputs, outputs, schema, precision ceiling"
---

<objective>
Wire the Phase 36 fact contracts into the kernel as a crate-private native provider with `AnalysisDb` storage, provider manifest metadata, output digesting, cache identity vocabulary, and test-facing debug output.

Purpose: Make the new substrate real engine data rather than detached structs.
Output: Kernel can run a no-op or seed provider that stores normalized empty/seeded Phase 36 output with deterministic metadata and output digest.
</objective>

<execution_context>
@.planning/phases/36-p0-type-value-place-alias-substrate/36-CONTEXT.md
@.planning/phases/36-p0-type-value-place-alias-substrate/36-PATTERNS.md
@.planning/phases/36-p0-type-value-place-alias-substrate/36-01-PLAN.md
@.agents/skills/rust-best-practices/SKILL.md
@AGENTS.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Add AnalysisDb storage and provider output replacement</name>
  <files>crates/polint/src/core/mod.rs, crates/polint/src/analysis/provider.rs, crates/polint/src/analysis/types/provider.rs</files>
  <read_first>
    - crates/polint/src/core/mod.rs
    - crates/polint/src/analysis/provider.rs
    - crates/polint/src/analysis/domains/provider.rs
    - crates/polint/src/analysis/summaries/provider.rs
    - crates/polint/src/analysis/entrypoints/provider.rs
  </read_first>
  <action>
    Add `AnalysisDb` fields and accessors for Phase 36 outputs: type facts, narrowed type facts, value facts, allocation tokens, access paths, points-to constraints/sets, and alias answers. Add a single replacement method such as `replace_type_value_alias_facts(output)` or separate replacement methods if that matches existing style. Create `analysis/types/provider.rs` with a provider entrypoint that consumes `AnalysisDb`, builds normalized seed output from existing places/MIR/domain/call/entrypoint data if available, records cache stats, computes output digest, stores output, and returns diagnostics/cache stats/output digest.
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis::types::provider --locked</automated>
    <automated>cargo check -p polint --locked</automated>
  </verify>
  <acceptance_criteria>
    - `AnalysisDb` exposes read accessors for each Phase 36 output family.
    - `AnalysisDb` has replacement logic that normalizes output before storage.
    - Provider unit test proves empty input produces deterministic empty output digest.
    - `cargo check -p polint --locked` passes.
  </acceptance_criteria>
  <done>AnalysisDb can store and expose private Phase 36 output rows.</done>
</task>

<task type="auto">
  <name>Task 2: Register provider manifest and kernel run step</name>
  <files>crates/polint/src/analysis_kernel/provider.rs, crates/polint/src/analysis_kernel/mod.rs</files>
  <read_first>
    - crates/polint/src/analysis_kernel/provider.rs
    - crates/polint/src/analysis_kernel/mod.rs
    - crates/polint/src/analysis/entrypoints/provider.rs
    - crates/polint/src/analysis/extensions/provider.rs
  </read_first>
  <action>
    Add schema constant `TYPE_VALUE_ALIAS_SCHEMA` with label `type-value-alias-facts-1`. Add provider manifest `polint.type_value_alias` with `ProviderKind::WholeRepoDerived`, `LanguageScope::MultiLanguage`, `CachePolicy::InMemoryDerived`, `PrecisionCeiling::SetupAware`, inputs listed in D-22, and outputs listed in D-23. Insert provider execution after `polint.entrypoints` and before `polint.extensions` unless implementation discovers an extension-aware split is required; if split, document exact order in code comments and tests. Update provider order tests to include the new provider.
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis_kernel::provider --locked</automated>
    <automated>cargo test -p polint --lib analysis_kernel::tests::provider_order --locked</automated>
  </verify>
  <acceptance_criteria>
    - `provider_order_for_test()` includes `polint.type_value_alias` after `polint.entrypoints`.
    - Manifest outputs include `type_facts`, `narrowed_type_facts`, `value_facts`, `allocation_tokens`, `access_paths`, `points_to_constraints`, `points_to_sets`, and `alias_answers` or equivalent labels.
    - Kernel run invokes the provider and carries its output digest into `KernelRunReport`.
  </acceptance_criteria>
  <done>Kernel provider order includes a deterministic Phase 36 provider.</done>
</task>

<task type="auto">
  <name>Task 3: Add cache key, metadata, and debug scaffolding</name>
  <files>crates/polint/src/analysis/types/cache_key.rs, crates/polint/src/analysis/types/debug.rs, crates/polint/src/analysis_kernel/metadata.rs, crates/polint/src/analysis_kernel/incremental/keys.rs</files>
  <read_first>
    - crates/polint/src/analysis/domains/cache_key.rs
    - crates/polint/src/analysis/entrypoints/cache_key.rs
    - crates/polint/src/analysis_kernel/incremental/keys.rs
    - crates/polint/src/analysis_kernel/metadata.rs
  </read_first>
  <action>
    Add provider parameter digest code that includes budget/precision-tier settings and absent model/extension/tool slots. Extend incremental key vocabulary with a layer/query kind for type/value/alias output if the existing generic provider-output digest path needs one. Attach metadata to every Phase 36 row using the new FactFamily variants and `polint.type_value_alias` as producer/layer id. Add test-facing debug rows with counts by fact family, language, status, precision, alias status, and budget status. Debug output must omit raw source bodies, absolute paths, parser IDs, and timestamps.
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis::types::cache_key analysis::types::debug --locked</automated>
    <automated>cargo test -p polint --lib analysis_kernel::metadata --locked</automated>
  </verify>
  <acceptance_criteria>
    - Cache key tests prove config, lifecycle, upstream digest, extension sentinel, model sentinel, tool sentinel, and budget setting changes alter the provider parameter/output digest.
    - Metadata tests prove Phase 36 FactFamily rows use `polint.type_value_alias` producer/layer id.
    - Debug snapshot test proves deterministic sorted rows and absence of absolute paths.
  </acceptance_criteria>
  <done>Provider output participates in metadata, debug, and deterministic cache identity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream facts -> Phase 36 provider | Provider consumes many internal facts and must not trust dangling IDs or nondeterministic order. |
| cache -> analysis | Cached identity must include every behavior-affecting input before reuse is allowed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-03 | Tampering | provider cache identity | mitigate | Include provider/schema/config/lifecycle/upstream/model/extension/tool/budget inputs in digest tests. |
| T-36-04 | Information Disclosure | debug output | mitigate | Debug tests reject absolute paths, raw source bodies, parser IDs, and timestamps. |
| T-36-SC | Tampering | dependency installation | accept | No new third-party dependencies are required in this plan. |
</threat_model>

<verification>
Run:
- `cargo test -p polint --lib analysis::types::provider analysis::types::cache_key analysis::types::debug --locked`
- `cargo test -p polint --lib analysis_kernel::provider analysis_kernel::metadata --locked`
- `cargo check -p polint --locked`
</verification>

<success_criteria>
The Phase 36 provider is registered, deterministic, metadata-backed, cache-key-aware, and still private.
</success_criteria>

<output>
Create `.planning/phases/36-p0-type-value-place-alias-substrate/36-02-SUMMARY.md` when done.
</output>
