---
phase: 36-p0-type-value-place-alias-substrate
plan: 07
title: Validation, Debug, Eval Fixtures, Public No-Leak Proof, And Roadmap Closeout
type: execute
wave: 6
depends_on: [36-06]
files_modified:
  - crates/polint/src/analysis/types/validate.rs
  - crates/polint/src/analysis/types/debug.rs
  - crates/polint/src/eval/model.rs
  - crates/polint/src/eval/observe.rs
  - crates/polint/src/analysis_kernel/mod.rs
  - tests/eval-fixtures/type-value-alias/go-core/expected.polint-eval.toml
  - tests/eval-fixtures/type-value-alias/ts-js-core/expected.polint-eval.toml
  - tests/eval-fixtures/type-value-alias/extension-precision/expected.polint-eval.toml
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements: [SAE-PREC-01]
must_haves:
  truths:
    - "D-27/D-28/D-29/D-30 are covered: validation, debug snapshots, eval fixtures, and public no-leak proof exist."
    - "Roadmap success criteria are covered: receiver narrowing, function values, object/property allocations, field-sensitivity limits, unresolved aliases, official-tool digests where used, alias statuses, normalized official outputs, and optional whole-repo points-to."
    - "SAE-PREC-01 is marked complete only after tests and no-leak proof pass."
---

<objective>
Close Phase 36 with validation, deterministic debug/eval fixtures, public no-leak proof, and planning/state/requirement closeout.

Purpose: Prove the private type/value/place/alias substrate is correct enough for Phases 37-39 to consume without promoting unsupported public APIs.
Output: Validation diagnostics, debug snapshots, eval fixtures, no-leak tests, full regression commands, and GSD closeout updates.
</objective>

<execution_context>
@.planning/phases/36-p0-type-value-place-alias-substrate/36-CONTEXT.md
@.planning/phases/36-p0-type-value-place-alias-substrate/36-PATTERNS.md
@docs/API-VISIBILITY-PLAN.md
@.agents/skills/rust-best-practices/SKILL.md
@AGENTS.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Add validation diagnostics for Phase 36 facts</name>
  <files>crates/polint/src/analysis/types/validate.rs, crates/polint/src/analysis_kernel/validation.rs</files>
  <read_first>
    - crates/polint/src/analysis_kernel/validation.rs
    - crates/polint/src/analysis/entrypoints/validate.rs
    - crates/polint/src/analysis/calls/validate.rs
    - crates/polint/src/analysis/types/facts.rs
    - crates/polint/src/analysis/aliases/facts.rs
  </read_first>
  <action>
    Implement validation checks from D-27: dangling references to file/function/MIR body/MIR op/place/call/entrypoint/CFG IDs, invalid spans, duplicate stable keys, missing provenance, malformed access paths, precision ceiling violations, type/value/status incompatibilities, unsupported official-tool setup, points-to budget misuse, and alias answers claiming MustAlias/NoAlias without sufficient evidence. Emit deterministic `polint/internal`, `polint/capability`, `polint/model`, or `polint/extension` diagnostics as appropriate.
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis::types::validate analysis_kernel::validation --locked</automated>
  </verify>
  <acceptance_criteria>
    - Validation tests cover dangling references, duplicate stable keys, malformed access paths, precision ceiling violations, setup-missing official-tool rows, and impossible MustAlias claims.
    - Diagnostics are deterministic and include stable evidence identifiers.
    - Invalid facts do not enter normal stores as accepted facts.
  </acceptance_criteria>
  <done>Phase 36 validation catches malformed and overconfident facts deterministically.</done>
</task>

<task type="auto">
  <name>Task 2: Add debug snapshots and eval observation</name>
  <files>crates/polint/src/analysis/types/debug.rs, crates/polint/src/eval/model.rs, crates/polint/src/eval/observe.rs, tests/eval-fixtures/type-value-alias/go-core/expected.polint-eval.toml, tests/eval-fixtures/type-value-alias/ts-js-core/expected.polint-eval.toml, tests/eval-fixtures/type-value-alias/extension-precision/expected.polint-eval.toml</files>
  <read_first>
    - crates/polint/src/analysis/types/debug.rs
    - crates/polint/src/eval/model.rs
    - crates/polint/src/eval/observe.rs
    - tests/eval-fixtures/abstract-domains/core/expected.polint-eval.toml
    - tests/eval-fixtures/framework-entrypoints/mixed-go-ts/expected.polint-eval.toml
  </read_first>
  <action>
    Add deterministic debug snapshots with counts by language, fact family, type/value/alias status, precision, unsupported reason, solver budget status, and native/extension provenance. Extend eval observation with a `type_value_alias` area or equivalent rows for type facts, narrowed types, value facts, allocation tokens, access paths, points-to facts, alias statuses, and validation events. Add eval fixtures for Go core, TS/JS core, and extension precision. Fixtures must cover receiver narrowing, function values, object/property allocations, field-sensitivity limits, unresolved aliases, all five alias statuses, official-tool digest behavior where used, dynamic unknowns, and cold/warm/no-cache determinism.
  </action>
  <verify>
    <automated>cargo test -p polint --test eval_fixtures --locked -- type_value_alias</automated>
    <automated>cargo test -p polint --lib analysis::types::debug --locked</automated>
  </verify>
  <acceptance_criteria>
    - Eval fixtures include Go and TS/JS source files under `tests/eval-fixtures/type-value-alias/`.
    - Expected eval output asserts all five alias statuses.
    - Debug snapshot test proves deterministic order and absence of raw source bodies, absolute paths, parser IDs, and timestamps.
    - Cold/warm/no-cache fixture comparison remains deterministic.
  </acceptance_criteria>
  <done>Debug and eval prove the private substrate across native and extension cases.</done>
</task>

<task type="auto">
  <name>Task 3: Add public no-leak proof and close SAE-PREC-01</name>
  <files>crates/polint/src/analysis_kernel/mod.rs, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - crates/polint/src/analysis_kernel/mod.rs
    - docs/API-VISIBILITY-PLAN.md
    - crates/polint/src/sdk/prelude.rs
    - crates/polint/src/sdk/mod.rs
    - crates/polint/src/runner.rs
    - crates/polint/src/lib.rs
    - README.md
    - docs/facts/
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
  </read_first>
  <action>
    Add or extend public no-leak tests to assert that normal `polint check --format json`, CLI help, SDK prelude/module, runner, crate root, README, docs/facts, and API visibility docs do not expose private Phase 36 provider IDs, fact types, modules, solver/debug terms, or preview SDK view names. Mark `SAE-PREC-01` complete in `.planning/REQUIREMENTS.md` only after validation/eval/no-leak tests pass, and update `.planning/STATE.md` phase progress summary according to GSD state handler expectations if the executor workflow has not already done so.
  </action>
  <verify>
    <automated>cargo test -p polint --lib -- no_leak --locked</automated>
    <automated>cargo test -p polint --test eval_fixtures --locked -- type_value_alias</automated>
    <automated>cargo test -p polint --locked</automated>
    <automated>cargo clippy -p polint -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - No-leak test covers provider id `polint.type_value_alias`, fact names `TypeFact`, `NarrowedTypeFact`, `ValueFact`, `AllocationTokenFact`, `AccessPathFact`, `PointsToConstraintFact`, `AliasAnswerFact`, and view names `Types<'_>`, `Values<'_>`, `Aliases<'_>`.
    - Normal public CLI JSON/help and SDK/runner/readme/docs do not mention private Phase 36 internals.
    - `SAE-PREC-01` is marked complete only after full validation commands pass.
    - `cargo test -p polint --locked` and `cargo clippy -p polint -- -D warnings` pass.
  </acceptance_criteria>
  <done>Phase 36 is validated, no-leak proven, and ready for refined call graph consumers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internal -> public | Private Phase 36 internals must not become supported SDK/CLI contracts. |
| eval/debug -> public output | Internal evidence output must stay test-facing and deterministic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-14 | Information Disclosure | public API leak | mitigate | No-leak tests scan public surfaces for provider IDs, fact names, solver/debug terms, and preview view names. |
| T-36-15 | Repudiation | precision claims | mitigate | Eval fixtures and validation diagnostics record status, precision, evidence, and unknown reasons. |
| T-36-SC | Tampering | dependency installation | accept | No new dependencies are required; full clippy/test regression catches drift. |
</threat_model>

<verification>
Run:
- `cargo test -p polint --lib -- no_leak --locked`
- `cargo test -p polint --test eval_fixtures --locked -- type_value_alias`
- `cargo test -p polint --locked`
- `cargo clippy -p polint -- -D warnings`
</verification>

<success_criteria>
Phase 36 success criteria are proven by eval/no-leak/regression tests and `SAE-PREC-01` can be closed honestly.
</success_criteria>

<output>
Create `.planning/phases/36-p0-type-value-place-alias-substrate/36-07-SUMMARY.md` when done.
</output>
