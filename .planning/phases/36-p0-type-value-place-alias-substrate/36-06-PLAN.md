---
phase: 36-p0-type-value-place-alias-substrate
plan: 06
title: Extension Type/Value/Alias Facts, Merge Rules, And Quarantine
type: execute
wave: 5
depends_on: [36-05]
files_modified:
  - crates/polint/src/analysis/extensions/sinks.rs
  - crates/polint/src/analysis/extensions/provider.rs
  - crates/polint/src/analysis/types/provider.rs
  - crates/polint/src/analysis/types/validate.rs
  - crates/polint/src/analysis_kernel/incremental/quarantine.rs
  - tests/eval-fixtures/type-value-alias/extension-precision/expected.polint-eval.toml
autonomous: true
requirements: [SAE-PREC-01]
must_haves:
  truths:
    - "D-25/D-26 are covered: extension-provided type/value/allocation/points-to/alias facts merge only after validation and cannot delete native facts."
    - "Extension-provided exactness is precision-ceiling gated and quarantine-eligible."
    - "Default-vs-extended eval can show unknown reduction without hiding native uncertainty."
---

<objective>
Allow validated Phase 34 extension facts to add type/value/allocation/points-to/alias precision through the typed sink boundary, with merge rules, precision ceilings, cache participation, and quarantine.

Purpose: Preserve the product model where repo-local Rust extensions can improve precision safely, while native facts remain authoritative.
Output: Extension candidates for Phase 36 fact families validate, merge additively, affect cache/quarantine identity, and show default-vs-extended precision improvement in fixtures.
</objective>

<execution_context>
@.planning/phases/36-p0-type-value-place-alias-substrate/36-CONTEXT.md
@.planning/phases/36-p0-type-value-place-alias-substrate/36-PATTERNS.md
@.planning/phases/34-rust-extension-provider-sink/34-CONTEXT.md
@research/agent-extension-surface/RECOMMENDED_IMPLEMENTATION.md
@.agents/skills/rust-best-practices/SKILL.md
@AGENTS.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Extend typed extension sink vocabulary for Phase 36 facts</name>
  <files>crates/polint/src/analysis/extensions/sinks.rs, crates/polint/src/analysis/extensions/provider.rs</files>
  <read_first>
    - crates/polint/src/analysis/extensions/sinks.rs
    - crates/polint/src/analysis/extensions/provider.rs
    - crates/polint/src/analysis/types/facts.rs
    - crates/polint/src/analysis/aliases/facts.rs
  </read_first>
  <action>
    Extend extension candidate handling so accepted `fact_family` values can represent Phase 36 type, narrowed type, value, allocation token, access path, points-to constraint, and alias assertion candidates. Use structured payload labels only where the existing generic sink remains the right boundary; add specialized conversion helpers if necessary to avoid ad hoc string parsing in provider code. Preserve normalization of binding refs, evidence, and payload labels. Add tests for accepted, candidate, and rejected Phase 36 extension candidates.
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis::extensions::sinks analysis::extensions::provider --locked</automated>
  </verify>
  <acceptance_criteria>
    - Extension sink tests include Phase 36 fact-family labels.
    - Malformed Phase 36 extension candidate is rejected before merge.
    - Accepted candidate carries extension id, provider id, precision, confidence, status, evidence, and stable key.
  </acceptance_criteria>
  <done>Extension sink can carry Phase 36 precision candidates safely.</done>
</task>

<task type="auto">
  <name>Task 2: Merge validated extension facts additively</name>
  <files>crates/polint/src/analysis/types/provider.rs, crates/polint/src/analysis/types/validate.rs, crates/polint/src/analysis/extensions/provider.rs</files>
  <read_first>
    - crates/polint/src/analysis/types/provider.rs
    - crates/polint/src/analysis/extensions/provider.rs
    - crates/polint/src/analysis_kernel/validation.rs
    - crates/polint/src/analysis_kernel/metadata.rs
  </read_first>
  <action>
    Add validation and merge logic so extension Phase 36 facts merge after native facts by stable key. Native facts remain authoritative. Extension facts may add new facts or add lower/equal precision evidence to unresolved native facts; they cannot delete native facts or silently override conflicting native payloads. Conflicts produce deterministic `polint/model` or `polint/extension` diagnostics and rejected extension rows. Exact extension alias/type facts require validation evidence; otherwise downgrade or reject according to existing precision ceiling policy.
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis::types::validate analysis::extensions::provider --locked</automated>
  </verify>
  <acceptance_criteria>
    - Test proves extension cannot delete native type/value/alias facts.
    - Test proves conflicting same-stable-key extension fact is rejected or diagnosed.
    - Test proves valid extension alias assertion can reduce Unknown to NoAlias or MustAlias only with required evidence.
    - Precision ceiling test rejects or downgrades generated-unvalidated Exact claims.
  </acceptance_criteria>
  <done>Extension Phase 36 facts merge additively with precision gates and conflict diagnostics.</done>
</task>

<task type="auto">
  <name>Task 3: Wire extension digests, quarantine, and default-vs-extended eval</name>
  <files>crates/polint/src/analysis/types/cache_key.rs, crates/polint/src/analysis_kernel/incremental/quarantine.rs, tests/eval-fixtures/type-value-alias/extension-precision/expected.polint-eval.toml</files>
  <read_first>
    - crates/polint/src/analysis/types/cache_key.rs
    - crates/polint/src/analysis_kernel/incremental/quarantine.rs
    - tests/eval-fixtures/extension/rejection-delta/expected.polint-eval.toml
    - tests/eval-fixtures/framework-entrypoints/mixed-go-ts/expected.polint-eval.toml
  </read_first>
  <action>
    Ensure extension source/manifest/protocol/schema/dependency/config digests participate in Phase 36 provider digest when extension facts influence type/value/alias output. Extension-influenced Phase 36 cache/query/summary/diagnostic nodes must be quarantine-eligible; native-only nodes remain reusable. Add an eval fixture where native analysis produces Unknown alias/type precision and an extension adds validated evidence that improves the status. Eval must report accepted/rejected extension facts and default-vs-extended delta without hiding native unknown rows.
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis::types::cache_key analysis_kernel::incremental::quarantine --locked</automated>
    <automated>cargo test -p polint --test eval_fixtures --locked -- type_value_alias</automated>
  </verify>
  <acceptance_criteria>
    - Extension digest change alters Phase 36 provider output identity when extension facts are used.
    - Quarantine test proves extension-influenced Phase 36 cache node is quarantined and native-only node is not.
    - Eval fixture records default-vs-extended unknown reduction or explicit precision improvement.
    - Rejected extension facts are visible in internal eval/debug output but not merged into stores.
  </acceptance_criteria>
  <done>Extension-provided Phase 36 precision is cache-safe, quarantine-aware, and eval-visible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| extension process -> native analysis | Repo-local extension output is untrusted until validated. |
| extension-influenced cache | Cache entries depending on extension facts must be quarantined on extension changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-11 | Tampering | extension merge | mitigate | Additive merge only; native facts authoritative; conflicts rejected or diagnosed. |
| T-36-12 | Elevation of Privilege | extension exactness claims | mitigate | Precision ceiling validation rejects/downgrades unsupported Exact claims. |
| T-36-13 | Repudiation | extension evidence | mitigate | Accepted and rejected extension facts carry stable evidence in debug/eval output. |
</threat_model>

<verification>
Run:
- `cargo test -p polint --lib analysis::extensions analysis::types::validate --locked`
- `cargo test -p polint --lib analysis::types::cache_key analysis_kernel::incremental::quarantine --locked`
- `cargo test -p polint --test eval_fixtures --locked -- type_value_alias`
- `cargo check -p polint --locked`
</verification>

<success_criteria>
Extension facts can improve Phase 36 precision under validation, provenance, precision ceilings, cache identity, quarantine, and eval proof.
</success_criteria>

<output>
Create `.planning/phases/36-p0-type-value-place-alias-substrate/36-06-SUMMARY.md` when done.
</output>
