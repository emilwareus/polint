---
phase: 36-p0-type-value-place-alias-substrate
plan: 03
title: Go Type, Value, Allocation, Access Path, And Narrowing Facts
type: execute
wave: 3
depends_on: [36-02]
files_modified:
  - crates/polint/src/analysis/types/go.rs
  - crates/polint/src/analysis/types/provider.rs
  - crates/polint/src/analysis/types/cache_key.rs
  - crates/polint/src/analysis/mir/lower_go.rs
  - crates/polint/src/go/lifecycle.rs
  - tests/eval-fixtures/type-value-alias/go-core/expected.polint-eval.toml
autonomous: true
requirements: [SAE-PREC-01]
must_haves:
  truths:
    - "D-08/D-09 are covered for Go: receiver narrowing, nil/interface/pointer receiver facts, and official-tool input digests are represented where used."
    - "D-11/D-12/D-13 are covered for Go: composite literal allocations, function values, selectors, fields, indexes, and access paths produce normalized facts."
    - "Unsupported Go constructs produce Unknown/Unsupported rows instead of exact claims."
---

<objective>
Populate the Phase 36 provider with first-tier Go type, value, allocation, access-path, and local narrowing facts over existing Go MIR/CFG/place/domain data.

Purpose: Prove the precision substrate on Go without adding a mandatory global points-to pass.
Output: Go facts for receiver narrowing, function values, composite/object allocations, selector/field access paths, nil/interface/pointer receiver cases, and official-tool digest participation where official tooling is used.
</objective>

<execution_context>
@.planning/phases/36-p0-type-value-place-alias-substrate/36-CONTEXT.md
@.planning/phases/36-p0-type-value-place-alias-substrate/36-PATTERNS.md
@research/type-alias-points-to/languages/go.md
@.agents/skills/rust-best-practices/SKILL.md
@AGENTS.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Extract Go type and access-path seed facts</name>
  <files>crates/polint/src/analysis/types/go.rs, crates/polint/src/analysis/types/provider.rs, crates/polint/src/analysis/mir/lower_go.rs</files>
  <read_first>
    - crates/polint/src/analysis/mir/lower_go.rs
    - crates/polint/src/analysis/places.rs
    - crates/polint/src/analysis/domains/store.rs
    - crates/polint/src/analysis/types/facts.rs
    - research/type-alias-points-to/languages/go.md
  </read_first>
  <action>
    Add `analysis/types/go.rs` with a Go extractor that walks Go MIR bodies, places, operations, domain observations, and unsupported semantic rows. Emit type facts for parameters, receivers, locals, globals, call returns, nil/interface/pointer receiver evidence, and unresolved/setup-missing cases. Emit access-path facts for selectors, fields, indexes, derefs, and call returns using existing `PlaceId` and `PlaceProjection` data. Keep facts language-owned but normalized into shared Phase 36 fact rows.
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis::types::go --locked</automated>
  </verify>
  <acceptance_criteria>
    - `analysis/types/go.rs` contains an extractor function invoked by the Phase 36 provider.
    - Go receiver places produce type/access-path rows with `Language::Go`.
    - Unsupported Go rows produce Unknown or Unsupported Phase 36 facts, not Present/Exact facts.
    - Unit tests cover receiver, selector, index, deref, nil, and setup-missing cases.
  </acceptance_criteria>
  <done>Go type and access-path seed facts are produced from existing MIR/place/domain data.</done>
</task>

<task type="auto">
  <name>Task 2: Add Go value and allocation tokens</name>
  <files>crates/polint/src/analysis/types/go.rs, crates/polint/src/analysis/values/facts.rs, tests/eval-fixtures/type-value-alias/go-core/repo/service.go</files>
  <read_first>
    - crates/polint/src/analysis/mir/op.rs
    - crates/polint/src/analysis/mir/lower_go.rs
    - crates/polint/src/analysis/values/facts.rs
    - tests/eval-fixtures/semantic-mir/core/repo/service.go
  </read_first>
  <action>
    Emit Go value facts for nil, booleans/truthiness where existing domains prove it, string/number literals where MIR already exposes literals, function values, method values, call returns, and unknown values. Emit allocation tokens for composite literals, address-taken values where visible, function objects/method values, and synthetic unknown allocation tokens only when needed to preserve uncertainty. Stable allocation keys must include language, file/function/body, operation or span evidence, and allocation kind.
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis::types::go --locked</automated>
  </verify>
  <acceptance_criteria>
    - Go composite literal fixture produces `AllocationKind::CompositeLiteral`.
    - Go function value fixture produces `ValueKind::FunctionObject`.
    - Go unknown dynamic/unsupported fixture produces `ValueKind::Unknown` or Unknown status with evidence.
    - Allocation token stable keys are deterministic across repeated test runs.
  </acceptance_criteria>
  <done>Go value facts and allocation tokens are available for later alias/call refinement.</done>
</task>

<task type="auto">
  <name>Task 3: Include official-tool input digests when used</name>
  <files>crates/polint/src/analysis/types/cache_key.rs, crates/polint/src/go/lifecycle.rs, tests/eval-fixtures/type-value-alias/go-core/expected.polint-eval.toml</files>
  <read_first>
    - crates/polint/src/go/lifecycle.rs
    - crates/polint/src/analysis_kernel/incremental/input_snapshot.rs
    - crates/polint/src/analysis/types/cache_key.rs
    - .planning/AGENTS.md
  </read_first>
  <action>
    If this plan consults official Go tooling or Go lifecycle metadata for type facts, include its setup/toolchain/module-root/build-tags/package-pattern/include-tests digests in the Phase 36 provider digest. If official tooling is not activated yet, add an explicit absent/off status row and digest sentinel so future activation changes cache identity. Add tests proving toggling a Go lifecycle/toolchain digest changes the Phase 36 provider digest when Go type facts depend on it.
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis::types::cache_key --locked</automated>
  </verify>
  <acceptance_criteria>
    - Cache key tests prove Go lifecycle/toolchain digest changes affect Phase 36 output identity when official-tool input is active.
    - If official Go tooling is absent, cache key contains an explicit absent sentinel and facts use SetupMissing/Unknown where appropriate.
    - No repository lifecycle files are written for Go setup.
  </acceptance_criteria>
  <done>Go official-tool input participation is explicit and cache-safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Go tool/lifecycle input | Official Go tooling or lifecycle metadata may affect type facts and cache identity. |
| unsupported semantics | Reflective/dynamic Go constructs must not be reported as exact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-05 | Spoofing | official Go setup | mitigate | Include lifecycle/toolchain/config digests and explicit absent/setup-missing rows. |
| T-36-06 | Information Disclosure | Go debug/eval rows | mitigate | Emit stable keys and fact summaries only, not raw source bodies or absolute paths. |
| T-36-SC | Tampering | dependency installation | accept | Use existing Go lifecycle/tooling boundaries; do not add arbitrary analyzers. |
</threat_model>

<verification>
Run:
- `cargo test -p polint --lib analysis::types::go analysis::types::cache_key --locked`
- `cargo test -p polint --test cli --locked`
- `cargo check -p polint --locked`
</verification>

<success_criteria>
Go fixtures cover receiver narrowing, function values, composite/property allocations, unresolved aliases/setup gaps, and official-tool digest behavior where used.
</success_criteria>

<output>
Create `.planning/phases/36-p0-type-value-place-alias-substrate/36-03-SUMMARY.md` when done.
</output>
