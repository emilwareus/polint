---
phase: 36-p0-type-value-place-alias-substrate
plan: 01
title: Private Type, Value, Allocation, Access Path, And Alias Contracts
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/polint/src/analysis/ids.rs
  - crates/polint/src/analysis/mod.rs
  - crates/polint/src/analysis/types/mod.rs
  - crates/polint/src/analysis/types/facts.rs
  - crates/polint/src/analysis/types/store.rs
  - crates/polint/src/analysis/values/mod.rs
  - crates/polint/src/analysis/values/facts.rs
  - crates/polint/src/analysis/values/store.rs
  - crates/polint/src/analysis/access_paths/mod.rs
  - crates/polint/src/analysis/access_paths/facts.rs
  - crates/polint/src/analysis/access_paths/store.rs
  - crates/polint/src/analysis/points_to/mod.rs
  - crates/polint/src/analysis/points_to/facts.rs
  - crates/polint/src/analysis/aliases/mod.rs
  - crates/polint/src/analysis/aliases/facts.rs
  - crates/polint/src/analysis/aliases/store.rs
  - crates/polint/src/analysis_kernel/metadata.rs
autonomous: true
requirements: [SAE-PREC-01]
must_haves:
  truths:
    - "D-01/D-03/D-04 are covered: private fact families exist for type, narrowed type, value, allocation token, access path, points-to constraint/output, and alias answer rows."
    - "D-02 is covered: Phase 36 references existing PlaceId/PlaceFact and does not replace semantic MIR place identity."
    - "D-05 is covered: all type/value/place/alias output containers normalize and sort by stable key before storage, metadata assignment, debug serialization, eval observation, and cache digesting."
    - "D-06 is covered: type facts keep declared, inferred/resolved, narrowed-at-CFG-location, extension-provided, unknown, unsupported, and setup-missing as separate evidence phases."
    - "D-07 is covered: the shared type envelope supports primitive/literal/nullish, callable, class/object/module, nominal, structural, union/intersection-like, generic placeholder, Any, Unknown, and Unsupported distinctions."
    - "D-18 is covered: Alias status vocabulary includes NoAlias, MayAlias, MustAlias, PartialAlias, and Unknown."
    - "All new fact families are crate-private and have deterministic stable-key-based normalized output containers."
  artifacts:
    - path: "crates/polint/src/analysis/types/facts.rs"
      provides: "TypeFact, NarrowedTypeFact, TypePhase, TypePrecision, TypeStatus, TypeShape vocabulary"
    - path: "crates/polint/src/analysis/values/facts.rs"
      provides: "ValueFact, AllocationTokenFact, ValueKind, AllocationKind, ValueStatus vocabulary"
    - path: "crates/polint/src/analysis/access_paths/facts.rs"
      provides: "AccessPathFact and projection/key/status vocabulary over existing PlaceId"
    - path: "crates/polint/src/analysis/points_to/facts.rs"
      provides: "PointsToConstraintFact, PointsToSetFact or equivalent solver-output fact, PtVar/ObjectToken IDs"
    - path: "crates/polint/src/analysis/aliases/facts.rs"
      provides: "AliasAnswerFact, AliasStatus, AliasReason, AliasEvidence vocabulary"
    - path: "crates/polint/src/analysis_kernel/metadata.rs"
      provides: "FactFamily variants and labels for all Phase 36 fact families"
---

<objective>
Define the private Phase 36 fact contracts, IDs, normalized output containers, stores, and FactFamily variants for normalized type, value, allocation, access-path, points-to, and alias facts.

Purpose: Establish the stable internal vocabulary that every later Phase 36 plan depends on, while reusing existing MIR `PlaceId` identity and preserving public API discipline.
Output: Crate-private contract modules with unit tests proving sorting, ID assignment, status/precision separation, and alias vocabulary.
</objective>

<execution_context>
@.planning/phases/36-p0-type-value-place-alias-substrate/36-CONTEXT.md
@.planning/phases/36-p0-type-value-place-alias-substrate/36-PATTERNS.md
@.agents/skills/rust-best-practices/SKILL.md
@AGENTS.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@research/type-alias-points-to/FINAL-REPORT.md
@research/type-alias-points-to/RECOMMENDED_IMPLEMENTATION.md
@crates/polint/src/analysis/places.rs
@crates/polint/src/analysis/mir/op.rs
@crates/polint/src/analysis/domains/facts.rs
@crates/polint/src/analysis/entrypoints/facts.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dense IDs and crate-private modules</name>
  <files>crates/polint/src/analysis/ids.rs, crates/polint/src/analysis/mod.rs, crates/polint/src/analysis/types/mod.rs, crates/polint/src/analysis/values/mod.rs, crates/polint/src/analysis/access_paths/mod.rs, crates/polint/src/analysis/points_to/mod.rs, crates/polint/src/analysis/aliases/mod.rs</files>
  <read_first>
    - .agents/skills/rust-best-practices/SKILL.md
    - AGENTS.md
    - crates/polint/src/analysis/ids.rs
    - crates/polint/src/analysis/mod.rs
    - crates/polint/src/analysis/places.rs
    - crates/polint/src/analysis/entrypoints/mod.rs
  </read_first>
  <action>
    Add dense ID newtypes following the existing `pub(crate) struct X(pub(crate) u64)` pattern: `TypeFactId`, `TypeSetId`, `NarrowedTypeId`, `ValueFactId`, `AbstractValueId`, `AllocationTokenId`, `AccessPathId`, `PointsToConstraintId`, `PointsToSetId`, `PtVarId`, `ObjectTokenId`, and `AliasAnswerId`. Register `pub(crate)` modules `types`, `values`, `access_paths`, `points_to`, and `aliases` in `analysis/mod.rs`. Each module gets a `mod.rs` exposing only crate-private `facts` and `store` submodules as needed. Do not modify `lib.rs`, SDK, runner, CLI, README, or `docs/facts`.
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis::ids --locked</automated>
    <automated>rg -n "pub mod types|pub mod values|pub mod aliases|Types&lt;'_|Aliases&lt;'_|Values&lt;'" crates/polint/src/lib.rs crates/polint/src/sdk crates/polint/src/runner README.md docs || true</automated>
  </verify>
  <acceptance_criteria>
    - `crates/polint/src/analysis/ids.rs` contains all 12 Phase 36 dense ID newtypes.
    - `crates/polint/src/analysis/mod.rs` contains `pub(crate) mod types`, `values`, `access_paths`, `points_to`, and `aliases`.
    - `cargo test -p polint --lib analysis::ids --locked` passes.
    - Public SDK, runner, README, and docs do not contain `Types<'_>`, `Values<'_>`, or `Aliases<'_>`.
  </acceptance_criteria>
  <done>Dense IDs and private module registration compile without public surface changes.</done>
</task>

<task type="auto">
  <name>Task 2: Define fact rows and vocabulary enums</name>
  <files>crates/polint/src/analysis/types/facts.rs, crates/polint/src/analysis/values/facts.rs, crates/polint/src/analysis/access_paths/facts.rs, crates/polint/src/analysis/points_to/facts.rs, crates/polint/src/analysis/aliases/facts.rs</files>
  <read_first>
    - crates/polint/src/analysis/domains/facts.rs
    - crates/polint/src/analysis/places.rs
    - crates/polint/src/analysis/mir/op.rs
    - crates/polint/src/analysis/calls/facts.rs
    - crates/polint/src/analysis/entrypoints/facts.rs
  </read_first>
  <action>
    Define fact structs and enums with serde derives and crate-private fields:
    - `TypeFact`: id, subject, type_set, phase, language, file/function/body/place/cfg location references as optional fields, precision, confidence/status, provenance, stable_key. The `phase` enum must explicitly include the D-06 values: Declared, Inferred, Resolved, FlowNarrowed, ExtensionProvided, Unknown, Unsupported, and SetupMissing.
    - `NarrowedTypeFact`: id, place, cfg node/edge or operation location, predicate/evidence, narrowed type_set, status, precision, stable_key.
    - `ValueFact`: id, subject place/operation, value_kind, language, file/function/body, precision, status, provenance, stable_key.
    - `AllocationTokenFact`: id, token kind, language, file/function/body, source place/operation/span, provenance, stable_key.
    - `AccessPathFact`: id, base place, projections, depth, status, language, file/function/body, stable_key.
    - `PointsToConstraintFact`: id, constraint kind, src/dst PtVar/ObjectToken refs, field/index key where applicable, status, precision, stable_key.
    - `PointsToSetFact`: id, variable, object tokens, status, precision, budget status, stable_key.
    - `AliasAnswerFact`: id, left/right access path or place refs, `AliasStatus`, reason, evidence, precision, status, stable_key.

    Include required enum vocabulary from 36-CONTEXT.md: TypePhase includes Declared, Inferred, Resolved, FlowNarrowed, ExtensionProvided, Unknown, Unsupported, SetupMissing. TypeShape or equivalent supports primitive/literal/nullish, callable signature, class/object/module, nominal ID, structural shape ID, union/intersection-like set, generic placeholder, Any, Unknown, and Unsupported distinctions per D-07. ValueKind includes Null, Undefined, Nil, Bool, Number, String, Literal, FunctionObject, ClassObject, ModuleObject, Object, Array, CompositeLiteral, CallReturn, Unknown. AllocationKind includes ObjectLiteral, ArrayLiteral, CompositeLiteral, FunctionObject, ClassObject, ModuleNamespace, Closure, SyntheticFrameworkObject, ExtensionModeledObject, Unknown. AliasStatus includes exactly `NoAlias`, `MayAlias`, `MustAlias`, `PartialAlias`, `Unknown`.
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis::types analysis::values analysis::access_paths analysis::points_to analysis::aliases --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "enum AliasStatus" crates/polint/src/analysis/aliases/facts.rs` returns one match.
    - `rg -n "NoAlias|MayAlias|MustAlias|PartialAlias|Unknown" crates/polint/src/analysis/aliases/facts.rs` returns all five alias statuses.
    - `rg -n "struct TypeFact|struct NarrowedTypeFact" crates/polint/src/analysis/types/facts.rs` returns matches.
    - `rg -n "Declared|FlowNarrowed|ExtensionProvided|SetupMissing|Any|Unsupported" crates/polint/src/analysis/types/facts.rs` returns matches proving D-06 and D-07 vocabulary exists.
    - `rg -n "struct ValueFact|struct AllocationTokenFact" crates/polint/src/analysis/values/facts.rs` returns matches.
    - `rg -n "struct AccessPathFact" crates/polint/src/analysis/access_paths/facts.rs` returns one match.
    - `rg -n "struct PointsToConstraintFact|struct PointsToSetFact" crates/polint/src/analysis/points_to/facts.rs` returns matches.
    - `cargo test -p polint --lib analysis::types analysis::values analysis::access_paths analysis::points_to analysis::aliases --locked` passes.
  </acceptance_criteria>
  <done>Fact rows and required status/precision vocabularies exist and compile.</done>
</task>

<task type="auto">
  <name>Task 3: Add normalized stores and FactFamily variants</name>
  <files>crates/polint/src/analysis/types/store.rs, crates/polint/src/analysis/values/store.rs, crates/polint/src/analysis/access_paths/store.rs, crates/polint/src/analysis/aliases/store.rs, crates/polint/src/analysis_kernel/metadata.rs</files>
  <read_first>
    - crates/polint/src/analysis/entrypoints/store.rs
    - crates/polint/src/analysis/calls/store.rs
    - crates/polint/src/analysis/domains/store.rs
    - crates/polint/src/analysis_kernel/metadata.rs
  </read_first>
  <action>
    Add output/store structs for type, value/allocation, access path, points-to, and alias facts. Each output has `normalized(mut self) -> Self` that sorts by stable key and reassigns dense IDs deterministically before storage, metadata assignment, debug serialization, eval observation, and cache digesting per D-05. Each store builds BTreeMap indexes by language, file, function, place/access-path, status, and precision where useful. Validate that rows referencing `PlaceId`, `MirBodyId`, `MirOpId`, `CallSiteId`, or CFG node IDs only reference existing rows when the store has enough context; otherwise leave cross-family validation for Plan 36-07.

    Extend `FactFamily` in `analysis_kernel/metadata.rs` with Phase 36 variants and labels: Type, NarrowedType, Value, AllocationToken, AccessPath, PointsToConstraint, PointsToSet, AliasAnswer, and a generic TypeValueAliasEvent if event rows are added.
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis::types::store analysis::values::store analysis::access_paths::store analysis::aliases::store --locked</automated>
    <automated>cargo test -p polint --lib analysis_kernel::metadata --locked</automated>
  </verify>
  <acceptance_criteria>
    - Store tests prove output sorting is deterministic and dense IDs are reassigned from zero.
    - `analysis_kernel/metadata.rs` labels every Phase 36 FactFamily variant.
    - No new `pub` item is exported from crate root, SDK, or runner.
    - Store tests pass under `cargo test -p polint --lib`.
  </acceptance_criteria>
  <done>Normalized private stores and FactFamily labels are ready for provider wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internal facts | New fact rows are private internal data consumed by later providers, debug, eval, and validation. |
| public API | New contract names must not become rule-author or CLI-visible API. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | Information Disclosure | SDK/CLI/public docs | mitigate | Keep modules `pub(crate)` and add no public exports; later no-leak plan verifies this. |
| T-36-02 | Tampering | stable IDs and stores | mitigate | Normalize by stable key and reassign dense IDs deterministically before storage. |
| T-36-SC | Tampering | dependency installation | accept | No new third-party dependencies are required in this plan. |
</threat_model>

<verification>
Run:
- `cargo test -p polint --lib analysis::ids --locked`
- `cargo test -p polint --lib analysis::types analysis::values analysis::access_paths analysis::points_to analysis::aliases --locked`
- `cargo check -p polint --locked`
</verification>

<success_criteria>
Phase 36 private fact contracts compile, use existing place identity, include all required alias statuses, and expose no public SDK/CLI surface.
</success_criteria>

<output>
Create `.planning/phases/36-p0-type-value-place-alias-substrate/36-01-SUMMARY.md` when done.
</output>
