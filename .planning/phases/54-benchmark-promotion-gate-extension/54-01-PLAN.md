---
phase: 54-benchmark-promotion-gate-extension
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/polint/src/eval/metrics.rs
  - crates/polint/src/eval/report.rs
  - crates/polint/src/eval/markdown.rs
  - crates/polint/src/eval/competitors.rs
autonomous: true
requirements: [BENCH-01]

must_haves:
  truths:
    - "D-09/D-10/D-12: F0.5 is added as a precision-weighted internal metric alongside existing precision, recall, F1, F2, and F3 without weakening report schema compatibility."
    - "D-11/D-04: per-language deltas are represented as separate language/suite/scoring-mode/precision-tier rows, not a single averaged milestone value."
    - "Roadmap SC2: promotion reports track F0.5 alongside F1 and carry per-language deltas for later gate enforcement."
  artifacts:
    - path: "crates/polint/src/eval/metrics.rs"
      provides: "F0.5 computation"
      contains: "f_score(0.5"
    - path: "crates/polint/src/eval/report.rs"
      provides: "defaulted report fields for F0.5 and per-language deltas"
      contains: "PerLanguageDeltaRow"
    - path: "crates/polint/src/eval/markdown.rs"
      provides: "deterministic markdown rendering for F0.5 and deltas"
      contains: "F0.5"
---

<objective>
Add the metric/report foundation for v1.3 promotion: F0.5 tracking and deterministic per-language delta rows that later gates can enforce without aggregating away weak suites.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/54-benchmark-promotion-gate-extension/54-CONTEXT.md
@.planning/phases/51-adaptation-model-layer/51-CONTEXT.md
@.planning/phases/52-refined-calls-rework-unknown-taxonomy-consolidation/52-CONTEXT.md
@crates/polint/src/eval/metrics.rs
@crates/polint/src/eval/report.rs
@crates/polint/src/eval/markdown.rs
@crates/polint/src/eval/competitors.rs
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compute F0.5 in internal scanner metrics</name>
  <files>crates/polint/src/eval/metrics.rs, crates/polint/src/eval/report.rs</files>
  <read_first>
    - crates/polint/src/eval/metrics.rs
    - crates/polint/src/eval/report.rs
    - crates/polint/src/eval/markdown.rs
  </read_first>
  <action>
    Add `f0_5: Option<f64>` to `ComputedMetrics` and `ScannerMetricSection`, compute it with the existing `f_score` helper using beta `0.5`, and propagate it into `MetricSections::scanner`. Keep `MetricSummary` top-level fields unchanged unless the existing layout-lock tests are intentionally updated with compatibility coverage.
  </action>
  <verify>
    <automated>cargo test -p polint --lib eval::metrics 2>&1 | tail -100</automated>
  </verify>
  <acceptance_criteria>
    - `ComputedMetrics` has an `f0_5: Option<f64>` field.
    - `ScannerMetricSection` has an `f0_5: Option<f64>` field.
    - A metrics unit test proves `precision=0.75` and `recall=0.60` computes the beta=0.5 score.
    - Existing F1/F2/F3 values remain unchanged in their current tests.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add defaulted per-language delta rows</name>
  <files>crates/polint/src/eval/report.rs, crates/polint/src/eval/competitors.rs</files>
  <read_first>
    - crates/polint/src/eval/report.rs
    - crates/polint/src/eval/competitors.rs
    - crates/polint/src/eval/suite.rs
  </read_first>
  <action>
    Add a defaulted internal `per_language_deltas: Vec<PerLanguageDeltaRow>` field under `MetricSections`. Define `PerLanguageDeltaRow` in `report.rs` with deterministic fields: `language`, `suite_id`, `scoring_mode`, `precision_tier`, `baseline_precision`, `current_precision`, `precision_delta`, `baseline_recall`, `current_recall`, `recall_delta`, `baseline_f0_5`, `current_f0_5`, and `f0_5_delta`. Add a sort/normalize helper so rows are ordered by language, suite id, scoring mode, then precision tier.
  </action>
  <verify>
    <automated>cargo test -p polint --lib eval::report 2>&1 | tail -100</automated>
  </verify>
  <acceptance_criteria>
    - Older JSON without `per_language_deltas` deserializes with an empty vector.
    - Rows sort deterministically by `language`, `suite_id`, `scoring_mode`, and `precision_tier`.
    - The struct stores F0.5 baseline/current/delta separately from F1.
    - No per-language delta code averages Go and Jelly into one value.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Render F0.5 and deltas in reports</name>
  <files>crates/polint/src/eval/markdown.rs, crates/polint/src/eval/report.rs</files>
  <read_first>
    - crates/polint/src/eval/markdown.rs
    - crates/polint/src/eval/report.rs
    - crates/polint/src/eval/metrics.rs
  </read_first>
  <action>
    Update the markdown comparison table and scanner metric table to include an `F0.5` column. Add a `Per-Language Deltas` section that renders `language`, `suite_id`, `scoring_mode`, `precision_tier`, precision delta, recall delta, and F0.5 delta using the existing `metric_cell` formatting.
  </action>
  <verify>
    <automated>cargo test -p polint --lib eval::markdown 2>&1 | tail -100</automated>
  </verify>
  <acceptance_criteria>
    - Rendered markdown contains the exact header text `F0.5`.
    - Rendered markdown contains the exact section header `## Per-Language Deltas`.
    - Empty delta rows render as a deterministic `_none_` or one-row placeholder, not an omitted section.
    - Markdown tests assert the F0.5 column appears alongside F1.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Risk | Mitigation |
|----------|------|------------|
| Report schema compatibility | Adding F0.5 could break older report JSON. | Use defaulted section fields and add deserialization tests for older JSON. |
| Delta aggregation | Weak language results could be hidden by a strong aggregate. | Store and render deltas by language, suite, scoring mode, and precision tier. |
| Public API surface | Internal eval rows could become public SDK promises. | Keep new structs `pub(crate)` and run the public-surface leak gate in later plans. |
</threat_model>

<verification>
- `cargo test -p polint --lib eval::metrics`
- `cargo test -p polint --lib eval::report`
- `cargo test -p polint --lib eval::markdown`
</verification>

<success_criteria>
- BENCH-01 metric foundation exists for F0.5 and per-language deltas.
- Existing F1/F2/F3 behavior remains stable.
- Reports expose deterministic, non-aggregated language/suite deltas for promotion gates.
</success_criteria>
