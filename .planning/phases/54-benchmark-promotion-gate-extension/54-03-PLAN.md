---
phase: 54-benchmark-promotion-gate-extension
plan: 03
type: execute
wave: 2
depends_on: [54-01]
files_modified:
  - .github/workflows/ci.yml
  - crates/polint/src/eval/go_rta.rs
  - crates/polint/src/eval/ts_tokens.rs
  - crates/polint/src/eval/ts_object_model.rs
  - crates/polint/tests/public_surface_leak.rs
autonomous: true
requirements: [BENCH-01]

must_haves:
  truths:
    - "D-13/D-14/D-15/D-16: the existing Go+TS polyglot fixture becomes a named promotion/CI canary proving Go RTA, TS token, TS object-model, and no cross-language edges."
    - "D-17/D-18/D-19/D-20: the existing public-surface leak gate remains frozen, runs locally and in CI, and visibility fixes are preferred over allowlist expansion."
    - "Roadmap SC3/SC4: polyglot canary and public-API leak CI gates are PR-blocking v1.3 exit gates."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "promotion gate CI job wiring"
      contains: "promotion gate"
    - path: "tests/eval-fixtures/polyglot-canary/go-ts/expected.polint-eval.toml"
      provides: "canonical polyglot canary fixture"
      contains: "polyglot-canary-go-ts"
    - path: "crates/polint/tests/public_surface_leak.rs"
      provides: "frozen public prelude leak gate"
      contains: "ALLOWED_PRELUDE"
---

<objective>
Promote the existing mixed Go+TS solver canary and public-surface leak test into explicit v1.3 promotion checks that run on solver changes and block CI when internals leak.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/54-benchmark-promotion-gate-extension/54-CONTEXT.md
@.planning/phases/48-go-rta-driver/48-CONTEXT.md
@.planning/phases/49-js-ts-function-token-propagation-driver/49-CONTEXT.md
@.planning/phases/50-js-ts-object-property-prototype-this-model-driver/50-CONTEXT.md
@tests/eval-fixtures/polyglot-canary/go-ts/expected.polint-eval.toml
@crates/polint/src/eval/go_rta.rs
@crates/polint/src/eval/ts_tokens.rs
@crates/polint/src/eval/ts_object_model.rs
@crates/polint/tests/public_surface_leak.rs
@.github/workflows/ci.yml
@docs/API-VISIBILITY-PLAN.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the polyglot canary a named promotion check</name>
  <files>crates/polint/src/eval/go_rta.rs, crates/polint/src/eval/ts_tokens.rs, crates/polint/src/eval/ts_object_model.rs</files>
  <read_first>
    - tests/eval-fixtures/polyglot-canary/go-ts/expected.polint-eval.toml
    - crates/polint/src/eval/go_rta.rs
    - crates/polint/src/eval/ts_tokens.rs
    - crates/polint/src/eval/ts_object_model.rs
  </read_first>
  <action>
    Ensure each existing polyglot canary test name and assertion text clearly includes the `polyglot` selector and validates its lane: Go RTA resolves only Go edges, TS token propagation resolves only intra-TS token edges, TS object-model resolves only intra-TS property-backed edges, and no Go-to-TS or TS-to-Go solver edge is emitted.
  </action>
  <verify>
    <automated>cargo test -p polint polyglot --lib --locked 2>&1 | tail -120</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p polint polyglot --lib --locked` runs the Go RTA, TS token, and TS object-model canary tests.
    - At least one assertion fails if a solver-derived edge crosses the Go/TS boundary.
    - The fixture path `tests/eval-fixtures/polyglot-canary/go-ts` remains the canonical canary path.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add a CI promotion gate job for solver canary and leaks</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml
    - crates/polint/tests/public_surface_leak.rs
    - crates/polint/src/eval/determinism_gate.rs
  </read_first>
  <action>
    Add or update a CI job named `promotion gate` that runs on Linux and macOS with `fail-fast: false`, sets up Go and Rust like the determinism gate, and runs these exact commands: `cargo test -p polint polyglot --lib --locked`, `cargo test --package polint --test public_surface_leak --locked`, and `cargo test -p polint --lib eval::determinism_gate --locked`. Preserve the existing standalone `leak-gate` and `determinism-gate` jobs unless the workflow already has a strictly equivalent job.
  </action>
  <verify>
    <automated>rg -n "promotion gate|cargo test -p polint polyglot --lib --locked|public_surface_leak|eval::determinism_gate" .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains a job or step named `promotion gate`.
    - The promotion gate job runs on both `ubuntu-latest` and `macos-latest`.
    - The promotion gate job contains the command `cargo test -p polint polyglot --lib --locked`.
    - The promotion gate job contains the command `cargo test --package polint --test public_surface_leak --locked`.
    - The promotion gate job contains the command `cargo test -p polint --lib eval::determinism_gate --locked`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Keep the public prelude leak gate frozen</name>
  <files>crates/polint/tests/public_surface_leak.rs, docs/API-VISIBILITY-PLAN.md</files>
  <read_first>
    - crates/polint/tests/public_surface_leak.rs
    - docs/API-VISIBILITY-PLAN.md
    - crates/polint/src/lib.rs
  </read_first>
  <action>
    Run the public-surface leak test after plans 01 and 02 add eval/gate internals. If any new solver, promotion, benchmark, cache, or eval name appears in prelude output, tighten module/item visibility and keep `ALLOWED_PRELUDE` unchanged.
  </action>
  <verify>
    <automated>cargo test --package polint --test public_surface_leak --locked 2>&1 | tail -80</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --package polint --test public_surface_leak --locked` passes.
    - `ALLOWED_PRELUDE` is not extended for v1.3 solver, benchmark, gate, cache, or eval internals.
    - No new public `pub use` exposes `eval`, `gates`, `solver`, `cache`, or benchmark types through `polint::sdk::prelude::*`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Risk | Mitigation |
|----------|------|------------|
| Mixed-language solver output | A shared solver change could create cross-language false edges. | Run the polyglot canary on every promotion path and assert no Go/TS boundary crossing. |
| CI coverage | Local tests could pass while PR gating omits the canary or leak check. | Add a named Linux/macOS promotion gate job with exact canary, leak, and determinism commands. |
| Public SDK prelude | Internal gate or solver names could leak by accidental `pub use`. | Keep `ALLOWED_PRELUDE` frozen and fix visibility on failure. |
</threat_model>

<verification>
- `cargo test -p polint polyglot --lib --locked`
- `cargo test --package polint --test public_surface_leak --locked`
- `cargo test -p polint --lib eval::determinism_gate --locked`
- `rg -n "promotion gate|polyglot|public_surface_leak" .github/workflows/ci.yml`
</verification>

<success_criteria>
- BENCH-01 polyglot canary is a named, repeatable promotion check.
- CI blocks public API leaks and mixed-language solver regressions.
- v1.3 internals remain absent from `polint::sdk::prelude::*`.
</success_criteria>
