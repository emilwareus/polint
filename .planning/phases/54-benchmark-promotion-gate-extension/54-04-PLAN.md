---
phase: 54-benchmark-promotion-gate-extension
plan: 04
type: execute
wave: 3
depends_on: [54-02, 54-03]
files_modified:
  - .planning/phases/54-benchmark-promotion-gate-extension/54-AUDIT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - research/evaluation-harness/baselines/README.md
autonomous: true
requirements: [BENCH-01]

must_haves:
  truths:
    - "D-27/D-28/D-29: milestone closeout reconciles roadmap, requirements, state, and a final audit with exact commands, metric values, gate verdicts, and limitations."
    - "D-08/D-20: external Go/Jelly recall claims are recorded only when measured, and public leak-gate verification is recorded rather than inferred from file existence."
    - "Roadmap SC5: v1.3 audit records final Go and Jelly recall numbers against the baseline target with precision floors held, or records explicit limitations for unavailable external suites."
  artifacts:
    - path: ".planning/phases/54-benchmark-promotion-gate-extension/54-AUDIT.md"
      provides: "final v1.3 benchmark promotion audit"
      contains: "BENCH-01"
    - path: ".planning/REQUIREMENTS.md"
      provides: "BENCH-01 closeout status"
      contains: "[x] **BENCH-01**"
    - path: ".planning/ROADMAP.md"
      provides: "phase 54 plan list and completion/audit notes"
      contains: "Benchmark Promotion Gate Extension"
---

<objective>
Close the v1.3 milestone by running the final promotion verification, recording measured Go/Jelly benchmark evidence and limitations, and reconciling planning state for BENCH-01.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/54-benchmark-promotion-gate-extension/54-CONTEXT.md
@research/evaluation-harness/GRAPH-ENGINE-BENCHMARK-RESEARCH.md
@research/evaluation-harness/STANDARD.md
@research/evaluation-harness/baselines/README.md
@research/evaluation-harness/decisions/decision-log.md
@crates/polint/src/eval/external/go_x_tools_callgraph.rs
@crates/polint/src/eval/external/jelly_callgraph.rs
@crates/polint/src/eval/gates.rs
@.github/workflows/ci.yml
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run and capture final promotion verification</name>
  <files>.planning/phases/54-benchmark-promotion-gate-extension/54-AUDIT.md</files>
  <read_first>
    - crates/polint/src/eval/gates.rs
    - crates/polint/src/eval/external/go_x_tools_callgraph.rs
    - crates/polint/src/eval/external/jelly_callgraph.rs
    - research/evaluation-harness/baselines/README.md
  </read_first>
  <action>
    Create `54-AUDIT.md` with a deterministic table for each required proof: Go precision floor, Jelly precision floor, F0.5/F1 values, per-language delta verdicts, flooding verdict, polyglot canary verdict, public-surface leak verdict, determinism verdict, and runtime/RSS/cache verdicts where configured. Record the exact command and exit status for every row.
  </action>
  <verify>
    <automated>rg -n "BENCH-01|Go|Jelly|F0.5|polyglot|public-surface|determinism|limitation" .planning/phases/54-benchmark-promotion-gate-extension/54-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - `54-AUDIT.md` contains the exact string `BENCH-01`.
    - `54-AUDIT.md` has rows for Go precision, Jelly precision, F0.5, F1, per-language deltas, flooding, polyglot canary, public-surface leak, and determinism.
    - Every audit row includes a command or source artifact path.
    - Unavailable external suite evidence is marked `limited` or `skipped`, not `passed`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Run final local verification commands</name>
  <files>.planning/phases/54-benchmark-promotion-gate-extension/54-AUDIT.md</files>
  <read_first>
    - .planning/phases/54-benchmark-promotion-gate-extension/54-AUDIT.md
    - .github/workflows/ci.yml
    - crates/polint/tests/public_surface_leak.rs
  </read_first>
  <action>
    Run and record these commands in `54-AUDIT.md`: `cargo test -p polint --lib eval::gates --locked`, `cargo test -p polint polyglot --lib --locked`, `cargo test --package polint --test public_surface_leak --locked`, `cargo test -p polint --lib eval::determinism_gate --locked`, `cargo test -p polint --locked`, `cargo clippy -p polint --all-targets --locked -- -D warnings`, `cargo fmt --all -- --check`, and `git diff --check`.
  </action>
  <verify>
    <automated>rg -n "cargo test -p polint --lib eval::gates --locked|cargo test -p polint polyglot --lib --locked|public_surface_leak|cargo clippy -p polint --all-targets --locked -- -D warnings|cargo fmt --all -- --check|git diff --check" .planning/phases/54-benchmark-promotion-gate-extension/54-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - `54-AUDIT.md` records all eight final verification commands.
    - Failed or skipped commands include the failure reason or limitation.
    - Passing commands include a timestamp-free result such as `exit 0` or `passed`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Reconcile roadmap and requirements closeout</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, research/evaluation-harness/baselines/README.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - research/evaluation-harness/baselines/README.md
    - .planning/phases/54-benchmark-promotion-gate-extension/54-AUDIT.md
  </read_first>
  <action>
    Mark `BENCH-01` complete only after audit evidence exists. Update Phase 54's ROADMAP plan list and status, record final Go/Jelly numbers or limitations in the benchmark baseline notes, and update STATE.md to show v1.3 Phase 54 complete or needs-review based on the audit outcome.
  </action>
  <verify>
    <automated>rg -n "\\[x\\] \\*\\*BENCH-01\\*\\*|Phase 54|54-AUDIT|Benchmark Promotion Gate Extension" .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md research/evaluation-harness/baselines/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` marks `BENCH-01` `[x]` only if `54-AUDIT.md` contains the final evidence rows.
    - `.planning/ROADMAP.md` lists all Phase 54 plans.
    - `.planning/STATE.md` no longer says Phase 54 is merely ready to plan.
    - `research/evaluation-harness/baselines/README.md` references the final audit or records why external final numbers are limited.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Risk | Mitigation |
|----------|------|------------|
| Benchmark claims | Final recall/precision claims could exceed measured evidence. | Record commands, values, and limitations in `54-AUDIT.md`; mark unavailable suites limited/skipped. |
| Planning state | Requirements could be marked complete before gates pass. | Update BENCH-01 only after audit evidence exists. |
| CI/local mismatch | Local verification could ignore CI-only leak or canary checks. | Audit records both local commands and CI workflow wiring for promotion/leak/determinism gates. |
</threat_model>

<verification>
- `rg -n "BENCH-01|Go|Jelly|F0.5|polyglot|public-surface|determinism" .planning/phases/54-benchmark-promotion-gate-extension/54-AUDIT.md`
- `rg -n "\\[x\\] \\*\\*BENCH-01\\*\\*" .planning/REQUIREMENTS.md`
- `rg -n "54-AUDIT|promotion gate" .planning/ROADMAP.md research/evaluation-harness/baselines/README.md`
</verification>

<success_criteria>
- BENCH-01 has a final audit with measured evidence or explicit limitations.
- Phase 54 planning and milestone state are reconciled.
- v1.3 benchmark promotion claims remain truthful and reproducible.
</success_criteria>
