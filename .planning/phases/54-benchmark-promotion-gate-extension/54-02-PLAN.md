---
phase: 54-benchmark-promotion-gate-extension
plan: 02
type: execute
wave: 2
depends_on: [54-01]
files_modified:
  - crates/polint/src/eval/gates.rs
  - crates/polint/src/eval/runner.rs
  - tests/eval-fixtures/promotion/cfg-call-flow-evidence/expected.polint-eval.toml
autonomous: true
requirements: [BENCH-01]

must_haves:
  truths:
    - "D-01/D-02/D-03/D-04/D-05: promotion gates hard-fail per-suite precision floors, with Go fixed at >=60%, Jelly configurable, deterministic gate rows, and no averaging across weak/strong scopes."
    - "D-06/D-07/D-08: flooding traps are explicit failures and recall claims never bypass precision floors or measured evidence."
    - "D-21/D-22/D-23: runtime, RSS, cache, unknown, rejected-fact, and deterministic-output evidence remain deterministic gate inputs."
    - "D-24/D-25/D-26: gate configuration stays internal and extends the existing `PromotionGateThresholds` / `SuiteGateConfig` path."
    - "Roadmap SC1: hard precision floors and flooding rejection are enforced by promotion gates."
  artifacts:
    - path: "crates/polint/src/eval/gates.rs"
      provides: "precision floor, F0.5, per-language delta, flooding, and existing budget gate checks"
      contains: "precision_floor"
    - path: "crates/polint/src/eval/runner.rs"
      provides: "promotion fixture gate coverage"
      contains: "evaluate_promotion_gates"
---

<objective>
Extend the internal promotion gate so v1.3 promotion fails on weak precision, flooding, missing deltas, unstable output, runtime/cache issues, or unsupported gate evidence.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/54-benchmark-promotion-gate-extension/54-CONTEXT.md
@.planning/phases/53-cache-solver-budgets-consolidation/53-CONTEXT.md
@crates/polint/src/eval/gates.rs
@crates/polint/src/eval/runner.rs
@crates/polint/src/eval/report.rs
@crates/polint/src/eval/suite.rs
@tests/eval-fixtures/promotion/cfg-call-flow-evidence/expected.polint-eval.toml
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add internal precision floor thresholds</name>
  <files>crates/polint/src/eval/gates.rs</files>
  <read_first>
    - crates/polint/src/eval/gates.rs
    - crates/polint/src/eval/report.rs
    - crates/polint/src/eval/suite.rs
  </read_first>
  <action>
    Extend `PromotionGateThresholds` with defaulted precision-floor configuration and add gate checks that fail when `run.metrics.precision` or a matching per-language delta row's current precision falls below the configured threshold. Include the fixed Go floor value `0.60` in a test config and support a configurable Jelly floor through `SuiteGateConfig` rather than a hard-coded Jelly constant.
  </action>
  <verify>
    <automated>cargo test -p polint --lib eval::gates 2>&1 | tail -120</automated>
  </verify>
  <acceptance_criteria>
    - A gate test fails when Go precision is `0.59` with a `0.60` threshold.
    - A gate test passes when Go precision is exactly `0.60`.
    - A Jelly floor test sets a non-Go threshold through config and does not rely on the Go constant.
    - Gate check output includes `observed`, `threshold`, and `verdict` for the precision floor.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Enforce F0.5 and per-language delta gates</name>
  <files>crates/polint/src/eval/gates.rs, crates/polint/src/eval/report.rs</files>
  <read_first>
    - crates/polint/src/eval/gates.rs
    - crates/polint/src/eval/report.rs
    - crates/polint/src/eval/metrics.rs
  </read_first>
  <action>
    Add gate checks that require configured per-language delta rows to exist and evaluate their precision, recall, and F0.5 deltas independently. Gate check metric names must include the language and scoring mode, for example `per_language_delta.go.oracle-rta.f0_5`.
  </action>
  <verify>
    <automated>cargo test -p polint --lib eval::gates 2>&1 | tail -120</automated>
  </verify>
  <acceptance_criteria>
    - A test with Go delta below threshold fails even if a Jelly row passes.
    - A test with Jelly delta below threshold fails even if a Go row passes.
    - A test with no required delta row fails with a missing-row gate check.
    - Gate metric names include language and scoring mode text.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Reject flooding fixtures regardless of recall movement</name>
  <files>crates/polint/src/eval/gates.rs, crates/polint/src/eval/runner.rs, tests/eval-fixtures/promotion/cfg-call-flow-evidence/expected.polint-eval.toml</files>
  <read_first>
    - crates/polint/src/eval/gates.rs
    - crates/polint/src/eval/runner.rs
    - tests/eval-fixtures/promotion/cfg-call-flow-evidence/expected.polint-eval.toml
  </read_first>
  <action>
    Add a flooding gate check over `false_positive_trap_hits`, false positives, or a dedicated fixture marker so that a run with improved recall and excessive false positives fails. Use the existing promotion fixture as the seed for one pass case and add a focused failing test fixture or synthetic `EvaluationRun` for the recall-by-flooding case.
  </action>
  <verify>
    <automated>cargo test -p polint --lib eval::gates eval::runner 2>&1 | tail -140</automated>
  </verify>
  <acceptance_criteria>
    - A flooding test has recall higher than its baseline/input but fails the promotion gate.
    - The failing gate check uses the exact metric name `flooding` or `false_positive_trap_hits`.
    - Existing promotion fixture pass behavior remains deterministic.
    - The test does not mark flooding as a warning-only condition.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Risk | Mitigation |
|----------|------|------------|
| Promotion config | Public users could mistake internal gate config for supported CLI/API. | Keep new gate types crate-private/internal and avoid public CLI flags. |
| Recall evidence | A solver can improve recall by emitting too many edges. | Fail on precision floors and flooding traps before considering recall improvement. |
| Machine-specific evidence | RSS/runtime/cache data can be transient. | Normalize missing measurements and keep deterministic output-hash checks. |
</threat_model>

<verification>
- `cargo test -p polint --lib eval::gates`
- `cargo test -p polint --lib eval::runner`
- `cargo test -p polint --lib eval::metrics`
</verification>

<success_criteria>
- BENCH-01 hard precision floors are enforced per suite/language/scoring mode.
- Recall-by-flooding is a fail-build condition.
- F0.5 and per-language delta evidence is consumed by promotion gates.
</success_criteria>
