---
phase: 50-js-ts-object-property-prototype-this-model-driver
plan: 05
type: execute
wave: 4
depends_on: [50-04]
files_modified:
  - tests/eval-fixtures/ts-object-model/object-literal/expected.polint-eval.toml
  - tests/eval-fixtures/ts-object-model/object-literal/repo/src/app.ts
  - tests/eval-fixtures/ts-object-model/prototype-this/expected.polint-eval.toml
  - tests/eval-fixtures/ts-object-model/prototype-this/repo/src/app.ts
  - tests/eval-fixtures/ts-object-model/budget/expected.polint-eval.toml
  - tests/eval-fixtures/ts-object-model/budget/repo/src/app.ts
  - tests/eval-fixtures/determinism/ts_object_model/expected.polint-eval.toml
  - tests/eval-fixtures/polyglot-canary/go-ts/expected.polint-eval.toml
  - tests/eval-fixtures/polyglot-canary/go-ts/repo/tokens.ts
  - crates/polint/src/eval/ts_object_model.rs
  - crates/polint/src/eval/determinism_gate.rs
  - crates/polint/src/eval/mod.rs
  - crates/polint/tests/public_surface_leak.rs
  - .planning/ROADMAP.md
autonomous: true
requirements: [JS-05]

must_haves:
  truths:
    - "D-27/D-28/D-29/D-30: Native fixtures prove object literal, computed-property collapse, prototype termination, arrow-vs-method this, budget overflow, and justified conversion of property/prototype/this unresolved reasons."
    - "D-31/D-32/D-33: Polyglot, Jelly, determinism, and public-surface leak gates prove object-model integration without Go<->TS edges, recall flooding, or public SDK leakage."
    - "Roadmap SC1/SC2/SC3/SC4: JS-05 is closed only when object model implementation, budget evidence, native fixtures/determinism, capability flag, and Jelly deltas are all documented."
  artifacts:
    - path: "crates/polint/src/eval/ts_object_model.rs"
      provides: "crate-private native gate for TS object-model fixtures and Jelly/polyglot assertions"
      contains: "object_model"
    - path: "tests/eval-fixtures/ts-object-model/prototype-this/expected.polint-eval.toml"
      provides: "prototype-walk termination and arrow-vs-method this proof"
      contains: "prototype"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 50 plan list and wave annotations"
      contains: "50-05"
---

<objective>
Close JS-05 with executable proof: native object-model fixtures, budget/unknown evidence, determinism, polyglot non-interference, Jelly benchmark deltas for `oracle-jelly` and `whole-repo`, public-surface leak proof, and roadmap bookkeeping.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/50-js-ts-object-property-prototype-this-model-driver/50-CONTEXT.md
@.planning/phases/50-js-ts-object-property-prototype-this-model-driver/50-PATTERNS.md
@.planning/phases/50-js-ts-object-property-prototype-this-model-driver/50-03-PLAN.md
@.planning/phases/50-js-ts-object-property-prototype-this-model-driver/50-04-PLAN.md
@.planning/phases/49-js-ts-function-token-propagation-driver/49-03-SUMMARY.md
@AGENTS.md

<interfaces>
- `tests/eval-fixtures/ts-tokens/*` proves Phase 49 token behavior and offers fixture style.
- `tests/eval-fixtures/polyglot-canary/go-ts/` currently proves Go RTA plus TS token edges without cross-language edges.
- `eval/determinism_gate.rs` contains determinism fixture precedents.
- `public_surface_leak.rs` must pass without adding object-model internals to `ALLOWED_PRELUDE`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add object literal, property, and computed-key native fixture gate</name>
  <files>tests/eval-fixtures/ts-object-model/object-literal/, crates/polint/src/eval/ts_object_model.rs, crates/polint/src/eval/mod.rs</files>
  <read_first>
    - crates/polint/src/eval/ts_tokens.rs
    - crates/polint/src/eval/fixtures.rs
    - tests/eval-fixtures/ts-tokens/alias-parameter-return/expected.polint-eval.toml
    - tests/eval-fixtures/ts-tokens/alias-parameter-return/repo/src/app.ts
  </read_first>
  <action>
    Create `tests/eval-fixtures/ts-object-model/object-literal/` with `[solver.js] object_model = true`. Include `const holder = { propertyTarget }; holder.propertyTarget(); holder["propertyTarget"]();` and a separate computed-key case. Add `crates/polint/src/eval/ts_object_model.rs` to run the fixture and inspect internal solver rows, asserting object-model derived edges exist for exact property calls and computed bucket behavior stays bounded/conservative.
  </action>
  <verify>
    <automated>cargo test -p polint eval::ts_object_model::object_literal 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - Exact dot property call resolves to `propertyTarget`.
    - Exact string-literal element access resolves to `propertyTarget`.
    - Computed key uses computed bucket evidence and does not emit all exact property targets by default.
    - Fixture proves at least one prior `PropertyFlowRequired` case is justified by object-model facts.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add prototype, class, accessor, and this native fixture gate</name>
  <files>tests/eval-fixtures/ts-object-model/prototype-this/, crates/polint/src/eval/ts_object_model.rs</files>
  <read_first>
    - crates/polint/src/analysis/solver/ts_object_model/prototype.rs
    - crates/polint/src/analysis/solver/ts_object_model/receiver.rs
    - tests/eval-fixtures/go-rta/interface-dispatch/expected.polint-eval.toml
  </read_first>
  <action>
    Create `tests/eval-fixtures/ts-object-model/prototype-this/` with class instance method, `extends`, direct `Ctor.prototype.m = fn`, getter/setter, arrow lexical `this`, method receiver `this`, constructor `this`, bound function, `call`, and `apply` cases. The eval gate must assert prototype-derived and receiver-derived edges for stable cases, plus unsupported evidence for dynamic prototype/native cases.
  </action>
  <verify>
    <automated>cargo test -p polint eval::ts_object_model::prototype_this 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - Prototype method call resolves through bounded lookup.
    - `extends` resolves superclass method under depth cap.
    - Arrow-vs-method `this` fixture proves arrows preserve lexical receiver and methods bind call receiver.
    - Bound function, `call`, and `apply` resolve only when receiver facts are known.
    - At least one prior `PrototypeModelRequired` and one prior `ThisModelRequired` case is justified by object-model facts.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add object-model budget, determinism, and polyglot canary proof</name>
  <files>tests/eval-fixtures/ts-object-model/budget/, tests/eval-fixtures/determinism/ts_object_model/, tests/eval-fixtures/polyglot-canary/go-ts/, crates/polint/src/eval/ts_object_model.rs, crates/polint/src/eval/determinism_gate.rs</files>
  <read_first>
    - crates/polint/src/eval/determinism_gate.rs
    - crates/polint/src/eval/go_rta.rs
    - crates/polint/src/eval/ts_tokens.rs
    - tests/eval-fixtures/polyglot-canary/go-ts/expected.polint-eval.toml
    - tests/eval-fixtures/polyglot-canary/go-ts/repo/tokens.ts
  </read_first>
  <action>
    Add `tests/eval-fixtures/ts-object-model/budget/` with tight object caps proving property bucket overflow, receiver fanout overflow, and prototype-depth termination. Add `tests/eval-fixtures/determinism/ts_object_model/` and a 10-seed determinism gate. Update the polyglot canary to enable object model and assert Go RTA, TS token, and TS object-model edges are all intra-language, with zero Go-to-TS and zero TS-to-Go derived edges.
  </action>
  <verify>
    <automated>cargo test -p polint eval::ts_object_model::budget eval::determinism_gate::ts_object_model eval::ts_object_model::polyglot 2>&1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - Property bucket overflow produces explicit `BudgetExceeded` or object-model budget evidence.
    - Receiver fanout overflow produces explicit budget evidence.
    - Prototype-depth fixture terminates and emits no post-cap fake target.
    - Object-model determinism fixture is byte-identical across 10 permutations.
    - Polyglot canary has no Go<->TS derived edge.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Record Jelly deltas, leak gate, full regression, and roadmap plan list</name>
  <files>crates/polint/src/eval/ts_object_model.rs, crates/polint/tests/public_surface_leak.rs, .planning/ROADMAP.md</files>
  <read_first>
    - crates/polint/src/eval/external/jelly_callgraph.rs
    - research/evaluation-harness/GRAPH-ENGINE-BENCHMARK-RESEARCH.md
    - crates/polint/tests/public_surface_leak.rs
    - .planning/ROADMAP.md
  </read_first>
  <action>
    Add Jelly-focused object-model evidence for both `oracle-jelly` and `whole-repo` scoring modes. If the full Jelly corpus is unavailable locally, record self-contained fixture metrics and an explicit corpus-unavailable note that does not claim exact coverage. Run `public_surface_leak` without extending `ALLOWED_PRELUDE`. Run full `cargo test -p polint` and `cargo clippy -p polint --all-targets`. Update `.planning/ROADMAP.md` Phase 50 `**Plans:**` from `TBD` to the five plan list with brief objectives; wave annotations may be added by the GSD roadmap annotation step after planning.
  </action>
  <verify>
    <automated>cargo test -p polint eval::ts_object_model 2>&1 | tail -40 && cargo test -p polint --test public_surface_leak 2>&1 | tail -30 && cargo test -p polint 2>&1 | tail -30 && cargo clippy -p polint --all-targets 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Jelly evidence covers both `oracle-jelly` and `whole-repo` modes or explicitly records why local corpus metrics are unavailable.
    - Evidence does not claim exact coverage and does not use recall-by-flooding.
    - `public_surface_leak` passes with `ALLOWED_PRELUDE` unchanged.
    - Full `cargo test -p polint` passes.
    - `cargo clippy -p polint --all-targets` passes with no new warnings.
    - `.planning/ROADMAP.md` lists 50-01 through 50-05.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Risk | Mitigation |
|----------|------|------------|
| Benchmark reporting | Recall could be overstated by fixture-only evidence or broad candidate edges. | Report scoring mode explicitly, note corpus gaps, assert precision/no-flooding invariants, leave Phase 54 hard floors separate. |
| Fixture coverage | Tests could miss termination or receiver edge cases. | Include happy path, budget overflow, determinism, prototype termination, `this`, and polyglot canary fixtures. |
| Public surface | New private object-model types could leak through SDK/prelude. | Run `public_surface_leak` and fix visibility; do not edit `ALLOWED_PRELUDE`. |

## STRIDE Notes

- Tampering: fixture gates inspect provenance-backed internal solver rows rather than text-only docs.
- Denial of service: budget fixtures prove bounded object/property/prototype behavior.
- Information disclosure: object-model internals remain crate-private and unlisted from public prelude.
</threat_model>

<verification>
- `cargo test -p polint eval::ts_object_model`
- `cargo test -p polint eval::determinism_gate::ts_object_model`
- `cargo test -p polint --test public_surface_leak`
- `cargo test -p polint`
- `cargo clippy -p polint --all-targets`
</verification>

<success_criteria>
- Native fixtures prove object literal, computed bucket, prototype/class, accessor, and `this` behavior.
- Budget fixtures prove property/receiver/prototype exhaustion is explicit and bounded.
- Polyglot canary proves no Go<->TS interference.
- Jelly deltas are recorded honestly for `oracle-jelly` and `whole-repo`.
- Public leak, full test, and clippy gates pass.
</success_criteria>
