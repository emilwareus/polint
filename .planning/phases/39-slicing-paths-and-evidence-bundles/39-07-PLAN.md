---
phase: 39-slicing-paths-and-evidence-bundles
plan: 07
title: Evidence Debug Eval Fixtures And Public Boundary Proof
type: execute
wave: 7
depends_on: [39-02, 39-03, 39-04, 39-05, 39-06]
files_modified:
  - crates/polint/src/analysis/evidence/debug.rs
  - crates/polint/src/analysis/evidence/validate.rs
  - crates/polint/src/analysis_kernel/debug.rs
  - crates/polint/src/analysis_kernel/validation.rs
  - crates/polint/src/eval/model.rs
  - crates/polint/src/eval/observed.rs
  - crates/polint/src/eval/fixtures.rs
  - crates/polint/tests/cli.rs
  - tests/eval-fixtures/evidence/
autonomous: true
requirements: [SAE-PREC-04]
must_haves:
  truths:
    - "D-21: Validation checks dangling references, invalid spans, impossible edge kind/source-target pairs, missing provenance, unvalidated exactness, context-mismatched call/return paths, missing summary expansion status, omitted-region metadata for truncation, and renderer loss."
    - "D-22: Evidence debug output includes counts, path counts, ranking inputs, unknowns, omitted regions, expansion handles, summary opacity, hidden node counts, and cache/replay keys without raw source bodies, absolute paths, parser object ids, timestamps, or nondeterministic ordering."
    - "D-23: Eval fixtures cover local dependence, thin-versus-full slices, source-to-sink paths, sanitizer/barrier behavior, interprocedural context, summaries, extension evidence, uncertainty, deterministic ranking, renderer determinism, and compact path limits."
    - "D-24: Public no-leak proof covers JSON check output, AI-friendly output, CLI help, SDK exports, runner surface, README, docs/facts, and SARIF/JSON rendering while keeping internal provider ids, debug schemas, and unpromoted SDK views private."
  artifacts:
    - path: "tests/eval-fixtures/evidence/"
      provides: "Native and extension evidence fixture suite"
---

<objective>
Close Phase 39 with fixture coverage, debug/eval observability, and a public-boundary proof that evidence remains an internal analysis/reporting feature.
</objective>

<execution_context>
@.planning/phases/39-slicing-paths-and-evidence-bundles/39-CONTEXT.md
@.planning/phases/39-slicing-paths-and-evidence-bundles/39-PATTERNS.md
@.agents/skills/rust-best-practices/SKILL.md
@AGENTS.md
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add evidence debug and validation rows</name>
  <files>crates/polint/src/analysis/evidence/debug.rs, crates/polint/src/analysis_kernel/debug.rs, crates/polint/src/analysis_kernel/validation.rs</files>
  <read_first>
    - crates/polint/src/analysis_kernel/debug.rs
    - crates/polint/src/analysis_kernel/validation.rs
    - crates/polint/src/analysis/evidence/facts.rs
    - crates/polint/src/analysis/evidence/validate.rs
  </read_first>
  <action>Add debug/validation rows for evidence graph size, selected path count, omitted count, summary expansion keys, replay keys, unknown reasons, extension verdicts, and budget caps. Keep row ordering deterministic and use compact reason codes plus short messages.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis_kernel::validation --locked</automated>
  </verify>
  <acceptance_criteria>
    - Debug output identifies exact, partial, unknown, summary-backed, extension-backed, and budget-limited evidence cases.
    - Validation rows include enough replay keys to reproduce selected paths through internal query helpers.
    - Debug/validation rows do not expose raw source text or absolute paths.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add native evidence eval fixture suite</name>
  <files>crates/polint/src/eval/model.rs, crates/polint/src/eval/observed.rs, crates/polint/src/eval/fixtures.rs, tests/eval-fixtures/evidence/</files>
  <read_first>
    - crates/polint/src/eval/model.rs
    - crates/polint/src/eval/observed.rs
    - crates/polint/src/eval/fixtures.rs
    - research/program-slicing-evidence/VALIDATION.md
  </read_first>
  <action>Add eval fixtures for local dependence, thin/full slices, interprocedural call stacks, summaries, chops, ranked paths, compact limits, uncertainty, and SARIF/JSON evidence projections. The fixtures should assert observed evidence rows rather than only snapshotting free-form output.</action>
  <verify>
    <automated>cargo test -p polint --lib eval_native_fixture_runner_evidence_fixture_passes --locked</automated>
    <automated>cargo test -p polint --lib eval_evidence_manifests_cover_required_taxonomy --locked</automated>
  </verify>
  <acceptance_criteria>
    - Fixture manifests cover all Phase 39 success-criteria categories from ROADMAP.md.
    - Fixture assertions check provenance, uncertainty, ranking, hidden counts, replay keys, and bounded output.
    - Repeated fixture runs produce byte-stable observed JSON for evidence rows.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add extension evidence fixture coverage</name>
  <files>crates/polint/src/eval/model.rs, crates/polint/src/eval/observed.rs, tests/eval-fixtures/evidence/</files>
  <read_first>
    - crates/polint/src/analysis/evidence/validate.rs
    - crates/polint/src/eval/model.rs
    - crates/polint/src/eval/observed.rs
    - tests/eval-fixtures/
  </read_first>
  <action>Add fixtures that demonstrate accepted extension evidence, precision downgrade, candidate-only evidence, rejected invalid evidence, and native may-edge preservation. Assert both native and extension-augmented observed rows where applicable.</action>
  <verify>
    <automated>cargo test -p polint --lib eval_evidence_extension_fixture_passes --locked</automated>
  </verify>
  <acceptance_criteria>
    - Extension fixtures prove invalid rows cannot suppress native conservative evidence.
    - Downgrade fixtures show exact claims becoming may/partial when validation is insufficient.
    - Accepted extension fixtures show deterministic provenance and merge verdict rows.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Prove evidence internals stay out of public rule API</name>
  <files>crates/polint/tests/cli.rs, docs/facts/evidence.md</files>
  <read_first>
    - crates/polint/src/sdk/mod.rs
    - crates/polint/src/sdk/prelude.rs
    - examples/
    - docs/API-VISIBILITY-PLAN.md
  </read_first>
  <action>Add a public-boundary regression that fails if internal evidence modules are re-exported through `polint::sdk`, `polint::sdk::prelude`, examples, or generated rule-authoring paths. If user-facing docs mention evidence, phrase it as diagnostic/reporting evidence and mark public fact views as future work unless implemented by a later phase.</action>
  <verify>
    <automated>cargo test -p polint --test cli evidence_public_no_leak --locked</automated>
    <automated>cargo test --workspace --locked</automated>
  </verify>
  <acceptance_criteria>
    - Repo-local rule examples still use only supported public SDK imports.
    - Evidence internals are not exported as supported fact views or prelude items in this phase.
    - Any documentation added for evidence accurately states limits and does not promise exact coverage.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evidence debug -> user output | Debug/eval must explain evidence without leaking sensitive source bodies or host-local paths. |
| internal evidence -> SDK surface | Evidence implementation details must not become accidental public API. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T39-15 | Repudiation | eval coverage | Mitigated | Require manifest coverage for each ROADMAP success-criteria category. |
| T39-16 | Information Disclosure | debug/eval output | Mitigated | Add negative assertions for raw source text, absolute paths, timestamps, and parser ids. |
| T39-17 | Elevation of Privilege | public SDK boundary | Mitigated | Add regression that evidence internals are not re-exported through supported rule-authoring APIs. |
</threat_model>

<verification>
- `cargo fmt --all --check`
- `cargo test -p polint --lib eval_native_fixture_runner_evidence_fixture_passes --locked`
- `cargo test -p polint --lib eval_evidence_extension_fixture_passes --locked`
- `cargo test -p polint --lib eval_evidence_manifests_cover_required_taxonomy --locked`
- `cargo test -p polint --test cli evidence_public_no_leak --locked`
- `cargo test --workspace --locked`
- `cargo clippy --workspace --all-targets --locked -- -D warnings`
</verification>

<success_criteria>
Phase 39 is demonstrably complete through deterministic fixtures, evidence debug rows, public-boundary tests, workspace tests, and clippy.
</success_criteria>
