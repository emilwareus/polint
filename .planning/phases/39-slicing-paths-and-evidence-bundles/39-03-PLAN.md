---
phase: 39-slicing-paths-and-evidence-bundles
plan: 03
title: Bounded Path Chop And Ranking Queries
type: execute
wave: 3
depends_on: [39-02]
files_modified:
  - crates/polint/src/analysis/evidence/query.rs
  - crates/polint/src/analysis/evidence/rank.rs
  - crates/polint/src/analysis/slicing/paths.rs
  - crates/polint/src/analysis/slicing/mod.rs
autonomous: true
requirements: [SAE-PREC-04]
must_haves:
  truths:
    - "D-09: Source-to-sink path search is bounded and deterministic from the beginning, using shortest/bounded k-path extraction without unbounded enumeration."
    - "D-10: Path ranking is display-only and prefers native/exact evidence, fewer unknowns, fewer unvalidated/model/heuristic edges, shorter paths, non-opaque summaries, and direct source/sink relevance without changing solver truth."
    - "Chop queries intersect source-forward and sink-backward reachability without unbounded all-pairs materialization."
  artifacts:
    - path: "crates/polint/src/analysis/slicing/paths.rs"
      provides: "Bounded path and chop query implementation"
    - path: "crates/polint/src/analysis/evidence/rank.rs"
      provides: "Deterministic path ranking"
---

<objective>
Add bounded source-to-sink path, chop, and deterministic ranking queries over the evidence graph.
</objective>

<execution_context>
@.planning/phases/39-slicing-paths-and-evidence-bundles/39-CONTEXT.md
@.planning/phases/39-slicing-paths-and-evidence-bundles/39-PATTERNS.md
@.agents/skills/rust-best-practices/SKILL.md
@AGENTS.md
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement bounded path and chop query shapes</name>
  <files>crates/polint/src/analysis/slicing/paths.rs, crates/polint/src/analysis/slicing/mod.rs, crates/polint/src/analysis/evidence/query.rs</files>
  <read_first>
    - crates/polint/src/analysis/data_flow/query.rs
    - crates/polint/src/analysis/slicing/local.rs
    - crates/polint/src/analysis/evidence/facts.rs
    - research/program-slicing-evidence/RECOMMENDED_IMPLEMENTATION.md
  </read_first>
  <action>Add crate-private `PathQuery`, `PathMode`, `PathBudget`, `PathResult`, `EvidencePath`, and `ChopQuery` or exact equivalents. Implement bounded BFS for local/unweighted paths, bounded k-path extraction with `max_paths`, `max_nodes`, `max_edges`, and `max_depth`, and chop queries by intersecting source-forward and sink-backward reachable evidence node sets. Every truncated result must include omitted-region metadata and `BudgetExceeded` or equivalent status.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::slicing::paths --locked</automated>
  </verify>
  <acceptance_criteria>
    - A test proves source-to-sink path search returns a bounded path for a direct local flow.
    - A test proves multiple possible paths respect `max_paths` and produce deterministic ordering.
    - A test proves budget truncation creates omitted-region metadata and a budget/partial status.
    - A test proves chop output excludes nodes not in the forward/backward intersection.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add deterministic path ranking</name>
  <files>crates/polint/src/analysis/evidence/rank.rs, crates/polint/src/analysis/slicing/paths.rs</files>
  <read_first>
    - .planning/phases/39-slicing-paths-and-evidence-bundles/39-CONTEXT.md
    - research/program-slicing-evidence/FINAL-REPORT.md
    - crates/polint/src/analysis/evidence/facts.rs
  </read_first>
  <action>Create a deterministic ranker that scores paths using concrete components: native/exact edge bonus, unknown penalty, unvalidated model penalty, heuristic penalty, path length penalty, summary opaque penalty, and direct source-sink bonus. Use stable-key tie-breaks so shuffled input order does not change ranked output. Ranking must affect display order only and must not drop solver-visible paths except through explicit path budget limits.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::evidence::rank --locked</automated>
  </verify>
  <acceptance_criteria>
    - Ranking tests prove exact/native shorter paths sort before unknown/heuristic/model-heavy alternatives.
    - Ranking tests prove identical score paths use stable-key tie-breaks independent of insertion order.
    - Ranking does not mutate path status, precision, provenance, or solver reachability.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ranking -> truth | Ranking must not change solver truth or hide lower-ranked paths without explicit budget truncation. |
| path budget -> output | Truncated path search must be visibly partial. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T39-06 | Tampering | path ranking | Mitigated | Keep ranking as display metadata and verify path status/provenance are unchanged. |
| T39-07 | Denial of Service | path enumeration | Mitigated | Enforce max paths/nodes/edges/depth and emit budget rows. |
</threat_model>

<verification>
- `cargo test -p polint --lib analysis::slicing::paths --locked`
- `cargo test -p polint --lib analysis::evidence::rank --locked`
</verification>

<success_criteria>
Evidence path and chop queries are bounded, deterministic, ranked, and explicit about truncation.
</success_criteria>
