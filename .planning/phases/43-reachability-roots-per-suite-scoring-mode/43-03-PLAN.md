---
phase: 43-reachability-roots-per-suite-scoring-mode
plan: 03
type: execute
wave: 3
depends_on: ["43-01", "43-02"]
files_modified:
  - crates/polint/src/eval/report.rs
  - crates/polint/src/eval/observed.rs
  - crates/polint/src/eval/determinism_gate.rs
  - crates/polint/src/eval/mod.rs
  - tests/eval-fixtures/determinism/go_reachable/repo
  - tests/eval-fixtures/determinism/go_reachable/.polint.toml
  - tests/eval-fixtures/determinism/go_reachable/expected.polint-eval.toml
  - tests/eval-fixtures/determinism/ts_reachable/repo
  - tests/eval-fixtures/determinism/ts_reachable/.polint.toml
  - tests/eval-fixtures/determinism/ts_reachable/expected.polint-eval.toml
  - .github/workflows/ci.yml
autonomous: true
requirements: [REACH-03]
must_haves:
  truths:
    - "Decisions implemented by this plan (traceability) — D-20, D-21, D-22, D-23, D-24, D-25: the N=10 byte-identical determinism gate, the shuffled provider-execution/row-insertion permutation surface, provider_manifests()-driven near-zero-maintenance inheritance, reserved solver_step_count/budget_exceeded_reasons on a #[serde(default)] MetricSections section (not the frozen MetricSummary), Go+TS determinism fixtures with at least one unreachable call, fast-CI Linux+macOS independent passes, and the documented phases 44-54 per-phase inheritance obligation"
    - "the determinism gate runs the eval observation 10 times under seeded distinct provider-order/row-order permutations and asserts byte-identical normalized observed JSON"
    - "reserved solver_step_count (default 0) and budget_exceeded_reasons (default empty) live on a new #[serde(default)] MetricSections section, NOT on the frozen MetricSummary"
    - "Go and TS/JS determinism fixtures each have roots + direct calls + at least one unreachable call, exercising reachable-graph marking inside the gate"
    - "the gate harness is driven by provider_manifests() so phases 44-54 auto-enroll new providers with no per-phase harness edit"
    - "the gate runs in fast CI on Linux + macOS, both passing independently (no averaging)"
  artifacts:
    - path: "crates/polint/src/eval/determinism_gate.rs"
      provides: "N=10 seeded-permutation byte-identical harness driven by provider_manifests() + per-phase inheritance doc"
      contains: "provider_manifests"
    - path: "crates/polint/src/eval/report.rs"
      provides: "SolverMetricSection reserved fields on MetricSections via #[serde(default)]"
      contains: "solver_step_count"
    - path: ".github/workflows/ci.yml"
      provides: "determinism-gate job on ubuntu-latest + macos-latest, fail-fast false"
      contains: "determinism"
  key_links:
    - from: "crates/polint/src/eval/determinism_gate.rs"
      to: "analysis_kernel::provider::provider_manifests"
      via: "parametric provider-set enumeration so future solver providers auto-enroll"
      pattern: "provider_manifests"
    - from: "crates/polint/src/eval/report.rs"
      to: "MetricSections.solver"
      via: "#[serde(default)] reserved section sibling of categorized_failures"
      pattern: "solver_step_count|budget_exceeded_reasons"
---

<objective>
Reserve the `solver_step_count` / `budget_exceeded_reasons` JSON shape now (defaulted, on a new `#[serde(default)]` `MetricSections` section — never on the frozen `MetricSummary`), build the parametric determinism-gate harness (10 seeded provider-order/row-order permutations -> byte-identical normalized observed JSON, driven by `provider_manifests()`), add Go + TS/JS determinism fixtures that exercise reachable-graph marking, wire a fast-CI Linux+macOS determinism-gate job, and document the per-phase inheritance obligation for phases 44-54.

Purpose: REACH-03. The gate's value is INHERITANCE — driving it off `provider_manifests()` means phases 44-54 get coverage for free; a hand-maintained provider list would rot and the "inherited by every subsequent solver phase" requirement would quietly break. Reserving the solver fields now keeps the byte-identical gate stable across the whole milestone when Phase 47+ populates them.
Output: reserved `SolverMetricSection`, `eval/determinism_gate.rs`, 2 determinism fixtures, a CI job, and the documented inheritance convention.
</objective>

<execution_context>
@/Users/emilwareus/conductor/workspaces/exlint/islamabad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/conductor/workspaces/exlint/islamabad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-reachability-roots-per-suite-scoring-mode/43-CONTEXT.md
@.planning/phases/43-reachability-roots-per-suite-scoring-mode/43-PATTERNS.md
@.planning/phases/43-reachability-roots-per-suite-scoring-mode/43-01-SUMMARY.md
@.planning/phases/43-reachability-roots-per-suite-scoring-mode/43-02-SUMMARY.md
@AGENTS.md

<interfaces>
<!-- From crates/polint/src/eval/report.rs — frozen-MetricSummary discipline. -->
- `MetricSummary` is layout-locked by a destructure test; extensions live on `MetricSections`, NOT MetricSummary.
- `MetricSections { scanner, graph, paths, unknowns, performance, suite_native, adaptation, jelly_oracle_coverage, categorized_failures }` — add a `#[serde(default)] solver: SolverMetricSection` sibling.
- `CategorizedFailureSection` derive shape: `#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq, Eq)] #[serde(rename_all = "snake_case", deny_unknown_fields)]` — copy for `SolverMetricSection`.

<!-- Parametric source the gate is driven by. From crates/polint/src/analysis_kernel/provider.rs -->
- `provider_manifests() -> &'static [ProviderManifest]` and `#[cfg(test)] provider_order_for_test() -> Vec<&'static str>`.

<!-- The gate generalizes these existing byte-identity proofs. -->
- `eval/observed.rs::provider_order_invariants()` iterates `AnalysisKernel::provider_manifests()` and projects deterministic observed rows.
- `eval/report.rs::eval_report_normalization_makes_json_order_independent` + `deterministic_output_hash`.
- `analysis/domains/solver.rs::deterministic_shuffled_rows_produce_byte_identical_result_digests` (single-provider seeded-shuffle precedent).

<!-- CI analog. From .github/workflows/ci.yml leak-gate job (lines 112-133): matrix [ubuntu-latest, macos-latest], fail-fast: false, no averaging. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reserve solver_step_count / budget_exceeded_reasons on a new MetricSections section</name>
  <files>crates/polint/src/eval/report.rs, crates/polint/src/eval/observed.rs</files>
  <read_first>
    - crates/polint/src/eval/report.rs (lines 64-94 frozen MetricSummary; 96-112 MetricSections with #[serde(default)] siblings; 144-152 CategorizedFailureSection derive shape to copy; 723-758 metric_summary_layout_unchanged destructure layout-lock test; 760-766 normalization order-independence test)
    - crates/polint/src/eval/observed.rs (where observed JSON is assembled — the reserved fields must surface in the observed/report JSON, defaulted)
    - .planning/phases/43-reachability-roots-per-suite-scoring-mode/43-PATTERNS.md (section "eval/report.rs + eval/observed.rs" — D-23: NOT on MetricSummary)
  </read_first>
  <behavior>
    - Test: a fresh `SolverMetricSection::default()` serializes with `solver_step_count = 0` and `budget_exceeded_reasons = []`.
    - Test: older v1.2 report JSON lacking the `solver` section still deserializes (the `#[serde(default)]` on the MetricSections field preserves backward compatibility — Pattern M).
    - Test: a destructure layout-lock test for `SolverMetricSection` (mirroring metric_summary_layout_unchanged) names exactly `solver_step_count` and `budget_exceeded_reasons` — adding a field forces the test to update.
    - Test: `MetricSummary` destructure layout-lock test is UNCHANGED (the new fields did NOT go on MetricSummary).
  </behavior>
  <action>
    In `eval/report.rs`, define `#[derive(Serialize, Deserialize, Clone, Debug, Default, PartialEq, Eq)] #[serde(rename_all = "snake_case", deny_unknown_fields)] pub(crate) struct SolverMetricSection { solver_step_count: u64, budget_exceeded_reasons: Vec<String> }` copying the `CategorizedFailureSection` derive shape (report.rs:144-152). Add it to `MetricSections` as a `#[serde(default)] pub(crate) solver: SolverMetricSection` sibling of `jelly_oracle_coverage`/`categorized_failures` (report.rs:108-111) — D-23 is explicit that these reserved fields live on `MetricSections`, NOT on the frozen `MetricSummary` (which is layout-locked by the destructure test at report.rs:723-758). Add a doc comment stating these are RESERVED for the solver introduced in Phase 47+, defaulted to zero/empty in Phase 43 so the byte-identical determinism gate stays stable across the milestone (D-23).

    In `eval/observed.rs`, ensure the observed/report JSON surfaces the defaulted `solver` section so byte-identity of the full normalized observed JSON transitively covers solver step counts (b) and budget reasons (c) once Phase 47+ populates them (D-20).

    Add the `SolverMetricSection` destructure layout-lock test mirroring `metric_summary_layout_unchanged`, the default-values test, and the backward-compat deserialize test from `<behavior>`. Do NOT modify the `MetricSummary` field list or its layout-lock test.
  </action>
  <verify>
    <automated>cargo test -p polint eval::report 2>&1 | tail -25 && cargo test -p polint eval::observed 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `SolverMetricSection` exists with exactly `solver_step_count: u64` and `budget_exceeded_reasons: Vec<String>` and is a `#[serde(default)]` field on `MetricSections` (`grep -c 'solver: SolverMetricSection' crates/polint/src/eval/report.rs` >= 1).
    - `MetricSummary` field list is byte-unchanged (no field added inside the `struct MetricSummary` block; `solver_step_count` does not appear inside that block).
    - The default-values test, the backward-compat (older JSON without `solver`) deserialize test, and the new section's destructure layout-lock test all pass.
    - `metric_summary_layout_unchanged` is unmodified and still passes.
  </acceptance_criteria>
  <done>Reserved solver fields are on a defaulted MetricSections section, MetricSummary is untouched, older JSON still deserializes, and both layout-lock tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parametric determinism-gate harness + Go/TS fixtures</name>
  <files>crates/polint/src/eval/determinism_gate.rs, crates/polint/src/eval/mod.rs, tests/eval-fixtures/determinism/go_reachable/repo, tests/eval-fixtures/determinism/go_reachable/.polint.toml, tests/eval-fixtures/determinism/go_reachable/expected.polint-eval.toml, tests/eval-fixtures/determinism/ts_reachable/repo, tests/eval-fixtures/determinism/ts_reachable/.polint.toml, tests/eval-fixtures/determinism/ts_reachable/expected.polint-eval.toml</files>
  <read_first>
    - crates/polint/src/analysis_kernel/provider.rs (lines 84-94 provider_manifests/provider_order_for_test — the parametric source)
    - crates/polint/src/eval/observed.rs (lines 484-500 provider_order_invariants iterating provider_manifests — the iteration the gate copies)
    - crates/polint/src/eval/report.rs (lines 760-766 eval_report_normalization_makes_json_order_independent + deterministic_output_hash — the byte-identity proof the gate generalizes to N=10)
    - crates/polint/src/analysis/domains/solver.rs (line 435 deterministic_shuffled_rows_produce_byte_identical_result_digests — single-provider seeded-shuffle precedent)
    - crates/polint/src/eval/mod.rs (lines 9-26 pub(crate) mod list — register determinism_gate)
    - tests/eval-fixtures/identity/dedup/ (structural precedent: repo/ + .polint.toml + expected.polint-eval.toml)
    - .planning/phases/43-reachability-roots-per-suite-scoring-mode/43-PATTERNS.md (section "eval/determinism_gate.rs" — D-20..D-25; "No Analog Found" notes the N=10 harness is net-new but assembled from existing pieces)
  </read_first>
  <behavior>
    - Test: running the eval observation N=10 times under distinct SEEDED permutations of (1) provider execution order where the DAG allows and (2) provider output-row / fact-insertion order produces byte-identical normalized observed JSON across all 10 runs (D-20/D-21).
    - Test: the permutation seeds are distinct (the 10 permutations are not all the identity permutation).
    - Test: the gate's provider set is sourced from `provider_manifests()` (assert the shuffled-provider count equals `provider_manifests().len()`), so a newly-registered provider auto-enrolls without a harness edit (D-22).
    - Test: the Go fixture and the TS/JS fixture each produce at least one root, at least one direct call, and at least one `CallReachabilityFact` with `in_reachable_graph = false` (the gate exercises reachable-graph marking, D-24).
  </behavior>
  <action>
    Create `crates/polint/src/eval/determinism_gate.rs` (`pub(crate)`/test-facing) and register `pub(crate) mod determinism_gate;` in `eval/mod.rs:9-26`. Implement the FIXED contract (D-20/D-21): run the eval observation N=10 times under seeded DISTINCT permutations of (1) provider execution order wherever the dependency DAG allows independent reordering and (2) provider output-row / input fact-insertion order (re-inserting facts in permuted order); assert byte-identical normalized observed JSON across all 10. Drive the provider set off `analysis_kernel::provider::provider_manifests()` (copy the iteration shape from `eval/observed.rs::provider_order_invariants` at observed.rs:484-500) so phases 44-54 auto-enroll — NEVER hand-maintain a provider list (D-22, near-zero-maintenance). Generalize the existing single-run order-independence proof (`eval/report.rs::eval_report_normalization_makes_json_order_independent`) and the single-provider seeded-shuffle precedent (`domains/solver.rs:435`) to the N=10 multi-provider case.

    Add a top-of-file doc comment recording the D-25 per-phase obligation: every subsequent solver-introducing phase (44-54) MUST (a) ensure its new provider is part of the shuffled set (automatic via D-22) and (b) keep the determinism-gate fixture green as a NAMED acceptance gate in its verification — this is the REACH-03 "inherited by every subsequent solver phase" requirement.

    Add two fixtures under `tests/eval-fixtures/determinism/` following the `tests/eval-fixtures/identity/dedup/` structure (`repo/` + `.polint.toml` + `expected.polint-eval.toml`):
    - `go_reachable/`: a Go repo with `func main` (root), at least one direct call from main to a reachable function, AND at least one function with a direct call that is NOT reachable from main (so a `CallReachabilityFact` with `in_reachable_graph = false` is produced).
    - `ts_reachable/`: a TS/JS repo with an exported function (root via `Exported`, or a configured root via `[reachability] roots`), at least one direct call reachable from it, AND at least one unreachable direct call.
    Each `.polint.toml` may set `[reachability] roots` if needed to seed roots. The `expected.polint-eval.toml` captures the deterministic observed snapshot. Add the gate test that consumes both fixtures and asserts the N=10 byte-identity + the per-fixture root/call/unreachable-mark invariants.
  </action>
  <verify>
    <automated>cargo test -p polint eval::determinism_gate 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `eval/determinism_gate.rs` exists, is registered in `eval/mod.rs`, and drives its provider set from `provider_manifests()` (`grep -c 'provider_manifests' crates/polint/src/eval/determinism_gate.rs` >= 1; no hand-written provider-name array — `grep -c '"polint\.' crates/polint/src/eval/determinism_gate.rs` == 0).
    - The N=10 seeded-permutation byte-identity test passes for both fixtures; the distinct-seeds test passes; the shuffled-provider-count-equals-manifest-count test passes (D-22 auto-enrollment proof).
    - Both `tests/eval-fixtures/determinism/{go_reachable,ts_reachable}/` exist with `repo/` + `.polint.toml` + `expected.polint-eval.toml`; each fixture yields >= 1 root, >= 1 direct call, and >= 1 `in_reachable_graph = false` mark.
    - The D-25 per-phase inheritance obligation is documented in the gate file (`grep -c 'inherit' crates/polint/src/eval/determinism_gate.rs` >= 1 and the comment names phases 44-54).
  </acceptance_criteria>
  <done>The N=10 parametric determinism gate passes byte-identically over Go + TS/JS fixtures, is driven by provider_manifests() for auto-enrollment, and documents the phases 44-54 inheritance obligation.</done>
</task>

<task type="auto">
  <name>Task 3: Fast-CI Linux + macOS determinism-gate job</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (lines 112-133 leak-gate job — matrix [ubuntu-latest, macos-latest], fail-fast: false, no averaging — the exact analog to copy)
    - crates/polint/src/eval/determinism_gate.rs (the test name/path the CI job invokes)
    - .planning/phases/43-reachability-roots-per-suite-scoring-mode/43-PATTERNS.md (D-24 cross-platform contract)
  </read_first>
  <action>
    Add a `determinism-gate` job to `.github/workflows/ci.yml` modeled exactly on the existing `leak-gate` job (lines 112-133): `strategy.fail-fast: false`, `matrix.os: [ubuntu-latest, macos-latest]`, `runs-on: ${{ matrix.os }}`, checkout + `dtolnay/rust-toolchain@stable` + `Swatinem/rust-cache@v2`, then a step running the determinism gate test (e.g. `cargo test -p polint --lib eval::determinism_gate --locked`, matching the actual test path from Task 2). Both platforms must pass INDEPENDENTLY — no cross-platform averaging, consistent with the Phase 42 leak-gate contract (D-24). Add a comment stating phases 44-54 inherit this gate as a precondition (mirroring the leak-gate comment at ci.yml:113-117). This is a fast-CI (PR-blocking) job, not nightly.
  </action>
  <verify>
    <automated>cd /Users/emilwareus/conductor/workspaces/exlint/nagoya && python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/ci.yml')); j=d['jobs']['determinism-gate']; assert j['strategy']['fail-fast'] is False; assert set(j['strategy']['matrix']['os'])=={'ubuntu-latest','macos-latest'}; print('determinism-gate job ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` has a `determinism-gate` job with `fail-fast: false` and `matrix.os = [ubuntu-latest, macos-latest]` (the python yaml assertion passes).
    - The job step invokes the determinism gate test via cargo, matching the Task 2 test path.
    - A comment in the job states phases 44-54 inherit the gate (`grep -c '44' .github/workflows/ci.yml` >= 1 in the new job block).
    - The existing `leak-gate` job is unchanged.
  </acceptance_criteria>
  <done>A fast-CI determinism-gate job runs on Linux + macOS independently with no averaging and documents the phases 44-54 inheritance precondition.</done>
</task>

<task type="auto">
  <name>Task 4: Public-surface-leak gate stays green</name>
  <files>crates/polint/tests/public_surface_leak.rs</files>
  <read_first>
    - crates/polint/tests/public_surface_leak.rs (the v1.3 leak gate; ALLOWED_PRELUDE list)
    - AGENTS.md (Public API and visibility conventions — do NOT extend ALLOWED_PRELUDE)
  </read_first>
  <action>
    Confirm `SolverMetricSection`, the determinism-gate harness, and any new helpers are `pub(crate)`/test-facing (no bare `pub`). Run the existing leak gate WITHOUT extending `ALLOWED_PRELUDE` (locked 97-entry list from Phase 42). If `unreachable_pub` fires, fix it by tightening visibility to `pub(crate)` per the AGENTS.md convention — never by relaxing the lint. This task is a verification gate.
  </action>
  <verify>
    <automated>cargo test -p polint --test public_surface_leak 2>&1 | tail -15 && cargo clippy -p polint 2>&1 | grep -c 'unreachable_pub' || true</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p polint --test public_surface_leak` passes.
    - `ALLOWED_PRELUDE` is byte-unchanged from Phase 42 (no list entries added).
    - `cargo clippy -p polint` reports zero `unreachable_pub` warnings for the eval determinism/report additions.
  </acceptance_criteria>
  <done>Leak gate green, ALLOWED_PRELUDE unchanged, SolverMetricSection and the gate harness stay pub(crate)/test-facing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| determinism fixtures -> eval observation | Fixture `repo/` + `.polint.toml` are checked-in test inputs (trusted), copied into a temp dir before the kernel runs (existing eval fixture copy already rejects symlink escape). |
| reserved solver JSON fields -> downstream JSON consumers | Adding fields to the report JSON could break older consumers; the `#[serde(default)]` discipline must keep older v1.2 JSON deserializable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-03-01 | Tampering | non-determinism slipping past the gate | mitigate | The N=10 seeded-permutation byte-identity assertion over `provider_manifests()`-driven shuffles is the gate itself; a regression in any provider's determinism fails the gate on Linux AND macOS independently (Task 2/Task 3, D-20/D-24). |
| T-43-03-02 | Denial of Service / Compatibility | report JSON shape break for older consumers | mitigate | Reserved fields are added on a `#[serde(default)]` `MetricSections` section, never on the frozen `MetricSummary`; the Task 1 backward-compat test proves older JSON still deserializes (D-23). |
| T-43-03-03 | Tampering | determinism fixtures reading outside the workspace | mitigate | Fixtures are copied into a temp dir before `AnalysisKernel::run` via the existing eval native-fixture adapter that rejects symlink escape (Phase 22 decision); no new file-read surface is introduced. |
| T-43-03-04 | Information Disclosure | gate harness / SolverMetricSection leaking to public SDK | mitigate | All additions are `pub(crate)`/test-facing; the Task 4 leak gate proves zero new public types; ALLOWED_PRELUDE unchanged. |
| T-43-03-SC | Tampering | npm/pip/cargo installs | accept | No new package-manager installs (intra-crate Rust + fixtures + CI YAML; no `Cargo.toml` dependency changes). Seeded shuffling uses the existing in-tree RNG/seed approach (mirror domains/solver.rs:435); if a new dependency is required, stop and add a legitimacy checkpoint first. |
</threat_model>

<verification>
- `cargo test -p polint eval::report eval::observed eval::determinism_gate` all green.
- The N=10 byte-identity gate passes over Go + TS/JS fixtures on the local machine.
- `determinism-gate` CI job present with Linux + macOS matrix, fail-fast false (python yaml assertion passes).
- `cargo test -p polint --test public_surface_leak` stays green; ALLOWED_PRELUDE unchanged.
- MetricSummary layout-lock test unchanged and passing; older JSON deserializes.
</verification>

<success_criteria>
- ROADMAP success criterion 3 met: determinism gate fixture passes — 10 shuffled provider-order runs produce byte-identical observed JSON, identical solver step counts (reserved, default 0), and identical budget-exceeded reasons (reserved, default empty).
- ROADMAP success criterion 4 met: the gate is wired (driven by `provider_manifests()`) so every subsequent solver-introducing phase inherits it as an acceptance gate, documented in the gate file and CI job.
</success_criteria>

<output>
Create `.planning/phases/43-reachability-roots-per-suite-scoring-mode/43-03-SUMMARY.md` when done.
</output>
