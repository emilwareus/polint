---
phase: 48-go-rta-driver
plan: 03
type: execute
wave: 3
depends_on: [48-02]
files_modified:
  - tests/eval-fixtures/go-rta/iteration-cap/expected.polint-eval.toml
  - tests/eval-fixtures/go-rta/iteration-cap/repo/go.mod
  - tests/eval-fixtures/go-rta/iteration-cap/repo/main.go
  - tests/eval-fixtures/go-rta/interface-dispatch/expected.polint-eval.toml
  - tests/eval-fixtures/go-rta/interface-dispatch/repo/go.mod
  - tests/eval-fixtures/go-rta/interface-dispatch/repo/main.go
  - tests/eval-fixtures/go-rta/address-taken/expected.polint-eval.toml
  - tests/eval-fixtures/go-rta/address-taken/repo/go.mod
  - tests/eval-fixtures/go-rta/address-taken/repo/main.go
  - tests/eval-fixtures/determinism/go_rta/expected.polint-eval.toml
  - tests/eval-fixtures/determinism/go_rta/repo/go.mod
  - tests/eval-fixtures/determinism/go_rta/repo/main.go
  - tests/eval-fixtures/polyglot-canary/go-ts/expected.polint-eval.toml
  - tests/eval-fixtures/polyglot-canary/go-ts/repo/go.mod
  - tests/eval-fixtures/polyglot-canary/go-ts/repo/main.go
  - tests/eval-fixtures/polyglot-canary/go-ts/repo/tokens.ts
  - tests/eval-fixtures/polyglot-canary/go-ts/repo/package.json
  - tests/eval-fixtures/polyglot-canary/go-ts/repo/tsconfig.json
  - crates/polint/src/eval/go_rta.rs
  - crates/polint/src/eval/determinism_gate.rs
  - crates/polint/src/eval/mod.rs
autonomous: true
requirements: [GO-05]

must_haves:
  truths:
    - "An iteration-cap fixture whose interface-dispatch graph exceeds a tight RTA cap emits BudgetStatus::BudgetExceeded (observable in solver output / budget_exceeded_reasons), proving runaway dispatch is signalled, not dropped (GO-05 success criterion 2)."
    - "Native fixtures exercising interface dispatch, method values/closures (address-taken), and dynamic calls prove RTA produces the expected reachable-only Go edges and demonstrate the recall lift toward the 70-90% ceiling (GO-05 success criterion 4) (D-15)."
    - "A polyglot Go+TS canary fixture shows Go edges resolved by the RTA driver while TS behavior is unchanged (TsTokensPolicy still a stub) — no cross-language interference through the shared solver core (GO-05 success criterion 3)."
    - "The Phase 43 determinism gate stays green: a go_rta determinism fixture is byte-identical across 10 seeded provider-order/row-order permutations (D-17)."
    - "The Phase 42 public-surface-leak gate stays green: no go_rta type is reachable from polint::sdk::prelude::*; ALLOWED_PRELUDE is unchanged."
  artifacts:
    - path: "crates/polint/src/eval/go_rta.rs"
      provides: "crate-private test gate driving the go-rta native fixtures + asserting BudgetExceeded, reachable-only RTA edges, and polyglot non-regression"
      min_lines: 40
    - path: "tests/eval-fixtures/go-rta/iteration-cap/expected.polint-eval.toml"
      provides: "the BudgetExceeded acceptance artifact (D-14)"
      contains: "invariant"
    - path: "tests/eval-fixtures/polyglot-canary/go-ts/expected.polint-eval.toml"
      provides: "the polyglot Go+TS canary (D-16, created here; Phase 54 promotes to a hard gate)"
      contains: "invariant"
  key_links:
    - from: "crates/polint/src/eval/determinism_gate.rs"
      to: "tests/eval-fixtures/determinism/go_rta/"
      via: "assert_n10_byte_identical(fixture_dir(\"go_rta\")) + reachable-graph marking invariant"
      pattern: "go_rta"
    - from: "crates/polint/src/eval/go_rta.rs"
      to: "tests/eval-fixtures/go-rta/"
      via: "load_native_fixture + run_native_fixture_for_test driving the RTA fixtures"
      pattern: "go-rta|go_rta"
---

<objective>
Prove GO-05's four success criteria with fixtures and gates (CONTEXT D-14, D-15, D-16, D-17): the iteration-cap `BudgetExceeded` proof, native Go RTA fixtures (interface dispatch, method values/closures, dynamic calls) demonstrating the recall lift, the **polyglot Go+TS canary** (created here for the first time — it does not exist yet), and the inherited determinism + public-surface-leak gates staying green.

Purpose: This closes the phase. The hard per-suite precision floor (Go ≥60%) and the canary-as-a-hard-gate land in Phase 54 (BENCH-01); Phase 48 *adds* the canary and *demonstrates* the lift while holding precision honestly. `oracle-rta` scoring filters to reachable-from-roots edges (Phase 43 D-17), so getting the root seed/marking wiring right is load-bearing — backwards filtering silently misreads recall.

Output: native fixture trees under `tests/eval-fixtures/`, a crate-private `eval::go_rta` gate, a `determinism/go_rta` fixture wired into the inherited gate, and a verified-green `cargo test -p polint` + leak/determinism gates. No new public surface.
</objective>

<execution_context>
@/Users/emilwareus/conductor/workspaces/exlint/islamabad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/conductor/workspaces/exlint/islamabad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-go-rta-driver/48-CONTEXT.md
@.planning/phases/48-go-rta-driver/48-PATTERNS.md
@AGENTS.md
@.planning/phases/48-go-rta-driver/48-02-SUMMARY.md

<interfaces>
<!-- Exact in-repo contracts the executor needs. Use these directly — no exploration needed. -->

Native fixture structure (every fixture is `tests/eval-fixtures/<family>/<case>/`):
- `expected.polint-eval.toml` with `schema_version = "polint-eval-fixture-1"`, `case_id = "..."`, `area = "..."`, `[repo] path = "repo"`, `[budget] max_runtime_ms = 120000`, then `[[expected]]` rows.
- `repo/` subtree: `.polint.toml` (optional), `go.mod`, `*.go` (and for polyglot: `package.json`, `tsconfig.json`, `*.ts`).
- Invariant-style row (from framework-entrypoints/mixed-go-ts/expected.polint-eval.toml:22): `[[expected]] invariant = { name = "...", value = "true", mode = "exact" }`.

Native fixture harness (crates/polint/src/eval/fixtures.rs):
```
pub(crate) fn load_native_fixture(fixture_dir: &Path) -> anyhow::Result<NativeFixture>
pub(crate) fn run_native_fixture_for_test(...)            // runs a fixture's repo through AnalysisKernel and compares observed vs expected
pub(crate) fn evaluation_run_for_fixture_with_observed_for_test(...)  // returns observed invariants for custom assertions
// per-area precedent gates already exist: run_semantic_mir_core_fixture_for_test, run_cfg_core_fixture_for_test, etc.
```

Per-area gate precedent (crates/polint/src/eval/semantic_graph_snapshot.rs):
```
fn fixture_dir(name) -> PathBuf { ...join("../../tests/eval-fixtures/semantic-graph").join(name) }
fn run_fixture_kernel(name) -> KernelOutput { let fixture = load_native_fixture(&fixture_dir(name)).expect(..); ... }
fn provider_output_digest(output, provider_id) -> Digest
// asserts each kind emitted + byte-stable across two runs
```

Determinism gate (crates/polint/src/eval/determinism_gate.rs) — auto-enrolls via provider_manifests(); fixtures live in tests/eval-fixtures/determinism/:
```
fn fixture_dir(name) -> PathBuf { ...join("../../tests/eval-fixtures/determinism").join(name) }
fn assert_n10_byte_identical(&fixture_dir(name))                 // 10 seeded provider-order/row-order permutations -> byte-identical observed JSON
fn assert_reachability_marking_exercised(&fixture_dir(name), name)  // >=1 root, >=1 call site, >=1 in_reachable_graph=false
// existing: go_reachable_fixture_is_byte_identical_under_ten_seeded_permutations (determinism_gate.rs:270), go_reachable_fixture_exercises_reachable_graph_marking (:280)
```

x/tools RTA suite + adapter (D-15):
- research/evaluation-harness/suites/go-x-tools-rta-callgraph.toml — `scoring_mode = "oracle-rta"`, adapter_id "go_x_tools_rta_callgraph", checkout path research/evaluation-harness/repos/golang-tools (ignored_by_git; may be ABSENT locally).
- crates/polint/src/eval/external/go_x_tools_callgraph.rs — `enumerate_go_x_tools_callgraph_cases` returns a `limitations` note + empty cases when the local clone is absent (go_x_tools_callgraph.rs:171-186); `normalize_kernel_output` (#[cfg(test)], :92-129) projects kernel edges to `go.rta.call_graph.{graph_kind}` oracle keys via the identity renderer; `suite_native_metrics` emits case_count/expected_edge_count.
- The oracle-rta scoring filters scored edges to reachable-from-roots (Phase 43 D-17) — the marking comes from `analysis::reachability::traverse` over `db.call_targets()`; the RTA-derived edges live in `db.solver_derived_edges()` and feed the call-graph oracle projection.

Reachable marking contract (Phase 43 D-18; crates/polint/src/analysis/reachability/traverse.rs):
```
compute_reachable_set(db, roots) -> ReachableSet   // BFS over resolved direct-call edges (db.call_targets())
mark_call_reachability(db, roots) -> Vec<CallReachabilityFact>  // in_reachable_graph = reachable.contains(site.caller)
// CallReachabilityReason::{RootSeed, ReachableFromRoot, OutsideReachableGraph}
```

Leak gate (crates/polint/tests/public_surface_leak.rs) — ALLOWED_PRELUDE is a locked snapshot (97 entries); adding ANY new public prelude entry fails the gate. All go_rta types are pub(crate) (D-17), so this gate must stay GREEN with ZERO edits to ALLOWED_PRELUDE.

go.mod baseline (existing fixtures use module names like `example.com/...` and the repo's Go version). Copy the go.mod + go version from tests/eval-fixtures/framework-entrypoints/mixed-go-ts/repo/go.mod (proven to load).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Iteration-cap + interface-dispatch + address-taken native Go RTA fixtures + the eval::go_rta gate</name>
  <files>tests/eval-fixtures/go-rta/iteration-cap/ (expected.polint-eval.toml + repo/go.mod + repo/main.go + repo/.polint.toml), tests/eval-fixtures/go-rta/interface-dispatch/ (+repo/), tests/eval-fixtures/go-rta/address-taken/ (+repo/), crates/polint/src/eval/go_rta.rs, crates/polint/src/eval/mod.rs</files>
  <read_first>
    - tests/eval-fixtures/determinism/go_reachable/expected.polint-eval.toml (the minimal order-independent manifest template) + tests/eval-fixtures/determinism/go_reachable/repo/ (the Go repo precedent)
    - tests/eval-fixtures/framework-entrypoints/mixed-go-ts/repo/go.mod + main.go (a proven-loading Go repo with go.mod + go version)
    - crates/polint/src/eval/fixtures.rs (load_native_fixture, run_native_fixture_for_test, evaluation_run_for_fixture_with_observed_for_test — the reusable harness this gate calls)
    - crates/polint/src/eval/semantic_graph_snapshot.rs (the per-area gate pattern: fixture_dir + run_fixture_kernel + byte-stable assertions to mirror in eval::go_rta)
    - crates/polint/src/eval/mod.rs (where eval submodules are declared — add `mod go_rta;` / `#[cfg(test)] mod go_rta;` consistent with how semantic_graph_snapshot is declared)
    - crates/polint/src/analysis/solver/provider.rs (budget_exceeded_diagnostic + the solver output; how BudgetExceeded surfaces — the iteration-cap fixture asserts this signal)
    - .planning/phases/48-go-rta-driver/48-PATTERNS.md (the `tests/eval-fixtures/...` Pattern Assignment: iteration-cap (1) + x/tools RTA (2) precedents and the oracle-rta reachable-filter warning)
    - .planning/phases/48-go-rta-driver/48-CONTEXT.md (D-14 iteration-cap is a named acceptance artifact; D-15 native RTA fixtures; D-07 seed from ReachabilityRootFact.target_function — get the marking right)
  </read_first>
  <action>
    Create three native fixtures under `tests/eval-fixtures/go-rta/`:

    1. **`iteration-cap/`** (D-14 — the GO-05 criterion-2 proof). `repo/main.go` declares an interface with many implementers and a `main` that drives interface dispatch in a large/cyclic shape — enough concrete types + dynamic callsites that the RTA fixpoint exceeds a deliberately-tight cap. To make the cap bite deterministically, set a tight `[solver.go]` budget in `repo/.polint.toml` (e.g. `[solver.go] max_rta_rounds = 1` or `max_candidates_per_callsite = 1`) so the fixture's own config forces exhaustion (this exercises the Task-1-Plan-2 config→budget threading end to end). `expected.polint-eval.toml`: `area = "go-rta"`, an `[[expected]] invariant = { name = "solver.budget_exceeded.<...>", value = "true", mode = "exact" }` asserting the BudgetExceeded signal is observed. (Confirm the exact observed-invariant name the eval harness exposes for the solver budget signal — it is reserved by Phase 43 D-23 as `budget_exceeded_reasons`/`solver_step_count` on the `SolverMetricSection`; assert a `.nonzero`/present-style boolean rather than a brittle exact count, matching the determinism-gate `.nonzero` style.)

    2. **`interface-dispatch/`** (D-15). `repo/main.go`: a `main` (the root) that calls an interface method where exactly one (or a small known set of) concrete type is instantiated → RTA must resolve precisely that callee. Include a second concrete type that is declared-but-NOT-instantiated to prove the instantiated-type filter excludes it (RTA, not CHA). `expected.polint-eval.toml` asserts ≥1 RTA-resolved derived edge and (if the harness exposes per-edge observation) that the dispatch resolved to the instantiated type only.

    3. **`address-taken/`** (D-15). `repo/main.go`: a `main` that takes a function value / method value / closure and invokes it indirectly → RTA resolves the func-value dispatch via the address-taken set. `expected.polint-eval.toml` asserts the indirect call's RTA edge is derived.

    Each `repo/` has a `go.mod` (copy module-name/go-version shape from mixed-go-ts) and the `.go` files; keep them minimal and self-contained (no external deps — these run without the x/tools clone). Keep `main` as a `RootKind::Main` root so the reachable-from-roots seed is honest (D-07).

    Create `crates/polint/src/eval/go_rta.rs` (declared in `eval/mod.rs`) as a `#[cfg(test)]` crate-private gate mirroring `semantic_graph_snapshot.rs`: `fixture_dir(name)` joins `../../tests/eval-fixtures/go-rta`; one test per fixture using `load_native_fixture` + `run_native_fixture_for_test` (or `evaluation_run_for_fixture_with_observed_for_test` for the custom BudgetExceeded assertion). Tests assert: iteration-cap → BudgetExceeded observed; interface-dispatch → the RTA edge present AND the non-instantiated type excluded; address-taken → the indirect edge present; and a byte-stability check (two runs of any fixture produce identical observed JSON). Source the RTA edges from `db.solver_derived_edges()` via the kernel output (mirror how `semantic_graph_snapshot` reaches provider output / `go_x_tools_callgraph::normalize_kernel_output` reaches `graph_edges_from_kernel_output`).
  </action>
  <verify>
    <automated>cargo test -p polint eval::go_rta 2>&1 | tail -25</automated>
  </verify>
  <done>
    The three `tests/eval-fixtures/go-rta/*` fixtures exist and load; `eval::go_rta` tests pass: iteration-cap asserts an observed `BudgetExceeded` signal (driven by the fixture's own tight `[solver.go]` config), interface-dispatch proves the instantiated-type filter (instantiated callee resolved, non-instantiated excluded), address-taken proves func-value resolution; observed JSON is byte-stable across two runs. No external x/tools clone is required for these.
  </done>
</task>

<task type="auto">
  <name>Task 2: Polyglot Go+TS canary + go_rta determinism fixture wired into the inherited gate</name>
  <files>tests/eval-fixtures/polyglot-canary/go-ts/ (expected.polint-eval.toml + repo/{go.mod,main.go,tokens.ts,package.json,tsconfig.json}), tests/eval-fixtures/determinism/go_rta/ (expected.polint-eval.toml + repo/{go.mod,main.go}), crates/polint/src/eval/determinism_gate.rs</files>
  <read_first>
    - tests/eval-fixtures/framework-entrypoints/mixed-go-ts/ (THE precedent for a mixed Go+TS repo: go.mod + main.go + main_test.go AND package.json + tsconfig.json + *.ts; the invariant-style expected.polint-eval.toml mixed-go-ts/expected.polint-eval.toml:22-105)
    - tests/eval-fixtures/determinism/go_reachable/ (the determinism fixture template + repo)
    - crates/polint/src/eval/determinism_gate.rs (fixture_dir determinism_gate.rs:217, assert_n10_byte_identical, assert_reachability_marking_exercised, go_reachable_fixture_* tests determinism_gate.rs:270-281 — add the go_rta analogues)
    - crates/polint/tests/public_surface_leak.rs (the ALLOWED_PRELUDE locked list — confirm NO edit is needed; all go_rta types are pub(crate))
    - .planning/phases/48-go-rta-driver/48-PATTERNS.md (the `tests/eval-fixtures/...` Pattern Assignment (3) polyglot canary — copy the mixed-go-ts layout)
    - .planning/phases/48-go-rta-driver/48-CONTEXT.md (D-16 the canary does NOT exist yet — create it; Phase 54 promotes it to a hard gate; D-17 determinism + leak gates stay green, ALLOWED_PRELUDE unchanged)
  </read_first>
  <action>
    **Polyglot Go+TS canary (D-16)** — create `tests/eval-fixtures/polyglot-canary/go-ts/`. `repo/` has Go sources (`go.mod`, `main.go` with `main` as root + an interface dispatch the RTA driver resolves) AND TS sources (`package.json`, `tsconfig.json`, `tokens.ts` with functions/callsites). `expected.polint-eval.toml` (`area = "polyglot-canary"`) asserts with `[[expected]] invariant = { name = "...", value = "...", mode = "exact" }` rows that: (a) ≥1 Go RTA-resolved derived edge exists (Go driver active), and (b) TS behavior is unchanged — i.e. the TS-derived observations match what they would be with the Go RTA driver present but the TS token policy still a stub (no cross-language interference through the shared solver core). For (b), assert a stable TS observation invariant (e.g. TS function/callsite counts `.nonzero`, or that NO TS token-propagation edges appear — since `TsTokensPolicy` is still empty). Add a test in `eval::go_rta.rs` (or a sibling `eval::polyglot_canary` module) driving this fixture and asserting both halves. This canary is ADDED here; do not wire it as a hard promotion gate (that is Phase 54).

    **Determinism fixture (D-17)** — create `tests/eval-fixtures/determinism/go_rta/` mirroring `determinism/go_reachable/`: `repo/main.go` with `main` (root) driving interface dispatch so the RTA driver derives edges, plus an unreachable orphan dispatch so the reachable-graph marking invariant holds (≥1 root, ≥1 call site, ≥1 `in_reachable_graph=false`). Keep `expected.polint-eval.toml` order-independent (no brittle per-row assertions — the gate asserts full normalized-JSON byte-identity). In `determinism_gate.rs` add two tests mirroring the `go_reachable` pair: `go_rta_fixture_is_byte_identical_under_ten_seeded_permutations` calling `assert_n10_byte_identical(&fixture_dir("go_rta"))` and `go_rta_fixture_exercises_reachable_graph_marking` calling `assert_reachability_marking_exercised(&fixture_dir("go_rta"), "go_rta")`. The `polint.solver` provider already auto-enrolls in the gate via `provider_manifests()`, so no gate-harness edit is needed beyond adding these fixture-specific tests.

    **Note on the x/tools native suite (D-15):** the existing `go-x-tools-rta-callgraph` suite + `go_x_tools_callgraph` adapter already project RTA edges to the oracle and degrade gracefully when the local clone is absent. Do NOT add a new adapter. If the local clone exists in CI, the suite scores the RTA lift automatically; the self-contained fixtures in Task 1 are the always-runnable proof. Confirm the adapter still compiles and its `#[cfg(test)] normalize_kernel_output` now observes RTA-derived edges (it reads `graph_edges_from_kernel_output`, which includes `solver_derived_edges`).

    Run the full suite plus the two gates explicitly to confirm green. Confirm `public_surface_leak.rs` passes with ZERO edits to `ALLOWED_PRELUDE` (if it fails because a `go_rta` type leaked to `pub`, fix visibility to `pub(crate)` — do NOT extend the allowlist, D-17).
  </action>
  <verify>
    <automated>cargo test -p polint eval::determinism_gate eval::go_rta 2>&1 | tail -25 && cargo test -p polint --test public_surface_leak 2>&1 | tail -15</automated>
  </verify>
  <done>
    The polyglot Go+TS canary fixture exists and its gate test asserts BOTH Go RTA edges resolved AND unchanged TS behavior (no cross-language interference); the `determinism/go_rta` fixture is byte-identical across 10 seeded permutations and exercises reachable-graph marking; the x/tools adapter compiles and observes RTA edges; `public_surface_leak` passes with ALLOWED_PRELUDE unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 3: Full-suite green sweep + roadmap/traceability finalization</name>
  <files>.planning/ROADMAP.md (no source files; verification + bookkeeping task)</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 48 entry + the 4 Success Criteria + the Phase Progress table row to update)
    - .planning/REQUIREMENTS.md (the GO-05 traceability row — Phase 48 status flips to Complete on phase close, but this plan only records plan completion; leave the requirement status edit to phase verification unless the convention is to flip here)
    - crates/polint/src/analysis_kernel/provider.rs (provider_order_for_test + the slot snapshots — confirm unchanged; memory polint-kernel-provider-snapshot-sites notes ~7 sites and "run full cargo test -p polint")
    - .planning/phases/48-go-rta-driver/48-PATTERNS.md (the "Adding/altering a provider slot is a known snapshot chore" note — Phase 48 reuses the existing slot, so order snapshots should NOT change; if they did, that is a regression)
  </read_first>
  <action>
    Run the FULL `cargo test -p polint` (per the kernel-snapshot memory: the whole suite, not a filter) and `cargo clippy -p polint --all-targets` to catch any drift across the three plans — especially provider-order snapshots, cache-input digest tests, and the determinism/leak gates. Resolve any failures: the EXPECTED churn is the updated cache_key parts-list tests and the new `go_semantic`/`go_rta` tests; the FORBIDDEN churn is a changed `provider_order_for_test()` snapshot, a changed points-to fixture, or an ALLOWED_PRELUDE diff (those indicate a regression to fix, not bless).

    Update `.planning/ROADMAP.md`: set the Phase 48 `**Plans:**` line to `3 total` and list the three plans with brief objectives; update the Phase Progress table row for Phase 48 to `3/3` (status stays `Not started`→`Complete` only when phase verification confirms — follow the repo's convention; if the convention is plans-complete-implies-row-complete, set it, otherwise leave status and just record plan count). Do not flip the REQUIREMENTS.md GO-05 row here unless phase-close convention dictates it.

    This is a verification + bookkeeping task; if the full suite is already green from Plans 1-2 + this plan's Tasks 1-2, this task is the green-sweep confirmation plus the roadmap edit. Record the final observed RTA recall/precision numbers (from the self-contained fixtures and, if the x/tools clone is present, the suite metrics) in the SUMMARY so Phase 54's promotion gate has a baseline.
  </action>
  <verify>
    <automated>cargo test -p polint 2>&1 | tail -15 && cargo clippy -p polint --all-targets 2>&1 | tail -8</automated>
  </verify>
  <done>
    `cargo test -p polint` and `cargo clippy -p polint --all-targets` are green with no warnings; `provider_order_for_test()` snapshots and points-to fixtures are unchanged; ALLOWED_PRELUDE is unchanged; ROADMAP Phase 48 shows 3 plans / 3 complete; the SUMMARY records the observed RTA lift baseline.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture `repo/` Go+TS sources → AnalysisKernel | Test-only inputs under `tests/eval-fixtures/` run through the real kernel (including the already-hardened Go sidecar for the Go halves). No new runtime trust surface; fixtures are checked-in, not user-supplied at runtime. |
| RTA-derived edges → oracle-rta scoring / canary assertions | The canary + oracle scoring must observe HONEST edges; mis-wired reachable filtering would misreport recall (Phase 43 D-17 footgun). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-03-01 | Tampering | recall-by-flooding (a fixture that passes by emitting every plausible edge) | mitigate | Fixtures assert the instantiated-type FILTER (a declared-but-not-instantiated type is excluded — Task 1 interface-dispatch) and honest-unresolved; no fixture rewards flooding. The hard precision floor that fails-build on flooding lands in Phase 54 (BENCH-01); Phase 48 demonstrates the lift with precision held honestly. |
| T-48-03-02 | Information disclosure | polyglot canary leaking cross-language state through the shared solver core | mitigate | The canary explicitly asserts TS behavior is UNCHANGED with the Go RTA driver active (TsTokensPolicy still a stub → no TS token edges), proving no cross-language interference (D-16). |
| T-48-03-03 | Tampering | a go_rta type accidentally promoted to the public SDK prelude | mitigate | `public_surface_leak.rs` runs with ALLOWED_PRELUDE locked; any new `pub` reachable from `polint::sdk::prelude::*` fails the gate. Fix is to tighten visibility to `pub(crate)`, never extend the allowlist (D-17). |
| T-48-03-04 | Denial of service | non-deterministic / non-terminating fixture under the determinism gate | mitigate | The `determinism/go_rta` fixture asserts byte-identity across 10 seeded permutations; BTree accumulation + dense-IDs-after-stable-key-sort (Plan 2) keep it stable; the RTA budget bounds termination. |
| T-48-03-SB | Tampering | sidecar process boundary under fixture runs | inherited (preserve) | Fixtures run the existing hardened sidecar; no process-boundary code is touched. No regression. |

No `high`-severity threat. No package-manager installs. No blocking checkpoint required.
</threat_model>

<verification>
- `cargo test -p polint` green (full suite).
- `cargo test -p polint eval::go_rta` — iteration-cap BudgetExceeded, interface-dispatch instantiated-type filter, address-taken func-value resolution, polyglot canary non-regression all pass.
- `cargo test -p polint eval::determinism_gate` — `go_rta` fixture byte-identical across 10 permutations + reachable-graph marking exercised.
- `cargo test -p polint --test public_surface_leak` — green, ALLOWED_PRELUDE unchanged.
- `cargo clippy -p polint --all-targets` — no warnings.
- `provider_order_for_test()` snapshots unchanged.
</verification>

<success_criteria>
- Iteration-cap fixture emits BudgetExceeded (criterion 2 proven).
- Native interface-dispatch / address-taken / dynamic-call fixtures prove reachable-only RTA edges + the instantiated-type filter and demonstrate the recall lift (criterion 4).
- Polyglot Go+TS canary added, showing Go edges resolved + TS unchanged (criterion 3).
- Determinism gate (10-shuffle byte-identical) and public-surface-leak gate stay green; ALLOWED_PRELUDE unchanged (criterion 4 non-regression + D-17).
- No new public surface; no `high` threat.
</success_criteria>

<output>
Create `.planning/phases/48-go-rta-driver/48-03-SUMMARY.md` when done. Record: the fixture cases created + their asserted invariants, the observed BudgetExceeded invariant name used, the observed RTA recall/precision baseline (self-contained + x/tools suite if present) for Phase 54, and confirmation the determinism + leak + provider-order snapshots stayed green/unchanged.
</output>
