---
phase: 22-internal-evaluation-harness-mvp
plan: 04
type: execute
wave: 4
depends_on:
  - 22-03
files_modified:
  - crates/polint/src/eval/fixtures.rs
  - crates/polint/src/eval/matcher.rs
  - crates/polint/src/eval/metrics.rs
  - crates/polint/src/eval/report.rs
  - crates/polint/tests/cli.rs
  - tests/eval-fixtures/provenance/metadata/expected.polint-eval.toml
  - tests/eval-fixtures/provenance/metadata/repo/.polint.toml
  - tests/eval-fixtures/provenance/metadata/repo/src/app.ts
  - tests/eval-fixtures/cache/current-determinism/expected.polint-eval.toml
  - tests/eval-fixtures/cache/current-determinism/repo/.polint.toml
  - tests/eval-fixtures/cache/current-determinism/repo/component.tsx
  - tests/eval-fixtures/cache/current-determinism/repo/payment.go
  - tests/eval-fixtures/extension/rejection-delta/expected.polint-eval.toml
  - tests/eval-fixtures/extension/rejection-delta/repo/.polint.toml
  - tests/eval-fixtures/extension/rejection-delta/repo/src/app.ts
autonomous: true
requirements:
  - SAE-FND-03
must_haves:
  truths:
    - "Native fixtures assert facts, graph edges, paths, diagnostics, invariants, and runtime budgets."
    - "Fixture coverage includes kernel, provenance, current cache behavior, and synthetic extension invariant categories."
    - "Expected/observed output is deterministic and hashes exclude timestamps and machine-local paths."
    - "Phase 22 does not promote a public eval CLI, SDK surface, public JSON schema, external benchmark adapter, typed layer cache key vocabulary, persistent layer cache, or real extension provider sink."
  artifacts:
    - path: "tests/eval-fixtures/provenance/metadata/expected.polint-eval.toml"
      provides: "Provenance/metadata native fixture"
      contains: "producer_id"
    - path: "tests/eval-fixtures/cache/current-determinism/expected.polint-eval.toml"
      provides: "Current cache determinism fixture without typed layer cache semantics"
      contains: "cache.current_determinism"
    - path: "tests/eval-fixtures/extension/rejection-delta/expected.polint-eval.toml"
      provides: "Synthetic extension rejection and delta invariant fixture"
      contains: "extension.synthetic_rejected_fact"
    - path: "crates/polint/tests/cli.rs"
      provides: "Public behavior and no-public-eval-command proof"
      contains: "eval_harness_stays_internal"
  key_links:
    - from: "tests/eval-fixtures/provenance/metadata/expected.polint-eval.toml"
      to: "crates/polint/src/analysis_kernel/debug.rs"
      via: "expected metadata rows match observed metadata debug facts"
      pattern: "metadata_debug_json_for_test"
    - from: "tests/eval-fixtures/cache/current-determinism/expected.polint-eval.toml"
      to: "crates/polint/src/cache/mod.rs"
      via: "fixture asserts current cache determinism, not typed layer cache behavior"
      pattern: "Cache"
    - from: "tests/eval-fixtures/extension/rejection-delta/expected.polint-eval.toml"
      to: "crates/polint/src/eval/matcher.rs"
      via: "synthetic expected/observed extension rows exercise rejection and delta matching"
      pattern: "Rejected"
---

<objective>
Add the required native fixture coverage and public-boundary proof for Phase 22.

Purpose: close SAE-FND-03 with deterministic native evidence for kernel, provenance, cache, and extension invariants while keeping public eval, external adapters, typed cache keys, layer persistence, and real extension activation out of this phase.
Output: native fixtures and tests covering every Phase 22 success criterion.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/22-internal-evaluation-harness-mvp/22-CONTEXT.md
@research/evaluation-harness/FINAL-REPORT.md
@research/evaluation-harness/RECOMMENDED_IMPLEMENTATION.md
@research/evaluation-harness/VALIDATION.md
@AGENTS.md
@docs/API-VISIBILITY-PLAN.md
@.agents/skills/rust-best-practices/SKILL.md
@.claude/skills/polint/SKILL.md
@.planning/phases/22-internal-evaluation-harness-mvp/22-01-SUMMARY.md
@.planning/phases/22-internal-evaluation-harness-mvp/22-02-SUMMARY.md
@.planning/phases/22-internal-evaluation-harness-mvp/22-03-SUMMARY.md
@.planning/phases/21-provenance-precision-and-validation-metadata/21-04-SUMMARY.md
@.planning/phases/07-cache-and-performance/07-04-SUMMARY.md
@crates/polint/src/eval/fixtures.rs
@crates/polint/src/eval/matcher.rs
@crates/polint/src/eval/metrics.rs
@crates/polint/src/eval/report.rs
@crates/polint/src/analysis_kernel/debug.rs
@crates/polint/src/cache/mod.rs
@crates/polint/src/lib.rs
@crates/polint/src/cli/mod.rs
@crates/polint/src/sdk/mod.rs
@crates/polint/src/runner/mod.rs
@crates/polint/tests/cli.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add provenance and current-cache native fixtures</name>
  <files>crates/polint/src/eval/fixtures.rs, tests/eval-fixtures/provenance/metadata/expected.polint-eval.toml, tests/eval-fixtures/provenance/metadata/repo/.polint.toml, tests/eval-fixtures/provenance/metadata/repo/src/app.ts, tests/eval-fixtures/cache/current-determinism/expected.polint-eval.toml, tests/eval-fixtures/cache/current-determinism/repo/.polint.toml, tests/eval-fixtures/cache/current-determinism/repo/component.tsx, tests/eval-fixtures/cache/current-determinism/repo/payment.go</files>
  <read_first>
    <file>.planning/phases/22-internal-evaluation-harness-mvp/22-CONTEXT.md</file>
    <file>research/evaluation-harness/RECOMMENDED_IMPLEMENTATION.md</file>
    <file>.planning/phases/21-provenance-precision-and-validation-metadata/21-04-SUMMARY.md</file>
    <file>.planning/phases/07-cache-and-performance/07-04-SUMMARY.md</file>
    <file>crates/polint/src/eval/fixtures.rs</file>
    <file>crates/polint/src/eval/observed.rs</file>
    <file>crates/polint/src/eval/matcher.rs</file>
    <file>crates/polint/src/eval/report.rs</file>
    <file>crates/polint/src/analysis_kernel/debug.rs</file>
    <file>crates/polint/src/cache/mod.rs</file>
    <file>tests/eval-fixtures/provenance/metadata/expected.polint-eval.toml</file>
    <file>tests/eval-fixtures/cache/current-determinism/expected.polint-eval.toml</file>
  </read_first>
  <behavior>
    - Test 1: the provenance fixture passes and observes fact rows with `producer_id = "polint.source"` and `producer_id = "polint.ts.syntax"`, stable keys, precision labels, and relative paths only.
    - Test 2: the cache fixture passes when run cold and warm, with byte-identical deterministic JSON and identical `output_hash`.
    - Test 3: the cache fixture asserts `cache.current_determinism` and does not assert typed `LayerKey`, `InputSnapshot`, provider output metadata, or persistent layer cache hit/miss semantics.
    - Test 4: both fixtures include at least one runtime budget expected item and only the budget pass/fail affects deterministic hashes.
  </behavior>
  <action>Add fixture tests in `eval/fixtures.rs` named `eval_provenance_fixture_passes` and `eval_cache_current_determinism_fixture_passes`. Add `tests/eval-fixtures/provenance/metadata/` with a small TS repo containing `.polint.toml` include/exclude settings and `src/app.ts` that imports and calls one exported function. Its manifest must expect fact rows for at least one source file and one TS syntax fact using exact `family`, exact `provider_id`, exact `relative_path`, and partial stable-key matching where the full stable key contains content hashes. Add `tests/eval-fixtures/cache/current-determinism/` with the same small mixed TS/Go shape as `write_phase7_cache_fixture`: `.polint.toml`, `component.tsx`, and `payment.go`. Its manifest must assert invariant `cache.current_determinism = "cold_warm_no_cache_equal"` and runtime budget `cache.current_determinism` with `max_runtime_ms = 5000`; the fixture runner test must run the fixture twice and compare deterministic JSON and `output_hash`. Do not add typed cache-key vocabulary such as `InputSnapshot`, `LayerKey`, `QueryKey`, `SummaryKey`, or `DiagnosticKey`; do not add layer cache hit/miss counters or persistent cache behavior beyond current cache determinism per D-09 and D-11.</action>
  <acceptance_criteria>
    <criterion>`rg -n "eval_provenance_fixture_passes|eval_cache_current_determinism_fixture_passes" crates/polint/src/eval/fixtures.rs` returns both fixture tests.</criterion>
    <criterion>`rg -n "producer_id|polint.source|polint.ts.syntax|relative_path" tests/eval-fixtures/provenance/metadata/expected.polint-eval.toml` returns provenance expectations.</criterion>
    <criterion>`rg -n "cache.current_determinism|cold_warm_no_cache_equal|max_runtime_ms = 5000" tests/eval-fixtures/cache/current-determinism/expected.polint-eval.toml` returns cache expectations.</criterion>
    <criterion>`rg -n "InputSnapshot|LayerKey|QueryKey|SummaryKey|DiagnosticKey|provider output metadata|layer cache|hit/miss" crates/polint/src/eval tests/eval-fixtures/cache/current-determinism` returns no implementation matches.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p polint --lib eval_provenance_fixture_passes --locked</automated>
    <automated>cargo test -p polint --lib eval_cache_current_determinism_fixture_passes --locked</automated>
  </verify>
  <done>Native provenance and current-cache fixtures pass deterministically without introducing Phase 23 or Phase 24 cache semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add synthetic extension rejection/delta fixture and full item-kind coverage</name>
  <files>crates/polint/src/eval/fixtures.rs, crates/polint/src/eval/matcher.rs, crates/polint/src/eval/metrics.rs, tests/eval-fixtures/extension/rejection-delta/expected.polint-eval.toml, tests/eval-fixtures/extension/rejection-delta/repo/.polint.toml, tests/eval-fixtures/extension/rejection-delta/repo/src/app.ts</files>
  <read_first>
    <file>.planning/phases/22-internal-evaluation-harness-mvp/22-CONTEXT.md</file>
    <file>research/evaluation-harness/RECOMMENDED_IMPLEMENTATION.md</file>
    <file>crates/polint/src/eval/fixtures.rs</file>
    <file>crates/polint/src/eval/matcher.rs</file>
    <file>crates/polint/src/eval/metrics.rs</file>
    <file>tests/eval-fixtures/extension/rejection-delta/expected.polint-eval.toml</file>
  </read_first>
  <behavior>
    - Test 1: the synthetic extension fixture passes without invoking any real extension/provider sink.
    - Test 2: the fixture includes expected and observed rows that exercise diagnostics, facts, graph edges, paths, invariants, runtime budgets, false-positive traps, and rejected extension facts.
    - Test 3: rejected extension facts are visible as `ObservedStatus::Rejected` and counted separately from accepted/default facts.
    - Test 4: default-vs-extension delta invariants are represented as normalized invariant items, not as a real extension execution mode.
  </behavior>
  <action>Add fixture test `eval_extension_synthetic_rejection_delta_fixture_passes` in `eval/fixtures.rs`. Extend the fixture runner, if needed, to load optional manifest-owned synthetic `observed` rows for fixtures whose manifest sets `synthetic_observed = true`; this is allowed only for `area = "extension"` and must be rejected for other areas. Add `tests/eval-fixtures/extension/rejection-delta/` with a minimal repo and manifest. The manifest must include exact expected rows for: diagnostic `extension.synthetic.delta`, fact `extension.synthetic_accepted_fact` with status `accepted`, fact `extension.synthetic_rejected_fact` with status `rejected`, graph edge `extension.default_to_extended`, path `extension.synthetic_path` with at least three nodes, invariant `extension.default_vs_extension_delta = "changed_facts=2,rejected=1"`, invariant `extension.real_sink_active = "false"`, runtime budget `extension.synthetic_delta` with `max_runtime_ms = 5000`, and one false-positive trap expected diagnostic `extension.synthetic.false_positive_trap`. The synthetic observed rows must include the accepted fact, rejected fact, graph edge, path, invariants, runtime budget pass, and trap hit so matcher/metrics prove those shapes. Do not add or call a real repo-local extension/provider sink, extension fact merge API, or extension activation flag; this implements D-09, D-10, D-12, D-13, and D-14.</action>
  <acceptance_criteria>
    <criterion>`rg -n "eval_extension_synthetic_rejection_delta_fixture_passes|synthetic_observed" crates/polint/src/eval/fixtures.rs` returns extension fixture logic.</criterion>
    <criterion>`rg -n "extension.synthetic_rejected_fact|status = \"rejected\"|extension.default_vs_extension_delta|extension.real_sink_active|false_positive_trap" tests/eval-fixtures/extension/rejection-delta/expected.polint-eval.toml` returns extension expectations.</criterion>
    <criterion>`rg -n "GraphEdge|Path|RuntimeBudget|Invariant|Diagnostic|Fact" tests/eval-fixtures/extension/rejection-delta/expected.polint-eval.toml crates/polint/src/eval/fixtures.rs` returns full item-kind coverage.</criterion>
    <criterion>`rg -n "ExtensionProvider|extension sink|activate_extension|provider sink|merge API" crates/polint/src/eval tests/eval-fixtures/extension/rejection-delta` returns no real extension implementation matches.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p polint --lib eval_extension_synthetic_rejection_delta_fixture_passes --locked</automated>
    <automated>cargo test -p polint --lib eval_metrics --locked</automated>
    <automated>cargo test -p polint --lib eval_matcher --locked</automated>
  </verify>
  <done>Synthetic extension fixture exercises rejection, delta, graph/path, diagnostic, fact, invariant, runtime, and trap semantics without implementing the real extension surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove public behavior and boundary preservation</name>
  <files>crates/polint/src/eval/fixtures.rs, crates/polint/src/eval/report.rs, crates/polint/tests/cli.rs</files>
  <read_first>
    <file>.planning/phases/22-internal-evaluation-harness-mvp/22-CONTEXT.md</file>
    <file>AGENTS.md</file>
    <file>docs/API-VISIBILITY-PLAN.md</file>
    <file>.claude/skills/polint/SKILL.md</file>
    <file>crates/polint/src/lib.rs</file>
    <file>crates/polint/src/cli/mod.rs</file>
    <file>crates/polint/src/sdk/mod.rs</file>
    <file>crates/polint/src/runner/mod.rs</file>
    <file>crates/polint/src/eval/fixtures.rs</file>
    <file>crates/polint/src/eval/report.rs</file>
    <file>crates/polint/tests/cli.rs</file>
  </read_first>
  <behavior>
    - Test 1: structural tests assert `eval` is crate-private and no `polint eval` command, SDK prelude export, runner function, or documented public eval schema was added.
    - Test 2: `polint eval` exits as an unrecognized command rather than a hidden supported command.
    - Test 3: a normal temp-repo `polint check --format json --fail-on none` run remains byte-identical across two runs and contains no internal eval report fields.
    - Test 4: the full native fixture suite runs and verifies kernel, provenance, cache, and extension categories are all present.
  </behavior>
  <action>Add a lib test in `eval/fixtures.rs` named `eval_native_fixture_suite_covers_required_categories` that enumerates `tests/eval-fixtures/` and asserts at least one passing fixture under `kernel`, `provenance`, `cache`, and `extension`. Add CLI integration test `eval_harness_stays_internal` in `crates/polint/tests/cli.rs`. The CLI test must run `polint eval` in a temp repo and assert failure with unrecognized-command wording from clap; run `polint check --format json --fail-on none` twice on a minimal TS repo and assert byte-identical stdout; assert public check JSON does not contain `polint-eval-internal-1`, `ExpectedItem`, `ObservedItem`, `MetricSummary`, `output_hash`, `provider_order.0`, or `extension.synthetic_rejected_fact`. Add structural assertions that `crates/polint/src/lib.rs` contains `pub(crate) mod eval;` and does not contain `pub mod eval`, and that `crates/polint/src/sdk` and `crates/polint/src/runner` contain no `eval::` imports or eval re-exports. This implements D-01, D-02, D-21, and the quality gate that Phase 22 promotes no public eval CLI/SDK contract.</action>
  <acceptance_criteria>
    <criterion>`rg -n "eval_native_fixture_suite_covers_required_categories" crates/polint/src/eval/fixtures.rs` returns one match.</criterion>
    <criterion>`rg -n "eval_harness_stays_internal" crates/polint/tests/cli.rs` returns one match.</criterion>
    <criterion>`rg -n "pub\\(crate\\) mod eval;" crates/polint/src/lib.rs` returns one match and `rg -n "pub mod eval" crates/polint/src/lib.rs` returns no matches.</criterion>
    <criterion>`rg -n "eval::|polint-eval-internal-1|ExpectedItem|ObservedItem|MetricSummary|output_hash" crates/polint/src/sdk crates/polint/src/runner crates/polint/src/cli/mod.rs` returns no public-surface matches.</criterion>
    <criterion>`find tests/eval-fixtures -name expected.polint-eval.toml | sort` includes fixtures below `kernel/`, `provenance/`, `cache/`, and `extension/`.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p polint --lib eval_native_fixture_suite_covers_required_categories --locked</automated>
    <automated>cargo test -p polint --test cli eval_harness_stays_internal --locked</automated>
    <automated>cargo test -p polint --lib eval --locked</automated>
    <automated>cargo test --workspace --all-features --locked</automated>
    <automated>cargo clippy --workspace --all-targets --all-features --locked -- -D warnings</automated>
  </verify>
  <done>All required native fixture categories pass and public CLI/SDK/check behavior remains unchanged with no public eval contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Native fixtures -> internal reports | Fixture manifests and synthetic observed rows become pass/fail evidence and must not smuggle external benchmark content or path leaks. |
| Eval internals -> public CLI/SDK | Internal harness code must not create a public eval command, public SDK export, runner entrypoint, or documented public schema. |
| Synthetic extension rows -> extension claims | Synthetic extension fixtures must prove schema and matcher behavior without pretending the real extension sink exists. |
| Cache fixture -> future cache semantics | Current cache determinism must not be misrepresented as typed layer cache or persistent layer-cache support. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-04-01 | Information Disclosure | native fixture reports | mitigate | Reuse Plan 22-03 path normalization and assert public JSON excludes eval report fields and internal fixture details. |
| T-22-04-02 | Spoofing | extension fixture | mitigate | Require `extension.real_sink_active = "false"` and reject synthetic observed rows outside `area = "extension"`. |
| T-22-04-03 | Tampering | cache fixture claims | mitigate | Acceptance checks grep for absence of Phase 23/24 typed cache vocabulary and layer cache semantics. |
| T-22-04-04 | Elevation of Privilege | public eval command | mitigate | CLI test asserts `polint eval` remains unrecognized and structural tests assert no SDK/runner eval exports. |
| T-22-04-05 | Repudiation | fixture category coverage | mitigate | Suite coverage test enumerates fixture directories and requires passing kernel, provenance, cache, and extension categories. |
</threat_model>

<verification>
Run the automated task commands, then run:
```bash
cargo test -p polint --lib eval --locked
cargo test -p polint --test cli kernel_metadata_preserves_public_check_behavior --locked
cargo test --workspace --all-features --locked
cargo clippy --workspace --all-targets --all-features --locked -- -D warnings
cargo fmt --all -- --check
```
</verification>

<success_criteria>
Plan 22-04 is complete when native fixtures cover kernel, provenance, current cache, and synthetic extension invariants; every eval item kind is exercised; deterministic JSON/hash behavior is proven; and no public eval CLI/SDK/schema or deferred external benchmark/cache/extension work is added.
</success_criteria>

<output>
After completion, create `.planning/phases/22-internal-evaluation-harness-mvp/22-04-SUMMARY.md`.
</output>
