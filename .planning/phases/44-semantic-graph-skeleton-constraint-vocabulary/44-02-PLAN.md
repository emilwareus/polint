---
phase: 44-semantic-graph-skeleton-constraint-vocabulary
plan: 02
type: execute
wave: 2
depends_on: [44-01]
files_modified:
  - crates/polint/src/analysis/semantic_graph/constraints.rs
  - crates/polint/src/analysis/semantic_graph/build.rs
  - crates/polint/src/analysis/semantic_graph/store.rs
  - crates/polint/src/analysis/semantic_graph/mod.rs
  - crates/polint/src/analysis/ids.rs
autonomous: true
requirements: [GRAPH-02]

must_haves:
  truths:
    - "ConstraintKind is a closed 7-variant enum (CopyEdge, Alloc, FieldLoad, FieldStore, CallConstraint, ModelEdge, TypeConstraint) (D-08)"
    - "ConstraintFact mirrors PointsToConstraintFact shape exactly { id, kind, status, precision, stable_key } (D-10)"
    - "The unified ConstraintKind vocabulary stays separated from points_to::PointsToConstraintKind via a top-of-module guard doc comment, with no merge/rename/import (D-09)"
    - "Constraints are emitted from already-available v1.2 facts (calls, values, access_paths) plus Call/MemberOf/Alloc edges from nodes (D-11)"
    - "ModelEdge is reserved with zero emitted constraints, documented as honest emptiness (D-11)"
    - "Building the graph mutates no upstream fact family (composition over mutation) (D-13)"
  artifacts:
    - path: "crates/polint/src/analysis/semantic_graph/constraints.rs"
      provides: "ConstraintKind enum + ConstraintFact family + points_to conceptual-map comment"
      contains: "enum ConstraintKind"
    - path: "crates/polint/src/analysis/semantic_graph/build.rs"
      provides: "build_semantic_graph: projects nodes/edges/constraints from existing facts"
      contains: "fn build_semantic_graph"
    - path: "crates/polint/src/analysis/ids.rs"
      provides: "SemanticConstraintId newtype"
      contains: "struct SemanticConstraintId"
  key_links:
    - from: "crates/polint/src/analysis/semantic_graph/build.rs"
      to: "analysis::calls / analysis::values / analysis::access_paths facts"
      via: "reads existing facts by stable key, emits Call/CallConstraint/Alloc/CopyEdge/Field* "
      pattern: "fn build_semantic_graph"
    - from: "crates/polint/src/analysis/semantic_graph/constraints.rs"
      to: "analysis::points_to::facts::PointsToConstraintKind"
      via: "doc comment mapping CopyEdge<->Copy, Alloc<->AddressOf (no code coupling)"
      pattern: "PointsToConstraintKind"
---

<objective>
Define the GRAPH-02 constraint vocabulary: the closed `ConstraintKind` enum (7 roadmap-named variants), the `ConstraintFact` family mirroring `PointsToConstraintFact`, the `SemanticConstraintId` newtype, and the `build_semantic_graph` population pass that emits a real-but-minimal graph (nodes + Call/MemberOf/Alloc edges + Copy/Alloc/Field/Call constraints) from already-available v1.2 facts. `ModelEdge` is reserved with zero emissions (no producer until Phase 49). This proves the vocabulary end-to-end so Plan 03's snapshot fixtures have real constraints to assert.

Purpose: Deliver the closed constraint vocabulary frontends emit into, without coupling to Phase 47's solver or to the points-to sub-domain's internal constraint language (D-09).
Output: New `semantic_graph/{constraints,build}.rs`, extended `store.rs` (constraints into output/indexes), modified `ids.rs`.
</objective>

<execution_context>
@/Users/emilwareus/conductor/workspaces/exlint/islamabad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/conductor/workspaces/exlint/islamabad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-semantic-graph-skeleton-constraint-vocabulary/44-CONTEXT.md
@.planning/phases/44-semantic-graph-skeleton-constraint-vocabulary/44-PATTERNS.md
@.planning/phases/44-semantic-graph-skeleton-constraint-vocabulary/44-01-SUMMARY.md

# EXACT fact-family shape ConstraintFact mirrors (D-10) + the points-to enum D-09 guards against merging
@crates/polint/src/analysis/points_to/facts.rs

# Population precedent: project from existing facts without mutation
@crates/polint/src/analysis/reachability/discover.rs
@crates/polint/src/analysis/reachability/traverse.rs

<interfaces>
<!-- ConstraintFact MUST be the exact shape of PointsToConstraintFact (points_to/facts.rs:7-14) -->
pub(crate) struct PointsToConstraintFact { id, kind, status, precision, stable_key }

<!-- Reuse the status/precision vocabulary shape from points_to/facts.rs:76-93 -->
PointsToStatus { Present, Unknown, Unsupported, SetupMissing, BudgetExceeded }
PointsToPrecision { FlowInsensitive, LocalFlowSensitive, SummaryProjected, Heuristic, Unknown, Unsupported }

<!-- Source fact families to project from (D-11) — reference by stable key, mutate none (D-13) -->
- analysis::calls (CallSiteFact / CallTargetFact) -> Call edges + CallConstraint
- analysis::values (AllocationTokenFact / ValueFact) -> Alloc / CopyEdge
- analysis::access_paths (AccessPathProjection::Field/Property) -> FieldLoad / FieldStore
- scope/place/object containment -> MemberOf edges
- TypeConstraint references analysis::ids::TypeFactId
- ModelEdge: reserved — emit ZERO (no producer until Phase 49 / ADAPT-01)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SemanticConstraintId + ConstraintKind enum + ConstraintFact family</name>
  <read_first>
    - crates/polint/src/analysis/ids.rs:84-210 (newtype pattern + assert_small_id_contract list)
    - crates/polint/src/analysis/points_to/facts.rs:7-93 (PointsToConstraintFact exact shape; PointsToConstraintKind payload-carrying enum; PointsToStatus/PointsToPrecision)
    - crates/polint/src/analysis/semantic_graph/facts.rs (Plan 01: NodeKind/EdgeKind + SemanticNodeId)
    - crates/polint/src/analysis/reachability/facts.rs:350-397 (variant-count + pinned-order lock test pattern)
  </read_first>
  <behavior>
    - SemanticConstraintId(pub(crate) u64) with the full derive set incl. Default, registered in assert_small_id_contract.
    - ConstraintKind is a closed 7-variant enum in pinned order: CopyEdge { dst: SemanticNodeId, src: SemanticNodeId }, Alloc { dst: SemanticNodeId, object: SemanticNodeId }, FieldLoad { dst: SemanticNodeId, base: SemanticNodeId, field: String }, FieldStore { base: SemanticNodeId, field: String, src: SemanticNodeId }, CallConstraint { callsite: SemanticNodeId }, ModelEdge { /* reserved, no producer */ }, TypeConstraint { node: SemanticNodeId, type_fact: analysis::ids::TypeFactId }.
    - All payload fields are Ord so the Ord derive survives; enum is NOT Copy.
    - constraint_kind_has_exactly_7_variants exhaustive-match test compile-fails on a new variant; an as_str() returns the snake_case tag.
    - ConstraintFact { id: SemanticConstraintId, kind: ConstraintKind, status, precision, stable_key } mirrors PointsToConstraintFact exactly (reuse points_to status/precision enums as the field types, or define equivalents — choose one and document).
  </behavior>
  <action>
    In `analysis/ids.rs` add `SemanticConstraintId(pub(crate) u64)` with the same derive set as `SemanticNodeId` (incl. `Default`) and register it in `assert_small_id_contract`. Create `semantic_graph/constraints.rs`. Define `ConstraintKind` as a closed payload-carrying enum with EXACTLY the 7 roadmap variants and the field shapes in `<behavior>` — variant payloads reference `SemanticNodeId` / existing fact IDs (`TypeConstraint` -> `analysis::ids::TypeFactId`), NOT run-local dense IDs of other families. Keep every field `Ord` (correction V1). Derive `Debug, Clone, PartialEq, Eq, PartialOrd, Ord, Hash, Serialize, Deserialize` + `#[serde(rename_all = "snake_case")]`; add `as_str()` and the `constraint_kind_has_exactly_7_variants` + pinned-order lock tests. Define `ConstraintFact` mirroring `PointsToConstraintFact` (D-10) — reuse `points_to::facts::PointsToStatus`/`PointsToPrecision` as the status/precision field types (do not invent a redundant enum). Add a doc comment mapping the conceptual relationship to `points_to` (`CopyEdge` <-> points-to `Copy`, `Alloc` <-> `AddressOf`, `FieldLoad`/`FieldStore` <-> same) for Phase 47's later folding — explicitly NO code coupling, NO import beyond the status/precision field types (D-09). Register `pub(crate) mod constraints;` in `semantic_graph/mod.rs`.
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis::semantic_graph::constraints 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p polint --lib analysis::semantic_graph::constraints` exits 0 and `cargo test -p polint --lib analysis::ids` covers SemanticConstraintId.
    - `grep -n "enum ConstraintKind" crates/polint/src/analysis/semantic_graph/constraints.rs` shows the enum; an exhaustive-match test guards exactly 7 variants.
    - The conceptual-map comment names `PointsToConstraintKind`; `grep -c "use.*points_to::.*solver" constraints.rs` returns 0 (no solver coupling).
    - No `#[repr(u8)]`; no `pub` (non-crate) item.
  </acceptance_criteria>
  <done>ConstraintKind (7 closed variants) + ConstraintFact + SemanticConstraintId compile with lock tests; D-09 separation documented, no points-to merge.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend SemanticGraphOutput/Store to carry and index constraints</name>
  <read_first>
    - crates/polint/src/analysis/semantic_graph/store.rs (Plan 01 output/indexes)
    - crates/polint/src/analysis/semantic_graph/constraints.rs (Task 1)
    - crates/polint/src/analysis/points_to/store.rs:9-89 (constraints-by-kind index sidecar)
  </read_first>
  <behavior>
    - SemanticGraphOutput gains a constraints: Vec<ConstraintFact> field.
    - normalized() also sorts constraints by (stable_key, id) and reassigns dense SemanticConstraintId by index.
    - SemanticGraphStore::from_output builds a constraints-by-ConstraintKind BTreeMap index (D-14) and validates that every SemanticNodeId referenced by a constraint resolves to a stored node (referential check, dangling -> AnalysisError::InvalidFact).
  </behavior>
  <action>
    Extend `SemanticGraphOutput` with a `constraints: Vec<ConstraintFact>` field and update `normalized()` to sort+dense-reassign constraints exactly as nodes/edges (sort by `(stable_key, id)`, reassign by index). Extend `SemanticGraphStore::from_output` to build the constraints-by-`ConstraintKind`-tag index and to referentially-validate every `SemanticNodeId` a constraint references against the stored node set (dangling -> `AnalysisError::InvalidFact`), mirroring the edge-endpoint check from Plan 01. Add a test asserting a permuted-then-normalized constraint set reassigns identical dense IDs.
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis::semantic_graph::store 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p polint --lib analysis::semantic_graph::store` exits 0 including a constraint-shuffle-stability test and a dangling-constraint-ref test asserting `AnalysisError::InvalidFact`.
    - `grep -n "constraints" crates/polint/src/analysis/semantic_graph/store.rs` shows the new field + index.
  </acceptance_criteria>
  <done>Store carries constraints with a constraints-by-kind index and referential validation; normalization stays shuffle-stable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: build_semantic_graph — emit real-but-minimal nodes/edges/constraints from existing facts</name>
  <read_first>
    - crates/polint/src/analysis/semantic_graph/build.rs (this file — created here)
    - crates/polint/src/analysis/semantic_graph/{facts,constraints,store}.rs (Plan 01 + Tasks 1-2)
    - crates/polint/src/analysis/reachability/discover.rs (project facts from db without mutation)
    - crates/polint/src/analysis/reachability/traverse.rs (derive marks by walking, composition not mutation)
    - crates/polint/src/analysis/calls/facts.rs (CallSiteFact / CallTargetFact — source for Call edges + CallConstraint)
    - crates/polint/src/analysis/values/facts.rs (AllocationTokenFact / ValueFact — source for Alloc / CopyEdge)
    - crates/polint/src/analysis/access_paths/facts.rs (AccessPathProjection::Field/Property — source for FieldLoad/FieldStore)
    - crates/polint/src/analysis_kernel/metadata.rs:370-385 (stable_key_from_parts) + crates/polint/src/analysis/stable_key.rs:16-18 (semantic_stable_key wrapper)
  </read_first>
  <behavior>
    - build_semantic_graph(db) -> SemanticGraphOutput projects nodes from functions/callsites/scopes/places/modules/packages; emits Call edges + CallConstraint from analysis::calls; Alloc / CopyEdge / FieldLoad / FieldStore constraints from analysis::values + analysis::access_paths where those facts already express them; MemberOf edges from scope/place/object containment.
    - Node stable_key is built from (node-kind label, referenced existing stable identity) via the length-prefixed recipe — NEVER run-local dense IDs (D-06). Edge stable_key from (edge-kind label, source node stable key, target node stable key).
    - ModelEdge: emit ZERO; a comment documents the honest emptiness (no producer until Phase 49).
    - Building mutates NO upstream fact family (D-13) — db is read-only here; only the returned SemanticGraphOutput is new.
    - A unit test over a small in-memory db (or a tiny fixture) asserts >=1 of each: node kind present from real facts, >=1 Call edge, >=1 CallConstraint, and zero ModelEdge constraints.
  </behavior>
  <action>
    Create `semantic_graph/build.rs` with `pub(crate) fn build_semantic_graph(db: &AnalysisDb) -> SemanticGraphOutput`. Project nodes from the already-stored families (functions, callsites, scopes, places, modules, packages) using the corresponding existing IDs as `NodeKind` payloads, building each node's `stable_key` from `(node-kind label, referenced existing stable identity string)` via `analysis::stable_key::semantic_stable_key` / `stable_key_from_parts` (D-06) — never dense IDs. Emit `Call` edges + `CallConstraint` from `analysis::calls` call-site/target facts (reference by stable key). Emit `Alloc`/`CopyEdge` constraints from `analysis::values` allocation/value facts and `FieldLoad`/`FieldStore` from `analysis::access_paths` field/property projections, only where the existing facts already express that relationship (do not fabricate edges to inflate recall — D-07 honesty). Emit `MemberOf` edges from scope/place/object containment. Emit ZERO `ModelEdge` and document the reserved emptiness in a comment (D-11). Read `db` only — mutate nothing (D-13). Choose conservative precision labels (reject `Exact`; the ceiling is validated in Plan 03). Register `pub(crate) mod build;` in `mod.rs`. Add the assertion test described in `<behavior>` (a tiny inline/fixture db is acceptable — keep it minimal; the full Go/TS snapshot acceptance is Plan 03).
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis::semantic_graph::build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p polint --lib analysis::semantic_graph::build` exits 0.
    - `grep -n "fn build_semantic_graph" crates/polint/src/analysis/semantic_graph/build.rs` present; build reads `db` and returns a `SemanticGraphOutput`.
    - The build test asserts >=1 Call edge, >=1 CallConstraint, and exactly 0 ModelEdge constraints.
    - `grep -ci "ModelEdge" crates/polint/src/analysis/semantic_graph/build.rs` shows the reserved-emptiness comment; no fabricated ModelEdge emission.
    - `cargo build -p polint` exits 0; no new clippy warnings in semantic_graph.
  </acceptance_criteria>
  <done>build_semantic_graph emits a real-but-minimal graph from existing facts with composed stable keys, zero ModelEdge, and no upstream mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream facts -> build_semantic_graph | Read-only projection over already-bounded v1.2 facts; emits a new derived graph, mutates no source. |
| pub(crate) constraint vocabulary -> SDK prelude | The new ConstraintKind/ConstraintFact must never reach `polint::sdk::prelude::*`. |
| ConstraintKind vs points_to::PointsToConstraintKind | Conceptual boundary (D-09): the unified frontend vocabulary must not merge/import the points-to sub-domain's internal constraint language. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-02-01 | Tampering | Accidental coupling/merge of ConstraintKind with points_to::PointsToConstraintKind | mitigate | D-09 module guard doc comment; conceptual-map comment only, no solver import (`grep` gate in acceptance); folding deferred to Phase 47. |
| T-44-02-02 | Information Disclosure | New constraint types leaking to public SDK | mitigate | All types `pub(crate)`; leak gate proven green in Plan 03. |
| T-44-02-03 | Denial of Service | Unbounded constraint emission from pathological input | accept | Emission is a bounded read-only projection over already-bounded upstream facts; no fixpoint/solving here (deferred to Phase 47). |
| T-44-02-04 | Spoofing/Correctness | Fabricated ModelEdge or invented edges inflating recall | mitigate | ModelEdge emits ZERO (honest emptiness, D-11); edges emitted only where existing facts express them (D-07); test asserts 0 ModelEdge. |
| T-44-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new package-manager installs in this plan. |
</threat_model>

<verification>
- `cargo test -p polint --lib analysis::semantic_graph` exits 0 (constraints + store + build).
- `cargo build -p polint` exits 0.
- ConstraintKind has exactly 7 variants (exhaustive-match lock test); ModelEdge emits zero; no points-to merge/import beyond status/precision field types.
- No upstream fact family mutated (build reads db only).
</verification>

<success_criteria>
- Closed `ConstraintKind` (CopyEdge, Alloc, FieldLoad, FieldStore, CallConstraint, ModelEdge, TypeConstraint) + `ConstraintFact` family exist; `build_semantic_graph` emits a real-but-minimal graph from already-available v1.2 facts with composed stable keys.
- `ModelEdge` reserved with zero emissions, documented; graph derivation mutates no source family; all types `pub(crate)`.
</success_criteria>

<output>
Create `.planning/phases/44-semantic-graph-skeleton-constraint-vocabulary/44-02-SUMMARY.md` when done.
</output>
