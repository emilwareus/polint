---
phase: 44-semantic-graph-skeleton-constraint-vocabulary
plan: 03
type: execute
wave: 3
depends_on: [44-01, 44-02]
files_modified:
  - crates/polint/src/analysis/semantic_graph/provider.rs
  - crates/polint/src/analysis/semantic_graph/cache_key.rs
  - crates/polint/src/analysis/semantic_graph/validate.rs
  - crates/polint/src/analysis/semantic_graph/mod.rs
  - crates/polint/src/core/mod.rs
  - crates/polint/src/analysis_kernel/provider.rs
  - crates/polint/src/analysis_kernel/mod.rs
  - crates/polint/src/analysis_kernel/validation.rs
  - tests/eval-fixtures/semantic-graph/go_graph/expected.polint-eval.toml
  - tests/eval-fixtures/semantic-graph/go_graph/repo/.polint.toml
  - tests/eval-fixtures/semantic-graph/go_graph/repo/go.mod
  - tests/eval-fixtures/semantic-graph/go_graph/repo/main.go
  - tests/eval-fixtures/semantic-graph/ts_graph/expected.polint-eval.toml
  - tests/eval-fixtures/semantic-graph/ts_graph/repo/.polint.toml
  - tests/eval-fixtures/semantic-graph/ts_graph/repo/package.json
  - tests/eval-fixtures/semantic-graph/ts_graph/repo/src/app.ts
autonomous: true
requirements: [GRAPH-01, GRAPH-02]

must_haves:
  truths:
    - "polint.semantic_graph registers as a provider after polint.type_value_alias and before polint.refined_calls (D-16)"
    - "The provider stores graph facts, participates in the cache key digesting every consumed provider output plus the provider/schema version, and emits an empty-output sentinel (D-17)"
    - "The ProviderManifest inputs / cache_key.rs self-document which SC3 dependency-index inputs are present now vs deferred (reserved for Phases 47/49/50), consistent with the cache-key digest recipe (D-17)"
    - "validate() rejects dangling endpoints, duplicate stable keys, and FactPrecision::Exact, surfacing structured diagnostics rather than silent drops (D-15)"
    - "Go + TS/JS snapshot fixtures assert byte-stable, total-ordered nodes/edges/constraints with >=1 of each kind (D-12)"
    - "The Phase 43 determinism gate stays green (auto-enrolled via provider_manifests) and the Phase 42 public-surface-leak gate stays green (D-18)"
  artifacts:
    - path: "crates/polint/src/analysis/semantic_graph/provider.rs"
      provides: "derive_semantic_graph_with_cache_stats pipeline + output digest"
      contains: "fn derive_semantic_graph_with_cache_stats"
    - path: "crates/polint/src/analysis/semantic_graph/cache_key.rs"
      provides: "SEMANTIC_GRAPH_SCHEMA_LABEL + parameter digest + lock tests + SC3 present-vs-deferred inputs comment"
      contains: "SEMANTIC_GRAPH_SCHEMA_LABEL"
    - path: "crates/polint/src/analysis/semantic_graph/validate.rs"
      provides: "validate_semantic_graph structural + precision-ceiling pass"
      contains: "fn validate_semantic_graph"
    - path: "tests/eval-fixtures/semantic-graph/go_graph/expected.polint-eval.toml"
      provides: "Go snapshot fixture manifest"
      contains: "polint-eval-fixture-1"
    - path: "tests/eval-fixtures/semantic-graph/ts_graph/expected.polint-eval.toml"
      provides: "TS/JS snapshot fixture manifest"
      contains: "polint-eval-fixture-1"
  key_links:
    - from: "crates/polint/src/analysis_kernel/mod.rs"
      to: "semantic_graph::provider::derive_semantic_graph_with_cache_stats"
      via: "kernel run splice between type_value_alias and refined_calls"
      pattern: "derive_semantic_graph_with_cache_stats"
    - from: "crates/polint/src/analysis_kernel/provider.rs"
      to: "provider_order_for_test"
      via: "polint.semantic_graph inserted between type_value_alias and refined_calls in all order vectors"
      pattern: "polint.semantic_graph"
    - from: "crates/polint/src/analysis_kernel/validation.rs"
      to: "validate_semantic_graph"
      via: "called between validate_type_value_alias and validate_refined_calls"
      pattern: "validate_semantic_graph"
---

<objective>
Wire `polint.semantic_graph` into the kernel: provider pipeline + output digest + cache key, validation pass (structural + precision ceiling), provider-manifest registration in the correct order slot, kernel run splice, AnalysisDb store path, Go + TS/JS snapshot fixtures asserting byte-stable graph output, determinism-gate inheritance, and the public-surface-leak proof. This closes GRAPH-01 (provider manifest + cache key + validation) and GRAPH-02 (snapshot-fixture acceptance).

Purpose: Make the skeleton + vocabulary a real, cached, validated, deterministic provider that the Phase 43 gate shuffles and the Phase 42 leak gate guards — forward-compatible with Phase 47's refined-calls rework.
Output: New `semantic_graph/{provider,cache_key,validate}.rs`; modified kernel `provider.rs`/`mod.rs`/`validation.rs` and `core/mod.rs`; new `tests/eval-fixtures/semantic-graph/{go_graph,ts_graph}/`.
</objective>

<execution_context>
@/Users/emilwareus/conductor/workspaces/exlint/islamabad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/conductor/workspaces/exlint/islamabad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-semantic-graph-skeleton-constraint-vocabulary/44-CONTEXT.md
@.planning/phases/44-semantic-graph-skeleton-constraint-vocabulary/44-PATTERNS.md
@.planning/phases/44-semantic-graph-skeleton-constraint-vocabulary/44-01-SUMMARY.md
@.planning/phases/44-semantic-graph-skeleton-constraint-vocabulary/44-02-SUMMARY.md

# Full provider/cache-key/validate analog (Phase 43)
@crates/polint/src/analysis/reachability/provider.rs
@crates/polint/src/analysis/reachability/cache_key.rs
@crates/polint/src/analysis/reachability/validate.rs

# Determinism gate (auto-enrolls the new provider via provider_manifests; no harness edit)
@crates/polint/src/eval/determinism_gate.rs

<interfaces>
<!-- Kernel run splice point — analysis_kernel/mod.rs -->
- type_value_alias push completes at mod.rs:494-499 producing `type_value_alias_output_digest`
- type_value_alias_dependency_output_digest computed at mod.rs:501-507
- refined_calls runs at mod.rs:508+ . INSERT polint.semantic_graph between these (after 507, before 508).
- Upstream digests already in scope: entrypoints_calls_digest, identity_dependency_output_digest (note: consumed earlier — re-derive an in-scope `*_dependency_output_digest` the same way), abstract_domains_dependency_output_digest, entrypoints_dependency_output_digest, reachability_output_digest, type_value_alias_dependency_output_digest, entrypoints_symbol_digest/topology (consumed by type_value_alias — re-derive in-scope copies if needed).

<!-- Provider manifest slot — analysis_kernel/provider.rs -->
- polint.type_value_alias manifest ends at provider.rs:620; polint.refined_calls begins at :621. Insert the new ProviderManifest at :621.
- provider_order_for_test() order vectors at :763-785, :790-814, :847-870 — insert "polint.semantic_graph" between "polint.type_value_alias" and "polint.refined_calls" in ALL THREE (and the provider_order_report ProviderOrderRow block ~:1208-1274).
- Real live order around the slot: ... reachability, extensions, type_value_alias, [semantic_graph], refined_calls, data_flow ...

<!-- Validation sequence — analysis_kernel/validation.rs:64-65 -->
- validate_type_value_alias(db,..) at :64 ; validate_refined_calls(db,..) at :65 . INSERT validate_semantic_graph between them.

<!-- AnalysisDb store — core/mod.rs (replace_* family ~:1054-1413). Add replace_semantic_graph_facts mirroring replace_reachability_facts(:1372) / replace_type_value_alias_facts(:1413). -->

<!-- Cache key recipe — reachability/cache_key.rs: SEMANTIC_GRAPH_SCHEMA_LABEL + frozen &[&str] parameter digest + *_locks_parts_list + algorithm_version_bump_invalidates lock tests. -->

<!-- SC3 dependency-index inputs (ROADMAP Phase 44 Success Criterion 3) — full list: semantic index, module graph, MIR, CFG, direct calls, types, summaries, entrypoints, extensions, accepted adaptation models, solver budgets. Phase 44 has producers for only a subset; MIR/CFG/summaries(?)/accepted-adaptation-models/solver-budgets have no producer until Phases 47/49/50/51/53. -->

<!-- Fixture format — tests/eval-fixtures/determinism/{go_reachable,ts_reachable}/ : expected.polint-eval.toml (schema_version="polint-eval-fixture-1", case_id, area="facts", [repo] path="repo", [budget] max_runtime_ms=120000) + repo/.polint.toml + go.mod/package.json + minimal source. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cache_key.rs + validate.rs + AnalysisDb store path</name>
  <read_first>
    - crates/polint/src/analysis/semantic_graph/{store,facts,constraints,build}.rs (Plans 01-02)
    - crates/polint/src/analysis/reachability/cache_key.rs (SCHEMA_LABEL + frozen parameter digest + the two lock tests)
    - crates/polint/src/analysis/reachability/validate.rs:14-187 (check_duplicate_stable_keys, referential checks, reject_exact_precision, push_diagnostic evidence helper)
    - crates/polint/src/core/mod.rs:1372-1413 (replace_reachability_facts / replace_type_value_alias_facts store path)
  </read_first>
  <behavior>
    - SEMANTIC_GRAPH_SCHEMA_LABEL = "semantic-graph-facts-1"; semantic_graph_provider_parameter_digest() over a frozen &[&str] algorithm-version list with *_locks_parts_list and algorithm_version_bump_invalidates lock tests.
    - cache_key.rs carries a self-documenting comment listing which SC3 dependency-index inputs are present now vs deferred-and-why (reserved for Phases 47/49/50/51/53), so the partial coverage reads as intentional, not a silent omission. Comment/doc only — no behavior change; still digest only the inputs that have producers today.
    - validate_semantic_graph(db, diagnostics) asserts: every edge endpoint and every constraint node-ref resolves to a stored node; no duplicate stable keys; dense IDs contiguous and stable-key-sorted; precision ceiling rejects FactPrecision::Exact on derived nodes/edges/constraints. Failures are structured Diagnostics carrying family/stable_key/field/reason, never silent drops.
    - AnalysisDb::replace_semantic_graph_facts(output) stores nodes/edges/constraints with metadata, mirroring replace_reachability_facts, returning Result for the referential-construction case.
  </behavior>
  <action>
    Create `semantic_graph/cache_key.rs` mirroring `reachability/cache_key.rs`: a `SEMANTIC_GRAPH_SCHEMA_LABEL = "semantic-graph-facts-1"` const and `semantic_graph_provider_parameter_digest()` over a frozen `&[&str]` of schema label + algorithm-version strings (e.g. node/edge/constraint emission versions), plus the `*_locks_parts_list` and `algorithm_version_bump_invalidates` lock tests and a schema-label test. Above the frozen parameter/input list, add a short self-documenting doc comment that enumerates the ROADMAP Phase 44 Success-Criterion-3 dependency-index inputs and marks each PRESENT-NOW (e.g. semantic index, module graph, direct calls, types, entrypoints, extensions) vs DEFERRED-AND-WHY (e.g. MIR, CFG, summaries, accepted adaptation models, solver budgets — reserved for Phases 47/49/50/51/53; no producer exists yet). This is a comment/doc addition only — emit/digest ZERO of the deferred inputs and change no runtime behavior. Create `semantic_graph/validate.rs` with `pub(crate) fn validate_semantic_graph(db, diagnostics)` mirroring `reachability/validate.rs`: duplicate-stable-key check, per-edge/per-constraint referential checks, dense-IDs-contiguous-and-sorted assertion, and a `reject_exact_precision` ceiling reusing the precision vocabulary; emit evidence-bearing `Diagnostic`s via a `push_diagnostic` helper. In `core/mod.rs` add `pub(crate) fn replace_semantic_graph_facts(...)` mirroring `replace_reachability_facts` (:1372) — store the normalized nodes/edges/constraints with provider/family metadata, returning `Result<(), AnalysisError>` (the store's referential construction may fail). Register `pub(crate) mod cache_key;` and `pub(crate) mod validate;` in `semantic_graph/mod.rs`.
  </action>
  <verify>
    <automated>cargo test -p polint --lib "analysis::semantic_graph::cache_key" "analysis::semantic_graph::validate" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p polint --lib analysis::semantic_graph::cache_key` and `...::validate` exit 0.
    - `grep -n "SEMANTIC_GRAPH_SCHEMA_LABEL" crates/polint/src/analysis/semantic_graph/cache_key.rs` shows `"semantic-graph-facts-1"`; both lock tests present.
    - `cache_key.rs` contains the SC3 present-vs-deferred dependency-index inputs comment naming the deferred inputs (MIR, CFG, summaries, accepted adaptation models, solver budgets) and the phases that will produce them; no deferred input is actually digested.
    - `grep -n "fn validate_semantic_graph" .../validate.rs` and `grep -n "fn replace_semantic_graph_facts" crates/polint/src/core/mod.rs` present.
    - A validate test asserts an Exact-precision derived row produces a diagnostic.
  </acceptance_criteria>
  <done>Cache-key parameter digest + schema label locked, with SC3 present-vs-deferred inputs self-documented; validation enforces structural + precision-ceiling invariants; AnalysisDb store path exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: provider.rs pipeline + kernel manifest registration + run splice + validation wiring</name>
  <read_first>
    - crates/polint/src/analysis/semantic_graph/{cache_key,store,build,validate}.rs (Task 1)
    - crates/polint/src/analysis/reachability/provider.rs:16-268 (run-output struct, 7-phase pipeline, output digest, manifest-lookup helper)
    - crates/polint/src/analysis_kernel/provider.rs:573-644 (type_value_alias + refined_calls manifests) and :760-870 (the three provider_order_for_test vectors)
    - crates/polint/src/analysis_kernel/mod.rs:425-535 (reachability run splice precedent + type_value_alias/refined_calls run blocks and the *_dependency_output_digest pattern)
    - crates/polint/src/analysis_kernel/validation.rs:40-72 (validation sequence)
  </read_first>
  <behavior>
    - derive_semantic_graph_with_cache_stats(db, input_snapshot, manifest, <upstream output digests>) returns { diagnostics, cache_stats, output_digest: Option<Digest> } following the 7-phase reachability pipeline: build_semantic_graph (no mutation) -> normalized() -> output digest over stable serde payloads (never dense IDs, empty-output sentinel) -> dense IDs post-digest -> replace_semantic_graph_facts -> on store error output_digest=None.
    - Output digest parts include provider_id/version/schema, parameters, config, and the output digests of polint.calls, polint.identity, polint.abstract_domains, polint.entrypoints, polint.reachability, polint.type_value_alias, plus symbol/module-topology digests (D-17).
    - A ProviderManifest { id: "polint.semantic_graph", kind: WholeRepoDerived, inputs: &[...consumed families...], outputs: &["semantic_nodes","semantic_edges","semantic_constraints"], language_scope: MultiLanguage, cache_policy: InMemoryDerived, schema_versions: SEMANTIC_GRAPH_SCHEMA, precision_ceiling: SetupAware } is registered between type_value_alias and refined_calls.
    - The manifest `inputs` slice carries a short adjacent comment marking which SC3 dependency-index inputs are present now (the families actually listed in `inputs`) vs deferred (MIR, CFG, summaries, accepted adaptation models, solver budgets — Phases 47/49/50/51/53), consistent with the cache_key.rs note. Comment-only; the `inputs` slice still contains ONLY the families with producers today.
    - "polint.semantic_graph" appears in all three provider_order_for_test vectors and the provider_order_report row block, between "polint.type_value_alias" and "polint.refined_calls".
    - The kernel run invokes the provider between type_value_alias and refined_calls and pushes its provider output; validation calls validate_semantic_graph between validate_type_value_alias and validate_refined_calls.
  </behavior>
  <action>
    Create `semantic_graph/provider.rs` mirroring `reachability/provider.rs` in full: a `SemanticGraphProviderRunOutput { diagnostics, cache_stats, output_digest: Option<Digest> }` struct and `pub(crate) fn derive_semantic_graph_with_cache_stats(db, input_snapshot, manifest, <upstream digests>)` implementing the 7-phase pipeline (S5): build via `build_semantic_graph` (read-only), `normalized()`, compute the output digest (S4) over stored stable serde payloads with the empty-output sentinel and NEVER dense IDs, assign dense IDs post-digest, `db.replace_semantic_graph_facts(...)`, and return `output_digest: None` on store error. Digest parts per D-17 (list above). Add `pub(crate) const SEMANTIC_GRAPH_PROVIDER_ID` use from store and `pub(crate) mod provider;` in `mod.rs`.

    In `analysis_kernel/provider.rs`: add `const SEMANTIC_GRAPH_SCHEMA: &[SchemaVersion]` next to `REACHABILITY_SCHEMA`, pointing at `SEMANTIC_GRAPH_SCHEMA_LABEL`. Insert the `ProviderManifest` for `polint.semantic_graph` (fields in `<behavior>`) immediately after the `polint.type_value_alias` manifest (after :620, before the `polint.refined_calls` manifest at :621). Directly above the `inputs: &[...]` slice, add a short comment listing the SC3 dependency-index inputs as present-now (the families enumerated in the slice) vs deferred-and-why (MIR, CFG, summaries, accepted adaptation models, solver budgets — reserved for Phases 47/49/50/51/53), mirroring the cache_key.rs note so the partial coverage is self-documenting; do NOT add any deferred family to the actual `inputs` slice. Insert `"polint.semantic_graph"` between `"polint.type_value_alias"` and `"polint.refined_calls"` in ALL THREE `provider_order_for_test()` assertion vectors (:763-785, :790-814, :847-870) and in the `provider_order_report_for_test` `ProviderOrderRow` block (~:1208-1274) — confirm against the LIVE vector (extensions sits between reachability and type_value_alias).

    In `analysis_kernel/mod.rs`: splice the provider run between the `type_value_alias` push (ends :499 / dependency digest :501-507) and the `refined_calls` run (:508). Pass the upstream output digests it consumes (re-derive any `*_dependency_output_digest` that was moved/consumed earlier, the same way the existing code does with `unwrap_or_else(|| Digest::absent(...))`). Push its provider output via `provider_output_for_with_optional_digest("polint.semantic_graph", ...)`.

    In `analysis_kernel/validation.rs`: call `validate_semantic_graph(db, &mut diagnostics)` between `validate_type_value_alias` (:64) and `validate_refined_calls` (:65).
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis_kernel::provider 2>&1 | tail -12 && cargo build -p polint 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p polint --lib analysis_kernel::provider` exits 0 with all three order vectors including `"polint.semantic_graph"` in the correct slot.
    - `grep -c "polint.semantic_graph" crates/polint/src/analysis_kernel/provider.rs` >= 5 (manifest + 3 order vectors + report row).
    - The `polint.semantic_graph` manifest `inputs` slice has an adjacent present-vs-deferred SC3 comment naming the deferred inputs; no deferred family is present in the slice itself.
    - `grep -n "derive_semantic_graph_with_cache_stats" crates/polint/src/analysis_kernel/mod.rs` shows the run splice; `grep -n "validate_semantic_graph" crates/polint/src/analysis_kernel/validation.rs` shows the call.
    - `cargo build -p polint` exits 0.
  </acceptance_criteria>
  <done>Provider runs in the correct slot, participates in the cache key, stores facts, is validated in-sequence, appears in every provider-order assertion, and its manifest inputs self-document present-vs-deferred SC3 coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Go + TS/JS snapshot fixtures, determinism-gate inheritance, and public-surface-leak proof</name>
  <read_first>
    - tests/eval-fixtures/determinism/go_reachable/ (expected.polint-eval.toml + repo/.polint.toml + go.mod + main.go) — exact fixture-tree template
    - tests/eval-fixtures/determinism/ts_reachable/ (expected.polint-eval.toml + repo/.polint.toml + package.json + src/app.ts)
    - crates/polint/src/eval/determinism_gate.rs:1-62 (auto-enrollment via provider_manifests; no harness edit; per-phase obligation to keep fixtures green)
    - crates/polint/src/analysis/semantic_graph/debug.rs OR reachability/debug.rs (metadata_debug_json_for_test pattern for snapshot observation)
    - crates/polint/tests/public_surface_leak.rs:1-90 (do NOT modify; do NOT extend ALLOWED_PRELUDE)
  </read_first>
  <behavior>
    - tests/eval-fixtures/semantic-graph/go_graph/ and ts_graph/ each contain expected.polint-eval.toml (schema_version="polint-eval-fixture-1", area="facts", [repo] path="repo", [budget] max_runtime_ms=120000) plus a minimal repo exercising calls + allocs + field ops so the graph emits >=1 of each node/edge/constraint kind.
    - A snapshot test asserts the normalized serialized nodes/edges/constraints for each fixture are byte-stable, total-ordered by stable key, and byte-identical across provider-order shuffles (consistent with the cross-platform contract).
    - The Phase 43 determinism gate (eval::determinism_gate) still passes with polint.semantic_graph auto-enrolled — no gate harness edit.
    - public_surface_leak.rs stays green unmodified; ALLOWED_PRELUDE unchanged.
  </behavior>
  <action>
    Add an optional `semantic_graph/debug.rs` (`metadata_debug_json_for_test`) mirroring `reachability/debug.rs` ONLY if needed to source the snapshot observation; register `pub(crate) mod debug;` under `#[cfg(test)]` or `pub(crate)` as the analog does. Create `tests/eval-fixtures/semantic-graph/go_graph/` and `ts_graph/` by copying the structure of the determinism fixtures: `expected.polint-eval.toml` (header per `<behavior>`, doc comment describing the case), `repo/.polint.toml` (workspace include + language config), `repo/go.mod` + `repo/main.go` (Go) / `repo/package.json` + `repo/src/app.ts` (TS), each minimal source exercising a direct call (-> Call edge + CallConstraint), an allocation (-> Alloc), and a field load/store (-> FieldLoad/FieldStore) so the snapshot covers >=1 of each kind. Add a snapshot test (in the eval/fixtures test path or a dedicated `semantic_graph` snapshot test) asserting the normalized graph JSON for each fixture is byte-stable and total-ordered by stable key. Do NOT edit `eval::determinism_gate` — confirm it auto-enrolls the new provider (`provider_manifests().len()` assertion already covers it). Do NOT modify `public_surface_leak.rs` or extend `ALLOWED_PRELUDE`.
  </action>
  <verify>
    <automated>cargo test -p polint --lib determinism_gate 2>&1 | tail -8 && cargo test -p polint --test public_surface_leak 2>&1 | tail -8 && cargo test -p polint semantic_graph 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p polint --lib determinism_gate` exits 0 (gate green with the new provider auto-enrolled).
    - `cargo test -p polint --test public_surface_leak` exits 0 (leak gate green, ALLOWED_PRELUDE unchanged).
    - Both `tests/eval-fixtures/semantic-graph/go_graph/expected.polint-eval.toml` and `ts_graph/expected.polint-eval.toml` exist with `schema_version = "polint-eval-fixture-1"`.
    - The snapshot test asserts >=1 node, >=1 edge, >=1 constraint of the expected kinds per fixture, byte-stable.
    - `git diff --stat crates/polint/tests/public_surface_leak.rs` shows no changes.
  </acceptance_criteria>
  <done>Go + TS/JS snapshot fixtures prove real byte-stable constraint emission; the determinism gate stays green via auto-enrollment; the public-surface-leak gate stays green unmodified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream provider outputs -> semantic_graph provider | Read-only consumption of already-bounded provider outputs; the provider stores a new derived layer, mutates no source. |
| semantic_graph provider output -> cache layer | Cache reuse is gated on the output digest; an incomplete cache key would reuse stale/incorrect graph state. |
| pub(crate) graph/provider types -> SDK prelude | Phase 42 leak gate forbids any v1.3 graph/provider/constraint type reaching `polint::sdk::prelude::*`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-03-01 | Tampering | Cache-key incompleteness causing stale/incorrect graph reuse | mitigate | Output digest includes every consumed provider output digest + provider/schema version (D-17); frozen parameter-digest list with `algorithm_version_bump_invalidates` lock test; empty-output sentinel distinguishes empty vs populated graphs. Deferred SC3 inputs (MIR/CFG/summaries/adaptation models/solver budgets) have no producer yet and are self-documented as reserved, not silently dropped — they enter the digest when their producers land (Phases 47/49/50/51/53). |
| T-44-03-02 | Information Disclosure | New graph/provider/cache-key types leaking to public SDK | mitigate | All types `pub(crate)`; `public_surface_leak.rs` runs unmodified and green on Linux+macOS; ALLOWED_PRELUDE not extended (acceptance gate). |
| T-44-03-03 | Tampering | Non-deterministic provider output failing the inherited determinism gate | mitigate | Provider auto-enrolls in the Phase 43 10-shuffle gate via `provider_manifests()`; output digest over stable payloads never dense IDs; gate green is a named acceptance criterion. |
| T-44-03-04 | Denial of Service | Pathological source input causing unbounded graph construction | accept | The provider is a bounded read-only aggregation over already-bounded upstream provider outputs; unbounded solving/budgets are deferred to Phase 47's budgeted solver. |
| T-44-03-05 | Repudiation/Correctness | Silent drop of malformed nodes/edges/constraints | mitigate | `validate_semantic_graph` surfaces dangling refs, duplicate stable keys, and Exact-precision violations as structured diagnostics (family/stable_key/field/reason), never silent drops (D-15). |
| T-44-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new package-manager installs; determinism gate's seeded PRNG is in-tree (no `rand` dep), matching the established no-new-deps discipline. |
</threat_model>

<verification>
- `cargo test -p polint --lib analysis::semantic_graph` exits 0 (provider + cache_key + validate).
- `cargo test -p polint --lib analysis_kernel::provider` exits 0 (all three order vectors include polint.semantic_graph in the correct slot).
- `cargo test -p polint --lib determinism_gate` exits 0 (Phase 43 gate inherited and green — NAMED acceptance criterion D-18).
- `cargo test -p polint --test public_surface_leak` exits 0 with ALLOWED_PRELUDE unchanged (success criterion 4).
- `cargo test -p polint semantic_graph` snapshot fixtures (Go + TS) pass byte-stable.
- `cargo build -p polint` exits 0.
</verification>

<success_criteria>
- `polint.semantic_graph` provider is registered (correct order slot), stores graph facts, participates in a cache key digesting every consumed provider output + schema version, is validated in-sequence (structural + precision ceiling), and self-documents which SC3 dependency-index inputs are present now vs deferred (MIR/CFG/summaries/adaptation models/solver budgets, reserved for Phases 47/49/50/51/53).
- Go + TS/JS snapshot fixtures assert byte-stable nodes/edges/constraints with >=1 of each kind (GRAPH-02 acceptance).
- The Phase 43 determinism gate stays green (auto-enrolled) and the Phase 42 public-surface-leak gate stays green unmodified (GRAPH-01 success criterion 4).
</success_criteria>

<output>
Create `.planning/phases/44-semantic-graph-skeleton-constraint-vocabulary/44-03-SUMMARY.md` when done.
</output>
