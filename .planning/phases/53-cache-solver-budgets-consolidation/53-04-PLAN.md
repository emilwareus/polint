---
phase: 53-cache-solver-budgets-consolidation
plan: 04
type: execute
wave: 3
depends_on: [53-02, 53-03]
files_modified:
  - crates/polint/src/eval/performance.rs
  - crates/polint/src/eval/report.rs
  - crates/polint/src/eval/markdown.rs
  - crates/polint/src/eval/fixtures.rs
  - crates/polint/tests/public_surface_leak.rs
  - .planning/ROADMAP.md
autonomous: true
requirements: [CACHE-01, CACHE-02]

must_haves:
  truths:
    - "D-16/D-17/D-18: benchmark/eval reporting includes deterministic cold/warm RSS threshold columns without making Phase 53 the final promotion gate."
    - "D-20/D-21: Phase 53 does not add public budget commands or overclaim benchmark precision/recall."
    - "D-22/D-23/D-24/D-25: focused cache/budget tests, public leak, determinism, full test, clippy, format, and diff checks pass before closeout."
    - "Roadmap SC4: cold/warm RSS thresholds appear as required columns in the benchmark report."
  artifacts:
    - path: "crates/polint/src/eval/report.rs"
      provides: "internal report schema for RSS threshold fields"
      contains: "rss"
    - path: "crates/polint/src/eval/markdown.rs"
      provides: "deterministic benchmark report rendering"
      contains: "RSS"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 53 plan list and closeout bookkeeping"
      contains: "53-04"
---

<objective>
Add deterministic cold/warm RSS threshold fields to internal benchmark reporting, run final cache/budget/public-surface/full regression gates, and update roadmap/state artifacts for Phase 53 readiness.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/53-cache-solver-budgets-consolidation/53-CONTEXT.md
@.planning/phases/53-cache-solver-budgets-consolidation/53-01-PLAN.md
@.planning/phases/53-cache-solver-budgets-consolidation/53-02-PLAN.md
@.planning/phases/53-cache-solver-budgets-consolidation/53-03-PLAN.md
@crates/polint/src/eval/report.rs
@crates/polint/src/eval/markdown.rs
@crates/polint/src/eval/performance.rs
@crates/polint/tests/public_surface_leak.rs
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add defaulted RSS threshold fields to internal eval report</name>
  <files>crates/polint/src/eval/performance.rs, crates/polint/src/eval/report.rs, crates/polint/src/eval/fixtures.rs</files>
  <read_first>
    - crates/polint/src/eval/performance.rs
    - crates/polint/src/eval/report.rs
    - crates/polint/src/eval/fixtures.rs
    - crates/polint/src/eval/model.rs
  </read_first>
  <action>
    Add internal cold/warm RSS threshold and observed fields to the benchmark/eval report model. Use deterministic names such as `cold_rss_threshold_mb`, `cold_rss_observed_mb`, `warm_rss_threshold_mb`, and `warm_rss_observed_mb` or an equivalent nested struct. Fields must default cleanly for existing fixtures and represent unavailable measurements explicitly as `null` or `"-"` in rendered output.
  </action>
  <verify>
    <automated>cargo test -p polint eval::report eval::fixtures 2>&1 | tail -100</automated>
  </verify>
  <acceptance_criteria>
    - Serialized eval JSON includes defaulted RSS fields or a defaulted RSS section.
    - Existing fixture JSON without RSS fields still deserializes.
    - Missing RSS measurements render deterministically as `null` or `-`.
    - No transient paths, timestamps, or machine-specific labels enter report snapshots.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Render RSS columns in markdown/benchmark reports</name>
  <files>crates/polint/src/eval/markdown.rs, crates/polint/src/eval/report.rs</files>
  <read_first>
    - crates/polint/src/eval/markdown.rs
    - crates/polint/src/eval/report.rs
    - crates/polint/src/eval/performance.rs
  </read_first>
  <action>
    Extend the deterministic markdown or benchmark report table with cold/warm RSS threshold and observed columns. Keep Phase 53 reporting non-promotional: do not fail precision floors or final recall targets here, and do not state that v1.3 benchmark goals are met.
  </action>
  <verify>
    <automated>cargo test -p polint eval::markdown 2>&1 | tail -80</automated>
  </verify>
  <acceptance_criteria>
    - Markdown output has column labels for cold RSS threshold, cold RSS observed, warm RSS threshold, and warm RSS observed or equivalent concise labels.
    - Snapshot/order tests remain deterministic when provider/performance rows are shuffled.
    - Report text does not include absolute temp paths or timestamps.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Run final cache, budget, leak, and workspace gates</name>
  <files>crates/polint/src/, crates/polint/tests/public_surface_leak.rs, crates/polint/tests/cache_invalidation_v13.rs</files>
  <read_first>
    - crates/polint/tests/public_surface_leak.rs
    - crates/polint/tests/cache_invalidation_v13.rs
    - .planning/phases/53-cache-solver-budgets-consolidation/53-CONTEXT.md
  </read_first>
  <action>
    Run focused cache-key/cache-invalidation, budget, unknown taxonomy, eval report, public surface leak, full test, clippy, format, and diff checks. Fix visibility issues by tightening visibility, not by expanding public allowlists.
  </action>
  <verify>
    <automated>cargo test -p polint --test cache_invalidation_v13 && cargo test -p polint --lib cache_key unknown_taxonomy && cargo test -p polint eval::go_rta eval::ts_tokens eval::ts_object_model eval::markdown && cargo test -p polint --test public_surface_leak && cargo test -p polint && cargo clippy -p polint --all-targets -- -D warnings && cargo fmt --all -- --check && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - Focused cache invalidation tests pass.
    - Focused budget/eval/unknown tests pass.
    - `cargo test -p polint --test public_surface_leak` passes with `ALLOWED_PRELUDE` unchanged.
    - `cargo test -p polint` passes.
    - `cargo clippy -p polint --all-targets -- -D warnings` passes.
    - `cargo fmt --all -- --check` passes.
    - `git diff --check` passes.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Record Phase 53 roadmap closeout</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/phases/53-cache-solver-budgets-consolidation/53-CONTEXT.md
    - .planning/phases/53-cache-solver-budgets-consolidation/53-01-PLAN.md
    - .planning/phases/53-cache-solver-budgets-consolidation/53-02-PLAN.md
    - .planning/phases/53-cache-solver-budgets-consolidation/53-03-PLAN.md
  </read_first>
  <action>
    Update `.planning/ROADMAP.md` Phase 53 `**Plans:**` from `TBD` to the four-plan list after implementation and verification evidence exists. Update GSD state through `gsd-sdk` handlers where available so Phase 54 is the next milestone phase.
  </action>
  <verify>
    <automated>gsd-sdk query find-phase 53 && gsd-sdk query audit-uat</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` lists 53-01 through 53-04 with concise objectives.
    - `gsd-sdk query find-phase 53` reports four plans after planning and four summaries after execution.
    - `gsd-sdk query audit-uat` reports zero blocking UAT debt.
    - STATE points to Phase 54 only after Phase 53 summaries and verification exist.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Risk | Mitigation |
|----------|------|------------|
| Benchmark reporting | Phase 53 could accidentally claim Phase 54 promotion. | Add RSS columns only; leave hard floors and recall claims to Phase 54. |
| Public surface | Report fields could expose solver/cache internals publicly. | Keep report/internal fields out of SDK and run public leak tests. |
| Final verification | Narrow tests could miss integration regressions. | Run focused gates plus full test, clippy, fmt, and diff checks. |
</threat_model>

<verification>
- `cargo test -p polint --test cache_invalidation_v13`
- `cargo test -p polint --lib cache_key unknown_taxonomy`
- `cargo test -p polint eval::go_rta eval::ts_tokens eval::ts_object_model eval::markdown`
- `cargo test -p polint --test public_surface_leak`
- `cargo test -p polint`
- `cargo clippy -p polint --all-targets -- -D warnings`
- `cargo fmt --all -- --check`
- `git diff --check`
</verification>

<success_criteria>
- Internal benchmark reports include cold/warm RSS threshold columns.
- Cache and budget consolidation tests pass.
- Public leak and full workspace gates pass.
- Phase 53 roadmap closeout is ready for verification.
</success_criteria>
