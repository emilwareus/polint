---
phase: 53-cache-solver-budgets-consolidation
plan: 03
type: execute
wave: 2
depends_on: [53-01]
files_modified:
  - crates/polint/src/analysis/solver/budget.rs
  - crates/polint/src/analysis/solver/facts.rs
  - crates/polint/src/analysis/solver/provider.rs
  - crates/polint/src/analysis/unknown_taxonomy/collect.rs
  - crates/polint/src/eval/{go_rta.rs,ts_tokens.rs,ts_object_model.rs,adaptation.rs}
  - tests/eval-fixtures/{go-rta,ts-tokens,ts-object-model,adaptation-model}/
autonomous: true
requirements: [CACHE-02]

must_haves:
  truths:
    - "D-11/D-12/D-13: `BudgetStatus::BudgetExceeded` stays the unified status while private stable reasons identify the exact sub-budget that tripped."
    - "D-14/D-15: budget status and effective budget changes participate in downstream cache and unknown/report output when behavior changes."
    - "Roadmap SC3: token-set size, property abstraction, dynamic-call fanout, model expansion, and package/lifecycle-related caps surface budget exhaustion as facts or report rows."
  artifacts:
    - path: "crates/polint/src/analysis/solver/budget.rs"
      provides: "stable budget reason vocabulary"
      contains: "BudgetExceeded"
    - path: "crates/polint/src/analysis/unknown_taxonomy/collect.rs"
      provides: "budget-exceeded unknown collection"
      contains: "budget"
---

<objective>
Consolidate budget-exceeded evidence so every solver driver and adaptation lane emits stable, specific budget reasons through existing private facts, unknown taxonomy, and eval reports without creating new public budget APIs.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/53-cache-solver-budgets-consolidation/53-CONTEXT.md
@.planning/phases/53-cache-solver-budgets-consolidation/53-PATTERNS.md
@crates/polint/src/analysis/solver/budget.rs
@crates/polint/src/analysis/unknown_taxonomy/collect.rs
@crates/polint/src/eval/go_rta.rs
@crates/polint/src/eval/ts_tokens.rs
@crates/polint/src/eval/ts_object_model.rs
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add stable private budget reason vocabulary</name>
  <files>crates/polint/src/analysis/solver/budget.rs, crates/polint/src/analysis/solver/facts.rs</files>
  <read_first>
    - crates/polint/src/analysis/solver/budget.rs
    - crates/polint/src/analysis/solver/facts.rs
    - crates/polint/src/analysis/solver/provider.rs
    - crates/polint/src/analysis/solver/go_rta/fixpoint.rs
    - crates/polint/src/analysis/solver/ts_tokens/fixpoint.rs
    - crates/polint/src/analysis/solver/ts_object_model/fixpoint.rs
  </read_first>
  <action>
    Add a crate-private stable reason vocabulary or string helper for budget exhaustion. Required reason labels include `solver.max_steps`, `solver.max_outer_iterations`, `go.address_taken_threshold`, `go.max_candidates_per_callsite`, `go.max_rta_rounds`, `go.max_worklist_steps`, `js.max_tokens_per_var`, `js.max_candidates_per_callsite`, `js.max_token_worklist_steps`, `object.max_properties_per_object`, `object.max_prototype_depth`, `object.max_receiver_candidates_per_callsite`, `object.max_object_worklist_steps`, and `adaptation.max_model_derived_edges`.
  </action>
  <verify>
    <automated>cargo test -p polint --lib solver::budget solver:: 2>&1 | tail -100</automated>
  </verify>
  <acceptance_criteria>
    - `BudgetStatus::BudgetExceeded` remains the run-level status enum.
    - Budget reasons are stable lowercase dot-separated strings.
    - The reason vocabulary is crate-private or test-only.
    - Existing budget status serialization remains backward compatible.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Thread specific budget reasons through solver outputs</name>
  <files>crates/polint/src/analysis/solver/provider.rs, crates/polint/src/analysis/solver/go_rta/fixpoint.rs, crates/polint/src/analysis/solver/ts_tokens/fixpoint.rs, crates/polint/src/analysis/solver/ts_object_model/fixpoint.rs, crates/polint/src/analysis/adaptation/validate.rs</files>
  <read_first>
    - crates/polint/src/analysis/solver/provider.rs
    - crates/polint/src/analysis/solver/engine.rs
    - crates/polint/src/analysis/solver/policy.rs
    - crates/polint/src/analysis/solver/go_rta/fixpoint.rs
    - crates/polint/src/analysis/solver/ts_tokens/fixpoint.rs
    - crates/polint/src/analysis/solver/ts_object_model/fixpoint.rs
    - crates/polint/src/analysis/adaptation/validate.rs
  </read_first>
  <action>
    Thread the stable reason strings into existing solver output, budget-reason report rows, or provider metadata. When any driver trips a cap, preserve edges derived before the cap only if their provenance remains honest, set run-level `BudgetStatus::BudgetExceeded`, and record the precise reason string.
  </action>
  <verify>
    <automated>cargo test -p polint eval::go_rta eval::ts_tokens eval::ts_object_model adaptation_model 2>&1 | tail -120</automated>
  </verify>
  <acceptance_criteria>
    - Go fanout cap fixture records `go.max_candidates_per_callsite` or the exact selected Go cap reason.
    - TS token explosion fixture records `js.max_tokens_per_var` or the exact selected JS token cap reason.
    - TS object budget fixture records object-specific reason strings.
    - Adaptation model expansion cap records adaptation-specific reason strings.
    - No budget cap silently truncates without a reportable reason.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Surface budget reasons through unknown taxonomy and eval metrics</name>
  <files>crates/polint/src/analysis/unknown_taxonomy/collect.rs, crates/polint/src/eval/report.rs, crates/polint/src/eval/observed.rs, crates/polint/tests/cli.rs</files>
  <read_first>
    - crates/polint/src/analysis/unknown_taxonomy/collect.rs
    - crates/polint/src/analysis/unknown_taxonomy/facts.rs
    - crates/polint/src/eval/report.rs
    - crates/polint/src/eval/observed.rs
    - crates/polint/tests/cli.rs
  </read_first>
  <action>
    Update unknown taxonomy collection and eval observed rows so budget-exceeded rows include the stable reason string and affected family/provider. Keep the public `inspect unknowns` JSON compatible by using existing optional fields or existing reason/status fields; do not add a new public budget command.
  </action>
  <verify>
    <automated>cargo test -p polint --lib unknown_taxonomy eval:: && cargo test -p polint --test cli inspect_unknowns 2>&1 | tail -120</automated>
  </verify>
  <acceptance_criteria>
    - `polint inspect unknowns --format json` includes budget rows with a stable `reason` for budget-exceeded cases.
    - Eval solver metrics include deterministic budget-exceeded reasons.
    - Existing unknowns schema tests remain compatible.
    - No new public CLI command is added for budget inspection.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Risk | Mitigation |
|----------|------|------------|
| Budget evidence | A broad `BudgetExceeded` signal is not actionable. | Attach private stable reason strings to solver/eval/unknown rows. |
| Public surface | Budget helper types could become public. | Keep vocabulary crate-private and reuse existing unknown/report outputs. |
| Precision | Edges after budget exhaustion could look complete. | Preserve run-level status and reason; do not claim exact precision after caps. |
</threat_model>

<verification>
- `cargo test -p polint eval::go_rta eval::ts_tokens eval::ts_object_model`
- `cargo test -p polint --lib unknown_taxonomy`
- `cargo test -p polint --test cli inspect_unknowns`
</verification>

<success_criteria>
- Budget exhaustion has stable, specific, private reasons.
- All relevant solver/adaptation budget lanes report honest budget evidence.
- Unknown taxonomy and eval reports expose budget reasons without new public APIs.
</success_criteria>
