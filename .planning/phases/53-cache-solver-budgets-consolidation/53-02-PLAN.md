---
phase: 53-cache-solver-budgets-consolidation
plan: 02
type: execute
wave: 2
depends_on: [53-01]
files_modified:
  - crates/polint/tests/cache_invalidation_v13.rs
  - crates/polint/src/analysis_kernel/incremental/
  - crates/polint/src/go/semantic/cache_key.rs
  - crates/polint/src/analysis/adaptation/cache_key.rs
  - crates/polint/src/analysis/solver/cache_key.rs
  - tests/eval-fixtures/adaptation-model/
autonomous: true
requirements: [CACHE-01, CACHE-02]

must_haves:
  truths:
    - "D-06/D-07/D-08/D-09/D-10: cache proof uses paired positive and negative fixtures that assert observable cache behavior, not digest inequality alone."
    - "D-14/D-15: solver budget and normalized effective-budget changes invalidate only behavior-affecting downstream output."
    - "Roadmap SC2: single-input-mutation fixtures prove must-invalidate and must-preserve-hit behavior."
  artifacts:
    - path: "crates/polint/tests/cache_invalidation_v13.rs"
      provides: "cross-family must-invalidate and must-preserve-hit integration proof"
      contains: "must_invalidate"
    - path: "tests/eval-fixtures/adaptation-model/"
      provides: "model file mutation and reorder proof"
      contains: "model.polint-model.toml"
---

<objective>
Add Phase 53 cache invalidation fixtures that mutate one relevant input at a time, prove affected downstream cache layers recompute, and prove irrelevant/no-op changes preserve cache hits.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/53-cache-solver-budgets-consolidation/53-CONTEXT.md
@.planning/phases/53-cache-solver-budgets-consolidation/53-01-PLAN.md
@crates/polint/src/analysis_kernel/incremental/
@crates/polint/src/go/semantic/cache_key.rs
@crates/polint/src/analysis/adaptation/cache_key.rs
@crates/polint/src/analysis/solver/cache_key.rs
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add must-invalidate integration fixture harness</name>
  <files>crates/polint/tests/cache_invalidation_v13.rs</files>
  <read_first>
    - crates/polint/tests/layer_cache.rs
    - crates/polint/src/analysis_kernel/incremental/mod.rs
    - crates/polint/src/analysis_kernel/mod.rs
    - crates/polint/src/config/mod.rs
  </read_first>
  <action>
    Create `crates/polint/tests/cache_invalidation_v13.rs` or extend the closest existing cache integration test. The harness must run a temp repo twice with cache enabled, capture provider cache stats or layer manifests, mutate one input, rerun, and assert the targeted provider has a miss/recompute/write while unaffected cheap layers preserve hits where expected.
  </action>
  <verify>
    <automated>cargo test -p polint --test cache_invalidation_v13 -- --nocapture 2>&1 | tail -120</automated>
  </verify>
  <acceptance_criteria>
    - The test file contains a helper that captures provider cache stats or layer manifest digest changes.
    - A baseline warm rerun proves at least one relevant provider hit before mutation.
    - A mutation rerun proves at least one targeted provider miss/recompute/write after mutation.
    - The fixture uses temp repos or existing small eval fixtures, not external corpora.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Cover Go sidecar and lifecycle cache lanes</name>
  <files>crates/polint/tests/cache_invalidation_v13.rs, crates/polint/src/go/semantic/cache_key.rs</files>
  <read_first>
    - crates/polint/src/go/semantic/cache_key.rs
    - crates/polint/src/go/semantic/process.rs
    - crates/polint/src/go/semantic/provider.rs
    - crates/polint/src/go/lifecycle.rs
  </read_first>
  <action>
    Add positive cases for Go sidecar digest, Go toolchain or `x_tools_version`, and lifecycle config (`build_tags`, `package_patterns`, `include_tests`) affecting `polint.go.semantic` and downstream solver/refined-call outputs. Add a negative case proving an intentionally irrelevant lifecycle field such as `files_without_module_root` preserves the digest/hit when the effective analysis inputs are unchanged.
  </action>
  <verify>
    <automated>cargo test -p polint --test cache_invalidation_v13 go_semantic 2>&1 | tail -100</automated>
  </verify>
  <acceptance_criteria>
    - Changing `build_tags` changes the Go semantic cache input and invalidates the Go semantic lane.
    - Changing package patterns or include-tests changes the Go semantic cache input.
    - Changing `files_without_module_root` alone preserves the Go semantic digest as documented.
    - Downstream solver/refined-call cache behavior changes only when the upstream Go semantic output changes.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Cover solver and adaptation cache lanes</name>
  <files>crates/polint/tests/cache_invalidation_v13.rs, crates/polint/src/analysis/solver/cache_key.rs, crates/polint/src/analysis/adaptation/cache_key.rs, tests/eval-fixtures/adaptation-model/</files>
  <read_first>
    - crates/polint/src/analysis/solver/cache_key.rs
    - crates/polint/src/analysis/adaptation/cache_key.rs
    - crates/polint/src/analysis/adaptation/store.rs
    - crates/polint/src/config/mod.rs
    - tests/eval-fixtures/adaptation-model/accepted-model/model.polint-model.toml
  </read_first>
  <action>
    Add positive cases for solver Go/JS/object/adaptation budget changes and adaptation model file content changes. Add negative cases for normalized no-op budget config and deterministic model fact/file reordering. Assert effective budget digests, model digests, semantic graph constraints, solver output, and refined calls invalidate only when behavior can change.
  </action>
  <verify>
    <automated>cargo test -p polint --test cache_invalidation_v13 solver adaptation 2>&1 | tail -120</automated>
  </verify>
  <acceptance_criteria>
    - Changing `solver.js.max_tokens_per_var` invalidates solver output.
    - Changing `solver.js.object_model` or an object sub-budget invalidates solver output when object model behavior can change.
    - Changing an accepted model target changes the adaptation model digest and downstream semantic graph/solver output.
    - Reordering equivalent model facts preserves the adaptation model digest.
    - A zero/no-op solver cap that normalizes to default preserves the effective budget digest.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Risk | Mitigation |
|----------|------|------------|
| Cache test reliability | Digest-only tests can pass while production cache stats are stale. | Assert observable hit/miss/recompute/write behavior where available. |
| Fixture scope | External corpus dependence can make cache proof flaky. | Use temp repos and small native fixtures only. |
| Normalization | Raw TOML changes can cause false invalidation. | Assert effective budget/model normalization, not raw text differences. |
</threat_model>

<verification>
- `cargo test -p polint --test cache_invalidation_v13`
- `cargo test -p polint --lib go::semantic::cache_key adaptation::cache_key solver::cache_key`
</verification>

<success_criteria>
- CACHE-01 and CACHE-02 have positive and negative cache fixtures.
- Relevant single-input mutations invalidate the right downstream layers.
- No-op and deterministic reorder mutations preserve cache hits.
</success_criteria>
