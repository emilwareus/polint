---
phase: 21-provenance-precision-and-validation-metadata
plan: 04
type: execute
wave: 4
depends_on:
  - 21-03
files_modified:
  - crates/polint/src/analysis_kernel/debug.rs
  - crates/polint/src/analysis_kernel/mod.rs
  - crates/polint/tests/cli.rs
autonomous: true
requirements:
  - SAE-FND-02
must_haves:
  truths:
    - "Crate-private/test-facing debug JSON can show provenance for files, imports, symbols, and references."
    - "Debug JSON is deterministic and excludes timestamps, absolute machine paths, nondeterministic map order, and transient memory details."
    - "Metadata remains internal: no SDK view, crate-root export, runner contract, or public CLI output is added."
    - "Existing polint check, SDK fact views, examples, diagnostics rendering, ignore handling, and rule execution remain compatible."
  artifacts:
    - path: "crates/polint/src/analysis_kernel/debug.rs"
      provides: "Deterministic crate-private metadata debug JSON helpers"
      contains: "metadata_debug_json_for_test"
    - path: "crates/polint/src/analysis_kernel/mod.rs"
      provides: "Test-facing debug helper access under analysis kernel"
      contains: "metadata_debug_json_for_test"
    - path: "crates/polint/tests/cli.rs"
      provides: "Behavior-preservation proof for public check output after metadata validation"
      contains: "kernel_metadata_preserves_public_check_behavior"
  key_links:
    - from: "crates/polint/src/analysis_kernel/debug.rs"
      to: "crates/polint/src/core/mod.rs"
      via: "reads AnalysisDb facts and FactMetaStore"
      pattern: "fact_meta"
    - from: "crates/polint/src/analysis_kernel/debug.rs"
      to: "serde_json"
      via: "deterministic metadata report serialization"
      pattern: "serde_json"
    - from: "crates/polint/src/lib.rs"
      to: "public API boundary"
      via: "absence of public metadata exports"
      pattern: "pub\\(crate\\) mod analysis_kernel"
---

<objective>
Add deterministic internal provenance debug JSON and prove public behavior remains unchanged.

Purpose: satisfy the Phase 21 debug JSON success criterion without promoting a stable CLI or SDK contract, and close compatibility coverage for SAE-FND-02.
Output: crate-private/test-facing metadata debug JSON for files, imports, symbols, and references plus tests proving determinism, path hygiene, no public exposure, and existing `polint check` behavior.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-provenance-precision-and-validation-metadata/21-CONTEXT.md
@AGENTS.md
@docs/API-VISIBILITY-PLAN.md
@.agents/skills/rust-best-practices/SKILL.md
@.claude/skills/polint/SKILL.md
@research/analysis-kernel/FINAL-REPORT.md
@research/analysis-kernel/RECOMMENDED_IMPLEMENTATION.md
@research/semantic-index/FINAL-REPORT.md
@.planning/phases/20-private-analysis-kernel-facade/20-01-SUMMARY.md
@.planning/phases/20-private-analysis-kernel-facade/20-02-SUMMARY.md
@.planning/phases/21-provenance-precision-and-validation-metadata/21-01-SUMMARY.md
@.planning/phases/21-provenance-precision-and-validation-metadata/21-02-SUMMARY.md
@.planning/phases/21-provenance-precision-and-validation-metadata/21-03-SUMMARY.md
@crates/polint/src/analysis_kernel/mod.rs
@crates/polint/src/core/mod.rs
@crates/polint/src/lib.rs
@crates/polint/src/sdk/mod.rs
@crates/polint/src/runner/mod.rs
@crates/polint/tests/cli.rs

<interfaces>
Existing public module boundary in `crates/polint/src/lib.rs`:
```rust
pub mod runner;
pub mod sdk;
pub(crate) mod analysis_kernel;
```

Debug JSON fields required by this plan for every metadata row:
```text
family, run_id, stable_key, producer_id, layer_id, precision, confidence, validation
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic crate-private provenance debug JSON</name>
  <files>crates/polint/src/analysis_kernel/debug.rs, crates/polint/src/analysis_kernel/mod.rs</files>
  <read_first>
    <file>.planning/phases/21-provenance-precision-and-validation-metadata/21-CONTEXT.md</file>
    <file>crates/polint/src/analysis_kernel/mod.rs</file>
    <file>crates/polint/src/analysis_kernel/metadata.rs</file>
    <file>crates/polint/src/core/mod.rs</file>
    <file>crates/polint/src/diagnostics/mod.rs</file>
  </read_first>
  <behavior>
    - Test 1: debug JSON for a kernel run with a TS file and imported symbol contains non-empty `files`, `imports`, `symbols`, and `references` arrays.
    - Test 2: every debug row includes `family`, `run_id`, `stable_key`, `producer_id`, `layer_id`, `precision`, `confidence`, and `validation`.
    - Test 3: serializing the same database twice returns byte-identical pretty JSON.
    - Test 4: debug JSON does not contain the temp root absolute path, `SystemTime`, `0x`, pointer-like strings, or timestamp fields.
  </behavior>
  <action>Create `crates/polint/src/analysis_kernel/debug.rs` and register it in `analysis_kernel/mod.rs` with `#[cfg(test)] mod debug;`. Add `#[cfg(test)] pub(crate) fn metadata_debug_json_for_test(db: &AnalysisDb) -> serde_json::Value` on `AnalysisKernel`. The report must contain exactly these top-level keys in deterministic order when serialized from typed structs: `files`, `imports`, `symbols`, and `references`. Each row must include metadata fields `family`, `run_id`, `stable_key`, `producer_id`, `layer_id`, `precision`, `confidence`, and `validation`; file rows must include `path`, `language`, and `content_hash`; import rows must include `path`, `language`, `import_path`, `span`; symbol rows must include `name`, `qualified_name`, `kind`, `namespace`, `path`, `span`, and existing `precision`; reference rows must include `name`, `qualified_name`, `status`, `target`, `candidates`, `path`, `span`, and existing `precision`. Sort arrays by relative path, span start byte, name, stable key, and run id. Use relative paths from `SourceFile.relative_path`; never include `SourceFile.path`, timestamps, process ids, pointer formatting, or memory addresses. Keep the helper crate-private and test-facing per D-03, D-18, D-19, and D-20.</action>
  <acceptance_criteria>
    <criterion>`rg -n "#\\[cfg\\(test\\)\\] mod debug;" crates/polint/src/analysis_kernel/mod.rs` returns one match.</criterion>
    <criterion>`rg -n "metadata_debug_json_for_test" crates/polint/src/analysis_kernel/debug.rs crates/polint/src/analysis_kernel/mod.rs` returns matches under `#[cfg(test)]` only.</criterion>
    <criterion>`rg -n "files|imports|symbols|references|producer_id|stable_key|confidence|validation" crates/polint/src/analysis_kernel/debug.rs` returns debug row fields.</criterion>
    <criterion>`rg -n "SystemTime|Instant|as_ptr|{:p}|SourceFile\\.path" crates/polint/src/analysis_kernel/debug.rs` returns no matches.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p polint --lib metadata_debug --locked</automated>
  </verify>
  <done>Test-facing debug JSON explains provenance for files, imports, symbols, and references deterministically without exposing machine-local or transient details.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove no public exposure and public behavior compatibility</name>
  <files>crates/polint/src/analysis_kernel/debug.rs, crates/polint/src/analysis_kernel/mod.rs, crates/polint/tests/cli.rs</files>
  <read_first>
    <file>.planning/phases/21-provenance-precision-and-validation-metadata/21-CONTEXT.md</file>
    <file>AGENTS.md</file>
    <file>docs/API-VISIBILITY-PLAN.md</file>
    <file>.claude/skills/polint/SKILL.md</file>
    <file>crates/polint/src/lib.rs</file>
    <file>crates/polint/src/sdk/mod.rs</file>
    <file>crates/polint/src/runner/mod.rs</file>
    <file>crates/polint/src/analysis_kernel/mod.rs</file>
    <file>crates/polint/tests/cli.rs</file>
  </read_first>
  <behavior>
    - Test 1: a structural test asserts metadata/debug helpers are absent from `crates/polint/src/lib.rs`, `crates/polint/src/sdk`, and `crates/polint/src/runner`.
    - Test 2: a temp-repo external rule using only `polint::sdk::prelude::*` still runs through `polint check --format json --fail-on none` and receives existing symbol/reference or metric facts without metadata fields in public JSON.
    - Test 3: repeated `polint check --format json --fail-on none` output remains byte-identical for the same temp repo.
  </behavior>
  <action>Add a lib test near the debug helper asserting `analysis_kernel` remains `pub(crate)` in `crates/polint/src/lib.rs` and that new metadata-only internals such as `FactMeta`, `FactMetaStore`, `metadata_debug_json_for_test`, provenance metadata helpers, `producer_id`, `layer_id`, `confidence`, and `validation` are not exported through `sdk::prelude` or `runner`. Do not assert that existing public `stable_key` fields are absent from the SDK; D-04 and D-21 require preserving compatibility for existing symbol/definition/reference fact views that intentionally expose stable keys. Add a CLI integration test named `kernel_metadata_preserves_public_check_behavior` in `crates/polint/tests/cli.rs`. The fixture must create a temp repo with a local rule importing only `polint::sdk::prelude::*`, request an existing public view such as `Symbols<'_>` and `References<'_>` or the metric views, run `polint check --format json --fail-on none` twice, assert byte-identical stdout, assert no `polint/internal` diagnostics appear on a clean repo, and assert the public JSON report does not contain metadata-only keys `producer_id`, `layer_id`, `confidence`, or `validation`. This implements D-03, D-04, D-18 through D-22, and keeps Phase 25 inspect/test public CLI work out of scope per D-23 and D-24.</action>
  <acceptance_criteria>
    <criterion>`rg -n "kernel_metadata_preserves_public_check_behavior" crates/polint/tests/cli.rs` returns one match.</criterion>
    <criterion>`rg -n "metadata_debug_json_for_test" crates/polint/src/lib.rs crates/polint/src/sdk crates/polint/src/runner` returns no matches.</criterion>
    <criterion>`rg -n "producer_id|layer_id|confidence|validation" crates/polint/tests/cli.rs` returns assertions that these keys are absent from public JSON output.</criterion>
    <criterion>Existing public `stable_key` fields on symbol, definition, and reference facts remain source-compatible for rules importing `polint::sdk::prelude::*`.</criterion>
    <criterion>`rg -n "pub\\(crate\\) mod analysis_kernel" crates/polint/src/lib.rs` returns one match and `rg -n "pub mod analysis_kernel" crates/polint/src/lib.rs` returns no matches.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p polint --lib metadata_debug_helpers_are_not_public --locked</automated>
    <automated>cargo test -p polint --test cli kernel_metadata_preserves_public_check_behavior --locked</automated>
    <automated>cargo test -p polint --test cli kernel_delegation_preserves_existing_rule_facts --locked</automated>
  </verify>
  <done>Metadata debug inspection remains internal/test-facing, public `polint check` output stays deterministic and metadata-free, and existing rule-author flows remain compatible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AnalysisDb -> debug JSON | Internal metadata is serialized for tests and must remain deterministic and path-safe. |
| Internal helpers -> public API | Debug/provenance helpers must not leak into SDK, runner, crate-root public modules, or public CLI output. |
| Public check JSON -> users/CI | Existing public JSON output must remain compatible and must not include internal metadata fields. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04-01 | Information Disclosure | metadata debug JSON | mitigate | Serialize relative paths only and test absence of temp roots, timestamps, pointer-like strings, and transient runtime details. |
| T-21-04-02 | Tampering | debug row ordering | mitigate | Sort debug arrays by stable relative path/span/name/stable key/run id before serialization. |
| T-21-04-03 | Spoofing | public API boundary | mitigate | Structural tests assert metadata helpers are absent from `lib.rs`, `sdk`, and `runner`. |
| T-21-04-04 | Repudiation | public check compatibility | mitigate | CLI test runs the same temp repo twice and asserts byte-identical public JSON with no metadata-only keys. |
</threat_model>

<verification>
Run the automated commands from each task, then run:
```bash
cargo test -p polint --lib analysis_kernel --locked
cargo test --workspace --all-features --locked
cargo clippy --workspace --all-targets --all-features --locked -- -D warnings
cargo fmt --all -- --check
```
</verification>

<success_criteria>
Debug JSON covers files, imports, symbols, and references per D-06, D-18, D-19, and D-20; metadata remains internal per D-03; existing public behavior remains compatible per D-21; no deferred public inspect/test/cache/extension work from D-23 or D-24 is implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/21-provenance-precision-and-validation-metadata/21-04-SUMMARY.md`.
</output>
