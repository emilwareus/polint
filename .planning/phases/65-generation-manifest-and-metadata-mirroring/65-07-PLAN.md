---
phase: 65-generation-manifest-and-metadata-mirroring
plan: 07
type: execute
wave: 7
depends_on: [65-06]
files_modified:
  - crates/polint/src/analysis_kernel/incremental/keys.rs
  - crates/polint/src/analysis_kernel/mod.rs
  - crates/polint/src/go/adapter.rs
  - crates/polint/src/go/tests.rs
  - crates/polint/src/ts/adapter.rs
  - crates/polint/src/ts/tests.rs
  - crates/polint/src/module_graph/mod.rs
  - crates/polint/src/symbol_graph/mod.rs
  - crates/polint/src/metrics.rs
  - crates/polint/src/analysis/extensions/cache_key.rs
autonomous: true
requirements: [META-01, META-04]
must_haves:
  truths:
    - "D-06/D-14: syntax, module graph/topology, symbol graph, metrics, and extension LayerKeys use provider-scoped analysis settings; remaining semantic providers migrate in Plans 08-09."
    - "D-14: Go/TS syntax layer and file-cache hits survive severity/files/allow/max/custom-option changes."
    - "D-15: relevant setting/capability/upstream mutations invalidate while unreferenced siblings preserve hits."
  artifacts:
    - path: "crates/polint/src/analysis_kernel/incremental/keys.rs"
      provides: "LayerKey with purpose-specific analysis settings"
      contains: "analysis_settings_digest"
---

<objective>
Migrate the first production LayerKey slice and syntax cache identity from full config/rule inputs to declared provider analysis dependencies.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
@.agents/skills/rust-best-practices/SKILL.md
@.claude/skills/polint/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-CONTEXT.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-PATTERNS.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-06-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Migrate LayerKey and graph/metric producers to scoped settings</name>
  <files>crates/polint/src/analysis_kernel/incremental/keys.rs, crates/polint/src/analysis_kernel/mod.rs, crates/polint/src/module_graph/mod.rs, crates/polint/src/symbol_graph/mod.rs, crates/polint/src/metrics.rs, crates/polint/src/analysis/extensions/cache_key.rs</files>
  <read_first>
    - .agents/skills/rust-best-practices/SKILL.md
    - .claude/skills/polint/SKILL.md
    - crates/polint/src/analysis_kernel/incremental/keys.rs (every LayerKey constructor/test, including from_existing_file_cache)
    - crates/polint/src/analysis_kernel/incremental/input_snapshot.rs (v2 analysis settings/capability accessors)
    - crates/polint/src/analysis_kernel/mod.rs (all production provider calls and upstream digests)
    - crates/polint/src/module_graph/mod.rs, crates/polint/src/symbol_graph/mod.rs, crates/polint/src/metrics.rs (all key/output/edge builders)
    - crates/polint/src/analysis/extensions/cache_key.rs (extension options currently occupying the config slot)
  </read_first>
  <action>
    Add the analysis_settings_digest semantic accessor/constructor path and require DigestKind::AnalysisSettings in the syntax, module graph/topology, symbol graph, metrics, and extension specialized paths migrated here. Keep explicit temporary Digest seams for semantic MIR/CFG/calls/domains/direct-summary callers until Plans 08-09 and for core/dependency-only general-constructor callers until Plans 10-11; enumerate each seam in source tests so a full suite remains green. Extension options enter only through the declared extension analysis-settings projection, not raw RuleOptions. Audit from_existing_file_cache: because rg finds no production caller, remove it and its compatibility test, or require an explicit scoped analysis-settings input and exclude CacheKey.rule_hash/full config; do not leave a translator that can smuggle old full keys into LayerKey. Do not pass full InputSnapshot.config through an upstream output as an indirect substitute.

    For module/symbol/metrics output and dependency builders, use the same scoped value in both LayerKey and typed dependency metadata. Relevant source/lifecycle/provider/upstream/settings mutations change keys; unrelated provider settings and all rule-only fields preserve keys. Update every LayerKey fixture and source assertion.
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis_kernel::incremental::keys::tests --locked &amp;&amp; cargo test -p polint --lib module_graph::tests --locked &amp;&amp; cargo test -p polint --lib symbol_graph::tests --locked &amp;&amp; cargo test -p polint --lib metrics::tests --locked</automated>
  </verify>
  <done>The syntax/graph/metrics/extension LayerKey slice uses scoped settings and all remaining seams are explicitly bounded.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve syntax hits across rule-only changes</name>
  <files>crates/polint/src/analysis_kernel/mod.rs, crates/polint/src/go/adapter.rs, crates/polint/src/go/tests.rs, crates/polint/src/ts/adapter.rs, crates/polint/src/ts/tests.rs</files>
  <read_first>
    - crates/polint/src/go/adapter.rs and crates/polint/src/ts/adapter.rs (layer and legacy file CacheKey construction)
    - crates/polint/src/go/tests.rs and crates/polint/src/ts/tests.rs (cold/warm/corrupt cache matrices)
  </read_first>
  <action>
    Route production Go/TS syntax calls to their scoped settings digest and rename cache-identity parameters/locals from config_hash to analysis_settings_hash so the old full-config meaning cannot be inferred. Remove rule_hash and full plan digest from syntax LayerKey and file-fact CacheKey identity; keep compatibility parameters only if an existing private wrapper needs them, mark them non-identity, and close the seam by source tests. Parser/source/tool/language changes still invalidate.

    Add real cold/warm tests: severity, files, allow_files, max, and custom settings changes preserve Go/TS hits when capabilities/settings/models/extensions are unchanged; relevant parser setting, capability, model/extension, and upstream changes invalidate. Add an unreferenced capability/setting sibling preserve control. Plan 09 migrates and tests the separate SCC cache key after the remaining provider outputs are scoped.
  </action>
  <verify>
    <automated>cargo test -p polint --lib go::tests --locked &amp;&amp; cargo test -p polint --lib ts::tests --locked &amp;&amp; cargo test -p polint --lib analysis::extensions::cache_key::tests --locked &amp;&amp; cargo check -p polint --all-features --locked</automated>
  </verify>
  <acceptance_criteria>
    - Every LayerKey in this plan's syntax/graph/metrics/extension slice uses DigestKind::AnalysisSettings; remaining semantic and general-constructor seams are enumerated for Plans 08-11.
    - from_existing_file_cache is removed when still unused, or cannot consume CacheKey.rule_hash/full config as LayerKey identity.
    - No production LayerKey producer consumes input_snapshot.config.digest.
    - Go/TS LayerKey and file-fact CacheKey construction names/uses analysis_settings_hash and never the full config_hash.
    - Syntax cache identity contains no rule_hash, full plan digest, severity, files, max, or arbitrary rule settings.
    - Rule-only production mutations preserve hits; requested relevant capability/settings/model/extension/upstream mutations invalidate.
    - Unrelated capability/settings siblings preserve hits.
    - Full config/rule identities remain present in InputSnapshot/run metadata for diagnostics/store manifests.
  </acceptance_criteria>
  <done>Primary layer and syntax caches implement D-14 with production hit/invalidation tests.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description | Data Crossing |
|---|---|---|
| Snapshot identities to LayerKey | Full and scoped digests are both available at key construction | Config identity, provider settings, capabilities |
| Rule execution to parser/layer caches | Rule changes coexist with reusable analysis outputs | Rule code/options, parser facts, provider layers |

## Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-65-07-01 | Tampering | LayerKey construction | mitigate | Purpose-check analysis_settings_digest and source-audit every real producer. |
| T-65-07-02 | Denial of Service | Syntax/layer caches | mitigate | Production preserve-hit tests exclude rule-only identity from analysis caches. |
| T-65-07-03 | Spoofing | Layer dependency identity | mitigate | Canonical declared capability/settings/model/extension/upstream inputs replace opaque plan hashes. |
| T-65-07-04 | Information Disclosure | Cache keys | mitigate | Use typed digests and repo-relative keys; never store source bodies or environment values. |
</threat_model>

<verification>
- rg -n 'AnalysisSettings|analysis_settings' crates/polint/src/analysis_kernel/incremental/keys.rs crates/polint/src/go crates/polint/src/ts crates/polint/src/module_graph crates/polint/src/symbol_graph crates/polint/src/metrics.rs crates/polint/src/analysis/extensions/cache_key.rs
- cargo check -p polint --all-features --locked
- cargo fmt --all -- --check
</verification>

<success_criteria>
- This first layer/syntax slice preserves analysis hits across rule-only changes and leaves only enumerated compile-safe seams for Plans 08-11.
</success_criteria>

<output>Create 65-07-SUMMARY.md when complete.</output>
