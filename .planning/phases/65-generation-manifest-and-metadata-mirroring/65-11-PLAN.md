---
phase: 65-generation-manifest-and-metadata-mirroring
plan: 11
type: execute
wave: 11
depends_on: [65-10]
files_modified:
  - crates/polint/src/analysis_kernel/incremental/dependency_input.rs
  - crates/polint/src/analysis_kernel/incremental/change_set.rs
  - crates/polint/src/analysis_kernel/incremental/dependency_index.rs
  - crates/polint/src/analysis_kernel/incremental/invalidation.rs
  - crates/polint/src/analysis_kernel/incremental/layer_cache.rs
  - crates/polint/src/analysis_kernel/incremental/quarantine.rs
  - crates/polint/src/analysis_kernel/incremental/keys.rs
  - crates/polint/src/metrics.rs
  - crates/polint/src/module_graph/mod.rs
  - crates/polint/src/symbol_graph/mod.rs
autonomous: true
requirements: [META-01, META-04]
must_haves:
  truths:
    - "D-11/D-12/D-13: every production dependency producer emits typed endpoints and one canonical edge truth."
    - "The first serde-visible CacheNode::DependencyInput shape and polint-dependency-index-next-typed label land atomically; polint-dependency-index-1 never writes a typed node."
    - "Legacy String-bearing CacheNode input/extension/tool variants are removed only after all producers and core consumers migrate in this same compile-clean plan."
    - "D-14: rule code/options remain diagnostic dependencies; requested capabilities/settings/models/extensions reach only linked analysis layers."
    - "D-15: typed edge serialization is deterministic; v1 and superseded temporary layer manifests fail closed to recompute until the final schema is published after QueryKey is complete."
  artifacts:
    - path: "crates/polint/src/module_graph/mod.rs"
      provides: "Typed module graph/topology dependency producers"
      contains: "InputDependencyKey"
    - path: "crates/polint/src/analysis_kernel/incremental/dependency_index.rs"
      provides: "Typed-only dependency index behind a unique pre-QueryDependencyInputs temporary wire label"
      contains: "polint-dependency-index-next-typed"
    - path: "crates/polint/src/analysis_kernel/incremental/dependency_input.rs"
      provides: "Serde support added only with the first typed CacheNode wire shape"
      contains: "Serialize"
  key_links:
    - from: "crates/polint/src/analysis_kernel/incremental/dependency_input.rs"
      to: "crates/polint/src/analysis_kernel/incremental/dependency_index.rs"
      via: "InputDependencyKey becomes serde-visible only as CacheNode::DependencyInput under the temporary typed schema label"
      pattern: "DependencyInput|polint-dependency-index-next-typed"
    - from: "crates/polint/src/module_graph/mod.rs"
      to: "crates/polint/src/analysis_kernel/incremental/dependency_index.rs"
      via: "real source/config/lifecycle/provider inputs emit typed CacheNode endpoints into the canonical edge vector"
      pattern: "CacheNode::DependencyInput|InputDependencyKey"
    - from: "crates/polint/src/analysis_kernel/incremental/layer_cache.rs"
      to: "crates/polint/src/analysis_kernel/incremental/dependency_index.rs"
      via: "manifest write/read pin follows the central temporary schema constant and rejects v1 typed-node claims"
      pattern: "DEPENDENCY_INDEX_SCHEMA"
---

<objective>
Migrate all real dependency producers, then remove legacy variants behind a unique temporary wire label at one compile-clean boundary; defer the stable v2 publication until QueryKey reaches its final shape.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
@.agents/skills/rust-best-practices/SKILL.md
@.claude/skills/polint/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-CONTEXT.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-PATTERNS.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-10-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Migrate producers, remove legacy variants, and stage the typed wire shape</name>
  <files>crates/polint/src/analysis_kernel/incremental/dependency_input.rs, crates/polint/src/analysis_kernel/incremental/change_set.rs, crates/polint/src/analysis_kernel/incremental/dependency_index.rs, crates/polint/src/analysis_kernel/incremental/invalidation.rs, crates/polint/src/analysis_kernel/incremental/layer_cache.rs, crates/polint/src/analysis_kernel/incremental/quarantine.rs, crates/polint/src/analysis_kernel/incremental/keys.rs, crates/polint/src/metrics.rs, crates/polint/src/module_graph/mod.rs, crates/polint/src/symbol_graph/mod.rs</files>
  <read_first>
    - .agents/skills/rust-best-practices/SKILL.md
    - .claude/skills/polint/SKILL.md
    - crates/polint/src/analysis_kernel/incremental/dependency_input.rs (Plan 10 non-wire typed constructors/codecs and absence of serde)
    - crates/polint/src/module_graph/mod.rs (every production and test CacheNode input)
    - crates/polint/src/symbol_graph/mod.rs (every production input)
    - crates/polint/src/metrics.rs (every production input)
    - crates/polint/src/analysis_kernel/incremental/keys.rs (general LayerKey constructor purpose seam)
    - crates/polint/src/analysis_kernel/incremental/mod.rs (read-only typed-input and DEPENDENCY_INDEX_SCHEMA re-exports)
    - crates/polint/tests/cli.rs (read-only private vocabulary baseline; final typed/schema pin remains Plan 15-owned)
    - crates/polint/src/analysis_kernel/incremental/dependency_index.rs, change_set.rs, invalidation.rs, quarantine.rs, and layer_cache.rs (all legacy matches/fixtures)
  </read_first>
  <action>
    Atomically introduce the first serde-visible typed-node shape: add serde derives/coverage to the Plan 10 InputDependencyKey vocabulary, add CacheNode::DependencyInput(InputDependencyKey), and change DEPENDENCY_INDEX_SCHEMA from polint-dependency-index-1 to exactly polint-dependency-index-next-typed in the same plan boundary before any typed node can be written. Add CacheNodeKind plus exact label/parse codecs for CacheNodeKind, DependencyKind, ShapeKind, and ChangeKind; never use Debug, wildcard fallback, prefix parsing, or store-only mapping. Extend change-set validation, invalidation, quarantine, layer-cache sentinel handling, canonical edge construction, and every exhaustive match for the typed variant. Typed input digests must agree with ChangeSet rows.

    Then migrate every source/config/lifecycle/provider/provider-schema/toolchain/topology/import/upstream/rule endpoint in module_graph, symbol_graph, and metrics to CacheNode::DependencyInput with the exact InputDependencyKind, normalized repo-relative stable key, typed digest, and Present/Absent/Unsupported/SetupMissing state already available from InputSnapshot/LayerKey. Use a dedicated typed manifest-relative sentinel; do not parse old prefixes. Update all producer pattern tests and prove ordering across at least 20 provider/source permutations.

    Migrate module_graph/symbol_graph/metrics dependency-only LayerKey::new calls to DigestKind::AnalysisSettings, then make the general constructor purpose-strict so no Config/RuleOptions digest can occupy analysis_settings_digest. After repository-wide rg shows zero production or fixture construction of CacheNode::Input(String), Extension(String), and ToolInvocation(String), remove those three variants. Simplify all core exhaustive matches to typed-only handling. Serialize one canonical edge vector and reconstruct forward/reverse indexes; never persist two writable truths.

    Do not publish polint-dependency-index-2 in this plan because serde-visible CacheNode::Query still embeds the pre-QueryDependencyInputs QueryKey shape. The exact private temporary label polint-dependency-index-next-typed names only this typed/pre-query-declarations shape. Audit every consumer: dependency_index.rs owns construction/serialization, layer_cache.rs writes and validates the manifest pin, invalidation.rs rejects mismatched schemas, and incremental/mod.rs is a read-only re-export. Make internal current-label tests consume DEPENDENCY_INDEX_SCHEMA rather than duplicate a stable literal. Add typed-node round-trip coverage that asserts every newly serialized index/manifest carries polint-dependency-index-next-typed, plus explicit polint-dependency-index-1 fixtures (including a forged v1 label over typed-node JSON) that fail closed before the typed node can be accepted as v1. No v1 compatibility decoder, label rewrite, or prefix parser remains. Do not modify or pin crates/polint/tests/cli.rs to a temporary label; Plan 15 owns the final CLI v2 vocabulary/schema update. Temporary manifests are accepted only by this exact intermediate shape; Plan 14 rotates to a different temporary label when QueryKey changes, and Plan 15 publishes the single final v2 label with every pin updated.

    RuleCode/RuleOptions endpoints connect only to Diagnostic nodes. RequestedCapability/AnalysisSetting/Model/Extension endpoints connect to an analysis layer only when the provider declares them. Finish with repository-wide producer/schema audits, focused serde/fail-closed tests, and cargo check.
  </action>
  <verify>
    <automated>cargo test -p polint --lib module_graph::tests --locked &amp;&amp; cargo test -p polint --lib symbol_graph::tests --locked &amp;&amp; cargo test -p polint --lib metrics::tests --locked &amp;&amp; cargo test -p polint --lib analysis_kernel::incremental::dependency_index::tests --locked &amp;&amp; cargo test -p polint --lib analysis_kernel::incremental::layer_cache::tests --locked &amp;&amp; cargo test -p polint --lib analysis_kernel::incremental::invalidation::tests --locked &amp;&amp; cargo test -p polint --test cli --locked -- --test-threads=1 &amp;&amp; ! rg -n 'CacheNode::(Input|Extension|ToolInvocation)' crates tests &amp;&amp; rg -n 'DEPENDENCY_INDEX_SCHEMA: &amp;str = "polint-dependency-index-next-typed"' crates/polint/src/analysis_kernel/incremental/dependency_index.rs &amp;&amp; cargo check -p polint --all-features --locked</automated>
  </verify>
  <acceptance_criteria>
    - No String-bearing CacheNode input/extension/tool variant or constructor remains anywhere in the repository.
    - Every real endpoint has exact kind, stable key, digest purpose, and explicit status.
    - DEPENDENCY_INDEX_SCHEMA is exactly polint-dependency-index-next-typed; no source or fixture publishes polint-dependency-index-2 yet.
    - InputDependencyKey receives its first serde support here, and every current typed-node serialization test asserts polint-dependency-index-next-typed; no Plan 10/v1 writer ever serialized a typed node.
    - dependency_index, layer_cache, invalidation, and the incremental re-export are audited; internal current-shape assertions follow the constant, CLI is unchanged and does not pin the temporary label, and explicit/forged v1 fixtures fail closed without compatibility decoding.
    - canonical_edges round-trips identical maps/order under at least 20 permutations.
    - Rule-only edges affect diagnostics; declared capability/settings/model/extension edges affect only linked layers.
    - Module graph, symbol graph, metrics, incremental core, CLI tests, and cargo check all pass together.
  </acceptance_criteria>
  <done>The repository reaches one typed-only dependency vocabulary without a non-compiling intermediate state.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description | Data Crossing |
|---|---|---|
| Provider producers to dependency graph | Concrete run inputs become typed invalidation endpoints | Paths, lifecycle/config digests, capability/settings state |
| v1 manifests to temporary typed reader | Legacy wire data reaches a typed-only decoder | Serialized CacheNode variants and edge maps |

## Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-65-11-01 | Tampering | Producer endpoint classification | mitigate | Exact typed constructors and per-producer mutation tests verify kind/digest/status. |
| T-65-11-02 | Denial of Service | Variant removal | mitigate | Remove only after zero-match producer audit and finish with full cargo check. |
| T-65-11-03 | Spoofing | Legacy or mislabeled serialized nodes | mitigate | The first typed serde shape and temporary label land together; v1 and forged-v1 typed nodes fail closed with no prefix parser or compatibility decoder. |
| T-65-11-04 | Repudiation | Dependency truth | mitigate | One canonical vector and permutation tests reconstruct both traversal directions. |
</threat_model>

<verification>
- ! rg -n 'CacheNode::(Input|Extension|ToolInvocation)' crates tests
- rg -n 'DEPENDENCY_INDEX_SCHEMA|polint-dependency-index' crates/polint/src/analysis_kernel/incremental crates/polint/tests/cli.rs
- cargo test -p polint --lib analysis_kernel::incremental::dependency_index::tests --locked
- cargo test -p polint --lib analysis_kernel::incremental::layer_cache::tests --locked
- cargo test -p polint --lib analysis_kernel::incremental::invalidation::tests --locked
- cargo test -p polint --lib analysis_kernel::incremental --locked
- cargo test -p polint --test cli --locked -- --test-threads=1
- cargo check -p polint --all-features --locked
- cargo fmt --all -- --check
</verification>

<success_criteria>
- All producers and consumers compile on the typed-only temporary vocabulary, no prefix-based legacy path remains, and stable v2 publication is deferred until the final QueryKey shape.
</success_criteria>

<output>Create 65-11-SUMMARY.md when complete.</output>
