---
phase: 65-generation-manifest-and-metadata-mirroring
plan: 05
type: execute
wave: 5
depends_on: [65-04]
files_modified:
  - crates/polint/src/analysis_kernel/incremental/input_snapshot.rs
  - crates/polint/src/analysis/types/provider.rs
  - crates/polint/src/analysis/refined_calls/provider.rs
  - crates/polint/src/analysis/entrypoints/provider.rs
  - crates/polint/src/analysis/reachability/provider.rs
  - crates/polint/src/analysis/domains/provider.rs
  - crates/polint/src/analysis/summaries/provider.rs
  - crates/polint/src/analysis/calls/provider.rs
  - crates/polint/src/analysis/cfg/provider.rs
autonomous: true
requirements: [META-01, META-04]
must_haves:
  truths:
    - "D-06/D-12/D-14: every remaining constructor passes a concrete AnalysisPlan, including all entrypoint, CFG, calls, and summary fixtures."
    - "D-13: no compatibility path can erase requested capability or analysis-setting state."
    - "D-20: the repository compiles cleanly before the v2 field/schema change."
  artifacts:
    - path: "crates/polint/src/analysis_kernel/incremental/input_snapshot.rs"
      provides: "Only the plan-aware run-input constructor"
      contains: "from_run_inputs_with_plan"
---

<objective>
Close every remaining InputSnapshot constructor call and delete the capability-erasing compatibility seam.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
@.agents/skills/rust-best-practices/SKILL.md
@.claude/skills/polint/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-CONTEXT.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-PATTERNS.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-04-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Migrate remaining callers and remove the old constructor</name>
  <files>crates/polint/src/analysis_kernel/incremental/input_snapshot.rs, crates/polint/src/analysis/types/provider.rs, crates/polint/src/analysis/refined_calls/provider.rs, crates/polint/src/analysis/entrypoints/provider.rs, crates/polint/src/analysis/reachability/provider.rs, crates/polint/src/analysis/domains/provider.rs, crates/polint/src/analysis/summaries/provider.rs, crates/polint/src/analysis/calls/provider.rs, crates/polint/src/analysis/cfg/provider.rs</files>
  <read_first>
    - .agents/skills/rust-best-practices/SKILL.md
    - .claude/skills/polint/SKILL.md
    - crates/polint/src/analysis_kernel/incremental/input_snapshot.rs (local calls and temporary constructor)
    - crates/polint/src/analysis/types/provider.rs
    - crates/polint/src/analysis/refined_calls/provider.rs
    - crates/polint/src/analysis/entrypoints/provider.rs (all five calls)
    - crates/polint/src/analysis/reachability/provider.rs
    - crates/polint/src/analysis/domains/provider.rs
    - crates/polint/src/analysis/summaries/provider.rs
    - crates/polint/src/analysis/calls/provider.rs
    - crates/polint/src/analysis/cfg/provider.rs
  </read_first>
  <action>
    Migrate every listed call to from_run_inputs_with_plan using the exact existing plan. Replace inline empty-plan digests with named empty plans only for fixtures that assert no requested capabilities. Any capability-exercising fixture must retain or construct its real non-empty plan.

    Update input_snapshot.rs helpers to borrow AnalysisPlan and add non-empty ordering/status coverage. After repository-wide rg proves every call is plan-aware, delete the cfg(test) digest-only constructor. Do not change the serialized field shape in this plan.
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis_kernel::incremental::input_snapshot::tests --locked &amp;&amp; cargo test -p polint --lib analysis::entrypoints::provider::tests --locked &amp;&amp; cargo test -p polint --lib analysis::calls::provider::tests --locked &amp;&amp; cargo test -p polint --lib analysis::cfg::provider::tests --locked &amp;&amp; cargo test -p polint --lib analysis::refined_calls::provider::tests --locked</automated>
  </verify>
  <acceptance_criteria>
    - Repository-wide rg 'InputSnapshot::from_run_inputs\(' returns zero matches.
    - Every from_run_inputs_with_plan call has a concrete borrowed plan.
    - The old constructor is absent.
    - Empty plans have explicit zero-capability assertions; non-empty fixtures retain requested rows.
    - Focused tests and cargo check -p polint --all-features --locked pass before schema migration.
  </acceptance_criteria>
  <done>No caller or compatibility helper can erase requested capability/settings state.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description | Data Crossing |
|---|---|---|
| Remaining provider tests to snapshot seam | Diverse synthetic plans cross one canonical constructor | Plans, capability states, config/rule identities |

## Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-65-05-01 | Tampering | Compatibility constructor | mitigate | Remove it after a repository-wide zero-match audit. |
| T-65-05-02 | Spoofing | Empty plan fixtures | mitigate | Require named empties plus direct zero-capability assertions. |
| T-65-05-03 | Denial of Service | Partial migration | mitigate | Run all focused provider suites and cargo check at this compile-clean boundary. |
</threat_model>

<verification>
- rg -n 'InputSnapshot::from_run_inputs\(' crates tests
- cargo check -p polint --all-features --locked
- cargo fmt --all -- --check
</verification>

<success_criteria>
- The plan-aware constructor is the only run-input constructor and every caller supplies its real plan.
</success_criteria>

<output>Create 65-05-SUMMARY.md when complete.</output>
