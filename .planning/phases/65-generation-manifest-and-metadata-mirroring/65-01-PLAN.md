---
phase: 65-generation-manifest-and-metadata-mirroring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/polint/src/core/mod.rs
  - crates/polint/src/analysis_kernel/mod.rs
  - crates/polint/src/analysis_kernel/metadata.rs
  - crates/polint/src/analysis_kernel/provider.rs
  - crates/polint/src/analysis_kernel/incremental/mod.rs
  - crates/polint/src/analysis_kernel/incremental/digest.rs
  - crates/polint/src/analysis_kernel/incremental/keys.rs
  - crates/polint/src/analysis_kernel/incremental/input_snapshot.rs
  - crates/polint/src/analysis_kernel/incremental/run_report.rs
  - crates/polint/src/eval/performance.rs
autonomous: true
requirements: [STORE-04, META-01, META-04]
must_haves:
  truths:
    - "D-05/D-09: workspace, full-config, run, generation, fact, provider, key, and language identity is canonical crate-private kernel vocabulary before SQL projection."
    - "D-06/D-10: purpose-specific DigestKind values and existing Digest constructors are used; Debug text, SQLite IDs, and alternate hashes are forbidden."
    - "D-07: durable fact/provider identity excludes FactRef::run_id and retains payload_digest plus every semantic metadata field."
    - "D-14: ConfigIdentity is explicitly the complete manifest/diagnostic identity; provider-scoped analysis-setting identity is a separate contract landed in Plans 03 and 06-09."
    - "D-20: codecs and identities add no SDK, runner, CLI, config, or generated-skill contract."
  artifacts:
    - path: "crates/polint/src/analysis_kernel/incremental/digest.rs"
      provides: "Opaque identities and purpose-separated aggregate digest kinds"
      contains: "WorkspaceIdentity"
    - path: "crates/polint/src/analysis_kernel/metadata.rs"
      provides: "Stable fact rows independent of run-local handles"
      contains: "StableFactMetaRow"
---

<objective>
Establish canonical identities, stable codecs, and run-ID-free semantic fact/provider projections before any relational mirror.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
@.agents/skills/rust-best-practices/SKILL.md
@.claude/skills/polint/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-CONTEXT.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-PATTERNS.md
@docs/API-VISIBILITY-PLAN.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Define canonical identities, codecs, and stable semantic rows</name>
  <files>crates/polint/src/core/mod.rs, crates/polint/src/analysis_kernel/mod.rs, crates/polint/src/analysis_kernel/metadata.rs, crates/polint/src/analysis_kernel/provider.rs, crates/polint/src/analysis_kernel/incremental/mod.rs, crates/polint/src/analysis_kernel/incremental/digest.rs, crates/polint/src/analysis_kernel/incremental/keys.rs, crates/polint/src/analysis_kernel/incremental/input_snapshot.rs, crates/polint/src/analysis_kernel/incremental/run_report.rs, crates/polint/src/eval/performance.rs</files>
  <read_first>
    - .agents/skills/rust-best-practices/SKILL.md
    - .claude/skills/polint/SKILL.md
    - crates/polint/src/core/mod.rs (SDK-reachable Language definition and current labels)
    - crates/polint/src/analysis_kernel/incremental/digest.rs (DigestKind and canonical hashing)
    - crates/polint/src/analysis_kernel/incremental/keys.rs (LayerKind, PrecisionTier, LayerKey, SummaryKey, QueryKey)
    - crates/polint/src/analysis_kernel/incremental/input_snapshot.rs (InputComponentStatus)
    - crates/polint/src/analysis_kernel/provider.rs (manifest enums and labels)
    - crates/polint/src/analysis_kernel/metadata.rs (FactFamily, FactMetaStore, FactRef::run_id)
    - crates/polint/src/analysis_kernel/mod.rs (provider_output_summary_parts)
    - crates/polint/src/analysis_kernel/incremental/run_report.rs (provider_output_digest_from_manifest and precision mapping)
    - crates/polint/src/eval/performance.rs (direct synthetic provider_output_digest_from_manifest caller and kernel_report_with_stats fixture)
  </read_first>
  <action>
    Add opaque crate-private WorkspaceIdentity, ConfigIdentity, RunIdentity, and GenerationIdentity. ConfigIdentity accepts only the complete DigestKind::Config digest and remains the full config/store-manifest identity; do not use it as a provider analysis key. RunIdentity combines workspace, full config, input, and provider-manifest aggregates. GenerationIdentity combines RunIdentity with sorted semantic family aggregates.

    Add distinct digest purposes for workspace, run, generation, input snapshot, analysis settings, analysis requirements, provider manifest/output, layer, summary, query, fact metadata, dependency, and validation event aggregates. Add exhaustive label/parse_label pairs with typed unknown-label errors beside DigestKind, LayerKind, PrecisionTier, InputComponentStatus, ProviderKind, LanguageScope, CachePolicy, PrecisionCeiling, FactFamily, FactPrecision, FactConfidence, ValidationStatus, and core::Language. CachePolicy parsing returns a borrowed canonical view for its schema-bearing variant. Language maps exactly go, typescript, tsx, javascript, jsx, unknown; helpers remain pub(crate) or tighter.

    Add StableFactMetaRow with family, stable key, producer, layer, precision, confidence, validation, and the required payload_digest metadata. FactMetaStore::stable_rows sorts/deduplicates semantically and rejects conflicts. Migrate both provider_output_summary_parts and provider_output_digest_from_manifest in incremental/run_report.rs to stable rows, remove run_id, and keep one canonical precision mapping. Provider output identity must retain payload_digest but never payload bytes.

    Migrate eval/performance.rs in the same boundary because kernel_report_with_stats is a direct caller. Replace its opaque `&[format!("provider={}", manifest.id)]` synthetic digest parts with explicit StableFactMetaRow fixtures that populate family, stable_key, producer, layer, precision, confidence, validation, and payload_digest. The fixture must pass canonical stable rows to provider_output_digest_from_manifest, remain deterministic under row permutation, and change when a semantic row field changes. Do not retain or recreate a String-summary compatibility overload. Plan 02 may adapt this file's later closed-status and demand-trace renderers, but it must preserve this stable-row digest fixture. Curate only crate-private re-exports.
  </action>
  <verify>
    <automated>cargo test -p polint --lib language_stable_codec --locked &amp;&amp; cargo test -p polint --lib analysis_kernel::incremental::digest::tests --locked &amp;&amp; cargo test -p polint --lib analysis_kernel::metadata::tests --locked &amp;&amp; cargo test -p polint --lib analysis_kernel::incremental::run_report::tests --locked &amp;&amp; cargo test -p polint --lib eval::performance::tests --locked</automated>
  </verify>
  <acceptance_criteria>
    - All codecs round-trip every variant and reject unknown labels with exhaustive matches.
    - Workspace roots are normalized, hashed, and discarded; sentinels never serialize.
    - Wrong-purpose identity construction fails and shuffled inputs preserve run/generation identities.
    - Stable fact rows and provider output digests ignore run-ID permutations and change for every semantic field mutation, including payload_digest.
    - rg over provider_output_summary_parts and provider_output_digest_from_manifest finds no run_id contribution or duplicate precision mapping.
    - eval/performance.rs constructs explicit complete StableFactMetaRow fixtures; no `format!("provider=...")` synthetic digest input, opaque summary-parts list, or String compatibility overload remains, and focused eval::performance tests pass.
    - No source_text, source_bytes, fact_payload, ast_blob, mir_blob, cfg_blob, summary_blob, or graph-adjacency field is introduced.
  </acceptance_criteria>
  <done>Canonical kernel identities/codecs exist and semantic fact/provider identity excludes transient handles while retaining payload_digest metadata.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description | Data Crossing |
|---|---|---|
| Run metadata to durable identity | Transient roots and fact handles become canonical digests | Workspace paths, FactRef handles, semantic metadata |
| Kernel types to future store projection | Canonical enums and digests cross a private persistence boundary | Typed labels, stable fact rows, provider rows |

## Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-65-01-01 | Tampering | Identity construction | mitigate | Opaque purpose-checked identities and permutation/mutation tests reject cross-purpose or order-dependent digests. |
| T-65-01-02 | Information Disclosure | Workspace and fact projection | mitigate | Hash and discard roots; allow payload_digest metadata while forbidding source/body/payload-byte fields with sentinel tests. |
| T-65-01-03 | Spoofing | Stable codecs | mitigate | Exhaustive canonical label parsers reject unknown variants and avoid Debug-derived identity. |
| T-65-01-04 | Elevation of Privilege | Public Rust surface | mitigate | Keep all additions pub(crate) or tighter and preserve SDK/prelude boundaries. |
</threat_model>

<verification>
- cargo test -p polint --lib analysis_kernel::incremental::run_report::tests --locked
- cargo test -p polint --lib eval::performance::tests --locked
- cargo fmt --all -- --check
</verification>

<success_criteria>
- Store-facing identities/codecs are canonical kernel vocabulary.
- Fact/provider identity excludes transient handles and retains payload_digest without payload bytes.
- No SQLite or public/later-phase behavior is introduced.
</success_criteria>

<output>Create 65-01-SUMMARY.md when complete.</output>
