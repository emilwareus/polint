---
phase: 65-generation-manifest-and-metadata-mirroring
plan: 16
type: execute
wave: 16
depends_on: [65-15]
files_modified:
  - crates/polint/src/analysis_kernel/mod.rs
  - crates/polint/src/analysis_kernel/store/mod.rs
  - crates/polint/src/analysis_kernel/store/connection.rs
  - crates/polint/src/analysis_kernel/store/migrations.rs
  - crates/polint/src/analysis_kernel/store/schema.rs
  - crates/polint/src/analysis_kernel/store/tests.rs
autonomous: true
requirements: [STORE-04, STORE-05, META-01, META-04]
must_haves:
  truths:
    - "D-03/D-05: relational IDs are handles only; active selection never uses newest ID, timestamp, rowid, or insertion order."
    - "D-06-D-13: schema v2 mirrors canonical identities, exact query declarations, explicit statuses, payload_digest metadata, and one edge set as first-class rows."
    - "D-17/D-18: schema distinguishes pristine unbound, bound recoverable, active complete, and structurally untrustworthy states."
    - "D-19: semantic counts/digests exclude provider cache counters, DemandCacheStatus, durations, and timestamps; telemetry is separate."
    - "Every future-schema fixture changes at this boundary to CURRENT_SCHEMA_VERSION + 1 and existing kernel parity remains green."
  artifacts:
    - path: "crates/polint/src/analysis_kernel/store/migrations.rs"
      provides: "Strict v1-to-v2 migration with dynamic future pins"
      contains: "CURRENT_SCHEMA_VERSION: i32 = 2"
    - path: "crates/polint/src/analysis_kernel/store/schema.rs"
      provides: "Typed relational codecs and sanitized failure-event vocabulary"
      contains: "GenerationFailureEvent"
---

<objective>
Add strict schema v2 and typed relational codecs at one compile/test-clean migration boundary, including legal reservation/recovery states and dynamic future-schema fixtures.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
@.agents/skills/rust-best-practices/SKILL.md
@.claude/skills/polint/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-CONTEXT.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-PATTERNS.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-15-SUMMARY.md
@.planning/phases/64-store-foundation-and-boundary-proof/64-04-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add schema v2, codecs, lifecycle states, and dynamic future pins</name>
  <files>crates/polint/src/analysis_kernel/mod.rs, crates/polint/src/analysis_kernel/store/mod.rs, crates/polint/src/analysis_kernel/store/connection.rs, crates/polint/src/analysis_kernel/store/migrations.rs, crates/polint/src/analysis_kernel/store/schema.rs, crates/polint/src/analysis_kernel/store/tests.rs</files>
  <read_first>
    - .agents/skills/rust-best-practices/SKILL.md
    - .claude/skills/polint/SKILL.md
    - crates/polint/src/analysis_kernel/store/migrations.rs (single marker and hard-coded future fixtures)
    - crates/polint/src/analysis_kernel/store/connection.rs (transaction policy and hard-coded future fixture)
    - crates/polint/src/analysis_kernel/store/tests.rs (schema/future/recovery fixtures)
    - crates/polint/src/analysis_kernel/mod.rs (future-schema parity fixture)
    - crates/polint/src/analysis_kernel/store/commit_plan.rs (all normalized row families)
    - crates/polint/src/analysis_kernel/incremental/digest.rs, dependency_input.rs, dependency_index.rs, and crates/polint/src/core/mod.rs (canonical codecs)
  </read_first>
  <action>
    Raise CURRENT_SCHEMA_VERSION to 2 and atomically replace the sole marker 1 with 2. Create private normalized tables for store manifest; generations; input snapshot/files/components/details/scoped settings/requested capabilities/provider-schema snapshots; provider manifests/children; provider generations; layers/variable digests; summary/query keys and exact query-dependency children; fact metadata with required payload_digest; one dependency_edges relation; validation events; deterministic generation stats; optional separate telemetry; and sanitized generation_failure_events. Add generation-scoped FKs/cascades, semantic uniqueness, and indexes from both edge endpoints.

    Legal states: pristine unbound is null workspace/null active/zero generations; first committed reservation binds permanently; bound/null-active with pending/failed attempts is recoverable; bound active must reference a same-workspace complete generation. Unbound with rows or active pending/failed/missing/mismatched is invalid. Generation identity may repeat across retries; relational IDs are handles only.

    Define closed failure event CommitAttemptFailed; reasons WriteFailed, PostWriteValidationFailed, PublicationCommitFailed; stages Reservation, StoreRunInput, Providers, LayersSummariesQueries, FactMetadata, DependencyEdges, ValidationEvents, Statistics, Completion, Activation, TransactionCommit. Store only generation FK, ordinal, closed event/reason/stage codes—no free text/detail/timestamp/blob. Complete/active generations cannot own failure events.

    Typed encoders/decoders call canonical label/parse methods and use explicit semantic ORDER BY. Semantic generation stats/digests exclude ProviderOutputMeta.cache_stats, DemandCacheStatus, durations, and timestamps; optional telemetry cannot participate in selection. payload_digest is required, while exact forbidden columns/tables source_text, source_bytes, fact_payload, payload_blob, ast_blob, mir_blob, cfg_blob, summary_body, summary_blob, graph_nodes, and graph_edges are absent.

    Replace every hard-coded future schema value in migrations.rs, connection.rs, store/tests.rs, and analysis_kernel/mod.rs with CURRENT_SCHEMA_VERSION + 1 at this same boundary. Test empty/v1 migration, sole-marker invariant, mid-DDL rollback, all codecs, legal/illegal manifest shapes, shuffled insertion, wrong-kind/unknown/absolute-path rejection, and existing kernel parity. Do not defer a red suite to the writer plan.
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis_kernel::store::migrations::tests --locked &amp;&amp; cargo test -p polint --lib analysis_kernel::store::connection::tests --locked &amp;&amp; cargo test -p polint --lib analysis_kernel::store::schema::tests --locked &amp;&amp; cargo test -p polint --lib analysis_kernel::store::tests --locked -- --test-threads=1 &amp;&amp; cargo test -p polint --lib analysis_kernel::tests::semantic_store_check_parity::all_store_modes_preserve_byte_identical_json_and_exit_semantics --locked -- --test-threads=1 &amp;&amp; cargo check -p polint --all-features --locked</automated>
  </verify>
  <acceptance_criteria>
    - Empty/v1 stores migrate idempotently to one v2 marker; injected DDL failure preserves exact v1 state.
    - Every normalized family, exact query dependency, FK/unique/index, canonical codec, payload_digest, and separate telemetry projection exists.
    - Manifest validator accepts pristine unbound, bound recoverable, and bound active-complete states exactly.
    - Failure events persist only closed codes and cannot attach to complete/active generations.
    - Every future fixture uses CURRENT_SCHEMA_VERSION + 1 and existing kernel/store parity passes now.
    - Exact forbidden body/blob/source/graph columns/tables are absent.
    - Future/invalid schema preflight remains mutation-free and raw SQL/rusqlite stays private.
  </acceptance_criteria>
  <done>Strict schema v2 mirrors every metadata family and the repository is test-clean at the migration boundary.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description | Data Crossing |
|---|---|---|
| Existing SQLite file to v2 migration | Untrusted/older schema enters typed migration and decode | Schema version, tables, rows, codecs |
| Canonical plan rows to relational schema | Typed semantic and telemetry fields receive SQL projections | Identities, query dependencies, payload digest, counters |

## Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-65-16-01 | Tampering | Schema migration | mitigate | One immediate transaction, sole-marker replacement, and injected rollback tests. |
| T-65-16-02 | Spoofing | Typed relational codecs | mitigate | Canonical kernel parsers reject unknown/wrong-kind values. |
| T-65-16-03 | Information Disclosure | Metadata schema | mitigate | Retain payload_digest only; forbid exact source/body/blob/free-text/raw-error fields. |
| T-65-16-04 | Denial of Service | Future-schema handling | mitigate | Dynamic CURRENT_SCHEMA_VERSION + 1 fixtures and mutation-free preflight. |
| T-65-16-05 | Repudiation | Failure audit vocabulary | mitigate | Closed reason/stage codes with no free text and strict attachment rules. |
</threat_model>

<verification>
- cargo test -p polint --lib analysis_kernel::store --locked -- --test-threads=1
- cargo test -p polint --lib analysis_kernel::tests::semantic_store_check_parity::all_store_modes_preserve_byte_identical_json_and_exit_semantics --locked -- --test-threads=1
- cargo check -p polint --all-features --locked
- cargo fmt --all -- --check
</verification>

<success_criteria>
- Strict schema v2, sanitized failure vocabulary, dynamic future fixtures, payload-digest semantics, and parity all land together.
</success_criteria>

<output>Create 65-16-SUMMARY.md when complete.</output>
