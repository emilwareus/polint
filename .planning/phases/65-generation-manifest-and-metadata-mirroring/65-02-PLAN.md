---
phase: 65-generation-manifest-and-metadata-mirroring
plan: 02
type: execute
wave: 2
depends_on: [65-01]
files_modified:
  - crates/polint/src/analysis_kernel/incremental/run_manifest.rs
  - crates/polint/src/analysis_kernel/incremental/digest.rs
  - crates/polint/src/analysis_kernel/incremental/mod.rs
  - crates/polint/src/analysis_kernel/store/migrations.rs
  - crates/polint/src/analysis_kernel/store/connection.rs
  - crates/polint/src/analysis_kernel/store/generation.rs
  - crates/polint/src/analysis_kernel/store/mod.rs
  - crates/polint/src/analysis_kernel/store/tests.rs
autonomous: true
requirements: []
contributes_to: [STORE-04, STORE-05, META-01, META-04]
completion_scope: r2-only
completion_policy:
  mark_requirements_complete: false
  mark_phase_complete: false
  remaining_slices: [R3, R4, R5, R6]

must_haves:
  truths:
    - "D-01/D-02/D-03/D-04: this plan implements only restart slice R2, depends on locked R1, stays within three tasks/fifteen product-test files/2,500 handwritten additions/one manifest schema family/zero provider families, stops on later-slice prerequisites, and must not invoke requirement or phase completion."
    - "D-05/D-06/D-07/D-08/D-09: one private versioned canonical run manifest contains only schema, purpose-typed workspace and complete-config identities, canonical source membership, authenticated count, and a creation-independent recomputed run identity; every excluded metadata family is structurally absent, the digest is never sole equality, and no Debug or opaque JSON participates."
    - "D-10/D-11/D-12/D-13/D-14: the persisted workspace/cache-owner identity is derived from the canonical normalized root without storing the raw path, publication refuses to replace an active manifest owned by another workspace sharing the cache path, the existing complete config hash retains a closed purpose, source rows are canonical-path ordered with closed languages and no ordinals, duplicates/noncanonical rows refuse, and empty membership is valid."
    - "D-15/D-16/D-17: schema v3 adds one generation-owned header/source family; publication stages and authenticates the supplied manifest, completes the same reserved handle, and rotates the active pointer in one immediate transaction, while every failure preserves the previous manifested active truth across reopen."
    - "D-18/D-19/D-20: active handle, header, and sources are decoded in one read snapshot; identity is recomputed before trust; no-active, exact-match, semantic-mismatch, and malformed-refusal remain distinct; and handle-only reads cannot bypass manifest authentication."
    - "D-21/D-22/D-23: exact v2 migrates to v3 only when generation and selection rows are empty; populated v2 refuses through the private rebuild-needed path before persistent connection policy and again inside migration, without mutation; no legacy manifest is synthesized or deleted; v0/v1 may migrate through empty v2; and v3 reopen is idempotent."
    - "D-24/D-25/D-26/D-27: explicit relational columns, storage-class/count/length/aggregate preflight, typed decode, scalar/source tamper tests, nonsemantic-field absence proof, and the expanded publication failure matrix authenticate R2 without partial trust or unbounded allocation."
    - "D-28/D-29: disabled mode returns before path validation, workspace canonicalization, manifest construction, or I/O; normal analysis remains unwired and byte/exit compatible; tests are independent and individually under 60 seconds; and the deferred sub-five-minute CI work is untouched."
  artifacts:
    - path: "crates/polint/src/analysis_kernel/incremental/run_manifest.rs"
      provides: "Private canonical manifest projection, versioned typed codec, workspace/config/source purposes, and creation-independent identity"
      contains: "RunManifest"
    - path: "crates/polint/src/analysis_kernel/store/migrations.rs"
      provides: "Exact schema-v3 manifest family, empty-v2-only migration, and strict catalog/content authentication"
      contains: "CURRENT_SCHEMA_VERSION"
    - path: "crates/polint/src/analysis_kernel/store/generation.rs"
      provides: "Atomic manifest-aware publication and one-snapshot authenticated active reads"
      contains: "ManifestMatch"
    - path: "crates/polint/src/analysis_kernel/store/tests.rs"
      provides: "Round-trip, mismatch, tamper, allocation-bound, migration, rollback, reopen, and disabled proof"
      contains: "run_manifest"
  key_links:
    - from: "crates/polint/src/analysis_kernel/incremental/run_manifest.rs"
      to: "crates/polint/src/analysis_kernel/incremental/input_snapshot.rs"
      via: "a narrow constructor copies only approved canonical fields from FileSnapshot rows and the separately supplied complete config hash"
      pattern: "FileSnapshot|RunManifestInputs"
    - from: "crates/polint/src/analysis_kernel/store/generation.rs"
      to: "crates/polint/src/analysis_kernel/incremental/run_manifest.rs"
      via: "SQL writes consume the explicit encoded projection and reads return only a bounded, typed, identity-recomputed decode"
      pattern: "RunManifest|encode|decode"
    - from: "crates/polint/src/analysis_kernel/store/generation.rs"
      to: "crates/polint/src/analysis_kernel/store/connection.rs"
      via: "one existing-policy immediate writer transaction for publication and one read transaction for active handle/header/source reconstruction"
      pattern: "with_immediate_transaction|with_read_transaction"
    - from: "crates/polint/src/analysis_kernel/store/generation.rs"
      to: "crates/polint/src/analysis_kernel/store/migrations.rs"
      via: "queries and cardinality checks rely on the authenticated schema-v3 generation-owned manifest family"
      pattern: "run_manifests|run_manifest_sources|complete|active"
    - from: "crates/polint/src/analysis_kernel/store/mod.rs"
      to: "crates/polint/src/analysis_kernel/incremental/run_manifest.rs"
      via: "the private facade checks disabled state before building a manifest from borrowed inputs"
      pattern: "SemanticStore|RunManifestInputs|is_enabled"
---

<objective>
Implement restart slice R2: one private minimal audited run manifest bound to
R1's generation lifecycle.

Purpose: Establish a canonical, bounded, reconstructable workspace/config/source
identity before any provider family or warm reuse can be persisted.

Output: A private versioned manifest projection, schema version 3 with one
generation-owned manifest family, atomic manifest publication, one-snapshot
authenticated reads and exact matching, and migration/tamper/rollback proof.

This is incremental work toward STORE-04, STORE-05, META-01, and META-04. It
does not satisfy or close those requirements or the broader Phase 65 outcome.
The empty `requirements` field is deliberate because GSD copies that field into
completion state. `contributes_to` records traceability without making a false
completion claim.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-CONTEXT.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-PATTERNS.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-LEARNINGS.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-01-SUMMARY.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-REVIEW.md
@research/local-semantic-store/RESTART-PLAN.md
@research/local-semantic-store/IDENTITY-READINESS.md
@research/local-semantic-store/REVIEW-FINDINGS-TRIAGE.md
@research/local-semantic-store/implementation/STORE-CONTRACT.md
@docs/API-VISIBILITY-PLAN.md
@.agents/skills/rust-best-practices/SKILL.md

<interfaces>
From `crates/polint/src/analysis_kernel/incremental/input_snapshot.rs`:
- `InputSnapshot::files` is sorted by normalized `relative_path`.
- `FileSnapshot` contains `relative_path`, `Language`, a `SourceText` digest,
  `size_bytes`, and an mtime hint. R2 copies the first four only and must not
  serialize the snapshot or its mtime/lifecycle/rule/provider fields.
- The complete config hash is supplied separately from the snapshot component
  so status/detail vocabulary cannot enter the manifest.

From `crates/polint/src/analysis_kernel/incremental/digest.rs`:
- `Digest` is purpose-separated by closed `DigestKind`.
- `DigestBuilder` uses deterministic length-prefixed fields.
- `debug_part` exists for other private identities but is forbidden for this
  durable codec.

From `crates/polint/src/core/mod.rs` and `repo_fs.rs`:
- `Language` has explicit Go/TypeScript/Tsx/JavaScript/Jsx/Unknown variants.
  R2 stores only explicit supported labels and rejects `Unknown`.
- `normalize_repo_relative` provides the existing normalized path contract.
- `SourceFile` already owns the content hash and byte source used to build each
  `FileSnapshot`; R2 must not clone source text.

From `crates/polint/src/analysis_kernel/store/generation.rs`:
- `GenerationHandle` is an opaque positive relational handle.
- `publish_transaction` currently completes and selects the supplied reserved
  handle through one immediate transaction.
- `active` currently authenticates only the lifecycle relationship; schema v3
  must route it through manifest authentication.
- Five finite cfg(test)-only failure points already prove R1 rollback.

From `crates/polint/src/analysis_kernel/store/migrations.rs`:
- `CURRENT_SCHEMA_VERSION` is 2 and the ordered runner validates the supported
  prior schema before applying transactional migrations.
- Claimed-current catalog SQL, keys, indexes, foreign keys, triggers, marker,
  lifecycle contents, and `foreign_key_check` are authenticated exactly.
- Version 2 may contain manifestless complete generations, so R2 may migrate it
  only when generation and active tables are both empty.

From `crates/polint/src/analysis_kernel/store/connection.rs` and `store/mod.rs`:
- Writer and read-only wrappers own SQLite policy, preflight, foreign keys,
  WAL/synchronous behavior, 250 ms busy timeout, and typed error mapping.
- `SemanticStore` is the only private facade and checks disabled mode before
  path preparation.
- Raw connections, transactions, SQL, table names, and scalar IDs stay within
  `analysis_kernel::store`.

From `crates/polint/src/analysis_kernel/mod.rs`:
- Normal execution calls only `SemanticStore::maintain` after analysis and
  validation. R2 must not reserve, publish, match, or reuse a manifest there.
</interfaces>
</context>

<execution_guard>
This is a partial-scope execution inside a broader roadmap phase. Execute Plan
65-02 only and stop after its code/test commits and `65-02-SUMMARY.md` commit.

- Write `requirements-completed: []` in the summary.
- Do not call `requirements.mark-complete` for STORE-04, STORE-05, META-01, or
  META-04.
- Do not call `phase.complete 65`, mark the Phase 65 roadmap checkbox, advance
  STATE to Phase 66, or describe Phase 65 as complete.
- Leave Phase 65 open with R1 and R2 complete and R3-R6 remaining.
- Do not run the whole-phase verifier against the intentionally broader Phase
  65 outcome as though R2 claimed to satisfy it. Verify this plan's must-haves
  and commands, then stop.

These closeout exceptions are required by D-04 and override generic
execute-plan/execute-phase completion steps for this plan.
</execution_guard>

<scope_boundary>
R2 may add exactly one durable schema family: one manifest header plus its
owned canonical source rows. It may extend the private generation facade only
enough to publish, reconstruct, and compare that payload.

Do not add or persist rules, plans, providers, capabilities, lifecycle/tool
identity, models, extensions, layers, queries, summaries, facts, validation
events, dependency indexes, statistics, telemetry, timestamps, mtimes, raw
workspace paths, or opaque JSON. Do not redesign `InputSnapshot`, provider
keys, general cache identity, or recovery policy. Do not enable publication or
reuse from `AnalysisKernel::run`. Do not change public CLI/config/SDK/output,
docs/examples, or `.github/workflows/ci.yml`.

If implementation requires any excluded contract, another schema family, a
provider family, more than fifteen product/test files, more than 2,500
handwritten added lines, or more than three tasks, stop and split a prerequisite
or later slice rather than widening this plan.
</scope_boundary>

<tasks>

<task type="auto">
  <name>Task 1: Define the private canonical manifest and versioned codec</name>
  <files>crates/polint/src/analysis_kernel/incremental/run_manifest.rs, crates/polint/src/analysis_kernel/incremental/digest.rs, crates/polint/src/analysis_kernel/incremental/mod.rs</files>
  <read_first>
    - crates/polint/src/analysis_kernel/incremental/input_snapshot.rs (`InputSnapshot`, `FileSnapshot`, sorting, config component, mtime and excluded witnesses)
    - crates/polint/src/analysis_kernel/incremental/digest.rs (`Digest`, `DigestKind`, `DigestBuilder`, length-prefix codec, `debug_part`)
    - crates/polint/src/analysis_kernel/incremental/mod.rs (curated crate-private exports)
    - crates/polint/src/core/mod.rs (`Language`, `SourceFile`)
    - crates/polint/src/repo_fs.rs (`normalize_repo_relative`, normalized path contract)
    - crates/polint/src/cache/keys.rs (`config_hash` completeness and mutation-tested source)
    - .planning/phases/65-generation-manifest-and-metadata-mirroring/65-CONTEXT.md (D-05 through D-14, D-24 through D-26)
    - .planning/phases/65-generation-manifest-and-metadata-mirroring/65-PATTERNS.md (ownership decision and canonical codec)
  </read_first>
  <action>
    Create a private `run_manifest.rs` at the kernel identity boundary. Define
    small typed vocabulary equivalent to:

    - `RunManifestInputs<'a>` borrowing a workspace root, the complete config
      hash, and approved `FileSnapshot` rows without doing I/O in its trivial
      constructor;
    - `RunManifest` with the closed schema, typed workspace/config identities,
      canonical source rows, derived count, and derived run identity;
    - a canonical source row with normalized path, a closed supported-language
      codec, `SourceText` digest, and checked byte size; and
    - one explicit encoded form used by SQL write/read boundaries and one typed
      decoder that reconstructs and recomputes rather than trusting a stored
      run digest.

    Add only the minimal closed workspace/run digest purposes and byte/integer
    parts needed by the existing deterministic digest builder. Use explicit
    versioned field labels and length prefixes. Never call `debug_part`, serde,
    JSON, `Display`, lossy string conversion, timestamps, or ambient worker
    order when constructing identity.

    `RunManifest::from_inputs` must canonicalize and normalize the workspace
    root, encode its platform path units losslessly, hash them into a
    purpose-typed cache-owner identity, and retain no raw absolute root. It must
    wrap the already-computed complete config hash in one fixed config purpose.
    It must independently normalize and authenticate every source path, reject
    any noncanonical path, duplicate, `Language::Unknown`, wrong source digest
    kind, invalid digest representation, or numeric overflow, sort by path, and
    accept empty membership.

    Compute the source count from the canonical vector and derive the run
    identity from the manifest schema, every purpose/value, the count, and
    every canonical source field. Exact semantic equality must compare decoded
    typed fields, not only this digest. Keep constructors/accessors only as
    visible as the private store integration requires; do not export through
    `polint::sdk`, runner, CLI, or crate-root public modules.

    Add focused unit tests proving deterministic round-trip codec behavior,
    insertion-order independence, empty membership, exact field mutation
    sensitivity, duplicate/noncanonical/unknown-language rejection, wrong
    digest-purpose rejection, and absence of mtime/time/telemetry/provider/rule
    inputs. Use two real canonical temporary workspace roots to prove the
    workspace identity changes without exposing either raw path.
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis_kernel::incremental::run_manifest::tests --locked</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p polint --lib analysis_kernel::incremental::run_manifest::tests --locked` exits 0 in less than 60 seconds.
    - The typed and encoded forms contain exactly the D-06 fields and none of the D-07 fields.
    - Identical inputs, including reordered input rows, produce equal canonical fields and run identity; every approved field mutation changes equality or is rejected.
    - Empty membership is valid; duplicate/noncanonical paths, unknown language, wrong digest purpose, malformed digest, and overflow return typed errors.
    - The canonicalized absolute workspace root is encoded losslessly before hashing and is not retained, serialized, logged, or returned.
    - The complete config hash is retained with a fixed explicit purpose; no provider-scoped config identity is introduced.
    - No `Debug` formatting, serde/JSON, timestamps, mtimes, durations, cache telemetry, rule/plan/provider data, or source-text clone participates in construction or identity.
    - All new production vocabulary remains crate-private and the task changes only the three listed files.
  </acceptance_criteria>
  <done>One private canonical codec can construct, encode, decode, compare, and recompute the complete R2 manifest without importing excluded snapshot metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Add the exact schema-v3 family and bounded typed storage codec</name>
  <files>crates/polint/src/analysis_kernel/store/migrations.rs, crates/polint/src/analysis_kernel/store/connection.rs, crates/polint/src/analysis_kernel/store/generation.rs</files>
  <read_first>
    - crates/polint/src/analysis_kernel/store/migrations.rs (`MIGRATIONS`, supported-schema dispatch, exact catalog validators, lifecycle row invariants, v1 fixtures)
    - crates/polint/src/analysis_kernel/store/connection.rs (`with_immediate_transaction`, `with_read_connection`, open/preflight/error policy, typed fixture snapshots)
    - crates/polint/src/analysis_kernel/store/generation.rs (`GenerationHandle`, publication/read sequence, selection validation, failure seams)
    - crates/polint/src/analysis_kernel/incremental/run_manifest.rs (Task 1 typed and encoded codec)
    - .planning/phases/65-generation-manifest-and-metadata-mirroring/65-PATTERNS.md (relational shape, migration posture, bounded decode)
    - research/local-semantic-store/REVIEW-FINDINGS-TRIAGE.md (WR-03, WR-04, PERF-04, WR-13, PERF-07, WR-24, WR-28)
  </read_first>
  <action>
    Advance `CURRENT_SCHEMA_VERSION` to 3 and add exactly one ordered manifest
    migration. Add one explicit header table keyed by generation handle and one
    owned source table keyed by `(generation_id, relative_path)`. Use explicit
    columns for schema, workspace purpose/value, config purpose/value, declared
    source count, run purpose/value, source language/purpose/value, and byte
    size. Do not persist ordinals or an opaque payload.

    The header must reference its generation relationally; each source must
    reference its header. Committed-content validation must enforce zero
    manifests for pending generations and exactly one header for every complete
    generation. This lets publication stage and authenticate an uncommitted
    manifest for the supplied pending handle, then complete it before commit;
    no other reader can observe that intermediate state. Authenticate exact
    table SQL, columns, primary/foreign keys, checks, indexes, triggers, closed
    labels, ownership, declared/actual counts, active-manifest relationship,
    orphan absence, and `foreign_key_check`.

    Split supported-version validation so an exact v2 catalog is accepted as a
    migration source only when both `generations` and `active_generation` are
    empty. Because `open_uninitialized_writer` currently runs
    `preflight_schema` before `synchronous`/`journal_mode = WAL`, make that
    read-only preflight reject populated v2 before any persistent
    policy-changing pragma. Defensively repeat the exact emptiness check inside
    the existing immediate migration transaction before any v3 DDL, marker
    update, or `user_version` change. A populated exact v2 must map through
    existing typed invalid-schema/rebuild vocabulary and retain its journal
    mode, catalog, marker, lifecycle rows, selection, and version unchanged.
    Do not synthesize, delete, complete, deactivate, or rotate anything.
    Preserve ordered v0→v1→empty-v2→v3 and v1→empty-v2→v3 migrations, strict
    current-v3 reopen, and future/malformed refusal before mutation.

    Implement store-local manifest write and read helpers around the Task 1
    encoded form. SQL writing must use typed fields and verify exact affected
    row counts. Before allocating decoded text/rows, the reader must use the
    same transaction to preflight:

    - header/source cardinality and SQLite `typeof` storage classes;
    - explicit per-field byte lengths;
    - nonnegative, checked numeric ranges;
    - a concrete large-repository source-row maximum;
    - declared versus actual source count; and
    - aggregate text bytes plus bounded row overhead.

    Only after those checks may it allocate the bounded vector and read source
    rows ordered by normalized path. Decode closed labels through Task 1's one
    codec, reject coercion/unknowns/duplicates/noncanonical order, reconstruct
    the typed manifest, and recompute the run identity. A stored digest that
    differs from recomputation is a typed malformed-manifest refusal.

    Add a narrow `with_read_transaction` (or equivalent) on
    `ReadOnlyConnection` so selected handle, header, preflight, and all source
    rows come from one snapshot. Keep every rusqlite value and limit inside the
    private store subtree and reuse the existing busy/corrupt/future/invalid
    mapping. Do not add compression, retries, global state, another connection
    policy, or raw SQL test access outside `store`.

    Add focused migration/storage tests for fresh/v0/v1/empty-v2 migration,
    populated-v2 no-mutation refusal (with and without active selection) through
    the real writer-open path, idempotent v3 reopen, exact typed round-trip
    including empty sources, strict catalog/content rejection, wrong SQLite
    storage classes, boundary/over-limit counts and lengths, aggregate limits,
    and identity recomputation. Snapshot populated-v2 journal mode as well as
    catalog SQL, marker, lifecycle rows, selection, and `user_version` before
    and after refusal so the pre-policy ordering is proved rather than assumed.
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis_kernel::store::migrations::tests --locked &amp;&amp; cargo test -p polint --lib analysis_kernel::store::tests::run_manifest_storage --locked</automated>
  </verify>
  <acceptance_criteria>
    - Both focused commands exit 0, and no individual test takes 60 seconds.
    - `CURRENT_SCHEMA_VERSION` is 3 and only the one header/source manifest family is added after the existing generation lifecycle.
    - v0, v1, and empty exact v2 migrate to an empty valid v3; current v3 reopen is idempotent.
    - Populated exact v2 returns the existing private rebuild-needed/invalid-schema outcome from preflight before `journal_mode = WAL` or another persistent policy change, repeats the guard transactionally, and leaves journal mode, catalog, marker, rows, selection, and `user_version` unchanged.
    - Every committed complete generation has exactly one header, every pending generation has none, source ownership/counts are exact, and active selection joins one authenticated manifest.
    - Reader preflight rejects wrong storage class, excessive scalar length, count, aggregate payload, negative/overflow numeric value, orphan/cross-owned row, malformed label/path/digest, and identity mismatch before returning trusted data.
    - The active handle, header, preflight, and owned sources are read in one transaction/snapshot and decoded with the Task 1 codec.
    - SQL, table names, scalar row IDs, rusqlite types, and allocation-limit mechanics remain inside `analysis_kernel::store`.
    - No manifestless legacy row is synthesized, deleted, deactivated, or promoted.
  </acceptance_criteria>
  <done>Schema v3 can represent and strictly reconstruct only canonical R2 manifests, migrates only truthful empty predecessors, and bounds untrusted SQLite work before allocation.</done>
</task>

<task type="auto">
  <name>Task 3: Bind manifest publication and exact matching atomically, then prove rollback and tamper refusal</name>
  <files>crates/polint/src/analysis_kernel/store/generation.rs, crates/polint/src/analysis_kernel/store/connection.rs, crates/polint/src/analysis_kernel/store/mod.rs, crates/polint/src/analysis_kernel/store/tests.rs</files>
  <read_first>
    - crates/polint/src/analysis_kernel/store/generation.rs (Task 2 manifest storage helpers plus R1 transition/selection/failure sequence)
    - crates/polint/src/analysis_kernel/store/mod.rs (`SemanticStore`, disabled guard, error mapping, facade visibility)
    - crates/polint/src/analysis_kernel/store/connection.rs (Task 2 read transaction and typed fixtures)
    - crates/polint/src/analysis_kernel/store/tests.rs (independent temp-store lifecycle, reopen, contention, recovery, and failure-matrix patterns)
    - crates/polint/src/analysis_kernel/mod.rs (normal post-validation `maintain`-only integration; inspect but do not modify)
    - docs/API-VISIBILITY-PLAN.md (private store boundary)
    - .planning/phases/65-generation-manifest-and-metadata-mirroring/65-CONTEXT.md (D-15 through D-20 and D-25 through D-29)
  </read_first>
  <action>
    Replace the handle-only publication path with a manifest-aware private
    operation over the same reserved handle. The facade must accept borrowed
    `RunManifestInputs`, check `config.is_enabled()` before path validation or
    calling the I/O-performing manifest builder, then open the existing writer.
    Inside one immediate transaction:

    1. read and authenticate the current active manifest, if any, and refuse a
       typed workspace-ownership mismatch before candidate mutation when its
       workspace identity differs from the requested manifest;
    2. validate the in-memory canonical manifest;
    3. insert exactly one header and all canonical source rows for the supplied
       pending generation;
    4. read them back through the bounded typed codec and compare exact fields
       plus recomputed identity;
    5. transition that same handle from pending to complete;
    6. authenticate complete-generation/manifest cardinality;
    7. rotate the singleton active pointer to that same handle and authenticate
       the joined lifecycle/manifest relationship; and
    8. commit.

    Zero-row/repeated/unknown transitions, duplicate manifests, or any
    write/decode mismatch are typed refusals. The transaction rollback must
    restore the candidate to pending with no manifest rows and retain the prior
    active manifested generation. There is no general activate operation,
    manifestless publish path, recency query, or production kernel caller.

    Add a private active-manifest read and exact-match seam. It must use Task
    2's single read snapshot, recompute the decoded identity, and distinguish:

    - no active manifest;
    - exact canonical field match (returning the selected opaque handle);
    - valid semantic mismatch; and
    - malformed/refused storage as a typed error.

    Build requested inputs only after the disabled guard. Different canonical
    workspace roots against one shared store path must be a normal read-match
    mismatch and a typed publication ownership refusal, never overwrite or
    inferred ownership. The ownership check must run inside the same writer
    transaction as candidate publication so concurrent callers cannot rotate
    the pointer between check and commit. Any existing handle-only active read
    used by tests must delegate to the authenticated manifest read and discard
    only the already-validated payload; it may not query the pointer directly.

    Expand the finite cfg(test)-only failure enum without environment variables,
    features, locks, or public modes. Cover seams before/after header write,
    around source write, before/after stored decode/recompute, before/after
    completion, before/after selection, and before commit. For every seam,
    publish A/M1, reserve B, inject B/M2 failure, close all connections, reopen,
    and prove A/M1 is still the sole readable active truth while B is pending
    with no manifest. Also prove failed first publication preserves valid
    no-active state.

    Add independent round-trip/reopen and match tests:

    - identical inputs are exact after reopen;
    - workspace, config, source insertion/deletion/path/language/digest/size
      changes are valid mismatches;
    - after workspace A publishes, workspace B using the same store path cannot
      publish over A; close/reopen still returns A as the sole active truth and
      B's candidate remains pending without a manifest;
    - input/source insertion order is irrelevant;
    - empty source membership round-trips; and
    - timestamp, creation time, mtime, duration, cache outcome/counter, and
      worker-order changes cannot be expressed by the API or schema.

    Add a store-private deterministic tamper helper and mutate every header
    scalar while retaining the old run digest: schema, workspace
    purpose/value, config purpose/value, declared count, run purpose/value.
    Mutate representative source membership through insertion, deletion, path,
    language, purpose, digest, size, storage class, orphan/cross-owner, and
    bounded-allocation cases. Reopen must return typed refusal, never exact,
    mismatch, partial data, panic, or changed analysis output.

    Preserve normal `AnalysisKernel::run` as `maintain`-only. Keep existing
    public check/review parity tests unchanged unless a schema-relative
    test-only constant needs updating inside already listed store test helpers.
    Run format, focused tests, lint, public-surface proof, and workspace tests.
    Fix only R2 defects; do not change CI or weaken/serialize tests.
  </action>
  <verify>
    <automated>make lint &amp;&amp; cargo test --workspace --all-features --locked</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p polint --lib analysis_kernel::store::tests::run_manifest --locked`, `cargo test -p polint --lib analysis_kernel::store::tests --locked`, and the full automated command exit 0.
    - Identical canonical input is exact after close/reopen; every approved workspace/config/source mutation is a normal read mismatch; reordered inputs remain exact.
    - A different workspace sharing the store path receives a typed publication ownership refusal inside the writer transaction and cannot replace the prior active manifest; close/reopen proves the prior owner remains sole truth and the candidate remains pending without a manifest.
    - No-active, exact, mismatch, and malformed refusal are distinct typed outcomes, and handle-only active reads cannot bypass decoded manifest authentication.
    - Publication writes/authenticates the manifest, completes the same reserved handle, selects it, and commits in one immediate transaction.
    - Every named publication seam preserves the prior active manifested generation across reopen; failed first publication preserves no-active state; the candidate remains pending with no durable manifest.
    - Every header scalar tamper and representative source insert/delete/update/storage-class/ownership/bounds tamper returns typed refusal before trusted data is returned.
    - Disabled publication/match entry points return before creating a path, canonicalizing an invalid/nonexistent workspace, constructing a manifest, or opening SQLite.
    - Normal kernel analysis still calls only `SemanticStore::maintain`; no persisted provider facts are read and diagnostics, capabilities, ordering, rendered output, and exit status remain unchanged.
    - All ordinary correctness tests use independent stores, no required individual test exceeds 60 seconds, and `.github/workflows/ci.yml` is unchanged.
    - Final implementation stays within eight changed product/test files, 2,500 handwritten additions, one durable schema family, and zero provider families.
  </acceptance_criteria>
  <done>Only a fully decoded canonical manifest can become or match active durable truth, and every transactional/tamper failure preserves the prior truth without affecting normal analysis.</done>
</task>

</tasks>

<threat_model>
## Security Baseline

OWASP ASVS Level 1 applies to this private rebuildable local-cache boundary.
Attacker-controlled or corrupted SQLite values are untrusted. Any unresolved
HIGH concern blocks R2 closeout.

## Assets and Trust Boundaries

| Boundary | Asset at risk |
|----------|---------------|
| canonical workspace/config/source inputs → manifest codec | Deterministic semantic identity and workspace ownership privacy |
| local SQLite bytes → typed manifest | Memory bounds, process reliability, and refusal of forged/malformed state |
| pending candidate → active generation | Atomicity and preservation of the prior durable truth |
| shared `POLINT_CACHE_DIR` → workspace match | Prevention of cross-workspace reuse or overwrite by inferred ownership |
| private store → kernel/public product | API stability, raw-path privacy, and unchanged diagnostics/answers |

## HIGH Blocking Threats

| Threat ID | ASVS area | Threat | Required mitigation and proof |
|-----------|-----------|--------|-------------------------------|
| T-65-02-01 | V5 validation | A stored run digest is trusted while scalar/source fields were changed | Explicit typed decode, exact canonical-field comparison, run-identity recomputation, every-header-scalar and source mutation matrix. |
| T-65-02-02 | V5 validation / V1 architecture | SQLite count, type, or length coercion causes unbounded allocation, panic, or partial trust | Same-snapshot `typeof`/count/length/aggregate preflight, checked conversions, explicit bounds before allocation, typed over-limit tests. |
| T-65-02-03 | V1 architecture | Manifest, completion, and active-pointer changes tear, exposing a partial candidate or losing prior truth | One immediate transaction, supplied-handle ownership, expanded finite failure seams, close/reopen proof. |
| T-65-02-04 | V1 architecture / V5 validation | A populated schema-v2 lifecycle is silently assigned a false synthesized manifest or is mutated by WAL policy before refusal | Read-only populated-v2 preflight before persistent connection pragmas, repeat emptiness guard inside migration, and journal/catalog/data snapshot equality after rebuild-needed refusal. |
| T-65-02-05 | V1 architecture | Active handle reads bypass missing/malformed/cross-owned manifest rows | Route all active reads through one-snapshot lifecycle+header+source authentication; malformed state is an error. |
| T-65-02-06 | V14 configuration / privacy | Raw absolute workspace paths leak or a shared cache conflates workspaces or lets one overwrite the other's active truth | Lossless canonical-root hashing after disabled guard, persist only purpose/value, normal read mismatch plus in-transaction publication refusal for a second root, reopen preservation test, and no raw path in schema/log/result. |

## Additional Threats and Honest Limits

| Threat ID | Category | Threat | Mitigation or residual posture |
|-----------|----------|--------|--------------------------------|
| T-65-02-07 | Denial of service | Locking or oversized cache state stalls analysis | Existing 250 ms busy policy, explicit allocation bounds, no retry loop; availability failure maps to private skip/rebuild outcome. |
| T-65-02-08 | Tampering | An attacker creates a fully self-consistent cache rather than a one-field corruption | R2 establishes deterministic integrity checks, not secret-key authenticity. It persists/reuses no provider facts and cannot change analysis answers; later enablement must preserve conservative exact matching and rebuildability. |
| T-65-02-09 | Privacy | Error/debug output reveals workspace or stored payload | Typed errors carry closed reasons, not raw paths or SQL values; no public diagnostic/output wiring. |
| T-65-02-10 | Scope exposure | R2 pulls provider identity, warm reuse, or CI shortcuts into one review | Hard task/file/line/family budgets and stop-and-split guard; `.github/workflows/ci.yml` and normal kernel execution remain unchanged. |
</threat_model>

<verification>
- `cargo test -p polint --lib analysis_kernel::incremental::run_manifest::tests --locked`
- `cargo test -p polint --lib analysis_kernel::store::migrations::tests --locked`
- `cargo test -p polint --lib analysis_kernel::store::tests::run_manifest_storage --locked`
- `cargo test -p polint --lib analysis_kernel::store::tests::run_manifest --locked`
- `cargo test -p polint --lib analysis_kernel::store::tests --locked`
- `cargo test -p polint --lib analysis_kernel::tests::semantic_store_check_parity --locked`
- `cargo test -p polint --test public_surface_leak --locked`
- `cargo fmt --all -- --check`
- `make lint`
- `cargo test --workspace --all-features --locked`
- `git diff --check`
- Confirm the implementation changes no more than eight product/test files and
  2,500 handwritten lines, adds only the manifest header/source schema family,
  and leaves `.github/workflows/ci.yml`, public CLI/config/SDK/output files,
  providers, and normal kernel persistence/reuse wiring unchanged.
</verification>

<success_criteria>
- One canonical private projection represents only workspace, complete config,
  and exact source membership and recomputes its creation-independent identity.
- Schema v3 binds exactly one authenticated manifest to each complete
  generation and refuses a populated manifestless v2 store without mutation.
- Publication, completion, and selection are atomic; every injected failure
  preserves prior readable truth across reopen.
- Active reads use one snapshot and return only bounded, typed, identity-checked
  data; exact match, mismatch, absence, and malformed refusal remain distinct.
- Workspace roots remain private and distinct even when a cache path is shared.
- R2 remains disabled/unwired from normal analysis and adds no public or CI
  behavior.
- R2 is documented as an incremental contribution only. Phase 65 and
  STORE-04/STORE-05/META-01/META-04 remain open for R3-R6.
</success_criteria>

<output>
Create
`.planning/phases/65-generation-manifest-and-metadata-mirroring/65-02-SUMMARY.md`
when done. Its frontmatter must contain `requirements-completed: []`, and its
body must state that only R1 and R2 are complete with R3-R6 remaining. Stop
after committing the summary and a truthful Phase 65 state update; do not mark
Phase 65 or STORE-04/STORE-05/META-01/META-04 complete.
</output>
