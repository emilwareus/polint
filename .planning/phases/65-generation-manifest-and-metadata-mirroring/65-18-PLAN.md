---
phase: 65-generation-manifest-and-metadata-mirroring
plan: 18
type: execute
wave: 18
depends_on: [65-17]
files_modified:
  - crates/polint/src/analysis_kernel/mod.rs
  - crates/polint/src/analysis_kernel/incremental/run_report.rs
  - crates/polint/src/analysis_kernel/incremental/dependency_index.rs
  - crates/polint/src/analysis_kernel/incremental/invalidation.rs
  - crates/polint/src/analysis_kernel/store/mod.rs
  - crates/polint/src/analysis_kernel/store/generation.rs
  - crates/polint/src/analysis_kernel/store/tests.rs
  - crates/polint/tests/cli.rs
autonomous: true
requirements: [STORE-04, STORE-05, META-01, META-04]
must_haves:
  truths:
    - "D-01/D-02/D-16: real kernel commits only after authoritative validation and fact finalization through a store-private facade."
    - "D-03/D-04/D-17/D-18: old-complete fallback and every private failure state preserve policy truth/output."
    - "D-11-D-15: every META-04 class has persisted referenced-invalidate and unreferenced-preserve controls using the existing planner."
    - "D-19/D-20: telemetry cannot affect identity/selection; disabled and --no-cache paths perform zero handoff materialization, path creation, or store I/O; public bytes/exits are identical."
  artifacts:
    - path: "crates/polint/src/analysis_kernel/mod.rs"
      provides: "Enabled-only post-validation commit through private facade"
      contains: "commit_validated_run"
    - path: "crates/polint/src/analysis_kernel/store/tests.rs"
      provides: "Persisted META-04 mutation/status/order matrix"
      contains: "metadata_invalidation_matrix"
---

<objective>
Integrate the real enabled-only kernel commit and prove exact persisted invalidation, zero disabled work, telemetry independence, and byte-identical policy behavior.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
@.agents/skills/rust-best-practices/SKILL.md
@.claude/skills/polint/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-CONTEXT.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-PATTERNS.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-17-SUMMARY.md
@.planning/phases/64-store-foundation-and-boundary-proof/64-04-SUMMARY.md
@docs/API-VISIBILITY-PLAN.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Commit real finalized runs and execute the persisted META-04 matrix</name>
  <files>crates/polint/src/analysis_kernel/mod.rs, crates/polint/src/analysis_kernel/incremental/run_report.rs, crates/polint/src/analysis_kernel/incremental/dependency_index.rs, crates/polint/src/analysis_kernel/incremental/invalidation.rs, crates/polint/src/analysis_kernel/store/mod.rs, crates/polint/src/analysis_kernel/store/generation.rs, crates/polint/src/analysis_kernel/store/tests.rs, crates/polint/tests/cli.rs</files>
  <read_first>
    - .agents/skills/rust-best-practices/SKILL.md
    - .claude/skills/polint/SKILL.md
    - crates/polint/src/analysis_kernel/mod.rs (providers, validation, finalization, store maintenance)
    - crates/polint/src/analysis_kernel/incremental/run_report.rs (ValidatedRunMetadata)
    - crates/polint/src/analysis_kernel/store/mod.rs and generation.rs (private facade/writer/reader)
    - crates/polint/src/analysis_kernel/incremental/dependency_index.rs and invalidation.rs (typed edges/existing planner)
    - crates/polint/src/analysis_kernel/incremental/change_set.rs (read-only change vocabulary)
    - crates/polint/src/analysis_kernel/store/tests.rs and crates/polint/tests/cli.rs (round trip/parity fixtures)
  </read_first>
  <action>
    Add SemanticStore::commit_validated_run(config, &ValidatedRunMetadata) as the only parent-facing facade. Preserve exact production order: providers finish; validation report is built; diagnostics extend unchanged; fact metadata finalizes; then, only when StoreConfig is enabled, construct ValidatedRunMetadata and call the facade. Branch before snapshot/manifests/stable-row cloning, edge/family aggregation, StoreCommitPlan creation, path creation, connection work, or any counter side effect. The facade repeats the disabled guard and privately creates/commits StoreCommitPlan. analysis_kernel/mod.rs cannot name commit_plan, StoreCommitPlan, or from_validated_run.

    Keep only private sanitized store outcome and deterministic store stats in KernelRunReport. Store failures/mismatch/busy/schema/corruption/unsafe paths cannot alter AnalysisDb, capability support, diagnostics, renderers, or exits. Extend check and review byte/exit parity for complete, mismatch, first-failure recovery, audited failed, pending, busy, future, invalid, corrupt, and unsafe states. Default and --no-cache remain hard-off with zero handoff/plan/I/O counters.

    Build, commit, reopen, and feed the reconstructed DependencyIndex to InvalidationPlan::from_change_set. For every class include a referenced invalidate and unreferenced sibling Reuse: source file; package/project; provider manifest/version; provider schema; requested capability; analysis setting; Go lifecycle/tool; TS/JS lifecycle/tool; config; upstream layer; summary dependency; query parameter/option; exact QueryDependencyInputs; budget profile; reserved search manifest; extension code/declared input; model digest; rule code; rule options. Cover Present/Absent/Unsupported/SetupMissing transitions and unchanged state. Rule code/options and full ConfigIdentity connect to diagnostic/run-manifest nodes, so referenced mutations invalidate those nodes while unrelated analysis layers reuse. The corresponding provider-scoped AnalysisSetting connects to and invalidates only linked layers/queries; capability/model/extension inputs follow the same declared-edge rule. Run 20 shuffled snapshot/provider/edge/SQL-insertion permutations and compare rows/maps/semantic digests/actions. Claim invalidation vocabulary only, never warm payload reuse.

    Add semantic/telemetry regression rows: DemandCacheStatus, ProviderOutputMeta.cache_stats counters, durations, and timestamps may change private telemetry but must preserve provider/layer/query/fact/dependency/validation aggregates, RunIdentity, GenerationIdentity, active selection, and invalidation actions.
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis_kernel::tests::semantic_store --locked -- --test-threads=1 &amp;&amp; cargo test -p polint --lib analysis_kernel::tests::semantic_store_check_parity::all_store_modes_preserve_byte_identical_json_and_exit_semantics --locked -- --test-threads=1 &amp;&amp; cargo test -p polint --lib analysis_kernel::store::tests::metadata_invalidation_matrix --locked -- --test-threads=1 &amp;&amp; cargo test -p polint --test cli --locked -- --test-threads=1 &amp;&amp; cargo check -p polint --all-features --locked</automated>
  </verify>
  <acceptance_criteria>
    - Source order is validation report, unchanged diagnostics, fact finalization, enabled-only handoff/facade commit.
    - Disabled/no-cache creates no path/file/open and handoff/plan counters remain zero.
    - Parent kernel calls only commit_validated_run and cannot name plan/SQL internals.
    - Every META-04/status/rule/full-config/query-declaration class has persisted referenced invalidate and sibling Reuse after SQLite round trip.
    - Telemetry-only mutations preserve all semantic aggregates, run/generation identities, selection, and action vectors.
    - Twenty permutations yield identical stored rows, maps, semantic digests, and actions.
    - Check/review bytes/exits equal disabled mode for every listed store state.
  </acceptance_criteria>
  <done>The real metadata control plane is private, precisely invalidating, zero-cost when disabled, telemetry-independent, and policy-neutral.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description | Data Crossing |
|---|---|---|
| Finalized kernel run to store facade | Store failure is isolated from policy execution | Diagnostics, facts, metadata, private status |
| Persisted edges to invalidation planner | SQLite rows drive reuse/recompute actions | Typed endpoints, statuses, query declarations |
| Disabled configuration to kernel/store | Hard-off runs must avoid all persistence work | Config state, materialization counters, filesystem I/O |

## Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-65-18-01 | Tampering | Kernel/store ordering | mitigate | Commit only after validation/finalization and keep store outcomes policy-neutral. |
| T-65-18-02 | Denial of Service | Disabled path | mitigate | Dual early guards and zero-materialization/path/open counters. |
| T-65-18-03 | Tampering | Persisted invalidation | mitigate | Complete referenced/sibling/status/query mutation matrix uses the existing planner. |
| T-65-18-04 | Information Disclosure | Store outcome | mitigate | Private sanitized status and public output/CLI parity scans. |
| T-65-18-05 | Elevation of Privilege | Store facade visibility | mitigate | Parent can call only commit_validated_run; plan/connection/SQL remain private. |
| T-65-18-06 | Repudiation | Semantic determinism | mitigate | Telemetry-only and 20-order permutation tests preserve identities/actions. |
</threat_model>

<verification>
- cargo test -p polint --lib analysis_kernel::tests::semantic_store_check_parity::all_store_modes_preserve_byte_identical_json_and_exit_semantics --locked -- --test-threads=1
- cargo test -p polint --lib analysis_kernel::store::tests::metadata_invalidation_matrix --locked -- --test-threads=1
- cargo test -p polint --test cli --locked -- --test-threads=1
- cargo check -p polint --all-features --locked
- cargo fmt --all -- --check
</verification>

<success_criteria>
- Real finalized runs persist only when enabled, public behavior stays identical, telemetry stays non-semantic, and every invalidation class is precisely proven.
</success_criteria>

<output>Create 65-18-SUMMARY.md when complete.</output>
