---
phase: 65-generation-manifest-and-metadata-mirroring
plan: 10
type: execute
wave: 10
depends_on: [65-09]
files_modified:
  - crates/polint/src/analysis_kernel/incremental/dependency_input.rs
  - crates/polint/src/analysis_kernel/incremental/mod.rs
autonomous: true
requirements: [META-01, META-04]
must_haves:
  truths:
    - "D-09/D-12/D-13: non-wire typed input vocabulary covers every META-04 class with explicit status/digest and stable codecs before CacheNode can carry it."
    - "CacheNode and every serde-visible dependency-index/layer-manifest shape remain completely unchanged in this compile boundary."
    - "DEPENDENCY_INDEX_SCHEMA remains polint-dependency-index-1, and no v1 writer can serialize a typed input node."
  artifacts:
    - path: "crates/polint/src/analysis_kernel/incremental/dependency_input.rs"
      provides: "Canonical non-wire typed input endpoint vocabulary independent of store code"
      contains: "InputDependencyKey"
    - path: "crates/polint/src/analysis_kernel/incremental/mod.rs"
      provides: "Crate-private module/re-export seam for the staged non-wire vocabulary"
      contains: "dependency_input"
  key_links:
    - from: "crates/polint/src/analysis_kernel/incremental/dependency_input.rs"
      to: "crates/polint/src/analysis_kernel/incremental/input_snapshot.rs"
      via: "InputDependencyKey reuses the canonical explicit input status and typed digest vocabulary"
      pattern: "InputComponentStatus|Digest"
    - from: "crates/polint/src/analysis_kernel/incremental/mod.rs"
      to: "crates/polint/src/analysis_kernel/incremental/dependency_input.rs"
      via: "crate-private staging seam; no dependency-index or layer-manifest wire consumer is added yet"
      pattern: "dependency_input|InputDependencyKey"
---

<objective>
Define the complete typed dependency-input vocabulary as a non-wire kernel contract while leaving the v1 dependency-index and every producer/consumer unchanged.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
@.agents/skills/rust-best-practices/SKILL.md
@.claude/skills/polint/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-CONTEXT.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-PATTERNS.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-09-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Define typed dependency inputs without changing any wire shape</name>
  <files>crates/polint/src/analysis_kernel/incremental/dependency_input.rs, crates/polint/src/analysis_kernel/incremental/mod.rs</files>
  <read_first>
    - .agents/skills/rust-best-practices/SKILL.md
    - .claude/skills/polint/SKILL.md
    - crates/polint/src/analysis_kernel/incremental/input_snapshot.rs (InputComponentStatus and explicit unavailable states)
    - crates/polint/src/analysis_kernel/incremental/digest.rs (typed digest purposes and codec style)
    - crates/polint/src/analysis_kernel/incremental/mod.rs (private module/re-export conventions)
    - crates/polint/src/analysis_kernel/incremental/dependency_index.rs (read-only CacheNode and polint-dependency-index-1 wire baseline)
    - crates/polint/src/analysis_kernel/incremental/layer_cache.rs (read-only manifest schema pin)
  </read_first>
  <action>
    Create InputDependencyKey with kind, stable_key, Digest, and InputComponentStatus. Define exact InputDependencyKind variants SourceFile, PackageProject, ProviderManifest, ProviderSchema, RequestedCapability, AnalysisSetting, LanguageLifecycle, ToolInvocation, Config, UpstreamLayer, SummaryDependency, QueryOption, BudgetProfile, SearchManifest, ExtensionCode, ExtensionDeclaredInput, Model, RuleCode, and RuleOptions. SearchManifest remains reserved vocabulary only. Add canonical constructors and exact label/parse_label methods with typed unknown-label errors.

    Keep this type deliberately non-wire in this plan: do not derive or implement serde, add serialized fixtures, add it to CacheNode, change DependencyIndex, alter layer manifests, touch exhaustive consumers, or migrate producers. Add focused constructor/kind/status/digest-agreement and stable label/parser tests only. Register and privately re-export the module through incremental/mod.rs using the narrowest visibility; if a staged dead-code expectation is needed, its reason describes the enduring producer-migration boundary and contains no delivery-history reference.

    Explicitly audit the untouched wire baseline after implementation: `CacheNode` still has only its existing variants, no `CacheNode::DependencyInput` construction exists, DEPENDENCY_INDEX_SCHEMA is still exactly polint-dependency-index-1, dependency_input.rs contains no serde/Serialize/Deserialize, and the current v1 dependency-index serialization tests remain unchanged and green. Plan 11 will add the first serde-visible typed-node variant, its serde coverage, and the unique polint-dependency-index-next-typed label atomically.
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis_kernel::incremental::dependency_input::tests --locked &amp;&amp; cargo test -p polint --lib analysis_kernel::incremental::dependency_index::tests --locked &amp;&amp; ! rg -n 'CacheNode::DependencyInput|DependencyInput\(' crates/polint/src/analysis_kernel/incremental &amp;&amp; ! rg -n 'serde|Serialize|Deserialize' crates/polint/src/analysis_kernel/incremental/dependency_input.rs &amp;&amp; rg -n 'DEPENDENCY_INDEX_SCHEMA: &amp;str = "polint-dependency-index-1"' crates/polint/src/analysis_kernel/incremental/dependency_index.rs &amp;&amp; cargo check -p polint --all-features --locked</automated>
  </verify>
  <acceptance_criteria>
    - Every InputDependencyKind has stable codec, constructor, status, and digest-mismatch tests.
    - dependency_input.rs has no serde derive/implementation or serialization test; it is non-wire vocabulary only.
    - `CacheNode::DependencyInput` and every typed-node construction are absent, while legacy CacheNode::Input(String), Extension(String), and ToolInvocation(String) remain unchanged.
    - DEPENDENCY_INDEX_SCHEMA is exactly polint-dependency-index-1 and v1 serialization contains no typed node.
    - DependencyIndex, change_set, invalidation, quarantine, layer_cache, keys, and all real producers are unmodified by this plan and compile against the existing v1 shape.
  </acceptance_criteria>
  <done>The complete typed input vocabulary exists without creating any serde-visible typed CacheNode or changing the v1 wire contract.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description | Data Crossing |
|---|---|---|
| Input snapshot vocabulary to staged endpoint type | Canonical status and digest semantics become a non-wire typed value | Typed kinds, stable keys, digests, statuses |
| Staged vocabulary to existing v1 incremental core | A future endpoint type is visible to Rust modules but must not enter serialization | Private re-exports versus CacheNode/wire types |

## Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-65-10-01 | Denial of Service | Compile staging | mitigate | Limit edits to the new non-wire module/re-export and cargo-check every unchanged producer. |
| T-65-10-02 | Tampering | Typed input digest | mitigate | Purpose-check canonical constructors and mutation-test every input kind/status/digest before graph integration. |
| T-65-10-03 | Spoofing | Endpoint codec | mitigate | Closed label parsers reject unknown kinds without prefix inference. |
| T-65-10-04 | Repudiation | v1 wire boundary | mitigate | Source audits prove no serde implementation, CacheNode variant, typed serialization, or schema-label change lands here. |
</threat_model>

<verification>
- cargo test -p polint --lib analysis_kernel::incremental::dependency_input::tests --locked
- cargo test -p polint --lib analysis_kernel::incremental::dependency_index::tests --locked
- ! rg -n 'CacheNode::DependencyInput|DependencyInput\(' crates/polint/src/analysis_kernel/incremental
- ! rg -n 'serde|Serialize|Deserialize' crates/polint/src/analysis_kernel/incremental/dependency_input.rs
- rg -n 'DEPENDENCY_INDEX_SCHEMA: &str = "polint-dependency-index-1"' crates/polint/src/analysis_kernel/incremental/dependency_index.rs
- cargo check -p polint --all-features --locked
- cargo fmt --all -- --check
</verification>

<success_criteria>
- Typed dependency inputs are staged as canonical non-wire vocabulary while v1 serialization and every producer remain unchanged.
</success_criteria>

<output>Create 65-10-SUMMARY.md when complete.</output>
