---
phase: 65-generation-manifest-and-metadata-mirroring
plan: 13
type: execute
wave: 13
depends_on: [65-12]
files_modified:
  - crates/polint/src/analysis_kernel/mod.rs
  - crates/polint/src/analysis_kernel/validation.rs
  - crates/polint/src/analysis_kernel/incremental/run_report.rs
  - crates/polint/src/analysis/calls/validate.rs
  - crates/polint/src/analysis/entrypoints/validate.rs
  - crates/polint/src/eval/performance.rs
autonomous: true
requirements: [STORE-04, META-01]
must_haves:
  truths:
    - "D-16: the authoritative global fact validator emits deterministic structured events in the same pass without parsing diagnostics."
    - "D-02/D-19: event kind/status/count/digest is semantic; rendered text, duration, timestamp, and counters are excluded."
    - "Every validate_fact_metadata caller, including calls/validate.rs and entrypoints/validate.rs, migrates at this compile-clean return-type boundary."
    - "D-20: diagnostic bytes/order and policy behavior remain unchanged."
  artifacts:
    - path: "crates/polint/src/analysis_kernel/validation.rs"
      provides: "Structured validation report and complete event vocabulary"
      contains: "FactValidationReport"
---

<objective>
Return structured trust evidence from the authoritative validator and migrate every direct caller without a hidden compatibility gap.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
@.agents/skills/rust-best-practices/SKILL.md
@.claude/skills/polint/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-CONTEXT.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-PATTERNS.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-12-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Emit events and migrate the complete caller set</name>
  <files>crates/polint/src/analysis_kernel/mod.rs, crates/polint/src/analysis_kernel/validation.rs, crates/polint/src/analysis_kernel/incremental/run_report.rs, crates/polint/src/analysis/calls/validate.rs, crates/polint/src/analysis/entrypoints/validate.rs, crates/polint/src/eval/performance.rs</files>
  <read_first>
    - .agents/skills/rust-best-practices/SKILL.md
    - .claude/skills/polint/SKILL.md
    - crates/polint/src/analysis_kernel/validation.rs (top-level stages and every direct test call)
    - crates/polint/src/analysis_kernel/mod.rs (production validation/finalization order)
    - crates/polint/src/analysis/calls/validate.rs (all three direct calls)
    - crates/polint/src/analysis/entrypoints/validate.rs (direct call)
    - crates/polint/src/analysis_kernel/incremental/run_report.rs (private handoff)
    - crates/polint/src/eval/performance.rs (synthetic KernelRunReport constructor)
    - output of repository-wide rg 'validate_fact_metadata\s*\(' before edits
  </read_first>
  <action>
    Replace validate_fact_metadata's Vec&lt;Diagnostic&gt; return with FactValidationReport { diagnostics, events }. Define closed ValidationEventKind for every current top-level validation stage plus GlobalFactValidation and Passed/Failed status, with exact codecs and typed rejection. Each event records deterministic issue_count and purpose-specific digest built while the validator runs; never parse rendered diagnostics.

    In AnalysisKernel::run preserve exact order: produce report, extend diagnostics from report.diagnostics unchanged, retain report.events privately, then finish_all_fact_meta_insertions. KernelRunReport carries events for later handoff. The eval synthetic report passes an explicit empty event vector because no validator ran.

    Migrate every direct caller found by rg, not just production: all validation.rs test calls, the three calls in analysis/calls/validate.rs, and the call in analysis/entrypoints/validate.rs must read report.diagnostics or the exact report fields they assert. Do not leave an IntoIterator/Deref/implicit Vec compatibility shim that could hide a missed caller. End with the same repository-wide rg and cargo check.
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis_kernel::validation --locked &amp;&amp; cargo test -p polint --lib analysis::calls::validate --locked &amp;&amp; cargo test -p polint --lib analysis::entrypoints::validate --locked &amp;&amp; cargo test -p polint --lib analysis_kernel::tests::semantic_store --locked &amp;&amp; cargo test -p polint --lib eval_observed_kernel_tests --locked &amp;&amp; cargo check -p polint --all-features --locked</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one deterministic event exists per current top-level stage plus GlobalFactValidation in stable order.
    - Passing/failing fixtures preserve identical diagnostics and report correct status/count/digest.
    - Every event codec round-trips and rejects unknown labels.
    - Every rg-discovered direct caller uses FactValidationReport explicitly; no compatibility seam exists.
    - Source order remains report, unchanged diagnostic extension, fact finalization, later persistence.
    - No event identity includes message text, cache status/counters, timestamp, duration, source/body data, or relational concepts.
  </acceptance_criteria>
  <done>Authoritative validation yields structured events and every direct caller compiles on the new explicit report contract.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description | Data Crossing |
|---|---|---|
| Validators to durable trust evidence | Diagnostic-producing stages emit machine-readable events | Issue counts, statuses, event digests, messages |
| Return-type migration to test consumers | A Vec-shaped API becomes a structured report | Diagnostics and events across multiple modules |

## Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-65-13-01 | Spoofing | Validation evidence | mitigate | Build events inside validators with closed stage/status codecs, never from messages. |
| T-65-13-02 | Tampering | Caller migration | mitigate | Repository-wide call audit and no implicit Vec compatibility seam. |
| T-65-13-03 | Repudiation | Diagnostic compatibility | mitigate | Preserve ordered diagnostics and run existing byte/order regression tests. |
| T-65-13-04 | Information Disclosure | Validation event | mitigate | Exclude rendered messages, source/body data, paths, timestamps, and raw errors. |
</threat_model>

<verification>
- rg -n 'validate_fact_metadata\s*\(' crates/polint/src crates/polint/tests tests
- cargo test -p polint --lib analysis_kernel::validation --locked
- cargo check -p polint --all-features --locked
- cargo fmt --all -- --check
</verification>

<success_criteria>
- Complete deterministic validation evidence is available with diagnostics unchanged and no omitted caller.
</success_criteria>

<output>Create 65-13-SUMMARY.md when complete.</output>
