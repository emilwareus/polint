---
phase: 65-generation-manifest-and-metadata-mirroring
plan: 09
type: execute
wave: 9
depends_on: [65-08]
files_modified:
  - crates/polint/src/analysis_kernel/mod.rs
  - crates/polint/src/analysis/data_flow/cache_key.rs
  - crates/polint/src/analysis/data_flow/provider.rs
  - crates/polint/src/analysis/evidence/cache_key.rs
  - crates/polint/src/analysis/evidence/provider.rs
  - crates/polint/src/analysis/reachability/provider.rs
  - crates/polint/src/analysis/refined_calls/cache_key.rs
  - crates/polint/src/analysis/refined_calls/provider.rs
  - crates/polint/src/analysis/summaries/provider.rs
autonomous: true
requirements: [META-01, META-04]
must_haves:
  truths:
    - "D-14: data-flow, evidence, reachability, and refined-call production keys use scoped settings and exact declared upstream/model/extension inputs."
    - "D-14: SCC closure cache identity uses declared summary settings/capabilities/budget/upstreams, not config_hash, rule_hash, or full plan digest."
    - "D-14/D-15: a whole-kernel mutation matrix proves rule severity/files/max/options preserve analysis hits while requested capabilities/settings/models/extensions invalidate only linked providers."
    - "D-06: full ConfigIdentity/rule identity remains solely at snapshot, diagnostic, rule-result, and future store-manifest boundaries."
  artifacts:
    - path: "crates/polint/src/analysis_kernel/mod.rs"
      provides: "Production-key D-14 regression matrix"
      contains: "rule_only_changes_preserve_analysis_hits"
---

<objective>
Finish the production-key identity split and prove D-14 across a real kernel run, not only isolated constructors.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
@.agents/skills/rust-best-practices/SKILL.md
@.claude/skills/polint/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-CONTEXT.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-PATTERNS.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-08-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Migrate remaining policy-analysis provider keys</name>
  <files>crates/polint/src/analysis/data_flow/cache_key.rs, crates/polint/src/analysis/data_flow/provider.rs, crates/polint/src/analysis/evidence/cache_key.rs, crates/polint/src/analysis/evidence/provider.rs, crates/polint/src/analysis/reachability/provider.rs, crates/polint/src/analysis/refined_calls/cache_key.rs, crates/polint/src/analysis/refined_calls/provider.rs</files>
  <read_first>
    - .agents/skills/rust-best-practices/SKILL.md
    - .claude/skills/polint/SKILL.md
    - crates/polint/src/analysis/data_flow/cache_key.rs and crates/polint/src/analysis/data_flow/provider.rs
    - crates/polint/src/analysis/evidence/cache_key.rs and crates/polint/src/analysis/evidence/provider.rs
    - crates/polint/src/analysis/reachability/provider.rs (configured-roots full-config comment)
    - crates/polint/src/analysis/refined_calls/cache_key.rs and crates/polint/src/analysis/refined_calls/provider.rs
    - crates/polint/src/analysis_kernel/incremental/input_snapshot.rs (provider scopes and exact component access)
  </read_first>
  <action>
    Replace full config contributions with provider-scoped settings in all remaining cache/output parameter digests. Data-flow and evidence consume only declared solver/data-flow/evidence budgets, relevant lifecycle/capability state, upstream layers, and model/extension outputs. Reachability consumes its roots setting plus declared graph inputs. Refined calls consume their scoped parameters and upstream identity/call/graph inputs. Never fold every snapshot capability/model/extension row into a key; include only referenced rows.

    Add provider-level paired tests for relevant mutation invalidation and unreferenced sibling preservation, including custom RuleOptions settings as a preserve control.
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis::data_flow --locked &amp;&amp; cargo test -p polint --lib analysis::evidence --locked &amp;&amp; cargo test -p polint --lib analysis::reachability::provider::tests --locked &amp;&amp; cargo test -p polint --lib analysis::refined_calls --locked</automated>
  </verify>
  <done>All remaining provider keys use exact scoped analysis dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the production key split end to end</name>
  <files>crates/polint/src/analysis_kernel/mod.rs, crates/polint/src/analysis/summaries/provider.rs</files>
  <read_first>
    - crates/polint/src/analysis_kernel/mod.rs (all production provider calls, LayerKey/output aggregation, semantic-store tests)
    - crates/polint/src/cache/keys.rs (full and scoped mutation helpers)
    - crates/polint/src/analysis_plan.rs (full and analysis requirement identities)
    - crates/polint/src/analysis_kernel/incremental/input_snapshot.rs (full/scoped v2 rows)
    - crates/polint/src/analysis/summaries/provider.rs (run_scc_closure_with_cache and scc_closure_cache_key)
    - crates/polint/src/analysis/summaries/closure.rs (read-only SCC budget/query/upstream inputs)
  </read_first>
  <action>
    Replace direct-summary/SCC remaining specialized key inputs and SCC closure's CacheKey(config_digest, rule_digest, plan_digest) with DigestKind::AnalysisSettings plus a canonical SCC analysis identity derived from direct-summary scoped settings, relevant requested-capability projection, SCC budget/query parameters, model/extension declarations actually read, and upstream direct-summary/call dependencies. Close the last Plan 07 semantic LayerKey seam. Full rule/config identities remain only on run-manifest/diagnostic boundaries. Add SCC cold/warm preserve/invalidate controls.

    Add a temp-repo/real-cache kernel matrix that runs cold then warm while mutating one field at a time. Severity, files, allow_files, allow, deny, max, forbidden_imports, description, and custom rule settings must change full config/rule/store-manifest identity but preserve analysis layer/provider/SCC hits when requested capabilities/settings/models/extensions are unchanged. A newly requested relevant capability, support/setup transition, relevant provider setting, budget, model digest, extension code, or declared extension input must invalidate only linked providers. Unrequested/unreferenced sibling changes preserve hits.

    Add a repository-wide source assertion covering cache/keys.rs, InputSnapshot.config uses, every LayerKey producer, SCC closure, capability digest builders, and all provider cache/output key builders. Full identity may appear only at explicit manifest/diagnostic/rule-result boundaries; no analysis key may consume it directly or through a full plan/provider aggregate.
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis::summaries::provider::tests --locked &amp;&amp; cargo test -p polint --lib analysis_kernel::tests::rule_only_changes_preserve_analysis_hits --locked -- --test-threads=1 &amp;&amp; cargo test -p polint --lib analysis_kernel::tests::declared_analysis_inputs_invalidate_linked_providers --locked -- --test-threads=1 &amp;&amp; cargo check -p polint --all-features --locked</automated>
  </verify>
  <acceptance_criteria>
    - Production-key source audit finds no full config/rule/plan identity in any analysis LayerKey, SCC cache key, capability analysis digest, or provider output/cache digest.
    - Every specialized production LayerKey now requires DigestKind::AnalysisSettings; only the enumerated general-constructor core/helper seam remains for Plans 10-11.
    - Rule-only mutations demonstrably change full identities and preserve real warm analysis hits.
    - Relevant capability/settings/model/extension mutations invalidate only linked providers; unreferenced siblings reuse.
    - Custom rule options remain rule-result inputs unless an explicit provider mapping exists.
    - All focused suites and cargo check pass at this boundary.
  </acceptance_criteria>
  <done>D-14 is enforced across every production key and proven against real cache counters.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description | Data Crossing |
|---|---|---|
| Rule configuration to whole-kernel cache | Rule behavior and analysis dependencies coexist in one run | Rule options, provider settings, capabilities, cache stats |
| Snapshot to advanced providers | Providers select a subset of upstream/model/extension rows | Data-flow, evidence, reachability, refined-call inputs |

## Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-65-09-01 | Denial of Service | Whole analysis cache | mitigate | Real cold/warm mutation matrix proves rule-only fields preserve hits. |
| T-65-09-02 | Tampering | Advanced provider keys | mitigate | Exact field whitelists and source assertions reject full snapshot/config aggregates. |
| T-65-09-03 | Spoofing | Declared dependency set | mitigate | Relevant/unreferenced sibling tests prove only linked provider inputs participate. |
| T-65-09-04 | Repudiation | D-14 compliance | mitigate | Record full-identity changes alongside hit outcomes so preserve tests cannot pass vacuously. |
</threat_model>

<verification>
- rg -n 'input_snapshot\.config\.digest|rule_digest|plan\.digest\(\)' crates/polint/src/analysis crates/polint/src/analysis_kernel/incremental/keys.rs crates/polint/src/module_graph crates/polint/src/symbol_graph crates/polint/src/metrics.rs
- cargo check -p polint --all-features --locked
- cargo fmt --all -- --check
</verification>

<success_criteria>
- Every real analysis key uses declared capabilities/settings/models/extensions and preserves hits across rule-only changes.
</success_criteria>

<output>Create 65-09-SUMMARY.md when complete.</output>
