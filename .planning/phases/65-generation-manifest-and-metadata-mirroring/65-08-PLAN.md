---
phase: 65-generation-manifest-and-metadata-mirroring
plan: 08
type: execute
wave: 8
depends_on: [65-07]
files_modified:
  - crates/polint/src/analysis/provider.rs
  - crates/polint/src/analysis/cfg/provider.rs
  - crates/polint/src/analysis/calls/provider.rs
  - crates/polint/src/analysis/domains/provider.rs
  - crates/polint/src/analysis/entrypoints/provider.rs
  - crates/polint/src/analysis/identity/provider.rs
  - crates/polint/src/go/semantic/provider.rs
  - crates/polint/src/analysis/types/cache_key.rs
  - crates/polint/src/analysis/types/provider.rs
autonomous: true
requirements: [META-01, META-04]
must_haves:
  truths:
    - "D-06/D-14: MIR, CFG, calls, domains, entrypoints, identity, Go semantic, and type/alias production digests use only their scoped settings and declared inputs."
    - "D-14/D-15: rule-only mutations preserve provider output/cache identity; relevant provider settings or requested inputs invalidate."
    - "D-13: present/absent/unsupported/setup-missing lifecycle inputs remain first-class in provider identities."
  artifacts:
    - path: "crates/polint/src/analysis/cfg/provider.rs"
      provides: "Scoped CFG production digest"
      contains: "analysis_settings"
---

<objective>
Migrate the first semantic-provider slice from full InputSnapshot.config identity to declared provider-scoped analysis inputs.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
@.agents/skills/rust-best-practices/SKILL.md
@.claude/skills/polint/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-CONTEXT.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-PATTERNS.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-07-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Migrate semantic provider output/cache digests</name>
  <files>crates/polint/src/analysis/provider.rs, crates/polint/src/analysis/cfg/provider.rs, crates/polint/src/analysis/calls/provider.rs, crates/polint/src/analysis/domains/provider.rs, crates/polint/src/analysis/entrypoints/provider.rs, crates/polint/src/analysis/identity/provider.rs, crates/polint/src/go/semantic/provider.rs, crates/polint/src/analysis/types/cache_key.rs, crates/polint/src/analysis/types/provider.rs</files>
  <read_first>
    - .agents/skills/rust-best-practices/SKILL.md
    - .claude/skills/polint/SKILL.md
    - crates/polint/src/analysis_kernel/incremental/input_snapshot.rs (scoped settings/capability/lifecycle accessors)
    - crates/polint/src/analysis/provider.rs (semantic MIR output digest)
    - crates/polint/src/analysis/cfg/provider.rs (CFG digest builder)
    - crates/polint/src/analysis/calls/provider.rs (calls digest builder)
    - crates/polint/src/analysis/domains/provider.rs (domain digest builders)
    - crates/polint/src/analysis/entrypoints/provider.rs (entrypoint digest)
    - crates/polint/src/analysis/identity/provider.rs (identity digest)
    - crates/polint/src/go/semantic/provider.rs (Go lifecycle/setup digest)
    - crates/polint/src/analysis/types/cache_key.rs and crates/polint/src/analysis/types/provider.rs (type/alias keys)
  </read_first>
  <action>
    Replace each full input_snapshot.config.digest contribution with the exact provider-scoped analysis-settings identity and declared capability/lifecycle/model/extension/upstream rows consumed by that provider. Migrate the semantic MIR, CFG, calls, domains, identity, Go semantic, and type/alias specialized LayerKey inputs in this slice to DigestKind::AnalysisSettings and close their Plan 07 temporary seams. Do not add the aggregate capability set or full InputSnapshot identity as a convenience. Preserve explicit unavailable states and all existing provider parameter/version/schema inputs.

    Where a provider has no configurable analysis setting, use its typed absent scoped-settings digest rather than full config. Type/alias cache and output builders must use the same canonical projection. Update comments that claim configured inputs ride on the full config digest.

    Add table-driven production mutations in the existing per-file test modules: severity/files/allow_files/max/custom rule settings and an unrelated provider setting preserve output/cache identity; a relevant setting, requested capability, lifecycle/setup state, model/extension declaration, or upstream digest changes it. Ensure full ConfigIdentity still changes in the rule-only controls, proving the split rather than an inert test.
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis::provider::tests --locked &amp;&amp; cargo test -p polint --lib analysis::cfg::provider::tests --locked &amp;&amp; cargo test -p polint --lib analysis::calls::provider::tests --locked &amp;&amp; cargo test -p polint --lib analysis::domains::provider::tests --locked &amp;&amp; cargo test -p polint --lib analysis::entrypoints::provider::tests --locked &amp;&amp; cargo test -p polint --lib analysis::identity::provider::tests --locked &amp;&amp; cargo test -p polint --lib go::semantic::provider::tests --locked &amp;&amp; cargo test -p polint --lib analysis::types --locked</automated>
  </verify>
  <acceptance_criteria>
    - rg across all nine files finds no production input_snapshot.config.digest contribution.
    - Each builder names its provider scope and exact declared capability/lifecycle/model/extension/upstream inputs.
    - No Plan 07 temporary LayerKey seam remains for this provider slice.
    - Rule-only controls change ConfigIdentity/full plan identity but preserve provider identity and cache hits.
    - Relevant mutations invalidate and unreferenced sibling mutations preserve.
    - All provider suites and cargo check -p polint --all-features --locked pass at this boundary.
  </acceptance_criteria>
  <done>The first semantic provider slice implements the scoped D-14 contract in real output/cache keys.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description | Data Crossing |
|---|---|---|
| InputSnapshot to semantic providers | Providers choose among full and scoped identity rows | Config, capabilities, lifecycle, model/extension digests |

## Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-65-08-01 | Tampering | Provider digest builders | mitigate | Source-audit each full-config read and require exact scoped accessors. |
| T-65-08-02 | Denial of Service | Semantic provider caches | mitigate | Rule-only preserve-hit tests prevent avoidable broad invalidation. |
| T-65-08-03 | Spoofing | Unavailable input state | mitigate | Carry typed present/absent/unsupported/setup-missing rows into declared identities. |
| T-65-08-04 | Repudiation | Mutation coverage | mitigate | Paired relevant/unreferenced tests prove why each digest does or does not change. |
</threat_model>

<verification>
- rg -n 'input_snapshot\.config\.digest' crates/polint/src/analysis/provider.rs crates/polint/src/analysis/cfg/provider.rs crates/polint/src/analysis/calls/provider.rs crates/polint/src/analysis/domains/provider.rs crates/polint/src/analysis/entrypoints/provider.rs crates/polint/src/analysis/identity/provider.rs crates/polint/src/go/semantic/provider.rs crates/polint/src/analysis/types
- cargo check -p polint --all-features --locked
- cargo fmt --all -- --check
</verification>

<success_criteria>
- This provider slice depends on only declared analysis settings/capabilities/models/extensions and precise upstream facts.
</success_criteria>

<output>Create 65-08-SUMMARY.md when complete.</output>
