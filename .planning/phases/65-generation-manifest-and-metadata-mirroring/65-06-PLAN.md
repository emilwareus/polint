---
phase: 65-generation-manifest-and-metadata-mirroring
plan: 06
type: execute
wave: 6
depends_on: [65-05]
files_modified:
  - crates/polint/src/analysis_kernel/incremental/input_snapshot.rs
  - crates/polint/src/analysis_kernel/incremental/mod.rs
  - crates/polint/src/analysis/evidence/cache_key.rs
  - crates/polint/src/analysis/evidence/provider.rs
  - crates/polint/src/analysis/extensions/provider.rs
  - crates/polint/src/analysis/types/cache_key.rs
  - crates/polint/src/eval/performance.rs
  - crates/polint/src/eval/observed.rs
  - crates/polint/tests/cli.rs
  - tests/eval-fixtures/cache/input-snapshots/expected.polint-eval.toml
autonomous: true
requirements: [STORE-04, META-01, META-04]
must_haves:
  truths:
    - "D-05/D-06/D-09: InputSnapshot v2 owns typed workspace/full-config identity, provider-scoped analysis settings, and explicit requested-capability rows."
    - "D-12/D-13/D-14: full rule/config rows remain mirror truth while analysis_dependency_digest excludes rule-only metadata/options."
    - "D-19: mtime hints and any telemetry are excluded from canonical snapshot/analysis digests."
    - "D-20: all literals/renderers/schema fixtures update atomically with no public JSON drift."
  artifacts:
    - path: "crates/polint/src/analysis_kernel/incremental/input_snapshot.rs"
      provides: "InputSnapshot v2 with full and provider-scoped identity classes"
      contains: "polint-input-snapshot-2"
---

<objective>
Land InputSnapshot v2 atomically after all callers are plan-aware, preserving full manifest truth while exposing precise provider analysis dependencies.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
@.agents/skills/rust-best-practices/SKILL.md
@.claude/skills/polint/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-CONTEXT.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-PATTERNS.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-05-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add v2 identity rows and update every literal/schema consumer</name>
  <files>crates/polint/src/analysis_kernel/incremental/input_snapshot.rs, crates/polint/src/analysis_kernel/incremental/mod.rs, crates/polint/src/analysis/evidence/cache_key.rs, crates/polint/src/analysis/evidence/provider.rs, crates/polint/src/analysis/extensions/provider.rs, crates/polint/src/analysis/types/cache_key.rs, crates/polint/src/eval/performance.rs, crates/polint/src/eval/observed.rs, crates/polint/tests/cli.rs, tests/eval-fixtures/cache/input-snapshots/expected.polint-eval.toml</files>
  <read_first>
    - .agents/skills/rust-best-practices/SKILL.md
    - .claude/skills/polint/SKILL.md
    - crates/polint/src/analysis_kernel/incremental/input_snapshot.rs (struct, constructor, all serialization tests)
    - crates/polint/src/analysis_kernel/incremental/mod.rs (crate-private re-exports)
    - crates/polint/src/analysis/evidence/cache_key.rs and crates/polint/src/analysis/evidence/provider.rs (two literals)
    - crates/polint/src/analysis/extensions/provider.rs (two literals)
    - crates/polint/src/analysis/types/cache_key.rs (one literal)
    - crates/polint/src/eval/performance.rs (one literal)
    - crates/polint/src/eval/observed.rs and tests/eval-fixtures/cache/input-snapshots/expected.polint-eval.toml (all schema pins)
    - crates/polint/tests/cli.rs (private vocabulary assertions)
    - crates/polint/src/analysis/data_flow/cache_key.rs, crates/polint/src/analysis/data_flow/provider.rs, and crates/polint/src/analysis/refined_calls/cache_key.rs (read-only field-selective consumers)
  </read_first>
  <action>
    Bump INPUT_SNAPSHOT_SCHEMA_VERSION to polint-input-snapshot-2. Add workspace_identity, config_identity (the full config digest), analysis_settings (sorted provider-scoped identities), requested_capabilities (including mirror-only requesting rules/full rule behavior), and analysis_requirements_identity (the rule-independent capability projection). Retain existing full config/rules components for manifest and diagnostic truth. Add purpose-specific full input-snapshot aggregate over every semantic row; exclude mtime_hint_present, ordering, docs/hints/rendered reasons, counters, durations, and timestamps.

    Update all six InputSnapshot literals across the five audited source files with explicit test identities/settings/capabilities. No Default or serde-default escape may erase production state; every empty literal asserts no plan capability. Keep data-flow/evidence/type/refined-call key builders field-selective until Plans 08-09 migrate them to scoped settings.

    Update eval/observed, CLI private assertions, and the checked-in TOML to v2 at the same boundary. The full-pipeline fixture must contain at least one requested capability, typed status/language, a full config identity, and scoped settings. Present/Unsupported/SetupMissing remain distinct; absence means not requested. Root/source/environment sentinels must not serialize.
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis_kernel::incremental::input_snapshot::tests --locked &amp;&amp; cargo test -p polint --lib eval_observed_kernel_tests::observed_phase23_cache_counter_invariants --locked &amp;&amp; cargo test -p polint --lib eval::performance::tests --locked &amp;&amp; cargo test -p polint --test cli --locked input_snapshots_stay_internal -- --test-threads=1 &amp;&amp; cargo check -p polint --all-features --locked</automated>
  </verify>
  <acceptance_criteria>
    - INPUT_SNAPSHOT_SCHEMA_VERSION and every checked-in/internal schema expectation equal polint-input-snapshot-2.
    - Every InputSnapshot literal supplies all new fields explicitly and no default escape exists.
    - Rule-only mutations change config_identity/full snapshot identity but preserve analysis_requirements_identity and every unrelated provider setting digest.
    - Relevant provider setting/capability/model/extension mutations change their declared row; unreferenced sibling rows remain stable.
    - mtime/status counters/durations/timestamps do not affect canonical semantic digests.
    - Root/source/environment sentinels are absent from serialization and identities.
  </acceptance_criteria>
  <done>InputSnapshot v2 contains complete manifest truth and precise analysis dependency sources.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description | Data Crossing |
|---|---|---|
| Analysis plan/config to serialized snapshot | Full and scoped identities share one metadata envelope | Rule config, capabilities, provider settings |
| Workspace to snapshot identity | Repository inputs become durable metadata | Relative paths, source digests, root identity |

## Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-65-06-01 | Tampering | Snapshot identity classes | mitigate | Required typed fields and mutation tests prevent full/scoped identity substitution. |
| T-65-06-02 | Information Disclosure | Snapshot serialization | mitigate | Hash/discard roots and scan for source/environment/path sentinels. |
| T-65-06-03 | Denial of Service | Provider invalidation | mitigate | Analysis settings and capability projection exclude rule-only metadata/options. |
| T-65-06-04 | Repudiation | Schema migration | mitigate | Update every v2 fixture/renderer in one compile-clean plan and assert exact version. |
</threat_model>

<verification>
- cargo test -p polint --lib analysis_kernel::incremental::input_snapshot::tests --locked
- cargo check -p polint --all-features --locked
- cargo fmt --all -- --check
</verification>

<success_criteria>
- InputSnapshot v2 is complete, explicit, deterministic, and separates full manifest identity from provider analysis identity.
</success_criteria>

<output>Create 65-06-SUMMARY.md when complete.</output>
