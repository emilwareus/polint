---
phase: 65-generation-manifest-and-metadata-mirroring
plan: 06
title: R5 Go Syntax Metadata Mirror
type: execute
wave: 6
depends_on: [65-05]
files_modified:
  - crates/polint/src/analysis_kernel/go_syntax_projection.rs
  - crates/polint/src/analysis_kernel/mod.rs
  - crates/polint/src/analysis_kernel/provider.rs
  - crates/polint/src/analysis_kernel/incremental/layer_cache.rs
  - crates/polint/src/go/adapter.rs
  - crates/polint/src/go/tests.rs
  - crates/polint/src/analysis_kernel/store/go_syntax_mirror.rs
  - crates/polint/src/analysis_kernel/store/mod.rs
  - crates/polint/src/analysis_kernel/store/generation.rs
  - crates/polint/src/analysis_kernel/store/migrations.rs
  - crates/polint/src/analysis_kernel/store/tests.rs
  - crates/polint/src/runner/mod.rs
  - crates/polint/tests/public_surface_leak.rs
autonomous: true
requirements: []
contributes_to: [STORE-04, STORE-05, META-01, META-04]
completion_scope: r5-go-syntax-only
completion_policy:
  mark_requirements_complete: false
  mark_phase_complete: false
  remaining_slices:
    - R5 TypeScript syntax mirror
    - R5 additional provider increments after independent readiness audits
    - R6 private enablement and measured reuse
budget:
  max_tasks: 3
  max_product_test_files: 15
  declared_product_test_files: 13
  max_handwritten_additions: 2500
  max_new_durable_schema_families: 1
  retained_provider_families: [polint.metrics]
  newly_mirrored_provider_families: [polint.go.syntax]

must_haves:
  truths:
    - "D-01/D-02/D-03/D-04/D-05: this plan adds only the first R5 increment on locked R1-R4, including the narrowly required private-manifest compatibility repair for the already-public `StringLiterals<'_>` view, uses exactly three tasks and at most thirteen declared product/test files/2,500 handwritten additions/one new durable schema family/one newly mirrored provider, keeps `requirements: []` and Phase 65 open, stops on scope expansion, and leaves `.github/workflows/ci.yml` untouched."
    - "D-06/D-07/D-08/D-09/D-10: `polint.go.syntax` is the sole new mirrored provider; the only cross-language touch is a private file-presence ownership repair so `string_literals` selects Go syntax for Go files and TS syntax for TS/JS files (both for mixed repositories) without gating either language on an absent one; `polint.ts.syntax` mirroring, `polint.go.semantic`, `polint.source`, graph/solver/extension/model/summary/query providers, external Go commands, and the TypeScript acceptance half of issue #89 remain outside this increment."
    - "D-11/D-12/D-13/D-14/D-15/D-16: one path-sorted, path-unique Go source projection and one closed parser contract drive only the Go syntax key; exact source bytes, canonical relative paths, Go language, provider/schema/payload versions, and tree-sitter backend/grammar identity participate, while config/rules/plan/TypeScript/module lifecycle/external toolchain/extensions/models/timing/cache disposition do not."
    - "D-17/D-18/D-19/D-20/D-21/D-22/D-23: one relationship-validated canonical produced value covers all six Go-owned families—packages, functions, imports, Go tests, branch obligations, and string literals—plus deterministic parser diagnostics; private runtime capability ownership follows present source languages and failure-gates `StringLiterals<'_>` on every and only present-language owner; cold/warm/cache-disabled identities are equal; cache warnings remain run-local; layer reads require the exact sorted source/parser edge vector and reject every missing, extra, replaced, duplicated, or reordered edge."
    - "D-24/D-25/D-26/D-27/D-28/D-29: schema v5 adds exactly one private relational Go-syntax mirror family beside the accepted metrics family; every published generation owns one exact static Go syntax manifest and one legal six-state outcome; only success carries output identity plus canonical source/parser dependencies; only dependency-blocked carries the exact `polint.source` blocker; no facts, source bodies, generic `DependencyIndex`, or redundant reverse map persist."
    - "D-30/D-31/D-32/D-33/D-34/D-35/D-36: the accepted immediate transaction writes/readbacks/recomputes the run manifest, metrics mirror, and complete Go syntax projection before selection; every new failure seam rolls back candidate children and preserves the previous active generation; only exact empty v4 migrates; one-snapshot active reads authenticate all three projections; Go source/path/parser mutations miss while config/rules/plan/TypeScript/cache-only mutations preserve an exact Go match; normal `AnalysisKernel::run` remains maintenance-only."
    - "D-37/D-38/D-39/D-40/D-41/D-42/D-43/D-44: a real tiny Go fixture proves reserve/publish/close/reopen/read/exact-match beside metrics; non-success cannot match; relational, catalog, storage-class, bound, blocker, identity, dependency, and path tampering fails closed before unbounded allocation; disabled/refusal/public behavior and privacy remain intact; isolated network-free tests use no sleeps, environment mutation, global serialization, or external Go process; every required target is under sixty seconds; acceptance is exactly one additional metadata provider and leaves later R5 increments plus R6 open."
  artifacts:
    - path: "crates/polint/src/analysis_kernel/go_syntax_projection.rs"
      provides: "Canonical Go source/parser inputs, relationship-validated six-family produced syntax output, and sealed durable projection"
      contains: "GoSyntaxProviderProjection"
    - path: "crates/polint/src/analysis_kernel/provider.rs"
      provides: "Private static polint.go.syntax inventory with ordered string_literals output appended after the existing five outputs"
      contains: "string_literals"
    - path: "crates/polint/src/analysis_kernel/mod.rs"
      provides: "Private present-language string_literals capability ownership and exact runtime provider-failure gating"
      contains: "capability_providers"
    - path: "crates/polint/src/go/adapter.rs"
      provides: "Go-only key/dependency construction, canonical output identity, and semantic-versus-operational diagnostic separation"
      contains: "go_syntax_layer_dependency_edges"
    - path: "crates/polint/src/analysis_kernel/incremental/layer_cache.rs"
      provides: "Canonical dependency-vector preservation and fail-closed rejection of duplicate/reordered syntax edges"
      contains: "LayerCacheManifest"
    - path: "crates/polint/src/analysis_kernel/store/go_syntax_mirror.rs"
      provides: "Bounded relational Go syntax writer/reader, exact static metadata decode, witness recomputation, and typed reconstruction"
      contains: "GoSyntaxProviderProjection"
    - path: "crates/polint/src/analysis_kernel/store/migrations.rs"
      provides: "Strict schema-v5 authentication and exact-empty-v4-only migration for one Go syntax family"
      contains: "CURRENT_SCHEMA_VERSION"
    - path: "crates/polint/src/analysis_kernel/store/generation.rs"
      provides: "Atomic run-manifest plus metrics plus Go-syntax publication, active read, exact-match, and rollback seams"
      contains: "GoSyntaxMatch"
    - path: "crates/polint/src/analysis_kernel/store/tests.rs"
      provides: "Real success/non-success round trips, mutation pairs, rollback, allocation-bound, and SQLite tamper proof"
      contains: "go_syntax_provider_mirror"
    - path: "crates/polint/tests/public_surface_leak.rs"
      provides: "Negative proof that Go syntax store/parser vocabulary does not widen supported surfaces"
      contains: "semantic_store_markers_do_not_leak_into_supported_public_surfaces"
  key_links:
    - from: "CanonicalGoSyntaxInputs and GoSyntaxParserContract"
      to: "go_syntax_layer_key and go_syntax_layer_dependency_edges"
      via: "The same path-sorted source-row digests and parser-contract digest fill the Go key and exact forward edge vector; fixed generic config/lifecycle/toolchain slots use purpose-typed absence."
      pattern: "CanonicalGoSyntaxInputs|GoSyntaxParserContract|go_syntax_layer_key|go_syntax_layer_dependency_edges"
    - from: "CanonicalGoSyntaxOutput"
      to: "ProviderAnalysisResult.output_digest and sealed ProviderOutputIdentity"
      via: "The adapter validates package/function/import/test/branch/string-literal relationships and parser diagnostics before deriving one ProviderOutput digest used by cold, warm, and disabled-cache execution."
      pattern: "CanonicalGoSyntaxOutput|output_digest|ProviderOutputIdentity"
    - from: "Static polint.go.syntax ProviderManifest"
      to: "CanonicalGoSyntaxOutput and durable manifest members"
      via: "The private output inventory is exactly packages, functions, imports, go_tests, branch_obligations, string_literals; projection tests and schema member rows use that same order."
      pattern: "polint.go.syntax|string_literals|CanonicalGoSyntaxOutput|go_syntax_provider_members"
    - from: "StringLiterals capability requests plus AnalysisDb file languages"
      to: "AnalysisKernel::capability_providers and runtime_capability_blockers"
      via: "The private owner set includes polint.go.syntax iff Go files exist and polint.ts.syntax iff TS/JS files exist, in stable order; Go-only, TS-only, and mixed failure regressions prove an absent language never gates the requesting rule while every present owner does."
      pattern: "string_literals|capability_providers|runtime_capability_blockers|polint.go.syntax|polint.ts.syntax"
    - from: "Static polint.go.syntax manifest plus sealed outcome plus canonical dependencies"
      to: "GoSyntaxProviderProjection"
      via: "A closed constructor accepts only the current static manifest, success-only identity/dependencies, and dependency-blocked-only polint.source blocker."
      pattern: "polint.go.syntax|ProviderOutcome|GoSyntaxProviderProjection"
    - from: "SemanticStore::publish_generation"
      to: "run manifest, metrics mirror, and Go syntax relational rows"
      via: "One immediate transaction performs bounded readback, row-derived witness recomputation, exact typed comparison, completion, and active-pointer rotation."
      pattern: "publish_generation|publish_transaction|read_metrics|read_go_syntax"
    - from: "Selected complete generation"
      to: "Private active Go syntax read and GoSyntaxMatch"
      via: "One read snapshot authenticates lifecycle, workspace, run manifest, retained metrics projection, exact Go static manifest/outcome/identity/blockers/source rows/parser row before returning Exact, SemanticMiss, or NoActive."
      pattern: "active_go_syntax|match_active_go_syntax|GoSyntaxMatch"
---

# Phase 65 Plan 06: R5 Go Syntax Metadata Mirror

<objective>
Repair and mirror exactly one in-process syntax provider, `polint.go.syntax`,
without persisting syntax facts/source bodies or enabling normal-run durable
publication or reuse.

Purpose: close the retained Go half of the syntax dependency-authentication
finding with one canonical source/parser projection, one canonical produced
syntax identity, exact layer-cache dependency verification, and one private
schema-v5 metadata family published beside the accepted metrics projection.

Output: cache-independent Go syntax identity, exact source/parser layer edges,
an atomic relational Go syntax metadata mirror, and focused invalidation,
rollback, tamper, allocation-bound, parity, and privacy evidence. TypeScript
syntax, other R5 increments, R6, Phase 65, and all four mapped requirements
remain open.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/64-store-foundation-and-boundary-proof/64-CONTEXT.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-CONTEXT.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-PATTERNS.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-LEARNINGS.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-01-SUMMARY.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-02-SUMMARY.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-03-SUMMARY.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-04-SUMMARY.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-05-PLAN.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-05-SUMMARY.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-05-REVIEW.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-05-VERIFICATION.md
@.planning/forensics/report-20260719-phase-65-scope-collapse.md
@research/local-semantic-store/RESTART-PLAN.md
@research/local-semantic-store/IDENTITY-READINESS.md
@research/local-semantic-store/REVIEW-FINDINGS-TRIAGE.md
@research/local-semantic-store/implementation/STORE-CONTRACT.md
@research/local-semantic-store/decisions/DECISIONS.md
@docs/API-VISIBILITY-PLAN.md
@https://github.com/emilwareus/polint/issues/89

<interfaces>
Current contracts to preserve and extend narrowly:

- The code audit found a narrowly required compatibility repair:
  `go::adapter::extract_string_literals` emits `StringLiteralFact`,
  metadata assigns those rows to `polint.go.syntax`, and the already-public
  typed `StringLiterals<'_>` view consumes them, while the private static
  `ProviderManifest` currently declares only five outputs. Omitting this sixth
  Go-owned output from provider identity would certify an incomplete value and
  is forbidden.
- Repair only the private `polint.go.syntax` manifest inventory to the exact
  ordered outputs `["packages", "functions", "imports", "go_tests",
  "branch_obligations", "string_literals"]`. Go scope,
  `ExistingFileFactCache { schema: "go-facts-v2" }`, `go-facts-v2` schema
  version 2, and syntax precision remain unchanged. This records an existing
  public fact view; it adds no SDK, CLI, config, or output surface.
- `ProviderOutcome` already seals six legal final states. Only `Succeeded`
  carries `ProviderOutputIdentity`; `hard_dependencies("polint.go.syntax")` is
  exactly `["polint.source"]`.
- `go::adapter` currently builds a broad Go syntax layer key from source rows,
  config, absent lifecycle, a package-version tool label, and a Rust-`Debug`
  language rendering. It writes no layer dependencies, returns no output
  digest when caching is disabled, and allows per-file cache-write warnings
  into the outer syntax payload.
- `LayerCacheManifest::new` canonicalizes dependencies on write, while the
  reader currently repairs decoded order/duplicates before provider
  validation. Go syntax must compare the original decoded canonical vector so
  duplicate or reordered hostile rows cannot be hidden.
- `record_provider_projection` trusts an adapter-supplied output digest and
  otherwise falls back to a metadata summary containing transient run IDs.
  This increment makes every non-empty Go syntax execution supply the
  canonical produced-value digest; the fallback remains unchanged for other
  providers.
- Schema v4 owns the R1 lifecycle, R2 run manifest, and exactly one accepted
  `polint.metrics` projection. `PublicationInputs` and
  `generation::publish_transaction` require metrics and perform exact readback
  before completion/selection.
- Normal `AnalysisKernel::run` invokes only `SemanticStore::maintain` after
  provider sealing. Reserve/publish/read/match remain private/test-used and
  must stay unwired.
</interfaces>
</context>

<execution_guard>
Use accepted R4 product/test head
`4b925a08878d6113016b17a57b780542e1097a77` as the cumulative R5-Go diff
baseline. Planning/review/summary artifacts are not product/test files; every
budget command scopes to `crates/polint`.

Before Task 1 and after every task, record:

1. `git diff --name-only 4b925a08878d6113016b17a57b780542e1097a77..HEAD -- crates/polint`
2. `git diff --numstat 4b925a08878d6113016b17a57b780542e1097a77..HEAD -- crates/polint`
3. the sum of handwritten additions and distinct product/test path count;
4. `git diff --check 4b925a08878d6113016b17a57b780542e1097a77..HEAD`;
5. a protected-scope check for `.github/workflows/ci.yml`, public CLI/config,
   SDK/docs/examples/generated-skill files, ROADMAP, REQUIREMENTS, and STATE.

Task allocation is a stop gate:

- Task 1 hard cap: no more than 950 cumulative additions.
- Task 2 hard cap: no more than 1,950 cumulative additions.
- Task 3 final: no more than 2,500 cumulative additions.
- The allowed product/test set is exactly the thirteen frontmatter paths. A
  subset is valid; an undeclared fourteenth path requires re-planning, not an
  opportunistic edit.
- If exact Go output certification requires an output family beyond the six
  now explicitly declared Go-owned families,
  TypeScript changes, source/fact persistence, an external Go command,
  generic `LayerKey`/`DependencyIndex` redesign, normal-kernel store wiring,
  a second schema family/provider, more than three tasks, or more than 2,500
  additions, stop before editing the expanded scope.

Handwritten-addition allocation (hard ceilings, not targets to fill):

| File/group | Maximum additions | Owning task |
|---|---:|---:|
| `go_syntax_projection.rs` | 400 | 1 |
| `provider.rs` + `analysis_kernel/mod.rs` | 160 | 1 |
| `go/adapter.rs` + `incremental/layer_cache.rs` | 230 | 1 |
| `go/tests.rs` + `runner/mod.rs` | 160 | 1 |
| `store/go_syntax_mirror.rs` | 390 | 2 |
| `store/migrations.rs` | 260 | 2 |
| `store/generation.rs` + `store/mod.rs` | 200 | 2 |
| `store/tests.rs` schema/migration/publication coverage | 150 | 2 |
| `store/tests.rs` exact-match/rollback/tamper coverage | 350 | 3 |
| `public_surface_leak.rs` plus in-scope test remediation in earlier groups | 200 | 3 |
| **Total** | **2,500** | **1-3** |

The Task 1 rows sum to 950; Task 2 adds at most 1,000 for a cumulative
1,950; Task 3 adds at most 550 for the final 2,500 cap. Moving unused
allowance between groups is permitted only while the per-task cumulative caps,
thirteen-file set, and total cap all remain satisfied. Task 2 owns and must
finish its first 150 `store/tests.rs` additions before its verification runs;
Task 3 owns only the remaining 350-line exact-match/rollback/tamper extension.
</execution_guard>

<scope_boundary>
In scope:

- exact normalized Go source membership and one versioned in-process
  tree-sitter parser contract;
- the D-01 private compatibility repair that appends `string_literals` to the
  `polint.go.syntax` manifest output inventory already backing public
  `StringLiterals<'_>` facts;
- the private capability-owner repair that selects `polint.go.syntax` for
  present Go files and `polint.ts.syntax` for present TS/JS files, selecting
  both for mixed repositories without requiring either absent language;
- Go-only layer-key narrowing, exact source/parser dependency edges, and
  cache-independent produced syntax identity;
- run-local separation of file/layer cache warnings from semantic payloads;
- one exact static-manifest/sealed-outcome/dependency projection;
- one schema-v5 relational Go syntax metadata family;
- extension of the accepted manifest-plus-metrics immediate transaction;
- private active read/exact-match and isolated tests.

Out of scope:

- `polint.ts.syntax`, `polint.source`, `polint.go.semantic`, or another provider;
- source bodies, syntax facts, ASTs, `CachedFileFacts`, `FactMeta`, diagnostics
  as store rows, validation events, cache statistics, timings, or full
  `InputSnapshot` persistence;
- generic dependency-index/provider-framework redesign, reverse dependency
  tables, layer/query/summary payload storage, or public stable keys;
- TypeScript parser/cache/manifest/output/store changes beyond preserving its
  existing ownership when TS/JS files are present;
- Go binary discovery, module/workspace/package loading, build tags,
  environment/toolchain/process containment, or external Go certification;
- normal `AnalysisKernel::run` publication/read/reuse, public CLI/config/SDK/
  runner signatures, JSON/SARIF/diagnostic/output changes, docs/examples/
  generated skill changes, benchmarks/releases/platform matrices, or CI edits.
</scope_boundary>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Canonicalize Go syntax inputs, produced output, and layer dependencies</name>
  <files>crates/polint/src/analysis_kernel/go_syntax_projection.rs, crates/polint/src/analysis_kernel/mod.rs, crates/polint/src/analysis_kernel/provider.rs, crates/polint/src/analysis_kernel/incremental/layer_cache.rs, crates/polint/src/go/adapter.rs, crates/polint/src/go/tests.rs, crates/polint/src/runner/mod.rs</files>
  <read_first>
    - AGENTS.md
    - .agents/skills/rust-best-practices/SKILL.md
    - .claude/skills/polint/SKILL.md
    - .planning/phases/65-generation-manifest-and-metadata-mirroring/65-CONTEXT.md
    - research/local-semantic-store/IDENTITY-READINESS.md
    - research/local-semantic-store/REVIEW-FINDINGS-TRIAGE.md
    - crates/polint/src/analysis_kernel/go_syntax_projection.rs
    - crates/polint/src/analysis_kernel/metrics_projection.rs
    - crates/polint/src/analysis_kernel/mod.rs
    - crates/polint/src/analysis_kernel/provider.rs
    - crates/polint/src/analysis_kernel/outcome.rs
    - crates/polint/src/analysis_kernel/incremental/keys.rs
    - crates/polint/src/analysis_kernel/incremental/dependency_index.rs
    - crates/polint/src/analysis_kernel/incremental/layer_cache.rs
    - crates/polint/src/go/adapter.rs
    - crates/polint/src/go/tests.rs
    - crates/polint/src/core/mod.rs
    - crates/polint/src/metrics.rs
    - crates/polint/src/runner/mod.rs
  </read_first>
  <behavior>
    - "`CanonicalGoSyntaxInputs` contains every and only Go source as one path-sorted row of canonical repository-relative path, closed Go language, and `DigestKind::SourceText` digest recomputed from exact bytes; duplicate/noncanonical/absolute/unsupported paths, wrong language, digest mismatch, excessive rows, and count mismatch return `GoSyntaxProjectionError`."
    - "`GoSyntaxParserContract` is closed and versioned: `polint.go.syntax` ID/version, `go-facts-v2`, `go-syntax-layer-v1`, an explicit tree-sitter backend label, and an explicit tree-sitter-go grammar label. It contains no Rust `Debug`, cache/build/absolute path, ambient environment, or external Go identity."
    - "The private `polint.go.syntax` `ProviderManifest` declares outputs in the exact order `packages, functions, imports, go_tests, branch_obligations, string_literals`. This compatibility repair records an already-emitted/public fact family and changes no SDK/CLI/config/output contract."
    - "Private `capability_providers(\"string_literals\", db)` returns `polint.go.syntax` iff Go files exist and `polint.ts.syntax` iff TS/JS files exist, in stable provider order. Go-only and TS-only repositories never depend on the absent-language provider; mixed repositories depend on both, and runtime failure gating blocks a requesting rule when any present owner fails."
    - "`CanonicalGoSyntaxOutput` validates and hashes the complete declared Go value: package, function (including calls/test/export/complexity), import, Go-test, branch-obligation, string-literal (path/value/span/Go language), and deterministic `parser/go` diagnostic rows. It replaces transient file/function/branch IDs with path-owned semantic relationships, retains meaningful duplicate rows, checks all spans/references/counts, and sorts full tuples."
    - "`go_syntax_layer_key` contains source-row digests and the parser-contract digest; generic config, lifecycle, and toolchain slots are explicit purpose-typed absence. Changing Go bytes/path/membership/parser/provider/schema changes the key; config/rules/plan/TypeScript/cache mode does not."
    - "`go_syntax_layer_dependency_edges` returns the exact sorted vector of one `SourceText/Content` edge per canonical source row plus one `ProviderSchema/ProviderVersion` parser-contract edge. A read accepts only field-for-field vector equality; missing, extra, replaced, duplicated, unrelated, or reordered rows yield `InvalidEvicted` and recomputation."
    - "Cold computation, validated warm reuse, and cache-disabled computation over equal produced facts/diagnostics return the same nonempty provider output digest. Per-file and outer-layer write warnings remain visible only in the run that produced them and never enter `SyntaxLayerPayload`, layer output identity, or a later warm hit."
  </behavior>
  <action>
Implement D-11 through D-23 as one Go-owned identity contract:

1. Create `analysis_kernel::go_syntax_projection` with
   `CanonicalGoSyntaxSource`, `CanonicalGoSyntaxInputs`,
   `GoSyntaxParserContract`, `CanonicalGoSyntaxOutput`,
   `GoSyntaxProviderProjection`, and `GoSyntaxProjectionError`. Reuse the
   R4 canonical path and 16-hex digest rules without factoring a generic
   provider framework. Cap source/fact/diagnostic rows before allocation and
   use checked integer conversions.
2. Build source rows only from `Language::Go` files. Recompute each
   `SourceFile::content_hash` from `source.as_ref()`, normalize and require the
   stored relative path to equal its canonical spelling, sort by path, reject
   duplicates, and derive one purpose-separated row digest. Source text is
   borrowed for digest/span validation and never retained in the projection.
3. Define parser-contract constants with stable labels sourced from the pinned
   tree-sitter and tree-sitter-go dependency versions. Include provider ID and
   `ProviderManifest::provider_version()`, fact schema `go-facts-v2`, payload
   schema `go-syntax-layer-v1`, backend, and grammar in one
   `ProviderParameters` digest. Mutation tests independently change every
   member. Do not use `env::vars`, executable probes, `Debug`, or an external
   process.
4. Repair the private static `polint.go.syntax` manifest in `provider.rs` by
   appending `string_literals` after `branch_obligations`. Add a named manifest
   regression asserting the exact six-output order, Go scope, cache policy,
   schema, and precision. Do not add/re-export any SDK name:
   `StringLiterals<'_>` already exists and this change only makes its owning
   provider inventory truthful. In `analysis_kernel/mod.rs`, remove
   `string_literals` from the TS-only capability arm, give it the ordered
   candidate owners `[polint.go.syntax, polint.ts.syntax]`, and extend the
   private per-owner file-language presence filtering currently used for
   multi-language events: Go owns it only when Go files exist, TS owns it only when a
   TS-family file exists, and both own it in a mixed repository. Do not change
   the TS-only ownership of `ts_components`, `ts_classes`, or
   `jsx_attributes`.
5. Canonicalize each of the six declared output families from typed facts.
   For string literals, bind canonical source path, exact literal value, full
   span, and `Language::Go`; validate source ownership and byte/line bounds;
   preserve duplicate literal occurrences; sort the full tuple; include every
   row in the produced-output digest. Validate that
   every fact/span belongs to the named Go source; local IDs are unique where
   referenced; branch/test function references resolve to the exact canonical
   function occurrence; bounds fit the source; languages are Go; declared
   counts and multiplicity are exact. Canonicalize only `parser/go`
   diagnostics with their severity/file/range/message/fingerprint and reject
   operational `internal/cache` diagnostics from the semantic constructor.
   The audit has already established that omitting `string_literals` is
   forbidden. If it proves a seventh Go policy-visible family belongs to this
   manifest, stop under D-15 instead of hashing another incomplete value.
6. In `go::adapter`, derive inputs before cache access. Keep
   `LayerKey::syntax_layer_key` and the TypeScript call path unchanged; pass
   canonical Go source digests, the parser digest, and purpose-specific absent
   config/lifecycle/toolchain digests from the existing Go-only
   `go_syntax_layer_key` wrapper. Emit exact edges through
   `go_syntax_layer_dependency_edges` on every layer write.
7. Make `validate_syntax_layer_payload` reconstruct the canonical produced
   output from the typed payload and exact input sources, compare its digest
   with `manifest.output_digest`, and compare the decoded dependency vector
   exactly with the expected vector. In `layer_cache.rs`, retain canonical
   write sorting but reject noncanonical decoded dependency order/duplicates
   before any repair; include `LayerKind::GoSyntax` among dependency-required
   manifests. Do not change `DependencyIndex` or invalidation planning.
8. Split `GoFileAnalysis` semantic parser diagnostics from current-run cache
   warnings. Store only semantic diagnostics in `SyntaxLayerFile`; return
   operational warnings beside the payload and append them to the current
   result after canonical output identity is sealed. Compute and return the
   canonical output digest on cold, warm, cache-disabled, and write-failure
   paths.
9. Add compact table-driven projection/key/edge/payload tests in the declared
   files. Extend the production cold/warm/disabled fixture with a tiny Go file
   and assert exact `polint.go.syntax` outcome identity plus policy/diagnostic/
   ordering/exit equality while telemetry differs. Add string-literal
   relationship/span/path/value/language/multiplicity/digest mutations: each
   consumed change alters the canonical output identity, while insertion order
   and transient IDs do not. Retain corrupt-payload recomputation and
   current-run write-warning assertions. Add a named, table-driven runtime
   capability regression in `analysis_kernel/mod.rs` covering Go-only,
   TS/JS-only, and mixed file sets. For each set, assert the exact ordered
   `string_literals` owner vector and inject success/failure outcomes so a
   requesting rule is blocked by every failed present-language owner and by
   no absent-language owner.

Do not change the generic syntax constructor's TypeScript semantics, TS fact
production, or the per-file `CacheKey` bridge. The private present-language
`string_literals` owner filter above is the only permitted cross-language
routing repair. Any broader change triggers D-23 stop-and-split rather than a
generic cache migration.
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis_kernel::provider::tests::go_syntax_manifest_declares_all_owned_outputs --locked &amp;&amp; cargo test -p polint --lib analysis_kernel::tests::string_literals_capability_ownership_and_failure_gating_tracks_present_languages --locked &amp;&amp; cargo test -p polint --lib analysis_kernel::go_syntax_projection::tests --locked &amp;&amp; cargo test -p polint --lib go::tests::go_syntax_layer --locked &amp;&amp; cargo test -p polint --lib analysis_kernel::incremental::layer_cache::tests::go_syntax --locked &amp;&amp; cargo test -p polint --lib runner::tests::cold_warm_production_semantic_projection_matches --locked</automated>
  </verify>
  <acceptance_criteria>
    - D-11/D-12 pass: exact Go membership is path-canonical, unique, complete, sorted, byte-authenticated, row-bounded, and represented without source bodies or absolute paths.
    - D-13/D-14/D-15/D-16 pass: every parser-contract member is explicit and mutation-tested; excluded broad state cannot change the Go key; TypeScript/generic key behavior has no product diff; any newly discovered semantic input causes a stop, not a false reusable identity.
    - D-01/D-17/D-18/D-21 pass: the exact private manifest order includes `string_literals`, and all six declared fact families plus parser diagnostics are relationship-validated before output hashing; the exact owner vector is Go for Go-only, TS for TS/JS-only, and Go then TS for mixed repositories, with runtime failure gating on every and only present owner; string-literal path/value/span/language/multiplicity mutations have the required digest polarity; no transient ID, JSON byte identity, layer key, cache path, or fact metadata summary becomes provider identity.
    - D-19/D-20 pass: cold/warm/cache-disabled output identities and semantic projections are equal; current-run cache warnings remain user-visible but are absent from cached payload and identity.
    - D-22/D-23 pass: exact expected edges are written, and missing/extra/replaced/duplicated/reordered/unrelated dependency mutations recompute rather than hit; no generic dependency-index or per-file cache migration occurs.
    - Cumulative scope is a subset of the seven Task 1 paths and no more than 950 additions; otherwise execution stops before Task 2.
  </acceptance_criteria>
  <done>Go syntax has one exact source/parser input projection, one canonical produced-value identity, and one tamper-evident layer dependency vector.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add schema-v5 Go syntax mirror and atomic three-projection publication</name>
  <files>crates/polint/src/analysis_kernel/go_syntax_projection.rs, crates/polint/src/analysis_kernel/store/go_syntax_mirror.rs, crates/polint/src/analysis_kernel/store/mod.rs, crates/polint/src/analysis_kernel/store/generation.rs, crates/polint/src/analysis_kernel/store/migrations.rs, crates/polint/src/analysis_kernel/store/tests.rs</files>
  <read_first>
    - AGENTS.md
    - .agents/skills/rust-best-practices/SKILL.md
    - .planning/phases/65-generation-manifest-and-metadata-mirroring/65-CONTEXT.md
    - .planning/phases/65-generation-manifest-and-metadata-mirroring/65-05-SUMMARY.md
    - .planning/phases/65-generation-manifest-and-metadata-mirroring/65-05-REVIEW.md
    - research/local-semantic-store/implementation/STORE-CONTRACT.md
    - research/local-semantic-store/decisions/DECISIONS.md
    - crates/polint/src/analysis_kernel/go_syntax_projection.rs
    - crates/polint/src/analysis_kernel/metrics_projection.rs
    - crates/polint/src/analysis_kernel/provider.rs
    - crates/polint/src/analysis_kernel/outcome.rs
    - crates/polint/src/analysis_kernel/incremental/run_manifest.rs
    - crates/polint/src/analysis_kernel/store/provider_mirror.rs
    - crates/polint/src/analysis_kernel/store/go_syntax_mirror.rs
    - crates/polint/src/analysis_kernel/store/mod.rs
    - crates/polint/src/analysis_kernel/store/generation.rs
    - crates/polint/src/analysis_kernel/store/migrations.rs
    - crates/polint/src/analysis_kernel/store/connection.rs
    - crates/polint/src/analysis_kernel/store/tests.rs
  </read_first>
  <behavior>
    - "A fresh store has exact schema v5 containing the accepted lifecycle/run-manifest/metrics families plus one five-table `polint.go.syntax` family: `go_syntax_provider_mirror`, `go_syntax_provider_members`, `go_syntax_provider_blockers`, `go_syntax_provider_sources`, and `go_syntax_provider_parser`."
    - "Fresh/v0-v3 and exact empty v4 stores migrate transactionally to v5. A populated v4 store retains version, journal mode, catalog, generations, active pointer, run manifests, and metrics rows byte-for-byte and returns the existing rebuild-needed/refusal status."
    - "Every v5 publication input requires one retained metrics projection and one Go syntax projection. The Go header/member rows encode every static manifest field; only success has one output identity, exact source rows, and exactly one parser row; legal non-success has none; only dependency-blocked has the sorted unique blocker `polint.source`."
    - "The existing immediate transaction writes run manifest, metrics mirror, and Go syntax mirror; boundedly reads and reconstructs all three; recomputes counts, dense ordinals, and witnesses; compares exact typed values; then completes and selects. Every injected Go write/readback failure leaves the candidate pending with zero candidate children and preserves the previous complete selection after reopen."
    - "`active_generation`, manifest/metrics reads, and Go reads authenticate lifecycle, workspace, manifest, metrics, and Go syntax from one read transaction. `GoSyntaxMatch` returns `NoActive`, `Exact(handle)`, or `SemanticMiss`; malformed/future/busy/disabled state remains a typed error/refusal."
  </behavior>
  <action>
Implement D-24 through D-36 by extending the accepted R4 transaction:

1. Add store-private `go_syntax_mirror.rs`. Encode one versioned relational
   family with the five exact tables named above. The header owns provider/
   outcome/identity/count/witness scalars; members encode the single input,
   six outputs, and schema name/version in dense canonical order (eight total
   member rows: one input, six outputs, one schema); blockers
   encode only legal dependency-blocked history; sources encode path/language/
   source digest in canonical order; the parser table encodes exactly one
   closed parser contract on success. Use no JSON/blob/source body/fact row or
   raw SQL outside `analysis_kernel::store`.
2. Persist and field-compare the current static manifest: provider ID/version,
   closed provider kind, ordered input/output members, Go scope,
   existing-file-fact-cache policy/schema, schema name/version, and precision
   ceiling. Exhaustive closed encoders/decoders reject unknown labels. Rebuild
   `ProviderOutcome` through `from_closed_parts` and rebuild
   `GoSyntaxProviderProjection` through its validated durable constructor.
3. Enforce one outcome row per generation. `Succeeded` requires one
   authenticated `ProviderOutputIdentity`, exact canonical source rows, and
   exactly one parser row. Every non-success requires zero identity/source/
   parser rows. `DependencyBlocked` alone requires exactly one blocker equal
   to `hard_dependencies("polint.go.syntax")`; all other blocker shapes fail.
4. Preflight exact cardinality, `typeof`, scalar byte length, row count, dense
   ordinals, aggregate bytes including row overhead, checked integers,
   canonical paths, language, digest kinds/values, relationships, and catalog
   SQL before decoding or allocation. Recompute a row-derived mirror witness
   and compare exact reconstructed values; never treat a stored digest/count
   as sufficient evidence.
5. Raise `CURRENT_SCHEMA_VERSION` to 5 and add exactly one migration. Extend
   owned table/index/trigger/FK/content authentication for all new objects.
   Before any v4 mutation, authenticate exact v4 and prove generations,
   selection, manifests, metrics headers/members/blockers/sources/functions,
   and every other v4-owned row are empty; repeat inside the immediate
   migration transaction. Populated v4 is preserved and refused without a
   synthesized Go outcome.
6. Extend `PublicationInputs::new` to require
   `RunManifestInputs + MetricsProviderProjection + GoSyntaxProviderProjection`.
   No v5 facade may complete a generation without either provider. Add
   `go_syntax_mirror` writes after the retained metrics writes and read all
   three projections back before completion/selection.
7. Extend `PublicationFailurePoint` with before/after Go header, members,
   blockers, sources, parser, and Go readback seams. At each seam assert after
   close/reopen: old active projection unchanged, candidate pending, and zero
   candidate manifest/metrics/Go child rows.
8. Centralize selected projection loading inside one read transaction that
   validates the complete run manifest, retained metrics projection, and Go
   projection before returning any active handle/provider value. Add private
   `active_go_syntax` and `match_active_go_syntax`. Authenticate workspace
   ownership from the supplied run manifest, but compare only canonical Go
   inputs/parser/output for provider exactness so broad config changes do not
   create a false Go miss.
9. Before Task 2 verification, use Task 2's explicit 150-addition
   `store/tests.rs` allocation to add the focused schema-v5 catalog,
   exact-empty-v4 migration/preservation, required three-projection
   publication/readback, and Go write/readback failure-seam tests exercised by
   `go_syntax_provider_mirror_storage` and `generation_lifecycle`. These are
   Task 2 deliverables and may not be deferred to Task 3; share compact fixture
   builders so Task 3 can add its separately budgeted mutation/tamper cases.

Do not refactor `provider_mirror.rs` into a generic provider framework. Sharing
a small closed label/bound helper is allowed only inside the declared files
when it reduces code and leaves schema-v4 metrics behavior byte-for-byte exact.
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis_kernel::store::migrations::tests --locked &amp;&amp; cargo test -p polint --lib analysis_kernel::store::tests::go_syntax_provider_mirror_storage --locked &amp;&amp; cargo test -p polint --lib analysis_kernel::store::tests::generation_lifecycle --locked</automated>
  </verify>
  <acceptance_criteria>
    - D-24/D-25/D-26 pass: schema v5 adds one and only one private five-table Go syntax family; its dense static members contain one input, the exact six-output order including `string_literals`, and one schema; every published generation contains exactly one retained metrics projection and one exact Go static manifest/outcome.
    - D-27/D-28/D-29 pass: success-only identity/source/parser rows, dependency-blocked-only exact `polint.source` blocker, single forward provider-owned dependency projection, and zero fact/source-body/generic-index persistence are enforced by writer, reader, and schema validation.
    - D-30/D-31 pass: all three projections are written/read/recomputed/compared before completion; every Go failure seam preserves the old selected truth and leaves only the durable pending reservation.
    - D-32/D-33 pass: only exact empty v4 migrates; populated v4 and malformed/future/colliding current stores retain exact pre-open version/catalog/data/journal state and are refused; empty older schemas and exact v5 are transactional/idempotent.
    - D-34/D-35 pass: one-snapshot reads authenticate lifecycle/workspace/manifest/metrics/Go fields; provider exactness changes only for canonical Go source/parser/output differences, not broad run config.
    - D-36 passes by source audit: normal `AnalysisKernel::run` still calls only `SemanticStore::maintain` and consumes no durable provider value.
    - Task 2's schema/migration/publication assertions are present in `store/tests.rs` and both named store targets pass before Task 3 begins; their additions fit the dedicated 150-line Task 2 allocation rather than consuming Task 3's tamper-test allowance.
    - Cumulative scope is a subset of the Task 1-2 declared paths and no more than 1,950 additions; otherwise execution stops before Task 3.
  </acceptance_criteria>
  <done>One complete generation can atomically own the accepted metrics mirror plus one exact Go syntax metadata projection and reconstruct both before selection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove exact Go matching, invalidation polarity, rollback, tamper refusal, and privacy</name>
  <files>crates/polint/src/analysis_kernel/go_syntax_projection.rs, crates/polint/src/analysis_kernel/mod.rs, crates/polint/src/analysis_kernel/provider.rs, crates/polint/src/analysis_kernel/incremental/layer_cache.rs, crates/polint/src/go/adapter.rs, crates/polint/src/go/tests.rs, crates/polint/src/analysis_kernel/store/go_syntax_mirror.rs, crates/polint/src/analysis_kernel/store/mod.rs, crates/polint/src/analysis_kernel/store/generation.rs, crates/polint/src/analysis_kernel/store/migrations.rs, crates/polint/src/analysis_kernel/store/tests.rs, crates/polint/src/runner/mod.rs, crates/polint/tests/public_surface_leak.rs</files>
  <read_first>
    - AGENTS.md
    - .agents/skills/rust-best-practices/SKILL.md
    - .claude/skills/polint/SKILL.md
    - .planning/phases/65-generation-manifest-and-metadata-mirroring/65-CONTEXT.md
    - .planning/phases/65-generation-manifest-and-metadata-mirroring/65-05-SUMMARY.md
    - .planning/phases/65-generation-manifest-and-metadata-mirroring/65-05-REVIEW.md
    - .planning/phases/65-generation-manifest-and-metadata-mirroring/65-05-VERIFICATION.md
    - .planning/forensics/report-20260719-phase-65-scope-collapse.md
    - research/local-semantic-store/RESTART-PLAN.md
    - research/local-semantic-store/REVIEW-FINDINGS-TRIAGE.md
    - docs/API-VISIBILITY-PLAN.md
    - crates/polint/src/analysis_kernel/go_syntax_projection.rs
    - crates/polint/src/analysis_kernel/metrics_projection.rs
    - crates/polint/src/analysis_kernel/mod.rs
    - crates/polint/src/analysis_kernel/provider.rs
    - crates/polint/src/analysis_kernel/incremental/layer_cache.rs
    - crates/polint/src/go/adapter.rs
    - crates/polint/src/go/tests.rs
    - crates/polint/src/analysis_kernel/store/provider_mirror.rs
    - crates/polint/src/analysis_kernel/store/go_syntax_mirror.rs
    - crates/polint/src/analysis_kernel/store/mod.rs
    - crates/polint/src/analysis_kernel/store/generation.rs
    - crates/polint/src/analysis_kernel/store/migrations.rs
    - crates/polint/src/analysis_kernel/store/tests.rs
    - crates/polint/src/runner/mod.rs
    - crates/polint/tests/public_surface_leak.rs
  </read_first>
  <behavior>
    - "A real tiny repository with two Go files and one unrelated TypeScript file supplies real sealed `polint.metrics` and `polint.go.syntax` projections; reserve, atomic publish, connection close, reopen, authenticated active read, and `GoSyntaxMatch::Exact` preserve every static manifest/outcome/identity/source/parser field."
    - "At least one legal non-success Go outcome round-trips with exact history, no identity/source/parser rows, and `SemanticMiss`. Pending, unselected, missing-provider, duplicate-provider, and cross-generation rows are refused rather than interpreted as absence."
    - "Go content, membership, path, language, provider version, `go-facts-v2`, payload schema, backend/grammar contract, produced fact/diagnostic value, and exact layer/store dependency rows are must-invalidate cases. Config/rule/plan-only, TypeScript-only, cache mode/telemetry/write-warning changes are must-preserve cases when canonical Go output is equal."
    - "Every Go static-manifest/outcome/blocker/identity/source/parser field, count, ordinal, storage class, relationship, owned table/index/trigger/FK/catalog declaration, scalar length, row count, and aggregate bound is tampered independently and can never return trusted active data or Exact."
    - "Public SDK/runner/CLI/config/JSON/SARIF/diagnostics/order/exit/docs/examples/generated-skill behavior stays unchanged; all new vocabulary is `pub(crate)` or narrower; normal enabled/disabled/busy/future/corrupt store parity remains byte-identical."
  </behavior>
  <action>
Close D-37 through D-44 and the complete D-01 through D-44 exit:

1. Build one isolated temp repository containing two tiny `.go` files and one
   unrelated `.ts` file. Run the real kernel in process (tree-sitter only),
   select the real sealed metrics and Go outcomes, construct both canonical
   projections from the real `AnalysisDb`/parser diagnostics, reserve and
   publish, close all connections, reopen, and assert exact typed equality for
   lifecycle, run manifest, metrics, Go static manifest, outcome, identity,
   blockers, every source row, and parser row. Assert `GoSyntaxMatch::Exact`.
2. Round-trip `PlannedAbsent` and, if the existing compact helper supports it,
   `DependencyBlocked` with exactly `polint.source`. Assert no identity/source/
   parser rows and `SemanticMiss`. Delete/duplicate/cross-own the required Go
   header and prove current-schema reopen refuses without mutation.
3. Implement a named table-driven invalidation matrix. Independently mutate
   Go content, source membership, canonical path, language, provider version,
   fact schema, payload schema, parser backend, parser grammar, every declared
   output family—including string-literal path/value/span/language/
   multiplicity—parser diagnostic, and dependency row field/count/order.
   Every consumed mutation must change the key/output or return
   `SemanticMiss`. Independently change config, rule digest, plan digest,
   TypeScript bytes/path/membership, cache enabled state, cache counters, and
   injected write warnings; when canonical Go inputs/output are equal,
   `GoSyntaxMatch::Exact` and the sealed identity must remain equal.
4. Tamper layer manifests through isolated cache files: delete, add, replace,
   duplicate, and reorder source/parser edges while retaining key/payload/
   output digests. Each read must return `InvalidEvicted`, recompute, and write
   the exact canonical vector; none may record a verified hit.
5. Extend the publication rollback matrix for every new before/after Go write
   and readback seam. Snapshot lifecycle, selection, run-manifest rows,
   retained metrics rows, Go rows, schema version, and owned catalog; after
   close/reopen assert exact prior selected data plus the intentional pending
   reservation and no candidate child rows.
6. Add a compact table-driven SQLite tamper suite covering every manifest
   member, all six outcome legal-shape violations, blocker value/order/count,
   success identity fields, source/path/language/digest/order/count, parser
   contract fields/count, witness, cross-generation ownership, missing/extra
   rows, wrong storage classes, oversized scalar/row/aggregate values,
   unexpected owned index/trigger/table declaration, and FK drift. Bypass
   checks only in the local fixture used to create malformed bytes; close it
   before normal facade reopen and assert preservation plus typed refusal.
7. Re-run the cold/warm/cache-disabled production projection with Go present.
   Assert identical sealed Go identity, parser diagnostics, restored facts,
   rule answers, ordering, and exit; only telemetry differs. Retain corrupt
   cache eviction and current-run write-warning proof, including absence of
   that warning on a later valid warm hit.
8. Extend `public_surface_leak` forbidden markers for projection, parser
   contract, mirror/match/schema/table/dependency vocabulary. Audit all new
   visibility as `pub(crate)` or narrower and raw SQL/rusqlite as store-owned.
   Inspect `AnalysisKernel::run` and prove `maintain` remains its sole
   `SemanticStore` operation.
9. Run every focused command separately and record wall time below sixty
   seconds, then format, strict workspace/all-target/all-feature Clippy,
   workspace check, exact allowed-file/addition/protected-scope/forbidden-
   persistence/normal-wiring audits, fresh R5-Go-only code review, and
   R5-Go-only verification. Any unresolved ASVS-L1 HIGH, budget breach, or
   need for TypeScript/facts/normal reuse/CI changes blocks acceptance and
   triggers a split.

Tests must use independent `tempfile` repositories/stores and in-process
tree-sitter parsing only. Do not use the `go` executable, network, sleeps,
`std::env::set_var/remove_var`, process-global locks, `--test-threads=1`, or
global serialization. Task 3 may add at most 350 lines to `store/tests.rs`,
only for the exact-match/rollback/tamper extensions above; it builds on the
already-passing Task 2 schema/migration/publication coverage and does not
retroactively claim or defer that work.
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis_kernel::store::tests::go_syntax_provider_mirror --locked &amp;&amp; cargo test -p polint --lib analysis_kernel::store::tests::generation_lifecycle --locked &amp;&amp; cargo test -p polint --lib go::tests::go_syntax_layer --locked &amp;&amp; cargo test -p polint --lib runner::tests::cold_warm_production_semantic_projection_matches --locked &amp;&amp; cargo test -p polint --lib analysis_kernel::tests::semantic_store_check_parity --locked &amp;&amp; cargo test -p polint --test public_surface_leak semantic_store_markers_do_not_leak_into_supported_public_surfaces --locked</automated>
  </verify>
  <acceptance_criteria>
    - D-37 passes: real sealed metrics plus Go success survives reserve/publish/close/reopen/active-read/exact-match; legal Go non-success survives without reusable rows and cannot match.
    - D-18/D-25/D-38 pass with a named must-invalidate/must-preserve matrix covering the exact private six-output inventory, including string-literal path/value/span/language/multiplicity/digest, all source/parser inputs, and every exclusion; full run-manifest mismatch is not substituted for provider evidence.
    - D-39/D-40 pass: static manifest/outcome/blocker/identity/dependency/storage-class/catalog/FK/bounds tampering fails before trusted allocation or Exact; counts/ordinals/relationships are recomputed rather than trusted.
    - D-41 passes: disabled store performs zero I/O; busy/future/corrupt/refusal, complete selection, public bytes/order/diagnostics/exit, SDK/runner/CLI/config/docs/examples/generated skill, and private visibility remain unchanged.
    - D-42/D-43 pass: every focused test is isolated/network-free/process-free and below sixty seconds; fmt, strict Clippy, workspace check, diff, file, line, protected-scope, forbidden-persistence, and wiring audits pass; every scope/budget breach stops execution.
    - D-44 and D-01 through D-10 pass: exactly `polint.go.syntax` joins retained `polint.metrics`; no TypeScript/source/semantic/other provider, facts/source body, normal publication/reuse, public change, CI edit, requirement completion, or Phase 65 completion appears.
    - Final implementation uses exactly three tasks, a subset of the thirteen declared product/test paths, at most 2,500 handwritten additions under the stated per-group allocation, including the explicit 150/350 Task 2/Task 3 split for `store/tests.rs`, one new durable family, and one newly mirrored provider.
  </acceptance_criteria>
  <done>`polint.go.syntax` metadata can be written beside metrics, reopened, exactly authenticated/matched, invalidated by consumed changes, preserved across excluded changes, and refused after tampering without normal-run reuse.</done>
</task>

</tasks>

<threat_model>
## Security Baseline

Default enforcement is OWASP ASVS Level 1 for this private rebuildable local
cache boundary. SQLite catalog declarations/cells, layer manifests/payloads,
repository-relative paths/source metadata, and supplied projections are
untrusted until bounded typed validation succeeds. Any unresolved HIGH blocks
this increment; fix/re-review within scope or stop and split.

## Assets and Trust Boundaries

| Boundary | Asset at risk |
|---|---|
| Go source bytes/path + parser contract -> canonical inputs | Exact invalidation and absence of path/source leakage |
| Six-family Go facts, including StringLiteralFact -> canonical output | Complete provider identity for the already-public StringLiterals view |
| Present Go/TS-family files + StringLiterals request -> private owner set | Complete runtime failure gating without absent-language false blockers |
| Cached typed payload + dependency vector -> produced identity | Cold/warm/disabled equality and rejection of stale/tampered reuse |
| Static manifest + sealed outcome -> durable Go projection | Success-only reusable truth and exact blocker semantics |
| SQLite catalog/cells -> typed active projection | Allocation bounds, deterministic decode, and refusal of forged state |
| Pending candidate -> selected complete generation | Atomicity and preservation of the previous active truth |
| Empty/populated schema v4 -> schema v5 | Historical metrics truth and non-destructive migration |
| Private store/parser vocabulary -> public product | API, CLI, diagnostic, output, path, and source privacy |
| Bounded Go increment -> repository-wide provider work | Reviewability and prevention of scope collapse |

## HIGH Blocking Threats

| Threat ID | ASVS area | Threat | Required mitigation and proof |
|---|---|---|---|
| T-65-06-01 | V5 Validation | Omitted Go bytes/path/parser member or an unrelated config/tool value produces a false hit/miss | One canonical source/parser projection drives key, layer edges, store dependencies, and paired mutation tests for every included/excluded field. |
| T-65-06-02 | V5 Validation | Transient IDs, cache state, JSON bytes, write warnings, or omitted `StringLiteralFact` rows alter/incompletely define provider output identity | Exact private six-output manifest; complete typed fact/diagnostic relationship validation including literal path/value/span/language/multiplicity; produced-value digest; cold/warm/disabled/write-warning equality. |
| T-65-06-03 | V5 Validation | Missing/extra/replaced/duplicate/reordered layer edges are repaired into a trusted hit | Validate original decoded canonical dependency order/cardinality and exact expected vector before reuse; every tamper recomputes. |
| T-65-06-04 | V5 Validation / V1 Architecture | Forged SQLite manifest/outcome/identity/blocker/source/parser rows yield reusable success | Closed codecs, exact static manifest comparison, success-only rows, actual-hard-blocker validation, dense ordinals, recomputed witness, full table-driven tamper suite. |
| T-65-06-05 | V5 Validation / V7 Error Handling | Attacker-controlled catalog SQL/cell types/counts/lengths cause large allocation, panic, or partial trust | Same-snapshot `typeof`/count/byte/aggregate preflight, explicit maxima, checked conversions, bounded streaming decode, oversized hostile fixtures. |
| T-65-06-06 | V1 Architecture | Partial Go publication becomes active or destroys the old active generation | One immediate three-projection transaction, exact readback before completion, failure injection around every Go seam, close/reopen old-active proof. |
| T-65-06-07 | V1 Architecture | Populated v4 history is erased or assigned invented Go syntax history | Exact-v4/content emptiness preflight before mutation and repeated transactionally; populated-v4 version/catalog/data/journal preservation and refusal. |
| T-65-06-08 | V1 Architecture | Absolute paths, source text, SQL/provider internals, or a normal reuse switch leak publicly | Repo-relative/digest-only persistence, forbidden-persistence scan, private visibility, public leak gate, protected-file audit, maintenance-only kernel source audit. |
| T-65-06-09 | V1 Architecture | Review expands into TypeScript, Go semantic/toolchain, generic dependency framework, facts, CI, or normal reuse | Hard one-provider/one-family/task/file/line gates; immediate stop-and-split with later R5/R6 handoff. |
| T-65-06-10 | V1 Architecture / V5 Validation | `string_literals` remains TS-only, requires both providers in a single-language repository, or ignores one failed owner in a mixed repository | File-language-filtered private owner vector: Go-only -> Go, TS/JS-only -> TS, mixed -> Go then TS; table-driven runtime blocker tests inject each present/absent provider failure and assert exact rule-gating polarity. |

## Additional STRIDE Controls

| Category | Concern | Control |
|---|---|---|
| Spoofing | Unknown blocker/provider/parser labels impersonate current contracts | Compare every label with static manifest, hard dependency inventory, and closed parser constants. |
| Tampering | Independent key/layer/store dependency copies diverge | Derive all three from `CanonicalGoSyntaxInputs` and `GoSyntaxParserContract`; store one forward projection. |
| Repudiation | Digest-only checks hide missing/duplicate rows | Compare exact decoded fields/order/multiplicity/relationships and use digests only as corroborating witnesses. |
| Information disclosure | Source bodies, absolute paths, cache roots, usernames, or environment enter storage/output | Persist canonical relative paths plus digests only; scan schema/codecs/public output for forbidden vocabulary and fixture secrets. |
| Denial of service | Huge local DB/cache metadata or fixtures exhaust memory/time | Pre-allocation bounds, streamed rows, tiny temp fixtures, no sleeps/network/global serialization, each target below sixty seconds. |
| Elevation of privilege | Another provider or public API receives trust without readiness | One newly mirrored provider and private-only facade; present-language routing grants no durable trust to TS syntax; scope audit rejects every extra provider/surface. |

Any reproducible route to a forged `Succeeded`, Exact after a consumed mutation,
hit after dependency tampering, active partial candidate, mutated populated-v4
history, unbounded allocation, source/path leakage, normal-kernel reuse, or
scope expansion is HIGH and blocks completion.
</threat_model>

<failure_stop_gates>
- Stop before implementation if the canonical Go produced value cannot cover
  all six static-manifest outputs—including the code-audited, non-optional
  `string_literals` family—and parser diagnostics without persisting
  facts/source or editing an undeclared file.
- Stop before schema work if Task 1 exceeds 950 additions, changes TypeScript
  parsing/cache/manifest/output/store behavior beyond the private
  present-language `string_literals` owner repair, falsely gates either
  single-language repository on the absent provider, needs a generic
  key/dependency migration, or cannot prove cold/warm/cache-disabled output
  identity equality.
- Stop before Task 3 if cumulative scope exceeds 1,950 additions, thirteen
  declared paths, one new schema family, or one newly mirrored provider.
- Block acceptance for any unresolved ASVS-L1 HIGH, failed exact-empty-v4
  preservation proof, failed old-active rollback proof, attacker-sized decode
  before preflight, public vocabulary/source leakage, or normal-run store call
  beyond `maintain`.
- Never weaken exactness, remove a mutation/tamper case, serialize tests,
  increase timeouts, edit CI, invent old history, or raise a budget to make
  the slice pass.
</failure_stop_gates>

<verification>
Run each focused command separately and record wall time; every required test
target must finish below sixty seconds after normal debug compilation:

1. `cargo test -p polint --lib analysis_kernel::provider::tests::go_syntax_manifest_declares_all_owned_outputs --locked`
2. `cargo test -p polint --lib analysis_kernel::tests::string_literals_capability_ownership_and_failure_gating_tracks_present_languages --locked`
3. `cargo test -p polint --lib analysis_kernel::go_syntax_projection::tests --locked`
4. `cargo test -p polint --lib go::tests::go_syntax_layer --locked`
5. `cargo test -p polint --lib analysis_kernel::incremental::layer_cache::tests::go_syntax --locked`
6. `cargo test -p polint --lib analysis_kernel::outcome::tests --locked`
7. `cargo test -p polint --lib analysis_kernel::store::migrations::tests --locked`
8. `cargo test -p polint --lib analysis_kernel::store::tests::go_syntax_provider_mirror_storage --locked`
9. `cargo test -p polint --lib analysis_kernel::store::tests::go_syntax_provider_mirror --locked`
10. `cargo test -p polint --lib analysis_kernel::store::tests::generation_lifecycle --locked`
11. `cargo test -p polint --lib runner::tests::cold_warm_production_semantic_projection_matches --locked`
12. `cargo test -p polint --lib analysis_kernel::tests::semantic_store_check_parity --locked`
13. `cargo test -p polint --test public_surface_leak semantic_store_markers_do_not_leak_into_supported_public_surfaces --locked`
14. `cargo fmt --all -- --check`
15. `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings`
16. `cargo check --workspace --all-targets --all-features --locked`
17. `git diff --check 4b925a08878d6113016b17a57b780542e1097a77..HEAD`
18. Allowed-file audit:
    `test -z "$(comm -23 <(git diff --name-only 4b925a08878d6113016b17a57b780542e1097a77..HEAD -- crates/polint | sort) <(printf '%s\n' crates/polint/src/analysis_kernel/go_syntax_projection.rs crates/polint/src/analysis_kernel/incremental/layer_cache.rs crates/polint/src/analysis_kernel/mod.rs crates/polint/src/analysis_kernel/provider.rs crates/polint/src/analysis_kernel/store/generation.rs crates/polint/src/analysis_kernel/store/go_syntax_mirror.rs crates/polint/src/analysis_kernel/store/migrations.rs crates/polint/src/analysis_kernel/store/mod.rs crates/polint/src/analysis_kernel/store/tests.rs crates/polint/src/go/adapter.rs crates/polint/src/go/tests.rs crates/polint/src/runner/mod.rs crates/polint/tests/public_surface_leak.rs | sort))"`
19. Addition/file-cap audit:
    `test "$(git diff --numstat 4b925a08878d6113016b17a57b780542e1097a77..HEAD -- crates/polint | awk '{added += $1} END {print added + 0}')" -le 2500 &amp;&amp; test "$(git diff --name-only 4b925a08878d6113016b17a57b780542e1097a77..HEAD -- crates/polint | wc -l | tr -d ' ')" -le 13`
20. Protected-file audit:
    `test -z "$(git diff --name-only 4b925a08878d6113016b17a57b780542e1097a77..HEAD -- .github/workflows/ci.yml .planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md docs examples crates/polint/src/cli crates/polint/src/sdk .claude/skills .agents/skills)"`
21. Forbidden persistence audit:
    `test -z "$(rg -n 'CachedFileFacts|SyntaxLayerPayload|PackageFact|FunctionFact|ImportFact|TestFact|BranchObligation|StringLiteralFact|FactMeta|InputSnapshot|DependencyIndex|source_body|source_text_body' crates/polint/src/analysis_kernel/store/go_syntax_mirror.rs crates/polint/src/analysis_kernel/store/migrations.rs)"`
22. Scope and normal-wiring audit: prove schema v5 adds only the named Go
    syntax family; only `polint.metrics` and `polint.go.syntax` have durable
    provider mirrors; `polint.ts.syntax`, `polint.source`, and
    `polint.go.semantic` have no new store rows; inspect production
    `AnalysisKernel::run` and prove its sole `SemanticStore` call is
    `maintain`.

The file audit accepts a legitimate subset and rejects every undeclared path.
Do not add a full workspace test, external Go/toolchain fixture, release
benchmark, cross-platform matrix, network test, timeout increase, CI edit, or
sub-five-minute CI redesign to this required path.

After all commands pass, run two fresh bounded gates:

1. A code reviewer inspects the cumulative product/test range from
   `4b925a08878d6113016b17a57b780542e1097a77`, all ASVS HIGH threats,
   canonical source/parser/output identity, raw layer-edge tampering, schema-v5
   migration/preservation, three-projection publication rollback, bounded
   decode, privacy, and budget. Any Critical/Warning or unresolved HIGH reopens
   only an in-scope fix/re-review loop; transitive findings receive explicit
   stop/split or backlog disposition.
2. A verifier evaluates this plan only against D-01 through D-44 and records
   the first R5 increment as Go syntax metadata write/reopen/exact-match/
   tamper refusal. It must not certify the TypeScript half of issue #89,
   another R5 increment, R6, Phase 65, STORE-04, STORE-05, META-01, or META-04.
</verification>

<success_criteria>
- One canonical path-unique Go source projection and one closed parser contract
  drive the Go-only layer key, exact layer edges, durable dependencies, and
  paired mutation tests; broad run/cache/tool state is excluded.
- The private `polint.go.syntax` manifest truthfully declares the exact ordered
  six-family inventory, appending the already-emitted `string_literals` output
  without widening the already-public `StringLiterals<'_>` SDK surface.
- Private runtime ownership for `StringLiterals<'_>` is exactly Go syntax in a
  Go-only repository, TS syntax in a TS/JS-only repository, and both in stable
  order in a mixed repository; injected owner failures prove no absent
  language falsely gates a rule and no present owner failure is ignored.
- One relationship-validated canonical produced value covers all six declared
  Go syntax output families plus parser diagnostics; literal path/value/span/
  language/multiplicity participates; cold, warm, disabled, and
  write-warning paths yield equal sealed identity with telemetry separated.
- Layer reads reject every missing/extra/replaced/duplicated/reordered source
  or parser edge and recompute rather than record verified reuse.
- Schema v5 contains exactly one new private relational Go syntax family beside
  the accepted metrics family; only exact empty v4 migrates, and populated/
  malformed/future state is preserved and refused.
- A real sealed success and legal non-success survive atomic three-projection
  publication/reopen with exact reusable/non-reusable polarity; every failure
  seam preserves prior selected truth.
- SQLite/catalog/cell/path/source/dependency tampering and attacker-controlled
  sizes fail closed before unbounded allocation or Exact; public/source/path/
  SQL/provider vocabulary does not leak.
- Focused tests, format, strict Clippy, workspace check, diff/file/addition/
  protected/forbidden-persistence/wiring audits, clean bounded review, and
  R5-Go-only verification pass.
- Final scope is exactly three tasks, at most thirteen declared product/test
  files and 2,500 handwritten additions, one new durable schema family, and
  one newly mirrored provider. `.github/workflows/ci.yml` is unchanged.
- `requirements: []` remains true. The TypeScript syntax increment, additional
  readiness-gated R5 providers, R6, Phase 65, STORE-04, STORE-05, META-01, and
  META-04 remain open.
</success_criteria>

<output>
After execution, create
`.planning/phases/65-generation-manifest-and-metadata-mirroring/65-06-SUMMARY.md`
with `requirements-completed: []`, exact final file/addition/deletion/schema/
provider/table counts, the private `string_literals` manifest compatibility
repair, present-language capability-ownership/failure-gating proof, and
six-family identity proof, focused test timings, dependency-
mutation/migration/rollback/tamper results, fresh review/verifier disposition,
and an explicit
handoff listing the remaining TypeScript syntax R5 increment, other
readiness-gated R5 increments, and R6 private enablement/measured reuse.

Do not update ROADMAP, REQUIREMENTS, STATE, CONTEXT, DISCUSSION-LOG, Plans
01-05, CI, or any other planning artifact as completion bookkeeping from this
R5-Go-only plan. Apply the shipped-code comment policy: source/test comments,
fixtures, lint reasons, build files, generated text, and product-facing output
must describe enduring behavior and may not mention phase/plan/milestone
chronology.
</output>
