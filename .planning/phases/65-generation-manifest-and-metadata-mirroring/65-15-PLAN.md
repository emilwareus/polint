---
phase: 65-generation-manifest-and-metadata-mirroring
plan: 15
type: execute
wave: 15
depends_on: [65-14]
files_modified:
  - crates/polint/src/analysis_kernel/incremental/dependency_index.rs
  - crates/polint/src/analysis_kernel/incremental/layer_cache.rs
  - crates/polint/src/analysis_kernel/incremental/invalidation.rs
  - crates/polint/src/analysis_kernel/store/mod.rs
  - crates/polint/src/analysis_kernel/store/commit_plan.rs
  - crates/polint/tests/cli.rs
autonomous: true
requirements: [STORE-04, META-01, META-04]
must_haves:
  truths:
    - "D-01/D-02/D-16: one complete ValidatedRunMetadata maps to one fail-closed SQL-free StoreCommitPlan."
    - "D-05-D-13: the plan copies canonical identities, exact query declarations, statuses, payload_digest metadata, and one edge set without deriving parallel identity."
    - "D-19: semantic counts/digests exclude provider cache counters, DemandCacheStatus, durations, and timestamps; telemetry is separate."
    - "The final QueryKey shape and typed canonical edge vector publish once as polint-dependency-index-2; v1 and both temporary labels fail closed without compatibility decoding."
    - "The parent analysis kernel cannot name StoreCommitPlan or its constructor."
  artifacts:
    - path: "crates/polint/src/analysis_kernel/store/commit_plan.rs"
      provides: "Private complete SQL-free normalized plan"
      contains: "pub(super) struct StoreCommitPlan"
    - path: "crates/polint/src/analysis_kernel/incremental/dependency_index.rs"
      provides: "One final typed dependency-index schema after QueryKey stabilization"
      contains: "polint-dependency-index-2"
---

<objective>
Publish the one final dependency-index wire schema after QueryKey stabilization, then normalize one complete validated run into a private SQL-free commit plan and reject incomplete or telemetry-contaminated generations before any database opens.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
@.agents/skills/rust-best-practices/SKILL.md
@.claude/skills/polint/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-CONTEXT.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-PATTERNS.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-14-SUMMARY.md
@research/local-semantic-store/implementation/STORE-CONTRACT.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Publish the final dependency-index schema and retire temporary labels</name>
  <files>crates/polint/src/analysis_kernel/incremental/dependency_index.rs, crates/polint/src/analysis_kernel/incremental/layer_cache.rs, crates/polint/src/analysis_kernel/incremental/invalidation.rs, crates/polint/tests/cli.rs</files>
  <read_first>
    - .agents/skills/rust-best-practices/SKILL.md
    - .claude/skills/polint/SKILL.md
    - output of repository-wide rg 'DEPENDENCY_INDEX_SCHEMA|polint-dependency-index-' across crates/polint/src, crates/polint/tests, and tests
    - crates/polint/src/analysis_kernel/incremental/dependency_index.rs (final QueryKey-bearing canonical vector and schema fixture)
    - crates/polint/src/analysis_kernel/incremental/layer_cache.rs (manifest writer, exact schema check, invalid-read recomputation, fixtures)
    - crates/polint/src/analysis_kernel/incremental/invalidation.rs (schema mismatch guard and conservative action fixtures)
    - crates/polint/src/analysis_kernel/incremental/mod.rs (read-only DEPENDENCY_INDEX_SCHEMA re-export)
    - crates/polint/tests/cli.rs (private schema/vocabulary assertions)
  </read_first>
  <action>
    After Plan 14 has made QueryDependencyInputs mandatory and serde-visible, set DEPENDENCY_INDEX_SCHEMA exactly once to polint-dependency-index-2. This final label names the typed-only CacheNode vocabulary, one canonical sorted edge vector, and the final QueryKey shape including mandatory QueryDependencyInputs. Update every explicit pin/fixture in the same boundary: dependency_index construction/serialization, layer-cache manifest write/read expectations, invalidation schema checks, and CLI private assertions. Audit incremental/mod.rs as a read-only re-export that follows the central constant; there are no other consumers.

    Add exact backward/fail-closed coverage for polint-dependency-index-1, polint-dependency-index-next-typed, polint-dependency-index-next-query-inputs, unknown, and future labels. A stale layer manifest returns the existing invalid-read/recompute path; InvalidationPlan returns its conservative schema-mismatch action; direct dependency-index validation rejects the mismatched shape. Never deserialize a stale label as current, rewrite it in place, translate legacy string nodes, or infer a version from fields. Only polint-dependency-index-2 is accepted after this task, and all current-label tests use the central constant while the explicit stale-label table remains literal.
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis_kernel::incremental::dependency_index::tests --locked &amp;&amp; cargo test -p polint --lib analysis_kernel::incremental::layer_cache::tests --locked &amp;&amp; cargo test -p polint --lib analysis_kernel::incremental::invalidation::tests --locked &amp;&amp; cargo test -p polint --test cli --locked -- --test-threads=1 &amp;&amp; cargo check -p polint --all-features --locked</automated>
  </verify>
  <acceptance_criteria>
    - DEPENDENCY_INDEX_SCHEMA and every layer-cache/CLI/current-schema pin equal polint-dependency-index-2 in one compile-clean boundary.
    - The v2 serialized shape contains typed-only endpoints, one canonical edge vector, and mandatory QueryDependencyInputs.
    - v1, both exact temporary labels, unknown, and future labels fail closed to recompute/conservative invalidation with no compatibility decoder.
    - Repository-wide schema-consumer audit finds only dependency_index construction, layer-cache pinning, invalidation checking, the incremental re-export, and CLI private assertions.
  </acceptance_criteria>
  <done>One final v2 label names one final wire shape, and every older/intermediate shape fails closed.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize and validate every semantic family without SQL</name>
  <files>crates/polint/src/analysis_kernel/store/mod.rs, crates/polint/src/analysis_kernel/store/commit_plan.rs</files>
  <read_first>
    - .agents/skills/rust-best-practices/SKILL.md
    - .claude/skills/polint/SKILL.md
    - crates/polint/src/analysis_kernel/store/mod.rs (private facade)
    - crates/polint/src/analysis_kernel/incremental/run_report.rs (ValidatedRunMetadata)
    - crates/polint/src/analysis_kernel/incremental/input_snapshot.rs (v2 full/scoped rows)
    - crates/polint/src/analysis_kernel/provider.rs, incremental/keys.rs, dependency_input.rs, dependency_index.rs, metadata.rs, and validation.rs (canonical types)
    - research/local-semantic-store/implementation/STORE-CONTRACT.md
  </read_first>
  <action>
    Create private commit_plan with pub(super) StoreCommitPlan, StorePlanError, StoreGenerationStats, and owned normalized rows for identities; input snapshot/files/components/details/settings/capabilities/provider schemas; provider manifests/children/generations; layers/variable digests; summary/query keys including exact QueryDependencyInputs; stable fact sidecars; one dependency edge set; validation events; semantic stats; and separate optional telemetry rows.

    StoreCommitPlan::from_validated_run accepts only ValidatedRunMetadata, calls its integrity validator, and copies workspace/full-config/run/generation/family identities verbatim. It may compute deterministic row counts and logical byte sizes, never semantic identities. Provider cache counters, DemandCacheStatus, durations, and timestamps are excluded from normalized semantic rows/digests; counter/status-only mutation tests preserve the semantic plan.

    Validate successful required events, provider/schema references, stable uniqueness, exact query-dependency endpoints, canonical order/deduplication, explicit statuses, repo-relative paths, counts/digests, and full-run completeness. Require payload_digest and add separate exact forbidden-field assertions for source_text, source_bytes, fact_payload, payload_blob, ast_blob, mir_blob, cfg_blob, summary_body, summary_blob, graph_nodes, and graph_edges. commit_plan.rs has no rusqlite/SQL/table string/relational ID/alternate hash, and analysis_kernel parents cannot name the type or constructor.

    Add at least 20 shuffle seeds and one typed negative fixture per error family. Finish with focused tests and cargo check so this is an independently compiling boundary before schema work.
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis_kernel::store::commit_plan::tests --locked &amp;&amp; cargo check -p polint --all-features --locked</automated>
  </verify>
  <acceptance_criteria>
    - Constructor accepts only ValidatedRunMetadata and no root/config/registry/cache parameter.
    - Every required family has normalized first-class rows and one canonical dependency set.
    - Missing/failed events, duplicate facts, unknown references, dangling query inputs/endpoints, identity/count mismatch, bad status, or absolute path returns a typed error with no partial plan.
    - At least 20 permutations yield identical semantic rows, copied identities, counts, and logical sizes.
    - Counter/status/duration/timestamp-only mutations preserve the semantic plan.
    - payload_digest is retained while exact forbidden body/blob/source/graph fields are absent.
    - StoreCommitPlan remains pub(super), SQL-free, and unnameable by the parent kernel.
  </acceptance_criteria>
  <done>One complete validated run maps to one deterministic private SQL-free plan.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description | Data Crossing |
|---|---|---|
| ValidatedRunMetadata to StoreCommitPlan | Canonical kernel rows become owned persistence rows | Identities, facts, query declarations, validation events |
| Temporary dependency wires to final v2 | Intermediate or old manifests reach the final reader | Schema labels, typed endpoints, QueryDependencyInputs |

## Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-65-15-01 | Tampering | StoreCommitPlan | mitigate | Fail-closed integrity/reference/count validation and copy-only semantic identities. |
| T-65-15-02 | Information Disclosure | Normalized rows | mitigate | Retain payload_digest only; exact forbidden source/body/blob/path assertions. |
| T-65-15-03 | Denial of Service | Semantic identity | mitigate | Exclude cache status/counters/durations/timestamps and mutation-test the projection. |
| T-65-15-04 | Elevation of Privilege | Store plan visibility | mitigate | Keep the module private and type pub(super), with source/compile assertions. |
| T-65-15-05 | Spoofing | Dependency-index schema | mitigate | Publish v2 only after QueryKey stabilization and fail closed for v1, both temporary labels, unknown, and future inputs. |
</threat_model>

<verification>
- cargo test -p polint --lib analysis_kernel::incremental::dependency_index::tests --locked
- cargo test -p polint --lib analysis_kernel::incremental::layer_cache::tests --locked
- cargo test -p polint --lib analysis_kernel::incremental::invalidation::tests --locked
- cargo test -p polint --lib analysis_kernel::store::commit_plan::tests --locked
- cargo check -p polint --all-features --locked
- cargo fmt --all -- --check
</verification>

<success_criteria>
- One final dependency-index v2 shape is published before one private normalized plan losslessly mirrors semantic metadata, exact query declarations, and payload_digest without SQL or telemetry contamination.
</success_criteria>

<output>Create 65-15-SUMMARY.md when complete.</output>
