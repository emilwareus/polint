---
phase: 65-generation-manifest-and-metadata-mirroring
plan: 12
type: execute
wave: 12
depends_on: [65-11]
files_modified:
  - crates/polint/src/analysis_kernel/mod.rs
  - crates/polint/src/analysis_kernel/incremental/layer_cache.rs
  - crates/polint/src/analysis_kernel/incremental/stats.rs
  - crates/polint/src/analysis_kernel/incremental/run_report.rs
  - crates/polint/src/analysis_kernel/incremental/mod.rs
  - crates/polint/src/go/adapter.rs
  - crates/polint/src/ts/adapter.rs
  - crates/polint/src/module_graph/mod.rs
  - crates/polint/src/symbol_graph/mod.rs
  - crates/polint/src/metrics.rs
autonomous: true
requirements: [META-01, META-04]
must_haves:
  truths:
    - "D-06/D-09: every cache-capable provider retains its exact LayerKey, output_digest, required payload_digest metadata, validation, warnings, and typed dependencies for this run."
    - "D-08: retained metadata contains no payload bytes, source bodies, fact bodies, AST/MIR/CFG blobs, graph adjacency, or summary content."
    - "D-15: hit, miss, disabled, invalid-read, and failed-write paths retain equivalent semantic rows."
    - "D-19: cache path/status/counters remain telemetry and are excluded from layer/provider family identities."
    - "Go and TS path-parity assertions live in cfg(test) modules inside the owned adapter files that implement those branches; this plan claims no new assertion from unowned go/tests.rs or ts/tests.rs."
  artifacts:
    - path: "crates/polint/src/analysis_kernel/incremental/layer_cache.rs"
      provides: "Exact run-retained compact layer metadata"
      contains: "LayerRunMetadata"
  key_links:
    - from: "crates/polint/src/go/adapter.rs"
      to: "crates/polint/src/analysis_kernel/incremental/layer_cache.rs"
      via: "Go hit/miss/disabled/invalid-read/failed-write branches return LayerRunMetadata built from the in-memory key/manifest"
      pattern: "LayerRunMetadata|go_layer_run_metadata_is_identical"
    - from: "crates/polint/src/ts/adapter.rs"
      to: "crates/polint/src/analysis_kernel/incremental/layer_cache.rs"
      via: "TS hit/miss/disabled/invalid-read/failed-write branches return the same semantic layer projection"
      pattern: "LayerRunMetadata|ts_layer_run_metadata_is_identical"
---

<objective>
Retain exact current-run layer/key/dependency metadata on every cache path without scanning cache files or admitting payload content.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
@.agents/skills/rust-best-practices/SKILL.md
@.claude/skills/polint/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-CONTEXT.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-PATTERNS.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-11-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Bubble exact compact layer metadata through every cache outcome</name>
  <files>crates/polint/src/analysis_kernel/mod.rs, crates/polint/src/analysis_kernel/incremental/layer_cache.rs, crates/polint/src/analysis_kernel/incremental/stats.rs, crates/polint/src/analysis_kernel/incremental/run_report.rs, crates/polint/src/analysis_kernel/incremental/mod.rs, crates/polint/src/go/adapter.rs, crates/polint/src/ts/adapter.rs, crates/polint/src/module_graph/mod.rs, crates/polint/src/symbol_graph/mod.rs, crates/polint/src/metrics.rs</files>
  <read_first>
    - .agents/skills/rust-best-practices/SKILL.md
    - .claude/skills/polint/SKILL.md
    - crates/polint/src/analysis_kernel/incremental/layer_cache.rs (manifest, read/write outcomes, validation)
    - crates/polint/src/analysis_kernel/incremental/stats.rs (ProviderOutputMeta semantic/telemetry split)
    - crates/polint/src/analysis_kernel/incremental/run_report.rs (provider output construction)
    - crates/polint/src/go/adapter.rs and crates/polint/src/ts/adapter.rs (syntax cache paths)
    - crates/polint/src/module_graph/mod.rs, crates/polint/src/symbol_graph/mod.rs, and crates/polint/src/metrics.rs (all cache outcomes)
    - crates/polint/src/analysis_kernel/mod.rs (provider result aggregation)
  </read_first>
  <action>
    Change LayerCacheManifest.validation to ProviderValidationStatus while preserving the serialized native_trusted label. Add LayerRunMetadata containing exact LayerKey, output_digest, required payload_digest metadata, typed expanded dependency edges, PrecisionTier, ProviderValidationStatus, and sorted deterministic warning codes. Its single constructor converts a normalized manifest and replaces the typed manifest-relative sentinel with CacheNode::Layer(key.clone()); do not create another edge builder.

    Extend Go/TS syntax, module graph/topology, symbol graph, metrics results, and ProviderOutputMeta with sorted/deduplicated LayerRunMetadata. Add provider_output_from_manifest_with_layers for real providers and keep the empty-layer helper only for explicitly synthetic fixtures. Refactor each recompute path to build one normalized manifest in memory before the optional write: miss, invalid-read, disabled bypass, and failed-write all derive metadata from that same manifest; disabled mode skips only persistence, and failed write retains the pre-write metadata. A validated hit converts the manifest returned by the read. Never enumerate .polint/cache/layers after execution.

    Define the semantic layer projection as key, output_digest, payload_digest, precision, validation, warnings, and typed edges. Cache read/write status and all CacheStats counters are separate telemetry and must not enter layer/provider family, run, or generation identity. Add status-only and counter-only mutation tests plus equivalent-path tests. Assert explicit forbidden fields/columns by exact names: source_text, source_bytes, fact_payload, payload_blob, ast_blob, mir_blob, cfg_blob, summary_body, summary_blob, graph_nodes, and graph_edges, and separately require payload_digest.

    In each already-owned adapter file add an explicitly named `#[cfg(test)] mod layer_run_metadata_path_tests` beside the implementation. Add `go_layer_run_metadata_is_identical_across_hit_miss_disabled_invalid_read_and_failed_write` in go/adapter.rs and `ts_layer_run_metadata_is_identical_across_hit_miss_disabled_invalid_read_and_failed_write` in ts/adapter.rs. Each test uses one source/key/settings tuple and independently forces: cold miss+successful write, warm validated hit, disabled bypass, corrupt-manifest/blob invalid-read recompute, and a deterministic failed layer write by making the managed `layers` root a regular file. Assert the CacheStats (and `internal/cache` write diagnostic for the failure) prove that each intended branch ran; assert every result has exactly one LayerRunMetadata; then compare the complete semantic projection—LayerKey, output_digest, payload_digest, precision, validation, warning codes, and typed edges—for equality across all five paths. Also assert cache counters/status are absent from that projection. Keep all new Go/TS path assertions in these two adapter files; do not edit or rely on go/tests.rs or ts/tests.rs for this plan's metadata-parity claim.
  </action>
  <verify>
    <automated>cargo test -p polint --lib go::adapter::layer_run_metadata_path_tests::go_layer_run_metadata_is_identical_across_hit_miss_disabled_invalid_read_and_failed_write --locked -- --exact --test-threads=1 &amp;&amp; cargo test -p polint --lib ts::adapter::layer_run_metadata_path_tests::ts_layer_run_metadata_is_identical_across_hit_miss_disabled_invalid_read_and_failed_write --locked -- --exact --test-threads=1 &amp;&amp; cargo test -p polint --lib analysis_kernel::incremental::layer_cache::tests --locked &amp;&amp; cargo test -p polint --lib module_graph::tests --locked &amp;&amp; cargo test -p polint --lib symbol_graph::tests --locked &amp;&amp; cargo test -p polint --lib metrics::tests --locked</automated>
  </verify>
  <acceptance_criteria>
    - All six cache-capable result families expose exact current-run LayerRunMetadata.
    - Hit/miss/disabled/invalid-read/failed-write fixtures compare equal semantic rows for equivalent outputs.
    - The two exact adapter-local metadata-path tests prove their branch via CacheStats/write diagnostics and assert equality of key, output_digest, payload_digest, precision, validation, warnings, and typed edges; go/tests.rs and ts/tests.rs contain no assertion added by this plan.
    - Cache status/counter-only mutations preserve layer/provider semantic family digests.
    - At least 20 completion-order permutations yield identical provider/layer rows.
    - payload_digest is present and round-trips; exact forbidden body/blob/source/graph fields are absent.
    - No post-run cache-directory discovery exists.
  </acceptance_criteria>
  <done>Exact typed layer metadata reaches ProviderOutputMeta on every path, retaining payload_digest but no payload bytes/bodies.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description | Data Crossing |
|---|---|---|
| Layer cache outcomes to run metadata | Hit/miss/failure paths produce one semantic layer row | Keys, payload digest, warnings, cache counters |
| Cached manifest to typed dependency graph | Manifest metadata becomes canonical typed edges | Endpoint kinds, digests, status |

## Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-65-12-01 | Tampering | Layer metadata handoff | mitigate | Single manifest conversion and equivalent-path tests prevent missing/rewritten edges. |
| T-65-12-02 | Information Disclosure | Retained layer row | mitigate | Allow payload_digest only; exact forbidden-field and sentinel tests reject bodies/blobs/source. |
| T-65-12-03 | Denial of Service | Family identity | mitigate | Exclude cache status/counters and test status/counter-only mutations. |
| T-65-12-04 | Repudiation | Current-run provenance | mitigate | Direct provider propagation forbids stale post-run cache scans. |
</threat_model>

<verification>
- cargo test -p polint --lib analysis_kernel::incremental::layer_cache::tests --locked
- cargo test -p polint --lib go::adapter::layer_run_metadata_path_tests::go_layer_run_metadata_is_identical_across_hit_miss_disabled_invalid_read_and_failed_write --locked -- --exact --test-threads=1
- cargo test -p polint --lib ts::adapter::layer_run_metadata_path_tests::ts_layer_run_metadata_is_identical_across_hit_miss_disabled_invalid_read_and_failed_write --locked -- --exact --test-threads=1
- cargo test -p polint --lib module_graph::tests --locked
- cargo fmt --all -- --check
</verification>

<success_criteria>
- Compact layer metadata is exact, typed, deterministic, telemetry-independent, and free of payload content.
</success_criteria>

<output>Create 65-12-SUMMARY.md when complete.</output>
