---
phase: 65-generation-manifest-and-metadata-mirroring
plan: 19
type: execute
wave: 19
depends_on: [65-18]
files_modified:
  - crates/polint/tests/public_surface_leak.rs
  - tests/fixtures/public-surface-leak-probe/src/lib.rs
  - crates/polint/src/eval/bench/runner.rs
  - crates/polint/src/eval/bench/gate.rs
  - crates/polint/src/analysis_kernel/mod.rs
  - crates/polint/src/analysis_kernel/store/mod.rs
  - crates/polint/src/analysis_kernel/store/tests.rs
  - crates/polint/tests/cli.rs
autonomous: true
requirements: [STORE-04, STORE-05, META-01, META-04]
must_haves:
  truths:
    - "D-17/D-20: private identities/tables/statuses/failure codes and raw details never leak through SDK/runner/CLI/docs/examples/generated skill/output."
    - "D-20: ALLOWED_PRELUDE remains exactly 115 and check/review bytes/exits remain identical across every store state."
    - "D-19/D-20: real enabled metadata persistence reports nonzero bytes under unchanged +20% RSS/+25% cold-time and 16 MiB/50 ms floors."
    - "Semantic family/run/generation identities remain unchanged under cache-status/counter/duration/timestamp-only mutations."
    - "All focused, format, clippy, make lint, and workspace all-feature gates pass with no Phase 66+ scope."
  artifacts:
    - path: "crates/polint/tests/public_surface_leak.rs"
      provides: "Precise private metadata markers and negative controls"
      contains: "STORE_PRIVATE_MARKERS"
    - path: "crates/polint/src/eval/bench/gate.rs"
      provides: "Real enabled metadata-persistence boundary"
      contains: "real_store_enabled_measurement_passes_locked_boundary"
---

<objective>
Close Phase 65 behind privacy, parity, deterministic-identity, real enabled performance, lint, and full-workspace gates without weakening budgets or expanding scope.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
@.agents/skills/rust-best-practices/SKILL.md
@.claude/skills/polint/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-CONTEXT.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-PATTERNS.md
@.planning/phases/65-generation-manifest-and-metadata-mirroring/65-18-SUMMARY.md
@.planning/phases/64-store-foundation-and-boundary-proof/64-04-SUMMARY.md
@.planning/phases/63-ground-truth-and-performance-baseline/63-04-SUMMARY.md
@docs/API-VISIBILITY-PLAN.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Enforce private surface, parity, determinism, performance, and full verification</name>
  <files>crates/polint/tests/public_surface_leak.rs, tests/fixtures/public-surface-leak-probe/src/lib.rs, crates/polint/src/eval/bench/runner.rs, crates/polint/src/eval/bench/gate.rs, crates/polint/src/analysis_kernel/mod.rs, crates/polint/src/analysis_kernel/store/mod.rs, crates/polint/src/analysis_kernel/store/tests.rs, crates/polint/tests/cli.rs</files>
  <read_first>
    - .agents/skills/rust-best-practices/SKILL.md
    - .claude/skills/polint/SKILL.md
    - crates/polint/tests/public_surface_leak.rs (115-name allowlist, scans, negative controls)
    - tests/fixtures/public-surface-leak-probe/src/lib.rs (outside SDK consumer)
    - crates/polint/src/analysis_kernel/mod.rs and crates/polint/src/analysis_kernel/store/mod.rs (private integration outcomes)
    - crates/polint/src/analysis_kernel/store/tests.rs (lifecycle/invalidation/telemetry fixtures)
    - crates/polint/tests/cli.rs (public parity/private vocabulary)
    - crates/polint/src/eval/bench/runner.rs and crates/polint/src/eval/bench/gate.rs (real enabled boundary)
    - docs/API-VISIBILITY-PLAN.md
  </read_first>
  <action>
    Extend STORE_PRIVATE_MARKERS and working negative controls with precise module/type/table/internal-ID/identity/codec names, including WorkspaceIdentity, GenerationIdentity, StoreCommitPlan, InputDependencyKey, QueryDependencyInputs, RequestedCapabilitySnapshot, GenerationFailureEvent/Reason/Stage, generation_failure_events, generations, and dependency_edges. Keep generic words such as generation, manifest, status, workspace, identity, provider, dependency, and store allowed. Scan SDK, runner, CLI, crate root, README, API plan, docs/facts, examples, real check/review JSON, and generated polint skill text. Keep ALLOWED_PRELUDE exactly 115 and the external no_implicit_prelude fixture on supported SDK imports only.

    Re-run check/review parity for committed, mismatch, first-failure recovery, audited failed, unaudited pending, busy, future, invalid, corrupt, and unsafe states. Assert no private identity/status/stats/event/table/SQL/raw error leaks. Re-run semantic projection tests showing DemandCacheStatus, ProviderOutputMeta.cache_stats counters, durations, and timestamps cannot affect provider/layer/query/fact/dependency/validation aggregates, RunIdentity, GenerationIdentity, selection, or invalidation actions.

    Ensure the isolated enabled benchmark performs a real complete metadata generation and reports store_bytes greater than zero with equal diagnostics digest. Run the serialized ignored gate with unchanged 1.20 RSS ratio, 1.25 cold ratio, 16 MiB RSS floor, and 50 ms cold floor. Optimize avoidable work if needed; never relax floors/ratios or hide first-open migration.

    Run all focused suites, formatting, all-feature clippy, make lint, and full workspace all-feature tests. Audit final diff for public activation; semantic fact payload bytes or graph adjacency ingest (payload_digest metadata is required and allowed); summary content/reuse; query/graph CLI; search implementation; pruning/full crash-scale work; alternate hashes; visibility widening; and shipped delivery-history comments. None may appear. This is the explicit Phase 66+ fence.
  </action>
  <verify>
    <automated>cargo test -p polint --test public_surface_leak --locked -- --test-threads=1 &amp;&amp; cargo test -p polint --lib analysis_kernel::store::tests --locked -- --test-threads=1 &amp;&amp; cargo test -p polint --lib analysis_kernel::tests::semantic_store_check_parity::all_store_modes_preserve_byte_identical_json_and_exit_semantics --locked -- --test-threads=1 &amp;&amp; cargo test -p polint --lib eval::bench::runner::tests::semantic_store::isolated_modes_report_real_store_bytes_and_equal_diagnostics_digest --locked -- --test-threads=1 &amp;&amp; cargo test -p polint --lib eval::bench::gate::tests::semantic_store_boundary::real_store_enabled_measurement_passes_locked_boundary --locked -- --ignored --test-threads=1 &amp;&amp; cargo fmt --all -- --check &amp;&amp; cargo clippy --workspace --all-targets --all-features --locked -- -D warnings &amp;&amp; make lint &amp;&amp; cargo test --workspace --all-features --locked</automated>
  </verify>
  <acceptance_criteria>
    - Public leak suite passes, external fixture builds, ALLOWED_PRELUDE is exactly 115, and every marker family has a working negative control.
    - Check/review bytes/exits equal disabled mode for every listed state.
    - Telemetry-only mutations preserve all semantic aggregates, identities, selection, and invalidation actions.
    - Enabled runner reports nonzero store bytes and equal diagnostics digest.
    - Serialized performance boundary passes with thresholds/floors unchanged.
    - fmt, all-feature clippy, make lint, and full workspace tests exit zero.
    - Final diff retains payload_digest metadata but contains no payload bytes/body/blob/graph adjacency or other Phase 66+ implementation.
  </acceptance_criteria>
  <done>Phase 65 satisfies privacy, parity, determinism, performance, lint, and full-workspace gates without later-phase work.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description | Data Crossing |
|---|---|---|
| Private store/kernel to public surfaces | Internal vocabulary and failures coexist with SDK/CLI output | Types, table names, statuses, raw errors |
| Enabled store to performance harness | Real metadata persistence affects measured resources | Store bytes, RSS, cold time, diagnostics digest |
| Phase 65 diff to later roadmap | Metadata-only work borders payload/query/search phases | Payload digest versus payload bytes, public features |

## Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-65-19-01 | Information Disclosure | Public surface | mitigate | Precise markers, negative controls, outside consumer, docs/skill/output scans. |
| T-65-19-02 | Tampering | Public policy behavior | mitigate | Byte/exit parity across all store outcomes. |
| T-65-19-03 | Denial of Service | Enabled store resources | mitigate | Real serialized benchmark with immutable ratios and floors. |
| T-65-19-04 | Tampering | Semantic determinism | mitigate | Counter/status/duration/timestamp mutation matrix covers all identities and actions. |
| T-65-19-05 | Elevation of Privilege | Phase/public scope | mitigate | Final diff fence rejects public activation and every Phase 66+ feature. |
</threat_model>

<verification>
- cargo test -p polint --test public_surface_leak --locked -- --test-threads=1
- cargo test -p polint --lib eval::bench::gate::tests::semantic_store_boundary::real_store_enabled_measurement_passes_locked_boundary --locked -- --ignored --test-threads=1
- cargo fmt --all -- --check
- cargo clippy --workspace --all-targets --all-features --locked -- -D warnings
- make lint
- cargo test --workspace --all-features --locked
</verification>

<success_criteria>
- Private metadata stays invisible, semantic identity excludes telemetry, enabled cost stays inside the locked boundary, and no later-phase scope lands.
</success_criteria>

<output>Create 65-19-SUMMARY.md when complete.</output>
