---
phase: 45-js-ts-inventory-scope-bindings-module-graph-direct-calls
plan: 02
type: execute
wave: 2
depends_on: [45-01]
files_modified:
  - crates/polint/src/ts/mod.rs
  - crates/polint/src/ts/scope/mod.rs
  - crates/polint/src/ts/scope/facts.rs
  - crates/polint/src/ts/scope/extract.rs
  - crates/polint/src/ts/scope/store.rs
  - crates/polint/src/ts/scope/tests.rs
  - crates/polint/src/analysis/ids.rs
  - crates/polint/src/core/mod.rs
autonomous: true
requirements: [JS-02]

must_haves:
  truths:
    - "Private ts::scope module exists and uses Oxc semantic/scoping data as the source of truth where available (D-06)"
    - "Scope and binding facts cover var, let, const, functions, classes, imports, destructuring, parameters, catch bindings, re-exports, namespace imports, default imports, named imports, and aliases (D-07)"
    - "Binding facts carry honest statuses for present, unresolved, unsupported dynamic, and external bindings (D-08)"
    - "Direct binding remains narrow and excludes token propagation, property flow, prototypes, and this modeling (D-09)"
  artifacts:
    - path: "crates/polint/src/ts/scope/facts.rs"
      provides: "Scope and binding fact model"
      contains: "enum TsBindingKind"
    - path: "crates/polint/src/ts/scope/extract.rs"
      provides: "Oxc semantic/scoping extraction"
      contains: "extract_ts_scope"
    - path: "crates/polint/src/ts/scope/store.rs"
      provides: "Normalized scope/binding output and lookup indexes"
      contains: "bindings_by_name"
---

<objective>
Build the private JS/TS lexical scope and binding layer for JS-02. This plan uses Oxc semantic/scoping data to produce deterministic private facts that later Phase 45 plans use for direct binding and module-mediated calls.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-js-ts-inventory-scope-bindings-module-graph-direct-calls/45-CONTEXT.md
@.planning/phases/45-js-ts-inventory-scope-bindings-module-graph-direct-calls/45-PATTERNS.md
@.planning/phases/45-js-ts-inventory-scope-bindings-module-graph-direct-calls/45-01-SUMMARY.md

<interfaces>
- `symbol_graph::ts` is the main analog for `oxc_semantic::SemanticBuilder`, `Scoping`, scope IDs, import aliases, exports, and stable keys.
- `ts::inventory` from Plan 01 provides function/callsite source identities and spans.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define scope and binding fact model</name>
  <read_first>
    - crates/polint/src/symbol_graph/semantic.rs
    - crates/polint/src/symbol_graph/ts.rs
    - crates/polint/src/ts/inventory/facts.rs
    - crates/polint/src/analysis/ids.rs
  </read_first>
  <action>
    Add `ts/scope/facts.rs` with private facts for lexical scopes and bindings. Include enums for scope kind, declaration kind, binding kind, import/export kind, and binding status. The fact model must distinguish `var`, `let`, `const`, function declarations, function expressions, class declarations/expressions, parameters, destructuring bindings, catch bindings, imports, re-exports, aliases, namespace imports, default imports, and named imports. Carry stable_key, file, span, parent scope identity, name, declaration kind, and status.
  </action>
  <verify>
    <automated>cargo test -p polint --lib ts::scope::facts</automated>
  </verify>
  <acceptance_criteria>
    - `ts/scope/facts.rs` contains `TsBindingStatus` variants for present, unresolved, unsupported dynamic, and external or documented equivalents.
    - The binding kind/declaration kind model can represent every D-07 item.
    - All new items are private or `pub(crate)`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract scopes and bindings from Oxc semantic data</name>
  <read_first>
    - crates/polint/src/symbol_graph/ts.rs
    - crates/polint/src/ts/scope/facts.rs
    - crates/polint/src/ts/scope/extract.rs
    - crates/polint/src/ts/inventory/store.rs
  </read_first>
  <action>
    Create `ts/scope/extract.rs` using `oxc_semantic::SemanticBuilder` and `Scoping` to emit scope and binding facts. Use AST fallback only where semantic data lacks a direct row, and document each fallback in code comments. Preserve hoisting-sensitive distinctions for `var`, `let`/`const`, and function declarations. Add tests with imports, aliases, function params, destructuring, catch bindings, classes, and re-exports.
  </action>
  <verify>
    <automated>cargo test -p polint --lib ts::scope::extract</automated>
  </verify>
  <acceptance_criteria>
    - The extraction test asserts rows for var, let, const, function, class, import, destructuring, parameter, catch, re-export, namespace import, default import, named import, and alias cases.
    - Oxc semantic/scoping APIs are used in the extraction path.
    - Any AST fallback has an explicit comment naming the Oxc semantic gap it covers.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add normalized scope store and binding lookup indexes</name>
  <read_first>
    - crates/polint/src/ts/scope/facts.rs
    - crates/polint/src/ts/scope/store.rs
    - crates/polint/src/analysis/semantic_graph/store.rs
    - crates/polint/src/symbol_graph/model.rs
  </read_first>
  <action>
    Add `ts/scope/store.rs` with normalized output sorted by stable key and dense IDs assigned after sorting. Build indexes by file, parent scope, binding name, imported module source, exported name, and inventory function/callsite stable key where applicable. Add lookup helpers needed by Plan 03: same-file binding lookup by callsite scope/name and import/export alias lookup by resolved import identity.
  </action>
  <verify>
    <automated>cargo test -p polint --lib ts::scope</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p polint --lib ts::scope` exits 0.
    - Store has deterministic lookup by binding name and scope.
    - Normalization assigns dense IDs after stable-key sorting.
    - `ts/mod.rs` contains `pub(crate) mod scope;`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Preserve direct-binding boundary with unresolved statuses</name>
  <read_first>
    - crates/polint/src/ts/scope/extract.rs
    - crates/polint/src/ts/scope/store.rs
    - .planning/phases/45-js-ts-inventory-scope-bindings-module-graph-direct-calls/45-CONTEXT.md
  </read_first>
  <action>
    Add tests proving this plan does not implement token propagation, property flow, prototypes, or `this`: cases requiring those features must produce unresolved/unsupported binding statuses with named reasons. Include at least one callback/parameter case, one computed property call, one prototype method case, and one `this`-dependent method case.
  </action>
  <verify>
    <automated>cargo test -p polint --lib ts::scope::direct_binding_boundary</automated>
  </verify>
  <acceptance_criteria>
    - Boundary tests assert callback/parameter, computed property, prototype, and `this` cases remain unresolved or unsupported in Phase 45.
    - No code in `ts/scope` contains token-set propagation, prototype walk, or object allocation solving.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Oxc semantic data -> scope facts | Parser/scoping data is trusted only as bounded analyzer input; unsupported forms remain explicit statuses. |
| scope facts -> later binding/solver phases | This plan must not overclaim dynamic JS semantics that later phases own. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-02-01 | Tampering | Binding resolution status | mitigate | Dynamic/computed/prototype/this cases must emit unresolved/unsupported statuses with tests. |
| T-45-02-02 | Information Disclosure | Private scope facts | mitigate | Keep all scope/binding types private and verify leak gate in Plan 05. |
| T-45-02-03 | Denial of Service | Scope lookup depth | mitigate | Build deterministic indexes by scope/name/module; avoid repeated full scans in direct binding. |
| T-45-02-SC | Tampering | New dependencies | mitigate | Reuse Oxc semantic crates already present; add no new dependency unless justified in Cargo.toml and cache digests. |
</threat_model>

<verification>
- `cargo test -p polint --lib ts::scope` exits 0.
- Boundary tests prove Phase 49/50-only behavior remains unresolved/unsupported.
</verification>

<success_criteria>
- JS-02 lexical scope and binding facts exist for all required binding forms.
- Scope/binding output is deterministic, private, and indexed for Plan 03 direct binding.
</success_criteria>

<output>
Create `.planning/phases/45-js-ts-inventory-scope-bindings-module-graph-direct-calls/45-02-SUMMARY.md` when done.
</output>

