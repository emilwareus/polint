---
phase: 45-js-ts-inventory-scope-bindings-module-graph-direct-calls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/polint/src/ts/mod.rs
  - crates/polint/src/ts/inventory/mod.rs
  - crates/polint/src/ts/inventory/facts.rs
  - crates/polint/src/ts/inventory/extract.rs
  - crates/polint/src/ts/inventory/store.rs
  - crates/polint/src/ts/inventory/tests.rs
  - crates/polint/src/analysis/ids.rs
  - crates/polint/src/core/mod.rs
autonomous: true
requirements: [JS-01]

must_haves:
  truths:
    - "Private ts::inventory module exists, is declared pub(crate), and reuses Oxc parsing/span conversion rather than adding a parser stack (D-01, D-03)"
    - "Inventory facts cover required JS/TS function forms: declarations, expressions, arrows, methods, constructors, accessors, and class static blocks (D-02)"
    - "Inventory facts cover required callsite forms: calls, new, tagged templates, optional calls, dynamic import, and require (D-02)"
    - "Stable keys include file identity, Oxc span, lexical parent/scope identity where available, syntactic form, and display name; dense IDs are assigned only after stable-key sort (D-05)"
    - "Unsupported inventory forms emit explicit unresolved/unsupported rows rather than fabricated identities (D-04)"
  artifacts:
    - path: "crates/polint/src/ts/inventory/facts.rs"
      provides: "TsInventoryFunctionFact and TsInventoryCallsiteFact plus closed kind/status enums"
      contains: "enum TsFunctionInventoryKind"
    - path: "crates/polint/src/ts/inventory/extract.rs"
      provides: "Oxc AST traversal for required function and callsite forms"
      contains: "extract_ts_inventory"
    - path: "crates/polint/src/ts/inventory/store.rs"
      provides: "Normalized output/store indexes sorted by stable key"
      contains: "normalized"
  key_links:
    - from: "crates/polint/src/ts/adapter.rs"
      to: "crates/polint/src/ts/inventory/extract.rs"
      via: "reuse parse_source_type/span_from_oxc pattern"
      pattern: "span_from_byte_range"
---

<objective>
Create the private JS/TS inventory layer for JS-01: benchmark-grade function and callsite facts derived from Oxc with stable identities and Jelly-compatible spans. This plan only inventories source forms and stores normalized private facts; scope/binding/module resolution and semantic graph constraints land in later Phase 45 plans.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-js-ts-inventory-scope-bindings-module-graph-direct-calls/45-CONTEXT.md
@.planning/phases/45-js-ts-inventory-scope-bindings-module-graph-direct-calls/45-PATTERNS.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md

<interfaces>
- Existing parser/span analog: `crates/polint/src/ts/adapter.rs` uses `Parser`, `SourceType::from_path`, and `span_from_byte_range`.
- Existing Oxc semantic analog: `crates/polint/src/symbol_graph/ts.rs` has semantic stable-key and scope/reference patterns.
- Existing identity renderer: `crates/polint/src/analysis/identity/render/jelly_span.rs` remains the only Jelly renderer.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add private inventory fact model and IDs</name>
  <read_first>
    - crates/polint/src/analysis/ids.rs
    - crates/polint/src/core/mod.rs
    - crates/polint/src/analysis/semantic_graph/facts.rs
    - crates/polint/src/analysis/calls/facts.rs
  </read_first>
  <action>
    Add crate-private inventory ID newtypes in `analysis/ids.rs` for TS inventory functions and callsites if existing IDs cannot represent the new private rows. Add `crates/polint/src/ts/inventory/facts.rs` with `TsFunctionInventoryKind`, `TsCallsiteInventoryKind`, `TsInventoryStatus`, `TsInventoryFunctionFact`, and `TsInventoryCallsiteFact`. The function kind enum must include `Declaration`, `FunctionExpression`, `Arrow`, `Method`, `Constructor`, `Accessor`, and `ClassStaticBlock`. The callsite kind enum must include `Call`, `New`, `TaggedTemplate`, `OptionalCall`, `DynamicImport`, and `Require`. Each fact must carry file, span, stable_key, lexical_parent_key or equivalent optional parent identity, display_name where available, and status.
  </action>
  <verify>
    <automated>cargo test -p polint --lib ts::inventory::facts analysis::ids</automated>
  </verify>
  <acceptance_criteria>
    - `crates/polint/src/ts/inventory/facts.rs` contains all required function and callsite kind variants by exact name or documented equivalent.
    - All new fact/enum/ID items are `pub(crate)` or private; no new public SDK export appears.
    - `cargo test -p polint --lib ts::inventory::facts analysis::ids` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Oxc inventory extraction for function forms</name>
  <read_first>
    - crates/polint/src/ts/adapter.rs
    - crates/polint/src/symbol_graph/ts.rs
    - crates/polint/src/ts/inventory/facts.rs
    - crates/polint/src/ts/inventory/extract.rs
  </read_first>
  <action>
    Create `ts/inventory/extract.rs` with `extract_ts_inventory(file: &SourceFile) -> TsInventoryOutput` or an equivalent crate-private API. Traverse Oxc AST nodes to emit function inventory rows for declarations, expressions, arrows, class methods, constructors, getters/setters, and class static blocks. Use the existing `SourceType::from_path` and Oxc span conversion pattern; do not compute Jelly line/column strings here. Include tests with one fixture source containing every required function form and assert the emitted kind set and stable-key ordering.
  </action>
  <verify>
    <automated>cargo test -p polint --lib ts::inventory::extract_function_forms</automated>
  </verify>
  <acceptance_criteria>
    - The function-form test asserts at least one emitted row for every required function kind.
    - Function rows are sorted by stable_key after normalization and dense IDs are stable under input order changes.
    - No direct references to benchmark expected labels or Jelly fixture filenames appear in extraction logic.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Oxc inventory extraction for callsite forms</name>
  <read_first>
    - crates/polint/src/ts/adapter.rs
    - crates/polint/src/analysis/calls/extract.rs
    - crates/polint/src/ts/inventory/facts.rs
    - crates/polint/src/ts/inventory/extract.rs
  </read_first>
  <action>
    Extend `ts/inventory/extract.rs` to emit callsite inventory rows for normal calls, `new`, tagged templates, optional calls, string and non-string dynamic imports, and CommonJS `require`. For non-string dynamic imports, computed/dynamic callees, or unsupported syntactic forms, emit an unresolved/unsupported status with a reason instead of fabricating a target. Add tests asserting the callsite kind set, source spans, and unresolved reason for a non-string dynamic import.
  </action>
  <verify>
    <automated>cargo test -p polint --lib ts::inventory::extract_callsite_forms</automated>
  </verify>
  <acceptance_criteria>
    - The callsite-form test asserts at least one emitted row for `Call`, `New`, `TaggedTemplate`, `OptionalCall`, `DynamicImport`, and `Require`.
    - Non-string dynamic import emits an unresolved or unsupported status row with a named reason.
    - Extracted spans are converted with the existing byte-span helper and no direct Jelly string rendering occurs in this module.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Normalize/store inventory output and bridge to existing DB lifecycle</name>
  <read_first>
    - crates/polint/src/core/mod.rs
    - crates/polint/src/analysis/semantic_graph/store.rs
    - crates/polint/src/ts/inventory/store.rs
    - crates/polint/src/ts/mod.rs
  </read_first>
  <action>
    Add `ts/inventory/store.rs` with `TsInventoryOutput::normalized()` and indexes by file, stable key, function kind, and callsite kind. Add an `AnalysisDb` replace/accessor path if the planner chooses persistent DB storage. Register `pub(crate) mod inventory;` from `ts/mod.rs`. Add shuffle-stability tests that create permuted rows, normalize them, and assert identical stable keys and dense IDs.
  </action>
  <verify>
    <automated>cargo test -p polint --lib ts::inventory</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p polint --lib ts::inventory` exits 0.
    - Normalization sorts by stable_key before assigning dense IDs.
    - Store indexes allow deterministic lookup by file and stable key.
    - `ts/mod.rs` contains `pub(crate) mod inventory;`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source text -> Oxc parser | Untrusted repository source is parsed with the existing Oxc pipeline; parser diagnostics remain controlled diagnostics. |
| inventory facts -> public SDK | New facts are private and must not leak through `polint::sdk::prelude::*`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01-01 | Tampering | Stable identity construction | mitigate | Stable keys include file/span/form/parent/display and are assigned before dense IDs; shuffle tests enforce deterministic output. |
| T-45-01-02 | Information Disclosure | Private inventory facts | mitigate | Keep all types `pub(crate)` and verify with public-surface-leak in Plan 05. |
| T-45-01-03 | Repudiation | Unsupported AST forms | mitigate | Emit explicit unsupported/unresolved status rows with reasons instead of dropping or fabricating identities. |
| T-45-01-SC | Tampering | New dependencies | mitigate | No new parser dependency; reuse Oxc already in the workspace. |
</threat_model>

<verification>
- `cargo test -p polint --lib ts::inventory` exits 0.
- `cargo test -p polint --lib analysis::ids` exits 0 if IDs were added.
- `cargo test -p polint --test public_surface_leak` remains green in Plan 05.
</verification>

<success_criteria>
- JS-01 inventory facts exist for every required function and callsite form with stable Oxc-derived spans.
- Unsupported/dynamic forms are explicit status rows, not fabricated identities.
- All inventory code stays private and deterministic.
</success_criteria>

<output>
Create `.planning/phases/45-js-ts-inventory-scope-bindings-module-graph-direct-calls/45-01-SUMMARY.md` when done.
</output>

