---
phase: 45-js-ts-inventory-scope-bindings-module-graph-direct-calls
plan: 03
type: execute
wave: 3
depends_on: [45-01, 45-02]
files_modified:
  - crates/polint/src/ts/binding/mod.rs
  - crates/polint/src/ts/binding/facts.rs
  - crates/polint/src/ts/binding/direct.rs
  - crates/polint/src/ts/binding/store.rs
  - crates/polint/src/ts/binding/tests.rs
  - crates/polint/src/ts/mod.rs
  - crates/polint/src/module_graph/ts.rs
  - crates/polint/src/module_graph/model.rs
  - crates/polint/src/analysis/ids.rs
  - crates/polint/src/core/mod.rs
autonomous: true
requirements: [JS-02, JS-03]

must_haves:
  truths:
    - "Direct binding reuses existing module_graph::ts, module_graph::model, oxc_resolver, topology, ResolvedImportFact, and module topology cache machinery (D-10)"
    - "ESM, CommonJS, dynamic import, require, package entrypoints, workspace packages, and TypeScript path aliases are represented through existing module graph IDs/facts where possible (D-11)"
    - "Direct binding covers f(), ns.f(), imported aliases, re-exported aliases, local aliases, and statically resolved module members only (D-09)"
    - "Cache identity for new JS/TS binding layers includes syntax/scope/module graph outputs plus tsconfig/jsconfig/package/workspace/lockfile lifecycle inputs (D-12)"
    - "Unresolved dynamic imports, computed property names, eval, external packages, and unsupported native callbacks produce named unresolved/unsupported reasons (D-08, D-16)"
  artifacts:
    - path: "crates/polint/src/ts/binding/facts.rs"
      provides: "Direct binding fact and unresolved reason model"
      contains: "enum TsDirectBindingReason"
    - path: "crates/polint/src/ts/binding/direct.rs"
      provides: "Direct local/import/module binding resolver"
      contains: "resolve_direct_bindings"
    - path: "crates/polint/src/ts/binding/store.rs"
      provides: "Normalized binding output and indexes"
      contains: "bindings_by_callsite"
---

<objective>
Bridge JS/TS scope facts with the existing module graph to produce direct binding facts for JS-02/JS-03. This plan resolves only static local and module-mediated calls that are directly provable; it leaves token/property/object/prototype behavior unresolved for later phases.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-js-ts-inventory-scope-bindings-module-graph-direct-calls/45-CONTEXT.md
@.planning/phases/45-js-ts-inventory-scope-bindings-module-graph-direct-calls/45-PATTERNS.md
@.planning/phases/45-js-ts-inventory-scope-bindings-module-graph-direct-calls/45-01-SUMMARY.md
@.planning/phases/45-js-ts-inventory-scope-bindings-module-graph-direct-calls/45-02-SUMMARY.md

<interfaces>
- `module_graph::ts` and `module_graph::model` remain the resolution authority.
- `ts::inventory` provides callsite/function rows.
- `ts::scope` provides local/import/export binding rows.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define direct binding fact and unresolved reason model</name>
  <read_first>
    - crates/polint/src/analysis/calls/facts.rs
    - crates/polint/src/ts/inventory/facts.rs
    - crates/polint/src/ts/scope/facts.rs
    - crates/polint/src/module_graph/model.rs
  </read_first>
  <action>
    Add private `ts/binding/facts.rs` with direct binding facts connecting an inventory callsite to a resolved inventory function or an unresolved reason. Include reason/status variants for local binding missing, imported binding unresolved, non-string dynamic import, computed property, eval, external package unresolved, unsupported native callback, token flow required, property flow required, prototype model required, and this model required. Include references to existing `ResolvedImportId`, `ModuleNodeId`, inventory IDs/stable keys, and scope binding IDs; do not duplicate module graph payloads.
  </action>
  <verify>
    <automated>cargo test -p polint --lib ts::binding::facts</automated>
  </verify>
  <acceptance_criteria>
    - Binding facts reference module graph and inventory/scope identities rather than copying module path payloads.
    - All unresolved reason variants listed in the action are present or documented with exact equivalent names.
    - All new items are private or `pub(crate)`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resolve same-file and local alias direct calls</name>
  <read_first>
    - crates/polint/src/ts/binding/facts.rs
    - crates/polint/src/ts/scope/store.rs
    - crates/polint/src/ts/inventory/store.rs
    - crates/polint/src/analysis/calls/direct.rs
  </read_first>
  <action>
    Implement `ts/binding/direct.rs` local binding resolution for `f()`, `alias()`, and `ns.f()` where `ns` is a statically known namespace/local object binding and `f` resolves to an inventory function in the same file. Include local alias support for `const alias = f; alias()` when the RHS is a directly known function binding. Emit unresolved reasons for computed property calls and aliases requiring token propagation.
  </action>
  <verify>
    <automated>cargo test -p polint --lib ts::binding::direct_local</automated>
  </verify>
  <acceptance_criteria>
    - Tests assert direct binding for `f()`, `alias()`, and a static `ns.f()` case.
    - Tests assert computed property and token-flow-required aliases remain unresolved with named reasons.
    - No iterative token propagation or fixed-point solver appears in `ts/binding`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Resolve ESM/CommonJS/tsconfig module-mediated direct calls</name>
  <read_first>
    - crates/polint/src/module_graph/ts.rs
    - crates/polint/src/module_graph/model.rs
    - crates/polint/src/core/mod.rs
    - crates/polint/src/ts/binding/direct.rs
  </read_first>
  <action>
    Extend direct binding resolution to consume existing module graph and resolved import facts for `import { f as g } from "./m"; g()`, default imports, namespace imports, re-exports, `const f = require("./m").f; f()`, package entrypoint resolution, workspace packages, and TypeScript path aliases. Do not resolve modules by filesystem path inside `ts/binding`; read `ResolvedImportFact` / `ModuleNodeId` / package topology facts produced by module_graph.
  </action>
  <verify>
    <automated>cargo test -p polint --lib ts::binding::direct_modules module_graph::ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests cover ESM named/default/namespace imports, re-export aliases, CommonJS require member, and tsconfig path alias.
    - `ts/binding` does not instantiate `oxc_resolver::Resolver` directly; resolver usage remains in module graph code.
    - External or unresolved packages produce named unresolved reasons.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add normalized binding store and cache input contract</name>
  <read_first>
    - crates/polint/src/ts/binding/store.rs
    - crates/polint/src/analysis_kernel/incremental/keys.rs
    - crates/polint/src/analysis/types/cache_key.rs
    - crates/polint/src/analysis/semantic_graph/cache_key.rs
  </read_first>
  <action>
    Add normalized binding output/store indexes by callsite, target function, unresolved reason, module node, and stable key. Add cache-key helper or provider parameter digest documentation for binding output that includes TS syntax output, inventory/scope output, module graph output, tsconfig/jsconfig and extends inputs, package/workspace/lockfile inputs, config/lifecycle inputs, and provider/schema versions. If the binding layer is not yet a standalone cached provider, add a documented digest helper consumed by Plan 04.
  </action>
  <verify>
    <automated>cargo test -p polint --lib ts::binding</automated>
  </verify>
  <acceptance_criteria>
    - Binding output normalization is stable under row shuffle.
    - Store indexes support deterministic lookup by callsite and unresolved reason.
    - Cache/digest code or documentation names all D-12 inputs.
    - `ts/mod.rs` contains `pub(crate) mod binding;`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| module graph facts -> direct binding | Existing resolver output is trusted as the authority; binding code must not bypass it with ad hoc filesystem reads. |
| direct binding -> semantic graph constraints | Only directly proven bindings should become constraints; unresolved/dynamic cases stay explicit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-03-01 | Tampering | Parallel module resolution | mitigate | Binding code consumes module_graph facts and tests assert no direct `oxc_resolver::Resolver` construction in `ts/binding`. |
| T-45-03-02 | Information Disclosure | Private binding facts | mitigate | Keep all binding types private and verify public leak gate in Plan 05. |
| T-45-03-03 | Repudiation | Dynamic JS unresolved cases | mitigate | Named unresolved reasons cover dynamic imports, computed property, eval, token/property/prototype/this-required cases. |
| T-45-03-04 | Tampering | Cache-key incompleteness | mitigate | D-12 cache input contract names syntax/scope/module graph and tsconfig/package/lockfile inputs; Plan 05 validates determinism. |
</threat_model>

<verification>
- `cargo test -p polint --lib ts::binding` exits 0.
- `cargo test -p polint --lib module_graph::ts` exits 0.
- Static source assertion: `rg "Resolver::new|oxc_resolver::Resolver" crates/polint/src/ts/binding` returns no direct resolver construction.
</verification>

<success_criteria>
- JS-02 module graph and JS-03 direct binding facts exist for static local and module-mediated calls.
- Dynamic/non-direct behavior remains explicitly unresolved for later phases.
</success_criteria>

<output>
Create `.planning/phases/45-js-ts-inventory-scope-bindings-module-graph-direct-calls/45-03-SUMMARY.md` when done.
</output>

