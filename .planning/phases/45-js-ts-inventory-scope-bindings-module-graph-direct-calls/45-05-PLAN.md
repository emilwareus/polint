---
phase: 45-js-ts-inventory-scope-bindings-module-graph-direct-calls
plan: 05
type: execute
wave: 5
depends_on: [45-01, 45-02, 45-03, 45-04]
files_modified:
  - crates/polint/src/eval/semantic_graph_snapshot.rs
  - crates/polint/src/eval/external/jelly_callgraph.rs
  - tests/eval-fixtures/semantic-graph/ts_direct_bindings/expected.polint-eval.toml
  - tests/eval-fixtures/semantic-graph/ts_direct_bindings/repo/.polint.toml
  - tests/eval-fixtures/semantic-graph/ts_direct_bindings/repo/package.json
  - tests/eval-fixtures/semantic-graph/ts_direct_bindings/repo/tsconfig.json
  - tests/eval-fixtures/semantic-graph/ts_direct_bindings/repo/src/app.ts
  - tests/eval-fixtures/semantic-graph/ts_direct_bindings/repo/src/lib.ts
  - tests/eval-fixtures/jelly/ts-inventory-spans/expected.polint-eval.toml
  - tests/eval-fixtures/jelly/ts-inventory-spans/repo/.polint.toml
  - tests/eval-fixtures/jelly/ts-inventory-spans/repo/package.json
  - tests/eval-fixtures/jelly/ts-inventory-spans/repo/src/forms.ts
autonomous: true
requirements: [JS-01, JS-02, JS-03]

must_haves:
  truths:
    - "Native fixtures cover every required function/callsite form from JS-01 (D-17)"
    - "Module/binding fixtures cover ESM, CommonJS, TypeScript path aliases, namespace/default/named imports, re-exports, destructuring, local aliases, and unresolved dynamic imports (D-18)"
    - "Semantic graph snapshots assert CopyEdge and CallConstraint rows are stable, sorted, and resolvable (D-19)"
    - "Jelly oracle-span coverage is measured after inventory changes and preserves or improves the Phase 42 >=99% standard (D-20)"
    - "Phase 43 determinism gate and Phase 42 public-surface-leak gate remain green; ALLOWED_PRELUDE is not extended (D-19)"
  artifacts:
    - path: "tests/eval-fixtures/semantic-graph/ts_direct_bindings/expected.polint-eval.toml"
      provides: "Semantic graph fixture for TS direct bindings"
      contains: "polint-eval-fixture-1"
    - path: "tests/eval-fixtures/jelly/ts-inventory-spans/expected.polint-eval.toml"
      provides: "Jelly span coverage fixture"
      contains: "polint-eval-fixture-1"
---

<objective>
Close Phase 45 with fixtures and gates: JS-01 inventory span coverage, JS-02 scope/module binding coverage, JS-03 semantic graph constraints, determinism inheritance, cache invalidation proof, and public API leak proof.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-js-ts-inventory-scope-bindings-module-graph-direct-calls/45-CONTEXT.md
@.planning/phases/45-js-ts-inventory-scope-bindings-module-graph-direct-calls/45-PATTERNS.md
@.planning/phases/45-js-ts-inventory-scope-bindings-module-graph-direct-calls/45-01-SUMMARY.md
@.planning/phases/45-js-ts-inventory-scope-bindings-module-graph-direct-calls/45-02-SUMMARY.md
@.planning/phases/45-js-ts-inventory-scope-bindings-module-graph-direct-calls/45-03-SUMMARY.md
@.planning/phases/45-js-ts-inventory-scope-bindings-module-graph-direct-calls/45-04-SUMMARY.md
@.planning/phases/42-benchmark-identity-renderers-dedup-identity-taxonomy/42-CONTEXT.md
@.planning/phases/43-reachability-roots-per-suite-scoring-mode/43-CONTEXT.md
@.planning/phases/44-semantic-graph-skeleton-constraint-vocabulary/44-CONTEXT.md

<interfaces>
- `tests/eval-fixtures/semantic-graph/` is the fixture precedent.
- `crates/polint/src/eval/determinism_gate.rs` auto-enrolls providers through manifests.
- `crates/polint/tests/public_surface_leak.rs` must stay green and should not extend `ALLOWED_PRELUDE`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fixture coverage for inventory and Jelly spans</name>
  <read_first>
    - crates/polint/src/eval/external/jelly_callgraph.rs
    - crates/polint/src/analysis/identity/render/jelly_span.rs
    - crates/polint/src/ts/inventory/tests.rs
    - tests/eval-fixtures/identity/
  </read_first>
  <action>
    Add a fixture suite covering all JS-01 function/callsite forms: declarations, expressions, arrows, methods, constructors, accessors, class static blocks, calls, `new`, tagged templates, optional calls, dynamic import, and `require`. Include multi-line and nested cases to prove spans. Wire the fixture into the existing eval/snapshot path or add a focused test that reports Jelly oracle-span coverage for the fixture. The measured ratio must be >= 0.99 and unmatched spans must list file/span/reason.
  </action>
  <verify>
    <automated>cargo test -p polint jelly ts_inventory_spans</automated>
  </verify>
  <acceptance_criteria>
    - Fixture files exist under `tests/eval-fixtures/jelly/ts-inventory-spans/`.
    - Test output or snapshot includes coverage ratio >= 0.99.
    - Every JS-01 form has a named fixture assertion.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add module/binding semantic graph fixture</name>
  <read_first>
    - crates/polint/src/eval/semantic_graph_snapshot.rs
    - tests/eval-fixtures/semantic-graph/ts_graph/
    - crates/polint/src/analysis/semantic_graph/debug.rs
    - crates/polint/src/ts/binding/tests.rs
  </read_first>
  <action>
    Add `tests/eval-fixtures/semantic-graph/ts_direct_bindings/` with ESM named/default/namespace imports, re-exports, CommonJS require member, TypeScript path alias, destructuring, local aliases, and one unresolved dynamic import. Snapshot expected output must assert `CopyEdge` and `CallConstraint` rows are sorted by stable key and their endpoints resolve to graph nodes.
  </action>
  <verify>
    <automated>cargo test -p polint semantic_graph ts_direct_bindings</automated>
  </verify>
  <acceptance_criteria>
    - `tests/eval-fixtures/semantic-graph/ts_direct_bindings/expected.polint-eval.toml` contains `schema_version = "polint-eval-fixture-1"`.
    - Snapshot asserts at least one `CopyEdge` and one `CallConstraint`.
    - Fixture includes at least one unresolved non-string dynamic import with named reason.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add cache and determinism regression coverage</name>
  <read_first>
    - crates/polint/src/eval/determinism_gate.rs
    - crates/polint/src/analysis/semantic_graph/cache_key.rs
    - crates/polint/src/analysis_kernel/incremental/keys.rs
    - tests/eval-fixtures/determinism/
  </read_first>
  <action>
    Add or extend tests proving semantic graph / TS binding cache identity changes when tsconfig path aliases, package/workspace/lockfile inputs, source syntax, scope/binding output, or module graph output changes, and preserves hits for no-op changes. Confirm the Phase 43 determinism gate remains green with Phase 45 outputs enrolled.
  </action>
  <verify>
    <automated>cargo test -p polint --lib determinism_gate analysis::semantic_graph::cache_key analysis_kernel::incremental::keys</automated>
  </verify>
  <acceptance_criteria>
    - Must-invalidate tests cover tsconfig alias, package/workspace/lockfile, source syntax, and module graph output changes.
    - Must-preserve-hit test covers a no-op input change.
    - `cargo test -p polint --lib determinism_gate` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Prove public surface and full phase regression</name>
  <read_first>
    - crates/polint/tests/public_surface_leak.rs
    - crates/polint/src/sdk/mod.rs
    - crates/polint/src/sdk/facts.rs
    - .github/workflows/ci.yml
  </read_first>
  <action>
    Run the public-surface-leak gate and confirm no Phase 45 inventory/scope/binding/constraint types are reachable through `polint::sdk::prelude::*`. Do not extend `ALLOWED_PRELUDE`. If CI workflow changes are needed to keep the gate running on both Linux and macOS, update only the relevant check wiring without broad workflow churn.
  </action>
  <verify>
    <automated>cargo test -p polint --test public_surface_leak && cargo test -p polint --lib ts::inventory ts::scope ts::binding analysis::semantic_graph && cargo test -p polint semantic_graph jelly</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p polint --test public_surface_leak` exits 0.
    - `git diff -- crates/polint/tests/public_surface_leak.rs` shows no `ALLOWED_PRELUDE` growth.
    - Focused inventory/scope/binding/semantic_graph tests exit 0.
    - If `.github/workflows/ci.yml` is modified, the diff only keeps required Phase 42 leak checks active and does not rename public checks.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| private v1.3 facts -> public SDK | New facts must stay invisible to rule authors until a later explicit public promotion phase. |
| benchmark fixture -> product logic | Fixture labels must verify product semantics, not drive hard-coded benchmark behavior. |
| cache inputs -> reused semantic graph output | Missing digest inputs can reuse stale direct binding constraints. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-05-01 | Information Disclosure | Public SDK leak | mitigate | Run `public_surface_leak` unmodified; do not extend `ALLOWED_PRELUDE`. |
| T-45-05-02 | Tampering | Benchmark-specific hardcoding | mitigate | Fixtures assert general AST/module/binding behavior; no expected labels are used as rule logic. |
| T-45-05-03 | Tampering | Cache invalidation gaps | mitigate | Must-invalidate and must-preserve-hit tests for tsconfig/package/source/module inputs. |
| T-45-05-04 | Repudiation | Span mismatch diagnosis | mitigate | Jelly coverage report lists unmatched spans with file/span/reason. |
</threat_model>

<verification>
- `cargo test -p polint --lib ts::inventory ts::scope ts::binding analysis::semantic_graph` exits 0.
- `cargo test -p polint semantic_graph jelly` exits 0.
- `cargo test -p polint --lib determinism_gate` exits 0.
- `cargo test -p polint --test public_surface_leak` exits 0 with no `ALLOWED_PRELUDE` growth.
- `cargo test -p polint` exits 0 if runtime permits; otherwise record focused regression plus reason full run was skipped.
</verification>

<success_criteria>
- JS-01/JS-02/JS-03 are fully covered by fixture-backed tests.
- Jelly span coverage is measured and remains at least 99% for the covered fixture set.
- Determinism and public-boundary gates remain green.
</success_criteria>

<output>
Create `.planning/phases/45-js-ts-inventory-scope-bindings-module-graph-direct-calls/45-05-SUMMARY.md` when done.
</output>
