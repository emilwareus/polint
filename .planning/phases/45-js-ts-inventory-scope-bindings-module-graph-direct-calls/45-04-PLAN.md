---
phase: 45-js-ts-inventory-scope-bindings-module-graph-direct-calls
plan: 04
type: execute
wave: 4
depends_on: [45-01, 45-02, 45-03]
files_modified:
  - crates/polint/src/analysis/semantic_graph/build.rs
  - crates/polint/src/analysis/semantic_graph/provider.rs
  - crates/polint/src/analysis/semantic_graph/cache_key.rs
  - crates/polint/src/analysis/semantic_graph/validate.rs
  - crates/polint/src/analysis/semantic_graph/debug.rs
  - crates/polint/src/analysis/calls/facts.rs
  - crates/polint/src/analysis/calls/provider.rs
  - crates/polint/src/analysis/calls/store.rs
  - crates/polint/src/analysis_kernel/provider.rs
  - crates/polint/src/analysis_kernel/mod.rs
autonomous: true
requirements: [JS-03]

must_haves:
  truths:
    - "Static JS/TS aliases and bindings emit semantic graph CopyEdge constraints (D-13)"
    - "Direct JS/TS call obligations emit CallConstraint constraints anchored at callsite nodes (D-13)"
    - "Constraints reference existing semantic graph node IDs or stable fact identities and do not duplicate function/callsite/module/package payloads (D-14)"
    - "If direct call target facts are emitted, existing v1.2 calls/refined-calls contracts are preserved and algorithm/source labels are honest direct JS/TS binding labels (D-15)"
    - "No solver-derived edges are emitted; token/property/prototype/this-required cases remain unresolved/unsupported (D-16)"
  artifacts:
    - path: "crates/polint/src/analysis/semantic_graph/build.rs"
      provides: "Projection from TS direct bindings into CopyEdge and CallConstraint"
      contains: "project_ts_direct_bindings"
    - path: "crates/polint/src/analysis/semantic_graph/cache_key.rs"
      provides: "Phase 45 TS binding digest participation"
      contains: "ts_binding"
    - path: "crates/polint/src/analysis/semantic_graph/debug.rs"
      provides: "Test/debug JSON for TS constraints"
      contains: "ts_direct_binding"
---

<objective>
Wire Phase 45 direct JS/TS binding output into the Phase 44 semantic graph. This plan projects static aliases and direct calls into `CopyEdge` and `CallConstraint` rows while preserving the no-solver boundary.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-js-ts-inventory-scope-bindings-module-graph-direct-calls/45-CONTEXT.md
@.planning/phases/45-js-ts-inventory-scope-bindings-module-graph-direct-calls/45-PATTERNS.md
@.planning/phases/45-js-ts-inventory-scope-bindings-module-graph-direct-calls/45-01-SUMMARY.md
@.planning/phases/45-js-ts-inventory-scope-bindings-module-graph-direct-calls/45-02-SUMMARY.md
@.planning/phases/45-js-ts-inventory-scope-bindings-module-graph-direct-calls/45-03-SUMMARY.md
@.planning/phases/44-semantic-graph-skeleton-constraint-vocabulary/44-CONTEXT.md

<interfaces>
- `analysis::semantic_graph::constraints::ConstraintKind::{CopyEdge, CallConstraint}` is the target vocabulary.
- `analysis::semantic_graph::build::build_semantic_graph` is the existing projection path.
- `ts::binding` provides direct binding facts and unresolved reasons.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add semantic graph projection for TS direct bindings</name>
  <read_first>
    - crates/polint/src/analysis/semantic_graph/build.rs
    - crates/polint/src/analysis/semantic_graph/constraints.rs
    - crates/polint/src/analysis/semantic_graph/facts.rs
    - crates/polint/src/ts/binding/facts.rs
    - crates/polint/src/ts/inventory/facts.rs
  </read_first>
  <action>
    Extend `build_semantic_graph` with a `project_ts_direct_bindings` step that reads resolved TS direct binding facts and emits `CopyEdge` constraints for static aliases and `CallConstraint` rows for direct call obligations. Resolve endpoints through existing semantic graph node lookup or stable fact identities; do not copy function/callsite/module payloads into constraints. Add unit tests with one local alias and one imported alias asserting `CopyEdge` plus `CallConstraint` rows.
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis::semantic_graph::build::ts_direct_bindings</automated>
  </verify>
  <acceptance_criteria>
    - A local alias fixture emits at least one `CopyEdge` and one `CallConstraint`.
    - An imported alias fixture emits at least one `CopyEdge` and one `CallConstraint`.
    - Constraint stable keys are built from stable identities, not dense run-local IDs.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve calls/refined-calls contract if direct target facts are added</name>
  <read_first>
    - crates/polint/src/analysis/calls/facts.rs
    - crates/polint/src/analysis/calls/provider.rs
    - crates/polint/src/analysis/refined_calls/facts.rs
    - crates/polint/src/ts/binding/facts.rs
  </read_first>
  <action>
    Decide whether direct TS binding should also project into existing `CallTargetFact` rows. If yes, add only contract-preserving rows with an honest algorithm/source label for direct JS/TS binding and keep unresolved dynamic cases as unresolved facts. If no, add a code comment and test proving Phase 45 emits semantic graph constraints without changing the calls fact contract. In both cases, no public whole-program call graph view is introduced.
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis::calls analysis::semantic_graph</automated>
  </verify>
  <acceptance_criteria>
    - Existing calls/refined-calls tests still pass.
    - If a new direct target algorithm/source label is added, tests assert it appears only for direct TS binding.
    - If no direct target facts are added, a source assertion documents semantic-graph-only projection for Phase 45.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Thread TS binding output into semantic graph cache/debug/validation</name>
  <read_first>
    - crates/polint/src/analysis/semantic_graph/provider.rs
    - crates/polint/src/analysis/semantic_graph/cache_key.rs
    - crates/polint/src/analysis/semantic_graph/validate.rs
    - crates/polint/src/analysis/semantic_graph/debug.rs
    - crates/polint/src/ts/binding/store.rs
  </read_first>
  <action>
    Update semantic graph provider/cache key/debug/validation to include TS binding output as a consumed input. The cache key must digest TS syntax, inventory/scope/binding, module graph, config/lifecycle, tsconfig/jsconfig, package/workspace/lockfile, and provider/schema inputs as available. Validation must reject constraints whose referenced TS binding endpoints cannot resolve to graph nodes or known stable fact identities. Debug/test JSON should include enough TS direct-binding fields to snapshot constraint rows deterministically.
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis::semantic_graph::cache_key analysis::semantic_graph::validate analysis::semantic_graph::debug</automated>
  </verify>
  <acceptance_criteria>
    - Semantic graph cache key code or tests mention/digest TS binding output.
    - Validation test rejects a dangling TS direct-binding constraint endpoint.
    - Debug JSON test shows TS direct binding constraint rows sorted by stable key.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Preserve no-solver boundary in semantic graph projection</name>
  <read_first>
    - crates/polint/src/analysis/semantic_graph/build.rs
    - crates/polint/src/ts/binding/facts.rs
    - .planning/phases/45-js-ts-inventory-scope-bindings-module-graph-direct-calls/45-CONTEXT.md
  </read_first>
  <action>
    Add regression tests proving token-flow-required, property-flow-required, prototype-model-required, and this-model-required unresolved binding facts do not emit solver-derived graph edges or guessed call constraints. These rows may appear as unresolved/debug rows, but they must not create resolved direct target edges.
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis::semantic_graph::no_solver_boundary</automated>
  </verify>
  <acceptance_criteria>
    - Tests assert unresolved token/property/prototype/this cases emit zero resolved target edges.
    - `analysis::solver` is not referenced from Phase 45 code.
    - No fixed-point worklist or token-set propagation type is introduced.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TS direct binding -> semantic graph constraints | Only static direct bindings are projected into graph constraints. |
| semantic graph cache -> reused constraints | Cache input omissions could reuse stale constraints across tsconfig/package changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-04-01 | Tampering | Constraint endpoint resolution | mitigate | Validate every TS direct-binding endpoint resolves to a graph node or known stable fact identity. |
| T-45-04-02 | Tampering | Solver boundary | mitigate | Tests assert token/property/prototype/this unresolved cases do not emit resolved target edges. |
| T-45-04-03 | Information Disclosure | Semantic graph/debug rows | mitigate | Keep debug/test helpers crate-private and verify public leak gate in Plan 05. |
| T-45-04-04 | Tampering | Cache-key omissions | mitigate | Digest TS binding/module/config/lifecycle inputs and add invalidation tests in Plan 05. |
</threat_model>

<verification>
- `cargo test -p polint --lib analysis::semantic_graph` exits 0.
- `cargo test -p polint --lib analysis::calls analysis::refined_calls` exits 0 if call target projection was touched.
- Source assertion: `rg "analysis::solver|VecDeque|TokenSet" crates/polint/src/ts crates/polint/src/analysis/semantic_graph` shows no Phase 47/49 solver implementation introduced.
</verification>

<success_criteria>
- JS-03 direct binding facts emit `CopyEdge` and `CallConstraint` constraints into the semantic graph.
- Constraints compose existing identities and stay deterministic/private.
- Phase 45 does not emit solver-derived edges.
</success_criteria>

<output>
Create `.planning/phases/45-js-ts-inventory-scope-bindings-module-graph-direct-calls/45-04-SUMMARY.md` when done.
</output>

