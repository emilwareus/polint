# Phase 61-03 Plan: Closeout and Regression

## Goal

Close Phase 61 with docs/test traceability and focused verification.

## Tasks

1. Mark VAL-01 and VAL-02 complete in requirements and roadmap/state.
2. Add Phase 61 summaries and verification notes.
3. Run focused docs, external SDK, generated template, public leak, clippy, and
   rustdoc checks.
4. Keep Phase 62 responsible for the final milestone-wide promotion gate and
   full closeout.

## Verification

- `cargo test -p polint --test cli phase61_policy --locked`
- `cargo test -p polint --test cli new_rule_policy_template --locked`
- `cargo test -p polint --test public_surface_leak --locked`
- `cargo fmt --all --check`
- `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings`
- `cargo doc -p polint --no-deps --locked`

