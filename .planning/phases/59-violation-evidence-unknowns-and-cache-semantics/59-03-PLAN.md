# Phase 59-03 Plan: Cache, Determinism, Docs, and Closeout

## Goal

Prove the normalized policy evidence and unknown behavior is deterministic,
cache-safe, documented, and ready for Phase 60 templates.

## Tasks

1. Fold the preview policy-query schema/version into analysis-plan digest
   material when policy capabilities are requested.
2. Add tests proving query digests change when relevant query options change
   and remain stable for identical query objects.
3. Add repeated-run or shuffled-input tests proving policy query result ordering
   is deterministic.
4. Update `docs/facts/evidence.md`, policy fact docs, capability docs, and
   generated skill text with normalized policy evidence and cache semantics.
5. Update `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`,
   `.planning/STATE.md`, plan summaries, and final phase verification.
6. Run focused checks, public leak gate, formatting, clippy, docs, and targeted
   CLI regressions before committing Phase 59 locally.

## Verification

- `cargo test -p polint --lib analysis_plan --locked`
- `cargo test -p polint --lib policy_query --locked`
- `cargo test -p polint --test cli phase5 --locked`
- `cargo test -p polint --test public_surface_leak --locked`
- `cargo fmt --all --check`
- `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings`
- `cargo doc -p polint --no-deps --locked`

