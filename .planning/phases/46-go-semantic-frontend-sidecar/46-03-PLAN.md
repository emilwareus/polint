---
phase: 46-go-semantic-frontend-sidecar
plan: 03
type: execute
wave: 3
depends_on: [46-01, 46-02]
files_modified:
  - crates/polint/src/go/semantic/facts.rs
  - crates/polint/src/go/semantic/store.rs
  - crates/polint/src/go/semantic/lower.rs
  - crates/polint/src/go/semantic/validate.rs
  - crates/polint/src/go/semantic/cache_key.rs
  - crates/polint/src/core/mod.rs
  - crates/polint/src/analysis/semantic_graph/build.rs
  - crates/polint/src/analysis/semantic_graph/provider.rs
  - crates/polint/src/analysis/semantic_graph/cache_key.rs
  - crates/polint/src/analysis/identity/provider.rs
  - crates/polint/src/analysis/identity/render/go_relstring.rs
  - crates/polint/src/eval/external/go_x_tools_callgraph.rs
autonomous: true
requirements: [GO-02]
requirements_addressed: [GO-02]

must_haves:
  truths:
    - "Go semantic rows lower into private stable-keyed facts and compose existing semantic graph node/constraint identities rather than duplicating payloads (D-16, D-17)"
    - "Go callsites emit `CallConstraint` obligations and direct/static call evidence only when proven; interface/RTA expansion remains unresolved for Phase 48 (D-18)"
    - "Type, receiver, and method-set evidence is represented with the narrowest private fact or existing `TypeConstraint` vocabulary that is honest today (D-19)"
    - "Sidecar file paths are validated against discovered in-repository Go files before facts are accepted (D-20)"
    - "Full Go module import-path qualification feeds identity/RelString behavior for the Phase 42 deferral (D-14)"
    - "Lowered stable keys are length-prefixed/labeled from official Go identities plus spans, never run-local insertion order or dense IDs (D-13)"
    - "`go::lifecycle::GoAnalysisConfig` remains the authoritative lifecycle input for sidecar requests and lowering; no hidden side files or one-off lifecycle flags are introduced (D-21)"
  artifacts:
    - path: "crates/polint/src/go/semantic/facts.rs"
      provides: "private Go semantic fact families and statuses"
      contains: "GoSemanticFunctionFact"
    - path: "crates/polint/src/go/semantic/lower.rs"
      provides: "sidecar row to semantic graph/fact lowering"
      contains: "lower_go_semantic"
    - path: "crates/polint/src/analysis/semantic_graph/build.rs"
      provides: "projection from Go semantic facts into graph constraints"
      contains: "project_go_semantic"
---

<objective>
Lower accepted Go semantic sidecar rows into private Rust fact families and the Phase 44 semantic graph for GO-02. This plan maps package/function/method/callsite/type evidence to stable graph nodes and constraints, completes Go import-path identity deferrals, and preserves the no-RTA-solver boundary.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-go-semantic-frontend-sidecar/46-CONTEXT.md
@.planning/phases/46-go-semantic-frontend-sidecar/46-PATTERNS.md
@.planning/phases/46-go-semantic-frontend-sidecar/46-01-SUMMARY.md
@.planning/phases/46-go-semantic-frontend-sidecar/46-02-SUMMARY.md
@.planning/phases/44-semantic-graph-skeleton-constraint-vocabulary/44-CONTEXT.md
@.planning/phases/42-benchmark-identity-renderers-dedup-identity-taxonomy/42-CONTEXT.md

<interfaces>
- Target graph vocabulary: `analysis::semantic_graph::facts` and `analysis::semantic_graph::constraints`.
- Existing graph projection analog: `analysis::semantic_graph::build::project_ts_direct_bindings`.
- Identity deferral comments: `analysis::identity::provider::package_or_module_for_record` and `eval::external::go_x_tools_callgraph::go_rta_oracle_identity`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add private Go semantic fact model, store, and validation</name>
  <read_first>
    - crates/polint/src/go/semantic/protocol.rs
    - crates/polint/src/analysis/semantic_graph/facts.rs
    - crates/polint/src/analysis/semantic_graph/constraints.rs
    - crates/polint/src/ts/binding/facts.rs
    - crates/polint/src/core/mod.rs
  </read_first>
  <action>
    Add `go/semantic/facts.rs`, `store.rs`, and `validate.rs` with private facts for packages, functions, methods, receiver types, init functions, method sets, callsites, type facts, candidates, and package errors. Each fact must carry stable_key, source file/span where applicable, status/precision/provenance or an equivalent private vocabulary, and official Go identity fields from the protocol. Add `AnalysisDb` replace/accessor methods for the normalized output if the facts are stored in DB. Validation must reject duplicate stable keys, dangling file references, and accepted facts with repo-escaping file paths.
  </action>
  <verify>
    <automated>cargo test -p polint --lib go::semantic::facts go::semantic::store go::semantic::validate</automated>
  </verify>
  <acceptance_criteria>
    - `facts.rs` defines package, function, callsite, type, method-set, and package-error fact structs or documented equivalents.
    - `store.rs` normalizes by stable_key before dense ID assignment.
    - Validation tests reject duplicate stable keys and repo-escaping paths.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lower protocol rows to private facts with path and lifecycle checks</name>
  <read_first>
    - crates/polint/src/go/semantic/protocol.rs
    - crates/polint/src/go/semantic/client.rs
    - crates/polint/src/go/semantic/facts.rs
    - crates/polint/src/go/lifecycle.rs
    - crates/polint/src/symbol_graph/go.rs
  </read_first>
  <action>
    Add `go/semantic/lower.rs` with `lower_go_semantic` or equivalent. It should convert decoded sidecar rows into normalized private facts, validate every sidecar file path against discovered Go source files, attach lifecycle inputs from `GoAnalysisConfig`, preserve package-load error rows, and return diagnostics for invalid rows. Use typed errors and `Result`; do not panic on malformed sidecar content.
  </action>
  <verify>
    <automated>cargo test -p polint --lib go::semantic::lower</automated>
  </verify>
  <acceptance_criteria>
    - A lowering test accepts an in-repository path and rejects an absolute repo-escaping path.
    - A package-load error row is preserved as a private package error fact.
    - No production `unwrap()` or `expect()` calls are added in `go/semantic/lower.rs`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Project Go semantic facts into semantic graph constraints</name>
  <read_first>
    - crates/polint/src/analysis/semantic_graph/build.rs
    - crates/polint/src/analysis/semantic_graph/constraints.rs
    - crates/polint/src/analysis/semantic_graph/provider.rs
    - crates/polint/src/go/semantic/facts.rs
    - crates/polint/src/go/semantic/store.rs
  </read_first>
  <action>
    Extend semantic graph building with a `project_go_semantic` step. Project Go packages/functions/callsites/types into existing graph node/constraint vocabulary. Emit `CallConstraint` for callsite obligations and direct/static call evidence when the sidecar proves a target. Emit type/receiver/method-set evidence through `TypeConstraint` or private Go semantic fact references. Interface dispatch, dynamic dispatch, and RTA candidate expansion must stay unresolved/candidate-only for Phase 48 and must not create solver-derived `EdgeKind::Call` rows.
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis::semantic_graph::build::go_semantic</automated>
  </verify>
  <acceptance_criteria>
    - A direct Go call fixture emits a `CallConstraint` anchored at a callsite node.
    - An interface call fixture emits unresolved/candidate facts but zero solver-derived resolved call edges.
    - Constraint stable keys are built from stable identities, not dense run-local IDs.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Complete Go import-path identity and RTA oracle deferrals</name>
  <read_first>
    - crates/polint/src/analysis/identity/provider.rs
    - crates/polint/src/analysis/identity/render/go_relstring.rs
    - crates/polint/src/eval/external/go_x_tools_callgraph.rs
    - crates/polint/src/go/semantic/facts.rs
  </read_first>
  <action>
    Update Go identity construction so `package_or_module` can use semantic sidecar module/package import path facts when available, while preserving existing fallback to package-clause name and file path when semantic setup is missing. Update Go RelString tests and the Go x/tools RTA oracle path only where import-path identity improves benchmark matching without regressing existing bare-name fixtures. Remove or update Phase 46 deferral comments that are now fulfilled.
  </action>
  <verify>
    <automated>cargo test -p polint --lib analysis::identity::render::go_relstring eval::external::go_x_tools_callgraph</automated>
  </verify>
  <acceptance_criteria>
    - A test asserts semantic package import path appears in Go RelString output when sidecar package path is available.
    - A fallback test asserts missing sidecar facts preserve package-clause-name behavior.
    - `rg "deferred to Phase 46|Phase 46 deliverable" crates/polint/src/analysis/identity crates/polint/src/eval/external/go_x_tools_callgraph.rs` shows no stale fulfilled deferral comments.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validated protocol rows -> AnalysisDb | Only schema- and path-validated rows become private facts. |
| Go semantic facts -> semantic graph | Lowering must not invent solver-derived edges or duplicate identity payloads. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-03-01 | Tampering | Path mapping | mitigate | Validate every sidecar path against discovered in-repository Go files before storage. |
| T-46-03-02 | Tampering | Constraint emission | mitigate | Emit only proven direct/static constraints; keep dynamic/interface cases unresolved. |
| T-46-03-03 | Spoofing | Go import-path identity | mitigate | Prefer official package/module path facts and preserve fallback behavior when unavailable. |
| T-46-03-04 | Information Disclosure | Private graph facts | mitigate | Keep all Go semantic facts crate-private and verify leak gate in Plan 04. |
</threat_model>

<verification>
- `cargo test -p polint --lib go::semantic analysis::semantic_graph::build::go_semantic` exits 0.
- `cargo test -p polint --lib analysis::identity::render::go_relstring eval::external::go_x_tools_callgraph` exits 0.
- `rg "pub use.*go::semantic|sdk::prelude.*GoSemantic" crates/polint/src` finds no public SDK exposure.
</verification>

<success_criteria>
- GO-02 lowering exists, is deterministic/private, and writes Go semantic facts into the semantic graph vocabulary.
- Full Go package import-path identity is available when semantic facts are present.
- Phase 46 still emits no Go RTA solver-derived edges.
</success_criteria>

<output>
Create `.planning/phases/46-go-semantic-frontend-sidecar/46-03-SUMMARY.md` when done.
</output>
