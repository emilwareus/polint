---
phase: 46-go-semantic-frontend-sidecar
plan: 04
type: execute
wave: 4
depends_on: [46-01, 46-02, 46-03]
files_modified:
  - crates/polint/src/go/semantic/cache_key.rs
  - crates/polint/src/go/semantic/diagnostics.rs
  - crates/polint/src/go/semantic/debug.rs
  - crates/polint/src/analysis/semantic_graph/cache_key.rs
  - crates/polint/src/analysis/semantic_graph/debug.rs
  - crates/polint/src/analysis_kernel/provider.rs
  - crates/polint/src/analysis_kernel/mod.rs
  - crates/polint/tests/public_surface_leak.rs
  - docs/CONSUMER-SETUP.md
  - docs/facts/symbols-and-references.md
  - tests/eval-fixtures/semantic-graph/go_semantic/expected.polint-eval.toml
  - tests/eval-fixtures/semantic-graph/go_semantic/repo/.polint.toml
  - tests/eval-fixtures/semantic-graph/go_semantic/repo/go.mod
  - tests/eval-fixtures/semantic-graph/go_semantic/repo/main.go
autonomous: true
requirements: [GO-01, GO-02, GO-03, GO-04]
requirements_addressed: [GO-01, GO-02, GO-03, GO-04]

must_haves:
  truths:
    - "Sidecar schema/provider version, sidecar binary/source digest, Go toolchain version, x/tools version, lifecycle inputs, and upstream digests participate in cache identity (D-22, GO-04)"
    - "`GoPackagesLoadFailed`, `GoVersionUnsupported`, and `GoSidecarTimeout` are preserved as distinct private categories/diagnostics (D-23, GO-04)"
    - "Rules do not run with placeholder semantic facts when Go semantic setup is missing; capability/setup diagnostics are emitted instead (D-25)"
    - "Lifecycle fixtures cover single-module, nested-module, multi-module, checked-in go.work, synthetic go.work, build tags, include_tests, and package-load failure (D-26)"
    - "Process/protocol/cache/determinism/public-surface fixtures close the phase (D-27 through D-30)"
    - "Package-load errors are row-level when possible, allowing trustworthy packages to emit facts while reserving whole-run failure for protocol/toolchain/process failures (D-24)"
    - "Cache regression tests cover sidecar digest, Go toolchain version, lifecycle settings, and x/tools version with both must-invalidate and must-preserve-hit cases (D-29)"
  artifacts:
    - path: "crates/polint/src/go/semantic/cache_key.rs"
      provides: "Go semantic cache-key parameter and lifecycle/toolchain digest helpers"
      contains: "go_semantic_provider_parameter_digest"
    - path: "crates/polint/src/go/semantic/diagnostics.rs"
      provides: "distinct GO-04 failure categories"
      contains: "GoSidecarTimeout"
    - path: "tests/eval-fixtures/semantic-graph/go_semantic/expected.polint-eval.toml"
      provides: "Go semantic graph fixture manifest"
      contains: "polint-eval-fixture-1"
---

<objective>
Close Phase 46 by threading cache inputs and GO-04 failure taxonomy through the Go semantic frontend, adding lifecycle/protocol/graph fixtures, preserving setup-safe behavior, and proving determinism plus public API containment.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-go-semantic-frontend-sidecar/46-CONTEXT.md
@.planning/phases/46-go-semantic-frontend-sidecar/46-PATTERNS.md
@.planning/phases/46-go-semantic-frontend-sidecar/46-01-SUMMARY.md
@.planning/phases/46-go-semantic-frontend-sidecar/46-02-SUMMARY.md
@.planning/phases/46-go-semantic-frontend-sidecar/46-03-SUMMARY.md
@.planning/phases/43-reachability-roots-per-suite-scoring-mode/43-CONTEXT.md
@.planning/phases/44-semantic-graph-skeleton-constraint-vocabulary/44-CONTEXT.md

<interfaces>
- Cache analogs: `analysis::semantic_graph::cache_key`, `analysis::reachability::cache_key`.
- Failure analogs: `symbol_graph::go::GoSidecarFailure` and capability/setup diagnostics.
- Gates: `crates/polint/src/eval/determinism_gate.rs` and `crates/polint/tests/public_surface_leak.rs`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add cache key participation for sidecar, toolchain, and lifecycle inputs</name>
  <read_first>
    - crates/polint/src/go/semantic/cache_key.rs
    - crates/polint/src/analysis/semantic_graph/cache_key.rs
    - crates/polint/src/analysis/semantic_graph/provider.rs
    - crates/polint/src/go/lifecycle.rs
    - crates/polint/src/analysis/reachability/cache_key.rs
  </read_first>
  <action>
    Add `go/semantic/cache_key.rs` with schema/provider parameter digest helpers and stable component parts for sidecar binary/source digest, sidecar schema, Go toolchain version, x/tools version, module roots, package patterns, build tags, include_tests, offline mode, and relevant upstream provider digests. Thread this digest into the semantic graph provider cache key or the dedicated Go semantic provider cache key, whichever matches the provider DAG. Add positive must-invalidate tests for sidecar digest, Go version, x/tools version, build tags, and include_tests, plus a negative must-preserve-hit test for unrelated config.
  </action>
  <verify>
    <automated>cargo test -p polint --lib go::semantic::cache_key analysis::semantic_graph::cache_key</automated>
  </verify>
  <acceptance_criteria>
    - Cache tests prove sidecar digest, Go version, x/tools version, build tags, and include_tests change the digest.
    - A negative cache test proves unrelated config does not change the Go semantic digest.
    - `analysis::semantic_graph::cache_key` or provider code includes the Go semantic output digest as an input.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve distinct GO-04 failure categories and setup behavior</name>
  <read_first>
    - crates/polint/src/go/semantic/client.rs
    - crates/polint/src/go/semantic/lower.rs
    - crates/polint/src/symbol_graph/go.rs
    - crates/polint/src/core/mod.rs
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    Add `go/semantic/diagnostics.rs` or equivalent with distinct private categories for `GoPackagesLoadFailed`, `GoVersionUnsupported`, and `GoSidecarTimeout`. Map package-load rows to `GoPackagesLoadFailed`, unsupported Go/toolchain checks to `GoVersionUnsupported`, and client timeout paths to `GoSidecarTimeout`. Ensure setup-missing or unsupported Go semantic capability prevents rules from receiving placeholder semantic facts. Existing diagnostics may wrap these categories for user-facing output, but the distinct category string must remain stored for Phase 52.
  </action>
  <verify>
    <automated>cargo test -p polint --lib go::semantic::diagnostics go::semantic::client go::semantic::lower</automated>
  </verify>
  <acceptance_criteria>
    - Tests assert exact category strings `GoPackagesLoadFailed`, `GoVersionUnsupported`, and `GoSidecarTimeout`.
    - A setup-missing test asserts zero placeholder Go semantic facts are stored.
    - Timeout path maps to `GoSidecarTimeout`, not a generic command failure.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add lifecycle and semantic graph fixture coverage</name>
  <read_first>
    - tests/eval-fixtures/semantic-graph/go_graph/expected.polint-eval.toml
    - tests/eval-fixtures/determinism/go_reachable/expected.polint-eval.toml
    - crates/polint/src/go/lifecycle.rs
    - crates/polint/src/analysis/semantic_graph/debug.rs
    - crates/polint/src/go/semantic/debug.rs
  </read_first>
  <action>
    Add fixture or temp-repo tests covering single-module, nested-module, multi-module, checked-in `go.work`, synthetic `go.work`, build tags, include_tests false/true, and package-load failures. Add `tests/eval-fixtures/semantic-graph/go_semantic/` with `.polint.toml`, `go.mod`, `main.go`, and `expected.polint-eval.toml` schema `polint-eval-fixture-1`. The fixture must exercise function, method, receiver, init, direct call, interface call unresolved/candidate, type fact, and package/module path identity output. Add debug/snapshot helpers as needed to assert stable sorted graph/fact JSON.
  </action>
  <verify>
    <automated>cargo test -p polint go_semantic --lib && cargo test -p polint semantic_graph_go_semantic</automated>
  </verify>
  <acceptance_criteria>
    - The Go semantic fixture manifest exists and contains `schema_version = "polint-eval-fixture-1"`.
    - Lifecycle tests cover checked-in `go.work` and synthetic `go.work`.
    - Snapshot/debug tests assert sorted stable keys and at least one Go `CallConstraint`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Run inherited gates and update user-facing docs only where behavior changed</name>
  <read_first>
    - crates/polint/src/eval/determinism_gate.rs
    - crates/polint/tests/public_surface_leak.rs
    - docs/CONSUMER-SETUP.md
    - docs/facts/symbols-and-references.md
    - .planning/phases/46-go-semantic-frontend-sidecar/46-CONTEXT.md
  </read_first>
  <action>
    Keep `public_surface_leak.rs` green without extending public SDK allow-lists for Go semantic types. Run or update determinism tests so the Go semantic provider/facts are enrolled through provider manifests. Update `docs/CONSUMER-SETUP.md` only if the visible Go setup story changes, such as a new co-shipped sidecar binary or Go minimum. Update `docs/facts/symbols-and-references.md` only to clarify that the existing `polint-go-symbols` sidecar remains separate from the private `polint-go-frontend` semantic sidecar; do not advertise private semantic graph facts as public SDK facts.
  </action>
  <verify>
    <automated>cargo test -p polint --lib determinism_gate && cargo test -p polint --test public_surface_leak && cargo test -p polint --all-targets</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p polint --lib determinism_gate` exits 0.
    - `cargo test -p polint --test public_surface_leak` exits 0 and `ALLOWED_PRELUDE` is not extended for Go semantic types.
    - Docs do not claim public access to private Go semantic graph facts.
    - `cargo test -p polint --all-targets` exits 0 or any skipped test has a setup-missing reason recorded in the summary.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cache identity -> reused graph facts | Missing sidecar/toolchain/lifecycle inputs could reuse stale semantic facts. |
| private semantic categories -> future public unknowns CLI | GO-04 categories must be preserved before Phase 52 exposes consolidated unknowns. |
| private analysis -> public SDK | New semantic types must not leak into rule-author imports. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-04-01 | Tampering | Cache key | mitigate | Digest sidecar, toolchain, x/tools, lifecycle, and upstream provider inputs with must-invalidate tests. |
| T-46-04-02 | Repudiation | Failure taxonomy | mitigate | Store exact GO-04 category strings and assert them in tests. |
| T-46-04-03 | Information Disclosure | Public SDK | mitigate | Keep types private and run public-surface-leak unchanged. |
| T-46-04-04 | Denial of Service | Lifecycle/package loading | mitigate | Fixture package-load failures as diagnostics; no placeholder facts are stored. |
</threat_model>

<verification>
- `cargo test -p polint --lib go::semantic analysis::semantic_graph determinism_gate` exits 0.
- `cargo test -p polint --test public_surface_leak` exits 0.
- `cargo test -p polint --all-targets` exits 0, or the summary records any environment setup gap explicitly.
- `rg "GoPackagesLoadFailed|GoVersionUnsupported|GoSidecarTimeout" crates/polint/src/go/semantic` finds tests and mapping code.
</verification>

<success_criteria>
- GO-04 taxonomy and cache requirements are implemented and tested.
- Lifecycle, protocol, semantic graph, determinism, and public-surface gates close the phase.
- User-facing docs remain truthful and do not promote private Go semantic facts.
</success_criteria>

<output>
Create `.planning/phases/46-go-semantic-frontend-sidecar/46-04-SUMMARY.md` when done.
</output>
