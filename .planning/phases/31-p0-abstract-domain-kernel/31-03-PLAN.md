---
phase: 31-p0-abstract-domain-kernel
plan: 03
title: Domain Provider Storage Cache Debug Validation
type: execute
wave: 3
depends_on: ["31-02"]
files_modified:
  - crates/polint/src/analysis/ids.rs
  - crates/polint/src/analysis/domains/mod.rs
  - crates/polint/src/analysis/domains/facts.rs
  - crates/polint/src/analysis/domains/store.rs
  - crates/polint/src/analysis/domains/provider.rs
  - crates/polint/src/analysis/domains/cache_key.rs
  - crates/polint/src/analysis/domains/validate.rs
  - crates/polint/src/core/mod.rs
  - crates/polint/src/analysis_kernel/metadata.rs
  - crates/polint/src/analysis_kernel/provider.rs
  - crates/polint/src/analysis_kernel/mod.rs
  - crates/polint/src/analysis_kernel/incremental/keys.rs
  - crates/polint/src/analysis_kernel/validation.rs
  - crates/polint/src/analysis_kernel/debug.rs
  - crates/polint/src/eval/observed.rs
  - crates/polint/src/eval/fixtures.rs
  - tests/eval-fixtures/kernel/provider-order/expected.polint-eval.toml
autonomous: true
requirements: [SAE-INT-01]
must_haves:
  truths:
    - "Solved domain rows are stored in AnalysisDb with metadata, status, precision, and stable keys."
    - "The private `polint.abstract_domains` provider runs after calls and before metrics with deterministic output identity."
    - "Domain cache identity includes versions, schema, reduction/widening policy, MIR/CFG/calls inputs, config/lifecycle, and absent extension/model/toolchain slots."
    - "Domain validation and debug JSON remain crate-private/test-facing."
  artifacts:
    - path: "crates/polint/src/analysis/domains/facts.rs"
      provides: "Domain fact rows, statuses, precision labels, and top/unknown/budget reasons"
      contains: "pub(crate) struct DomainObservationFact"
    - path: "crates/polint/src/analysis/domains/provider.rs"
      provides: "Private provider that solves local domains and publishes normalized output"
      contains: "derive_abstract_domains_with_cache_stats"
    - path: "crates/polint/src/analysis/domains/cache_key.rs"
      provides: "Provider parameter digest for domain versions and policy inputs"
      contains: "abstract_domains_provider_parameter_digest"
  key_links:
    - from: "crates/polint/src/analysis_kernel/mod.rs"
      to: "crates/polint/src/analysis/domains/provider.rs"
      via: "kernel provider execution after polint.calls and before polint.metrics"
      pattern: "derive_abstract_domains_with_cache_stats"
    - from: "crates/polint/src/analysis_kernel/validation.rs"
      to: "crates/polint/src/analysis/domains/validate.rs"
      via: "metadata validation hook"
      pattern: "validate_abstract_domains"
---

<objective>
Publish local domain results through the existing private provider, metadata, cache-key, validation, and debug patterns.

Purpose: Make the abstract-domain kernel part of the internal analysis engine without promoting SDK/CLI contracts.
Output: Stored domain facts, provider manifest/order wiring, cache identity vocabulary, validation, and safe debug rows.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/31-p0-abstract-domain-kernel/31-CONTEXT.md
@./AGENTS.md
@.agents/skills/rust-best-practices/SKILL.md
@research/abstract-interpretation/RECOMMENDED_IMPLEMENTATION.md
@research/abstract-interpretation/VALIDATION.md
@.planning/phases/31-p0-abstract-domain-kernel/31-01-SUMMARY.md
@.planning/phases/31-p0-abstract-domain-kernel/31-02-SUMMARY.md
@crates/polint/src/analysis/provider.rs
@crates/polint/src/analysis/cfg/provider.rs
@crates/polint/src/analysis/calls/provider.rs
@crates/polint/src/analysis_kernel/provider.rs
@crates/polint/src/analysis_kernel/mod.rs
@crates/polint/src/analysis_kernel/incremental/keys.rs
@crates/polint/src/analysis_kernel/metadata.rs
@crates/polint/src/analysis_kernel/validation.rs
@crates/polint/src/analysis_kernel/debug.rs
@crates/polint/src/core/mod.rs

<interfaces>
Provider manifest order currently ends:
```rust
"polint.semantic_mir",
"polint.cfg",
"polint.calls",
"polint.metrics",
```
Insert `"polint.abstract_domains"` after calls and before metrics.

Provider output metadata helper:
```rust
provider_outputs.push(Self::provider_output_for_with_optional_digest(
    "polint.calls",
    &db,
    polint_calls_cache_stats,
    calls_output_digest,
));
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Store domain observation facts with metadata</name>
  <files>crates/polint/src/analysis/ids.rs, crates/polint/src/analysis/domains/facts.rs, crates/polint/src/analysis/domains/store.rs, crates/polint/src/analysis/domains/mod.rs, crates/polint/src/core/mod.rs, crates/polint/src/analysis_kernel/metadata.rs</files>
  <read_first>
    - crates/polint/src/analysis/domains/results.rs
    - crates/polint/src/analysis/domains/core.rs
    - crates/polint/src/analysis/cfg/facts.rs
    - crates/polint/src/analysis/calls/facts.rs
    - crates/polint/src/core/mod.rs
    - crates/polint/src/analysis_kernel/metadata.rs
  </read_first>
  <behavior>
    - RED: storage tests fail until domain rows normalize deterministically and stale rows are removed on replace.
    - GREEN: every row has producer/layer `polint.abstract_domains`, status, precision, stable key, and compact payload digest metadata.
    - GREEN: top, unknown, unsupported, setup-missing, and budget-exceeded rows survive storage instead of being dropped.
  </behavior>
  <action>Add `AbstractStateId`, `DomainObservationId`, and `DomainEventId` dense run-local handles to `analysis/ids.rs`. In `facts.rs`, define `DomainSlot::{Reachability, Nilness, Truthiness, Constants, Strings, Initializedness}`, `DomainLocation::{FunctionEntry, BlockEntry, BeforeOperation, AfterOperation, BlockExit}`, `DomainValue`, `DomainStatus::{Present, Top, Unknown, Unsupported, SetupMissing, BudgetExceeded}`, `DomainPrecision::{ExactLocal, SetupAware, Conservative, Heuristic, Unknown, Unsupported}`, `DomainObservationFact`, and `DomainEventFact`. In `store.rs`, add `DomainOutput::normalized` and `DomainStore` indexes by body, block, operation, place, slot, status, and stable key. Add `AnalysisDb::replace_abstract_domain_facts`, accessors, and metadata refresh using `POLINT_ABSTRACT_DOMAINS_PROVIDER_ID = "polint.abstract_domains"`. This implements D-03, D-10, D-11, and D-14.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::domains::facts --locked</automated>
    <automated>cargo test -p polint --lib analysis::domains::store --locked</automated>
    <automated>cargo test -p polint --lib abstract_domain_fact_storage --locked</automated>
    <automated>cargo test -p polint --lib abstract_domain_fact_metadata --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "AbstractStateId|DomainObservationId|DomainEventId" crates/polint/src/analysis/ids.rs` returns matches.
    - `rg -n "DomainSlot|DomainStatus|DomainPrecision|DomainObservationFact|DomainEventFact" crates/polint/src/analysis/domains/facts.rs` returns matches.
    - `rg -n "replace_abstract_domain_facts|abstract_domain_observations|abstract_domain_events" crates/polint/src/core/mod.rs` returns matches.
    - Storage and metadata tests pass.
  </acceptance_criteria>
  <done>Domain rows are stored deterministically with internal metadata and explicit status/precision labels.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire provider and cache identity</name>
  <files>crates/polint/src/analysis/domains/provider.rs, crates/polint/src/analysis/domains/cache_key.rs, crates/polint/src/analysis/domains/mod.rs, crates/polint/src/analysis_kernel/provider.rs, crates/polint/src/analysis_kernel/mod.rs, crates/polint/src/analysis_kernel/incremental/keys.rs, crates/polint/src/eval/observed.rs, crates/polint/src/eval/fixtures.rs, tests/eval-fixtures/kernel/provider-order/expected.polint-eval.toml</files>
  <read_first>
    - crates/polint/src/analysis/domains/solver.rs
    - crates/polint/src/analysis/domains/results.rs
    - crates/polint/src/analysis/calls/provider.rs
    - crates/polint/src/analysis_kernel/provider.rs
    - crates/polint/src/analysis_kernel/mod.rs
    - crates/polint/src/analysis_kernel/incremental/keys.rs
    - tests/eval-fixtures/kernel/provider-order/expected.polint-eval.toml
  </read_first>
  <behavior>
    - RED: provider order tests fail until `polint.abstract_domains` appears after `polint.calls` and before `polint.metrics`.
    - GREEN: provider output digest changes when domain version, reduction policy, widening policy, MIR digest, CFG digest, calls digest, config/lifecycle, or absent extension/model/toolchain components change.
    - GREEN: provider records recompute cache stats and publishes deterministic output digest even when no domain observations exist.
  </behavior>
  <action>Add `abstract_domains_provider_parameter_digest()` in `cache_key.rs` covering domain trait versions, slot versions, reduction graph version, max reduction rounds, widening fuel, iteration budget, and schema label per D-15. Add `derive_abstract_domains_with_cache_stats` in `provider.rs`: run `LocalDomainSolver` over the current `AnalysisDb`, convert `DomainResults` into normalized `DomainOutput`, compute a deterministic provider output digest from stable row keys/status/precision/top reasons and input snapshot components, record recompute stats, and call `db.replace_abstract_domain_facts`. Add manifest schema `abstract-domain-facts-1:1`, `LayerKind::AbstractDomains`, and `LayerKey::abstract_domains_layer_key` with semantic MIR, CFG, calls, symbol graph, module topology, syntax, config, lifecycle, absent model/toolchain/extension, and provider parameter digests. Wire `AnalysisKernel::run` after calls and before metrics; metrics dependency list remains syntax-only unless existing code requires otherwise. Update provider-order eval expectations to shift metrics index by one. This implements D-03, D-07, D-13, and D-15.</action>
  <verify>
    <automated>cargo test -p polint --lib provider_order --locked</automated>
    <automated>cargo test -p polint --lib abstract_domains_provider --locked</automated>
    <automated>cargo test -p polint --lib kernel_run_report_abstract_domains_row_carries_output_digest --locked</automated>
    <automated>cargo test -p polint --lib abstract_domains_layer_key --locked</automated>
    <automated>cargo test -p polint --lib abstract_domains_provider_parameters --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "polint\\.abstract_domains|abstract-domain-facts-1|derive_abstract_domains_with_cache_stats" crates/polint/src/analysis_kernel/provider.rs crates/polint/src/analysis_kernel/mod.rs crates/polint/src/analysis/domains/provider.rs` returns matches.
    - `rg -n "AbstractDomains|abstract_domains_layer_key|abstract_domains_provider_parameter_digest" crates/polint/src/analysis_kernel/incremental/keys.rs crates/polint/src/analysis/domains/cache_key.rs` returns matches.
    - Provider order fixture shows `polint.abstract_domains` immediately after `polint.calls`.
    - Provider/cache tests pass.
  </acceptance_criteria>
  <done>Private abstract-domain provider participates in kernel order, run reports, and cache identity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Validate and debug domain rows</name>
  <files>crates/polint/src/analysis/domains/validate.rs, crates/polint/src/analysis/domains/mod.rs, crates/polint/src/analysis_kernel/validation.rs, crates/polint/src/analysis_kernel/debug.rs</files>
  <read_first>
    - crates/polint/src/analysis/domains/facts.rs
    - crates/polint/src/analysis/domains/store.rs
    - crates/polint/src/analysis_kernel/validation.rs
    - crates/polint/src/analysis_kernel/debug.rs
    - crates/polint/src/analysis/cfg/validate.rs
    - crates/polint/src/analysis/calls/validate.rs
  </read_first>
  <behavior>
    - RED: validation tests fail until dangling body/block/operation/place/call references and duplicate stable keys produce deterministic `polint/internal` diagnostics.
    - GREEN: exact provider precision from `polint.abstract_domains` is rejected unless mapped as `ExactLocal` payload precision with metadata precision no stronger than setup-aware/conservative.
    - GREEN: debug JSON rows contain relative path, stable key, slot, location, status, precision, top/unknown/budget reason, and counts only.
  </behavior>
  <action>Add `validate_abstract_domains(db, diagnostics)` and call it from `validate_fact_metadata` after calls validation and before generic provider/precision checks. Validate stable-key uniqueness, required metadata, body/block/operation/place/call references, status/reason consistency, budget rows carrying a budget reason, top/unknown rows carrying a `TopReason`, and no `FactPrecision::Exact` metadata for the provider per D-11. Extend `metadata_debug_json_for_test` with an `"abstract_domains"` object containing `observations`, `events`, `counts`, and `index_counts`. Use repo-relative paths via existing debug helpers, stable keys, labels, and compact payload fragments; do not include raw source bodies, AST dumps, absolute paths, or run-local IDs as stable identity per D-14.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis_kernel::validation::abstract_domains --locked</automated>
    <automated>cargo test -p polint --lib analysis_kernel::debug::abstract_domains_debug_json --locked</automated>
    <automated>cargo test -p polint --lib analysis_kernel::validation --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "validate_abstract_domains" crates/polint/src/analysis/domains/validate.rs crates/polint/src/analysis_kernel/validation.rs` returns matches.
    - `rg -n "\"abstract_domains\"|observations|events|index_counts" crates/polint/src/analysis_kernel/debug.rs` returns matches.
    - Debug tests assert no absolute paths, no raw source, no AST labels, and deterministic byte-identical JSON.
    - Validation/debug tests pass.
  </acceptance_criteria>
  <done>Domain facts fail closed under validation and have safe crate-private debug snapshots.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| solver results -> AnalysisDb | Computed facts become shared internal state for later providers. |
| AnalysisDb -> debug JSON | Test-facing debug rows could leak source or machine-local details. |
| input snapshot/cache identity -> provider reuse | Missing digest inputs could cause stale domain facts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-03-01 | T | `DomainStore` | mitigate | Validate dangling references, duplicate stable keys, and status/reason consistency before downstream use. |
| T-31-03-02 | I | `analysis_kernel::debug` | mitigate | Debug JSON uses stable keys, relative paths, labels, counts, and compact payloads only; tests reject raw source, AST dumps, absolute paths, and run-local identity leakage. |
| T-31-03-03 | R | provider output metadata | mitigate | Kernel run report records provider id, schema, output digest, and cache stats for `polint.abstract_domains`. |
| T-31-03-04 | T | layer key | mitigate | Cache identity includes domain versions, schema, reduction/widening policy, semantic MIR/CFG/calls digests, config/lifecycle, and absent model/toolchain/extension slots. |
| T-31-03-05 | E | public boundary | mitigate | Provider remains crate-private with no SDK, CLI, runner, README, or docs/facts changes in this plan. |
</threat_model>

<verification>
Run:
- `cargo test -p polint --lib abstract_domains_provider --locked`
- `cargo test -p polint --lib abstract_domains_layer_key --locked`
- `cargo test -p polint --lib analysis_kernel::validation::abstract_domains --locked`
- `cargo test -p polint --lib analysis_kernel::debug::abstract_domains_debug_json --locked`
- `cargo test -p polint --lib provider_order --locked`
- `cargo fmt --all -- --check`
</verification>

<success_criteria>
Plan 31-03 succeeds when domain facts are stored with metadata, the provider runs in the kernel with deterministic identity, cache key inputs are complete, validation rejects malformed rows, and debug output is safe and private.
</success_criteria>

<output>
After completion, create `.planning/phases/31-p0-abstract-domain-kernel/31-03-SUMMARY.md`.
</output>
