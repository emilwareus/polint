---
phase: 31-p0-abstract-domain-kernel
plan: 02
title: Deterministic Local Solver And Transfers
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - crates/polint/src/analysis/domains/mod.rs
  - crates/polint/src/analysis/domains/solver.rs
  - crates/polint/src/analysis/domains/transfer.rs
  - crates/polint/src/analysis/domains/results.rs
autonomous: true
requirements: [SAE-INT-01]
must_haves:
  truths:
    - "Local domain solving is deterministic over MIR/CFG rows regardless of input row order."
    - "Transfer behavior is separated from lattice operations and remains monotone for P0 slots."
    - "Calls, unsupported semantics, dynamic writes, and budgets produce explicit top/unknown/budget statuses."
  artifacts:
    - path: "crates/polint/src/analysis/domains/solver.rs"
      provides: "Deterministic local worklist solver with iteration and widening fuel"
      contains: "pub(crate) struct LocalDomainSolver"
    - path: "crates/polint/src/analysis/domains/transfer.rs"
      provides: "MIR operation and CFG edge transfer effects for P0 domains"
      contains: "pub(crate) struct TransferCx"
    - path: "crates/polint/src/analysis/domains/results.rs"
      provides: "ResultsCursor-style access to solved location states"
      contains: "pub(crate) struct DomainResults"
  key_links:
    - from: "crates/polint/src/analysis/domains/solver.rs"
      to: "crates/polint/src/analysis/cfg/facts.rs"
      via: "sorted CFG functions, blocks, edges, and operation nodes"
      pattern: "cfg_functions\\(|basic_blocks\\(|cfg_edges\\("
    - from: "crates/polint/src/analysis/domains/transfer.rs"
      to: "crates/polint/src/analysis/mir/op.rs"
      via: "MirOperationKind and MirValue transfer matching"
      pattern: "MirOperationKind::"
---

<objective>
Implement the deterministic local abstract interpreter over private MIR/CFG/calls inputs.

Purpose: Turn the law-tested domain product into concrete local facts while preserving conservative unknown/top behavior.
Output: Solver, transfer logic, and result cursor modules ready for provider publication.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/31-p0-abstract-domain-kernel/31-CONTEXT.md
@./AGENTS.md
@.agents/skills/rust-best-practices/SKILL.md
@research/abstract-interpretation/RECOMMENDED_IMPLEMENTATION.md
@research/abstract-interpretation/VALIDATION.md
@research/abstract-interpretation/implementation/MIR-CONTRACT.md
@.planning/phases/31-p0-abstract-domain-kernel/31-01-SUMMARY.md
@crates/polint/src/analysis/domains/lattice.rs
@crates/polint/src/analysis/domains/core.rs
@crates/polint/src/analysis/domains/state.rs
@crates/polint/src/analysis/mir/body.rs
@crates/polint/src/analysis/mir/op.rs
@crates/polint/src/analysis/cfg/facts.rs
@crates/polint/src/analysis/cfg/graph.rs
@crates/polint/src/analysis/calls/facts.rs
@crates/polint/src/core/mod.rs

<interfaces>
MIR operation inputs:
```rust
pub(crate) enum MirOperationKind {
    StorageLive { place: PlaceId },
    Bind { place: PlaceId, value: MirValue },
    Assign { place: PlaceId, value: MirValue, mode: AssignMode },
    Read { place: PlaceId },
    Write { place: PlaceId, value: MirValue },
    Branch { predicate: MirPredicateId },
    Call { site: CallSiteId, callee: MirValue, arguments: Vec<PlaceId>, return_place: PlaceId },
    Return { value: Option<MirValue> },
    Unsupported { unsupported: UnsupportedId },
}
```

CFG edge kinds include true/false, loop back, call/throw/panic-like conservative edges:
```rust
pub(crate) enum CfgEdgeKind { Normal, True, False, LoopBack, Return, Throw, Panic, Unknown, Synthetic, ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic worklist solver</name>
  <files>crates/polint/src/analysis/domains/solver.rs, crates/polint/src/analysis/domains/results.rs, crates/polint/src/analysis/domains/mod.rs</files>
  <read_first>
    - crates/polint/src/analysis/domains/state.rs
    - crates/polint/src/analysis/cfg/facts.rs
    - crates/polint/src/analysis/cfg/graph.rs
    - crates/polint/src/core/mod.rs
    - research/abstract-interpretation/RECOMMENDED_IMPLEMENTATION.md
  </read_first>
  <behavior>
    - RED: solver tests fail until block order, operation order, and worklist tie-breaking are stable.
    - GREEN: repeated solves over shuffled-but-equivalent input rows produce byte-identical result digests.
    - GREEN: loop-back edges consume widening fuel and then mark affected states with `TopReason::Widened` or `TopReason::BudgetExceeded`.
  </behavior>
  <action>Add `LocalDomainSolver`, `SolverInput`, `SolverPolicy`, `SolverBudget`, `SolverResult`, `BlockState`, and `DomainResults` per D-07 and D-10. The solver consumes borrowed `AnalysisDb` rows: `mir_bodies`, `mir_operations`, `cfg_functions`, `basic_blocks`, `cfg_edges`, and call rows. Build per-function solve units by sorted body stable key, CFG function stable key, reverse-postorder, block stable key, operation ordinal, and operation stable key. Use a deterministic queue backed by sorted keys, not hash map iteration. Implement bounded iteration and widening fuel; budget exhaustion must mark the function result status as budget-exceeded/top rather than silently returning partial certainty. `DomainResults` must provide cursor-style access by body, block, operation, and place without exposing mutable state.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::domains::solver --locked</automated>
    <automated>cargo test -p polint --lib analysis::domains::results --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "struct LocalDomainSolver|struct SolverPolicy|struct DomainResults|BTree" crates/polint/src/analysis/domains/solver.rs crates/polint/src/analysis/domains/results.rs` returns matches.
    - Tests named with `deterministic`, `shuffled`, `widening`, and `budget` exist.
    - No solver code matches `HashMap|HashSet` unless wrapped by a deterministic sort before observation.
    - Focused solver/results tests pass.
  </acceptance_criteria>
  <done>Solver produces deterministic local results and explicit budget/top states.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement MIR and edge transfer effects</name>
  <files>crates/polint/src/analysis/domains/transfer.rs, crates/polint/src/analysis/domains/solver.rs</files>
  <read_first>
    - crates/polint/src/analysis/domains/core.rs
    - crates/polint/src/analysis/domains/state.rs
    - crates/polint/src/analysis/mir/op.rs
    - crates/polint/src/analysis/cfg/facts.rs
    - crates/polint/src/analysis/calls/facts.rs
    - research/abstract-interpretation/implementation/MIR-CONTRACT.md
  </read_first>
  <behavior>
    - RED: transfer monotonicity samples fail until each supported operation preserves the product-state order.
    - GREEN: literal assignments update constants, nilness/nullishness, truthiness, strings, and initializedness.
    - GREEN: unknown calls, unresolved calls, unsupported rows, dynamic writes, and unknown writes havoc affected slots to explicit top/unknown reasons.
    - GREEN: branch true/false edges apply explicit assumptions for reachability, nilness/nullishness, truthiness, and constant bools when MIR evidence supports it.
  </behavior>
  <action>Add `TransferCx`, `OperationTransfer`, `EdgeTransfer`, `BranchSense`, `TransferReason`, and `HavocTarget` in `transfer.rs` per D-02, D-08, D-09, D-10, and D-11. Match only polint-owned `MirOperationKind`, `MirValue`, `CfgEdgeKind`, and call facts; do not read parser AST nodes, raw source snippets, tree-sitter nodes, Oxc nodes, or language-tool object graphs. Implement StorageLive/Bind/Assign/Read/Write/Branch/Call/Return/Unsupported transfers. Calls must be conservative: direct call facts may keep argument initializedness and mark return_place unknown; unresolved/unsupported/setup-missing/budgeted calls must top/havoc the return place and affected places. Do not apply summaries, summary SCCs, extension models, or interprocedural effects.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::domains::transfer --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "struct TransferCx|enum BranchSense|enum HavocTarget|MirOperationKind::Call|UnresolvedCallReason|TopReason::UnresolvedCall" crates/polint/src/analysis/domains/transfer.rs` returns matches.
    - Tests named with `monotone`, `literal_assignment`, `branch_assumption`, `unresolved_call`, `unsupported`, and `dynamic_write` exist.
    - `rg -n "tree_sitter|oxc::|source_text|raw_source|parse" crates/polint/src/analysis/domains/transfer.rs` returns no matches.
    - Focused transfer tests pass.
  </acceptance_criteria>
  <done>Transfer logic handles P0 local semantics conservatively and monotonicity tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire transfer into result cursor semantics</name>
  <files>crates/polint/src/analysis/domains/solver.rs, crates/polint/src/analysis/domains/results.rs</files>
  <read_first>
    - crates/polint/src/analysis/domains/solver.rs
    - crates/polint/src/analysis/domains/transfer.rs
    - crates/polint/src/analysis/domains/results.rs
    - crates/polint/src/analysis/cfg/facts.rs
    - crates/polint/src/analysis/mir/body.rs
  </read_first>
  <behavior>
    - RED: cursor tests fail until entry, before-operation, after-operation, and block-exit states are queryable.
    - GREEN: unreachable blocks expose reachability `Unreachable` and do not fabricate value facts.
    - GREEN: solving the same function twice with identical inputs returns identical stable result rows and digests.
  </behavior>
  <action>Complete solver-result materialization per D-07 and D-14. `DomainResults` must expose deterministic iterators for functions, blocks, operations, place observations, and unknown/top events using stable keys rather than run-local IDs as persistent identity. Add cursor methods: `entry_state(body)`, `block_entry(block)`, `before_operation(operation)`, `after_operation(operation)`, and `block_exit(block)`. Ensure unreachable blocks keep `ReachabilityDomain::Unreachable` while other slots remain bottom/absent unless a conservative top reason is explicit. Keep all APIs crate-private.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::domains --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "entry_state|block_entry|before_operation|after_operation|block_exit" crates/polint/src/analysis/domains/results.rs` returns matches.
    - Result iteration uses stable keys in sort keys; acceptance grep for `stable_key` in `results.rs` returns matches.
    - Aggregate `analysis::domains` tests pass.
  </acceptance_criteria>
  <done>Domain solver produces cursor-queryable deterministic local results over MIR/CFG.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MIR/CFG/calls -> domain transfer | Private analysis facts derived from untrusted source code affect domain precision and unknown states. |
| solver budgets -> output state | Resource limits can change analysis precision and must be explicit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-02-01 | T | `transfer.rs` | mitigate | Transfer monotonicity tests cover all P0 slots for literal assignment, branch assumptions, calls, unsupported operations, and dynamic writes. |
| T-31-02-02 | D | `solver.rs` | mitigate | Solver has explicit iteration and widening fuel limits with budget-exceeded/top result status. |
| T-31-02-03 | I | `results.rs` | mitigate | Result rows expose stable keys and compact labels only; no raw source, AST dumps, or absolute paths are produced. |
| T-31-02-04 | S | call transfer | accept | Phase 31 does not authenticate external callees; unresolved/setup-missing calls are conservatively top/unknown and summaries are not applied. |
</threat_model>

<verification>
Run:
- `cargo test -p polint --lib analysis::domains --locked`
- `cargo fmt --all -- --check`
- `rg -n "tree_sitter|oxc::|raw_source|source_text" crates/polint/src/analysis/domains || true`
</verification>

<success_criteria>
Plan 31-02 succeeds when deterministic solving, monotone transfer samples, explicit top/unknown/budget behavior, and cursor-style local results are implemented without parser AST or public API dependencies.
</success_criteria>

<output>
After completion, create `.planning/phases/31-p0-abstract-domain-kernel/31-02-SUMMARY.md`.
</output>
