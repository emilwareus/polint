---
phase: 31-p0-abstract-domain-kernel
plan: 04
title: Domain Validation Debug And Provider Order Proof
type: execute
wave: 4
depends_on: ["31-03"]
files_modified:
  - crates/polint/src/analysis/domains/validate.rs
  - crates/polint/src/analysis/domains/mod.rs
  - crates/polint/src/analysis_kernel/validation.rs
  - crates/polint/src/analysis_kernel/debug.rs
  - crates/polint/src/eval/observed.rs
  - crates/polint/src/eval/fixtures.rs
  - tests/eval-fixtures/kernel/provider-order/expected.polint-eval.toml
autonomous: true
requirements: [SAE-INT-01]
must_haves:
  truths:
    - "Malformed abstract-domain rows fail closed during metadata validation with deterministic internal diagnostics."
    - "Domain debug JSON remains crate-private/test-facing and exposes compact stable evidence only."
    - "Provider-order proof shows `polint.abstract_domains` after `polint.calls` and before `polint.metrics`."
  artifacts:
    - path: "crates/polint/src/analysis/domains/validate.rs"
      provides: "Abstract-domain row validation for references, metadata, status, precision, and reasons"
      contains: "validate_abstract_domains"
    - path: "crates/polint/src/analysis_kernel/debug.rs"
      provides: "Crate-private debug JSON rows for abstract-domain observations/events/counts"
      contains: "\"abstract_domains\""
    - path: "tests/eval-fixtures/kernel/provider-order/expected.polint-eval.toml"
      provides: "Provider-order fixture proof with abstract domains between calls and metrics"
      contains: "polint.abstract_domains"
  key_links:
    - from: "crates/polint/src/analysis_kernel/validation.rs"
      to: "crates/polint/src/analysis/domains/validate.rs"
      via: "metadata validation hook after calls validation"
      pattern: "validate_abstract_domains"
    - from: "crates/polint/src/eval/observed.rs"
      to: "crates/polint/src/analysis_kernel/debug.rs"
      via: "provider-order eval observation"
      pattern: "polint.abstract_domains"
---

<objective>
Prove the new abstract-domain provider is validated, debuggable, and ordered correctly inside the private kernel.

Purpose: Close the validation/debug/provider-order proof after Plan 31-03 creates the provider, storage, and cache identity.
Output: Validation hook, safe debug JSON, and updated internal provider-order fixture proof.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/31-p0-abstract-domain-kernel/31-CONTEXT.md
@./AGENTS.md
@.agents/skills/rust-best-practices/SKILL.md
@research/abstract-interpretation/VALIDATION.md
@.planning/phases/31-p0-abstract-domain-kernel/31-03-SUMMARY.md
@crates/polint/src/analysis/domains/facts.rs
@crates/polint/src/analysis/domains/store.rs
@crates/polint/src/analysis/domains/provider.rs
@crates/polint/src/analysis_kernel/validation.rs
@crates/polint/src/analysis_kernel/debug.rs
@crates/polint/src/eval/observed.rs
@crates/polint/src/eval/fixtures.rs
@tests/eval-fixtures/kernel/provider-order/expected.polint-eval.toml

<interfaces>
Plan 31-03 creates:
```rust
pub(crate) const POLINT_ABSTRACT_DOMAINS_PROVIDER_ID: &str = "polint.abstract_domains";
pub(crate) fn derive_abstract_domains_with_cache_stats(...)
```

Existing validator pattern:
```rust
validate_cfg(db, diagnostics);
validate_calls(db, diagnostics);
validate_fact_metadata(db, diagnostics);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Validate and debug domain rows</name>
  <files>crates/polint/src/analysis/domains/validate.rs, crates/polint/src/analysis/domains/mod.rs, crates/polint/src/analysis_kernel/validation.rs, crates/polint/src/analysis_kernel/debug.rs</files>
  <read_first>
    - crates/polint/src/analysis/domains/facts.rs
    - crates/polint/src/analysis/domains/store.rs
    - crates/polint/src/analysis_kernel/validation.rs
    - crates/polint/src/analysis_kernel/debug.rs
    - crates/polint/src/analysis/cfg/validate.rs
    - crates/polint/src/analysis/calls/validate.rs
  </read_first>
  <behavior>
    - RED: validation tests fail until dangling body/block/operation/place/call references and duplicate stable keys produce deterministic `polint/internal` diagnostics.
    - GREEN: exact provider precision from `polint.abstract_domains` is rejected unless mapped as `ExactLocal` payload precision with metadata precision no stronger than setup-aware/conservative.
    - GREEN: debug JSON rows contain relative path, stable key, slot, location, status, precision, top/unknown/budget reason, and counts only.
  </behavior>
  <action>Add `validate_abstract_domains(db, diagnostics)` and call it from `validate_fact_metadata` after calls validation and before generic provider/precision checks. Validate stable-key uniqueness, required metadata, body/block/operation/place/call references, status/reason consistency, budget rows carrying a budget reason, top/unknown rows carrying a `TopReason`, and no `FactPrecision::Exact` metadata for the provider per D-11. Extend `metadata_debug_json_for_test` with an `"abstract_domains"` object containing `observations`, `events`, `counts`, and `index_counts`. Use repo-relative paths via existing debug helpers, stable keys, labels, and compact payload fragments; do not include raw source bodies, AST dumps, absolute paths, or run-local IDs as stable identity per D-14.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis_kernel::validation::abstract_domains --locked</automated>
    <automated>cargo test -p polint --lib analysis_kernel::debug::abstract_domains_debug_json --locked</automated>
    <automated>cargo test -p polint --lib analysis_kernel::validation --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "validate_abstract_domains" crates/polint/src/analysis/domains/validate.rs crates/polint/src/analysis_kernel/validation.rs` returns matches.
    - `rg -n "\"abstract_domains\"|observations|events|index_counts" crates/polint/src/analysis_kernel/debug.rs` returns matches.
    - Debug tests assert no absolute paths, no raw source, no AST labels, and deterministic byte-identical JSON.
    - Validation/debug tests pass.
  </acceptance_criteria>
  <done>Domain facts fail closed under validation and have safe crate-private debug snapshots.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove abstract-domain provider order</name>
  <files>crates/polint/src/eval/observed.rs, crates/polint/src/eval/fixtures.rs, tests/eval-fixtures/kernel/provider-order/expected.polint-eval.toml</files>
  <read_first>
    - crates/polint/src/analysis_kernel/provider.rs
    - crates/polint/src/analysis_kernel/mod.rs
    - crates/polint/src/eval/observed.rs
    - crates/polint/src/eval/fixtures.rs
    - tests/eval-fixtures/kernel/provider-order/expected.polint-eval.toml
  </read_first>
  <behavior>
    - RED: provider-order fixture fails until `polint.abstract_domains` appears after `polint.calls` and before `polint.metrics`.
    - GREEN: provider-order eval observation includes `polint.abstract_domains` with schema `abstract-domain-facts-1:1`.
    - GREEN: metrics index is shifted by exactly one relative to the previous fixture because the domain provider was inserted before metrics.
  </behavior>
  <action>Update the internal provider-order eval observation/fixture path to account for `polint.abstract_domains` from Plan 31-03. Keep this proof internal/test-facing per D-01 and D-03: do not add CLI flags, SDK views, runner APIs, README text, or docs/facts pages. Update `expected.polint-eval.toml` so the provider sequence shows source/syntax/module/symbol/MIR/CFG/calls, then `polint.abstract_domains`, then metrics, and preserve existing provider-order invariants. If eval observation needs provider schema/status fragments for the new provider, add only compact provider id/schema/status/count fields.</action>
  <verify>
    <automated>cargo test -p polint --lib provider_order --locked</automated>
    <automated>cargo test -p polint --lib eval::fixtures::provider_order --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "polint\\.abstract_domains" tests/eval-fixtures/kernel/provider-order/expected.polint-eval.toml` returns matches.
    - Provider order fixture shows `polint.abstract_domains` immediately after `polint.calls` and before `polint.metrics`.
    - Provider-order tests pass.
  </acceptance_criteria>
  <done>Internal provider-order fixture proves abstract domains run in the intended kernel position.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AnalysisDb -> validation | Computed rows and metadata must be rejected if references, stable keys, status, precision, or reasons are malformed. |
| AnalysisDb -> debug JSON | Test-facing debug rows could leak source or machine-local details. |
| provider manifest -> eval fixture | Internal provider ordering could drift and invalidate downstream cache/debug assumptions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-04-01 | T | `validate_abstract_domains` | mitigate | Validate dangling references, duplicate stable keys, status/reason consistency, and metadata precision before downstream use. |
| T-31-04-02 | I | `analysis_kernel::debug` | mitigate | Debug JSON uses stable keys, relative paths, labels, counts, and compact payloads only; tests reject raw source, AST dumps, absolute paths, and run-local identity leakage. |
| T-31-04-03 | R | provider-order fixture | mitigate | Eval fixture records deterministic provider id/schema/order evidence for `polint.abstract_domains` between calls and metrics. |
| T-31-04-04 | E | public boundary | mitigate | Validation, debug, and provider-order proof remain crate-private/test-facing with no SDK, CLI, runner, README, or docs/facts changes in this plan. |
</threat_model>

<verification>
Run:
- `cargo test -p polint --lib analysis_kernel::validation::abstract_domains --locked`
- `cargo test -p polint --lib analysis_kernel::debug::abstract_domains_debug_json --locked`
- `cargo test -p polint --lib provider_order --locked`
- `cargo fmt --all -- --check`
</verification>

<success_criteria>
Plan 31-04 succeeds when validation rejects malformed domain rows, debug output is safe and private, and provider-order proof shows `polint.abstract_domains` between calls and metrics.
</success_criteria>

<output>
After completion, create `.planning/phases/31-p0-abstract-domain-kernel/31-04-SUMMARY.md`.
</output>
