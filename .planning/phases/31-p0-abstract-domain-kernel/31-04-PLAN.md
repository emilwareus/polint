---
phase: 31-p0-abstract-domain-kernel
plan: 04
title: Domain Eval Fixtures And Public Boundary Proof
type: execute
wave: 4
depends_on: ["31-03"]
files_modified:
  - crates/polint/src/eval/model.rs
  - crates/polint/src/eval/observed.rs
  - crates/polint/src/eval/fixtures.rs
  - crates/polint/src/eval/mod.rs
  - crates/polint/tests/cli.rs
  - tests/eval-fixtures/abstract-domains/core/expected.polint-eval.toml
  - tests/eval-fixtures/abstract-domains/core/repo/.polint.toml
  - tests/eval-fixtures/abstract-domains/core/repo/go.mod
  - tests/eval-fixtures/abstract-domains/core/repo/domain.go
  - tests/eval-fixtures/abstract-domains/core/repo/web/package.json
  - tests/eval-fixtures/abstract-domains/core/repo/web/src/domain.ts
autonomous: true
requirements: [SAE-INT-01]
must_haves:
  truths:
    - "Internal eval can observe abstract-domain facts, top states, unknown states, and budget states deterministically."
    - "Native Go and TS/JS fixtures exercise reachability, nilness/nullishness, truthiness, constants, strings, initializedness, unsupported, top, and budget rows."
    - "No abstract-domain facts leak through public CLI JSON, inspect/test output, SDK, runner, README, or docs/facts."
  artifacts:
    - path: "tests/eval-fixtures/abstract-domains/core/expected.polint-eval.toml"
      provides: "Expected internal abstract-domain fact rows and invariants"
      contains: "abstract_domains.current_determinism"
    - path: "crates/polint/tests/cli.rs"
      provides: "Public no-leak integration proof for private abstract-domain internals"
      contains: "abstract_domain_internals_stay_private"
    - path: "crates/polint/src/eval/observed.rs"
      provides: "Debug JSON to eval fact normalization for abstract-domain rows"
      contains: "abstract_domain_facts_for_test"
  key_links:
    - from: "crates/polint/src/eval/observed.rs"
      to: "crates/polint/src/analysis_kernel/debug.rs"
      via: "metadata_debug_json_for_test()[\"abstract_domains\"]"
      pattern: "abstract_domains"
    - from: "crates/polint/tests/cli.rs"
      to: "public CLI/SDK boundary"
      via: "check, inspect rule, polint test, help, source scans"
      pattern: "abstract_domain_internals_stay_private"
---

<objective>
Prove Phase 31 through internal eval fixtures and public-boundary tests.

Purpose: Validate deterministic domain behavior and privacy before later summary, extension, and public promotion phases build on it.
Output: Eval area, fixture repo, determinism proof, unknown/top/budget assertions, and public no-leak tests.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/31-p0-abstract-domain-kernel/31-CONTEXT.md
@./AGENTS.md
@.agents/skills/rust-best-practices/SKILL.md
@.claude/skills/polint/SKILL.md
@research/abstract-interpretation/VALIDATION.md
@.planning/phases/31-p0-abstract-domain-kernel/31-03-SUMMARY.md
@crates/polint/src/eval/model.rs
@crates/polint/src/eval/observed.rs
@crates/polint/src/eval/fixtures.rs
@crates/polint/src/analysis_kernel/debug.rs
@crates/polint/tests/cli.rs
@tests/eval-fixtures/semantic-mir/core/expected.polint-eval.toml
@tests/eval-fixtures/cfg-core/expected.polint-eval.toml
@tests/eval-fixtures/direct-calls/core/expected.polint-eval.toml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add abstract-domain eval observation</name>
  <files>crates/polint/src/eval/model.rs, crates/polint/src/eval/observed.rs, crates/polint/src/eval/mod.rs</files>
  <read_first>
    - crates/polint/src/eval/model.rs
    - crates/polint/src/eval/observed.rs
    - crates/polint/src/eval/mod.rs
    - crates/polint/src/analysis_kernel/debug.rs
    - crates/polint/src/analysis/domains/facts.rs
  </read_first>
  <behavior>
    - RED: eval model tests fail until `AbstractDomains` fixture area and fact families parse.
    - GREEN: debug rows normalize into compact `ObservedFact` payloads with producer `polint.abstract_domains`.
    - GREEN: top, unknown, unsupported, setup_missing, and budget_exceeded statuses count as unknown-like evidence.
  </behavior>
  <action>Add `ABSTRACT_DOMAIN_FACT_FAMILIES = ["DomainObservation", "DomainEvent"]` and fixture area `AbstractDomains` serialized as `abstract-domains`. Add `abstract_domain_facts_for_test(debug_json)` that reads `metadata_debug_json_for_test()["abstract_domains"]` observations/events/counts/index_counts and emits compact facts/invariants. Payload fragments may include relative path, span, body stable key, block stable key, operation stable key, place stable key, slot, location, value label, status, precision, and reason. Do not include raw source, AST dumps, absolute paths, or dense run-local IDs. Extend unknown-like metrics tests for domain top/unknown/budget rows per D-10, D-13, and D-14.</action>
  <verify>
    <automated>cargo test -p polint --lib eval::abstract_domain_rows --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "ABSTRACT_DOMAIN_FACT_FAMILIES|AbstractDomains|abstract_domain_facts_for_test" crates/polint/src/eval/model.rs crates/polint/src/eval/observed.rs` returns matches.
    - Eval tests assert `top`, `unknown`, `unsupported`, `setup_missing`, and `budget_exceeded` contribute to unknown-like metrics.
    - `rg -n "raw_source|source_text|absolute_path|tree_sitter|oxc::" crates/polint/src/eval/observed.rs` shows no new abstract-domain payload dependency.
    - Focused eval tests pass.
  </acceptance_criteria>
  <done>Internal eval can observe domain rows safely and account for uncertainty honestly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add native abstract-domain fixture</name>
  <files>crates/polint/src/eval/fixtures.rs, tests/eval-fixtures/abstract-domains/core/expected.polint-eval.toml, tests/eval-fixtures/abstract-domains/core/repo/.polint.toml, tests/eval-fixtures/abstract-domains/core/repo/go.mod, tests/eval-fixtures/abstract-domains/core/repo/domain.go, tests/eval-fixtures/abstract-domains/core/repo/web/package.json, tests/eval-fixtures/abstract-domains/core/repo/web/src/domain.ts</files>
  <read_first>
    - crates/polint/src/eval/fixtures.rs
    - tests/eval-fixtures/semantic-mir/core/expected.polint-eval.toml
    - tests/eval-fixtures/cfg-core/expected.polint-eval.toml
    - tests/eval-fixtures/direct-calls/core/expected.polint-eval.toml
    - crates/polint/src/analysis/domains/provider.rs
    - crates/polint/src/analysis/domains/transfer.rs
  </read_first>
  <behavior>
    - RED: fixture runner fails until fixture files and expected domain rows exist.
    - GREEN: fixture covers Go nil, TS null/undefined/nullish, truthy/falsy branches, literal constants, string literals, initializedness before/after assignment, unreachable blocks, unknown calls, unsupported/dynamic writes, and budget behavior.
    - GREEN: cold, warm, and no-cache normalized eval outputs match and emit `abstract_domains.current_determinism`.
  </behavior>
  <action>Create `tests/eval-fixtures/abstract-domains/core` using the existing TOML native fixture format. Add a mixed Go + TS/JS repo that triggers every required P0 slot from SAE-INT-01: reachability, nilness/nullishness, truthiness, constants/literal sets, simple strings, and cheap initializedness. Include constructs that produce top/unknown/budget/unsupported rows: unresolved/function-value call, dynamic property/index write, unsupported MIR/CFG/calls evidence, and a small loop that consumes widening fuel. Add `run_abstract_domains_core_fixture_for_test` in `fixtures.rs` that runs cold, warm, and no-cache using an analysis plan that requests existing supported capabilities needed to execute current providers; compare deterministic normalized JSON before adding `abstract_domains.current_determinism`. Keep this fixture internal/test-facing per D-13.</action>
  <verify>
    <automated>cargo test -p polint --lib eval::fixtures::abstract_domains_core --locked</automated>
    <automated>cargo test -p polint --lib eval::abstract_domain_rows --locked</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/eval-fixtures/abstract-domains/core/expected.polint-eval.toml` succeeds.
    - `rg -n "DomainObservation|DomainEvent|Reachability|Nilness|Truthiness|Constants|Strings|Initializedness|top|unknown|budget|unsupported|abstract_domains.current_determinism" tests/eval-fixtures/abstract-domains/core/expected.polint-eval.toml` returns matches.
    - Fixture source contains Go and TS/JS examples for nil/nullish, branch truthiness, constants, strings, initialization, unknown calls, and loop widening.
    - Fixture tests pass.
  </acceptance_criteria>
  <done>Native eval fixture proves P0 domain coverage, uncertainty rows, and deterministic output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove public no-leak boundary</name>
  <files>crates/polint/tests/cli.rs</files>
  <read_first>
    - crates/polint/tests/cli.rs
    - .claude/skills/polint/SKILL.md
    - ./AGENTS.md
    - crates/polint/src/sdk/mod.rs
    - crates/polint/src/runner/mod.rs
    - crates/polint/src/cli/mod.rs
  </read_first>
  <behavior>
    - RED: public-boundary test fails until it checks public JSON/output and source surfaces for domain markers.
    - GREEN: external temp-repo rule using only supported `polint::sdk::prelude::*` views still runs.
    - GREEN: `polint check --format json`, `polint inspect rule --format json`, `polint test --format json`, CLI help, SDK, runner, CLI source, README, and docs/facts contain no promoted abstract-domain facts or views.
  </behavior>
  <action>Add `abstract_domain_internals_stay_private` in `crates/polint/tests/cli.rs` per D-01 and D-03. Build a mixed Go plus TS/JS temp repo with a local rule importing only `polint::sdk::prelude::*`, registering through `polint::runner::run_cli`, and requesting existing supported public views such as `ResolvedImports<'_>`, `ModuleGraphFacts<'_>`, `Symbols<'_>`, `References<'_>`, and metrics views. Assert public outputs exclude exact private markers: `polint.abstract_domains`, `abstract-domain-facts`, `DomainObservation`, `DomainEvent`, `Nilness<'_>`, `Truthiness<'_>`, `Constants<'_>`, `StringValues<'_>`, `Initializedness<'_>`, `AbstractDomains<'_>`, and `DataFlow<'_>`. Source-scan README, docs/facts, SDK, runner, CLI modules, and the crate-root public section for the same markers. Do not add docs/facts pages or public capability support.</action>
  <verify>
    <automated>cargo test -p polint --test cli abstract_domain_internals_stay_private --locked</automated>
    <automated>cargo test -p polint --test cli direct_calls_internals_stay_private --locked</automated>
    <automated>cargo test -p polint --test cli cfg_public_no_leak --locked</automated>
    <automated>cargo test -p polint --test cli semantic_mir_internals_stay_private --locked</automated>
    <automated>cargo fmt --all -- --check</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "abstract_domain_internals_stay_private" crates/polint/tests/cli.rs` returns one test.
    - Public no-leak test asserts check JSON, inspect JSON, polint test JSON, help text, and source-surface scans.
    - `rg -n "polint\\.abstract_domains|abstract-domain-facts|DomainObservation|DomainEvent|Nilness&lt;'_|Truthiness&lt;'_|Constants&lt;'_|StringValues&lt;'_|Initializedness&lt;'_|AbstractDomains&lt;'_" README.md docs/facts crates/polint/src/sdk crates/polint/src/runner crates/polint/src/cli || true` returns no production/public matches outside test assertions.
    - Public-boundary regression tests pass.
  </acceptance_criteria>
  <done>Abstract-domain internals stay private while existing public rule-author workflows remain compatible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| debug JSON -> eval facts | Test-facing observation converts internal debug rows into fixture facts. |
| fixture repo -> kernel | Native fixtures contain untrusted source and config used by parser/provider paths. |
| private internals -> public outputs | Accidental CLI/SDK/docs promotion could create unsupported public contracts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-04-01 | I | `eval::observed` | mitigate | Eval payloads use compact relative fragments only; tests reject raw source, AST dumps, absolute paths, and dense IDs. |
| T-31-04-02 | T | native fixture manifest | mitigate | Use existing fixture loader path validation and checked-in TOML expected rows; no synthetic observed rows outside extension fixtures. |
| T-31-04-03 | R | eval determinism | mitigate | Cold, warm, and no-cache normalized JSON are compared before emitting `abstract_domains.current_determinism`. |
| T-31-04-04 | E | public SDK/CLI/docs boundary | mitigate | Public no-leak integration test covers check/inspect/test/help outputs and source-surface scans. |
</threat_model>

<verification>
Run:
- `cargo test -p polint --lib eval::fixtures::abstract_domains_core --locked`
- `cargo test -p polint --test cli abstract_domain_internals_stay_private --locked`
- `cargo test -p polint --lib analysis::domains --locked`
- `cargo test -p polint --all-targets --locked`
- `cargo fmt --all -- --check`
</verification>

<success_criteria>
Plan 31-04 succeeds when internal eval proves domain coverage/determinism/top/unknown/budget behavior and public no-leak tests prove no unsupported abstract-domain API or CLI contract has been promoted.
</success_criteria>

<output>
After completion, create `.planning/phases/31-p0-abstract-domain-kernel/31-04-SUMMARY.md`.
</output>
