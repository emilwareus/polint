---
phase: 56-events-and-calls-query-surface
plan: 03
title: Capability Support Docs And External Tests
type: execute
wave: 2
depends_on: [56-01, 56-02]
files_modified:
  - crates/polint/src/analysis_plan.rs
  - crates/polint/src/analysis_kernel/mod.rs
  - crates/polint/src/cli/mod.rs
  - crates/polint/tests/cli.rs
  - docs/facts/events.md
  - docs/facts/calls.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [CALL-01, CALL-02, CALL-03, CALL-04]
must_haves:
  truths:
    - "events and calls must be marked supported only after query methods are backed by real provider facts."
    - "Rules that request only Events<'_> and/or Calls<'_> must execute; rules that request ControlFlow<'_> or DataFlow<'_> must still fail closed."
    - "Public docs must describe limits and heuristic behavior truthfully."
  artifacts:
    - path: "crates/polint/tests/cli.rs"
      provides: "External temp-repo tests for event/call rule authoring"
    - path: "docs/facts/events.md"
      provides: "Public Events docs"
    - path: "docs/facts/calls.md"
      provides: "Public Calls docs"
---

<objective>
Promote the product-facing support boundary for `events` and `calls`, update docs, and verify the external rule-authoring workflow through CLI tests.
</objective>

<execution_context>
@.planning/phases/56-events-and-calls-query-surface/56-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@crates/polint/tests/cli.rs
@docs/facts/events.md
@docs/facts/calls.md
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Promote capability support and pipeline triggers</name>
  <files>crates/polint/src/analysis_plan.rs, crates/polint/src/analysis_kernel/mod.rs, crates/polint/src/cli/mod.rs</files>
  <read_first>
    - crates/polint/src/analysis_plan.rs
    - crates/polint/src/analysis_kernel/mod.rs
    - crates/polint/src/cli/mod.rs
    - crates/polint/src/rule_manifest.rs
  </read_first>
  <action>Mark `events` and `calls` as supported capabilities while leaving `control_flow`, `dataflow`, `cfg`, and `call_graph` unsupported/reserved. Add `events` and `calls` to semantic pipeline trigger capabilities so requesting these views runs the providers they consume. Keep facts-list stability honest as preview/provider-backed, not stable raw graph access.</action>
  <verify>
    <automated>cargo test -p polint --lib policy_preview_capabilities_have_distinct_names --locked</automated>
    <automated>cargo test -p polint --lib policy_capabilities_report_phase_support_boundaries --locked</automated>
    <automated>cargo run -p polint --locked -- facts list --format json</automated>
  </verify>
  <acceptance_criteria>
    - Rules requesting only `events` and/or `calls` are no longer blocked by `polint/capability`.
    - Rules requesting `control_flow` or `dataflow` still fail closed.
    - The semantic pipeline runs for event/call rules so backing facts are present.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add external temp-repo coverage</name>
  <files>crates/polint/tests/cli.rs</files>
  <read_first>
    - crates/polint/tests/cli.rs
    - crates/polint/src/sdk/facts.rs
    - crates/polint/src/sdk/policy.rs
  </read_first>
  <action>Add or update temp-repo tests where generated `.polint/rules` code imports only `polint::sdk::prelude::*`, requests `Events<'_>`/`Calls<'_>`, uses `EventPattern`/`ReachQuery`, and asserts JSON diagnostics from `polint check --format json`. Update Phase 55 fail-closed expectations so only still-unsupported views block execution.</action>
  <verify>
    <automated>cargo test -p polint --test cli phase56_events_and_calls_rule_reports_json --locked</automated>
    <automated>cargo test -p polint --test cli phase55_preview_rule_syntax_compiles_and_fails_closed --locked</automated>
  </verify>
  <acceptance_criteria>
    - External rules use public SDK imports only.
    - CLI output contains user-facing policy diagnostics with status/precision/root/path evidence.
    - Existing reserved capability diagnostics remain correct for later-phase views.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update docs and GSD closeout</name>
  <files>docs/facts/events.md, docs/facts/calls.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - docs/facts/events.md
    - docs/facts/calls.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
  </read_first>
  <action>Document provider-backed Phase 56 behavior, supported patterns, exact/list matching, precision/unknown evidence, current write-event/root limitations, and examples. Mark CALL-01 through CALL-04 complete only after implementation and tests pass, then add summaries and verification artifacts.</action>
  <verify>
    <automated>cargo doc -p polint --no-deps --locked</automated>
    <automated>cargo clippy --workspace --all-targets --all-features --locked -- -D warnings</automated>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - Docs do not claim exact coverage where behavior is heuristic or limited.
    - Phase artifacts list passed verification commands and residual limitations.
    - Phase 56 is complete in ROADMAP/STATE only after verification passes.
  </acceptance_criteria>
</task>
</tasks>

<verification>
- `cargo fmt --all --check`
- `cargo test -p polint --lib --locked`
- `cargo test -p polint --test cli phase56_events_and_calls_rule_reports_json --locked`
- `cargo test -p polint --test cli phase55_preview_rule_syntax_compiles_and_fails_closed --locked`
- `cargo doc -p polint --no-deps --locked`
- `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings`
- `git diff --check`
</verification>

<success_criteria>
Events and calls policy-query capabilities are provider-backed, documented honestly, exercised through external rule-authoring tests, and promoted in planning state while later-phase views remain fail-closed.
</success_criteria>
