---
phase: 56-events-and-calls-query-surface
plan: 02
title: Reachable Call Policy Query
type: execute
wave: 1
depends_on: [56-01]
files_modified:
  - crates/polint/src/sdk/facts.rs
  - crates/polint/src/sdk/policy.rs
autonomous: true
requirements: [CALL-02, CALL-03, CALL-04]
must_haves:
  truths:
    - "Calls<'_>::forbidden_reachable(ReachQuery) must answer policy-level reachability without exposing raw call graph internals."
    - "ReachQuery knobs include roots, target, tests inclusion, max depth, max paths, and minimum precision; unsupported precision or budget cases must be visible."
    - "Existing reachability roots/marks and refined-call precision floors must be preserved."
  artifacts:
    - path: "crates/polint/src/sdk/facts.rs"
      provides: "Private reachable-call query implementation"
---

<objective>
Implement `Calls<'_>::forbidden_reachable(ReachQuery)` over private refined-call and reachability facts, returning deterministic policy violations with root, path, callsite, target, precision, status, and unknown evidence.
</objective>

<execution_context>
@.planning/phases/56-events-and-calls-query-surface/56-CONTEXT.md
@.planning/REQUIREMENTS.md
@crates/polint/src/analysis/reachability/facts.rs
@crates/polint/src/analysis/reachability/traverse.rs
@crates/polint/src/analysis/refined_calls/facts.rs
@crates/polint/src/sdk/facts.rs
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Build private query indexes</name>
  <files>crates/polint/src/sdk/facts.rs</files>
  <read_first>
    - crates/polint/src/sdk/facts.rs
    - crates/polint/src/analysis/reachability/facts.rs
    - crates/polint/src/analysis/refined_calls/store.rs
    - crates/polint/src/core/mod.rs
  </read_first>
  <action>Add small private helper structs/functions in `sdk::facts` or a private submodule to index call sites, refined call edges, and reachability marks by stable key/function. Build deterministic adjacency from refined call edges whose precision/status pass the query floor. Do not expose the helper types publicly.</action>
  <verify>
    <automated>cargo test -p polint --lib calls_forbidden_reachable --locked</automated>
  </verify>
  <acceptance_criteria>
    - The search does not clone large source strings.
    - Traversal order is deterministic.
    - Internal helper types stay private or `pub(crate)` only where needed.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement bounded reachability search</name>
  <files>crates/polint/src/sdk/facts.rs</files>
  <read_first>
    - crates/polint/src/sdk/policy.rs
    - crates/polint/src/analysis/calls/facts.rs
    - crates/polint/src/analysis/refined_calls/facts.rs
    - crates/polint/src/analysis/reachability/facts.rs
  </read_first>
  <action>Replace `Calls::forbidden_reachable` preview panic with a bounded BFS/shortest-path search. Roots default to existing supported reachability roots; explicit `query.roots` filters roots by supported event/root descriptors. Apply `include_tests`, `max_depth`, `max_paths`, and `minimum_precision`. Return one violation per target path up to the result budget, using the target callsite as the diagnostic location.</action>
  <verify>
    <automated>cargo test -p polint --lib calls_forbidden_reachable --locked</automated>
    <automated>cargo check -p polint --locked</automated>
  </verify>
  <acceptance_criteria>
    - A forbidden target reachable from a supported root produces a violation with root/path/callsite/target evidence.
    - Calls reachable only from tests are excluded by default and included when `include_tests = true`.
    - `max_depth` and `max_paths` constrain traversal deterministically and surface budget evidence when they truncate answers.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Preserve unknown and precision semantics</name>
  <files>crates/polint/src/sdk/facts.rs, crates/polint/src/sdk/policy.rs</files>
  <read_first>
    - crates/polint/src/analysis/calls/facts.rs
    - crates/polint/src/analysis/refined_calls/facts.rs
    - crates/polint/src/analysis/unknown_taxonomy/collect.rs
  </read_first>
  <action>Map refined-call statuses and precisions into `PolicyStatus` / `PolicyPrecision` without overstating certainty. Unresolved, ambiguous, unsupported, setup-missing, rejected, and budget-exceeded edges should produce evidence when they affect the query. Do not silently promote conservative or heuristic results to exact.</action>
  <verify>
    <automated>cargo test -p polint --lib policy_query_precision --locked</automated>
  </verify>
  <acceptance_criteria>
    - Precision floors filter or downgrade results consistently.
    - Unknown and budget states are visible in returned diagnostics/evidence.
    - v1.3 refined-call validation and precision floors are not weakened.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat ID | Component | Risk | Mitigation |
|-----------|-----------|------|------------|
| T56-03 | Reachability search | False exactness hides unsupported or unresolved calls | Map statuses conservatively and include unknown evidence. |
| T56-04 | Search budget | Truncated paths look like complete absence | Return or annotate budget-exceeded evidence when limits bind. |
</threat_model>

<verification>
- `cargo test -p polint --lib calls_forbidden_reachable --locked`
- `cargo test -p polint --lib policy_query_precision --locked`
- `cargo check -p polint --locked`
- `git diff --check`
</verification>

<success_criteria>
`Calls<'_>::forbidden_reachable(ReachQuery)` returns stable policy-level violations for forbidden reachable calls, with root/path/callsite/target evidence and honest unknown/budget/precision handling.
</success_criteria>
