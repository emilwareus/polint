---
phase: 56-events-and-calls-query-surface
plan: 01
title: Event Query Projection
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/polint/src/sdk/facts.rs
  - crates/polint/src/sdk/policy.rs
autonomous: true
requirements: [CALL-01, CALL-04]
must_haves:
  truths:
    - "Events<'_>::matching(EventPattern) must return provider-backed matches for supported event kinds without exposing raw AST, MIR, CFG, solver, graph, or AnalysisDb internals."
    - "The Phase 55 public syntax remains the only authoring style: plain pattern/query values and one typed view method."
    - "Unsupported event kinds or missing backing facts must be honest and deterministic, not placeholder success."
  artifacts:
    - path: "crates/polint/src/sdk/facts.rs"
      provides: "Provider-backed Events::matching implementation"
    - path: "crates/polint/src/sdk/policy.rs"
      provides: "Crate-private pattern and violation helpers for SDK query execution"
---

<objective>
Promote `Events<'_>::matching(EventPattern)` from Phase 55 preview panic to a deterministic provider-backed query over existing call facts.
</objective>

<execution_context>
@.planning/phases/56-events-and-calls-query-surface/56-CONTEXT.md
@.planning/phases/55-sdk-query-vocabulary-and-preview-contract/55-CONTEXT.md
@.planning/REQUIREMENTS.md
@AGENTS.md
@.agents/skills/rust-best-practices/SKILL.md
@.agents/skills/polint/SKILL.md
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Add internal pattern and violation helpers</name>
  <files>crates/polint/src/sdk/policy.rs</files>
  <read_first>
    - crates/polint/src/sdk/policy.rs
    - crates/polint/src/diagnostics.rs
  </read_first>
  <action>Add crate-private helpers that let SDK query code inspect `EventPattern` kind/values and construct `PolicyViolation`s without making pattern internals or constructors public. Preserve public fields and existing `PolicyViolation::status`, `precision`, and `diagnostic` behavior. Add public read-only accessors for file/range/evidence only if tests or rule authors need them; otherwise keep construction crate-private.</action>
  <verify>
    <automated>cargo check -p polint --locked</automated>
  </verify>
  <acceptance_criteria>
    - `sdk::facts` can match `EventPattern` without duplicating string parsing logic.
    - External rule crates still cannot construct arbitrary raw-internal `PolicyViolation`s.
    - No public raw analysis IDs or provider names are introduced.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement call event matching</name>
  <files>crates/polint/src/sdk/facts.rs</files>
  <read_first>
    - crates/polint/src/sdk/facts.rs
    - crates/polint/src/analysis/calls/facts.rs
    - crates/polint/src/analysis/refined_calls/facts.rs
    - crates/polint/src/core/mod.rs
  </read_first>
  <action>Replace `Events::matching` preview panic with deterministic matching for `EventPattern::call`. Prefer refined-call edges as the semantic projection, fall back to direct call-site shape only where necessary, and build `PolicyViolation`s with callsite file/range, status, precision, target evidence, callsite evidence, and unknown evidence for unresolved/budget statuses. Keep `EventPattern::write_field` documented as unsupported/no-backed-results if existing facts cannot provide stable write events in this phase.</action>
  <verify>
    <automated>cargo test -p polint --lib events_matching --locked</automated>
    <automated>cargo check -p polint --locked</automated>
  </verify>
  <acceptance_criteria>
    - `Events::matching(EventPattern::call("x"))` returns stable call event violations for matching canonical targets.
    - Results include file/range, precision, status, and evidence suitable for diagnostics.
    - `Events::matching` does not panic for supported inputs and does not expose internal IDs.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
| Threat ID | Component | Risk | Mitigation |
|-----------|-----------|------|------------|
| T56-01 | Event matching | Raw solver/call graph internals leak into public diagnostics | Evidence uses stable public labels and paths, not dense IDs or provider types. |
| T56-02 | Pattern helpers | Private pattern representation becomes accidental public API | Keep helpers `pub(crate)` and avoid public getters unless needed for user code. |
</threat_model>

<verification>
- `cargo test -p polint --lib events_matching --locked`
- `cargo check -p polint --locked`
- `git diff --check`
</verification>

<success_criteria>
`Events<'_>::matching(EventPattern::call(...))` returns deterministic semantic call-event matches backed by existing facts, with honest status/precision/evidence and no public raw internals.
</success_criteria>
