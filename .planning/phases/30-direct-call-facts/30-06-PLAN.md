---
phase: 30-direct-call-facts
plan: 06
type: execute
wave: 5
depends_on: ["30-04"]
files_modified:
  - crates/polint/tests/cli.rs
  - crates/polint/src/analysis_plan.rs
  - crates/polint/src/sdk/facts.rs
autonomous: true
requirements: [SAE-SEM-05]
must_haves:
  truths:
    - "Public `CallGraph<'_>` and `call_graph` capability remain unsupported."
    - "Public CLI JSON/help, inspect/test JSON, SDK, runner, README, and docs do not expose private call internals."
    - "External temp-repo rules using supported SDK views continue to work while call graph requests produce capability diagnostics."
  artifacts:
    - path: "crates/polint/tests/cli.rs"
      provides: "Public no-leak and capability-honesty integration tests"
      contains: "direct_calls_internals_stay_private"
    - path: "crates/polint/src/analysis_plan.rs"
      provides: "Reserved unsupported `call_graph` capability behavior"
      contains: "call_graph"
    - path: "crates/polint/src/sdk/facts.rs"
      provides: "Reserved inert `CallGraph<'_>` view"
      contains: "pub struct CallGraph"
  key_links:
    - from: "crates/polint/tests/cli.rs"
      to: "crates/polint/src/sdk/facts.rs"
      via: "source-surface no-leak scan"
      pattern: "CallGraph<'_>"
    - from: "crates/polint/tests/cli.rs"
      to: "crates/polint/src/analysis_plan.rs"
      via: "capability diagnostic for `call_graph`"
      pattern: "unsupported capability `call_graph`"
---

<objective>
Prove direct-call internals do not become public APIs and `call_graph` remains unsupported.

Purpose: Phase 30 builds private analysis substrate only; public call graph promotion belongs to later gates.
Output: public no-leak integration tests and unsupported capability regression coverage.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-direct-call-facts/30-CONTEXT.md
@AGENTS.md
@docs/API-VISIBILITY-PLAN.md
@crates/polint/tests/cli.rs
@crates/polint/src/sdk/facts.rs
@crates/polint/src/analysis_plan.rs
@crates/polint/src/runner/mod.rs
@README.md
@docs/facts/imports.md
@docs/facts/symbols-and-references.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add public no-leak proof for private direct-call internals</name>
  <files>crates/polint/tests/cli.rs</files>
  <read_first>
    - `.planning/phases/30-direct-call-facts/30-CONTEXT.md`
    - `AGENTS.md`
    - `docs/API-VISIBILITY-PLAN.md`
    - `crates/polint/tests/cli.rs`
    - `crates/polint/src/sdk/facts.rs`
    - `crates/polint/src/runner/mod.rs`
    - `crates/polint/src/cli`
    - `README.md`
    - `docs/facts`
  </read_first>
  <behavior>
    - Test 1: public `polint check --format json`, `polint inspect rule --format json`, and `polint test --format json` outputs exclude private call markers.
    - Test 2: CLI help, SDK source, runner source, CLI source, crate-root public section, README, and `docs/facts` exclude private call provider/schema/family markers.
    - Test 3: an external temp-repo rule using only `polint::sdk::prelude::*`, `ResolvedImports<'_>`, `ModuleGraphFacts<'_>`, `Symbols<'_>`, and `References<'_>` still runs and emits diagnostics.
  </behavior>
  <action>Add `direct_calls_internals_stay_private` to `crates/polint/tests/cli.rs`, modeled after `semantic_mir_internals_stay_private`, `module_topology_internals_stay_private`, and `cfg_public_no_leak`. The temp repo must import only `polint::sdk::prelude::*` and register through `polint::runner::run_cli`; it must not import `polint::analysis`, `polint::core`, `polint::graph`, or any private calls module. Assert exact forbidden public markers are absent from check/inspect/test JSON, help text, SDK/runner/CLI source, README, and `docs/facts`: `polint.calls`, `calls-facts-1`, `CallSiteFact`, `CallTargetFact`, `UnresolvedCallFact`, `analysis::calls`, `direct-call-facts`, `Calls<'_>`, `CallEdges<'_>`, and any supported docs path `docs/facts/calls.md` or `docs/facts/call-graph.md` per D-20 and D-22. Allow the pre-existing reserved `CallGraph<'_>` source type only inside the explicit unsupported-reserved scan.</action>
  <acceptance_criteria>
    - `rg -n "direct_calls_internals_stay_private|polint\\.calls|calls-facts-1|CallSiteFact|CallTargetFact|UnresolvedCallFact|analysis::calls" crates/polint/tests/cli.rs` finds the new test and forbidden marker list.
    - `test ! -f docs/facts/calls.md && test ! -f docs/facts/call-graph.md` passes unless the implementation deliberately added a preview-gated public surface with separate docs/tests; Phase 30 should not.
    - `rg -n "polint::sdk::prelude::\\*|polint::runner::run_cli" crates/polint/tests/cli.rs` finds the external-consumer rule fixture.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p polint --test cli direct_calls_internals_stay_private --locked</automated>
  </verify>
  <done>Private call internals are absent from public outputs/surfaces while supported SDK rule workflows remain compatible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove `call_graph` capability remains unsupported</name>
  <files>crates/polint/tests/cli.rs, crates/polint/src/analysis_plan.rs, crates/polint/src/sdk/facts.rs</files>
  <read_first>
    - `.planning/phases/30-direct-call-facts/30-CONTEXT.md`
    - `crates/polint/tests/cli.rs`
    - `crates/polint/src/analysis_plan.rs`
    - `crates/polint/src/sdk/facts.rs`
  </read_first>
  <behavior>
    - Test 1: a temp-repo rule requesting `CallGraph<'_>` produces a `polint/capability` diagnostic for unsupported `call_graph`.
    - Test 2: the rule requesting `CallGraph<'_>` does not execute with fabricated call facts.
    - Test 3: `analysis_plan::support_for("call_graph")` remains `Unsupported` with docs path `docs/roadmap/00_ROADMAP.md`.
  </behavior>
  <action>Add CLI integration coverage `call_graph_capability_remains_unsupported`, modeled after `cfg_capability_remains_unsupported`. If needed, add focused unit assertions in `analysis_plan.rs` that `support_for("call_graph")` remains unsupported. Do not add methods to `CallGraph<'_>` in `sdk/facts.rs`; keep it an inert reserved view with `_db` only per D-21. Ensure diagnostics are honest and do not imply private direct-call facts are available to rule authors.</action>
  <acceptance_criteria>
    - `rg -n "call_graph_capability_remains_unsupported|unsupported capability `call_graph`|local/needs-call-graph" crates/polint/tests/cli.rs` finds capability test coverage.
    - `rg -n "\"call_graph\".*Unsupported|Capability is reserved for a later phase" crates/polint/src/analysis_plan.rs` confirms unsupported planning.
    - `rg -n "impl<'a> CallGraph|pub fn .*CallGraph|call_sites\\(|outgoing\\(|incoming\\(|targets\\(" crates/polint/src/sdk/facts.rs` returns no public `CallGraph<'_>` query methods.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p polint --test cli call_graph_capability_remains_unsupported --locked</automated>
    <automated>cargo test -p polint --lib analysis_plan::tests::reserved_capabilities_remain_unsupported --locked</automated>
  </verify>
  <done>`CallGraph<'_>` remains a reserved unsupported public view with no fabricated execution path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Run public-boundary regression set and formatting</name>
  <files>crates/polint/tests/cli.rs, crates/polint/src/analysis_plan.rs, crates/polint/src/sdk/facts.rs</files>
  <read_first>
    - `.planning/phases/30-direct-call-facts/30-CONTEXT.md`
    - `crates/polint/tests/cli.rs`
    - `crates/polint/src/analysis_plan.rs`
    - `crates/polint/src/sdk/facts.rs`
  </read_first>
  <behavior>
    - Test 1: existing semantic MIR, module topology, CFG, and new direct-call no-leak tests all pass.
    - Test 2: existing `cfg` unsupported capability and new `call_graph` unsupported capability tests both pass.
    - Test 3: formatting passes after public-boundary test additions.
  </behavior>
  <action>Run the public-boundary regression commands and fix only issues caused by the new Phase 30 public-boundary tests. Do not reword public docs to advertise direct-call facts; remove accidental public call markers if found. Keep any doc adjustments limited to preserving unsupported/private wording and avoid adding `docs/facts/calls.md` or `docs/facts/call-graph.md` per D-22.</action>
  <acceptance_criteria>
    - `cargo test -p polint --test cli direct_calls_internals_stay_private --locked` passes.
    - `cargo test -p polint --test cli call_graph_capability_remains_unsupported --locked` passes.
    - `cargo fmt --all -- --check` passes.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p polint --test cli semantic_mir_internals_stay_private --locked</automated>
    <automated>cargo test -p polint --test cli module_topology_internals_stay_private --locked</automated>
    <automated>cargo test -p polint --test cli cfg_public_no_leak --locked</automated>
    <automated>cargo test -p polint --test cli cfg_capability_remains_unsupported --locked</automated>
    <automated>cargo test -p polint --test cli direct_calls_internals_stay_private --locked</automated>
    <automated>cargo test -p polint --test cli call_graph_capability_remains_unsupported --locked</automated>
    <automated>cargo fmt --all -- --check</automated>
  </verify>
  <done>Phase 30 public boundary is proven alongside prior private-analysis no-leak protections.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| private calls -> public CLI/SDK/docs | Internal facts must not become public contracts accidentally. |
| capability planning -> rule execution | Unsupported call graph requests must block rule execution with diagnostics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-06-01 | I | public JSON/help/docs | mitigate | Assert forbidden private markers are absent from check, inspect, test, help, README, docs/facts, SDK, runner, and CLI source. |
| T-30-06-02 | E | `CallGraph<'_>` reserved view | mitigate | Keep no query methods and prove `call_graph` capability remains unsupported with diagnostics. |
| T-30-06-03 | R | external rule compatibility | mitigate | Temp-repo rule uses only supported SDK views and proves existing workflows still run. |
</threat_model>

<verification>
Run:
```bash
cargo test -p polint --test cli direct_calls_internals_stay_private --locked
cargo test -p polint --test cli call_graph_capability_remains_unsupported --locked
cargo test -p polint --test cli cfg_capability_remains_unsupported --locked
cargo fmt --all -- --check
```
</verification>

<success_criteria>
- Private call internals do not leak into public outputs or docs.
- `CallGraph<'_>` and `call_graph` remain unsupported.
- Existing public rule-author workflows remain compatible.
</success_criteria>

<output>
After completion, create `.planning/phases/30-direct-call-facts/30-06-SUMMARY.md`
</output>
