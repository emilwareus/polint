---
phase: 30-direct-call-facts
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - crates/polint/src/analysis/calls/mod.rs
  - crates/polint/src/analysis/calls/cache_key.rs
  - crates/polint/src/analysis/calls/provider.rs
  - crates/polint/src/analysis_kernel/provider.rs
  - crates/polint/src/analysis_kernel/mod.rs
  - crates/polint/src/analysis_kernel/incremental/keys.rs
  - crates/polint/src/eval/observed.rs
  - crates/polint/src/eval/fixtures.rs
  - tests/eval-fixtures/kernel/provider-order/expected.polint-eval.toml
autonomous: true
requirements: [SAE-SEM-05]
must_haves:
  truths:
    - "The private `polint.calls` provider runs after CFG and before metrics."
    - "Call output has deterministic provider output digests and future-fit layer keys."
    - "Provider-order eval fixtures include the private `polint.calls` provider without exposing call facts publicly."
  artifacts:
    - path: "crates/polint/src/analysis/calls/provider.rs"
      provides: "Private `polint.calls` provider shell and output digest"
      contains: "derive_calls_with_cache_stats"
    - path: "crates/polint/src/analysis_kernel/provider.rs"
      provides: "`polint.calls` provider manifest"
      contains: "calls-facts-1"
    - path: "crates/polint/src/analysis/calls/cache_key.rs"
      provides: "Calls layer-key and provider-parameter vocabulary"
      contains: "calls_provider_parameter_digest"
  key_links:
    - from: "crates/polint/src/analysis_kernel/mod.rs"
      to: "crates/polint/src/analysis/calls/provider.rs"
      via: "kernel provider execution after `polint.cfg`"
      pattern: "derive_calls_with_cache_stats"
    - from: "crates/polint/src/analysis_kernel/incremental/keys.rs"
      to: "crates/polint/src/analysis/calls/cache_key.rs"
      via: "calls layer-key inputs include upstream provider digests"
      pattern: "calls_layer_key"
---

<objective>
Wire the private calls provider slot, cache identity, and provider-order fixture path.

Purpose: downstream extraction and resolution work needs a manifest-owned provider boundary with deterministic identity and fail-closed validation.
Output: `polint.calls` manifest, kernel execution row, recompute-only provider shell, layer-key vocabulary, and provider-order fixture updates.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-direct-call-facts/30-CONTEXT.md
@research/call-graphs/implementation/BOOTSTRAP-INTEGRATION.md
@crates/polint/src/analysis/calls/facts.rs
@crates/polint/src/analysis/calls/store.rs
@crates/polint/src/analysis/cfg/provider.rs
@crates/polint/src/analysis/cfg/cache_key.rs
@crates/polint/src/analysis_kernel/provider.rs
@crates/polint/src/analysis_kernel/mod.rs
@crates/polint/src/analysis_kernel/incremental/keys.rs
@crates/polint/src/analysis_kernel/validation.rs
@crates/polint/src/analysis_kernel/debug.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add `polint.calls` provider manifest and kernel slot</name>
  <files>crates/polint/src/analysis/calls/mod.rs, crates/polint/src/analysis/calls/provider.rs, crates/polint/src/analysis_kernel/provider.rs, crates/polint/src/analysis_kernel/mod.rs, crates/polint/src/eval/observed.rs, crates/polint/src/eval/fixtures.rs, tests/eval-fixtures/kernel/provider-order/expected.polint-eval.toml</files>
  <read_first>
    - `.planning/phases/30-direct-call-facts/30-CONTEXT.md`
    - `crates/polint/src/analysis/calls/store.rs`
    - `crates/polint/src/analysis/cfg/provider.rs`
    - `crates/polint/src/analysis_kernel/provider.rs`
    - `crates/polint/src/analysis_kernel/mod.rs`
    - `crates/polint/src/eval/observed.rs`
    - `tests/eval-fixtures/kernel/provider-order/expected.polint-eval.toml`
    - planned new file: `crates/polint/src/analysis/calls/provider.rs`
  </read_first>
  <behavior>
    - Test 1: provider order is `... polint.semantic_mir`, `polint.cfg`, `polint.calls`, `polint.metrics`.
    - Test 2: kernel run report includes `polint.calls` with schema `calls-facts-1:1`, non-empty output digest, and recompute count `1`.
    - Test 3: provider manifest outputs include `call_sites`, `call_targets`, and `unresolved_calls`.
  </behavior>
  <action>Create `analysis/calls/provider.rs` with `CallsProviderOutput` and `derive_calls_with_cache_stats(db, input_snapshot, manifest, semantic_mir_output_digest, cfg_output_digest, symbol_graph_output_digest, module_topology_output_digest, upstream_syntax_output_digests)`. For this provider shell, derive an empty normalized `CallOutput`, compute a deterministic digest from manifest/schema/input snapshots/upstream output digests/output rows, record one recompute, and publish through `db.replace_call_facts`. Add `CALLS_SCHEMA` (`calls-facts-1`, version `1`) and a `ProviderManifest` with id `polint.calls`, kind `WholeRepoDerived`, inputs `source_files`, `functions`, `symbols`, `references`, `resolved_imports`, `import_to_package_edges`, `mir_bodies`, `mir_operations`, `places`, `cfg_functions`, `cfg_edges`; outputs `call_sites`, `call_targets`, `unresolved_calls`; language scope `MultiLanguage`; cache policy `InMemoryDerived`; precision ceiling `SetupAware` per D-02, D-04, and D-17. Wire `AnalysisKernel::run` after `polint.cfg` and before `polint.metrics`; update provider-order eval expectations.</action>
  <acceptance_criteria>
    - `rg -n "polint.calls|calls-facts-1|call_sites|call_targets|unresolved_calls" crates/polint/src/analysis_kernel/provider.rs` finds manifest data.
    - `rg -n "derive_calls_with_cache_stats" crates/polint/src/analysis_kernel/mod.rs crates/polint/src/analysis/calls/provider.rs` finds the kernel link and provider function.
    - `rg -n "provider_order\\.8|polint.calls|provider_order\\.9|polint.metrics" tests/eval-fixtures/kernel/provider-order/expected.polint-eval.toml crates/polint/src/eval/fixtures.rs crates/polint/src/eval/observed.rs` finds updated provider-order assertions.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p polint --lib provider_order --locked</automated>
    <automated>cargo test -p polint --lib calls_provider --locked</automated>
    <automated>cargo test -p polint --lib kernel_run_report_calls_row_carries_output_digest --locked</automated>
  </verify>
  <done>`polint.calls` is a private manifest-owned provider with a deterministic empty output row in the kernel report.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add calls layer-key vocabulary and output digest coverage</name>
  <files>crates/polint/src/analysis/calls/cache_key.rs, crates/polint/src/analysis/calls/mod.rs, crates/polint/src/analysis/calls/provider.rs, crates/polint/src/analysis_kernel/incremental/keys.rs</files>
  <read_first>
    - `.planning/phases/30-direct-call-facts/30-CONTEXT.md`
    - `research/call-graphs/implementation/BOOTSTRAP-INTEGRATION.md`
    - `crates/polint/src/analysis/cache_key.rs`
    - `crates/polint/src/analysis/cfg/cache_key.rs`
    - `crates/polint/src/analysis_kernel/incremental/keys.rs`
    - `crates/polint/src/analysis/calls/provider.rs`
    - planned new file: `crates/polint/src/analysis/calls/cache_key.rs`
  </read_first>
  <behavior>
    - Test 1: `calls_provider_parameter_digest()` contains `calls-facts-1`, `call_sites`, `call_targets`, `unresolved_calls`, `direct_binding`, `go_direct`, and `ts_js_direct`.
    - Test 2: `LayerKey::calls_layer_key` changes when semantic MIR, CFG, symbol graph, module topology, syntax, config, lifecycle, extension, model, toolchain, or calls parameters change.
    - Test 3: provider output digest includes stable keys/statuses/reasons and excludes raw source, AST dumps, absolute paths, timestamps, and dense IDs as identity.
  </behavior>
  <action>Create `analysis/calls/cache_key.rs` with `calls_provider_parameter_digest()`. Extend `LayerKind` and `LayerKey` with `Calls`/`calls_layer_key`, modeled after `cfg_layer_key`, including provider/schema, source/config/lifecycle inputs, semantic MIR output digest, CFG output digest, symbol/reference output digest, module topology output digest, upstream Go/TS syntax output digests, selected direct-call provider parameters, and absent extension/model/toolchain slots per D-17. Extend `calls_output_digest` in `analysis/calls/provider.rs` to include provider/schema/config/lifecycle/model/extension/toolchain inputs and normalized call site/target/unresolved row payload fragments using stable keys, status, precision, kind, algorithm, reason, and target stable identity where present; do not include raw source bodies, raw AST dumps, absolute paths, timestamps, parser allocation IDs, or run-local dense IDs per D-18.</action>
  <acceptance_criteria>
    - `rg -n "calls_provider_parameter_digest|calls-facts-1|direct_binding|go_direct|ts_js_direct" crates/polint/src/analysis/calls/cache_key.rs` finds provider parameters.
    - `rg -n "LayerKind::Calls|calls_layer_key|calls_lifecycle_inputs|calls_parameters|calls_toolchain" crates/polint/src/analysis_kernel/incremental/keys.rs` finds key vocabulary.
    - `rg -n "raw_source|source_text|absolute_path|SystemTime|Instant::now|run_id" crates/polint/src/analysis/calls/provider.rs crates/polint/src/analysis/calls/cache_key.rs` returns no identity inputs for call output digest/key code.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p polint --lib calls_layer_key --locked</automated>
    <automated>cargo test -p polint --lib calls_provider_parameters --locked</automated>
  </verify>
  <done>Calls have future-fit cache/output identity with all required upstream inputs and no transient identity data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kernel scheduler -> calls provider | Provider order and upstream digest dependencies determine what facts are trusted. |
| cache key -> layer identity | Future reuse keys must fail closed when upstream inputs change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-02-01 | T | `calls_layer_key` | mitigate | Include provider/schema, config/lifecycle, semantic MIR, CFG, symbol/reference, module topology, syntax, model, extension, and toolchain inputs. |
| T-30-02-02 | I | provider digest | mitigate | Hash only stable keys, compact labels, and configured input digests; exclude raw source, AST dumps, absolute paths, timestamps, and dense IDs. |
</threat_model>

<verification>
Run:
```bash
cargo test -p polint --lib calls_provider --locked
cargo test -p polint --lib calls_layer_key --locked
cargo fmt --all -- --check
```
</verification>

<success_criteria>
- `polint.calls` is a private provider after `polint.cfg` and before `polint.metrics`.
- Calls have deterministic output and layer-key identity.
- Provider order fixtures include `polint.calls`.
</success_criteria>

<output>
After completion, create `.planning/phases/30-direct-call-facts/30-02-SUMMARY.md`
</output>
