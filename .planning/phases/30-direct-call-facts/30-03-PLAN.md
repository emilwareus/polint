---
phase: 30-direct-call-facts
plan: 03
type: execute
wave: 3
depends_on: ["30-02"]
files_modified:
  - crates/polint/src/analysis/calls/validate.rs
  - crates/polint/src/analysis/calls/mod.rs
  - crates/polint/src/analysis_kernel/validation.rs
  - crates/polint/src/analysis_kernel/debug.rs
autonomous: true
requirements: [SAE-SEM-05]
must_haves:
  truths:
    - "Validation understands every new call fact family and rejects malformed call rows deterministically."
    - "Test-only debug snapshots expose safe call rows and aggregate counts without public schema promotion."
    - "Validation/debug tests exercise all D-10 store indexes, including outgoing by symbol, incoming by function, and unresolved by status."
  artifacts:
    - path: "crates/polint/src/analysis/calls/validate.rs"
      provides: "Call fact validation"
      contains: "validate_calls"
    - path: "crates/polint/src/analysis_kernel/validation.rs"
      provides: "Call validation hook in metadata validation"
      contains: "validate_calls"
    - path: "crates/polint/src/analysis_kernel/debug.rs"
      provides: "Test-only call debug JSON rows and counts"
      contains: "CallDebugReport"
  key_links:
    - from: "crates/polint/src/analysis_kernel/validation.rs"
      to: "crates/polint/src/analysis/calls/validate.rs"
      via: "call validation hook after CFG validation"
      pattern: "validate_calls"
    - from: "crates/polint/src/analysis_kernel/debug.rs"
      to: "crates/polint/src/analysis/calls/store.rs"
      via: "debug snapshots read normalized call rows and D-10 indexes"
      pattern: "unresolved_by_status"
---

<objective>
Add calls validation and test-only debug snapshots.

Purpose: provider/cache wiring should stay separate from validation/debug so malformed call rows fail closed before later extraction and target plans build on them.
Output: `validate_calls` hook, safe test-only call debug rows/counts, and D-10 index coverage for validation/debug paths.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-direct-call-facts/30-CONTEXT.md
@research/call-graphs/implementation/BOOTSTRAP-INTEGRATION.md
@crates/polint/src/analysis/calls/facts.rs
@crates/polint/src/analysis/calls/store.rs
@crates/polint/src/analysis/cfg/validate.rs
@crates/polint/src/analysis_kernel/validation.rs
@crates/polint/src/analysis_kernel/debug.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add call fact validation hook</name>
  <files>crates/polint/src/analysis/calls/validate.rs, crates/polint/src/analysis/calls/mod.rs, crates/polint/src/analysis_kernel/validation.rs</files>
  <read_first>
    - `.planning/phases/30-direct-call-facts/30-CONTEXT.md`
    - `crates/polint/src/analysis/calls/facts.rs`
    - `crates/polint/src/analysis/calls/store.rs`
    - `crates/polint/src/analysis/cfg/validate.rs`
    - `crates/polint/src/analysis_kernel/validation.rs`
    - planned new file: `crates/polint/src/analysis/calls/validate.rs`
  </read_first>
  <behavior>
    - Test 1: validation emits deterministic `polint/internal` diagnostics for dangling body/op/place/function/symbol/site references, invalid spans, duplicate stable keys, malformed unresolved rows, targets without sites, contradictory statuses, and missing unresolved reasons.
    - Test 2: setup-aware precision ceiling rejects exact precision for `polint.calls` rows.
    - Test 3: validation tests exercise every D-10 accessor: `sites_by_caller`, `targets_by_site`, `outgoing_by_function`, `outgoing_by_symbol`, `incoming_by_symbol`, `incoming_by_function`, `unresolved_by_reason`, and `unresolved_by_status`.
  </behavior>
  <action>Create `analysis/calls/validate.rs` with `validate_calls(db, diagnostics)`, modeled after CFG validation. Hook it from `analysis_kernel::validation::validate_fact_metadata` after CFG validation per D-16. Add focused tests that build a populated `CallStore` and assert deterministic validation behavior through every D-10 index/accessor, including `outgoing_by_symbol`, `incoming_by_function`, and `unresolved_by_status` per D-10. Validation must reject malformed rows instead of panicking or silently accepting inconsistent call facts.</action>
  <acceptance_criteria>
    - `rg -n "validate_calls|dangling.*call|missing unresolved reason|contradictory" crates/polint/src/analysis/calls/validate.rs crates/polint/src/analysis_kernel/validation.rs` finds validation coverage.
    - `rg -n "sites_by_caller|targets_by_site|outgoing_by_function|outgoing_by_symbol|incoming_by_symbol|incoming_by_function|unresolved_by_reason|unresolved_by_status" crates/polint/src/analysis/calls/validate.rs crates/polint/src/analysis/calls/store.rs` finds deterministic D-10 index coverage.
    - `rg -n "Exact.*polint\\.calls|SetupAware|precision ceiling" crates/polint/src/analysis/calls/validate.rs crates/polint/src/analysis_kernel/validation.rs` finds provider precision-ceiling checks.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p polint --lib analysis_kernel::validation::calls --locked</automated>
  </verify>
  <done>Calls validation exists internally, is wired into metadata validation, and covers all D-10 indexes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add safe test-only call debug snapshots</name>
  <files>crates/polint/src/analysis_kernel/debug.rs</files>
  <read_first>
    - `.planning/phases/30-direct-call-facts/30-CONTEXT.md`
    - `crates/polint/src/analysis/calls/facts.rs`
    - `crates/polint/src/analysis/calls/store.rs`
    - `crates/polint/src/analysis_kernel/debug.rs`
  </read_first>
  <behavior>
    - Test 1: `metadata_debug_json_for_test` contains `calls` with call-site, target, unresolved rows and aggregate counts by language, call kind, algorithm, status, unresolved reason, and provider.
    - Test 2: debug tests include deterministic snapshots for outgoing by function/symbol, incoming by target symbol/function, and unresolved by reason/status.
    - Test 3: debug rows include no raw source bodies, AST dumps, absolute paths, timestamps, parser allocation IDs, or run-local dense IDs as identity.
  </behavior>
  <action>Extend `analysis_kernel/debug.rs` with a `calls` report containing arrays `sites`, `targets`, `unresolved`, `index_counts`, and `counts`; rows must include family, stable_key, producer_id, layer_id, status, precision, relative path, span, kind/algorithm/reason payload, and no raw source/AST/absolute path. Counts must cover `by_language`, `by_call_kind`, `by_algorithm`, `by_status`, `by_unresolved_reason`, and `by_provider` per D-18. `index_counts` must make D-10 grep-verifiable by naming `outgoing_by_function`, `outgoing_by_symbol`, `incoming_by_symbol`, `incoming_by_function`, `unresolved_by_reason`, and `unresolved_by_status`. Keep the debug helper `#![cfg(test)]` only.</action>
  <acceptance_criteria>
    - `rg -n "calls:|CallDebugReport|by_language|by_call_kind|by_algorithm|by_status|by_unresolved_reason|by_provider" crates/polint/src/analysis_kernel/debug.rs` finds debug snapshot fields.
    - `rg -n "outgoing_by_function|outgoing_by_symbol|incoming_by_symbol|incoming_by_function|unresolved_by_reason|unresolved_by_status|index_counts" crates/polint/src/analysis_kernel/debug.rs` finds D-10 debug coverage.
    - `rg -n "pub fn metadata_debug_json_for_test|#!\\[cfg\\(test\\)\\]" crates/polint/src/analysis_kernel/debug.rs` confirms debug remains test-only.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p polint --lib analysis_kernel::debug::calls_debug_json --locked</automated>
    <automated>cargo fmt --all -- --check</automated>
  </verify>
  <done>Calls debug snapshots exist internally, include safe aggregate/index coverage, and avoid public/debug schema promotion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| call facts -> validation/debug | Internal rows are serialized into test-only snapshots and validation diagnostics. |
| store indexes -> debug snapshots | Deterministic indexes are exposed only as aggregate test evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-03-01 | I | calls debug JSON | mitigate | Serialize only relative paths, spans, stable keys, labels, and compact payload fragments; keep helper `cfg(test)`. |
| T-30-03-02 | D | validation | mitigate | Emit bounded deterministic diagnostics for malformed rows rather than panicking or silently accepting inconsistent call facts. |
| T-30-03-03 | T | D-10 index coverage | mitigate | Add validation/debug tests that name and exercise every required call-store accessor. |
</threat_model>

<verification>
Run:
```bash
cargo test -p polint --lib analysis_kernel::validation::calls --locked
cargo test -p polint --lib analysis_kernel::debug::calls_debug_json --locked
cargo fmt --all -- --check
```
</verification>

<success_criteria>
- Calls validation/debug snapshots are internal and test-facing.
- Malformed call facts fail closed with deterministic diagnostics.
- All D-10 indexes are covered by grep-verifiable validation/debug tests.
</success_criteria>

<output>
After completion, create `.planning/phases/30-direct-call-facts/30-03-SUMMARY.md`
</output>
