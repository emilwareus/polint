---
phase: 30-direct-call-facts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/polint/src/analysis/mod.rs
  - crates/polint/src/analysis/ids.rs
  - crates/polint/src/analysis/calls/mod.rs
  - crates/polint/src/analysis/calls/facts.rs
  - crates/polint/src/analysis/calls/store.rs
  - crates/polint/src/core/mod.rs
  - crates/polint/src/analysis_kernel/metadata.rs
autonomous: true
requirements: [SAE-SEM-05]
must_haves:
  truths:
    - "polint has crate-private direct-call fact contracts for sites, targets, and unresolved evidence."
    - "Run-local call IDs are separate from deterministic stable keys."
    - "Call storage exposes deterministic indexes for callers, sites, targets, incoming, outgoing, and unresolved reasons."
  artifacts:
    - path: "crates/polint/src/analysis/calls/facts.rs"
      provides: "CallSiteFact, CallTargetFact, UnresolvedCallFact, status, kind, algorithm, and reason vocabulary"
      contains: "CallSiteFact"
    - path: "crates/polint/src/analysis/calls/store.rs"
      provides: "CallOutput and CallStore deterministic normalization and indexes"
      contains: "CallStore"
    - path: "crates/polint/src/core/mod.rs"
      provides: "AnalysisDb storage, replacement, accessors, and metadata refresh for calls"
      contains: "replace_call_facts"
  key_links:
    - from: "crates/polint/src/analysis/calls/store.rs"
      to: "crates/polint/src/analysis/calls/facts.rs"
      via: "normalized CallOutput and CallStore indexes over call fact structs"
      pattern: "CallOutput"
    - from: "crates/polint/src/core/mod.rs"
      to: "crates/polint/src/analysis_kernel/metadata.rs"
      via: "FactFamily metadata rows for call facts"
      pattern: "FactFamily::CallSite"
---

<objective>
Create the private direct-call fact contracts and deterministic storage foundation.

Purpose: Phase 30 needs normalized internal facts before provider wiring or language resolution can be implemented safely.
Output: `analysis::calls` contracts, `CallTargetId`, `AnalysisDb` storage/accessors, metadata families, and deterministic store indexes.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-direct-call-facts/30-CONTEXT.md
@research/call-graphs/RECOMMENDED_IMPLEMENTATION.md
@research/call-graphs/implementation/BOOTSTRAP-INTEGRATION.md
@crates/polint/src/analysis/ids.rs
@crates/polint/src/analysis/mir/op.rs
@crates/polint/src/analysis/places.rs
@crates/polint/src/analysis/cfg/facts.rs
@crates/polint/src/analysis/cfg/store.rs
@crates/polint/src/core/mod.rs
@crates/polint/src/analysis_kernel/metadata.rs

<interfaces>
Existing private ID precedent:
```rust
pub(crate) struct CallSiteId(pub(crate) u64);
pub(crate) struct MirBodyId(pub(crate) u64);
pub(crate) struct MirOpId(pub(crate) u64);
pub(crate) struct PlaceId(pub(crate) u64);
```

Existing MIR call source:
```rust
MirOperationKind::Call {
    site: CallSiteId,
    callee: MirValue,
    arguments: Vec<PlaceId>,
    return_place: PlaceId,
}
```

Existing CFG storage pattern to mirror:
```rust
pub(crate) struct CfgOutput { ... }
impl CfgOutput { pub(crate) fn normalized(mut self) -> Self { ... } }
pub(crate) fn replace_cfg_facts(&mut self, output: CfgOutput) -> Result<(), AnalysisError>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add call IDs and crate-private call fact contracts</name>
  <files>crates/polint/src/analysis/mod.rs, crates/polint/src/analysis/ids.rs, crates/polint/src/analysis/calls/mod.rs, crates/polint/src/analysis/calls/facts.rs</files>
  <read_first>
    - `.planning/phases/30-direct-call-facts/30-CONTEXT.md`
    - `research/call-graphs/implementation/BOOTSTRAP-INTEGRATION.md`
    - `crates/polint/src/analysis/mod.rs`
    - `crates/polint/src/analysis/ids.rs`
    - `crates/polint/src/analysis/mir/op.rs`
    - `crates/polint/src/analysis/places.rs`
    - `crates/polint/src/analysis/cfg/facts.rs`
    - planned new file: `crates/polint/src/analysis/calls/facts.rs`
  </read_first>
  <behavior>
    - Test 1: `CallTargetId` is `Copy`, ordered, hashable, serializable, and distinct from `CallSiteId`.
    - Test 2: call fact structs preserve dense IDs and stable keys as separate fields.
    - Test 3: status/reason vocabulary includes `Resolved`, `Ambiguous`, `Unresolved`, `Unsupported`, `SetupMissing`, `FunctionValue`, `DynamicProperty`, `InterfaceDispatch`, `Eval`, `CallApplyBind`, and `FrameworkDispatch`.
  </behavior>
  <action>Add `CallTargetId(pub(crate) u64)` to `analysis::ids` per D-06. Register `pub(crate) mod calls;` in `analysis/mod.rs` per D-01. Create `analysis/calls/mod.rs` and `analysis/calls/facts.rs` with `pub(crate)` structs/enums: `CallSiteFact`, `CallTargetFact`, `UnresolvedCallFact`, `CallSyntaxKind`, `CallCallee`, `CallEdgeKind`, `CallAlgorithm`, `CallTargetStatus`, `UnresolvedCallReason`, `CallPrecision`, and `CallProvenance`. `CallSiteFact` must carry language, file, caller `FunctionId`, optional owner `SymbolId`, MIR `body`, MIR `operation`, span, syntax kind, callee shape, optional receiver `PlaceId`, argument `PlaceId`s, optional result `PlaceId`, status/precision, and `stable_key` per D-05 through D-09. `CallTargetFact` must carry site, caller, optional target function/symbol, edge kind, algorithm, status, reason, provenance, precision, and `stable_key` per D-08. Keep everything crate-private and do not add SDK, runner, CLI, or docs exports per D-01, D-03, D-21, and D-22.</action>
  <acceptance_criteria>
    - `rg -n "pub\\(crate\\) mod calls" crates/polint/src/analysis/mod.rs` finds the module registration.
    - `rg -n "struct CallTargetId" crates/polint/src/analysis/ids.rs` finds the new dense ID.
    - `rg -n "struct CallSiteFact|struct CallTargetFact|struct UnresolvedCallFact|enum CallAlgorithm|enum UnresolvedCallReason" crates/polint/src/analysis/calls/facts.rs` finds all required contracts.
    - `rg -n "pub struct CallSiteFact|pub struct CallTargetFact|pub enum CallGraph|pub mod calls" crates/polint/src/sdk crates/polint/src/runner crates/polint/src/lib.rs` returns no new public call surface except the pre-existing reserved `CallGraph<'_>` type.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p polint --lib analysis::calls::facts --locked</automated>
    <automated>cargo test -p polint --lib analysis::ids --locked</automated>
  </verify>
  <done>`analysis::calls` fact vocabulary exists, compiles, is tested, and remains crate-private.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add deterministic call output and store indexes</name>
  <files>crates/polint/src/analysis/calls/mod.rs, crates/polint/src/analysis/calls/store.rs</files>
  <read_first>
    - `.planning/phases/30-direct-call-facts/30-CONTEXT.md`
    - `research/call-graphs/implementation/BOOTSTRAP-INTEGRATION.md`
    - `crates/polint/src/analysis/calls/facts.rs`
    - `crates/polint/src/analysis/cfg/store.rs`
    - `crates/polint/src/module_graph/topology.rs`
    - planned new file: `crates/polint/src/analysis/calls/store.rs`
  </read_first>
  <behavior>
    - Test 1: `CallOutput::normalized()` sorts sites, targets, and unresolved rows deterministically without silently dropping duplicates.
    - Test 2: `CallStore::from_output` builds `sites_by_caller`, `targets_by_site`, `outgoing_by_function`, `incoming_by_symbol`, and `unresolved_by_reason` with stable `BTreeMap` ordering.
    - Test 3: targets without matching sites are rejected with `AnalysisError`, not stored.
  </behavior>
  <action>Create `analysis/calls/store.rs` with `CallOutput { sites, targets, unresolved }` and `CallStore` per D-05 and D-10. Implement `CallOutput::empty`, `CallOutput::normalized`, `CallStore::from_output`, accessors for `sites()`, `targets()`, `unresolved()`, `sites_by_caller(FunctionId)`, `targets_by_site(CallSiteId)`, `outgoing_by_function(FunctionId)`, `incoming_by_symbol(SymbolId)`, and `unresolved_by_reason(UnresolvedCallReason)`. Use `BTreeMap`/sorted `Vec` indexes per D-10. Validate referential integrity from target/unresolved rows to site rows in the store constructor so later provider code cannot publish malformed rows per D-16.</action>
  <acceptance_criteria>
    - `rg -n "struct CallOutput|struct CallStore|sites_by_caller|targets_by_site|outgoing_by_function|incoming_by_symbol|unresolved_by_reason" crates/polint/src/analysis/calls/store.rs` finds all indexes.
    - `rg -n "BTreeMap" crates/polint/src/analysis/calls/store.rs` confirms deterministic index maps.
    - `rg -n "from_output.*Result|dangling call site|matching site" crates/polint/src/analysis/calls/store.rs` confirms malformed target handling exists.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p polint --lib analysis::calls::store --locked</automated>
  </verify>
  <done>Call facts have normalized output and deterministic query indexes ready for provider publication.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Store call facts in AnalysisDb with metadata families</name>
  <files>crates/polint/src/core/mod.rs, crates/polint/src/analysis_kernel/metadata.rs</files>
  <read_first>
    - `.planning/phases/30-direct-call-facts/30-CONTEXT.md`
    - `crates/polint/src/analysis/calls/facts.rs`
    - `crates/polint/src/analysis/calls/store.rs`
    - `crates/polint/src/core/mod.rs`
    - `crates/polint/src/analysis_kernel/metadata.rs`
    - `crates/polint/src/analysis/store.rs`
  </read_first>
  <behavior>
    - Test 1: `AnalysisDb::replace_call_facts` removes stale rows from a previous run.
    - Test 2: call metadata rows use producer/layer id `polint.calls` and family labels `CallSite`, `CallTarget`, and `UnresolvedCall`.
    - Test 3: call metadata maps unresolved/unsupported/setup-missing statuses to non-exact precision.
  </behavior>
  <action>Add internal `FactFamily` variants and labels for `CallSite`, `CallTarget`, and `UnresolvedCall` per D-16. Add `CALLS_PROVIDER_ID: &str = "polint.calls"` to `core/mod.rs`. Add `AnalysisDb` storage fields for call sites, call targets, unresolved calls, and an optional `CallStore`/index cache. Implement `replace_call_facts(CallOutput) -> Result<(), AnalysisError>`, crate-private accessors `call_sites()`, `call_targets()`, `unresolved_calls()`, and indexed lookup helpers. Refresh metadata immediately after replacement using stable keys from the call facts and payload fragments that include status, precision, kind/algorithm/reason, site stable key, and target stable key where present. Do not touch `FunctionFact.calls: Vec<String>` except to leave it compatible per D-03.</action>
  <acceptance_criteria>
    - `rg -n "CALLS_PROVIDER_ID|replace_call_facts|call_sites\\(|call_targets\\(|unresolved_calls\\(" crates/polint/src/core/mod.rs` finds storage and accessors.
    - `rg -n "CallSite|CallTarget|UnresolvedCall" crates/polint/src/analysis_kernel/metadata.rs` finds all fact families and labels.
    - `rg -n "FunctionFact.*calls|calls: Vec<String>" crates/polint/src/core/mod.rs` still shows the legacy field unchanged.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p polint --lib call_fact_storage --locked</automated>
    <automated>cargo test -p polint --lib call_fact_metadata --locked</automated>
    <automated>cargo fmt --all -- --check</automated>
  </verify>
  <done>Call facts can be atomically stored in `AnalysisDb` with metadata and without public API promotion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MIR/place facts -> call store | Private upstream analysis rows are transformed into new call identities and indexes. |
| call output -> metadata sidecar | Stable keys and payload digests become validation inputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01-01 | T | `CallStore::from_output` | mitigate | Reject targets/unresolved rows whose `site` is absent; add tests for dangling references. |
| T-30-01-02 | I | `FactMeta` payloads | mitigate | Use compact status/kind/stable-key fragments only; do not include raw source bodies, AST dumps, absolute paths, or run-local IDs as identity. |
| T-30-01-03 | E | SDK/runner visibility | mitigate | Keep modules and accessors `pub(crate)` and assert no new `sdk`, `runner`, or crate-root public exports. |
</threat_model>

<verification>
Run the task-level commands plus:
```bash
cargo test -p polint --lib analysis::calls --locked
cargo fmt --all -- --check
```
</verification>

<success_criteria>
- `analysis::calls` exists and is crate-private.
- `CallSiteFact`, `CallTargetFact`, and `UnresolvedCallFact` are normalized, indexed, and metadata-backed.
- Legacy `FunctionFact.calls` is not promoted into the semantic call substrate.
- No public `Calls<'_>` or supported `CallGraph<'_>` surface is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/30-direct-call-facts/30-01-SUMMARY.md`
</output>
