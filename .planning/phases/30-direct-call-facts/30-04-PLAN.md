---
phase: 30-direct-call-facts
plan: 04
type: execute
wave: 4
depends_on: ["30-03"]
files_modified:
  - crates/polint/src/analysis/calls/mod.rs
  - crates/polint/src/analysis/calls/direct.rs
  - crates/polint/src/analysis/calls/provider.rs
  - crates/polint/src/analysis/calls/validate.rs
  - crates/polint/src/analysis/calls/store.rs
autonomous: true
requirements: [SAE-SEM-05]
must_haves:
  truths:
    - "Direct call target facts consume semantic references, symbols, imports, topology, MIR, and place evidence where available."
    - "Only direct/binding/static resolution algorithms emit resolved targets in Phase 30."
    - "Dynamic, interface, function-token, framework, and summary-assisted cases remain unresolved/unsupported facts."
  artifacts:
    - path: "crates/polint/src/analysis/calls/direct.rs"
      provides: "Direct target resolver for Go and TS/JS"
      contains: "resolve_direct_call_targets"
    - path: "crates/polint/src/analysis/calls/provider.rs"
      provides: "Provider output with call targets"
      contains: "resolve_direct_call_targets"
    - path: "crates/polint/src/analysis/calls/validate.rs"
      provides: "Target-specific validation"
      contains: "CallTarget"
  key_links:
    - from: "crates/polint/src/analysis/calls/direct.rs"
      to: "crates/polint/src/core/mod.rs"
      via: "uses `symbols()`, `references()`, `resolved_imports()`, and call site rows"
      pattern: "references"
    - from: "crates/polint/src/analysis/calls/direct.rs"
      to: "crates/polint/src/analysis/calls/facts.rs"
      via: "emits `CallTargetFact` with algorithm/status/reason"
      pattern: "CallTargetFact"
---

<objective>
Add direct/binding/static call target resolution for Go and TypeScript/JavaScript.

Purpose: Phase 30 must provide useful direct targets while preserving honest unresolved states for every case that needs later refined providers.
Output: `CallTargetFact` rows for directly resolved callees and unresolved/status updates for non-direct cases.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-direct-call-facts/30-CONTEXT.md
@research/call-graphs/RECOMMENDED_IMPLEMENTATION.md
@research/call-graphs/implementation/BOOTSTRAP-INTEGRATION.md
@research/call-graphs/languages/go.md
@research/call-graphs/languages/typescript-javascript.md
@crates/polint/src/analysis/calls/facts.rs
@crates/polint/src/analysis/calls/extract.rs
@crates/polint/src/analysis/calls/unresolved.rs
@crates/polint/src/symbol_graph/semantic.rs
@crates/polint/src/symbol_graph/go.rs
@crates/polint/src/symbol_graph/ts.rs
@crates/polint/src/module_graph/topology.rs
@crates/polint/src/core/mod.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resolve direct lexical/import/static call targets</name>
  <files>crates/polint/src/analysis/calls/mod.rs, crates/polint/src/analysis/calls/direct.rs, crates/polint/src/analysis/calls/provider.rs</files>
  <read_first>
    - `.planning/phases/30-direct-call-facts/30-CONTEXT.md`
    - `research/call-graphs/implementation/BOOTSTRAP-INTEGRATION.md`
    - `crates/polint/src/analysis/calls/facts.rs`
    - `crates/polint/src/analysis/calls/extract.rs`
    - `crates/polint/src/analysis/calls/provider.rs`
    - `crates/polint/src/symbol_graph/semantic.rs`
    - `crates/polint/src/core/mod.rs`
    - planned new file: `crates/polint/src/analysis/calls/direct.rs`
  </read_first>
  <behavior>
    - Test 1: a lexical function call with a resolved `ReferenceFact` emits a `CallTargetFact` with algorithm `DirectReference`, status `Resolved`, and a target symbol/function where available.
    - Test 2: an imported function call resolved through existing import/reference/module rows emits algorithm `ImportBinding`.
    - Test 3: constructor/static/member calls with precise semantic reference evidence emit algorithm `StaticMember` or `DirectReference`; member calls without precise reference evidence stay unresolved.
  </behavior>
  <action>Create `analysis/calls/direct.rs` with `resolve_direct_call_targets(db: &AnalysisDb, sites: &[CallSiteFact]) -> Vec<CallTargetFact>`. Build deterministic indexes over `ReferenceFact`, `SymbolFact`, `FunctionFact`, resolved imports, semantic import/resolution rows, and import-to-package topology rows where useful. For D-08 and D-11, emit targets only when existing semantic references/symbols identify one precise direct target. Use algorithms `DirectReference`, `ImportBinding`, and `StaticMember` only. Set edge kinds `Direct`, `Constructor`, `StaticMember`, or `MethodDirect` based on call syntax. Include provider provenance `NativeDirect`, status `Resolved`, setup-aware precision, and stable keys containing call-site stable key, algorithm, target stable key, provider/schema identity, and absent model identity per D-09. Wire results into provider output after sites/unresolved rows.</action>
  <acceptance_criteria>
    - `rg -n "resolve_direct_call_targets|DirectReference|ImportBinding|StaticMember|CallTargetFact" crates/polint/src/analysis/calls/direct.rs crates/polint/src/analysis/calls/provider.rs` finds direct resolution wiring.
    - `rg -n "GoCha|GoRta|GoVta|FunctionTokenFlow|SummaryAssisted|FrameworkModel|RepoModel|PointsTo" crates/polint/src/analysis/calls/direct.rs` returns no emitted resolved-target algorithms from deferred tiers.
    - `rg -n "references\\(|symbols\\(|resolved_imports\\(|import_to_package" crates/polint/src/analysis/calls/direct.rs` confirms semantic/reference/topology inputs are consumed.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p polint --lib analysis::calls::direct --locked</automated>
    <automated>cargo test -p polint --lib calls_provider --locked</automated>
  </verify>
  <done>Direct target facts are emitted only for precise direct/binding/static evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve unresolved statuses for non-direct Go and TS/JS cases</name>
  <files>crates/polint/src/analysis/calls/direct.rs, crates/polint/src/analysis/calls/unresolved.rs, crates/polint/src/analysis/calls/provider.rs</files>
  <read_first>
    - `.planning/phases/30-direct-call-facts/30-CONTEXT.md`
    - `research/call-graphs/languages/go.md`
    - `research/call-graphs/languages/typescript-javascript.md`
    - `crates/polint/src/analysis/calls/direct.rs`
    - `crates/polint/src/analysis/calls/unresolved.rs`
    - `crates/polint/src/analysis/mir/lower_go.rs`
    - `crates/polint/src/analysis/mir/lower_ts.rs`
  </read_first>
  <behavior>
    - Test 1: Go interface dispatch, function values, reflection, goroutines, and setup-missing package data do not emit resolved targets unless precise direct reference evidence exists.
    - Test 2: TS/JS dynamic property calls, callable values, `eval`, proxies/getters/setters, dynamic imports, decorators, `call`/`apply`/`bind`, and framework dispatch do not emit resolved targets unless precise direct reference evidence exists.
    - Test 3: missing targets are represented in unresolved rows or unresolved target statuses; they are not silent omissions.
  </behavior>
  <action>Strengthen direct resolution and unresolved derivation for D-12 through D-15. Add tests that assert no resolved target facts are emitted for Go CHA/RTA/VTA candidates, interface dispatch, function values, reflection, goroutine-spawned interprocedural edges, TS/JS function-token-flow candidates, dynamic property/member calls, `eval`, `call`/`apply`/`bind`, dynamic import, decorators, proxies/getters/setters, framework dispatch, summaries, and repo models. Where the call site exists, ensure an `UnresolvedCallFact` with a specific reason is present. This task must not implement Go SSA static/RTA/VTA, TS/JS function-token flow, framework recognizers, repo-local models, summaries, points-to, or public query views.</action>
  <acceptance_criteria>
    - `rg -n "InterfaceDispatch|FunctionValue|Reflection|GoroutineBoundary|DynamicProperty|Eval|CallApplyBind|DynamicImport|FrameworkDispatch" crates/polint/src/analysis/calls/direct.rs crates/polint/src/analysis/calls/unresolved.rs` finds explicit non-direct coverage.
    - `rg -n "ssa|RTA|VTA|CHA|function_token|points_to|summary|framework recognizer|repo_model" crates/polint/src/analysis/calls/direct.rs` returns no implemented refined provider logic outside enum labels/tests asserting exclusion.
    - Focused tests assert unresolved rows for at least one Go function-value case and one TS/JS dynamic call case.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p polint --lib analysis::calls::direct::non_direct_cases --locked</automated>
    <automated>cargo test -p polint --lib analysis::calls::unresolved --locked</automated>
  </verify>
  <done>Every non-direct call class remains honestly unresolved/unsupported instead of becoming a fake direct edge.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Validate target rows and indexed incoming/outgoing edges</name>
  <files>crates/polint/src/analysis/calls/store.rs, crates/polint/src/analysis/calls/validate.rs, crates/polint/src/analysis/calls/provider.rs</files>
  <read_first>
    - `.planning/phases/30-direct-call-facts/30-CONTEXT.md`
    - `crates/polint/src/analysis/calls/store.rs`
    - `crates/polint/src/analysis/calls/validate.rs`
    - `crates/polint/src/analysis/calls/provider.rs`
    - `crates/polint/src/analysis_kernel/debug.rs`
  </read_first>
  <behavior>
    - Test 1: `incoming_by_symbol` and `outgoing_by_function` indexes include resolved target rows in deterministic order.
    - Test 2: validation rejects target rows that have `Resolved` status with no target function/symbol/synthetic target.
    - Test 3: validation rejects unresolved/unsupported target rows without reason and targets whose site is missing.
  </behavior>
  <action>Complete target-specific store and validation behavior per D-10 and D-16. Ensure `CallStore` indexes resolved targets by caller/function and incoming symbol where `target_symbol` exists. Add validation checks for malformed target rows: target without site, resolved status without target identity, unresolved/unsupported/setup-missing status without reason, contradictory target identity plus unsupported reason, duplicate stable keys, invalid spans, and provider precision-ceiling violations. Ensure provider tests cover populated `CallTargetFact` rows and output digest changes when target status/algorithm changes.</action>
  <acceptance_criteria>
    - `rg -n "incoming_by_symbol|outgoing_by_function|Resolved.*target|missing.*reason|duplicate stable" crates/polint/src/analysis/calls/store.rs crates/polint/src/analysis/calls/validate.rs` finds validations/indexes.
    - `cargo test -p polint --lib analysis_kernel::validation::calls --locked` output includes passing target validation tests.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p polint --lib analysis::calls::store --locked</automated>
    <automated>cargo test -p polint --lib analysis_kernel::validation::calls --locked</automated>
    <automated>cargo fmt --all -- --check</automated>
  </verify>
  <done>Resolved targets are indexed and malformed direct/unresolved rows fail validation deterministically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| semantic refs/imports/topology -> target facts | Existing semantic evidence becomes direct call target rows. |
| unresolved dynamic evidence -> call graph substrate | Unsupported language features become explicit uncertainty data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-04-01 | S | direct target resolver | mitigate | Emit `Resolved` targets only when existing references/symbols identify a precise direct target. |
| T-30-04-02 | T | target stable keys | mitigate | Include call-site stable key, algorithm, target stable key or unresolved reason, provider/schema identity, and absent model identity. |
| T-30-04-03 | R | non-direct cases | mitigate | Add tests proving deferred algorithm families stay unresolved/unsupported with explicit reasons. |
</threat_model>

<verification>
Run:
```bash
cargo test -p polint --lib analysis::calls::direct --locked
cargo test -p polint --lib analysis::calls::unresolved --locked
cargo test -p polint --lib analysis_kernel::validation::calls --locked
cargo test -p polint --lib calls_provider --locked
cargo fmt --all -- --check
```
</verification>

<success_criteria>
- Direct/binding/static targets are emitted when semantic evidence is precise.
- Go and TS/JS non-direct call classes remain explicit unresolved/unsupported rows.
- Incoming/outgoing/targets-by-site indexes work for resolved and unresolved data.
</success_criteria>

<output>
After completion, create `.planning/phases/30-direct-call-facts/30-04-SUMMARY.md`
</output>
