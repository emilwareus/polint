---
phase: 55-sdk-query-vocabulary-and-preview-contract
plan: 03
title: Public Docs External Tests And Boundary Proof
type: execute
wave: 3
depends_on: [55-01, 55-02]
files_modified:
  - docs/API-VISIBILITY-PLAN.md
  - docs/facts
  - crates/polint/tests/cli.rs
  - crates/polint/tests/public_surface_leak.rs
  - tests/fixtures/public-surface-leak-probe/src/lib.rs
autonomous: true
requirements: [API-01, API-02, API-03, API-04, API-05, API-06]
must_haves:
  truths:
    - "D-15/D-16: Phase 55 validation proves compile, manifest, capability, and fail-closed behavior; full query-result behavior is deferred to Phases 56-59."
    - "D-17: public-surface leak gates are deliberately updated for the new preview names and still prove raw internals are unreachable."
    - "D-18: polint facts list, docs/facts pages, macro capability tests, and reserved/preview diagnostics are updated together."
    - "D-01/D-10/D-14: docs show policy-level views and one query-object syntax, not raw graph traversal or parser/solver IDs."
  artifacts:
    - path: "crates/polint/tests/cli.rs"
      provides: "External temp-repo and CLI regression tests for preview syntax and fail-closed behavior"
    - path: "crates/polint/tests/public_surface_leak.rs"
      provides: "Updated deliberate public prelude allow-list and raw-internal leak proof"
    - path: "docs/API-VISIBILITY-PLAN.md"
      provides: "Promotion record for Phase 55 preview public names"
---

<objective>
Document the Phase 55 preview contract and prove an external rule author can compile the intended syntax while raw internals remain outside supported public surfaces.
</objective>

<execution_context>
@.planning/phases/55-sdk-query-vocabulary-and-preview-contract/55-CONTEXT.md
@.planning/REQUIREMENTS.md
@docs/API-VISIBILITY-PLAN.md
@docs/facts/capability-plans.md
@docs/facts/data-flow.md
@AGENTS.md
@.agents/skills/rust-best-practices/SKILL.md
@.agents/skills/polint/SKILL.md
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add external temp-repo preview syntax coverage</name>
  <files>crates/polint/tests/cli.rs</files>
  <read_first>
    - crates/polint/tests/cli.rs
    - crates/polint/src/sdk/mod.rs
    - crates/polint/src/sdk/facts.rs
    - crates/polint/src/sdk/policy.rs
    - crates/polint/src/analysis_plan.rs
  </read_first>
  <action>Add at least one temp-repo style CLI test that creates generated `.polint/rules`, imports only `polint::sdk::prelude::*`, registers through `polint::runner::run_cli`, and compiles a rule signature using one or more Phase 55 preview views plus query/pattern construction. The test must assert `polint check --format json` reports `polint/capability` for unsupported preview capabilities and does not include a marker diagnostic that the rule body would have emitted if it ran. Include a second focused assertion that `Cfg<'_>` and `CallGraph<'_>` remain reserved raw capabilities, not replacements for `ControlFlow<'_>` or `Calls<'_>`.</action>
  <verify>
    <automated>cargo test -p polint --test cli phase55_preview_rule_syntax_compiles_and_fails_closed --locked</automated>
    <automated>cargo test -p polint --test cli reserved_cfg_and_call_graph_remain_unsupported --locked</automated>
  </verify>
  <acceptance_criteria>
    - The temp rule imports only `polint::sdk::prelude::*` and `polint::runner::run_cli`.
    - The rule constructs `FlowQuery::new(...)` or another Phase 55 query object with typed patterns.
    - JSON output includes `polint/capability` and excludes the marker diagnostic from the preview rule body.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update public-surface leak gates</name>
  <files>crates/polint/tests/public_surface_leak.rs, tests/fixtures/public-surface-leak-probe/src/lib.rs</files>
  <read_first>
    - crates/polint/tests/public_surface_leak.rs
    - tests/fixtures/public-surface-leak-probe/src/lib.rs
    - docs/API-VISIBILITY-PLAN.md
    - crates/polint/src/sdk/mod.rs
  </read_first>
  <action>Deliberately update the public prelude allow-list and probe fixture for the new preview names from Plan 55-01. Assert that `Events`, `Calls`, `ControlFlow`, policy-level `DataFlow`, `ReachQuery`, `GuardQuery`, `LifecycleQuery`, `FlowQuery`, `EventPattern`, `SourcePattern`, `SinkPattern`, `GuardPattern`, and `BarrierPattern` are reachable only through the supported SDK prelude path. Preserve or add negative checks that raw CFG/call graph/semantic graph/data-flow graph, solver/provider modules, `AnalysisDb`, private IDs, parser AST types, and internal analysis modules are not reachable from supported public imports.</action>
  <verify>
    <automated>cargo test -p polint --test public_surface_leak --locked</automated>
  </verify>
  <acceptance_criteria>
    - The leak gate allow-list contains the intentional Phase 55 preview names.
    - The probe still fails if private graph, solver, provider, parser, or analysis database types leak through public surfaces.
    - The test names and fixture comments identify the additions as preview policy-query names, not raw graph APIs.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Document preview status and promotion record</name>
  <files>docs/API-VISIBILITY-PLAN.md, docs/facts, docs/facts/capability-plans.md, docs/facts/data-flow.md</files>
  <read_first>
    - docs/API-VISIBILITY-PLAN.md
    - docs/facts/README.md
    - docs/facts/capability-plans.md
    - docs/facts/data-flow.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/55-sdk-query-vocabulary-and-preview-contract/55-CONTEXT.md
  </read_first>
  <action>Update docs to describe the Phase 55 preview contract honestly. `docs/API-VISIBILITY-PLAN.md` must record the intentional promotion of `Events<'_>`, `Calls<'_>`, `ControlFlow<'_>`, policy-level `DataFlow<'_>`, query structs, and pattern structs. `docs/facts/` must include checked-in documentation for preview events/calls/control-flow/data-flow vocabulary, capability names, unsupported/fail-closed behavior, simple syntax examples, exact/list pattern matching limits, and the deferral of full query results to Phases 56-59. Remove or rewrite text that describes `DataFlow<'_>` only as a reserved raw graph view.</action>
  <verify>
    <automated>rg -n "Events<'_>|Calls<'_>|ControlFlow<'_>|FlowQuery|SourcePattern|polint/capability|fail closed|preview" docs/API-VISIBILITY-PLAN.md docs/facts</automated>
    <automated>rg -n "raw CFG|raw call graph|AnalysisDb|solver|provider" docs/facts docs/API-VISIBILITY-PLAN.md</automated>
  </verify>
  <acceptance_criteria>
    - Docs show the one query-object syntax and name `events`, `calls`, `control_flow`, and `dataflow`.
    - Docs state Phase 55 preview capabilities fail closed until behavior lands in later phases.
    - Docs do not tell rule authors to import `polint::core`, parser adapters, raw graph modules, providers, `AnalysisDb`, or private IDs.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Run focused Phase 55 verification</name>
  <files>docs/API-VISIBILITY-PLAN.md, docs/facts, crates/polint/tests/cli.rs, crates/polint/tests/public_surface_leak.rs</files>
  <read_first>
    - .planning/phases/55-sdk-query-vocabulary-and-preview-contract/55-CONTEXT.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - crates/polint/tests/cli.rs
    - crates/polint/tests/public_surface_leak.rs
  </read_first>
  <action>Run focused checks proving Phase 55 success criteria before the phase is marked executed. Use exact test names added during implementation where possible. Record any command that cannot run, the exact failure, and the remediation in the eventual phase summary; do not claim full query behavior exists.</action>
  <verify>
    <automated>cargo fmt --all --check</automated>
    <automated>cargo test -p polint --test cli phase55_preview_rule_syntax_compiles_and_fails_closed --locked</automated>
    <automated>cargo test -p polint --test public_surface_leak --locked</automated>
    <automated>cargo test -p polint-macros --locked</automated>
    <automated>cargo check -p polint --locked</automated>
  </verify>
  <acceptance_criteria>
    - Focused Phase 55 tests pass or exact failures are captured in the execution summary.
    - The implementation evidence maps API-01 through API-06 to source, docs, and tests.
    - Verification text distinguishes compile/manifest/capability proof from deferred query-result behavior.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs and examples -> user expectations | Preview text must not imply support for query behavior that is deferred. |
| public leak gate -> SDK boundary | Allow-list updates must not accidentally admit private internals. |
| temp repo -> external rule authors | Tests must behave like outside consumers, not privileged internal modules. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T55-06 | Information Disclosure | public-surface leak gate | Mitigated | Keep negative probes for raw graph, solver, provider, parser, `AnalysisDb`, and private ID types. |
| T55-07 | Spoofing | docs and examples | Mitigated | State preview and fail-closed status on every new facts page and avoid examples that imply completed query results. |
| T55-08 | Repudiation | external SDK validation | Mitigated | Add temp-repo JSON assertions for capability diagnostics and marker-diagnostic absence. |
</threat_model>

<verification>
- `cargo fmt --all --check`
- `cargo test -p polint --test cli phase55_preview_rule_syntax_compiles_and_fails_closed --locked`
- `cargo test -p polint --test public_surface_leak --locked`
- `cargo test -p polint-macros --locked`
- `cargo check -p polint --locked`
</verification>

<success_criteria>
Phase 55 has documented preview semantics, external temp-repo syntax proof, fail-closed capability proof, and public-boundary tests that admit only the intentional policy-query vocabulary.
</success_criteria>
