---
phase: 03-core-facts-and-diagnostics
plan: "03"
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - crates/polint-fs/Cargo.toml
  - crates/polint-fs/src/lib.rs
  - crates/polint-cli/tests/cli.rs
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/VERIFICATION.md
autonomous: true
requirements:
  - FS-02
  - CORE-01
  - CORE-02
  - DIAG-01
  - TEST-01
  - TEST-03
  - TEST-04
must_haves:
  truths:
    - "File discovery returns deterministic root-relative order."
    - "CLI JSON diagnostics remain deterministic across repeated runs."
    - "Full workspace verification passes after core, diagnostics, and discovery hardening."
    - "Planning status records close only the verified Phase 3 scope."
  artifacts:
    - path: "crates/polint-fs/src/lib.rs"
      provides: "Deterministic discovery ordering and tests"
    - path: "crates/polint-cli/tests/cli.rs"
      provides: "Cross-crate deterministic output regression test"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Reconciled Phase 3 requirement status"
  key_links:
    - from: "crates/polint-fs/src/lib.rs"
      to: "crates/polint-core/src/lib.rs"
      via: "load_analysis_files -> AnalysisDb::add_file"
      pattern: "add_file"
    - from: "crates/polint-cli/tests/cli.rs"
      to: "polint check --format json"
      via: "assert_cmd"
      pattern: "check.*--format.*json"
---

<objective>
Close Phase 3 by proving deterministic file discovery and cross-crate diagnostic output, then reconcile project status against verified evidence.

Purpose: `FS-02` and the phase-level determinism goal are only complete when discovery order, analysis DB file insertion, and CLI-visible diagnostic output are stable together.
Output: Focused filesystem/CLI tests, full workspace verification, and updated GSD status records.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-core-facts-and-diagnostics/03-CONTEXT.md
@.planning/phases/02-cli-config-and-discovery/02-VERIFICATION.md
@.planning/phases/03-core-facts-and-diagnostics/03-01-SUMMARY.md
@.planning/phases/03-core-facts-and-diagnostics/03-02-SUMMARY.md
@crates/polint-fs/Cargo.toml
@crates/polint-fs/src/lib.rs
@crates/polint-cli/tests/cli.rs
@crates/polint-cli/src/main.rs
@crates/polint-core/src/lib.rs
@crates/polint-diagnostics/src/lib.rs
</context>

<must_haves>
Truths:
- Discovery order is sorted by normalized root-relative path after include/exclude, `.gitignore`, and language filtering.
- Repeated `polint check --format json --fail-on none` runs over the same repo produce identical parsed JSON diagnostic arrays.
- Full workspace fmt, clippy, and tests pass after all Phase 3 changes.
- Requirements/status docs mark only verified Phase 3 scope complete and keep later Go/TS extraction, cache/performance, production SARIF, and broad CI command hardening open.

Artifacts:
- `crates/polint-fs/src/lib.rs` contains deterministic discovery tests and property coverage where useful.
- `crates/polint-cli/tests/cli.rs` contains a repeated-run JSON determinism integration test.
- `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, and `.planning/VERIFICATION.md` record the Phase 3 outcome.

Key links:
- `polint-fs::discover_files` feeds `polint-fs::load_analysis_files`.
- `load_analysis_files` inserts files into `AnalysisDb` in discovery order.
- CLI output order comes from parser diagnostics plus `run_rules` plus `dedupe_diagnostics`.
</must_haves>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove file discovery determinism</name>
  <files>crates/polint-fs/Cargo.toml, crates/polint-fs/src/lib.rs</files>
  <read_first>
    @.planning/phases/03-core-facts-and-diagnostics/03-CONTEXT.md
    @.planning/phases/02-cli-config-and-discovery/02-VERIFICATION.md
    @crates/polint-fs/src/lib.rs
    @crates/polint-config/src/lib.rs
    @crates/polint-core/src/lib.rs
  </read_first>
  <behavior>
    - Test `discovery_order_is_root_relative_and_stable_with_nested_files`: files created in unsorted nested order are returned sorted by normalized relative path.
    - Test `discovery_filters_before_sorting`: unsupported extensions, excluded files, and `.gitignore` matches are absent before asserting final sorted order.
    - Property test `discovery_include_exclude_decision_is_stable`: include/exclude matching is deterministic for generated relative paths and explicit excludes win over includes.
    - Test `load_analysis_files_preserves_discovery_order_in_file_ids`: `AnalysisDb::files()` uses discovery order so `FileId` assignment is deterministic per D-05 and D-21.
  </behavior>
  <action>
    Add `proptest.workspace = true` under `[dev-dependencies]` in `crates/polint-fs/Cargo.toml` if needed.

    In `crates/polint-fs/src/lib.rs`, keep Phase 2 discovery semantics intact per D-22: `.gitignore` remains honored, include/exclude glob behavior remains unchanged, and supported languages remain Go, TS, TSX, JS, and JSX. Do not broaden discovery behavior beyond deterministic ordering and testability.

    If helpful, extract a small `pub(crate)` helper such as `should_include_relative_path(include: &GlobSet, exclude: &GlobSet, relative_path: &str) -> bool` and use it from `discover_files`; this gives property tests a pure decision point without changing the public API. Keep final output sorted by `relative_path` after filtering per D-21.

    Add tests with temp directories that create files in intentionally unsorted order and assert exact relative-path vectors. Add a `load_analysis_files` test that checks `db.files()[0].relative_path`, `db.files()[0].id`, and subsequent file IDs.
  </action>
  <verify>
    <automated>cargo test -p polint-fs --lib discovery_order_is_root_relative_and_stable_with_nested_files</automated>
    <automated>cargo test -p polint-fs --lib discovery_filters_before_sorting</automated>
    <automated>cargo test -p polint-fs --lib load_analysis_files_preserves_discovery_order_in_file_ids</automated>
  </verify>
  <acceptance_criteria>
    `FS-02` has focused tests proving deterministic discovery order, filtering, and AnalysisDb file ID insertion order without changing Phase 2 discovery semantics.
  </acceptance_criteria>
  <done>`polint-fs` deterministic discovery tests pass and no unsupported language files are admitted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add repeated-run CLI output determinism coverage</name>
  <files>crates/polint-cli/tests/cli.rs</files>
  <read_first>
    @.planning/phases/03-core-facts-and-diagnostics/03-CONTEXT.md
    @crates/polint-cli/tests/cli.rs
    @crates/polint-cli/src/main.rs
    @crates/polint-fs/src/lib.rs
    @crates/polint-diagnostics/src/lib.rs
  </read_first>
  <behavior>
    - Test `check_json_output_is_deterministic_across_repeated_runs`: run `polint check --format json --fail-on none` three times over one temp repo and assert parsed `serde_json::Value` arrays are identical.
    - Test data uses a small mixed repo with unsorted file creation order and at least two TS/JS diagnostics so file ordering is observable.
    - The test asserts output order by diagnostic `file` fields, not by raw stdout byte comparison alone.
  </behavior>
  <action>
    Extend `crates/polint-cli/tests/cli.rs` using the existing `assert_cmd`, `tempfile`, `write_file`, and `stdout_json` patterns from Phase 2. Do not add broad clean/failing Go/TS fixture coverage here per D-26.

    Create a temp repo with `.polint.toml` enabling `examples/ts-no-raw-colors`, then create files in non-sorted order such as `src/z.tsx`, `src/a.ts`, and an excluded or ignored file. Run the check command three times with `--profile phase3 --format json --fail-on none`.

    Assert that each parsed JSON array is equal to the first run and that the visible diagnostic file list is sorted as `src/a.ts` before `src/z.tsx`. This ties `polint-fs`, `AnalysisDb` file IDs, diagnostics sorting, and CLI rendering together for D-05, D-16, D-20, and D-21.
  </action>
  <verify>
    <automated>cargo test -p polint-cli --test cli check_json_output_is_deterministic_across_repeated_runs</automated>
  </verify>
  <acceptance_criteria>
    Repeated CLI JSON checks over the same repository produce identical parsed diagnostics in deterministic file/range/rule order.
  </acceptance_criteria>
  <done>The focused CLI determinism integration test passes without adding Phase 4/5 fixture breadth or Phase 8 CI command coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Run full Phase 3 verification and reconcile status</name>
  <files>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/VERIFICATION.md</files>
  <read_first>
    @.planning/PROJECT.md
    @.planning/ROADMAP.md
    @.planning/REQUIREMENTS.md
    @.planning/STATE.md
    @.planning/phases/03-core-facts-and-diagnostics/03-CONTEXT.md
    @.planning/phases/03-core-facts-and-diagnostics/03-01-SUMMARY.md
    @.planning/phases/03-core-facts-and-diagnostics/03-02-SUMMARY.md
  </read_first>
  <action>
    Run the full workspace checks after Tasks 1 and 2:

    ```bash
    cargo fmt -- --check
    cargo clippy --workspace --all-targets -- -D warnings
    cargo test --workspace
    ```

    If and only if all checks pass, update planning status records for Phase 3. In `.planning/REQUIREMENTS.md`, mark `FS-02`, `CORE-01`, `CORE-02`, and `DIAG-01` complete. Keep `TEST-01`, `TEST-03`, and `TEST-04` scoped/in progress unless their full cross-phase definitions are completely satisfied; record Phase 3 evidence specifically for core/diagnostics/discovery unit tests, human/JSON snapshots, and span/diagnostic/discovery property tests.

    In `.planning/PROJECT.md`, move the verified Phase 3 active capabilities into Validated language without claiming Phase 4/5 extraction, Phase 7 cache/performance, Phase 8 SARIF/CLI hardening, or v2 exact semantics. In `.planning/ROADMAP.md`, add the Phase 3 plan list if missing and reflect this plan's completion state consistently with GSD conventions. In `.planning/VERIFICATION.md`, append a Phase 3 verification record with command names, pass/fail status, and the no-worktree policy from D-03.
  </action>
  <verify>
    <automated>cargo fmt -- --check</automated>
    <automated>cargo clippy --workspace --all-targets -- -D warnings</automated>
    <automated>cargo test --workspace</automated>
    <automated>rg -n "FS-02|CORE-01|CORE-02|DIAG-01|TEST-01|TEST-03|TEST-04" .planning/REQUIREMENTS.md .planning/PROJECT.md .planning/ROADMAP.md .planning/VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    Full workspace verification passes, Phase 3 source requirements are marked complete, broad test requirements are scoped honestly, and no deferred Phase 4/5/7/8/v2 work is overclaimed.
  </acceptance_criteria>
  <done>Verification evidence and planning status records match the implemented Phase 3 scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem to discovery | Repository paths, `.gitignore`, and config globs are untrusted local inputs to file selection. |
| Discovery to AnalysisDb | File order determines `FileId` assignment and downstream diagnostic order. |
| CLI output to automation | JSON diagnostics may be consumed by scripts expecting deterministic parseable output. |
| Verification to planning status | GSD docs must reflect verified behavior without overstating deferred work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering | `discover_files` include/exclude filtering | mitigate | Test exclude precedence and stable filtering before sorting; preserve Phase 2 glob semantics. |
| T-03-03-02 | Repudiation | CLI JSON output | mitigate | Add repeated-run parsed JSON equality test so deterministic output can be proven. |
| T-03-03-03 | Denial of Service | File walking | accept | Phase 3 only proves deterministic ordering; performance/caching/parallel walk hardening remains Phase 7 and current walker remains bounded by repo contents. |
| T-03-03-04 | Tampering | Planning status updates | mitigate | Require full fmt/clippy/test success before marking requirements complete or scoped. |
</threat_model>

<verification>
Run these after all tasks:

```bash
cargo fmt -- --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
```
</verification>

<success_criteria>
- `FS-02` is satisfied by deterministic discovery and cross-crate output tests.
- `CORE-01`, `CORE-02`, and `DIAG-01` are reflected as complete only after Plans 03-01 and 03-02 evidence is present.
- `TEST-01`, `TEST-03`, and `TEST-04` are recorded with Phase 3 evidence while preserving remaining cross-phase scope.
- The same temp repo produces identical parsed CLI JSON diagnostics across repeated runs.
- GSD status records remain truthful and do not claim deferred Go/TS extraction, cache/performance, production SARIF, or broader CLI hardening.
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-facts-and-diagnostics/03-03-SUMMARY.md`.
</output>
