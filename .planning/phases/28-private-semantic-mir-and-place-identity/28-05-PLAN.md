---
phase: 28-private-semantic-mir-and-place-identity
plan: 05
type: execute
wave: 4
depends_on: [28-02, 28-03, 28-04]
files_modified:
  - crates/polint/src/analysis/provider.rs
  - crates/polint/src/analysis/cache_key.rs
  - crates/polint/src/analysis/validate.rs
  - crates/polint/src/analysis/mod.rs
  - crates/polint/src/analysis_kernel/provider.rs
  - crates/polint/src/analysis_kernel/mod.rs
  - crates/polint/src/analysis_kernel/incremental/keys.rs
  - crates/polint/src/analysis_kernel/validation.rs
  - crates/polint/src/analysis_kernel/debug.rs
autonomous: true
requirements: [SAE-SEM-03]
requirements_addressed: [SAE-SEM-03]
must_haves:
  truths:
    - "A private `polint.semantic_mir` provider runs after symbol and topology facts and before downstream consumers."
    - "MIR/place cache identity includes provider/schema, source, lifecycle, config, plan/provider parameters, upstream syntax, semantic, topology, and absent extension/model/tool slots."
    - "Validation rejects duplicate stable keys, invalid spans, missing owners, dangling place/body references, malformed projections, unsupported rows without evidence, and provider precision-ceiling violations."
  artifacts:
    - path: "crates/polint/src/analysis/provider.rs"
      provides: "Semantic MIR provider derivation"
      contains: "derive_semantic_mir_with_cache_stats"
    - path: "crates/polint/src/analysis_kernel/provider.rs"
      provides: "polint.semantic_mir provider manifest"
      contains: "polint.semantic_mir"
    - path: "crates/polint/src/analysis_kernel/validation.rs"
      provides: "MIR/place validation integration"
      contains: "validate_semantic_mir"
  key_links:
    - from: "crates/polint/src/analysis_kernel/mod.rs"
      to: "crates/polint/src/analysis/provider.rs"
      via: "post-module-topology provider call"
      pattern: "derive_semantic_mir_with_cache_stats"
    - from: "crates/polint/src/analysis_kernel/incremental/keys.rs"
      to: "crates/polint/src/analysis/cache_key.rs"
      via: "semantic MIR layer key helper"
      pattern: "semantic_mir_layer_key"
---

<objective>
Wire semantic MIR into the private kernel with provider metadata, cache identity, validation, and test-only debug output.

Purpose: Language-specific lowering must become a real internal provider artifact with deterministic run reports and validation before eval fixtures or downstream phases consume it.
Output: `polint.semantic_mir` manifest, kernel call, output digest/cache stats row, cache key helper, validation diagnostics, and debug JSON rows.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/28-private-semantic-mir-and-place-identity/28-CONTEXT.md
@research/implementation-bootstrap/RECOMMENDED_IMPLEMENTATION.md
@research/abstract-interpretation/implementation/MIR-CONTRACT.md
@.planning/phases/21-provenance-precision-and-validation-metadata/21-CONTEXT.md
@.planning/phases/23-input-snapshots-and-cache-key-vocabulary/23-CONTEXT.md
@.planning/phases/24-persistent-layer-cache-for-existing-cheap-facts/24-05-SUMMARY.md
@.planning/phases/26-semantic-index-deepening/26-05-SUMMARY.md
@.planning/phases/27-layered-module-package-topology-graph/27-05-SUMMARY.md
@crates/polint/src/analysis_kernel/mod.rs
@crates/polint/src/analysis_kernel/provider.rs
@crates/polint/src/analysis_kernel/incremental/keys.rs
@crates/polint/src/analysis_kernel/validation.rs
@crates/polint/src/analysis_kernel/debug.rs
@crates/polint/src/analysis/mir/lower_go.rs
@crates/polint/src/analysis/mir/lower_ts.rs
@crates/polint/src/core/mod.rs

<interfaces>
Existing provider order after Phase 27:

```rust
polint.source -> polint.go.syntax -> polint.ts.syntax -> polint.module_graph -> polint.symbol_graph -> polint.module_topology -> polint.metrics
```

New provider must run after `polint.module_topology` and before `polint.metrics`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add semantic MIR provider derivation and kernel wiring</name>
  <files>crates/polint/src/analysis/provider.rs, crates/polint/src/analysis/mod.rs, crates/polint/src/analysis_kernel/provider.rs, crates/polint/src/analysis_kernel/mod.rs</files>
  <read_first>
    - crates/polint/src/analysis_kernel/provider.rs
    - crates/polint/src/analysis_kernel/mod.rs
    - crates/polint/src/analysis/mir/lower_go.rs
    - crates/polint/src/analysis/mir/lower_ts.rs
    - crates/polint/src/core/mod.rs
    - .planning/phases/28-private-semantic-mir-and-place-identity/28-CONTEXT.md
  </read_first>
  <behavior>
    - Test provider order is `polint.source`, `polint.go.syntax`, `polint.ts.syntax`, `polint.module_graph`, `polint.symbol_graph`, `polint.module_topology`, `polint.semantic_mir`, `polint.metrics`.
    - Test `KernelRunReport.provider_outputs` contains a `polint.semantic_mir` row with schema label, output digest, and cache stats.
    - Test a mixed Go and TS/JS kernel run stores non-empty MIR bodies and places when functions have bodies.
  </behavior>
  <action>Create `analysis::provider` with `pub(crate) fn derive_semantic_mir_with_cache_stats(db: &mut AnalysisDb, input_snapshot: &InputSnapshot, manifest: &ProviderManifest, module_topology_output_digest: Digest, symbol_graph_output_digest: Digest, upstream_syntax_output_digests: Vec<Digest>) -> SemanticMirProviderOutput`. `SemanticMirProviderOutput` must contain `diagnostics: Vec<Diagnostic>`, `cache_stats: CacheStats`, and `output_digest: Option<Digest>`. The function must call `lower_go_mir(db)` and `lower_ts_mir(db)`, merge outputs with deterministic sorting, call `db.replace_semantic_mir`, and compute an output digest from normalized body/place/operation/unsupported stable keys and payload fragments. Add provider manifest `polint.semantic_mir` with inputs `source_files`, `functions`, `symbols`, `references`, `scopes`, `semantic_imports`, `import_to_package_edges`, outputs `mir_bodies`, `mir_operations`, `places`, `unsupported_semantics`, schema `semantic-mir-facts-1:1`, language scope `MultiLanguage`, cache policy `InMemoryDerived`, and precision ceiling `SetupAware` per D-02 and D-20. Wire `AnalysisKernel::run` to call this provider after `polint.module_topology` and before metrics, passing upstream Go/TS syntax output digests, symbol graph output digest, and module topology output digest, then record the provider output row in `KernelRunReport`.</action>
  <verify>
    <automated>cargo test -p polint --lib semantic_mir_provider --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "derive_semantic_mir_with_cache_stats|SemanticMirProviderOutput|lower_go_mir|lower_ts_mir|replace_semantic_mir" crates/polint/src/analysis/provider.rs` returns matches.
    - `rg -n "polint.semantic_mir|semantic-mir-facts-1|mir_bodies|mir_operations|places|unsupported_semantics" crates/polint/src/analysis_kernel/provider.rs` returns matches.
    - `cargo test -p polint --lib provider_order --locked` passes with `polint.semantic_mir` between `polint.module_topology` and `polint.metrics`.
    - `cargo test -p polint --lib kernel_run_report_semantic_mir_row_carries_output_digest --locked` passes.
    - `cargo test -p polint --lib semantic_mir_provider --locked` passes.
  </acceptance_criteria>
  <done>The semantic MIR provider runs inside the private kernel, publishes internal run-report metadata, and stores normalized MIR/place artifacts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add semantic MIR cache identity vocabulary</name>
  <files>crates/polint/src/analysis/cache_key.rs, crates/polint/src/analysis/mod.rs, crates/polint/src/analysis_kernel/incremental/keys.rs</files>
  <read_first>
    - crates/polint/src/analysis_kernel/incremental/keys.rs
    - crates/polint/src/analysis_kernel/incremental/input_snapshot.rs
    - crates/polint/src/analysis_kernel/provider.rs
    - crates/polint/src/analysis/provider.rs
    - .planning/phases/23-input-snapshots-and-cache-key-vocabulary/23-CONTEXT.md
    - .planning/phases/28-private-semantic-mir-and-place-identity/28-CONTEXT.md
  </read_first>
  <behavior>
    - Test `semantic_mir_layer_key` changes when source digests, Go lifecycle, TS/JS lifecycle, config digest, plan/provider parameters, upstream syntax output digest, symbol output digest, or module topology output digest changes.
    - Test the key includes absent extension, model, and toolchain digests.
    - Test the key excludes rule code content except through the existing analysis-plan/provider parameter digest.
  </behavior>
  <action>Create `analysis::cache_key` with `semantic_mir_provider_parameter_digest()` over exact strings `mir_bodies`, `mir_operations`, `places`, `unsupported_semantics`, `go_lowering`, `ts_js_lowering`, and schema `semantic-mir-facts-1`. Extend `LayerKind` with `SemanticMir` and add `LayerKey::semantic_mir_layer_key(manifest, source_function_digests, config_digest, go_lifecycle_digest, ts_js_lifecycle_digest, upstream_syntax_output_digests, symbol_graph_output_digest, module_topology_output_digest, semantic_mir_parameter_digest)` per D-22. The key must include provider id/version/schema, source/function input digests, lifecycle digests, config digest, parameter digest, dependency layer digests for Go syntax, TS syntax, symbol graph, and module topology, plus absent `ExtensionCode`, absent model, and absent toolchain digests. Add focused key-diff tests under `semantic_mir_layer_key`.</action>
  <verify>
    <automated>cargo test -p polint --lib semantic_mir_layer_key --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "SemanticMir|semantic_mir_layer_key|semantic_mir_provider_parameter_digest|semantic-mir-facts-1" crates/polint/src/analysis/cache_key.rs crates/polint/src/analysis_kernel/incremental/keys.rs` returns matches.
    - `rg -n "ExtensionCode|model|toolchain|symbol_graph_output_digest|module_topology_output_digest" crates/polint/src/analysis_kernel/incremental/keys.rs crates/polint/src/analysis/cache_key.rs` returns matches.
    - `cargo test -p polint --lib semantic_mir_layer_key --locked` passes.
  </acceptance_criteria>
  <done>Semantic MIR has a deterministic internal layer-key vocabulary ready for cache persistence without changing public output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Validate MIR/place artifacts and expose test-only debug JSON</name>
  <files>crates/polint/src/analysis/validate.rs, crates/polint/src/analysis/mod.rs, crates/polint/src/analysis_kernel/validation.rs, crates/polint/src/analysis_kernel/debug.rs</files>
  <read_first>
    - crates/polint/src/analysis_kernel/validation.rs
    - crates/polint/src/analysis_kernel/debug.rs
    - crates/polint/src/analysis/mir/body.rs
    - crates/polint/src/analysis/mir/op.rs
    - crates/polint/src/analysis/places.rs
    - crates/polint/src/core/mod.rs
    - .planning/phases/21-provenance-precision-and-validation-metadata/21-CONTEXT.md
  </read_first>
  <behavior>
    - Test duplicate/conflicting stable keys for MIR bodies, operations, places, and unsupported rows produce deterministic `polint/internal` diagnostics.
    - Test invalid spans, missing owner function/file references, dangling body/place/op references, malformed projection chains, empty unknown evidence, and unsupported rows without construct/evidence/action are rejected.
    - Test `polint.semantic_mir` metadata with `FactPrecision::Exact` is rejected by provider precision-ceiling validation.
    - Test `metadata_debug_json_for_test` includes deterministic `mir.bodies`, `mir.operations`, `mir.places`, and `mir.unsupported` arrays sorted by path/span/stable key/run id.
  </behavior>
  <action>Create `analysis::validate` with `validate_semantic_mir(db: &AnalysisDb, ids: &IdSets, diagnostics: &mut Vec<Diagnostic>)` and call it from `analysis_kernel::validation::validate_fact_metadata` per D-21. Validate duplicate stable keys, invalid spans, missing owner references, dangling place/body/op references, malformed projection chains, unsupported rows without evidence, unsupported rows with empty affected domains, and `polint.semantic_mir` provider precision-ceiling violations. Diagnostics must use rule id `polint/internal` and evidence labels `family`, `stable_key`, `field`, and `reason`. Extend test-only `metadata_debug_json_for_test` with `mir.bodies`, `mir.operations`, `mir.places`, and `mir.unsupported` rows including stable key, producer id, layer id, status, precision, path, span, owner function, operation kind, place root/projections, and unsupported construct/conservative action. Debug rows must avoid raw source, absolute paths, parser AST dumps, dense IDs as stable identity, temp directories, and wall-clock data per D-05, D-10, D-16, D-20, D-21, and D-24.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis_kernel::validation::semantic_mir --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "validate_semantic_mir|MirBody|MirOperation|UnsupportedSemantic|malformed projection|precision ceiling" crates/polint/src/analysis/validate.rs crates/polint/src/analysis_kernel/validation.rs` returns matches.
    - `rg -n "mir\\.bodies|mir\\.operations|mir\\.places|mir\\.unsupported|SemanticMirDebug" crates/polint/src/analysis_kernel/debug.rs` returns matches.
    - `cargo test -p polint --lib analysis_kernel::validation::semantic_mir --locked` passes.
    - `cargo test -p polint --lib analysis_kernel::debug::semantic_mir_debug_json --locked` passes.
  </acceptance_criteria>
  <done>MIR/place validation and debug JSON make unsupported/unknown behavior observable internally and reject malformed artifacts before downstream consumers use them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| language lowering -> kernel provider | Lowered Go and TS/JS rows become shared internal provider output. |
| provider output -> cache identity | Cache keys must include all lifecycle, config, source, provider, and upstream dependency inputs that can affect MIR. |
| MIR/place artifacts -> validation/debug | Malformed or over-precise rows must become diagnostics before downstream phases consume them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-05-01 | T | Provider ordering | mitigate | Add provider order test placing `polint.semantic_mir` after `polint.module_topology` and before metrics. |
| T-28-05-02 | R | Cache identity | mitigate | Add `semantic_mir_layer_key` tests for source, lifecycle, config, provider parameters, upstream syntax, symbol, topology, and absent extension/model/tool slots. |
| T-28-05-03 | T | Malformed MIR/place rows | mitigate | Validate stable keys, spans, owner refs, dangling refs, projection chains, unsupported evidence, and precision ceilings. |
| T-28-05-04 | I | Debug JSON | mitigate | Keep debug JSON cfg(test)/crate-private and exclude raw source, parser AST dumps, absolute paths, temp roots, and wall-clock data. |
</threat_model>

<verification>
Run `cargo test -p polint --lib semantic_mir_provider --locked`, `cargo test -p polint --lib provider_order --locked`, `cargo test -p polint --lib kernel_run_report_semantic_mir_row_carries_output_digest --locked`, `cargo test -p polint --lib semantic_mir_layer_key --locked`, `cargo test -p polint --lib analysis_kernel::validation::semantic_mir --locked`, `cargo test -p polint --lib analysis_kernel::debug::semantic_mir_debug_json --locked`, and `cargo fmt --all -- --check`.
</verification>

<success_criteria>
`polint.semantic_mir` is a private provider with deterministic run-report metadata, cache-key vocabulary, validation diagnostics, and debug visibility, with no public surface promotion.
</success_criteria>

<output>
After completion, create `.planning/phases/28-private-semantic-mir-and-place-identity/28-05-SUMMARY.md`.
</output>
