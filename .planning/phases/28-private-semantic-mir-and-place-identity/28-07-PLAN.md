---
phase: 28-private-semantic-mir-and-place-identity
plan: 07
type: execute
wave: 6
depends_on: [28-06]
files_modified:
  - crates/polint/tests/cli.rs
autonomous: true
requirements: [SAE-SEM-03]
requirements_addressed: [SAE-SEM-03]
must_haves:
  truths:
    - "Public check JSON, inspect rule JSON, polint test JSON, CLI help, SDK, runner, crate root, and docs do not expose semantic MIR/place internals."
    - "Existing public rule-author behavior through `polint::sdk::prelude::*` and `polint::runner::run_cli` remains compatible."
    - "No public `Mir`, `Places`, `Cfg`, `CallGraph`, or `DataFlow` view is promoted in Phase 28."
  artifacts:
    - path: "crates/polint/tests/cli.rs"
      provides: "Public no-leak and compatibility integration test"
      contains: "semantic_mir_internals_stay_private"
  key_links:
    - from: "crates/polint/tests/cli.rs"
      to: "public CLI JSON"
      via: "no-leak assertions"
      pattern: "semantic_mir_internals_stay_private"
    - from: "crates/polint/tests/cli.rs"
      to: "polint::sdk::prelude::*"
      via: "external temp-repo compatibility rule"
      pattern: "run_cli"
---

<objective>
Prove Phase 28 public-boundary compatibility and no-leak behavior.

Purpose: MIR/place internals are valuable only if they do not accidentally become unsupported public contracts before validation and promotion gates.
Output: Public CLI/SDK/docs no-leak test and external temp-repo rule compatibility proof.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/28-private-semantic-mir-and-place-identity/28-CONTEXT.md
@AGENTS.md
@.claude/skills/polint/SKILL.md
@docs/API-VISIBILITY-PLAN.md
@crates/polint/tests/cli.rs
@crates/polint/src/sdk/mod.rs
@crates/polint/src/runner.rs
@docs/facts/symbols-and-references.md
@docs/facts/imports.md

<interfaces>
Supported public rule-author imports remain:

```rust
use polint::sdk::prelude::*;
polint::runner::run_cli(vec![rule()])
```

Unsupported Phase 28 names must remain absent from public output and source surfaces.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove semantic MIR internals stay private</name>
  <files>crates/polint/tests/cli.rs</files>
  <read_first>
    - crates/polint/tests/cli.rs
    - .claude/skills/polint/SKILL.md
    - AGENTS.md
    - docs/API-VISIBILITY-PLAN.md
    - docs/facts/symbols-and-references.md
    - docs/facts/imports.md
    - crates/polint/src/sdk/mod.rs
    - crates/polint/src/runner.rs
  </read_first>
  <behavior>
    - Test external temp-repo rule code imports only `polint::sdk::prelude::*`, registers through `polint::runner::run_cli`, requests existing supported public views, and produces a diagnostic.
    - Test `polint check --format json`, `polint inspect rule --format json`, and `polint test --format json` exclude semantic MIR provider ids, schema names, row family names, debug JSON section names, and proposed SDK view names.
    - Test CLI help, SDK source, runner source, crate root public exports, README, and docs exclude public MIR/place/CFG/call/dataflow query names.
  </behavior>
  <action>Add integration test `semantic_mir_internals_stay_private` in `crates/polint/tests/cli.rs` per D-01 and D-24. Use a temp repo with mixed Go and TS/JS sources and a local rule pack that imports only `polint::sdk::prelude::*`, registers through `polint::runner::run_cli`, and requests existing public views such as `Symbols<'_>`, `References<'_>`, `ResolvedImports<'_>`, or `ModuleGraphFacts<'_>`. Run `polint check --format json`, `polint inspect rule --format json`, and `polint test --format json`, then assert outputs do not contain exact forbidden markers: `polint.semantic_mir`, `semantic-mir-facts`, `MirBody`, `MirOperation`, `PlaceFact`, `UnsupportedSemanticFact`, `SemanticStore`, `analysis::mir`, `analysis::places`, `mir.bodies`, `mir.operations`, `mir.places`, `mir.unsupported`, `Mir<'_>`, `Places<'_>`, `Cfg<'_>`, `CallGraph<'_>`, `DataFlow<'_>`, `polint mir`, `polint facts`, and `polint explain`. Add source-surface scans over `README.md`, `docs`, `crates/polint/src/sdk`, `crates/polint/src/runner.rs`, `crates/polint/src/cli`, and the public section of `crates/polint/src/lib.rs` for the same markers, allowing matches only inside this test's forbidden-marker list and private implementation modules. Do not modify public docs to describe semantic MIR as supported behavior.</action>
  <verify>
    <automated>cargo test -p polint --test cli semantic_mir_internals_stay_private --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "semantic_mir_internals_stay_private|polint.semantic_mir|semantic-mir-facts|Mir<'_>|Places<'_>|CallGraph<'_>|DataFlow<'_>" crates/polint/tests/cli.rs` returns matches in assertions.
    - `rg -n "Mir<'_|Places<'_|Cfg<'_|CallGraph<'_|DataFlow<'_|polint mir|semantic-mir-facts|polint\\.semantic_mir" README.md docs crates/polint/src/sdk crates/polint/src/runner.rs crates/polint/src/cli` returns no matches outside the no-leak test assertions.
    - `cargo test -p polint --test cli semantic_mir_internals_stay_private --locked` passes.
    - `cargo test -p polint --test cli semantic_index_internals_stay_private --locked` passes.
    - `cargo test -p polint --test cli module_topology_internals_stay_private --locked` passes.
  </acceptance_criteria>
  <done>Semantic MIR/place internals stay out of public CLI, SDK, runner, crate-root, help, JSON, README, and docs surfaces while existing public rule behavior remains compatible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internal MIR/provider/debug names -> public CLI JSON | Private names must not leak into user-facing machine output. |
| internal modules -> SDK/runner/crate-root/docs | Unsupported analysis APIs must not become public contracts. |
| temp-repo rule pack -> public SDK contract | Compatibility proof must behave like an external rule author. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-07-01 | I | Public check/inspect/test JSON | mitigate | Add exact forbidden-marker assertions for provider id, schema, row families, debug sections, and proposed public views. |
| T-28-07-02 | I | SDK/runner/docs source surfaces | mitigate | Scan public source/doc surfaces for MIR/place/CFG/call/dataflow names and commands. |
| T-28-07-03 | R | External rule compatibility | mitigate | Compile and run a temp-repo rule pack using only `polint::sdk::prelude::*` and `polint::runner::run_cli`. |
| T-28-07-04 | E | Premature API promotion | mitigate | Do not add public MIR/place docs, CLI commands, SDK views, crate-root exports, or runner helpers. |
</threat_model>

<verification>
Run `cargo test -p polint --test cli semantic_mir_internals_stay_private --locked`, `cargo test -p polint --test cli semantic_index_internals_stay_private --locked`, `cargo test -p polint --test cli module_topology_internals_stay_private --locked`, the public-surface `rg` scans from acceptance criteria, and `cargo fmt --all -- --check`.
</verification>

<success_criteria>
Phase 28 public-boundary proof is complete: semantic MIR/place internals remain private while existing public rule-author workflows keep working.
</success_criteria>

<output>
After completion, create `.planning/phases/28-private-semantic-mir-and-place-identity/28-07-SUMMARY.md`.
</output>
