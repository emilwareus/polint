---
phase: 28-private-semantic-mir-and-place-identity
plan: 06
type: execute
wave: 5
depends_on: [28-05]
files_modified:
  - crates/polint/src/eval/model.rs
  - crates/polint/src/eval/observed.rs
  - crates/polint/src/eval/fixtures.rs
  - crates/polint/src/eval/matcher.rs
  - crates/polint/src/eval/metrics.rs
  - crates/polint/src/eval/report.rs
  - crates/polint/src/eval/mod.rs
  - tests/eval-fixtures/semantic-mir/core/expected.polint-eval.toml
  - tests/eval-fixtures/semantic-mir/core/repo/.polint.toml
  - tests/eval-fixtures/semantic-mir/core/repo/go.mod
  - tests/eval-fixtures/semantic-mir/core/repo/service.go
  - tests/eval-fixtures/semantic-mir/core/repo/web/package.json
  - tests/eval-fixtures/semantic-mir/core/repo/web/src/app.ts
autonomous: true
requirements: [SAE-SEM-03]
requirements_addressed: [SAE-SEM-03]
must_haves:
  truths:
    - "Internal eval observes MIR bodies, operations, places, and unsupported rows through crate-private debug data."
    - "A native semantic-MIR fixture proves deterministic Go and TS/JS lowering snapshots."
    - "Fixture assertions cover supported places and explicit unsupported semantics, not downstream CFG/domain results."
  artifacts:
    - path: "crates/polint/src/eval/observed.rs"
      provides: "MIR eval observation"
      contains: "semantic_mir_facts"
    - path: "tests/eval-fixtures/semantic-mir/core/expected.polint-eval.toml"
      provides: "Expected deterministic MIR/place fixture rows"
      contains: "semantic-mir-core"
    - path: "crates/polint/src/eval/fixtures.rs"
      provides: "Semantic MIR fixture runner"
      contains: "run_semantic_mir_core_fixture_for_test"
  key_links:
    - from: "crates/polint/src/eval/observed.rs"
      to: "crates/polint/src/analysis_kernel/debug.rs"
      via: "metadata_debug_json_for_test semantic MIR sections"
      pattern: "mir"
    - from: "tests/eval-fixtures/semantic-mir/core/expected.polint-eval.toml"
      to: "crates/polint/src/eval/fixtures.rs"
      via: "FixtureArea::SemanticMir"
      pattern: "semantic-mir"
---

<objective>
Add deterministic internal eval coverage for semantic MIR and place identity.

Purpose: Phase 28 success is proven by MIR/place snapshots for Go and TS/JS with unsupported semantics visible before later CFG/domains consume the rows.
Output: Eval model/observation support, semantic-MIR native fixture repo, expected rows, and deterministic fixture tests.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/28-private-semantic-mir-and-place-identity/28-CONTEXT.md
@research/abstract-interpretation/implementation/MIR-CONTRACT.md
@.planning/phases/22-internal-evaluation-harness-mvp/22-CONTEXT.md
@.planning/phases/26-semantic-index-deepening/26-06-SUMMARY.md
@.planning/phases/27-layered-module-package-topology-graph/27-06-SUMMARY.md
@crates/polint/src/eval/model.rs
@crates/polint/src/eval/observed.rs
@crates/polint/src/eval/fixtures.rs
@crates/polint/src/analysis_kernel/debug.rs
@tests/eval-fixtures/semantic-index/core/expected.polint-eval.toml
@tests/eval-fixtures/module-topology/core/expected.polint-eval.toml

<interfaces>
Existing eval observed fact shape:

```rust
pub(crate) struct ObservedFact {
    pub(crate) family: String,
    pub(crate) stable_key: String,
    pub(crate) producer_id: Option<String>,
    pub(crate) precision: Option<String>,
    pub(crate) status: Option<ObservedStatus>,
    pub(crate) payload: Option<String>,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Teach internal eval to observe semantic MIR rows</name>
  <files>crates/polint/src/eval/model.rs, crates/polint/src/eval/observed.rs, crates/polint/src/eval/matcher.rs, crates/polint/src/eval/metrics.rs, crates/polint/src/eval/report.rs</files>
  <read_first>
    - crates/polint/src/eval/model.rs
    - crates/polint/src/eval/observed.rs
    - crates/polint/src/eval/matcher.rs
    - crates/polint/src/eval/metrics.rs
    - crates/polint/src/eval/report.rs
    - crates/polint/src/analysis_kernel/debug.rs
    - .planning/phases/28-private-semantic-mir-and-place-identity/28-CONTEXT.md
  </read_first>
  <behavior>
    - Test observed facts include `MirBody`, `MirOperation`, `Place`, and `UnsupportedSemantic` families from test-only debug JSON.
    - Test statuses `present`, `resolved`, `partial`, `unknown`, and `unsupported` are serialized deterministically and unknown-like statuses contribute to unknown metrics.
    - Test payloads include compact path/span/owner/kind/root/projection/construct fragments without raw source, absolute paths, parser dumps, or dense IDs as stable identity.
  </behavior>
  <action>Add `SEMANTIC_MIR_FACT_FAMILIES` with exact labels `MirBody`, `MirOperation`, `Place`, and `UnsupportedSemantic` to `eval/model.rs`. Add `ObservedStatus::Partial` if absent. Extend `observed.rs` so `observe_kernel_fixture_repo_with_plan_for_test` reads `AnalysisKernel::metadata_debug_json_for_test(&output.db)["mir"]` and emits normalized `ObservedFact` rows from `mir.bodies`, `mir.operations`, `mir.places`, and `mir.unsupported`. Use producer id from metadata, provenance `kernel.metadata_debug_json.mir`, precision/status labels from debug rows, and compact payload fragments such as `path=...;span=...;owner=...;kind=...;root=...;projections=...;construct=...`. Update matcher/metrics/report so `Partial`, `Unknown`, and `Unsupported` semantic-MIR rows are counted consistently with existing unknown-like semantic/topology statuses. This implements D-16, D-19, D-20, D-23, and D-24 while keeping eval support crate-private/test-facing.</action>
  <verify>
    <automated>cargo test -p polint --lib eval::semantic_mir_rows --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "SEMANTIC_MIR_FACT_FAMILIES|MirBody|MirOperation|UnsupportedSemantic|ObservedStatus::Partial|semantic_mir_facts" crates/polint/src/eval/model.rs crates/polint/src/eval/observed.rs crates/polint/src/eval/metrics.rs crates/polint/src/eval/report.rs` returns matches.
    - `rg -n "raw_source|source_text|tree_sitter|oxc_ast|absolute" crates/polint/src/eval/observed.rs` returns no matches in semantic MIR observation code.
    - `cargo test -p polint --lib eval::semantic_mir_rows --locked` passes.
  </acceptance_criteria>
  <done>Internal eval can observe semantic MIR/place rows and score unknown/unsupported states deterministically without exposing a public eval schema.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add semantic-MIR native fixture with Go and TS/JS snapshots</name>
  <files>crates/polint/src/eval/fixtures.rs, crates/polint/src/eval/mod.rs, tests/eval-fixtures/semantic-mir/core/expected.polint-eval.toml, tests/eval-fixtures/semantic-mir/core/repo/.polint.toml, tests/eval-fixtures/semantic-mir/core/repo/go.mod, tests/eval-fixtures/semantic-mir/core/repo/service.go, tests/eval-fixtures/semantic-mir/core/repo/web/package.json, tests/eval-fixtures/semantic-mir/core/repo/web/src/app.ts</files>
  <read_first>
    - crates/polint/src/eval/fixtures.rs
    - crates/polint/src/eval/mod.rs
    - tests/eval-fixtures/semantic-index/core/expected.polint-eval.toml
    - tests/eval-fixtures/module-topology/core/expected.polint-eval.toml
    - crates/polint/src/analysis/mir/lower_go.rs
    - crates/polint/src/analysis/mir/lower_ts.rs
  </read_first>
  <behavior>
    - Test the fixture emits deterministic MIR body and operation rows for at least one Go function and one TS/JS function.
    - Test fixture expected facts assert parameter, local, global/unknown, temporary, field/property, index, call-return, and unknown-root places.
    - Test fixture expected facts assert explicit unsupported rows for one Go construct and one TS/JS construct.
    - Test repeated fixture runs produce identical normalized eval JSON and output hash.
  </behavior>
  <action>Add `FixtureArea::SemanticMir` serialized as `semantic-mir` and `run_semantic_mir_core_fixture_for_test(fixture_dir: &Path)` in `eval/fixtures.rs`. The runner must use an `AnalysisPlan` requesting `symbols`, `references`, `resolved_imports`, and `module_graph` so upstream semantic/topology inputs exist, then run cold and warm observations and compare normalized JSON/output hash for determinism. Create `tests/eval-fixtures/semantic-mir/core/` with `.polint.toml`, `go.mod`, `service.go`, `web/package.json`, and `web/src/app.ts`. The Go fixture must include parameters, local declaration, package/global assignment, field/index access, branch, return, normal call, and one unsupported construct such as `defer` or goroutine. The TS/JS fixture must include parameters, local declaration, property/index access, branch/conditional, return, normal call, call return, and one unsupported construct such as `eval`, optional chaining, await, or destructuring. In `expected.polint-eval.toml`, assert `MirBody`, `MirOperation`, `Place`, and `UnsupportedSemantic` expected facts using partial stable-key fragments and explicit statuses/precision. Do not assert CFG edges, dominance, call targets, direct call facts, abstract-domain states, summaries, or public SDK views.</action>
  <verify>
    <automated>cargo test -p polint --lib eval::fixtures::semantic_mir_core --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "SemanticMir|semantic-mir|run_semantic_mir_core_fixture_for_test" crates/polint/src/eval/fixtures.rs crates/polint/src/eval/model.rs crates/polint/src/eval/mod.rs` returns matches.
    - `test -f tests/eval-fixtures/semantic-mir/core/expected.polint-eval.toml` succeeds.
    - `rg -n "MirBody|MirOperation|Place|UnsupportedSemantic|semantic-mir-core|defer|eval|CallReturn|Index" tests/eval-fixtures/semantic-mir/core/expected.polint-eval.toml tests/eval-fixtures/semantic-mir/core/repo` returns matches.
    - `cargo test -p polint --lib eval::fixtures::semantic_mir_core --locked` passes.
  </acceptance_criteria>
  <done>The native eval fixture proves deterministic Go and TS/JS MIR/place snapshots and explicit unsupported semantics for Phase 28.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| debug JSON -> eval rows | Test-only debug output becomes eval evidence and must stay deterministic and source-safe. |
| fixture repo -> kernel execution | Fixture copying must not mutate checked-in repos or follow symlink escapes. |
| eval fixture assertions -> product claims | Fixture must assert Phase 28 MIR/place behavior only, not deferred CFG/calls/domain behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-06-01 | I | Eval observed payloads | mitigate | Emit compact payload fragments only; exclude raw source, absolute paths, parser dumps, temp roots, and wall-clock data. |
| T-28-06-02 | T | Fixture repo copy | mitigate | Reuse existing native fixture copy path that rejects symlinks and parent traversal. |
| T-28-06-03 | R | Snapshot determinism | mitigate | Compare cold/warm normalized eval JSON and output hash for semantic-MIR fixture runs. |
| T-28-06-04 | E | Scope creep into later phases | mitigate | Expected fixture rows assert MIR/place/unsupported facts only and explicitly avoid CFG, direct-call targets, domains, summaries, and public views. |
</threat_model>

<verification>
Run `cargo test -p polint --lib eval::semantic_mir_rows --locked`, `cargo test -p polint --lib eval::fixtures::semantic_mir_core --locked`, and `cargo fmt --all -- --check`.
</verification>

<success_criteria>
Internal eval can observe semantic MIR/place rows, and the semantic-MIR fixture proves deterministic Go and TS/JS snapshots with explicit unsupported semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/28-private-semantic-mir-and-place-identity/28-06-SUMMARY.md`.
</output>
