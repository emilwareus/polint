---
phase: 28-private-semantic-mir-and-place-identity
plan: 02
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - crates/polint/src/analysis/store.rs
  - crates/polint/src/analysis/mod.rs
  - crates/polint/src/core/mod.rs
  - crates/polint/src/analysis_kernel/metadata.rs
autonomous: true
requirements: [SAE-SEM-03]
requirements_addressed: [SAE-SEM-03]
must_haves:
  truths:
    - "AnalysisDb can own private semantic MIR/place artifacts through a small SemanticStore boundary."
    - "All MIR body, MIR operation, place, and unsupported rows have Phase 21 metadata."
    - "Replacement APIs normalize rows deterministically and remove stale semantic MIR metadata before inserting new rows."
  artifacts:
    - path: "crates/polint/src/analysis/store.rs"
      provides: "SemanticStore and replacement boundary"
      contains: "pub(crate) struct SemanticStore"
    - path: "crates/polint/src/core/mod.rs"
      provides: "AnalysisDb semantic MIR storage and accessors"
      contains: "replace_semantic_mir"
    - path: "crates/polint/src/analysis_kernel/metadata.rs"
      provides: "MIR/place fact families"
      contains: "MirBody"
  key_links:
    - from: "crates/polint/src/core/mod.rs"
      to: "crates/polint/src/analysis/store.rs"
      via: "SemanticStore ownership"
      pattern: "SemanticStore"
    - from: "crates/polint/src/core/mod.rs"
      to: "crates/polint/src/analysis_kernel/metadata.rs"
      via: "FactFamily metadata refresh"
      pattern: "FactFamily::MirBody"
---

<objective>
Store Phase 28 MIR and place artifacts behind a private semantic store and metadata refresh path.

Purpose: MIR/place lowering must be available to downstream internal providers while keeping `AnalysisDb` from becoming the semantic engine and keeping public fact views unchanged.
Output: `SemanticStore`, `AnalysisDb::replace_semantic_mir`, crate-private accessors, metadata family variants, and storage tests.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/28-private-semantic-mir-and-place-identity/28-CONTEXT.md
@research/implementation-bootstrap/RECOMMENDED_IMPLEMENTATION.md
@research/abstract-interpretation/implementation/MIR-CONTRACT.md
@.planning/phases/21-provenance-precision-and-validation-metadata/21-CONTEXT.md
@.planning/phases/26-semantic-index-deepening/26-01-SUMMARY.md
@.agents/skills/rust-best-practices/SKILL.md
@crates/polint/src/core/mod.rs
@crates/polint/src/analysis_kernel/metadata.rs
@crates/polint/src/analysis/mir/body.rs
@crates/polint/src/analysis/places.rs

<interfaces>
Existing replacement pattern to mirror:

```rust
pub(crate) fn replace_semantic_index_facts(...)
pub(crate) fn missing_fact_metadata(&self) -> Vec<MissingFactMeta>;
```

New contracts from Plan 28-01:

```rust
pub(crate) struct MirOutput { ... }
pub(crate) struct PlaceFact { ... }
pub(crate) struct UnsupportedSemanticFact { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SemanticStore and deterministic MIR replacement API</name>
  <files>crates/polint/src/analysis/store.rs, crates/polint/src/analysis/mod.rs, crates/polint/src/core/mod.rs</files>
  <read_first>
    - crates/polint/src/analysis/mir/body.rs
    - crates/polint/src/analysis/mir/op.rs
    - crates/polint/src/analysis/places.rs
    - crates/polint/src/core/mod.rs
    - .planning/phases/28-private-semantic-mir-and-place-identity/28-CONTEXT.md
  </read_first>
  <behavior>
    - Test replacing MIR twice removes the first run's bodies, operations, places, and unsupported rows.
    - Test row IDs are reassigned by deterministic stable-key order on replacement.
    - Test dangling operation body/place references are not silently accepted by store normalization.
  </behavior>
  <action>Create `analysis::store` with `SemanticStore { mir_bodies, mir_operations, places, unsupported_semantics }` and borrowed accessors `mir_bodies()`, `mir_operations()`, `places()`, and `unsupported_semantics()`. Register it from `analysis/mod.rs` as `pub(crate) mod store;`. Add `semantic: Option<SemanticStore>` to `AnalysisDb` and implement `replace_semantic_mir(output: MirOutput)` per D-03, D-04, D-08, and D-20. The replacement must call `MirOutput::normalized`, rebuild indexes by `MirBodyId`, `MirOpId`, `PlaceId`, and `UnsupportedId`, and expose crate-private `semantic_store()`, `mir_bodies()`, `mir_operations()`, `mir_places()`, and `unsupported_semantics()` accessors. Do not add public SDK fact views or `RuleCtx` accessors.</action>
  <verify>
    <automated>cargo test -p polint --lib semantic_mir_storage --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "pub\\(crate\\) struct SemanticStore|replace_semantic_mir|semantic_store\\(|mir_bodies\\(|mir_operations\\(|mir_places\\(|unsupported_semantics\\(" crates/polint/src/analysis/store.rs crates/polint/src/core/mod.rs` returns matches.
    - `rg -n "pub fn .*mir|pub struct Mir|pub enum Mir|pub use .*Mir|RuleCtx.*mir" crates/polint/src/sdk crates/polint/src/runner.rs crates/polint/src/core/mod.rs` returns no matches for new public MIR accessors.
    - `cargo test -p polint --lib semantic_mir_storage --locked` passes.
  </acceptance_criteria>
  <done>AnalysisDb owns semantic MIR artifacts through a narrow private store and deterministic replacement API.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Attach metadata to MIR, place, and unsupported rows</name>
  <files>crates/polint/src/core/mod.rs, crates/polint/src/analysis_kernel/metadata.rs</files>
  <read_first>
    - crates/polint/src/core/mod.rs
    - crates/polint/src/analysis_kernel/metadata.rs
    - crates/polint/src/analysis_kernel/validation.rs
    - crates/polint/src/analysis/mir/body.rs
    - crates/polint/src/analysis/places.rs
    - .planning/phases/21-provenance-precision-and-validation-metadata/21-CONTEXT.md
  </read_first>
  <behavior>
    - Test `replace_semantic_mir` records metadata for every MIR body, MIR operation, place, and unsupported row.
    - Test `missing_fact_metadata` reports these families if metadata refresh is bypassed in a focused test.
    - Test metadata precision never claims `FactPrecision::Exact` for `polint.semantic_mir` rows.
  </behavior>
  <action>Extend `FactFamily` with exact variants `MirBody`, `MirOperation`, `Place`, and `UnsupportedSemantic` and labels matching those names per D-20. Add `SEMANTIC_MIR_PROVIDER_ID: &str = "polint.semantic_mir"` in `core/mod.rs` and metadata refresh helpers for the four new families. Map `MirStatus::Present`/`PlaceStatus::Resolved` to `FactPrecision::Syntax` or `SetupAware` only when backed by semantic owner context, never `Exact`; map `Unknown` to `FactPrecision::Unresolved`/`Low`; map `Unsupported` to `FactPrecision::Unsupported`/`Low`; map `Partial` to `FactPrecision::Heuristic`/`Medium`. Use validation `NativeTrusted` for native lowering rows. Extend `missing_fact_metadata` to include MIR bodies, operations, places, and unsupported rows. This implements D-19 and D-20 without widening public fact structs.</action>
  <verify>
    <automated>cargo test -p polint --lib semantic_mir_metadata --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "MirBody|MirOperation|Place|UnsupportedSemantic" crates/polint/src/analysis_kernel/metadata.rs` returns all four family variants and labels.
    - `rg -n "polint.semantic_mir|refresh_semantic_mir_metadata|FactFamily::MirBody|FactFamily::MirOperation|FactFamily::Place|FactFamily::UnsupportedSemantic" crates/polint/src/core/mod.rs` returns matches.
    - `rg -n "FactPrecision::Exact" crates/polint/src/core/mod.rs | rg "semantic_mir|MirBody|MirOperation|UnsupportedSemantic|Place"` returns no matches.
    - `cargo test -p polint --lib semantic_mir_metadata --locked` passes.
  </acceptance_criteria>
  <done>Every stored MIR/place/unsupported row receives internal metadata with honest precision, confidence, validation, and stable-key ownership.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Guard private boundaries for stored MIR artifacts</name>
  <files>crates/polint/src/core/mod.rs, crates/polint/src/analysis/store.rs</files>
  <read_first>
    - crates/polint/src/core/mod.rs
    - crates/polint/src/analysis/store.rs
    - crates/polint/src/sdk/mod.rs
    - crates/polint/src/sdk/facts.rs
    - AGENTS.md
    - docs/API-VISIBILITY-PLAN.md
  </read_first>
  <behavior>
    - Test SDK prelude source does not contain `Mir`, `Place`, `SemanticStore`, or `UnsupportedSemantic`.
    - Test crate root exposes `analysis` only through `pub(crate)` and not through `_bench`.
    - Test stored MIR accessors are unavailable outside crate-private code by grep-based source assertions.
  </behavior>
  <action>Add source-surface tests under `semantic_mir_storage` or an adjacent `public_boundary` test module. Assert `crates/polint/src/sdk/mod.rs`, `crates/polint/src/sdk/facts.rs`, `crates/polint/src/runner.rs`, and the crate-root `_bench` module do not contain `MirBody`, `MirOperation`, `PlaceFact`, `SemanticStore`, `UnsupportedSemanticFact`, `polint.semantic_mir`, or `semantic-mir-facts`. Keep all new accessors `pub(crate)` and do not add public docs under `docs/facts/` for MIR or places per D-01 and D-24.</action>
  <verify>
    <automated>cargo test -p polint --lib semantic_mir_storage_public_boundary --locked</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "semantic_mir_storage_public_boundary" crates/polint/src/core/mod.rs crates/polint/src/analysis/store.rs` returns a test name.
    - `rg -n "MirBody|MirOperation|PlaceFact|SemanticStore|UnsupportedSemanticFact|polint.semantic_mir|semantic-mir-facts" crates/polint/src/sdk crates/polint/src/runner.rs docs/facts README.md` returns no matches.
    - `cargo test -p polint --lib semantic_mir_storage_public_boundary --locked` passes.
  </acceptance_criteria>
  <done>Stored MIR/place artifacts stay private, and tests guard against accidental SDK, runner, crate-root, bench, or docs leakage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| semantic store -> AnalysisDb | Internal MIR artifacts are stored beside public-compatible facts without becoming public facts. |
| AnalysisDb metadata -> validation | New families must participate in missing-metadata and stable-key conflict checks. |
| stored MIR -> SDK/runner/docs | Stored MIR accessors must remain crate-private. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-02-01 | T | `replace_semantic_mir` | mitigate | Normalize rows, rebuild run-local IDs, and remove stale metadata on every replacement. |
| T-28-02-02 | R | MIR metadata | mitigate | Add FactFamily variants, missing-metadata coverage, stable-key ownership, and precision mapping tests. |
| T-28-02-03 | I | SDK/runner/docs boundary | mitigate | Add grep-based source-surface tests for `MirBody`, `PlaceFact`, `SemanticStore`, and provider/schema names. |
| T-28-02-04 | T | Precision claims | mitigate | Reject or test against `FactPrecision::Exact` for `polint.semantic_mir` syntax/semantic-lowering rows. |
</threat_model>

<verification>
Run `cargo test -p polint --lib semantic_mir_storage --locked`, `cargo test -p polint --lib semantic_mir_metadata --locked`, `cargo test -p polint --lib semantic_mir_storage_public_boundary --locked`, and `cargo fmt --all -- --check`.
</verification>

<success_criteria>
Private MIR/place artifacts are stored in a deterministic semantic store with metadata coverage, missing-metadata detection, honest precision, and no public access path.
</success_criteria>

<output>
After completion, create `.planning/phases/28-private-semantic-mir-and-place-identity/28-02-SUMMARY.md`.
</output>
