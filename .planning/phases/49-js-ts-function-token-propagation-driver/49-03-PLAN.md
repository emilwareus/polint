---
phase: 49-js-ts-function-token-propagation-driver
plan: 03
type: execute
wave: 3
depends_on: [49-02]
files_modified:
  - tests/eval-fixtures/ts-tokens/alias-parameter-return/expected.polint-eval.toml
  - tests/eval-fixtures/ts-tokens/alias-parameter-return/repo/src/app.ts
  - tests/eval-fixtures/ts-tokens/token-explosion/expected.polint-eval.toml
  - tests/eval-fixtures/ts-tokens/token-explosion/repo/src/app.ts
  - tests/eval-fixtures/ts-tokens/token-explosion/repo/.polint.toml
  - tests/eval-fixtures/polyglot-canary/go-ts/expected.polint-eval.toml
  - tests/eval-fixtures/polyglot-canary/go-ts/repo/tokens.ts
  - tests/eval-fixtures/determinism/ts_tokens/expected.polint-eval.toml
  - tests/eval-fixtures/determinism/ts_tokens/repo/src/app.ts
  - crates/polint/src/eval/ts_tokens.rs
  - crates/polint/src/eval/determinism_gate.rs
  - crates/polint/src/eval/mod.rs
  - crates/polint/tests/public_surface_leak.rs
  - .planning/ROADMAP.md
autonomous: true
requirements: [JS-04]

must_haves:
  truths:
    - "D-22/D-23/D-24: Native TS fixtures prove alias, assignment, parameter, return, closure propagation, plus token-explosion memory/budget behavior."
    - "D-25: Jelly-focused evidence records recall improvement without precision flooding; Phase 54 still owns hard promotion floors."
    - "D-26: The polyglot Go+TS canary now proves legitimate intra-TS token edges while preserving no Go<->TS cross-language edges and unchanged Go RTA behavior."
    - "D-27: Phase 43 determinism and Phase 42 public-surface leak gates are green; ALLOWED_PRELUDE is not extended."
  artifacts:
    - path: "crates/polint/src/eval/ts_tokens.rs"
      provides: "crate-private native gate for TS token fixtures, budget sentinel, and Jelly/polyglot assertions"
      contains: "too-many-tokens"
    - path: "tests/eval-fixtures/ts-tokens/token-explosion/expected.polint-eval.toml"
      provides: "BudgetExceeded and bounded-output proof for token explosion"
      contains: "budget"
    - path: "tests/eval-fixtures/polyglot-canary/go-ts/expected.polint-eval.toml"
      provides: "updated canary: TS token policy derives intra-TS edges; no Go<->TS edge"
      contains: "ts_tokens"
---

<objective>
Close JS-04 with executable proof: native TS token fixtures, token-explosion budget/memory proof, Jelly recall/precision evidence, updated polyglot canary, determinism gate, leak gate, and roadmap bookkeeping.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/49-js-ts-function-token-propagation-driver/49-CONTEXT.md
@.planning/phases/49-js-ts-function-token-propagation-driver/49-01-PLAN.md
@.planning/phases/49-js-ts-function-token-propagation-driver/49-02-PLAN.md
@.planning/phases/48-go-rta-driver/48-03-SUMMARY.md
@AGENTS.md

<interfaces>
- Native fixtures live under `tests/eval-fixtures/<family>/<case>/` with `expected.polint-eval.toml` and `repo/`.
- `tests/eval-fixtures/polyglot-canary/go-ts/expected.polint-eval.toml` currently documents `TsTokensPolicy` as a stub; Phase 49 must update this expectation.
- `eval/determinism_gate.rs` has fixture helpers and 10-seed byte-identity tests.
- `public_surface_leak.rs` must pass without adding solver/token names to `ALLOWED_PRELUDE`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add native TS token-flow fixture gate</name>
  <files>tests/eval-fixtures/ts-tokens/alias-parameter-return/, crates/polint/src/eval/ts_tokens.rs, crates/polint/src/eval/mod.rs</files>
  <read_first>
    - crates/polint/src/eval/fixtures.rs
    - crates/polint/src/eval/go_rta.rs
    - crates/polint/src/eval/semantic_graph_snapshot.rs
    - tests/eval-fixtures/semantic-graph/ts_direct_bindings/expected.polint-eval.toml
    - tests/eval-fixtures/jelly/ts-inventory-spans/expected.polint-eval.toml
  </read_first>
  <action>
    Create `tests/eval-fixtures/ts-tokens/alias-parameter-return/` with a minimal TS repo proving alias, assignment, parameter, return, and closure token propagation. Add `crates/polint/src/eval/ts_tokens.rs` as a crate-private test gate that runs the fixture and inspects stored solver-derived edges to assert token-policy edges exist for the expected callsites. Register the module in `eval/mod.rs` using the same `#[cfg(test)]` style as nearby eval gates.
  </action>
  <verify>
    <automated>cargo test -p polint eval::ts_tokens::alias_parameter_return 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - The fixture includes a direct alias call, parameter callback call, return-of-function call, and closure-captured function call.
    - The eval gate asserts at least one `ts_tokens`-produced solver edge for each covered flow class.
    - A property/prototype/this case remains unresolved in the fixture or a sibling assertion.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add token-explosion BudgetExceeded and bounded-memory proof</name>
  <files>tests/eval-fixtures/ts-tokens/token-explosion/, crates/polint/src/eval/ts_tokens.rs</files>
  <read_first>
    - crates/polint/src/analysis/solver/ts_tokens/fixpoint.rs
    - crates/polint/src/analysis/solver/provider.rs
    - crates/polint/src/eval/performance.rs
    - tests/eval-fixtures/go-rta/iteration-cap/expected.polint-eval.toml
  </read_first>
  <action>
    Create a token-explosion fixture with many possible function tokens flowing into one variable and a tight `[solver.js] max_tokens_per_var` in `.polint.toml`. The eval gate must assert the `"too-many-tokens"` sentinel is present as a budget/sentinel signal, `BudgetStatus::BudgetExceeded` is observed, and no sentinel is emitted as a real target function. Add a bounded-output or RSS ceiling assertion using the existing eval performance helpers if available; otherwise assert bounded derived-edge count and document RSS measurement as not exposed by the current harness.
  </action>
  <verify>
    <automated>cargo test -p polint eval::ts_tokens::token_explosion 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Fixture-local `[solver.js] max_tokens_per_var` forces overflow deterministically.
    - The observed result includes `"too-many-tokens"` only as sentinel/status/evidence.
    - `BudgetExceeded` is observed.
    - Derived edge count remains bounded under the tight cap.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update polyglot canary and add TS token determinism fixture</name>
  <files>tests/eval-fixtures/polyglot-canary/go-ts/, tests/eval-fixtures/determinism/ts_tokens/, crates/polint/src/eval/determinism_gate.rs, crates/polint/src/eval/ts_tokens.rs</files>
  <read_first>
    - tests/eval-fixtures/polyglot-canary/go-ts/expected.polint-eval.toml
    - tests/eval-fixtures/polyglot-canary/go-ts/repo/tokens.ts
    - tests/eval-fixtures/determinism/ts_reachable/expected.polint-eval.toml
    - crates/polint/src/eval/determinism_gate.rs
  </read_first>
  <action>
    Update the polyglot canary text and assertions: it should now assert legitimate intra-TS token-policy derived edges, no derived edge crosses the Go/TS language boundary, and Go RTA edges still resolve. Add `tests/eval-fixtures/determinism/ts_tokens/` with a TS repo whose token-flow inputs exercise ordering-sensitive aliases/calls. Add determinism tests in `determinism_gate.rs` mirroring existing fixtures: 10 seeded provider-order/row-order permutations produce byte-identical observed JSON.
  </action>
  <verify>
    <automated>cargo test -p polint eval::determinism_gate::ts_tokens eval::ts_tokens::polyglot 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - Polyglot canary asserts `ts_tokens` intra-TS edges are present.
    - Polyglot canary asserts zero Go-to-TS and zero TS-to-Go derived edges.
    - Go RTA canary assertion remains green.
    - `ts_tokens` determinism fixture is byte-identical across 10 permutations.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Jelly evidence, leak gate, full-suite sweep, and roadmap plan list</name>
  <files>crates/polint/src/eval/ts_tokens.rs, crates/polint/tests/public_surface_leak.rs, .planning/ROADMAP.md</files>
  <read_first>
    - crates/polint/src/eval/external/jelly_callgraph.rs
    - research/evaluation-harness/GRAPH-ENGINE-BENCHMARK-RESEARCH.md
    - crates/polint/tests/public_surface_leak.rs
    - .planning/ROADMAP.md
  </read_first>
  <action>
    Add a Jelly-focused assertion or documented eval output in `eval::ts_tokens` showing aggregate Jelly fixture recall improves from token propagation without precision flooding. If the full external Jelly corpus is unavailable locally, use the self-contained fixture metrics and document the unavailable corpus behavior the same way Go x/tools adapters degrade gracefully. Run public-surface leak gate without editing `ALLOWED_PRELUDE`. Run full `cargo test -p polint` and `cargo clippy -p polint --all-targets`. Update `.planning/ROADMAP.md` Phase 49 `**Plans:**` from `TBD` to the three plan list with brief objectives.
  </action>
  <verify>
    <automated>cargo test -p polint 2>&1 | tail -30 && cargo clippy -p polint --all-targets 2>&1 | tail -20 && cargo test -p polint --test public_surface_leak 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Jelly-focused evidence records recall improvement or an explicit local-corpus-unavailable note plus self-contained fixture metrics.
    - `public_surface_leak` passes with `ALLOWED_PRELUDE` unchanged.
    - Full `cargo test -p polint` passes.
    - `cargo clippy -p polint --all-targets` passes with no new warnings.
    - `.planning/ROADMAP.md` lists 49-01, 49-02, and 49-03 objectives.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Risk | Mitigation |
|----------|------|------------|
| Benchmark evidence | Recall could be inflated by broad or cross-language edges. | Assert precision/non-cross-language invariants and keep Phase 54 hard floors separate. |
| Test fixtures | Fixtures could prove only hand-picked cases and miss regressions. | Include determinism, token-explosion, polyglot, Jelly-oriented, and full-suite checks. |
| Public surface | Private solver internals could leak through SDK/prelude. | Run `public_surface_leak` and fix visibility instead of editing allowlist. |

## STRIDE Notes

- Tampering: fixture assertions check provenance-backed derived edges rather than text-only output.
- Denial of service: token-explosion fixture proves bounded output and budget signaling.
- Information disclosure: no public SDK/CLI exposure is added for token internals.
</threat_model>

<verification>
- `cargo test -p polint eval::ts_tokens`
- `cargo test -p polint eval::determinism_gate`
- `cargo test -p polint --test public_surface_leak`
- `cargo test -p polint`
- `cargo clippy -p polint --all-targets`
</verification>

<success_criteria>
- Native TS token fixtures prove JS-04 behavior.
- Token explosion produces `"too-many-tokens"` and `BudgetExceeded` without unbounded output.
- Polyglot canary proves intra-TS token edges and no cross-language interference.
- Determinism and leak gates are green.
- ROADMAP Phase 49 lists the three plans.
</success_criteria>
