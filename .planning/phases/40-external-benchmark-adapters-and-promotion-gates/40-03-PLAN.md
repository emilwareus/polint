---
phase: 40-external-benchmark-adapters-and-promotion-gates
plan: 03
title: Provider Cache Performance And Report Output
type: execute
wave: 3
depends_on: [40-01]
files_modified:
  - crates/polint/src/eval/report.rs
  - crates/polint/src/eval/metrics.rs
  - crates/polint/src/eval/observed.rs
  - crates/polint/src/eval/performance.rs
  - crates/polint/src/eval/markdown.rs
autonomous: true
requirements: [SAE-PROM-01]
must_haves:
  truths:
    - "D-22/D-23: deterministic JSON is the source of truth and Markdown summaries are derived."
    - "D-24/D-27: reports include provider/cache/runtime evidence needed for promotion gates."
  artifacts:
    - path: "crates/polint/src/eval/performance.rs"
      provides: "Provider/cache/runtime projection for eval reports"
    - path: "crates/polint/src/eval/markdown.rs"
      provides: "Derived human summary tables from deterministic eval JSON"
---

<objective>
Extend internal eval reports with provider output metadata, cache stats, runtime/performance summaries, suite-native metric maps, and generated Markdown summaries.
</objective>

<execution_context>
@.planning/phases/40-external-benchmark-adapters-and-promotion-gates/40-CONTEXT.md
@research/evaluation-harness/RECOMMENDED_IMPLEMENTATION.md
@research/evaluation-harness/algorithms/benchmark-scheduling.md
@.agents/skills/rust-best-practices/SKILL.md
@AGENTS.md
</execution_context>

<context>
@crates/polint/src/eval/report.rs
@crates/polint/src/eval/metrics.rs
@crates/polint/src/eval/observed.rs
@crates/polint/src/analysis_kernel/incremental/run_report.rs
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Project KernelRunReport provider/cache rows into eval reports</name>
  <files>crates/polint/src/eval/performance.rs, crates/polint/src/eval/observed.rs, crates/polint/src/eval/report.rs</files>
  <read_first>
    - crates/polint/src/analysis_kernel/incremental/run_report.rs
    - crates/polint/src/analysis_kernel/incremental/stats.rs
    - crates/polint/src/eval/observed.rs
  </read_first>
  <action>Add crate-private report structs for provider stats and cache stats sourced from `KernelRunReport`: provider id, provider/schema version, input/output digests, facts/diagnostics if available, validation rejections where observable, cache hits/misses/recomputes/writes/quarantines, and demand query cache stats. Keep exact wall-clock values out of deterministic output hash while still serializing observed runtime in reports.</action>
  <verify>
    <automated>cargo test -p polint --lib eval::performance --locked</automated>
  </verify>
  <acceptance_criteria>
    - Provider/cache rows serialize deterministically.
    - Output hash remains stable when runtime duration changes.
    - Cache stats aggregate by provider and by suite run.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add grouped metric sections for scanner, graph, path, unknown, performance, and adaptation</name>
  <files>crates/polint/src/eval/metrics.rs, crates/polint/src/eval/report.rs</files>
  <read_first>
    - crates/polint/src/eval/metrics.rs
    - research/evaluation-harness/STANDARD.md
    - research/evaluation-harness/RESEARCH-ANALYSIS.md
  </read_first>
  <action>Extend metric summaries with grouped sections while preserving existing scalar fields for current tests: scanner confusion metrics, graph edge metrics, path/evidence metrics, unknown counts by family/status, performance/cache metrics, suite-native metric map, and adaptation delta summary placeholders.</action>
  <verify>
    <automated>cargo test -p polint --lib eval::metrics --locked</automated>
  </verify>
  <acceptance_criteria>
    - Existing metric tests still pass.
    - New tests show graph/path/unknown/performance sections are deterministic and sorted.
    - Suite-native metric maps can include OWASP or future suite metrics without schema-specific code in the base report.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Generate Markdown summary tables from eval JSON</name>
  <files>crates/polint/src/eval/markdown.rs, crates/polint/src/eval/mod.rs</files>
  <read_first>
    - crates/polint/src/eval/report.rs
    - research/evaluation-harness/implementation/polint-eval-path.md
  </read_first>
  <action>Add a crate-private Markdown renderer that consumes normalized eval report structs and emits tables for comparison rows, scanner metrics, graph/fact/path metrics, provider/cache stats, adaptation deltas, and limitations. The renderer must not be the source of truth; JSON remains canonical.</action>
  <verify>
    <automated>cargo test -p polint --lib eval::markdown --locked</automated>
  </verify>
  <acceptance_criteria>
    - Markdown output is deterministic for shuffled input report rows.
    - Markdown visibly labels adapter-only suites and imported competitor baselines.
    - Markdown contains no absolute temp paths or timestamps.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime data -> deterministic hash | Timing noise must not break deterministic report identity. |
| markdown summaries -> claims | Derived summaries must not omit limitations present in JSON. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T40-06 | Repudiation | report hash | Mitigated | Exclude volatile runtime durations from output hash. |
| T40-07 | Information Disclosure | provider stats | Mitigated | Normalize paths and avoid raw source or absolute clone roots. |
</threat_model>

<verification>
- `cargo fmt --all --check`
- `cargo test -p polint --lib eval::performance --locked`
- `cargo test -p polint --lib eval::metrics --locked`
- `cargo test -p polint --lib eval::markdown --locked`
</verification>

<success_criteria>
Eval reports include deterministic provider/cache/performance evidence and derived Markdown summaries without destabilizing existing fixture report hashes.
</success_criteria>
