---
phase: 40-external-benchmark-adapters-and-promotion-gates
plan: 02
title: Adapter Trait And OWASP Expected Results Scoring
type: execute
wave: 2
depends_on: [40-01]
files_modified:
  - crates/polint/src/eval/mod.rs
  - crates/polint/src/eval/adapter.rs
  - crates/polint/src/eval/external/mod.rs
  - crates/polint/src/eval/external/owasp.rs
  - research/evaluation-harness/suites/owasp-java.toml
  - research/evaluation-harness/suites/owasp-python.toml
autonomous: true
requirements: [SAE-PROM-01]
must_haves:
  truths:
    - "D-20/D-23: suite manifests and adapter/scoring paths record unsupported-language adapter-only status honestly."
    - "D-28: OWASP Java/Python parser/scorer cannot be claimed as real polint analysis until language support exists."
  artifacts:
    - path: "crates/polint/src/eval/adapter.rs"
      provides: "Crate-private benchmark adapter trait"
    - path: "crates/polint/src/eval/external/owasp.rs"
      provides: "OWASP expected-results CSV parser and confusion-matrix scorer"
---

<objective>
Add the crate-private external benchmark adapter interface and the first OWASP expected-results parser/scorer, with Java/Python suites clearly marked adapter-only for current polint language support.
</objective>

<execution_context>
@.planning/phases/40-external-benchmark-adapters-and-promotion-gates/40-CONTEXT.md
@research/evaluation-harness/RECOMMENDED_IMPLEMENTATION.md
@research/evaluation-harness/REPO-INDEX.md
@research/evaluation-harness/VALIDATION.md
@.agents/skills/rust-best-practices/SKILL.md
@AGENTS.md
</execution_context>

<context>
@crates/polint/src/eval/suite.rs
@crates/polint/src/eval/model.rs
@crates/polint/src/eval/metrics.rs
@crates/polint/src/eval/matcher.rs
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define the benchmark adapter trait</name>
  <files>crates/polint/src/eval/adapter.rs, crates/polint/src/eval/mod.rs</files>
  <read_first>
    - crates/polint/src/eval/suite.rs
    - crates/polint/src/eval/fixtures.rs
    - research/evaluation-harness/implementation/polint-eval-path.md
  </read_first>
  <action>Add a crate-private `BenchmarkAdapter` trait with methods to load a suite manifest, enumerate cases, prepare a case workspace, normalize observed output, compute suite-native metrics, and report language support status. Keep the trait internal and independent from CLI command parsing.</action>
  <verify>
    <automated>cargo test -p polint --lib eval::adapter --locked</automated>
  </verify>
  <acceptance_criteria>
    - Adapter trait can represent native fixtures and external suites.
    - Adapter trait returns adapter-only status for unsupported languages.
    - No public SDK or runner API changes.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parse OWASP expected-results CSV into canonical cases</name>
  <files>crates/polint/src/eval/external/mod.rs, crates/polint/src/eval/external/owasp.rs</files>
  <read_first>
    - research/evaluation-harness/RECOMMENDED_IMPLEMENTATION.md
    - research/evaluation-harness/REPO-INDEX.md
    - research/evaluation-harness/STANDARD.md
  </read_first>
  <action>Implement an OWASP CSV parser that reads expected-results rows into canonical benchmark cases/expected diagnostics with case id, category/CWE where present, vulnerable/safe truth, and relative target selectors. Add unit tests using tiny inline CSV fixtures, not vendored benchmark files.</action>
  <verify>
    <automated>cargo test -p polint --lib eval::external::owasp --locked</automated>
  </verify>
  <acceptance_criteria>
    - Parser reproduces expected vulnerable/safe counts for small fixture CSVs.
    - CSV parsing preserves suite-native category and CWE metadata where available.
    - Tests do not commit copied OWASP benchmark source or full expected-results files.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Compute OWASP-native and unified metrics from synthetic observed rows</name>
  <files>crates/polint/src/eval/external/owasp.rs, crates/polint/src/eval/metrics.rs</files>
  <read_first>
    - crates/polint/src/eval/metrics.rs
    - crates/polint/src/eval/matcher.rs
    - research/evaluation-harness/algorithms/scoring-and-matching.md
  </read_first>
  <action>Add OWASP-native confusion-matrix metrics TP, FN, TN, FP, TPR, FPR, precision, F-score, and TPR-minus-FPR from canonical case results. Verify against synthetic observed rows so the adapter/scorer works even before Java/Python language support.</action>
  <verify>
    <automated>cargo test -p polint --lib eval::external::owasp --locked</automated>
  </verify>
  <acceptance_criteria>
    - Synthetic observed outputs produce deterministic OWASP-native and unified metric maps.
    - Adapter-only reports cannot be marked as polint baseline analysis.
    - False-positive traps and safe cases affect FP/TN counts correctly.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Add research suite manifests for OWASP Java and Python</name>
  <files>research/evaluation-harness/suites/owasp-java.toml, research/evaluation-harness/suites/owasp-python.toml</files>
  <read_first>
    - research/evaluation-harness/REPO-INDEX.md
    - research/evaluation-harness/implementation/polint-eval-path.md
  </read_first>
  <action>Add small manifest files pointing to the gitignored research clone paths and pinned commits recorded in `REPO-INDEX.md`. Mark both suites as `adapter_only` or equivalent because current polint does not analyze Java/Python. Include license-review placeholders and tier metadata.</action>
  <verify>
    <automated>cargo test -p polint --lib eval::suite --locked</automated>
  </verify>
  <acceptance_criteria>
    - Manifests contain source URL, source commit, expected file path, adapter id, language support status, and tier selectors.
    - Manifests do not commit benchmark source or full expected-result CSV content.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external CSV -> canonical model | Malformed or unsupported benchmark data must not corrupt reports. |
| adapter-only score -> public claim | Parser/scorer validation must not be presented as polint language analysis. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T40-04 | Tampering | expected-results CSV | Mitigated | Validate required columns, case ids, truth labels, and relative paths. |
| T40-05 | Repudiation | adapter-only outputs | Mitigated | Persist language support status in suite and report metadata. |
</threat_model>

<verification>
- `cargo fmt --all --check`
- `cargo test -p polint --lib eval::adapter --locked`
- `cargo test -p polint --lib eval::external::owasp --locked`
- `cargo test -p polint --lib eval::suite --locked`
</verification>

<success_criteria>
OWASP expected-results parsing and scoring are implemented as internal adapter/scorer validation, while unsupported Java/Python analysis is labeled honestly.
</success_criteria>
