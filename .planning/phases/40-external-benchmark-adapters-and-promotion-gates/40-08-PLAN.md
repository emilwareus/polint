---
phase: 40-external-benchmark-adapters-and-promotion-gates
plan: 08
title: Hidden Eval Entry Point Public Boundary And Closeout Proof
type: execute
wave: 8
depends_on: [40-04, 40-05, 40-06, 40-07]
files_modified:
  - crates/polint/src/cli/mod.rs
  - crates/polint/src/eval/mod.rs
  - crates/polint/src/eval/runner.rs
  - tests/eval-fixtures/promotion
  - docs/API-VISIBILITY-PLAN.md
autonomous: true
requirements: [SAE-PROM-01]
must_haves:
  truths:
    - "D-19/D-30: eval stays hidden/internal unless deliberately gated; public no-leak proof covers normal CLI, SDK, runner, README, and docs."
    - "D-24/D-28: final reports tie claims to measured reports and label unsupported adapter-only suites honestly."
  artifacts:
    - path: "crates/polint/src/cli/mod.rs"
      provides: "Hidden/unstable eval entry point or internal-only dispatch, if implementation chooses CLI access"
    - path: "docs/API-VISIBILITY-PLAN.md"
      provides: "Boundary note that eval/query promotion remains deferred to Phase 41"
---

<objective>
Add the hidden/internal eval execution path or test/release entry point, complete Phase 40 promotion-gate fixtures, and prove no unsupported public API or public CLI contract leaked before Phase 41.
</objective>

<execution_context>
@.planning/phases/40-external-benchmark-adapters-and-promotion-gates/40-CONTEXT.md
@docs/API-VISIBILITY-PLAN.md
@AGENTS.md
@.agents/skills/rust-best-practices/SKILL.md
</execution_context>

<context>
@crates/polint/src/cli/mod.rs
@crates/polint/src/eval/runner.rs
@crates/polint/src/sdk/mod.rs
@crates/polint/src/runner/mod.rs
@README.md
@docs/facts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add hidden or test-only eval execution entry point</name>
  <files>crates/polint/src/cli/mod.rs, crates/polint/src/eval/runner.rs</files>
  <read_first>
    - crates/polint/src/cli/mod.rs
    - crates/polint/src/eval/runner.rs
    - docs/API-VISIBILITY-PLAN.md
  </read_first>
  <action>Add the minimum internal path needed to run suite manifests by tier and write JSON/Markdown reports. Prefer a hidden/unstable command or test/release helper that is not shown in normal public help. If adding a hidden CLI command, mark it clearly unstable/internal and keep JSON schema unstabilized.</action>
  <verify>
    <automated>cargo test -p polint --lib eval::runner --locked</automated>
    <automated>cargo run -q -p polint-cli -- --help &gt; /tmp/polint-help.txt &amp;&amp; ! rg -n "\beval\b" /tmp/polint-help.txt</automated>
  </verify>
  <acceptance_criteria>
    - Internal eval path can run native promotion fixtures and write deterministic JSON/Markdown.
    - Normal public CLI help does not advertise a stable eval command.
    - Eval schema is labeled internal/unstable.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run full Phase 40 native promotion and report determinism proof</name>
  <files>tests/eval-fixtures/promotion, crates/polint/src/eval/report.rs</files>
  <read_first>
    - tests/eval-fixtures/README.md
    - crates/polint/src/eval/report.rs
    - crates/polint/src/eval/gates.rs
  </read_first>
  <action>Add or update tests that run the Phase 40 native promotion fixture pack twice and assert identical normalized output hashes, stable selected cases, stable Markdown summaries, and passing gate verdicts. Include at least one test proving runtime duration changes do not affect output hash.</action>
  <verify>
    <automated>cargo test -p polint --lib eval --locked</automated>
  </verify>
  <acceptance_criteria>
    - Native promotion fixtures pass.
    - Output hash determinism is proven across repeated runs.
    - Gate verdicts are deterministic and include threshold names.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Prove public no-leak boundary</name>
  <files>docs/API-VISIBILITY-PLAN.md, tests</files>
  <read_first>
    - docs/API-VISIBILITY-PLAN.md
    - AGENTS.md
    - crates/polint/src/sdk/mod.rs
    - crates/polint/src/runner/mod.rs
    - README.md
  </read_first>
  <action>Add source-surface or integration tests proving Phase 40 internals do not become supported SDK views, runner APIs, public docs/facts pages, README promises, or stable public CLI help. Update `docs/API-VISIBILITY-PLAN.md` only to note that eval/query promotion remains gated and Phase 41 owns public surfaces.</action>
  <verify>
    <automated>cargo test --workspace --all-targets --locked</automated>
    <automated>rg -n "polint eval|CallGraph&lt;'_|DataFlow&lt;'_|Evidence&lt;'_" README.md docs/facts crates/polint/src/sdk crates/polint/src/runner || true</automated>
  </verify>
  <acceptance_criteria>
    - No public SDK query view is added by Phase 40.
    - README and docs/facts do not advertise hidden eval or unpromoted graph/data-flow/evidence APIs.
    - API visibility plan documents the deferred promotion boundary.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Final verification and roadmap evidence update</name>
  <files>.planning/phases/40-external-benchmark-adapters-and-promotion-gates/40-VERIFICATION.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/40-external-benchmark-adapters-and-promotion-gates/40-CONTEXT.md
  </read_first>
  <action>After implementation plans execute, write Phase 40 verification evidence summarizing commands, passing gates, adapter-only limitations, supported-language smoke results, native promotion fixture results, and public-boundary proof. Update roadmap/state through GSD tooling, not direct edits, when closing the phase.</action>
  <verify>
    <automated>cargo fmt --all --check</automated>
    <automated>cargo clippy --workspace --all-targets -- -D warnings</automated>
    <automated>cargo test --workspace --all-targets --locked</automated>
  </verify>
  <acceptance_criteria>
    - Verification distinguishes real supported-language benchmark runs from adapter-only scoring.
    - Verification ties every public claim to a measured report.
    - Phase 41 remains the next step for public SDK/query promotion.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hidden eval -> public CLI | Internal eval must not become a stable public command by accident. |
| measured reports -> public claims | Claims must cite exact suite/version/mode/limitations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T40-16 | Information Disclosure | public reports/help | Mitigated | Public no-leak tests over CLI help, SDK, runner, README, and docs. |
| T40-17 | Repudiation | final claims | Mitigated | Require verification to link claims to measured report artifacts. |
</threat_model>

<verification>
- `cargo fmt --all --check`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace --all-targets --locked`
- hidden/internal eval native promotion fixture run, if command/helper exists
</verification>

<success_criteria>
Phase 40 has a runnable internal promotion-gate path, deterministic native gate proof, honest adapter-only labeling, and no accidental public SDK/CLI promotion before Phase 41.
</success_criteria>
