---
phase: 40-external-benchmark-adapters-and-promotion-gates
plan: 06
title: Agent Adaptation Prompt Artifacts And Delta Reports
type: execute
wave: 6
depends_on: [40-01, 40-03, 40-05]
files_modified:
  - crates/polint/src/eval/adaptation.rs
  - crates/polint/src/eval/delta.rs
  - crates/polint/src/eval/report.rs
  - research/evaluation-harness/prompts/default-adaptation-agent.md
  - tests/eval-fixtures/extension/adaptation-delta/expected.polint-eval.toml
autonomous: true
requirements: [SAE-PROM-01]
must_haves:
  truths:
    - "D-11/D-17: adapted benchmark runs are produced from a recorded prompt and auditable adaptation artifacts."
    - "D-31/D-32: adaptation cannot bypass validation and must report cost/quality tradeoffs."
  artifacts:
    - path: "research/evaluation-harness/prompts/default-adaptation-agent.md"
      provides: "Default subagent prompt template for adapted benchmark runs"
    - path: "crates/polint/src/eval/delta.rs"
      provides: "Default-vs-adapted case/fact/edge/path delta calculation"
---

<objective>
Add the default adaptation-agent prompt artifact, adapted-run validation, and default-vs-adapted delta reporting that names changed cases, facts, edges, paths, unknowns, rejected facts, and overhead.
</objective>

<execution_context>
@.planning/phases/40-external-benchmark-adapters-and-promotion-gates/40-CONTEXT.md
@research/evaluation-harness/implementation/polint-eval-path.md
@research/agent-extension-surface/FINAL-REPORT.md
@research/agent-rule-authoring/FINAL-REPORT.md
@.claude/skills/polint/SKILL.md
@.agents/skills/rust-best-practices/SKILL.md
@AGENTS.md
</execution_context>

<context>
@crates/polint/src/eval/adaptation.rs
@crates/polint/src/eval/report.rs
@crates/polint/src/analysis/extensions/
</context>

<tasks>
<task type="auto">
  <name>Task 1: Commit the default adaptation-agent prompt template</name>
  <files>research/evaluation-harness/prompts/default-adaptation-agent.md</files>
  <read_first>
    - research/evaluation-harness/implementation/polint-eval-path.md
    - .claude/skills/polint/SKILL.md
    - research/agent-extension-surface/RECOMMENDED_IMPLEMENTATION.md
  </read_first>
  <action>Create the default adaptation-agent prompt from the Phase 40 context. It must tell the agent to inspect target source, use the polint skill, model real repository semantics through rules/models/extensions, and forbid expected labels, case-id hardcoding, benchmark filenames, and validation bypasses. Include deliverables and budget placeholders.</action>
  <verify>
    <automated>rg -n "Do not read benchmark expected labels|Do not hardcode benchmark case IDs|Use the polint skill" research/evaluation-harness/prompts/default-adaptation-agent.md</automated>
  </verify>
  <acceptance_criteria>
    - Prompt includes allowed context, forbidden context, adaptation process, deliverables, and anti-gaming constraints.
    - Prompt references `.claude/skills/polint/SKILL.md`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate adapted-run artifacts before report acceptance</name>
  <files>crates/polint/src/eval/adaptation.rs</files>
  <read_first>
    - crates/polint/src/eval/adaptation.rs
    - research/evaluation-harness/prompts/default-adaptation-agent.md
  </read_first>
  <action>Add validation that an agent-adapted evaluation run has prompt path/hash, declared budget, allowed/forbidden inputs, changed files or explicit no-change explanation, digests for changed rules/extensions, and adaptation notes. Reject adapted reports that omit forbidden-input declarations.</action>
  <verify>
    <automated>cargo test -p polint --lib eval::adaptation --locked</automated>
  </verify>
  <acceptance_criteria>
    - Missing prompt hash fails validation.
    - Missing forbidden-input list fails validation.
    - No changed files requires an explicit no-change reason.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Compute default-vs-adapted deltas</name>
  <files>crates/polint/src/eval/delta.rs, crates/polint/src/eval/report.rs, crates/polint/src/eval/mod.rs</files>
  <read_first>
    - crates/polint/src/eval/report.rs
    - crates/polint/src/eval/metrics.rs
    - research/evaluation-harness/algorithms/scoring-and-matching.md
  </read_first>
  <action>Add delta computation between normalized baseline and adapted runs. Report new TP, removed FN, removed FP, new FP, new unknowns, resolved unknowns, accepted/rejected extension facts, changed graph edges, changed paths, runtime overhead ratio when available, and cache invalidation scope. Include case-level changed items, not only aggregate metrics.</action>
  <verify>
    <automated>cargo test -p polint --lib eval::delta --locked</automated>
  </verify>
  <acceptance_criteria>
    - Delta tests name concrete changed cases and item keys.
    - Rejected extension facts are visible even if aggregate scanner score improves.
    - Runtime/cache overhead can be absent but serializes deterministically when present.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add native adaptation-delta fixture</name>
  <files>tests/eval-fixtures/extension/adaptation-delta/expected.polint-eval.toml</files>
  <read_first>
    - tests/eval-fixtures/README.md
    - crates/polint/src/eval/delta.rs
  </read_first>
  <action>Add a small synthetic or native fixture showing baseline unknown/missing facts and adapted accepted/rejected extension facts. Use it to prove the report names improvements, regressions, rejected facts, and unknown changes.</action>
  <verify>
    <automated>cargo test -p polint --lib eval::delta --locked</automated>
    <automated>cargo test -p polint --lib eval_observed --locked</automated>
  </verify>
  <acceptance_criteria>
    - Fixture proves adapted mode can improve recall while still surfacing a rejected extension fact.
    - Fixture does not use hidden expected labels or benchmark-specific filenames.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adaptation agent -> benchmark result | Agent-authored code must be auditable and validation-gated. |
| expected labels -> adaptation | Expected labels must not influence adaptation before code is written. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T40-12 | Tampering | adapted score | Mitigated | Store prompt hash, changed file digests, and case-level deltas. |
| T40-13 | Information Disclosure | expected labels | Mitigated | Prompt and record forbidden inputs; no pre-adaptation labels in adaptation context. |
</threat_model>

<verification>
- `cargo fmt --all --check`
- `cargo test -p polint --lib eval::adaptation --locked`
- `cargo test -p polint --lib eval::delta --locked`
- `cargo test -p polint --lib eval_observed --locked`
</verification>

<success_criteria>
Agent-adapted benchmark results are reproducible, prompt-linked, validation-gated, and reported as concrete default-vs-adapted deltas with cost and limitations.
</success_criteria>
