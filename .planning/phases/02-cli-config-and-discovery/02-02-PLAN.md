---
phase: 02-cli-config-and-discovery
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/VERIFICATION.md
  - .planning/PROJECT.md
  - .planning/phases/02-cli-config-and-discovery/02-02-SUMMARY.md
autonomous: true
requirements:
  - CLI-01
  - CLI-02
  - CLI-03
  - CFG-01
  - CFG-02
  - FS-01
  - DIAG-02
  - TEST-02
must_haves:
  truths:
    - "Plan 02-01 completed and produced focused Phase 2 integration coverage."
    - "Workspace formatting, clippy, tests, and Phase 2 CLI JSON smoke checks pass."
    - "GSD status marks CLI-01, CLI-02, CLI-03, CFG-01, CFG-02, FS-01, and DIAG-02 complete."
    - "TEST-02 remains scoped/in progress because broader command and CI coverage continues through Phase 8."
    - "CLI-04, CLI-05, DIAG-03, cache persistence, custom rule loading, and broad snapshot/property testing are not overclaimed."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Phase 2 requirement status"
      contains: "CLI-01"
    - path: ".planning/VERIFICATION.md"
      provides: "Phase 2 closure command record"
      contains: "Phase 2 Closure Verification"
    - path: ".planning/phases/02-cli-config-and-discovery/02-02-SUMMARY.md"
      provides: "Phase 2 closure summary"
      contains: "Phase 2 closure"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "Phase 2 requirements"
      via: "traceability rows"
      pattern: "Phase 2"
    - from: ".planning/VERIFICATION.md"
      to: "cargo and CLI verification commands"
      via: "recorded closure run"
      pattern: "cargo clippy --workspace --all-targets -- -D warnings"
---

<objective>
Run full Phase 2 verification and reconcile GSD status after the focused tests/source hardening plan completes.

Purpose: Close Phase 2 accurately without overclaiming later phases.

Output: Updated planning/status documents and `02-02-SUMMARY.md`.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-cli-config-and-discovery/02-CONTEXT.md
@.planning/phases/02-cli-config-and-discovery/02-01-SUMMARY.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/VERIFICATION.md

This plan depends on `02-01` and should only run after its summary exists and its focused CLI integration tests pass.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full Phase 2 verification</name>
  <files>.planning/VERIFICATION.md, .planning/phases/02-cli-config-and-discovery/02-02-SUMMARY.md</files>
  <read_first>
    .planning/phases/02-cli-config-and-discovery/02-CONTEXT.md
    .planning/phases/02-cli-config-and-discovery/02-01-SUMMARY.md
    crates/polint-cli/tests/cli.rs
    crates/polint-cli/src/main.rs
  </read_first>
  <action>
    Confirm the dependency summary exists:

    ```bash
    test -f .planning/phases/02-cli-config-and-discovery/02-01-SUMMARY.md
    ```

    Run full workspace verification and Phase 2 CLI smoke checks:

    ```bash
    cargo fmt
    cargo clippy --workspace --all-targets -- -D warnings
    cargo test --workspace
    cargo run -q -p polint-cli -- check --format json --fail-on none > /tmp/exlint-phase2-check.json
    cargo run -q -p polint-cli -- check --profile full --format human --fail-on none > /tmp/exlint-phase2-check-human.txt
    cargo run -q -p polint-cli -- check --help > /tmp/exlint-phase2-check-help.txt
    python3 -m json.tool /tmp/exlint-phase2-check.json > /tmp/exlint-phase2-check.pretty.json
    rg -n -- '--profile|--format|--no-cache|--fail-on' /tmp/exlint-phase2-check-help.txt
    ```

    If any command fails, fix only the Phase 2 behavior causing the failure, then rerun the full verification command set.
  </action>
  <verify>
    <automated>test -f .planning/phases/02-cli-config-and-discovery/02-01-SUMMARY.md</automated>
    <automated>cargo fmt -- --check</automated>
    <automated>cargo clippy --workspace --all-targets -- -D warnings</automated>
    <automated>cargo test --workspace</automated>
    <automated>python3 -m json.tool /tmp/exlint-phase2-check.json >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    `cargo fmt -- --check`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo test --workspace` exit 0.
    JSON output from `polint check --format json --fail-on none` parses with `python3 -m json.tool`.
    `polint check --help` documents `--profile`, `--format`, `--no-cache`, and `--fail-on`.
  </acceptance_criteria>
  <done>Full Phase 2 verification passes after Plan 02-01.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile Phase 2 GSD status</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/VERIFICATION.md, .planning/PROJECT.md, .planning/phases/02-cli-config-and-discovery/02-02-SUMMARY.md</files>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    .planning/STATE.md
    .planning/VERIFICATION.md
    .planning/PROJECT.md
    .planning/phases/02-cli-config-and-discovery/02-CONTEXT.md
    .planning/phases/02-cli-config-and-discovery/02-01-SUMMARY.md
  </read_first>
  <action>
    After Task 1 passes, update GSD docs so Phase 2 status matches verified behavior:

    - In `.planning/REQUIREMENTS.md`, mark `CLI-01`, `CLI-02`, `CLI-03`, `CFG-01`, `CFG-02`, `FS-01`, and `DIAG-02` complete.
    - Leave the top-level `TEST-02` checklist item unchecked.
    - Change traceability rows for `CLI-01`, `CLI-02`, `CLI-03`, `CFG-01`, `CFG-02`, `FS-01`, and `DIAG-02` from `Pending` to `Complete`.
    - Change the `TEST-02` traceability row to `In Progress - Phase 2 CLI/config/discovery integration tests verified; broader command and CI coverage remains scheduled in later phases`.
    - Ensure `.planning/ROADMAP.md` Phase 2 contains `**Plans:** 2 plans` and these list items:
      - `- [x] 02-01-PLAN.md - Add focused tests and narrow fixes for the existing CLI/config/discovery loop.`
      - `- [ ] 02-02-PLAN.md - Run full verification and reconcile Phase 2 status records.`
    - Append `## Phase 2 Closure Verification` to `.planning/VERIFICATION.md` with current short commit hash, commands run, final result, and whether source fixes were needed.
    - Update `.planning/PROJECT.md` to move the Phase 2 validated CLI/config/discovery requirement text from Active to Validated, if still present.

    Create `.planning/phases/02-cli-config-and-discovery/02-02-SUMMARY.md` using the summary template. It must include:
    - frontmatter with `requirements-completed` for `CLI-01`, `CLI-02`, `CLI-03`, `CFG-01`, `CFG-02`, `FS-01`, and `DIAG-02`;
    - `requirements-scoped` for `TEST-02`;
    - a `## Commands` section listing the verification commands;
    - a `## Result` section;
    - a `## Self-Check: PASSED` section if all checks passed.
  </action>
  <verify>
    <automated>rg -n '^- \[x\] \*\*CLI-01\*\*' .planning/REQUIREMENTS.md</automated>
    <automated>rg -n '^- \[x\] \*\*DIAG-02\*\*' .planning/REQUIREMENTS.md</automated>
    <automated>rg -n '^## Phase 2 Closure Verification$' .planning/VERIFICATION.md</automated>
    <automated>test -f .planning/phases/02-cli-config-and-discovery/02-02-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    Phase 2 completed requirements are marked complete without marking CLI-04, CLI-05, DIAG-03, or full TEST-02 complete.
    `.planning/ROADMAP.md` lists both Phase 2 plans.
    `.planning/phases/02-cli-config-and-discovery/02-02-SUMMARY.md` exists and records whether source fixes were needed.
  </acceptance_criteria>
  <done>GSD status accurately reflects Phase 2 closure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification commands -> status docs | Local command results determine whether planning docs can mark requirements complete. |
| planning docs -> future GSD workflow | Status updates influence the next phase selection and future verification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Spoofing/repudiation | GSD status records | mitigate | Record verification commands, commit hash, result, and scoped TEST-02 status in `VERIFICATION.md` and summary. |
| T-02-06 | Tampering | Requirement completion state | mitigate | Require successful cargo and CLI smoke checks before status rows are marked complete. |
</threat_model>

<verification>
Overall verification for this plan:

```bash
cargo fmt -- --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
python3 -m json.tool /tmp/exlint-phase2-check.json >/dev/null
rg -n '^- \[x\] \*\*CLI-01\*\*' .planning/REQUIREMENTS.md
rg -n '^## Phase 2 Closure Verification$' .planning/VERIFICATION.md
test -f .planning/phases/02-cli-config-and-discovery/02-02-SUMMARY.md
```
</verification>

<success_criteria>
- Full workspace verification passes after focused Phase 2 hardening.
- GSD docs mark completed Phase 2 requirements accurately without overclaiming later phases.
- Phase 2 is ready for phase-level verification.
- No worktrees are created or used.
</success_criteria>

<must_haves>
Truths:
- Plan 02-01 completed first.
- Cargo and CLI smoke checks pass.
- GSD docs close only the requirements actually satisfied by Phase 2.

Artifacts:
- `.planning/REQUIREMENTS.md` records completed Phase 2 requirements and scoped TEST-02 status.
- `.planning/VERIFICATION.md` records Phase 2 closure verification.
- `.planning/phases/02-cli-config-and-discovery/02-02-SUMMARY.md` records execution outcome.

Key links:
- Requirement status changes are backed by command evidence.
- TEST-02 remains active for later phases.
</must_haves>

<output>
After completion, create `.planning/phases/02-cli-config-and-discovery/02-02-SUMMARY.md`.
</output>
