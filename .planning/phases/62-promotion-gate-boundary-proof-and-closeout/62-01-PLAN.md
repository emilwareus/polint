# Phase 62-01 Plan: Boundary Proof and Milestone Audit

## Goal

Record the public boundary proof and summarize which policy-query APIs are ready
as preview versus intentionally limited.

## Tasks

1. Review existing public leak gates for SDK/prelude, README, docs/facts,
   generated skills, and CLI surfaces.
2. Add a v1.4 milestone audit artifact documenting:
   - preview APIs ready for external rule-author use,
   - raw/internal APIs that remain reserved,
   - known preview limitations,
   - recommended v1.5 stabilization follow-ups.
3. Ensure docs and skill text do not instruct users to import internals or use
   raw graphs.

## Verification

- `cargo test -p polint --test public_surface_leak --locked`
- `cargo test -p polint --test cli phase61_policy --locked`

