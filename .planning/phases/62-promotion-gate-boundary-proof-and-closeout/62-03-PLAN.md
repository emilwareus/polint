# Phase 62-03 Plan: Full Verification and Closeout

## Goal

Run milestone exit verification and close the active v1.4 roadmap state.

## Tasks

1. Mark VAL-03 and VAL-04 complete.
2. Mark Phase 62 complete in roadmap/state.
3. Add Phase 62 summaries and final verification notes.
4. Run the final local verification suite:
   - formatting,
   - clippy,
   - docs,
   - public leak,
   - Phase 5/61/template CLI tests,
   - full workspace or full package tests as locally practical.
5. Commit locally without pushing.

## Verification

- `cargo fmt --all --check`
- `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings`
- `cargo doc -p polint --no-deps --locked`
- `cargo test -p polint --test public_surface_leak --locked`
- `cargo test -p polint --test cli phase5 --locked`
- `cargo test -p polint --test cli phase61_policy --locked`
- `cargo test -p polint --test cli new_rule_policy_template --locked`
- `cargo test -p polint --lib --locked`

