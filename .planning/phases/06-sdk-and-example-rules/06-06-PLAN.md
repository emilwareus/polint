---
phase: 06-sdk-and-example-rules
plan: "06"
type: execute
wave: 6
depends_on:
  - "06-05"
files_modified:
  - crates/polint-rules/Cargo.toml
  - crates/polint-rules/tests/snapshots.rs
requirements:
  - RULE-01
  - RULE-02
  - RULE-03
  - RULE-04
  - RULE-05
  - RULE-06
  - RULE-07
  - RULE-08
  - TEST-01
  - TEST-03
requirements_addressed:
  - RULE-01
  - RULE-02
  - RULE-03
  - RULE-04
  - RULE-05
  - RULE-06
  - RULE-07
  - RULE-08
  - TEST-01
  - TEST-03
autonomous: true
must_haves:
  truths:
    - "Representative human diagnostic snapshots cover complexity/import-boundary and Go heuristic rule families."
    - "Representative JSON diagnostic snapshots cover raw-color and denied-literal rule families."
    - "Full workspace formatting, clippy, and tests pass after Phase 6."
  artifacts:
    - path: "crates/polint-rules/Cargo.toml"
      provides: "`insta` and `serde_json` dev-dependencies for snapshot tests"
    - path: "crates/polint-rules/tests/snapshots.rs"
      provides: "human and JSON snapshots for Phase 6 example rule families"
  key_links:
    - from: "crates/polint-rules/tests/snapshots.rs"
      to: "polint_rules::built_in_rules"
      via: "rules executed through public registration path"
      pattern: "built_in_rules"
    - from: "crates/polint-rules/tests/snapshots.rs"
      to: "polint_diagnostics::render"
      via: "snapshot human and JSON output"
      pattern: "OutputFormat::Human|OutputFormat::Json"
---

<objective>
Add representative diagnostic snapshots and complete Phase 6 verification.

Purpose: Implements D-15 through D-18 and proves TEST-03 coverage for the requested example rule families while preserving Phase 8 ownership of production SARIF hardening.
Execution policy: per D-22, work directly in `/Users/emilwareus/Development/exlint` on `main` and do not create or use worktrees.
Output: Rule snapshot tests and full verification commands.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-sdk-and-example-rules/06-CONTEXT.md
@.planning/phases/03-core-facts-and-diagnostics/03-02-SUMMARY.md
@crates/polint-rules/src/lib.rs
@crates/polint-rules/Cargo.toml
@crates/polint-diagnostics/src/lib.rs
@crates/polint-core/src/lib.rs

<interfaces>
Use existing diagnostic rendering:

```rust
pub fn render(format: OutputFormat, diagnostics: &[Diagnostic]) -> String
```

Use existing rule execution:

```rust
pub fn run_rules(
    db: &AnalysisDb,
    rules: &[Arc<dyn Rule>],
    options: &BTreeMap<String, RuleOptions>,
    enabled: &BTreeSet<String>,
    parallel: bool,
) -> Vec<Diagnostic>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add rule-family human and JSON snapshots</name>
  <files>crates/polint-rules/Cargo.toml, crates/polint-rules/tests/snapshots.rs</files>
  <read_first>
    - crates/polint-rules/Cargo.toml
    - crates/polint-rules/src/lib.rs
    - crates/polint-diagnostics/src/lib.rs
    - crates/polint-core/src/lib.rs
    - .planning/phases/06-sdk-and-example-rules/06-CONTEXT.md
  </read_first>
  <behavior>
    - Snapshot tests execute rules through `polint_rules::built_in_rules()`, not private rule structs.
    - Human snapshots cover complexity/import-boundary and Go heuristic families.
    - JSON snapshots cover raw-color and denied-literal families and are parseable by `serde_json`.
    - Snapshot fixture facts are synthetic `AnalysisDb` facts with deterministic file paths, spans, and values.
  </behavior>
  <action>Add `[dev-dependencies] insta.workspace = true` and `serde_json.workspace = true` to `crates/polint-rules/Cargo.toml`. Create `crates/polint-rules/tests/snapshots.rs`. Add helpers that build a deterministic `AnalysisDb` with files `src/payment.go`, `src/payment_test.go`, and `src/component.tsx`; push facts needed by all eight rules; select rules from `built_in_rules()` by exact ID per D-09; run through `run_rules`; and render with `OutputFormat::Human` or `OutputFormat::Json`. Add representative snapshots per D-17 with tests named `snapshot_complexity_and_import_boundary_human`, `snapshot_go_heuristics_human`, `snapshot_raw_color_and_denied_literals_json`, and `snapshot_all_phase6_rule_ids_json`. Use `insta::assert_snapshot!` with inline snapshots. Parse JSON render output with `serde_json::from_str::<serde_json::Value>` before snapshotting the raw renderer string, matching the Phase 3 JSON snapshot pattern.</action>
  <verify>
    <automated>cargo test -p polint-rules --test snapshots</automated>
    <automated>rg -n "snapshot_complexity_and_import_boundary_human|snapshot_go_heuristics_human|snapshot_raw_color_and_denied_literals_json|snapshot_all_phase6_rule_ids_json|assert_snapshot|OutputFormat::Human|OutputFormat::Json" crates/polint-rules/tests/snapshots.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p polint-rules --test snapshots` passes.
    - Human snapshot output contains `examples/go-cyclomatic-complexity`, `examples/ts-cyclomatic-complexity`, `examples/go-import-boundaries`, `examples/go-branch-obligations`, `examples/go-test-suite-size`, and `examples/go-assertion-after-action`.
    - JSON snapshot output contains `examples/ts-no-raw-colors` and `examples/config-query-no-literal`.
    - The all-rule-ID snapshot contains all eight exact `examples/...` IDs.
  </acceptance_criteria>
  <done>TEST-03 has representative human and JSON snapshots for every Phase 6 rule family.</done>
</task>

<task type="auto">
  <name>Task 2: Run full Phase 6 verification</name>
  <files>crates/polint-rules/Cargo.toml, crates/polint-rules/tests/snapshots.rs</files>
  <read_first>
    - crates/polint-core/src/lib.rs
    - crates/polint-sdk/src/lib.rs
    - crates/polint-rules/src/lib.rs
    - crates/polint-cli/src/main.rs
    - crates/polint-cli/tests/cli.rs
    - crates/polint-rules/tests/snapshots.rs
  </read_first>
  <action>Run full workspace verification after the snapshot tests pass. Fix only formatting, clippy, or test issues caused by Phase 6 files. Do not add production SARIF hardening, graph command expansion, cache behavior, plugin loading, or dynamic rule compilation.</action>
  <verify>
    <automated>cargo fmt -- --check</automated>
    <automated>cargo clippy --workspace --all-targets -- -D warnings</automated>
    <automated>cargo test --workspace</automated>
    <automated>rg -n "SDK-01|SDK-02|RULE-01|RULE-02|RULE-03|RULE-04|RULE-05|RULE-06|RULE-07|RULE-08|TEST-01|TEST-03" .planning/phases/06-sdk-and-example-rules/06-*-PLAN.md</automated>
  </verify>
  <acceptance_criteria>
    - `cargo fmt -- --check` passes.
    - `cargo clippy --workspace --all-targets -- -D warnings` passes.
    - `cargo test --workspace` passes.
    - `rg -n "examples/go-cyclomatic-complexity|examples/ts-cyclomatic-complexity|examples/go-import-boundaries|examples/ts-no-raw-colors|examples/go-branch-obligations|examples/go-test-suite-size|examples/go-assertion-after-action|examples/config-query-no-literal" crates/polint-rules/src/lib.rs crates/polint-cli/tests/cli.rs crates/polint-rules/tests/snapshots.rs` finds all eight rule IDs in implementation and tests.
  </acceptance_criteria>
  <done>Phase 6 implementation is formatted, clippy-clean, workspace-tested, and requirements are traceable through plans and tests.</done>
</task>

</tasks>

<threat_model>
ASVS L1 posture: no high severity threat is accepted; mitigated threats use local validation, deterministic behavior, or bounded data exposure.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| synthetic facts -> diagnostic renderer | Snapshot tests feed constructed facts into rules and render diagnostics. |
| full workspace verification -> local machine | Cargo commands compile and run local tests. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06-01 | Tampering | inline snapshots | mitigate | Use deterministic synthetic facts and exact rule ID assertions so snapshot updates cannot hide missing rule families. |
| T-06-06-02 | Repudiation | rule coverage evidence | mitigate | Snapshot all eight exact requested rule IDs and keep parsed JSON validation before JSON snapshots. |
| T-06-06-03 | Denial of Service | workspace tests | accept | Commands are local cargo verification already required by project policy; no network or external service is involved. |
| T-06-06-04 | Information Disclosure | snapshot contents | accept | Snapshots contain synthetic literals and paths only. |
</threat_model>

<verification>
Run after completing all tasks in this plan:

```bash
cargo fmt -- --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
```
</verification>

<success_criteria>
- Rule snapshots cover representative human and JSON diagnostics for all Phase 6 example rule families.
- Full workspace verification passes.
- No Phase 8 SARIF/CI hardening, graph command expansion, cache work, plugin loading, or dynamic rule compilation is added.
</success_criteria>

<output>
After completion, create `.planning/phases/06-sdk-and-example-rules/06-06-SUMMARY.md`.
</output>
