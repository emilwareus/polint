---
phase: 07-cache-and-performance
plan: "04"
type: execute
wave: 4
depends_on:
  - "07-03"
files_modified:
  - crates/polint-cache/Cargo.toml
  - crates/polint-cli/src/main.rs
  - crates/polint-cli/tests/cli.rs
  - crates/polint-cache/src/lib.rs
  - crates/polint-core/src/lib.rs
requirements:
  - PERF-01
  - PERF-02
  - PERF-03
  - TEST-01
  - TEST-04
requirements_addressed:
  - PERF-01
  - PERF-02
  - PERF-03
  - TEST-01
  - TEST-04
autonomous: true
must_haves:
  truths:
    - "`polint profile-rules` reports per-rule timings in deterministic rule order."
    - "Timing tests assert row shape and parseable/nonnegative durations, not exact elapsed values."
    - "Final Phase 7 tests prove cache on/off behavior and deterministic output across repeated runs."
    - "No Phase 7 output claims a fixed speedup."
  artifacts:
    - path: "crates/polint-cli/src/main.rs"
      provides: "profile-rules timing rows and shared analysis/cache behavior"
    - path: "crates/polint-cli/tests/cli.rs"
      provides: "profile, cache, no-cache, and repeated-run integration tests"
    - path: "crates/polint-cache/src/lib.rs"
      provides: "final cache key/property tests if needed"
    - path: "crates/polint-core/src/lib.rs"
      provides: "final deterministic runner/property tests if needed"
  key_links:
    - from: "crates/polint-cli/src/main.rs"
      to: "crates/polint-cli/tests/cli.rs"
      via: "`profile_rules` emits deterministic `elapsed_ms=<number>` rows parsed by integration tests"
      pattern: "elapsed_ms"
    - from: "crates/polint-cli/src/main.rs"
      to: "crates/polint-go/src/lib.rs and crates/polint-ts/src/lib.rs"
      via: "`profile_rules` reuses shared analysis/cache setup before timing individual rules"
      pattern: "analyze_and_run"
    - from: "crates/polint-cache/src/lib.rs"
      to: "crates/polint-cli/tests/cli.rs"
      via: "cache key property tests and cold/warm/no-cache CLI equality tests"
      pattern: "CacheKey::for_file"
    - from: "crates/polint-core/src/lib.rs"
      to: "crates/polint-cli/tests/cli.rs"
      via: "deterministic rule diagnostics remain stable across cache states"
      pattern: "run_rules_parallel_matches_sequential"
---

<objective>
Close Phase 7 with per-rule profiling and end-to-end cache/performance proof.

Purpose: Implements D-11 through D-16 and proves all Phase 7 roadmap success criteria together.
Execution policy: work directly in `/Users/emilwareus/Development/exlint` on `main`; do not create or use worktrees.
Output: deterministic `profile-rules` timing output, cache on/off integration proof, repeated-run determinism, and final workspace verification.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-cache-and-performance/07-CONTEXT.md
@.planning/phases/02-cli-config-and-discovery/02-CONTEXT.md
@.planning/phases/03-core-facts-and-diagnostics/03-CONTEXT.md
@crates/polint-cli/src/main.rs
@crates/polint-cli/tests/cli.rs
@crates/polint-cache/src/lib.rs
@crates/polint-core/src/lib.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden profile-rules timing rows</name>
  <files>crates/polint-cli/src/main.rs, crates/polint-cli/tests/cli.rs</files>
  <read_first>
    - crates/polint-cli/src/main.rs
    - crates/polint-cli/tests/cli.rs
    - .planning/phases/07-cache-and-performance/07-CONTEXT.md
  </read_first>
  <behavior>
    - `polint profile-rules --profile <name> --fail-on none` prints one row per enabled rule.
    - Rows are ordered by the deterministic `built_in_rules()` order filtered by profile.
    - Each row includes rule ID, elapsed duration, and diagnostic count.
    - Diagnostic exit-code behavior follows `--fail-on` using all parser and rule diagnostics.
  </behavior>
  <action>Keep or update `profile_rules` so it shares the same analysis/cache setup as `check` where practical, but times each rule independently using sequential `run_rules` for meaningful per-rule rows. Format rows as tab-separated deterministic fields: `{rule_id}\telapsed_ms={duration_ms:.3}\tdiagnostics={count}`. Add an integration test named `profile_rules_reports_per_rule_timings` that runs `polint profile-rules --profile phase7 --fail-on none` in a temp repo, asserts output contains the enabled rule IDs in order, parses every `elapsed_ms=` token as `f64`, asserts each parsed value is `>= 0.0`, and asserts each row has a `diagnostics=` field. Add a second test or assertion path named `profile_rules_honors_fail_on_threshold` that runs the same fixture with `--fail-on warn` and expects exit code `1` when a warning diagnostic is produced. Do not snapshot exact duration values.</action>
  <verify>
    <automated>cargo test -p polint-cli --test cli profile_rules_reports_per_rule_timings</automated>
    <automated>rg -n "ProfileRules|profile_rules_reports_per_rule_timings|diagnostics" crates/polint-cli/src/main.rs crates/polint-cli/tests/cli.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p polint-cli --test cli profile_rules_reports_per_rule_timings` passes.
    - The test asserts rule row ordering by comparing indexes of at least two enabled rule IDs.
    - The test parses every `elapsed_ms=` token as a nonnegative `f64` and does not assert an exact duration value.
    - `profile_rules_honors_fail_on_threshold` proves `--fail-on warn` exits with code `1` when warning diagnostics are present.
  </acceptance_criteria>
  <done>`profile-rules` has deterministic, testable per-rule timing output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add final cache and determinism property coverage</name>
  <files>crates/polint-cache/Cargo.toml, crates/polint-cache/src/lib.rs, crates/polint-core/src/lib.rs, crates/polint-cli/tests/cli.rs</files>
  <read_first>
    - crates/polint-cache/Cargo.toml
    - crates/polint-cache/src/lib.rs
    - crates/polint-core/src/lib.rs
    - crates/polint-cli/tests/cli.rs
    - .planning/REQUIREMENTS.md
    - .planning/phases/07-cache-and-performance/07-CONTEXT.md
  </read_first>
  <behavior>
    - Cache key invariants have property or table-driven coverage.
    - Diagnostic output remains deterministic across cache cold and warm runs.
    - `--no-cache` fully bypasses cache reads/writes at the CLI level.
  </behavior>
  <action>Add `proptest.workspace = true` under `[dev-dependencies]` in `crates/polint-cache/Cargo.toml` if not already present. Add property tests where useful, such as a `proptest!` in `polint-cache` proving `CacheKey::for_file(path_a, hash, config, rule, schema)` differs when `path_a != path_b`, and a deterministic diagnostic sort test in `polint-core` if existing property coverage does not already cover the Phase 7 path. Extend CLI cache tests so they run this sequence in one temp repo: first cache-enabled `check` creates `.polint/cache`, second cache-enabled `check` produces byte-identical JSON, third `check --no-cache` produces byte-identical JSON and does not add new cache files after recording the pre-run file list.</action>
  <verify>
    <automated>cargo test -p polint-cache --lib proptest</automated>
    <automated>cargo test -p polint-cli --test cli cache</automated>
    <automated>rg -n "proptest!|check_no_cache_bypasses_cache_reads_and_writes|byte-identical|cache" crates/polint-cache/src/lib.rs crates/polint-cli/tests/cli.rs</automated>
  </verify>
  <acceptance_criteria>
    - Cache key property/table tests pass.
    - CLI cache tests prove cold/warm/no-cache JSON outputs are exactly equal.
    - The no-cache test records cache file count before and after and asserts no additional cache files are created.
  </acceptance_criteria>
  <done>Phase 7 cache and determinism behavior is covered by focused automated tests.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Run full Phase 7 verification and update requirement status evidence</name>
  <files>.planning/phases/07-cache-and-performance/07-04-SUMMARY.md</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/07-cache-and-performance/07-CONTEXT.md
    - crates/polint-cli/tests/cli.rs
  </read_first>
  <behavior>
    - Workspace formatting, clippy, and tests pass after all Phase 7 implementation.
    - Summary records which commands verified cache keys, no-cache behavior, Rayon determinism, profile-rules timings, and repeated-run output.
    - No docs claim exact speedups.
  </behavior>
  <action>Run the full verification commands listed below. Create `07-04-SUMMARY.md` using the GSD summary template. In the summary, include exact evidence lines for these five roadmap success criteria: cache key inputs, `--no-cache` bypass, Rayon safe parallelism, `profile-rules` timing rows, and repeated-run deterministic output. Do not mark `.planning/REQUIREMENTS.md` complete manually here unless the execute-phase/state tooling normally does that after verification.</action>
  <verify>
    <automated>cargo fmt -- --check</automated>
    <automated>cargo clippy --workspace --all-targets -- -D warnings</automated>
    <automated>cargo test --workspace</automated>
    <automated>rg -n "cache key|--no-cache|Rayon|profile-rules|deterministic" .planning/phases/07-cache-and-performance/07-04-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `cargo fmt -- --check` passes.
    - `cargo clippy --workspace --all-targets -- -D warnings` passes.
    - `cargo test --workspace` passes.
    - `07-04-SUMMARY.md` lists the exact tests/commands proving all five Phase 7 success criteria.
  </acceptance_criteria>
  <done>Phase 7 implementation has complete verification evidence.</done>
</task>

</tasks>

<threat_model>
ASVS L1 posture: no high severity threat is accepted; mitigated threats use deterministic output, bounded timing claims, and explicit no-cache proof.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rule execution -> profiling output | Rule timings are printed for local inspection. |
| timing data -> user interpretation | Variable durations could be mistaken for benchmark-grade results. |
| cache warm/cold state -> CLI output | Cache state must not change diagnostics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Repudiation | `profile-rules` rows | mitigate | Add integration tests proving deterministic rule row ordering and diagnostic counts. |
| T-07-04-02 | Information Disclosure | timing output | accept | Rule IDs and timing durations are local CLI metadata and do not expose source text. |
| T-07-04-03 | Denial of Service | profiling run | mitigate | Reuse existing single-run analysis and sequential per-rule timing; do not add benchmark loops. |
| T-07-04-04 | Spoofing | cache warm versus cold output | mitigate | Add cold/warm/no-cache exact JSON equality tests. |
</threat_model>

<verification>
Run after completing all tasks in this plan:

```bash
cargo fmt -- --check
cargo test -p polint-cli --test cli profile_rules_reports_per_rule_timings
cargo test -p polint-cli --test cli cache
cargo test -p polint-cache --lib
cargo test -p polint-core --lib run_rules_parallel_matches_sequential
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
```
</verification>

<success_criteria>
- `profile-rules` reports deterministic per-rule timing rows.
- Cache on/off behavior and repeated-run determinism are covered by CLI tests.
- Full workspace formatting, clippy, and tests pass.
- Phase 7 closes `PERF-01`, `PERF-02`, `PERF-03`, `TEST-01`, and the cache/performance slice of `TEST-04`.
</success_criteria>

<output>
After completion, create `.planning/phases/07-cache-and-performance/07-04-SUMMARY.md`.
</output>
