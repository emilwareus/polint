---
phase: 64-store-foundation-and-boundary-proof
plan: 04
type: execute
wave: 4
depends_on: [64-03]
files_modified:
  - crates/polint/tests/public_surface_leak.rs
  - tests/fixtures/public-surface-leak-probe/src/lib.rs
  - crates/polint/src/analysis_kernel/mod.rs
  - crates/polint/src/eval/bench/gate.rs
  - crates/polint/src/eval/bench/runner.rs
autonomous: true
requirements: [STORE-01, STORE-06, STORE-07, STORE-08, PERF-03, PROD-01, VAL-02]

must_haves:
  truths:
    - "D-13: empty, previous, idempotent-current, future, invalid/corrupt, pragma, read-only, and two-writer fixtures all pass before Phase 64 closes."
    - "D-15: store/SQL/table/raw-database vocabulary is absent from SDK exports, runner/CLI contracts, public JSON, README/docs/facts/examples/generated skill text, and the external consumer probe."
    - "D-16: a real isolated store-enabled Phase 64 measurement is evaluated against the committed store-disabled baseline; RSS > +20%, cold wall-clock > +25%, or diagnostics digest drift produces a blocking failure."
    - "All Phase 64 migration, pragma, read-only, contention, recovery, disabled-I/O, parity, leak, and performance proofs pass together under workspace lint/test gates."
    - "The public `polint check` contract and supported SDK prelude contain no new item or behavior because of the store foundation."
  artifacts:
    - path: "crates/polint/tests/public_surface_leak.rs"
      provides: "Outside-consumer and public-content store/SQL no-leak gate"
      contains: "semantic_store"
    - path: "crates/polint/src/eval/bench/gate.rs"
      provides: "Phase 64 real-measurement boundary gate over the committed baseline"
      contains: "phase_64"
    - path: "tests/fixtures/public-surface-leak-probe/src/lib.rs"
      provides: "External SDK-only consumer that proves store internals are not nameable"
      contains: "polint::sdk::prelude"
  key_links:
    - from: "crates/polint/src/eval/bench/gate.rs"
      to: "research/evaluation-harness/baselines/store-disabled-check.json"
      via: "StoreDisabledBaseline::load plus isolated store-enabled CurvePoint/digest"
      pattern: "StoreDisabledBaseline|run_repo_perf_point_isolated|diagnostics_digest"
    - from: "crates/polint/tests/public_surface_leak.rs"
      to: "tests/fixtures/public-surface-leak-probe/src/lib.rs"
      via: "cargo build external consumer plus source allowlist/no-private-namespace assertions"
      pattern: "public-surface-leak-probe|cargo"
---

<objective>
Close Phase 64 with public-boundary and measured regression gates that fail rather than silently accepting a leaky or expensive store foundation.

Purpose: The store is not complete until an outside consumer cannot name it, public output/docs contain no implementation vocabulary, and the actual enabled path stays within the locked Phase 63 RSS/cold-latency/diagnostics budgets.
Output: Expanded store/SQL no-leak gate, real Phase 64 isolated boundary-gate test, and full workspace verification evidence.
</objective>

<execution_context>
@/Users/emilwareus/.codex/get-shit-done/workflows/execute-plan.md
@/Users/emilwareus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/64-store-foundation-and-boundary-proof/64-CONTEXT.md
@.planning/phases/64-store-foundation-and-boundary-proof/64-PATTERNS.md
@.planning/phases/64-store-foundation-and-boundary-proof/64-03-SUMMARY.md
@.planning/phases/63-ground-truth-and-performance-baseline/63-03-SUMMARY.md
@.planning/phases/63-ground-truth-and-performance-baseline/63-04-SUMMARY.md

<interfaces>
From `crates/polint/tests/public_surface_leak.rs`:
- `ALLOWED_PRELUDE` is exact and must not change for Phase 64.
- The test builds the excluded outside-consumer fixture and rejects private namespace references.

From `crates/polint/src/eval/bench/gate.rs`:
- `evaluate_regression_budget(baseline, measured, thresholds, measured_diagnostics_digest)` checks run-attributable peak RSS, cold wall-clock, and optional check-scoped digest.
- `is_blocking` returns true on any Fail.
- Locked thresholds and absolute noise floors already exist and must not be loosened.

From Plan 64-03:
- Internal store-enabled isolated measurement and check-scoped digest helpers.
- `CurvePoint.size.store_bytes > 0` for enabled measurement.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the external/public no-leak gate for semantic-store internals</name>
  <files>crates/polint/tests/public_surface_leak.rs, tests/fixtures/public-surface-leak-probe/src/lib.rs, crates/polint/src/analysis_kernel/mod.rs</files>
  <read_first>
    - crates/polint/tests/public_surface_leak.rs (outside probe build, exact prelude allowlist, private namespace scan)
    - tests/fixtures/public-surface-leak-probe/src/lib.rs (`#![no_implicit_prelude]`, single SDK prelude import, private-namespace rejection)
    - crates/polint/src/analysis_kernel/mod.rs (existing family-specific no-leak source scans and JSON rendering tests)
    - docs/API-VISIBILITY-PLAN.md (supported API contract)
    - .planning/phases/64-store-foundation-and-boundary-proof/64-CONTEXT.md (D-09 and D-15)
  </read_first>
  <action>
    Keep `ALLOWED_PRELUDE` and its locked count unchanged. Extend the outside probe/source parser assertions so executable code cannot reference `polint::analysis_kernel::store`, `polint::store`, `SemanticStore`, `StoreConfig`, `StoreStatus`, `rusqlite`, `Connection`, or raw database/SQL types. Add a Phase 64 no-leak test that renders `polint check --format json` and scans supported public surfaces: `src/sdk`, `src/runner/mod.rs`, `src/cli/mod.rs`, `src/lib.rs`, README, docs/API-VISIBILITY-PLAN.md, docs/facts, examples, and generated skill text. Use a curated marker list that avoids generic false positives (do not ban ordinary words like `row`, `store`, or `connection` globally); ban exact internal module/type names, rusqlite namespace, the private bootstrap table name, raw-row-id labels, and SQL statement/table identifiers unique to the new store. The private implementation under `analysis_kernel/store` is intentionally excluded from the scan. Add a negative-control synthetic public string for every marker family so the parser/scanner cannot silently pass when broken.
  </action>
  <verify>
    <automated>cargo test -p polint --test public_surface_leak --locked -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p polint --test public_surface_leak --locked -- --test-threads=1` exits 0 and builds the outside-consumer fixture.
    - `ALLOWED_PRELUDE.len()` and every allowlist entry are unchanged from before Phase 64.
    - Public source/JSON scans include SDK, runner, CLI, lib, README, API visibility plan, docs/facts, examples, and generated skill text.
    - Negative controls prove exact store module/type, rusqlite, private table, and raw database identifier markers are detected.
    - The external probe imports only `polint::sdk::prelude::*` and contains no private store namespace in executable code.
  </acceptance_criteria>
  <done>An outside rule-author consumer and every supported public text/output surface remain free of semantic-store implementation vocabulary.</done>
</task>

<task type="auto">
  <name>Task 2: Wire and run the real Phase 64 regression-budget boundary gate</name>
  <files>crates/polint/src/eval/bench/gate.rs, crates/polint/src/eval/bench/runner.rs</files>
  <read_first>
    - crates/polint/src/eval/bench/gate.rs (`evaluate_regression_budget`, `is_blocking`, locked thresholds/noise floors)
    - crates/polint/src/eval/bench/runner.rs (store-enabled isolated measurement and diagnostics digest from Plan 03)
    - crates/polint/src/eval/baseline.rs (`StoreDisabledBaseline::load` and validation)
    - research/evaluation-harness/baselines/store-disabled-check.json (committed fixture-scale reference)
    - .planning/phases/63-ground-truth-and-performance-baseline/63-04-SUMMARY.md (fail-not-silent handoff)
  </read_first>
  <action>
    Add a crate-private/test-facing `evaluate_phase_64_boundary(repo_root, baseline_path)` (or equivalently named) that loads and validates `StoreDisabledBaseline`, measures a store-enabled check through the isolated child path, computes the store-enabled check-scoped diagnostics digest, calls `evaluate_regression_budget` with `Some(&digest)`, and returns the report plus measured point. It must not change `BaselineThresholds`, `PEAK_RSS_ABS_FLOOR_BYTES`, or `COLD_WALL_CLOCK_ABS_FLOOR_MS`. Add a deterministic tiny git-repo fixture matching the committed baseline generator's Go/TS content. The Phase 64 gate test must assert: database/store_bytes > 0; diagnostics digest check is Pass; aggregate report is not blocking; and every RSS/cold check is present. Retain existing unit tests that prove synthetic over-budget and digest-drift inputs are blocking. If the real isolated fixture exceeds a locked budget, treat the test failure as a Phase 64 blocker and optimize/fix the implementation—do not loosen thresholds or silently skip the gate.
  </action>
  <verify>
    <automated>cargo test -p polint --lib eval::bench::gate::tests::phase_64 --locked -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p polint --lib eval::bench::gate::tests::phase_64 --locked -- --test-threads=1` exits 0.
    - The measured point comes from the store-enabled isolated child and reports `store_bytes > 0`.
    - The report contains passing `diagnostics_digest_parity`, peak-RSS, and cold-wall-clock checks and `is_blocking == false`.
    - Existing tests still prove +20% RSS breach, +25% cold breach, zero baseline, and digest mismatch are blocking.
    - No threshold/default/noise-floor value is relaxed to make the real run pass.
  </acceptance_criteria>
  <done>The actual enabled store foundation passes the committed Phase 63 regression and diagnostics-parity gate; any future breach fails CI.</done>
</task>

<task type="auto">
  <name>Task 3: Run the complete Phase 64 verification matrix and close residual gaps</name>
  <files>crates/polint/src/analysis_kernel/store/mod.rs, crates/polint/src/analysis_kernel/store/connection.rs, crates/polint/src/analysis_kernel/store/migrations.rs, crates/polint/src/analysis_kernel/store/tests.rs, crates/polint/src/cache/mod.rs, crates/polint/src/analysis_kernel/mod.rs, crates/polint/src/analysis_kernel/incremental/run_report.rs, crates/polint/src/eval/bench/runner.rs, crates/polint/src/eval/bench/gate.rs, crates/polint/tests/public_surface_leak.rs</files>
  <read_first>
    - .planning/ROADMAP.md (all five Phase 64 success criteria)
    - .planning/REQUIREMENTS.md (all Phase 64 requirement text)
    - .planning/phases/64-store-foundation-and-boundary-proof/64-CONTEXT.md (D-01 through D-17)
    - .planning/phases/64-store-foundation-and-boundary-proof/64-01-SUMMARY.md (implemented scaffold/migrations)
    - .planning/phases/64-store-foundation-and-boundary-proof/64-02-SUMMARY.md (implemented connection/recovery)
    - .planning/phases/64-store-foundation-and-boundary-proof/64-03-SUMMARY.md (implemented kernel/parity/benchmark integration)
  </read_first>
  <action>
    Run every focused command from Plans 01–04, then `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings`, and `cargo test --workspace --all-features --locked`. Fix only Phase 64 implementation/test defects uncovered by these commands; do not weaken lints, ignore tests, broaden visibility, relax benchmark thresholds, enable the store by default, or add Phase 65+ tables/features. Review the final diff against all requirement IDs and D-01..D-17, confirm `rg "pub use.*store|pub mod store|rusqlite" crates/polint/src/sdk crates/polint/src/lib.rs crates/polint/src/runner crates/polint/src/cli` returns no store exposure (rusqlite should only appear in the private implementation/dependency files), and confirm normal CLI help/config/schema snapshots are unchanged. Record exact test results and any environment-gated benchmark limitations in the plan summary; no false claim of large-repo measurement is allowed.
  </action>
  <verify>
    <automated>make lint &amp;&amp; cargo test --workspace --all-features --locked</automated>
  </verify>
  <acceptance_criteria>
    - `make lint` exits 0.
    - `cargo test --workspace --all-features --locked` exits 0.
    - Every focused Phase 64 migration/connection/contention/recovery/parity/leak/regression test exits 0.
    - No supported SDK prelude item, CLI flag/help/config key, JSON schema, README workflow, example command, or generated skill instruction was added or changed to expose the store.
    - Final source inspection finds rusqlite handles/SQL only under `analysis_kernel/store/`; no provider/rule/runner API accepts them.
  </acceptance_criteria>
  <done>All Phase 64 outcome, reliability, visibility, and regression gates pass together with no scope creep or hidden limitations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| private store implementation → public SDK/CLI/docs | Internal names and schemas must not become compatibility contracts |
| committed Phase 63 baseline → Phase 64 measured run | Trusted reference gates real enabled-store overhead and diagnostics parity |
| focused tests → phase completion claim | Verification must cover real integrations and cannot be replaced by shallow source checks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-04-01 | Information disclosure | rusqlite/SQL/table/raw-ID vocabulary leaks publicly | mitigate | Exact marker scan across all supported surfaces plus outside-consumer compile and negative controls; allowlist count unchanged. |
| T-64-04-02 | Repudiation | regression gate exists but Phase 64 never invokes it | mitigate | Real store-enabled isolated measurement is loaded against committed baseline in a named CI test; `is_blocking` asserted false only on actual pass. |
| T-64-04-03 | Tampering | thresholds loosened to hide overhead | mitigate | Existing exact threshold/noise-floor tests remain; task forbids changes and diff review checks them. |
| T-64-04-04 | Denial of Service | SQLite native overhead exceeds locked budget | mitigate | Isolated RSS/cold gate blocks; implementation must be optimized or mode kept disabled, never silently accepted. |
| T-64-04-05 | Repudiation | byte parity proved only by empty/shallow path | mitigate | Plan 03 runs normal kernel/provider and renderer across six states; full workspace tests and digest gate repeat integration evidence. |
| T-64-04-06 | Information disclosure | scanner false-positive/false-negative pressure causes broad ignores | mitigate | Curated exact unique markers plus per-family synthetic negative controls; no global ban on generic words and no skipped paths. |
</threat_model>

<verification>
- `cargo test -p polint --test public_surface_leak --locked -- --test-threads=1`
- `cargo test -p polint --lib eval::bench::gate::tests::phase_64 --locked -- --test-threads=1`
- `cargo test -p polint --lib analysis_kernel::store::tests --locked -- --test-threads=1`
- `cargo test -p polint --lib analysis_kernel::tests::semantic_store_check_parity --locked -- --test-threads=1`
- `make lint`
- `cargo test --workspace --all-features --locked`
</verification>

<success_criteria>
- No store/SQL implementation vocabulary becomes public or nameable by an external SDK consumer.
- A real store-enabled isolated measurement passes the locked RSS, cold-latency, and diagnostics-parity gate.
- All migration, connection, contention, recovery, disabled-I/O, parity, leak, and workspace tests pass.
- Phase 64 closes without public activation, manifest/generation/fact/query scope creep, or dishonest benchmark claims.
</success_criteria>

<output>
Create `.planning/phases/64-store-foundation-and-boundary-proof/64-04-SUMMARY.md` when done.
</output>
