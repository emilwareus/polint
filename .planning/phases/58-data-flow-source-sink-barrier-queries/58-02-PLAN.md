# Phase 58-02 Plan: Provider Source Introduction and Capability Wiring

## Goal

Make the private data-flow graph contain enough backed facts for real repo-local
rules to consume HTTP source-to-call-sink policies.

## Tasks

1. Add private `SourceIntroduction` edges from trust-boundary source nodes to
   matching MIR parameter place nodes in the data-flow provider.
2. Preserve deterministic stable keys, normalized output ordering, and provider
   cache inputs.
3. Move the `dataflow` preview capability from unsupported to supported in
   capability planning.
4. Add `dataflow` to the semantic pipeline trigger capability list so rules
   requesting `DataFlow<'_>` receive provider facts.
5. Update existing preview/capability tests that still expect `dataflow` to
   fail closed.

## Verification

- `cargo test -p polint --lib policy_capabilities_report_phase_support_boundaries --locked`
- `cargo test -p polint --test cli phase55_preview_rule_syntax_compiles_and_fails_closed --locked`

