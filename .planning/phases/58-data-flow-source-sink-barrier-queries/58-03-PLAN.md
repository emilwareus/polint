# Phase 58-03 Plan: External Rule, Docs, and Closeout

## Goal

Prove Phase 58 through an outside-rule style test and update user-facing docs
so the preview behavior is valuable but honest.

## Tasks

1. Add a temp-repo CLI test where generated `.polint/rules` imports only
   `polint::sdk::prelude::*`, requests `DataFlow<'_>`, runs a `FlowQuery`, and
   asserts a JSON diagnostic.
2. Update `docs/facts/data-flow.md` or create it if missing, including syntax,
   supported patterns, limits, heuristic wording, budget behavior, and examples.
3. Update `docs/facts/capability-plans.md`, `docs/API-VISIBILITY-PLAN.md`, and
   polint skill text to describe `DataFlow<'_>` as provider-backed for Phase
   58.
4. Update `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`,
   plan summaries, and final phase verification.
5. Run focused checks, public leak gate, formatting, clippy, docs, and a broad
   library regression before committing Phase 58 locally.

## Verification

- `cargo test -p polint --test cli phase58_data_flow_rule_reports_json --locked`
- `cargo test -p polint --test public_surface_leak --locked`
- `cargo fmt --all --check`
- `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings`
- `cargo doc -p polint --no-deps --locked`
- `cargo test -p polint --lib --locked`

