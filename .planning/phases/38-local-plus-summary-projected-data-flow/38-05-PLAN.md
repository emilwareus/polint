---
phase: 38-local-plus-summary-projected-data-flow
plan: 05
title: Source Sink Sanitizer Barrier And Extension Model Facts
type: execute
wave: 5
depends_on: [38-03, 38-04]
files_modified:
  - crates/polint/src/analysis/data_flow/models.rs
  - crates/polint/src/analysis/data_flow/provider.rs
  - crates/polint/src/analysis/data_flow/facts.rs
  - crates/polint/src/analysis/extensions/sinks.rs
  - crates/polint/src/analysis/extensions/validate.rs
autonomous: true
requirements: [SAE-PREC-03]
must_haves:
  truths:
    - "D-15/D-16 are covered: trust boundaries and validated extension/model facts become source/sink/sanitizer/barrier/additional-step model facts."
    - "D-17 is covered: sanitizer and barrier facts affect query semantics without destructively removing base value-flow edges."
    - "D-18/D-19 are covered: model facts carry provider/model ids, precision ceilings, accepted/rejected status, validation diagnostics, quarantine behavior, and default-vs-extended delta evidence."
    - "SAE-PREC-03 provenance criterion is covered for source, sink, sanitizer, and barrier model facts."
  artifacts:
    - path: "crates/polint/src/analysis/data_flow/models.rs"
      provides: "Native and extension model binding for sources, sinks, sanitizers, barriers, and additional flow steps"
---

<objective>
Add provenance-rich source, sink, sanitizer, barrier, and additional-step model facts from trust boundaries and extension/model inputs.
</objective>

<execution_context>
@.planning/phases/38-local-plus-summary-projected-data-flow/38-CONTEXT.md
@.planning/phases/38-local-plus-summary-projected-data-flow/38-PATTERNS.md
@.agents/skills/rust-best-practices/SKILL.md
@AGENTS.md
</execution_context>

<context>
@crates/polint/src/analysis/entrypoints/facts.rs
@crates/polint/src/analysis/extensions/sinks.rs
@crates/polint/src/analysis/extensions/validate.rs
@crates/polint/src/analysis/data_flow/facts.rs
@crates/polint/src/analysis/data_flow/store.rs
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Convert trust boundaries into source model facts</name>
  <files>crates/polint/src/analysis/data_flow/models.rs</files>
  <read_first>
    - crates/polint/src/analysis/entrypoints/facts.rs
    - crates/polint/src/analysis/data_flow/facts.rs
  </read_first>
  <action>Create native model binding that maps `TrustBoundaryFact` rows for path/query/body/header/cookie, MCP args/resource URI, CLI args/flags/env/stdin, queue payload, and external return into data-flow source model facts and source edges. Preserve entrypoint stable key, trust boundary source kind, protocol/access path, precision, provider id, and provenance.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::data_flow::models --locked</automated>
  </verify>
  <acceptance_criteria>
    - Tests prove Express or MCP trust-boundary facts become source model facts.
    - Source facts carry provenance and input stable keys from trust boundaries.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add extension sink payloads for data-flow models</name>
  <files>crates/polint/src/analysis/extensions/sinks.rs, crates/polint/src/analysis/extensions/validate.rs</files>
  <read_first>
    - crates/polint/src/analysis/extensions/sinks.rs
    - crates/polint/src/analysis/extensions/validate.rs
    - crates/polint/src/analysis/data_flow/facts.rs
  </read_first>
  <action>Extend the typed extension sink boundary to accept data-flow source, sink, sanitizer, barrier, and additional-step model candidates. Each candidate must include model id/provider id, fact family, stable key, binding refs, optional span, precision, confidence, status, evidence, and payload labels. Validate bindings to existing stable facts or declared synthetic identities; reject dangling, malformed, exact-unvalidated, or unsupported payloads.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::extensions::validate --locked</automated>
    <automated>cargo test -p polint --lib analysis::extensions::sinks --locked</automated>
  </verify>
  <acceptance_criteria>
    - Extension validation accepts one well-formed data-flow sink or sanitizer candidate.
    - Extension validation rejects one dangling or exact-unvalidated data-flow model candidate.
    - Rejected model facts remain visible as rejected rows or diagnostics.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Merge accepted model facts into data-flow output</name>
  <files>crates/polint/src/analysis/data_flow/models.rs, crates/polint/src/analysis/data_flow/provider.rs</files>
  <read_first>
    - crates/polint/src/analysis/extensions/store.rs
    - crates/polint/src/analysis/data_flow/provider.rs
  </read_first>
  <action>Merge accepted extension/model facts into data-flow model facts and edges. Add default-vs-extended counts for model facts, source/sink/sanitizer/barrier rows, rejected rows, and changed reachability seeds. Ensure model facts are additive and cannot delete native source or local value-flow edges.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::data_flow::provider --locked</automated>
  </verify>
  <acceptance_criteria>
    - Tests prove accepted extension model facts are included with extension provenance.
    - Tests prove rejected extension model facts do not create accepted data-flow edges.
    - Provider output digest changes when accepted model facts change.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo-local model -> native analysis | Model facts are untrusted until validated and precision-ceiling checked. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T38-09 | Tampering | extension model facts | Mitigated | Validate bindings, preserve rejected rows, and never let models delete native facts. |
| T38-10 | Spoofing | model provenance | Mitigated | Store provider/model ids and input evidence on every model-derived fact. |
</threat_model>

<verification>
- `cargo fmt --all --check`
- `cargo test -p polint --lib analysis::data_flow --locked`
- `cargo test -p polint --lib analysis::extensions --locked`
</verification>

<success_criteria>
Source, sink, sanitizer, barrier, and additional-step facts exist with provenance, validation, precision ceilings, rejected-state visibility, and extension delta support.
</success_criteria>
