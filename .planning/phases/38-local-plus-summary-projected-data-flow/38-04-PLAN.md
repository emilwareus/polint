---
phase: 38-local-plus-summary-projected-data-flow
plan: 04
title: Direct-Call And Summary-Projected Flow Edges
type: execute
wave: 4
depends_on: [38-03]
files_modified:
  - crates/polint/src/analysis/data_flow/direct_calls.rs
  - crates/polint/src/analysis/data_flow/summary_edges.rs
  - crates/polint/src/analysis/data_flow/provider.rs
  - crates/polint/src/analysis/data_flow/facts.rs
autonomous: true
requirements: [SAE-PREC-03]
must_haves:
  truths:
    - "D-10/D-11 are covered: data flow projects argument/parameter, receiver, return, TITO, memory, call/effect, and summary rows through accepted direct/refined call facts."
    - "D-12 is covered: missing, unknown, unsupported, setup-missing, and budget-exceeded summaries produce unknown/havoc rows rather than no-flow."
    - "D-13/D-14 are covered: Phase 38 consumes refined calls and summaries without making refined calls depend on data flow; broad IFDS/IDE remains deferred."
  artifacts:
    - path: "crates/polint/src/analysis/data_flow/direct_calls.rs"
      provides: "Direct/refined call interprocedural flow projection"
    - path: "crates/polint/src/analysis/data_flow/summary_edges.rs"
      provides: "Summary-projected flow edges and missing-summary unknown rows"
---

<objective>
Add interprocedural data-flow projection from accepted direct/refined call edges and compact summary facts without eager whole-program path expansion.
</objective>

<execution_context>
@.planning/phases/38-local-plus-summary-projected-data-flow/38-CONTEXT.md
@.planning/phases/38-local-plus-summary-projected-data-flow/38-PATTERNS.md
@.agents/skills/rust-best-practices/SKILL.md
@AGENTS.md
</execution_context>

<context>
@crates/polint/src/analysis/calls/facts.rs
@crates/polint/src/analysis/refined_calls/facts.rs
@crates/polint/src/analysis/refined_calls/store.rs
@crates/polint/src/analysis/summaries/facts.rs
@crates/polint/src/analysis/summaries/store.rs
@crates/polint/src/analysis/data_flow/local.rs
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Project accepted call argument and return flow</name>
  <files>crates/polint/src/analysis/data_flow/direct_calls.rs</files>
  <read_first>
    - crates/polint/src/analysis/calls/facts.rs
    - crates/polint/src/analysis/refined_calls/facts.rs
    - crates/polint/src/analysis/data_flow/facts.rs
    - crates/polint/src/analysis/data_flow/store.rs
  </read_first>
  <action>Create a call projection builder that consumes direct call targets and accepted refined call edges. Emit argument-to-parameter, receiver-to-receiver, return-to-call-return, and unresolved-call unknown/havoc edges. Preserve `CallSiteId`, `CallTargetId` or `RefinedCallEdgeId`, call tier, status, precision, provenance, and input stable keys on every emitted edge.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::data_flow::direct_calls --locked</automated>
  </verify>
  <acceptance_criteria>
    - Tests prove argument-to-parameter and return-to-call-return edges include call-site evidence.
    - Tests prove unresolved or budget-exceeded call facts emit unknown/havoc or budget rows.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Project summary TITO and effects into flow edges</name>
  <files>crates/polint/src/analysis/data_flow/summary_edges.rs</files>
  <read_first>
    - crates/polint/src/analysis/summaries/facts.rs
    - crates/polint/src/analysis/summaries/store.rs
    - crates/polint/src/analysis/data_flow/direct_calls.rs
  </read_first>
  <action>Create summary projection that maps `SummaryDomainKind::DataFlowTito`, memory/effect summaries, and summary events into compact `SummaryProjected` data-flow edges. Project parameter/receiver-to-return and by-side-effect flows using `FlowRoot` and `FlowKind`. Preserve callable stable key, summary stable key, summary status, precision, and provenance.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::data_flow::summary_edges --locked</automated>
  </verify>
  <acceptance_criteria>
    - Tests prove a `DataFlowTito` summary creates a summary-projected edge.
    - Tests prove sanitizer/barrier summary kinds are represented without deleting base edges.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Represent missing or uncertain summaries as unknown/havoc</name>
  <files>crates/polint/src/analysis/data_flow/summary_edges.rs, crates/polint/src/analysis/data_flow/provider.rs</files>
  <read_first>
    - crates/polint/src/analysis/summaries/facts.rs
    - crates/polint/src/analysis/data_flow/provider.rs
  </read_first>
  <action>For missing callee summaries, `SummaryStatus::Unknown`, `Unsupported`, `SetupMissing`, and `BudgetExceeded`, emit explicit data-flow unknown/havoc or budget edges tied to the call site and summary event evidence. Wire direct-call and summary outputs into the provider before digesting and storage.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::data_flow::provider --locked</automated>
  </verify>
  <acceptance_criteria>
    - A missing summary test produces an unknown/havoc row, not no rows.
    - Provider tests prove summary-projected edges participate in output digest.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| call graph -> data-flow projection | Unresolved or heuristic call edges must not become exact interprocedural flow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T38-07 | Tampering | call projection precision | Mitigated | Preserve call/refined-call status, precision, tier, and provenance on projected flow. |
| T38-08 | Information Disclosure | summary projection | Mitigated | Store stable keys and compact evidence, not raw source or expanded callee bodies. |
</threat_model>

<verification>
- `cargo fmt --all --check`
- `cargo test -p polint --lib analysis::data_flow --locked`
</verification>

<success_criteria>
Data-flow provider emits interprocedural and summary-projected edges with explicit unknown/havoc behavior for missing or uncertain callees.
</success_criteria>
