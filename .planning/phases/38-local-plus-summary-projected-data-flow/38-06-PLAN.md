---
phase: 38-local-plus-summary-projected-data-flow
plan: 06
title: Budgeted Query-Scoped Path Search
type: execute
wave: 6
depends_on: [38-04, 38-05]
files_modified:
  - crates/polint/src/analysis/data_flow/query.rs
  - crates/polint/src/analysis/data_flow/paths.rs
  - crates/polint/src/analysis/data_flow/provider.rs
  - crates/polint/src/analysis/data_flow/facts.rs
autonomous: true
requirements: [SAE-PREC-03]
must_haves:
  truths:
    - "D-20/D-21 are covered: budgets for local edges, access-path depth/fanout, summary projection, interprocedural depth, path node/edge/depth limits, and model expansion produce visible deterministic budget rows."
    - "D-22/D-23 are covered: path search assembles bounded paths on demand from compact facts and preserves enough call-site context for basic argument/return realizability."
    - "D-24 is covered: query path search remains crate-private/test-facing and does not promote a stable CLI or SDK contract."
  artifacts:
    - path: "crates/polint/src/analysis/data_flow/query.rs"
      provides: "Crate-private query configuration and seed selection"
    - path: "crates/polint/src/analysis/data_flow/paths.rs"
      provides: "Bounded path search with truncation and sanitizer/barrier handling"
---

<objective>
Add a crate-private, query-scoped path search over compact data-flow facts with explicit limits, sanitizer/barrier semantics, and visible truncation/unknown markers.
</objective>

<execution_context>
@.planning/phases/38-local-plus-summary-projected-data-flow/38-CONTEXT.md
@.planning/phases/38-local-plus-summary-projected-data-flow/38-PATTERNS.md
@.agents/skills/rust-best-practices/SKILL.md
@AGENTS.md
</execution_context>

<context>
@crates/polint/src/analysis/demand/query.rs
@crates/polint/src/analysis/demand/trace.rs
@crates/polint/src/analysis/data_flow/facts.rs
@crates/polint/src/analysis/data_flow/store.rs
@crates/polint/src/analysis/data_flow/cache_key.rs
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define internal query and path result types</name>
  <files>crates/polint/src/analysis/data_flow/query.rs, crates/polint/src/analysis/data_flow/paths.rs, crates/polint/src/analysis/data_flow/facts.rs</files>
  <read_first>
    - crates/polint/src/analysis/demand/query.rs
    - crates/polint/src/analysis/data_flow/facts.rs
    - crates/polint/src/analysis/data_flow/store.rs
  </read_first>
  <action>Define crate-private query structs for source-to-sink, node-to-node, and summary-expanded path search. Include settings for max nodes, max edges, max depth, interprocedural depth, include summary edges, include sanitizer/barrier handling, include heuristic/model edges, and include unknown/havoc edges. Define path result rows with edge stable keys, node stable keys, status, budget/truncation reason, sanitizer/barrier stop reason, and call-site stack evidence.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::data_flow::query --locked</automated>
  </verify>
  <acceptance_criteria>
    - Query settings expose explicit limits for nodes, edges, depth, interprocedural depth, and summary expansion.
    - Path result types can represent found, blocked-by-sanitizer, blocked-by-barrier, truncated, unknown, and no-path statuses.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement bounded path search over stored facts</name>
  <files>crates/polint/src/analysis/data_flow/paths.rs</files>
  <read_first>
    - crates/polint/src/analysis/data_flow/store.rs
    - crates/polint/src/analysis/data_flow/query.rs
  </read_first>
  <action>Implement deterministic bounded graph search over `DataFlowStore` indexes. Sort frontier expansion by stable key. Honor sanitizer and barrier facts as query-level stops without deleting underlying edges. Preserve basic call-site context for argument/parameter and return/call-return edges so obviously mismatched interprocedural paths are not reported as clean paths. Return truncation/budget rows when limits are hit.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::data_flow::paths --locked</automated>
  </verify>
  <acceptance_criteria>
    - Tests prove a bounded source-to-sink path is found deterministically.
    - Tests prove sanitizer and barrier facts block query results while base edges remain stored.
    - Tests prove hitting max depth or max edges returns budget/truncation status.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Record query budget diagnostics in provider/debug data</name>
  <files>crates/polint/src/analysis/data_flow/provider.rs, crates/polint/src/analysis/data_flow/paths.rs</files>
  <read_first>
    - crates/polint/src/analysis/data_flow/provider.rs
    - crates/polint/src/analysis/data_flow/cache_key.rs
  </read_first>
  <action>Wire path-search budget settings into provider parameters and debug/test helpers. If provider emits stored query/path rows for fixtures, ensure they are included in output digest and metadata. If path rows are computed on demand only, ensure budget/truncation facts are still visible through debug/eval test-facing observation.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::data_flow --locked</automated>
  </verify>
  <acceptance_criteria>
    - Budget/truncation status is observable in tests.
    - No public CLI, SDK, or report JSON field is added for path search.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| query path search -> diagnostics/evidence | Phase 38 path rows are internal and must not become public evidence contracts before Phase 39/41. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T38-11 | Denial of Service | path search | Mitigated | Enforce deterministic node/edge/depth/interprocedural budgets. |
| T38-12 | Tampering | sanitizer/barrier semantics | Mitigated | Treat sanitizers/barriers as query stops, not destructive graph mutation. |
</threat_model>

<verification>
- `cargo fmt --all --check`
- `cargo test -p polint --lib analysis::data_flow --locked`
</verification>

<success_criteria>
Crate-private path search can prove bounded reachability, sanitizer/barrier stops, and budget truncation without promoting public data-flow query contracts.
</success_criteria>
