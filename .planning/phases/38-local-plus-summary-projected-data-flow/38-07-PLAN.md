---
phase: 38-local-plus-summary-projected-data-flow
plan: 07
title: Data-Flow Validation Debug Eval And Public Boundary Proof
type: execute
wave: 7
depends_on: [38-05, 38-06]
files_modified:
  - crates/polint/src/analysis/data_flow/validate.rs
  - crates/polint/src/analysis/data_flow/debug.rs
  - crates/polint/src/analysis/data_flow/mod.rs
  - crates/polint/src/analysis_kernel/debug.rs
  - crates/polint/src/analysis_kernel/validation.rs
  - crates/polint/src/eval/model.rs
  - crates/polint/src/eval/fixtures.rs
  - tests/eval-fixtures/data-flow/
  - tests/eval-fixtures/kernel/provider-order/expected.polint-eval.toml
autonomous: true
requirements: [SAE-PREC-03]
must_haves:
  truths:
    - "D-29 is covered: validation checks dangling references, spans, duplicate stable keys, malformed model bindings, precision/status contradictions, missing provenance, and missing budget markers."
    - "D-30 is covered: debug snapshots report counts by language, edge kind, model status, unknown/havoc, summary projection, budgets, path truncation, and default-vs-extended deltas without raw source or absolute paths."
    - "D-31 is covered: eval fixtures cover local flow, parameter/return flow, interprocedural flow, summary projection, source/sink/sanitizer/barrier behavior, missing-summary unknowns, extension-added flow, rejected models, false-positive traps, cache determinism, and budgets."
    - "D-32 is covered: public no-leak proof protects normal CLI JSON/help, SDK exports, runner surface, README, and docs/facts."
    - "SAE-PREC-03 is covered end-to-end: source/sink/sanitizer/barrier facts have provenance, unknown/havoc facts are visible, and query path limits are explicit."
  artifacts:
    - path: "crates/polint/src/analysis/data_flow/validate.rs"
      provides: "Data-flow validation checks"
    - path: "crates/polint/src/analysis/data_flow/debug.rs"
      provides: "Test-facing deterministic data-flow debug JSON"
    - path: "tests/eval-fixtures/data-flow/"
      provides: "Native data-flow eval fixture coverage"
---

<objective>
Close Phase 38 with validation, debug JSON, native eval fixtures, deterministic cache proof, and public no-leak coverage.
</objective>

<execution_context>
@.planning/phases/38-local-plus-summary-projected-data-flow/38-CONTEXT.md
@.planning/phases/38-local-plus-summary-projected-data-flow/38-PATTERNS.md
@.agents/skills/rust-best-practices/SKILL.md
@AGENTS.md
</execution_context>

<context>
@crates/polint/src/analysis/refined_calls/validate.rs
@crates/polint/src/analysis/refined_calls/debug.rs
@crates/polint/src/analysis_kernel/debug.rs
@crates/polint/src/analysis_kernel/validation.rs
@crates/polint/src/eval/model.rs
@crates/polint/src/eval/fixtures.rs
@tests/eval-fixtures/refined-calls/direct-vs-refined/expected.polint-eval.toml
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add validation for data-flow facts and models</name>
  <files>crates/polint/src/analysis/data_flow/validate.rs, crates/polint/src/analysis_kernel/validation.rs</files>
  <read_first>
    - crates/polint/src/analysis/refined_calls/validate.rs
    - crates/polint/src/analysis/extensions/validate.rs
    - crates/polint/src/analysis/data_flow/facts.rs
    - crates/polint/src/analysis/data_flow/store.rs
  </read_first>
  <action>Validate data-flow nodes, edges, model facts, budget facts, and optional query/path rows. Checks must cover dangling file/function/MIR/CFG/place/call/refined-call/summary/trust-boundary/model refs, invalid spans, duplicate stable keys, malformed source/sink/sanitizer/barrier bindings, impossible precision/status combinations, missing provenance, missing budget markers when limits are exceeded, and query/path rows exceeding declared limits. Invoke validation from the shared kernel validation path.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::data_flow::validate --locked</automated>
    <automated>cargo test -p polint --lib analysis_kernel::validation --locked</automated>
  </verify>
  <acceptance_criteria>
    - Tests reject dangling node and edge references.
    - Tests reject exact/unvalidated extension model facts.
    - Tests reject path/query rows that exceed limits without budget status.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add deterministic debug snapshots</name>
  <files>crates/polint/src/analysis/data_flow/debug.rs, crates/polint/src/analysis_kernel/debug.rs</files>
  <read_first>
    - crates/polint/src/analysis/refined_calls/debug.rs
    - crates/polint/src/analysis_kernel/debug.rs
    - crates/polint/src/analysis/data_flow/facts.rs
  </read_first>
  <action>Add test-facing debug JSON for data-flow facts with relative paths, stable keys, compact payload labels, node/edge counts by language/kind/status/provenance, source/sink/sanitizer/barrier/model counts, unknown/havoc counts, summary-projected counts, budget counts, path truncation counts, and default-vs-extended model deltas. Avoid raw source, absolute paths, parser object ids, timestamps, and nondeterministic ordering.</action>
  <verify>
    <automated>cargo test -p polint --lib analysis::data_flow::debug --locked</automated>
    <automated>cargo test -p polint --lib analysis_kernel::debug --locked</automated>
  </verify>
  <acceptance_criteria>
    - Debug tests prove rows sort deterministically by stable key.
    - Debug JSON contains unknown/havoc and budget count fields.
    - Debug JSON does not contain absolute workspace paths in test assertions.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add native data-flow eval fixtures</name>
  <files>crates/polint/src/eval/model.rs, crates/polint/src/eval/fixtures.rs, tests/eval-fixtures/data-flow/</files>
  <read_first>
    - crates/polint/src/eval/model.rs
    - crates/polint/src/eval/fixtures.rs
    - tests/eval-fixtures/refined-calls/direct-vs-refined/expected.polint-eval.toml
    - tests/eval-fixtures/type-value-alias/extension-precision/expected.polint-eval.toml
  </read_first>
  <action>Add a `DataFlow` fixture area and observation support for data-flow facts. Create fixtures covering: local param-local-return flow; direct-call argument/return flow; summary-projected TITO flow; trust-boundary source to model sink; sanitizer and barrier false-positive traps; missing summary as unknown/havoc; extension-added source/sink/additional-step with one rejected malformed model; cold/warm/no-cache deterministic output; and budget/path truncation invariants.</action>
  <verify>
    <automated>cargo test -p polint --lib eval_native_fixture_runner_data_flow_fixture_passes --locked</automated>
    <automated>cargo test -p polint --lib eval_data_flow_manifests_cover_required_taxonomy --locked</automated>
  </verify>
  <acceptance_criteria>
    - At least one fixture asserts `DataFlowEdge` facts with producer `polint.data_flow`.
    - At least one fixture asserts source, sink, sanitizer, barrier, unknown/havoc, extension-added, rejected model, and budget/path truncation invariants.
    - Cold/warm/no-cache fixture output hashes match.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Add public no-leak and final compatibility proof</name>
  <files>crates/polint/src/eval/fixtures.rs, tests/eval-fixtures/data-flow/, docs/facts/</files>
  <read_first>
    - docs/API-VISIBILITY-PLAN.md
    - AGENTS.md
    - crates/polint/src/sdk/facts.rs
    - crates/polint/src/runner/mod.rs
    - crates/polint/src/cli
  </read_first>
  <action>Add tests proving normal `polint check --format json`, CLI help, SDK exports, runner surface, README, and `docs/facts` do not expose private data-flow provider ids, model schemas, internal query JSON, or newly meaningful public `DataFlow<'_>` semantics. Keep existing unsupported public capability behavior unless Phase 41 later promotes it intentionally.</action>
  <verify>
    <automated>cargo test -p polint --lib data_flow_public_no_leak --locked</automated>
    <automated>cargo test --workspace --locked</automated>
    <automated>cargo clippy --workspace --all-targets --locked -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - Public no-leak tests fail if `polint.data_flow` appears in normal CLI JSON/help, SDK exports, runner docs, README, or `docs/facts`.
    - Existing public rule-author workflows remain compatible.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internal debug/eval -> public output | Test-facing internal data-flow details must not leak into public CLI or SDK contracts. |
| extension models -> data-flow facts | Rejected or heuristic model facts must stay distinguishable from native or validated facts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T38-13 | Information Disclosure | public output | Mitigated | Add no-leak tests across CLI JSON/help, SDK, runner, README, and docs/facts. |
| T38-14 | Tampering | model/eval rows | Mitigated | Validate references, precision/status combinations, and rejected model visibility. |
| T38-15 | Denial of Service | fixture path search | Mitigated | Assert deterministic budget behavior and truncation reporting. |
</threat_model>

<verification>
- `cargo fmt --all --check`
- `cargo test -p polint --lib analysis::data_flow --locked`
- `cargo test -p polint --lib eval_native_fixture_runner_data_flow_fixture_passes --locked`
- `cargo test -p polint --lib eval_data_flow_manifests_cover_required_taxonomy --locked`
- `cargo test --workspace --locked`
- `cargo clippy --workspace --all-targets --locked -- -D warnings`
</verification>

<success_criteria>
Phase 38 data-flow facts are validated, observable in internal debug/eval fixtures, deterministic under cache/budget conditions, and absent from public SDK/CLI/docs surfaces.
</success_criteria>
