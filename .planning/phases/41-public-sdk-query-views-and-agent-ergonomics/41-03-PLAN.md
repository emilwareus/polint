---
phase: 41-public-sdk-query-views-and-agent-ergonomics
plan: 03
title: Inspect Test And New Rule Agent Contracts
type: execute
wave: 2
depends_on: [41-01]
files_modified:
  - crates/polint/src/rule_manifest.rs
  - crates/polint/src/rule_test.rs
  - crates/polint/src/cli/mod.rs
  - crates/polint/tests/cli.rs
  - docs/schemas/polint-rule-inspect-v1.json
  - docs/schemas/polint-test-report-v1.json
  - docs/CONSUMER-SETUP.md
autonomous: true
requirements: [SAE-PROM-02]
must_haves:
  truths:
    - "D-10/D-11: stable public command JSON includes schema/version/scope/limits/status and deterministic ordering."
    - "D-14/D-15/D-16/D-18: agent ergonomics preserve artifact boundaries; generated rules/examples keep `polint::sdk::prelude::*`, `#[polint::rule]`, derived capabilities, and `polint::runner::run_cli`; `RuleCtx` stays narrow."
  artifacts:
    - path: "crates/polint/src/rule_manifest.rs"
      provides: "Stable inspect rule JSON metadata for agent debugging"
    - path: "crates/polint/src/rule_test.rs"
      provides: "Stable rule fixture report contract"
---

<objective>
Stabilize and harden the existing `polint inspect rule`, `polint test`, and `polint new-rule` loop so agents receive deterministic metadata and generated rules include useful testable artifacts.
</objective>

<execution_context>
@.planning/phases/41-public-sdk-query-views-and-agent-ergonomics/41-CONTEXT.md
@research/agent-rule-authoring/RECOMMENDED_IMPLEMENTATION.md
@.claude/skills/polint/SKILL.md
@.agents/skills/rust-best-practices/SKILL.md
@AGENTS.md
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Stabilize inspect rule JSON fields</name>
  <files>crates/polint/src/rule_manifest.rs, crates/polint/tests/cli.rs, docs/schemas/polint-rule-inspect-v1.json</files>
  <read_first>
    - crates/polint/src/rule_manifest.rs
    - crates/polint/src/cli/mod.rs
    - crates/polint/tests/cli.rs
    - docs/schemas/polint-rule-inspect-v1.json
  </read_first>
  <action>Audit `InspectRuleReport` and `RuleManifestWire` against `docs/schemas/polint-rule-inspect-v1.json`. Add stable fields only if they are already available without new rule semantics: manifest schema version, rule id, host path, severity, SDK version, fact views, derived capabilities, support status, configured common option fields, custom settings, and optional stability/precision/limitations values if the macro/manifest already stores them. Keep internal schema names out of public JSON unless documented.</action>
  <verify>
    <automated>cargo test -p polint --test cli inspect_rule_json_matches_schema_v1 --locked</automated>
  </verify>
  <acceptance_criteria>
    - `polint inspect rule --format json` validates against `docs/schemas/polint-rule-inspect-v1.json` in a temp repo.
    - JSON rows are sorted by rule id and host path.
    - Unsupported capability support rows include status, reason, hint, and docs path when present.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stabilize polint test JSON and fixture semantics</name>
  <files>crates/polint/src/rule_test.rs, crates/polint/tests/cli.rs, docs/schemas/polint-test-report-v1.json, docs/CONSUMER-SETUP.md</files>
  <read_first>
    - crates/polint/src/rule_test.rs
    - crates/polint/tests/cli.rs
    - docs/schemas/polint-test-report-v1.json
    - docs/CONSUMER-SETUP.md
  </read_first>
  <action>Audit `RuleTestReport` JSON for schema/version/tool/summary/cases/status/expected_diagnostics/observed_diagnostics/failures. Add deterministic ordering tests and document fixture semantics for `polint-test.toml`, `paths`, `expect.diagnostic`, `--rule`, `--case`, `--no-cache`, and `--keep-temp`. Do not add unrelated assertion languages unless fully tested.</action>
  <verify>
    <automated>cargo test -p polint --test cli polint_test_json_matches_schema_v1 --locked</automated>
  </verify>
  <acceptance_criteria>
    - `polint test --format json` validates against `docs/schemas/polint-test-report-v1.json` in a temp repo.
    - Case ordering is deterministic by rule and case name.
    - Docs explain positive/negative fixtures, expected diagnostics, and current runner limits.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make generated rule scaffolds agent-ready</name>
  <files>crates/polint/src/cli/mod.rs, crates/polint/tests/cli.rs, docs/CONSUMER-SETUP.md</files>
  <read_first>
    - crates/polint/src/cli/mod.rs
    - crates/polint/tests/cli.rs
    - .claude/skills/polint/SKILL.md
  </read_first>
  <action>Update `polint new-rule` scaffolding so every generated rule includes a rule module, registration in `.polint/rules/src/main.rs`, one positive fixture, one negative fixture, `polint-test.toml` files that run successfully out of the box, and a short rule README or limitation note if the existing scaffold location supports it. Per D-15 and D-16, keep generated Rust using only `polint::sdk::prelude::*`, `#[polint::rule]`, derived capabilities, and `polint::runner::run_cli`; do not add manual `impl Rule`, handwritten capabilities, internal imports, or broad `RuleCtx` fact access.</action>
  <verify>
    <automated>cargo test -p polint --test cli new_rule_generates_positive_and_negative_agent_fixtures --locked</automated>
    <automated>cargo test -p polint --test cli new_rule_generates_fixture_that_inspect_and_test_can_run --locked</automated>
  </verify>
  <acceptance_criteria>
    - `polint new-rule ts no-raw-colors` creates positive and negative fixture cases that `polint test --format json` reports as passed.
    - Generated rule source contains `use polint::sdk::prelude::*;` and no `impl Rule`, `Capabilities::new`, `polint::core`, `analysis_kernel`, `go::`, or `ts::` imports.
    - Generated docs or comments mention precision/limitations without claiming unsupported advanced facts.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local rule host -> inspect JSON | Rule metadata from repo-local code must be deterministic and schema-bound. |
| scaffold generation -> filesystem | New-rule generation must not overwrite or write outside `.polint/`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T41-05 | Tampering | rule scaffold writes | Mitigated | Reuse safe name validation and overwrite tests; add fixture path assertions. |
| T41-06 | Repudiation | inspect/test JSON | Mitigated | Validate JSON against checked-in schemas with deterministic ordering tests. |
</threat_model>

<verification>
- `cargo test -p polint --test cli inspect_rule_json_matches_schema_v1 --locked`
- `cargo test -p polint --test cli polint_test_json_matches_schema_v1 --locked`
- `cargo test -p polint --test cli new_rule_generates_positive_and_negative_agent_fixtures --locked`
- `cargo test -p polint --test cli new_rule_generates_fixture_that_inspect_and_test_can_run --locked`
</verification>

<success_criteria>
The existing inspect/test/new-rule loop is stable enough for agent workflows: versioned JSON, deterministic reports, and generated fixtures that pass without internal imports.
</success_criteria>
