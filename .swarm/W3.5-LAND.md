# W3.5 land note

- Worktree: `/Users/emilwareus/conductor/workspaces/exlint/swarm-W3.5`
- Branch: `swarm/W3.5`
- Deleted the standalone `analysis/calls/ts_value_flows.rs` frontend and its production call-provider dispatch.
- Deleted the sibling `analysis/calls/js_points_to` Oxc reparse pipeline. Base call facts now come only from MIR call-site extraction and direct binding; indirect TS/JS edges flow through the existing semantic graph, shared solver, and refined-call projection.
- No compatibility adapter or alternate value-flow dispatch remains.
- Public SDK surfaces and golden files are unchanged.

## Gates

- `test ! -f crates/polint/src/analysis/calls/ts_value_flows.rs`: pass.
- `rg -n 'ts_value_flows' crates/polint/src --glob '*.rs'`: pass; no matches.
- `cargo fmt --all -- --check`: pass.
- `cargo clippy -p polint --all-targets --all-features --locked -- -D warnings`: pass.
- `cargo test -p polint --test public_surface_leak --locked`: pass.
- `cargo test -p polint --test golden --locked`: pass; no golden regeneration.
