# W3.4 land note

- Worktree: `/Users/emilwareus/conductor/workspaces/exlint/swarm-W3.4`
- Branch: `swarm/W3.4`
- Rebased onto integration commit `24db5192`.
- Replaced the Go and TypeScript/JavaScript CFG lowerers with one language-neutral `lower_cfg` path over stored MIR bodies, blocks, statements, operations, and terminators.
- The shared lowerer derives branches, switches, returns, exceptional transfers, calls, suspensions, and unreachable control directly from MIR terminators without language dispatch.
- Unsupported MIR facts retain their conservative CFG boundaries through one shared construct mapping; CFG never reads or searches source text.
- Deleted `analysis/cfg/lower_go.rs`, `analysis/cfg/lower_ts.rs`, the provider's per-language merge path, and its dense-ID offset repair.
- Added a direct regression test proving the shared lowerer accepts a Go MIR body without language dispatch.
- Public SDK surfaces and golden files are unchanged.

## Gates

- Deleted-file assertions: pass.
- `rg -n 'source_text\(|contains_token' crates/polint/src/analysis/cfg/`: no matches.
- `cargo fmt --all -- --check`: pass.
- `cargo clippy -p polint --all-targets --all-features --locked -- -D warnings`: pass.
- `cargo test -p polint --test public_surface_leak --locked`: pass (8 tests).
- `cargo test -p polint --lib analysis::cfg --locked`: pass (31 tests).
- `cargo test -p polint --test golden --locked`: pass (8 tests).
