# W3.2 land note

- Worktree: `/Users/emilwareus/conductor/workspaces/exlint/swarm-W3.2`
- Branch: `swarm/W3.2`
- Added private `Throw`, `Call { normal, unwind }`, and `Suspend` MIR terminators with await, yield, channel receive, and channel send kinds.
- Go and TS/JS lowerers emit effectful terminators on the product path. Go panic calls carry unwind control; TS throw and suspend constructs no longer emit unsupported control placeholders.
- CFG lowering consumes the new terminators directly for throw, panic unwind, await, yield, and channel suspension edges.
- Deleted CFG construct-string recovery for throw, await, yield, async rejection, and Go panic call shapes. No dual effect-control path remains.
- Production wiring tests lower source through MIR storage and CFG consumption for both languages.
- Public SDK surfaces and golden files are unchanged.

## Gates

- Deleted-reader search: pass; no `construct == "throw"`, `Some("await")`, or `Some("yield")` arms remain under `analysis/cfg`.
- `cargo fmt --all -- --check`: pass.
- `cargo clippy -p polint --all-targets --all-features --locked -- -D warnings`: pass.
- `cargo test -p polint --test public_surface_leak --locked`: pass.
- `cargo test -p polint --lib analysis::mir --locked`: pass (32 tests).
- `cargo test -p polint --lib analysis::cfg --locked`: pass (36 tests).
- `cargo test -p polint --test golden --locked`: pass (8 tests).
