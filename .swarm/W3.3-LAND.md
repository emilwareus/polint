# W3.3 land note

- Worktree: `/Users/emilwareus/conductor/workspaces/exlint/swarm-W3.3`
- Branch: `swarm/W3.3`
- Rebases cleanly on integration commit `392d50f9`, preserving W3.2 `Throw`, `Call`, and `Suspend` terminators alongside the new value vocabulary.
- Added private `BinOp`, `Aggregate`, and `Closure { body, captures }` MIR values and migrated Go and TS/JS lowering to construct them instead of opaque `Unknown` evidence.
- Production Go and TS/JS lowerers resolve closure bodies and captured names, intern each capture as a place, and construct `MirValue::Closure { body, captures }`.
- Added `PlaceTypeFact` as the typed successor associated with each `PlaceFact`. Both production lowerers populate it through typed local and temporary insertion using the existing `TypeShape` lattice; `AnalysisDb::mir_place_types` exposes it to internal consumers without reparsing.
- Merge, normalization, validation, type/value derivation, data-flow, domain transfer, call extraction, and stable-key/debug formatting consume the new value and place-type structures directly.
- Structured aggregate lowering retains computed-key call traversal and explicit spread precision evidence. No replaced `Unknown` representation or parallel compatibility path remains.
- Public SDK surfaces and golden files are unchanged.

## Gates

- `cargo fmt --all -- --check`: pass.
- `cargo clippy -p polint --all-targets --all-features --locked -- -D warnings`: pass.
- `cargo test -p polint --test public_surface_leak --locked`: pass (8 tests).
- `cargo test -p polint --lib analysis::mir --locked`: pass (34 tests).
- `cargo test -p polint --test golden --locked`: pass (8 tests).
