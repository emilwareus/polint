# W3.1 land note

- Worktree: `/Users/emilwareus/conductor/workspaces/exlint/swarm-W3.1`
- Branch: `swarm/W3.1`
- Added private block-structured MIR rows, normalized storage, deterministic merging/digests, and the core `Goto`, targeted `Branch`, `Switch`, valued `Return`, `Unreachable`, and `Unsupported` terminators.
- Go and TS/JS MIR lowerers now emit blocks, statement rows, and terminators. CFG lowering reads only those terminators for ordinary branches, loops, switches, returns, and unreachable control.
- Deleted CFG source-text branch classification (`branch_shape`, `operation_evidence`, `contains_token`, and source slicing). There is no callable flat-operation CFG edge path.
- Product wiring: `derive_semantic_mir_with_cache_stats` stores emitted control-flow rows; `derive_cfg_with_cache_stats` reaches the Go and TS CFG lowerers that consume them.
- Wiring tests: `go_cfg_lowers_edges_from_production_mir_terminators` and `ts_cfg_lowers_edges_from_production_mir_terminators` fail if the production MIR rows are not emitted, stored, or consumed.
- Deliberately left alone: public SDK surfaces, W3.2 effect terminators and unsupported-shape semantics, W3.3 operation/type work, W3.4 shared CFG consolidation, M4, and all golden files.

## Gates

- Production `MirBlock` / `MirTerminator` constructor searches: pass.
- Deleted-reader search: pass; no `branch_shape`, `contains_token`, `operation_evidence`, or CFG source slicing remains in the language CFG lowerers.
- `cargo fmt --all -- --check`: pass.
- `cargo clippy -p polint --all-targets --all-features --locked -- -D warnings`: pass.
- `cargo test -p polint --test public_surface_leak --locked`: pass (8 tests).
- `cargo test -p polint --lib analysis::mir --locked`: pass (32 tests).
- `cargo test -p polint --lib analysis::cfg --locked`: pass (36 tests).
- `cargo test -p polint --test golden --locked`: pass (8 tests) on retry. The first run had no output diff and failed only the timing budget for `examples/go-sensitive-writes/json` (477 ms measured vs 358 ms baseline); the immediate isolated retry passed without changing goldens.
